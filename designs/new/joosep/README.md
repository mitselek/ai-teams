# MOVED (2026-08-31) -> `designs/deployed/joosep/`

Shipped: the container is live on RC `100.96.54.170:2231`, handed over to Joosep Madar, so the package
follows the tree's own `new/` -> `deployed/` rule. The whole package moved -- runbook, README,
`HANDOVER-JOOSEP.md`, registry rows, Dockerfile, compose, entrypoint, `joosep.sh`, `Connect-Joosep.ps1`
and `teams/paunvere/`. Nothing was left behind at this path.

Stub kept through at least **2026-10-01**; remove when a repo-wide grep shows zero inbound references.
As of the move there are ~28 inbound references outside this package -- in the wiki (Callimachus's),
the operations log, and three agent scratchpads -- **none of them mine to repoint**, which is exactly
why this stub exists. Repointing suffices only inside your own write scope.

(*FR:Brunel*)
