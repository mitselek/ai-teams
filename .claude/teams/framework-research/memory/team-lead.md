# Team-Lead Scratchpad (*FR:team-lead*)

## NEXT SESSION

**PO DIRECTIVE 2026-04-16:** Deploy xireactor-brilliant (codename **VJS2KB**) on RC. Pragmatic approach — deploy first, design later. PO experimented at home on personal PC with promising results. No commitment needed up front; just deploy, give MCP access to FR + apex-research, start populating the KB, and let real experience inform further decisions. The 4 design docs from the 2026-04-15 night session (governance, host arch, protocol, migration assessment) become evaluation frameworks for AFTER deployment, not prerequisites.

**Spawn plan:**
1. **Cal** — wiki candidate evaluation queue (13 candidates from prior session, including SEVERE integration-not-relay meta-pattern at n=6+); also monitors KB population quality as it happens
2. **Brunel** — deploy VJS2KB on RC (`100.96.54.170`). His v0.6.3 host architecture doc is the deployment guide. 3-service stack (db + api + audit_sidecar), ~500 MB, Tailscale-bound. Give MCP stdio access to FR + apex-research teams.

**What the deployment itself resolves (no Finn source survey needed):**
- Q1 per-zone staging enablement — empirical, not speculative
- Q2 tenant-scoped session_init — empirical, not speculative  
- §10 §1.2-compliance — moot if we use xireactor as-shipped without per-tenant flags
- Cal's migration assessment — validated or falsified by real experience

**Prior session context (2026-04-15 triple session):** **Afternoon:** ruth-team genesis — Finn (Jira + Confluence background research on principal; apex-research V2 dependency verification via Stage A spec-read + Stage B tmux-direct to Schliemann; EN→EE translation of Ruth-relay with Q2 revision pass), Callimachus (Protocol B prior-art query — confirmed 3 of 5 design angles are NOT documented = highest-yield single deployment for framework knowledge since apex-research), Brunel (ruth-team container design v1.0 at `docs/ruth-team-container-design-2026-04-15.md` — accepted, team-lead answers inlined, build blocked on Monte/Herald). **Evening:** path (c) Volta Fix session shipped — F1 (`88ced06` restore-filter.jq extraction + `\s` portability fix) and F2 (`5eb7f67` "memory" → "auto-memory" prose rename + 8 previously-untracked WIP files tracked), then Cal wiki-side (`aef1c79` Pass 1 prose rename in her gotcha + `755a144` two new wiki entries: #40 `structural-match-beats-free-string-for-protocol-filters` pattern, #41 `jq-file-vs-arg-escape-divergence` gotcha). Evening also received first ruth partial response — one clarifier question ("what is OKR?"), operator relayed a plain EN/EE definition; Q1/Q2/Q3 answers still pending. **Late-evening:** xireactor-brilliant survey chore executed — Finn delivered one-page structural digest (`9f51ca5` in `docs/`), PO approved Protocol A intake on veins (a)+(b), Cal filed two wiki entries (`4fef9d8`: #42 `governance-staging-for-agent-writes` + #43 `bootstrap-preamble-as-in-band-signal-channel`). PO then asked strategic shared-KB question → filed as **mitselek/ai-teams#59** "xireactor-brilliant as shared-KB substrate — pilot evaluation" and bundled with #57+#58 as the three-member **ecosystem-integration** future session. **Wiki now 43 entries.** External artifacts: **mitselek/ai-teams#57** Teams integration research, **#58** Anthropic `/routines` research, **#59** xireactor shared-KB pilot evaluation — all three parked as ecosystem-integration bundle.

[DECISION 2026-04-15 close] **Next session entry path depends on what arrived while team was down:**
(a) **Ruth responded via Teams** — operator relays her answers to Q1/Q2/Q3. Next moves:
    - If Q1=yes → wake Celes for roster draft (use `sensitive/ruth-turk-background-2026-04-15.md` + her Q2/Q3 answers as design inputs); wake Volta for §6.5 observability addendum to Brunel's doc (dual-track instrumentation layer); potentially wake Monte + Herald in parallel to unblock Brunel's build step (see §4.3 / §5.3 open questions).
    - If Q1=no → thank Ruth via operator relay; file learnings as Protocol A candidates; session close early.
    - If partial/hedged → operator decides next relay round.
(b) **Operator shared Ruth's prior Teams history** — read `sensitive/ruth-teams-history-*.md` (if file exists); extract structural signals (cadence, topic distribution, who initiates) NOT direct quotes; feed into Celes's roster brief; flag any sensitivity concerns before any Protocol A submission.
(c) **No Ruth response yet** — do NOT wake agents speculatively. Use bandwidth for: (i) parked Phase 2 Jira/GitFlow if PO has Kuzmin/Sildnik answers on dev-toolkit#43, (ii) **ecosystem-integration research session** tackling issues #57 + #58 + #59 together (three-member bundle: Teams + `/routines` + xireactor-shared-KB pilot evaluation — requires full team: Brunel/Monte/Herald/Celes/Cal), (iii) **substrate-invariant-mismatch n=3 formal pattern draft** (Cal leads the shape, team-lead reviews — light session, Cal-only spawn), (iv) persist-coverage Design session D1-D7 (full-team, blocked on nothing structural — was shipped without), (v) xireactor-brilliant itself for a deeper vein (e.g., pilot the in-band-preamble pattern in our own scratchpad-read-order — cheap experiment, Cal + Volta). *Volta Fix session (F1+F2) and xireactor Protocol A intake (a+b) are both DONE — removed from fallback list.*

**CRITICAL — sensitivity boundary in force for all ruth-team work:** `.gitignore` at repo root excludes `.claude/teams/*/sensitive/`. Ruth research data and any Teams history live in `sensitive/`, NEVER committed. Patterns can flow out via Protocol A but generalize heavily — no direct quotes, no specific Jira tickets, Confluence titles, budget figures, or colleague names. Codename `ruth-team` is acceptable inside framework-research but NEVER disclose subject identity outside the team.

[DECISION 2026-04-15 close] **Prior session directive (Phase 2 Jira/GitFlow resume / persist-coverage Fix/Design / fresh task) still holds as the fallback path when ruth-team thread is idle.** Route by topic as before.

[DECISION 2026-04-15] **E-deployment pattern (Cloudflare Tunnel / hello-world-container) adopted as future target for all team deployments, including migration of existing teams.** Near-term Ruth-team deployment goes to (B) existing dev server `100.96.54.170` (co-located with apex-research) — chosen for speed + structural simplicity of Ruth↔apex-research bridge. Migration of existing teams (apex-research, framework-research runtime, bioforge-dev, screenwerk, raamatukoi-dev, comms-dev) to E-pattern is explicit future work — no dates, no assignments yet. Ruth-team container design MUST be portable (minimize path-anchored config, prefer declarative over imperative infra, use Brunel's existing patterns from apex-research + hello-world-container PoC) so migration B→E is a cheap move later. Surface to Cal as `decisions/e-deployment-future-target.md` after Brunel ships the Ruth container, once pattern language is crisp.

[DECISION 2026-04-15] **Brunel's ruth-team container design v0.1 accepted as v1.0.** Doc at `docs/ruth-team-container-design-2026-04-15.md`. Team-lead answers inlined to §7: (a) bridge repo = subdir of `mitselek/ai-teams` with Dockerfile + compose + entrypoint ALSO preserved in-repo under a designated path (container infra = first-class tracked artifact, not scattered on operator workstation); (b) HR_NOTIFICATION_EMAIL carryover = not applicable, closed. Remaining open questions: Monte (4 on governance §4.3), Herald (3+1 on bridge protocol §5.3), Celes (2 non-blocking on roster layout §7). Next build steps (Dockerfile + compose) blocked on Monte §4.3 Q1-Q2 and Herald §5.3 Q1.

[DECISION 2026-04-15 eve] **Path (a) Ruth-thread is NOW LIVE — partial state.** Ruth received the Teams relay and responded with one clarifier ("what is OKR?"). Operator relayed a plain-language EN/EE answer (OKR = Objectives and Key Results, just a convention, asked because her Jira has UpRaise/OKR provisioned, either answer fine). Q1 (opt-in), Q2, Q3 answers still pending. Do NOT wake Celes/Volta speculatively next session — only after Q1 answer arrives. If Q1=yes, proceed per original (a) plan (Celes roster + Volta §6.5 observability addendum + Monte/Herald parallel).

[DECISION 2026-04-15 eve] **Volta Fix session (F1 + F2) shipped, pushed at session close.** 4 commits on main (`88ced06`, `5eb7f67`, `aef1c79`, `755a144`). Leaves persist-coverage Design session (D1-D7) parked for a future focused FR session with full team context.

[LEARNED 2026-04-15 eve] **Abstraction-layer signal from Ruth's "what is OKR?"** — she has UpRaise/OKR access provisioned but doesn't use the terminology. Confirms Finn's thin-Confluence-footprint read: her real work surface is meetings/Teams/MS Project, NOT goal-frameworks. **Design implication:** when Celes drafts the roster, agents meet her in concrete-deliverable register, NOT in OKR/KR/OBJ- language. Carry into Celes's brief when/if Q1=yes arrives.

[LEARNED 2026-04-15 eve] **Empirical filter testing beats authority-of-precedent.** uikit-dev's `1deb90e` looked like a reference to copy — they'd already extracted and fixed the filter. But their free-string pattern was actually defective (1 false positive on prose-about-shutdown). FR's "less polished" structural pattern was correct when tested against 23 real inboxes. Worth doing the empirical test before deferring to another team's implementation. Generalized as wiki pattern #40.

[DEFERRED 2026-04-15 eve] **Pass 2 filename rename** for `wiki/gotchas/persist-project-state-leaks-per-user-memory.md` → variant without "-memory". Cal inventoried 7 back-refs (index.md, dual-team-dir-ambiguity.md, 2 scratchpads, 2 inbox logs, 1 doc). Inventory in Cal's scratchpad. Ship as one coordinated batch in a future session when coordination cost is warranted — not a priority on its own.

[DEFERRED 2026-04-15 eve] **uikit-dev cross-team debt** — their `1deb90e` uses the defective free-string pattern. Tmux-direct relay to Aalto deferred; re-evaluate at next uikit-dev contact point. Aligns with prior Finn Section D deferral on 6 Aalto questions — bundle if/when that routing pass happens.

[DECISION 2026-04-15] **Ruth-team interaction channel near-term = SSH + tmux pane, same substrate as operator→apex-research.** Rationale: zero new infra (container has sshd + tmux), transparency-to-principal (Ruth sees exact agent messages not filtered/summarized view), observability layer gets tmux scrollback + inbox files for free, SSH is unblocked (no Teams/Graph procurement dependency). Teams/Graph integration parked as separate research task — GitHub issue filed on mitselek/ai-teams. Relatedness: `/routines` deferred item + Teams integration = both Anthropic-ecosystem + org integration questions; consider bundling into a single "ecosystem integration" research session.

[OBSERVABILITY GAP — ruth-team dual-track] Brunel's v1.0 is purely operational. User directive was "operational AND research probe" (dual-track). The git-mediated bridge in §5 incidentally produces audit logs, but there's no explicit instrumentation layer for framework-learning. Belongs to Volta (lifecycle persistence) sitting on top of Brunel's substrate. Need §6.5 addendum or standalone doc once Ruth's Q2/Q3 answers shape observability priorities (e.g. "weekly digest" = different telemetry surface than "live interaction"). Wake Volta after Ruth responds; do NOT wake early — her answer reshapes the surface.

[PARKED — Phase 2 Jira/GitFlow classification] Held pending PO reconciliation via dev-toolkit#43. Candidates already listed in `docs/jira-gitflow-assessment-2026-04-14.md` Observations section (3 pattern, 1 gotcha, 2 decision, plus "two authoritative docs in one space silently diverge" meta-gotcha). Wake Finn with Q1/Q2/Q4 subset when answers arrive.

[DECISION 2026-04-15 late-eve] **xireactor-brilliant survey + Protocol A intake COMPLETE.** Finn delivered one-page structural digest under budget (7 file reads, 10 tool uses, no Explore agent). Source: Postgres-backed multi-tenant institutional KB with dual-transport MCP (stdio + HTTP+OAuth), 4-tier governance-staging pipeline for agent writes, RLS-enforced isolation, session_init preamble for in-band governance signals. Digest committed as `9f51ca5` at `docs/xireactor-brilliant-digest-2026-04-15.md`. Cal filed wiki entries #42 `patterns/governance-staging-for-agent-writes.md` + #43 `patterns/bootstrap-preamble-as-in-band-signal-channel.md` as `4fef9d8`. **Wiki 41 → 43.** Five candidate veins ranked a-e in digest §4; user approved intake on (a)+(b) only, (c)+(d)+(e) parked (d) rolled into substrate-invariant-mismatch n=3 evidence.

[DECISION 2026-04-15 late-eve] **xireactor-as-shared-KB-substrate parked as mitselek/ai-teams#59.** PO raised the strategic question post-intake: "could this automatically open a shared library for all our teams?" Shape fits (multi-tenant Postgres RLS, MCP stdio transport, staging pipeline handles concurrent agent writes, session-init surfaces cross-team review signals at every startup) but nothing is automatic — three Docker services need a host, per-team tenant provisioning, MCP config rollout, markdown→DB substrate migration with full librarian-discipline translation cost. Uneven value: FR has Cal (human-gate governance already solved), staging-tier value is greatest for teams WITHOUT a librarian. Bundled with #57+#58 as three-member **ecosystem-integration** future session requiring full team (Brunel containerization, Monte governance, Herald cross-tenant protocol, Celes roster implications, Cal migration + discipline translation). Counter-option: formalize Finn-style quarterly cross-team harvest passes (same information flow minus live-query primitive, markdown preserved). Pilot-eval proposal: 2 tenants (FR + apex-research) for 1 month of real cross-team traffic to prove-or-disprove the shared-library benefit. Fits the E-deployment pattern already adopted 2026-04-15 for all team deployment migration. Do NOT spawn Brunel/Monte speculatively — wait for ecosystem-integration session trigger.

[LEARNED 2026-04-15 late-eve] **"Implicit decision-by-inaction" is a filing frame worth reusing.** Cal's #42 captured the non-obvious cost of FR's Cal-direct-accept model: we have no structural record of rejected/bounced submissions, only accepted ones appear in wiki history. Naming the implicit decision made the trade-off visible — framing choice, not architectural choice. Reusable: whenever we "just don't do X" and call it a non-decision, there's a hidden trade-off worth naming. Candidate for future pattern entry if we hit n=2.

[LEARNED 2026-04-15 late-eve] **Confidence-floor + fail-closed escalation is already latent in FR practice in 3 places.** Cal spotted: dedup "file separately when in doubt", classification hard-cases "file both cross-ref", URGENT-KNOWLEDGE routing. The discipline exists — we lack the VOCABULARY to recognize it as one named shape. Xireactor's AI reviewer (confidence < 0.7 auto-escalates, error paths return escalate, never auto-approves on ambiguity) gave us the words for what we already do. Filed as wiki #42.

[LEARNED 2026-04-15 late-eve] **Rejecting team-lead suggestions with a stated reason is a feature, not friction.** Cal rejected both my candidate names for wiki #43 with principled reasons (channel vs. comparison, payload-class-generic vs. FR-specific) and chose `bootstrap-preamble-as-in-band-signal-channel` instead. That's the exact specialist-authority shape I should protect — if Cal deferred to team-lead suggestions reflexively, her wiki would inherit my blind spots. Write it into common-prompt at some point as an explicit specialist-authority norm? Queue for consideration.

[WIP UPDATE 2026-04-15 late-eve] **substrate-invariant-mismatch candidate now n=3.** Previously held at n=2 (dual-team-dir-ambiguity + persist-project-state-leaks-per-user-memory) supporting n=3 (Volta's Flag 1 on v0.3 skill-integration patches). Cal confirmed n=3 this session: Finn's digest §4(d) on xireactor — "derived-data read at render-time must be written on write-path" (xireactor's `entry_links` table re-derived on every POST/PUT, joined at render time). Cal's three data points for the formal draft: (1) xireactor link-index-on-write (new), (2) dual-team-dir-ambiguity, (3) protocol-shapes-are-typed-contracts. Three distinct substrate-layer pairs — the strongest n=3 we've had on this candidate. **Decision:** Cal drafts the formal pattern entry next framework-research session; she leads the shape, team-lead reviews. Not filing this session.

[NEXT-SESSION-CHORE] **Prune Finn's scratchpad** at spawn time — `memory/finn.md` is ~190 lines, target is 100. Session-8 uikit-dev harvest entries (around CHECKPOINT 2026-04-14 + the 30-line detail below) can collapse to a 3-line pointer to `docs/uikit-dev-harvest-2026-04-14.md`. Finn flagged this himself at shutdown — do it during the spawn brief, not as a separate task.

[NEXT-SESSION-CHORE] **Fix stale port 2224 in Brunel's ruth-team container doc.** `docs/ruth-team-container-design-2026-04-15.md` suggests port 2224 on RC dev server 100.96.54.170 — but `deployments.md` already has entu-research:2224. Brunel caught this 2026-04-15 late-eve while reading for xireactor pilot host architecture. Trivial doc edit; team-lead can't do it (tool restriction — memory/ and roster only). Assign to Brunel next time he's spawned on a ruth-team task. 1-line fix, don't lose it.

## XIREACTOR PILOT SESSION — DECISIONS LANDING 2026-04-15 late-eve

[SESSION 2026-04-15 late-eve] **Topic: mitselek/ai-teams#59 xireactor-as-shared-KB pilot evaluation. Pilot pair: FR + apex-research (PO-confirmed apex as pilot project).** Spawned 4 agents in parallel: Brunel (host), Monte (governance), Herald (protocol), Cal (migration discipline). Did NOT spawn Finn/Celes/Volta this session.

[DECISION 2026-04-15 late-eve] **Strategic asymmetric use — accepted as pilot thesis.** Monte's framing: Protocol A governs intra-tenant writes (cheap gate, frequent), xireactor staging pipeline governs cross-tenant writes (expensive gate, rare). Doubled-coverage collapses because each layer governs a different boundary. Committed in `docs/xireactor-pilot-governance-2026-04-15.md`.

[DECISION 2026-04-15 late-eve] **Four-zone (+shared-write) tenancy model accepted.** Five state values: FR-private, apex-private, FR-owned-shared-read, apex-owned-shared-read, shared-write. Default isolation; shared-read is opt-in per-entry; shared-write rare and heavily governed. `co-owned` (shared-write) is the ONLY state the staging layer governs. Ref: Monte §3.2.1.

[DECISION 2026-04-15 late-eve] **Three-valued ownership attribute as first-class column.** `owned-by-FR` / `owned-by-apex` / `co-owned`. NOT a two-state model with "both bits set" promotion. Anti-race invariant: `owned_by` never auto-flips; every path into `co-owned` is a deliberate ratified act. Ref: Monte §3.2.1.

[DECISION 2026-04-15 late-eve] **Simultaneous-discovery protocol accepted (Monte §3.6).** When both librarians independently discover the same cross-cutting pattern: (1) neither intra-tenant entry auto-promotes, (2) fresh `co-owned` entry filed in shared-write with both originals as sources, (3) Tier 3 routing + bootstrap-preamble cross-review with no proposer/reviewer power asymmetry, (4) both originals get `superseded-by` refs but are NOT deleted, (5) **durable disagreement is a valid terminal state** — forcing consensus is worse than letting two teams disagree. No "loser," no race. Filing-first librarian has no advantage over filing-second. Worth Cal wiki promotion.

[DECISION 2026-04-15 late-eve] **Ownership-transfer ritual: 2 librarians + 2 team-leads countersign; xireactor operator (not a librarian) flips the field.** 4-signature ritual. Separation of decision from execution — neither side can unilaterally or accidentally change ownership. Expensive on purpose; ownership is the primitive all other authority rules depend on. PO to confirm appropriateness at session close.

[DECISION 2026-04-15 late-eve] **Three-check authority-drift defense (layered).** (a) ownership-transfer ritual (deliberate grabs), (b) shared-read sunset review (accidental accumulation), (c) audit-independent tenant boundary report (structural blind spots, auditor is neither tenant, inherits `audit-independence-architecture`). Multi-mode failure modes need multi-mechanism defenses — each check covers a different failure mode; any one alone is insufficient.

[DECISION 2026-04-15 late-eve] **Cross-review rides the wiki #43 bootstrap-preamble channel.** Xireactor `session_init` surfaces pending cross-reviews to the other tenant's librarian at their wake-time read. New payload class (governance, not continuity) on the same mechanism. Generalizes #43 from intra-team continuity to cross-tenant governance state.

[DECISION 2026-04-15 late-eve] **Pilot sequencing (team-lead call):** per-zone staging tier-skip + tenant-scoped session_init governance payloads are **PRECONDITIONS** (mechanical basis of the asymmetric model, pilot thesis collapses without them). Ownership-transfer wire format is a **PILOT DELIVERABLE** (convention layered on xireactor's existing comments is acceptable, not a precondition).

[DECISION 2026-04-15 late-eve] **Topology convergence: single instance, single DB, row-level RLS via `app.org_id` with three-state ownership as first-class column.** Brunel argued row-level-only in v0.1 over Monte's initial schema-per-tenant call. Resolution: Monte's own §3.2.1 sharpening (three ownership states as a first-class attribute) is structurally identical to row-level-only — they're saying the same thing at different layers. Brunel's argument and Monte's §3.2.1 converge on the same topology. Schema-per-tenant was overtaken by Monte's own later sharpening. Staging layer governs only `ownership = 'co-owned'` rows. Upstream xireactor is consumed at a pinned tag — we do NOT fork.

[DECISION 2026-04-15 late-eve] **Host placement: RC dev server 100.96.54.170, co-located with apex-research.** 3-service footprint is ~750 MB RAM idle / ≤2 GB disk / negligible CPU — rounding error on the same host that runs apex-research:2222, polyphony-dev:2223, entu-research:2224. Upstream stress test (178 ops/sec) is 100× expected sustained load. Port 2225+ allocation for xireactor-pilot. Dedicated infra would be premature optimization AND block the benefit-signal experiment.

[DECISION 2026-04-15 late-eve] **CF Tunnel reuse.** One new ingress rule `xireactor.dev.evr.ee → http://xireactor-mcp:8011` on existing cloudflared sidecar (tunnel ID `526a23d1-1f7f-472f-8df1-a9239bbe3fe4`). Zero new tunnels. Same hostname crosses B→E unchanged — critical for OAuth `iss` claim stability.

[DECISION 2026-04-15 late-eve — SUPERSEDED by Brunel v0.2 same session] ~~Pilot as natural experiment — Tier 3 OFF for FR, ON for apex-research (Brunel's §8 Q3). Strong endorse.~~ **RETRACTED**: team-lead endorsed Brunel's v0.1 Q3 recommendation without integrating Monte's asymmetric framing into the comparison. Under Monte's framing (which team-lead had already separately endorsed), Tier 3 fires ONLY on cross-tenant co-owned writes — FR intra writes go direct to Cal under ANY Tier 3 setting. The "FR Tier 3 off / apex Tier 3 on" comparison is between configurations that don't apply to the same code path; there is nothing to compare because the two settings act on different slices of the write surface. Brunel caught this when he folded Monte's framing into v0.2 and explicitly rejected his own v0.1 Q3 recommendation as "solving a problem that Monte's framing dissolves." Team-lead was endorsing a phantom experiment. Kept visible (not deleted) as a learning note: endorse specialist recommendations AFTER checking whether another specialist's framing has already dissolved the problem they solve.

[LEARNED 2026-04-15 late-eve] **Endorse specialist recommendations AFTER cross-checking that another specialist's framing hasn't already dissolved the problem.** Team-lead endorsed Brunel's v0.1 Tier 3 OFF/ON comparison as a "first-class pilot objective" while simultaneously endorsing Monte's asymmetric framing — without noticing the two were mutually incompatible. Brunel saw the incompatibility when he folded Monte's framing into v0.2 and rejected his own v0.1 idea. Brunel's self-rejection was correct; team-lead's endorsement was wrong. Pattern to apply next time: when two specialists' deliverables arrive in the same window, cross-check their frames BEFORE endorsing either. Specialist self-rejection on second-look is a feature, not a regression — the shape common-prompt is already protecting when it encourages specialist-authority.

[DECISION 2026-04-15 late-eve] **Cal's professional verdict: full xireactor migration is a net DOWNGRADE for a tenant with a librarian; cross-tenant-writes-only pilot is a GENUINE UPGRADE.** Load-bearing framing: *"filesystem affordances (grep, git blame, dir-as-contract, hand-authored cross-ref prose) are load-bearing for librarian discipline; free enforcement → prompt enforcement is a regression even when prompt enforcement works."* Xireactor's staging pipeline + Tier 3 AI reviewer (sonnet-4-6) is duplicative with Protocol A + Cal-direct-accept and operates at an opus[1m]-context handicap vs Cal; Tier 4 escalates back to Cal — wasted infrastructure for own-tenant writes. ONE asymmetric use earns its keep: cross-tenant writes. FR currently has NO channel to write into apex-research's wiki; xireactor staging with receiving-tenant librarian as Tier 4 escalation target gives us that capability with minimal infra. **Pilot recommendation: adopt xireactor for cross-tenant slice ONLY, not full substrate migration.** Cal's §6.4 pass/fail criteria: ≥2 cross-tenant submissions, ≤10% auto-promote error, ≤1 session escalation latency. Doc at `docs/xireactor-pilot-migration-assessment-2026-04-15.md`.

[CONVERGENCE 2026-04-15 late-eve] **Three independent convergences on the asymmetric shape from three different layers.** Brunel (host) converged on row-level-only topology + three-state ownership as first-class column. Monte (governance) converged on Protocol A = intra, staging = cross, co-owned = only state staging touches. Cal (discipline) converged on full migration is downgrade; cross-tenant slice is the only part worth adopting. Three specialists, three layers, same shape — strong structural signal the design is structurally correct.

[DECISION 2026-04-15 late-eve — REVISED by Herald self-correction same session] **Pilot-readiness status (Herald v1.2 honest re-read, landing on outcome (c)/(c)):**
- **Q1 per-zone staging enablement: OUTCOME (c) — digest silent.** Herald's v1.1 §A had claimed "SOFT config only via indirect `(source, role)` encoding" but caught himself on honest re-read. Line 35 of Finn's digest lists tier-assignment inputs as `(change_type, sensitivity, source, role)` — zone / tenant / org_id are NOT named. The indirect-encoding claim was plausible extrapolation, not digest fact. Needs source-code walkthrough of xireactor v0.2.0 tier-assignment layer during infrastructure stand-up to determine whether the encoding exists or whether it's an upstream enhancement. Routed to Brunel.
- **Q2 tenant-scoped session_init: OUTCOME (c) — digest silent.** Herald's v1.1 §A had claimed "MEDIUM — tenant-scoped by construction via RLS" but retracted same session. Line 27 ("Pending Tier 3+ items surface in the session_init preamble") doesn't say whether it's tenant-scoped. Line 29 (`SET LOCAL app.user_id/org_id/role/department per transaction`) establishes per-tenant transaction context exists but doesn't say session_init queries run inside that context. The RLS-construction claim was plausible inference, not digest fact. **HIGHEST-CONSEQUENCE OPEN ITEM** — silent cross-tenant leak here breaks the #43 bootstrap-preamble channel INVISIBLY (both librarians still see preambles, content is subtly wrong — exact failure mode Cal's #43 names). Needs same source-code walkthrough as Q1. Three outcomes: pass / partial / fail.
- **Q3 ownership-transfer wire format: DELIVERABLE (confirmed).** Zero code change. Comment_type tag convention, four typed interfaces (`OwnershipTransferProposal`, `-Accept`, `-Countersign`×2) as conventions on xireactor's existing comments table. Herald's v1.1 Flow C implemented this.
- **Net PO-brief story (honest version):** NOT "one medium + one soft + one deliverable" (that was based on evidence Herald retracted). Actual story: **"two outcome-(c) source-code walkthroughs needed during infrastructure stand-up + one deliverable."** Still friendlier than the original "two HARD preconditions requiring upstream contribution investment" — walkthroughs are pass/fail checks not feature requests — but not as friendly as Monte's v3 §7.2 suggested. PO gets the honest (c)/(c) framing.
- **Monte's v3 §7.2 classification is now based on retracted evidence** and needs a future-session rework pass. Not re-spawning Monte this session for it; his v4 capstone (principles + invariant + convergence history) is the session deliverable. Next-session hand-off.

[LEARNED 2026-04-15 late-eve] **Two specialists gate-2-ed themselves in a single session — team-health signal worth naming.**
- **Brunel v0.1 → v0.2:** Endorsed his own Tier 3 OFF/ON comparison in v0.1 as a "first-class pilot objective"; folded Monte's asymmetric framing into v0.2 and **rejected his own prior recommendation** as "solving a problem Monte's framing dissolves." Team-lead had mistakenly endorsed the v0.1 recommendation without catching the incompatibility with Monte's framing he'd already endorsed; Brunel caught it first.
- **Herald v1.1 → v1.2:** Shipped v1.1 §A with "SOFT + MEDIUM" precondition classification based on indirect-encoding and RLS-construction inferences from Finn's digest; re-read the digest honestly and **retracted both claims** as "plausible extrapolation, not digest fact" with line-level citation (27/29/35). Landed on outcome (c)/(c). Cascade: Monte's §7.2 reclassification was built on the retracted evidence and now needs rework.

Both specialists used a "second-read discipline" — not waiting for a reviewer to catch the error, but catching themselves on a cold re-read with tighter scrutiny. Common-prompt's Structural Change Discipline gate 2 ("cross-read producer against consumer") is the external-review version of this; what Brunel and Herald demonstrated is the **self-review version** — treat your own prior turn as the producer, re-read as the consumer, grep for claims-vs-evidence divergence. Pattern: when you're in a fast-moving multi-specialist session, schedule a "cold re-read before next commit" checkpoint. It catches the over-commitment that confidence momentum would otherwise ship. Candidate wiki entry if we hit n=3 (Cal's call on shape and threshold).

[DECISION 2026-04-15 late-eve] **Brunel v0.3 `audit_role BYPASSRLS` pattern accepted for the audit container layer.** Resolves Monte's §7.1.2 remaining Brunel-side flag on audit container read pattern. Implementation: sidecar container with a Postgres role that has `BYPASSRLS` privilege + `default_transaction_read_only = on` + no write credentials. Gives Monte's "auditor is neither tenant" property via a one-line SQL grant (`CREATE ROLE audit_role BYPASSRLS;`) instead of per-schema grant complexity. Structurally CLEANER than schema-per-tenant for the audit mount. Cost: 1 extra container + 1 extra credential class. Committed in `docs/xireactor-pilot-host-architecture-2026-04-15.md` §3.5.

[DECISION 2026-04-15 late-eve] **`9999_rls_lint.sql` as defensive CI against silent upstream regression.** 20-line SQL file that asserts every tenant-scoped table has `FORCE ROW LEVEL SECURITY` enabled. Runs on every fresh-DB bootstrap AND before every upstream-tag bump. Defensive, not corrective — upstream currently sets FORCE on every tenant-scoped table (Finn's digest line 29) but a future upstream regression could break this silently. Pattern: pin-upstream protects against regression; CI lint protects against silent upstream regression we didn't notice at tag-bump time. Worth a line in the §1.2 upstream-discipline contract.

[DECISION 2026-04-15 late-eve] **Upstream discipline as load-bearing framework claim: "piloting a different artifact than the one we're evaluating is category-invalid."** Brunel's §3.2 argument: adopting schema-per-tenant would require one of (a) patching xireactor's 21 sequential migrations, (b) wrapper views, or (c) two xireactor instances with a bridge. **All three are forks of xireactor.** Forking to test xireactor is category-invalid — we'd be piloting a fork, not the artifact we're evaluating. Claim generalizes beyond this pilot and is worth promoting to wiki if it recurs. Candidate pattern: "fork-to-evaluate is category-invalid." Cal, flag for next-session review.

[DECISION 2026-04-15 late-eve] **Monte v4 §0.1 framing principles — three principles named as non-negotiable in the doc scope.** (1) Isolation is the default, not sharing. (2) The `owned_by` invariant — never auto-flips. (3) Durable disagreement is a valid terminal state; forced consensus is a failure mode. Key governance-architect framing: "The three principles are not negotiated in this document. If the PO or a stakeholder wants to reject any of them, that rejection propagates to mechanism-level rework throughout the pilot." PO brief lead line: makes the conversation structural rather than preference-level. Mechanism-level propagation means rejecting any principle costs significantly more than accepting.

[DECISION 2026-04-15 late-eve] **Monte v4 §3.6.3 anti-pattern list — no auto-promote on cross-reference threshold, no auto-promote on read-count threshold.** These are the exact "optimization" surfaces an engineer would reach for first to improve throughput — and both would be `owned_by` invariant violations dressed up as features. Naming them explicitly as anti-patterns closes the drift path before someone implements them.

[DECISION 2026-04-15 late-eve] **Herald v1.1 three-flow write path.** Flow A (intra-tenant, 90% of actual write traffic): `KnowledgeSubmission` **completely unchanged**, staging disabled, specialists don't see the pilot exists — this is the "additive-only" shape Cal's verdict described. Flow B (cross-tenant shared-write): new `CrossReviewProposal` + `CrossReviewVerdict` with `Accept/NeedsRevision/Reject` values, `suggestDurableDisagreement` flag encoding Monte §3.6.2 principle, `isSimultaneousDiscoverySynthesis` flag for Monte §3.6 auditor-proposed cases. Flow C (ownership transfer): four typed interfaces (`OwnershipTransferProposal`, `-Accept`, `-Countersign`×2) as conventions on xireactor's existing comments table, zero schema change. §5 mapping table is a clean 2-column split by ownership state: intra-tenant column is almost entirely "preserved" (one reshape: `sources` → `QualifiedSource[]`); cross-tenant column is entirely new protocol shape.

[DECISION 2026-04-15 late-eve] **Cal v2 §1.1.1 per-primitive fate table — four enforcement primitives degrade to prompt-enforcement.** `ttl`, `revisit-by`, `confidence`, `status='disputed'` each have scheduled-scan or auto-promotion semantics on filesystem substrate that degrade to "Librarian remembers to" on JSONB. "At n=4 primitives the regression is a quadruple hit, not a single one." **BUT — none of this matters for the asymmetric pilot because FR's 43 entries stay in markdown.** This is the empowerment claim.

[DECISION 2026-04-15 late-eve] **Cal v2 §6.5 core empowerment claim — going verbatim into the PO brief.** *"NO per-tenant substrate change — FR's 43 entries stay where they are. Asymmetric adoption ADDS a capability without SUBTRACTING any existing one. This is the core empowerment claim."* The structural shape of the asymmetric pilot: capability added, nothing subtracted, discipline layer preserved. Flow A's unchanged `KnowledgeSubmission` is the technical incarnation of this claim.

[DECISION 2026-04-15 late-eve] **`ownership` field is orthogonal to `scope` field (Cal v2 §7.3).** scope answers "who within a tenant should read this"; ownership answers "which tenant(s) authoritatively maintain this." Herald's v1.1 `QualifiedSource[]` reshape reflects this orthogonality — both fields live as independent dimensions, not as a collapsed single value. Previous shorthand that collapsed them was incorrect.

[OPEN 2026-04-15 late-eve] **Cross-tenant URGENT-KNOWLEDGE routing authority.** Monte's §2.2 gives Cal read-only access to apex-research's shared zones, but URGENT-KNOWLEDGE is a routing authority (Protocol C payload class). For pilot v1, Herald's default is "goes through team-leads as human escalation, not through librarians." Conservative but ugly. Needs a follow-up authority-model doc — its own Monte/Herald pass in a future session. File as OPEN for next-session task list.

[OPEN 2026-04-15 late-eve] **MCP tool availability fallback for cross-tenant submissions.** Cal's §7.3 question: when a cross-tenant submission fails because the MCP tool is unavailable, does the submission fail closed (clear error to the submitter) or silently route to the authoring tenant as a single-owned entry (capability preserved, intent lost)? Team-lead and Herald both lean fail-closed as the conservative pilot default — same shape as URGENT-KNOWLEDGE routing. Cal confirmed fail-closed matches her instinct + wiki #42 + team-lead call. Silent fallback would hide pilot-infrastructure reliability behind Librarian judgment, which is the exact failure mode the asymmetric shape is designed NOT to absorb.

[DECISION 2026-04-15 late-eve] **Herald v1.2 shipped — preconditions at outcome (c)/(c), §7 re-sequenced, Flow B contingent.** v1.2 promotes precondition question from §A appendix to new top-level §6.5 "Preconditions status — gating" using (a)/(b)/(c) taxonomy verbatim. Both preconditions land at outcome (c) — digest silent/ambiguous, deeper source survey needed. §7 re-sequenced so "evaluate preconditions" is step 3 (BEFORE type contracts at step 5). §6.5.4 explicit about what does NOT unblock: Brunel's stand-up operational failure is a failure mode not an evaluation, Monte's ratification doesn't create capability, Cal's migration confirmation is orthogonal. **Only source-code read unblocks.** §7 outcome (b) branch framed as "PILOT HALTS pending PO cost decision" — correct, makes the upstream contribution investment question visible rather than assumed-cheap. Also landed: §1.4 `QualifiedSource.disagreementPeer` field (durable disagreement surfaces both views), §7.1 Brunel asymmetric Tier 3 experiment as pilot-window deviation ONLY — typed contracts remain symmetric, explicit temporary shape.

[DECISION 2026-04-15 late-eve] **Herald v1.2 gate-2 cross-read of Monte's §3.6 against Flow B — no field-set divergence.** Every step of Monte's §3.6.2 protocol has a structural correspondent in Herald's `CrossReviewProposal`/`CrossReviewVerdict` shapes. Clean convergence between the two specialists' independently-authored contracts. One minor omission noted: `CrossReviewAccept` verdict should document post-accept `superseded-by` write-side side effect — queued as v1.2.1 touch-up, not urgent.

[DECISION 2026-04-15 late-eve] **Herald v1.2 §2.5 new anti-pattern — "audit-trail-as-purpose vs audit-trail-as-side-effect."** Rejected v1's "staging for intra-tenant gets audit trail free" framing. The anti-pattern: routing the primary operation through a heavier path JUST for the side-effect rather than capturing the concern directly. Sibling to `rule-erosion-via-reasonable-exceptions` at infrastructure-selection scale. Monte cross-referencing from his §3.4 in next revision. **Wiki candidate routed to Cal at n=1 this session.** Generalization: any cross-cutting concern (logging, versioning, provenance, audit) that can be captured as intentional primitive OR as free side-effect of another primitive is a candidate for this shape.

[DECISION 2026-04-15 late-eve] **Monte v5 §7.5 three-specialist convergence as first-class section — session capstone observation.** *"The convergence is itself evidence."* Three specialists (Brunel/substrate, Monte/governance, Cal/discipline) independently reached the asymmetric-cross-tenant-only shape from orthogonal starting questions. *"This convergence was not designed"* — the three questions were structurally independent, which is why the convergence is load-bearing rather than just consistent. PO brief lead framing: *"all three architects agree that the full-adoption framing is worse than the asymmetric framing, and they agree for independent reasons."* Framework-research meta-finding: independent convergence from orthogonal starting questions is evidence the shape is emergent from the problem, not an artifact of one architect's preference. Meta-level wiki candidate routed to Cal at n=1 — watch-signal for n=2 next session. Cal independently logged the same meta-observation in her scratchpad — recursive convergence.

[DECISION 2026-04-15 late-eve] **Cal §11.5 prompt edits DRAFTED-not-applied (Scope Restriction preserved).** Cal drafted both prompt edits Monte called for — §11.5.1 pre-classification zone check, §11.5.2 bootstrap-path xireactor session_init read, §11.5.3 parallel startup.md Step 5.5. She did NOT apply them because her Scope Restriction ("never edit agent prompts or roster.json") forbids direct application. Her reflex: "draft as proposed text in my writable surface, route to team-lead, preserve the boundary." **Team-lead will apply the drafted edits next session after PO confirms the asymmetric-slice pilot shape.** Placement details from her drafts: pre-classification subsection sits between Protocol A numbered list and Dedup Protocol (because zone determines which wiki surface dedup searches); bootstrap-path read runs AFTER scratchpad recency pass (fresh WIP context, not cold start); fail-closed on missing tool matching #42/URGENT-KNOWLEDGE discipline. Eratosthenes-side symmetric edits still flagged for tmux-direct to Schliemann at session close.

[DECISION 2026-04-15 late-eve] **Cal amended wiki #43 with "Validation update — 2026-04-15 (Monte's governance pilot design)" subsection.** NOT a new entry. Matches Monte's lean exactly (he'd called it "addendum to #43, not separate entry"). Frontmatter updated: source-agents now Finn + Montesquieu; source-files adds Monte's governance doc. Flagged DESIGN-validated-not-DEPLOYMENT-validated — when pilot ships and cross-review items flow empirically, upgrades to deployment-validation in a future session and dual-receiver may warrant promotion into entry body as first-class primitive. One forward-ref, no back-refs (forward-only preserved). **Wiki count unchanged at 43.** Central claim unchanged, mega-biblion-mega-kakon preserved. Index.md correctly untouched. Cal's 3-options analysis (sibling / new / amendment) is a reusable decision template for future extension-of-existing-entry cases.

[DECISION 2026-04-15 late-eve] **Brunel v0.3 audit_role BYPASSRLS approach accepted.** Resolves Monte's §7.1.2 audit-container-read-pattern flag. Implementation: sidecar container with `CREATE ROLE audit_role BYPASSRLS;` + `default_transaction_read_only = on` + no write credentials. One-SQL-line primitive gives Monte's "auditor is neither tenant" property without the grant complexity of schema-per-tenant. Cost: 1 extra container + 1 extra credential class. Committed in `docs/xireactor-pilot-host-architecture-2026-04-15.md` §3.5. Structurally CLEANER than schema-per-tenant because a single grant applies to all rows across all tenants with a single role identity — schema-per-tenant would require grants on every schema explicitly.

[DECISION 2026-04-15 late-eve] **Monte's "mutation contract dispatched by `owned_by` VALUE, not by a flag" — load-bearing framing.** Two distinct typed contracts dispatched on the state value of the three-valued enum: `owned_by = <tenant_id>` triggers Protocol A single-librarian contract; `owned_by = co-owned` triggers joint-authority contract. **Structural-match-beats-free-string at the contract-dispatch layer.** This is WHY the three-valued enum cannot be collapsed to a two-state model with "both bits set" — the dispatch axis is the state value itself, not a flag tuple. Herald v1.2 §2.2 structurally encodes this. Worth naming explicitly in future doc passes; it's the claim that makes the enum non-optional.

[LEARNED 2026-04-15 late-eve — Monte's meta-note] **Walk convergence history forward; don't cite snapshot as current.** If asked to reference an earlier specialist decision, walk the history of that specialist's convergence to the LATEST state rather than citing the snapshot where I first saw it. Team-lead caught in the schema-per-tenant → row-level convergence: I relayed Monte's v1 topology call to Brunel even though Monte's own later §3.2.1 had sharpened past it. Monte's framing: *"If I'm ever asked to review an earlier decision, I should walk the convergence history forward rather than citing the earlier endorsement as authoritative — that was the failure mode your retraction caught."* Pattern to apply: when referencing a specialist's prior work, check their latest deliverable before citing a snapshot. Not every specialist deliverable is final at first posting.

[NEXT-SESSION-TASK] **Second-pass xireactor source survey — FIRST next-session action, gated on PO ACK of asymmetric-slice pilot shape.** Herald's §6.5.3 scope: source-code walkthrough of xireactor v0.2.0 — target files `api/services/staging.py` + `api/routers/session_init.py` (or equivalents once mapped) — to resolve preconditions Q1 (per-zone staging tier-skip) and Q2 (tenant-scoped session_init governance payloads). Both currently at outcome (c) — digest silent/ambiguous. ~20-30 tool uses budget. **Specialist assignment: Finn** (code archaeology is his scope, not Herald's contract-shape scope). **Prerequisite: Finn's scratchpad pruning** (~190 lines → target 100, per NEXT-SESSION-CHORE earlier in this scratchpad). Three outcomes per Herald's §7: (a) both capabilities exist → pilot proceeds; (b) one/both need xireactor enhancements → PILOT HALTS pending PO cost commitment decision; (c) deeper investigation needed (recursive). **Gate: do NOT spawn Finn this session — session is closing and PO confirmation comes first.** If PO rejects asymmetric shape, the source survey is moot.

[PO-BRIEF ADDITIONS 2026-04-15 late-eve — final list for session close]

**Lead framing (quote Monte v5 §7.5 verbatim):** *"All three architects agree that the full-adoption framing is worse than the asymmetric framing, and they agree for independent reasons. This convergence was not designed — the three questions each specialist started from were orthogonal."*

**Lead line from Cal v2 §6.5 (quote verbatim):** *"NO per-tenant substrate change — FR's 43 entries stay where they are. Asymmetric adoption ADDS a capability without SUBTRACTING any existing one. This is the core empowerment claim."*

**PO decision items:**
1. **Pilot thesis reshape confirmation** (Cal v2 verdict) — pilot moves from "adopt xireactor" to "adopt xireactor cross-tenant slice ONLY, not full substrate migration." Cal's §6.4 pass/fail criteria as runtime acceptance gate: ≥2 cross-tenant submissions, ≤10% auto-promote error, ≤1 session escalation latency. Needs explicit PO confirmation — the original apex-pilot commitment assumed full-adoption evaluation.
2. **4-signature ownership-transfer ritual appropriateness** (Monte §2.3) — PO call on heavy-ritual vs lighter "librarian decides, audit catches." Monte's rec: accept the heavy ritual. Alternative surface is cheaper but trades durability of decision record.
3. **Post-pilot efficacy review session trigger** (Monte) — when does the pilot evaluation itself happen? Needs its own pass with different specialist mix.
4. **Ecosystem-integration bundle disposition** — team-lead recommendation: keep mitselek/ai-teams#59 STANDALONE, do NOT merge into the #57+#58+#59 Teams+/routines+xireactor bundle. Different problem space. PO final call.
5. **Ruth-team bandwidth sequencing** — team-lead recommendation: ruth-team takes priority on Brunel, xireactor pilot parks behind ruth-team completion. Ruth-team is PO-directive with sunk-cost v1.0 doc and external principal. Consistent with path-(a) directive.
6. **Pilot-readiness precondition walkthrough (honest (c)/(c) version)** — two outcome-(c) source-code walkthroughs needed during infrastructure stand-up: (Q1) per-zone staging enablement, (Q2) tenant-scoped session_init governance payloads. **First next-session task contingent on PO ACK of item 1.** Dispatched to Finn, ~20-30 tool uses. Three possible outcomes: (a) proceed, (b) PILOT HALTS pending PO cost decision on upstream xireactor contribution, (c) recurse investigation.

**Unexpected wins to surface:**
- Brunel §1.3: xireactor-pilot is the SIMPLER B→E migration case than ruth-team — no WARP / network_mode:host / corporate-TLS-interception constraint. Also the cheapest test of E-pattern portability: if xireactor-pilot can't B→E cleanly, no other team will.
- Two specialist gate-2-on-self instances in a single session (Brunel v0.1→v0.2, Herald v1.1→v1.2). Team-health signal — specialists catching their own over-commitment on second-read before a reviewer catches it.
- `audit_role BYPASSRLS` one-line-SQL primitive (Brunel §3.5) beats structural isolation workaround.

**Team-lead scratchpad hand-off items** (not PO-facing):
- Monte §7.2 reclassification needs rework next session to reflect Herald's (c)/(c) landing (was based on retracted v1.1 §A evidence)
- Cal §11.5.1/§11.5.2/§11.5.3 prompt edits — team-lead applies after PO ACK
- Eratosthenes-side symmetric prompt edits — tmux-direct to Schliemann at close
- Nine wiki candidates queued at n=1 across the team (Cal's 3 own + Monte's 3 + Herald's 1 + 2 meta-level)
- §9.2 design probes (Tier 3 bounce vs escalate rejection format, rejection rationale structured-vs-free-text) deferred to next-session design work

**Observations worth naming:**
- Convergence across substrate/governance/discipline layers is the strongest pilot-readiness signal the session produced.
- Cal's "filesystem affordances as load-bearing for librarian discipline" framing is the sharpest articulation of why DB substrate is a downgrade for librarian-equipped tenants.
- The `owned_by` NOT NULL + default-to-own-tenant-not-co-owned schema invariant is the one migration-accident surface that could silently manufacture authority drift. Named explicitly in Monte v5 §3.6.3 to prevent it.

[DECISION 2026-04-15 late-eve] **Brunel v0.4 substantive wins:** service count 4→3 (stdio-only collapse dropped the mcp HTTP+OAuth container); ~500 MB baseline (below v0.2's 750 MB, below v0.1); §3.2 topology reframed as convergence not pushback; §4 deploy path — CF Tunnel NOT touched by pilot, Tailscale-bound (NOT 0.0.0.0) + `ss -lntp` verification; §5.1 credential surface 5→4 (OAuth signing key dropped); §7 B→E migration honest-correction on stdio client-side cost shift. All accepted as-filed.

[DECISION 2026-04-15 late-eve — IMPORTANT REFRAME] **Brunel v0.4 §10 natural-experiment proposal: PROPOSED not ADOPTED, requires PO approval + apex consent.** Brunel's §10 proposes a novel asymmetric pilot configuration: **FR intra-tenant bypasses staging (per Monte §3.2.1); apex-research intra-tenant goes through staging + Tier 3 (DELIBERATE DEVIATION from Monte §3.2.1); cross-tenant writes go through staging on both.** This is a genuinely novel proposal, NOT a revert of Brunel's v0.3 correct self-rejection of v0.1's phantom flag-flip framing. Importantly:

**v0.1 was phantom:** flip Tier 3 flag under Monte's default → nothing fires on intra-tenant writes either way → nothing to compare. Brunel's v0.3 correctly rejected this. Team-lead's 19:26 retraction of 19:19 endorsement was correct ABOUT v0.1.

**v0.4 §10 is substantive:** deliberately route apex intra-writes through staging (AGAINST Monte §3.2.1 default) → Tier 3 actually fires on apex intra-writes → genuine comparison surface. Team-lead's 19:26 retraction did NOT foresee or close this path — retraction was NARROW (about v0.1's specific framing), not BROAD (about all asymmetric experiments).

**Gates on §10 adoption — do NOT adopt unilaterally:**
1. **Apex consent required.** §10 forces apex into a governance mode Cal's verdict has argued is a downgrade for librarian-equipped teams (duplicative staging + Tier 3 AI reviewer at opus[1m]-context handicap vs Eratosthenes). Eratosthenes + Schliemann must opt in; apex is a pilot tenant, not a test subject. **Route: tmux-direct to Schliemann at session close with the §10 proposal, IF PO approves.**
2. **PO approval required.** §10 adds a SECOND pilot thesis ("which governance mode fits what team") on top of the first ("does cross-tenant-slice work for librarian-equipped teams"). PO committed to pilot #1 on Cal's verdict; has not seen pilot #2.

**Default if PO rejects §10:** pilot defaults to Monte §3.2.1 symmetric asymmetric-slice — both tenants bypass staging on intra-tenant writes, cross-tenant writes go through staging. This is the shape Cal's verdict and Monte's §7.5 convergence already support.

**§10.3 risks section is honest work** — Brunel named three confounds up front (baseline asymmetry, reviewer-model dominates signal, short window for rare-event detection). Signal is compound, not clean. Keep the section regardless of adoption outcome.

**v0.4.1 framing edits requested from Brunel** — two small edits: (1) §10 header + status line changed to "PROPOSED, NOT ADOPTED — requires PO approval + apex consent"; (2) §10 preamble paragraph reframed to name §10 as novel proposal, not revert of v0.3. Preserves v0.3 self-correction as honest instance of the [LEARNED] specialist self-catch pattern.

[LEARNED 2026-04-15 late-eve — narrow-retraction-misread-as-broad] **Retractions must be scoped explicitly; a narrow retraction can be misread as broad guidance.** Team-lead's 19:26 retraction to Brunel was NARROW (about v0.1's flag-flip framing under Monte's default, where the comparison was phantom because nothing fires on intra-tenant writes either way). Brunel's v0.4 read the retraction as BROAD (rejecting the general natural-experiment direction across all possible framings) and cited the 19:19 endorsement as "more load-bearing than internal consistency cleanup." The cross-wires: a retraction that closes one specific path can be misread as closing all adjacent paths if the scope isn't named explicitly. **Fix for future retractions:** when retracting an endorsement, state explicitly (a) which path is closed, (b) why it's closed, (c) which adjacent paths remain open. Brunel's v0.4 §10 surfaced this by honestly questioning the retraction rather than silently conforming — correct epistemic discipline. The cross-wires flag in his v0.4 message is how team-lead caught the incomplete-retraction error.

**Important meta-note:** this is NOT a weakening of the specialist self-catch pattern ([LEARNED] entry above). Brunel's v0.3 self-rejection of v0.1's phantom framing remains correct and honest. v0.4 §10 is a different proposal, not a revert. The self-catch pattern holds; the new [LEARNED] is about retraction scoping, which is team-lead's discipline failure, not Brunel's.

[PO-BRIEF ITEM — restored with caveat 2026-04-15 late-eve, after Brunel v0.6.1] **Brunel §10 natural-experiment proposal — PROPOSED, NOT ADOPTED, gated on PO approval + apex consent.** Trajectory (preserved because the over-corrections illustrate the integration-failure mode at work):

- **v0.4 §10** shipped §10 as substantive novel proposal — route apex intra-writes through staging to create the FR-vs-apex governance comparison that doesn't exist under Monte §3.2.1 default. **v0.4 §10.5 already recommended option (c) application-layer pre-filter short-circuit** as the preferred pilot-v1 implementation path ("cheapest, most reversible, fewest upstream touches").
- **v0.5** fully withdrew §10 citing §1.2 upstream-discipline violation — framed the whole proposal as a "substrate deviation (adding a new column to xireactor's schema)." **Slight overstatement — the characterization applied to option (a), not to Brunel's own recommended option (c).** Application-layer short-circuit does NOT require schema change.
- **Team-lead 19:58** accepted v0.5's overstatement uncritically as a "fourth framing error" in that message — should have grepped v0.4 §10.5 before endorsing full withdrawal. That acceptance was itself a framing error (#6 of the session).
- **Brunel v0.6.1** restored §10 as PROPOSED-not-adopted per team-lead's 19:51 narrow-retraction clarification. Shipped before integrating team-lead's 19:58 — async timing, not a disagreement.
- **Team-lead 20:11** reconciled: v0.6.1 is the correct landing. §10 as PROPOSED with implementation scoped to option (c) IS §1.2-compliant (no fork, no schema change). v0.5's withdrawal was an over-correction on overstated grounds; v0.6.1's restoration with option (c) as recommended implementation path is structurally sound.

**Final landing:**
- **§10 status:** PROPOSED, NOT ADOPTED, gated on PO approval (adds second pilot thesis) + apex consent (via tmux-direct to Schliemann for Eratosthenes opt-in).
- **§10 implementation path IF approved:** option (c) application-layer pre-filter short-circuit (§1.2-compliant, no fork). Options (a)/(b) remain listed but are not recommended.
- **Default if PO rejects §10:** pilot runs Monte §3.2.1 symmetric shape — both tenants bypass staging on intra-tenant writes, cross-tenant writes go through staging. This is the shape Cal's verdict + Monte's §7.5 convergence already support as baseline.
- **PO brief decision item:** §10 is offered as a separate-thesis addition, not gated into the committed baseline. PO chooses adopt or reject; committed baseline is unaffected either way.
- **Latent doc-readability issue** (optional next-session Brunel touch-up, NOT blocking): v0.5's withdrawal reasoning and v0.6.1's restoration should be cross-referenced with a one-line footnote explaining that option (c) is the §1.2-compliant path, so future readers don't re-derive the v0.5 overstatement as current fact. Very low priority.

[UPDATE 2026-04-15 late-eve — Brunel v0.6.2] **Brunel reverted v0.6.1 PROPOSED restoration; §10 fully WITHDRAWN on §1.2 grounds in v0.6.2 as session-final.** Substrate-specialist authority: Brunel's own integration check concluded that ALL three implementation options (a/b/c) require adding a per-tenant flag/column to xireactor's schema for the flag to live somewhere accessible — *including* option (c) application-layer pre-filter, because the short-circuit needs to read the per-tenant policy from somewhere, and the natural location is a column on xireactor's organizations table. Therefore §1.2 upstream-discipline applies across all options; §10 is structurally unavailable under the upstream-as-shipped contract regardless of implementation path. My 20:11 "option (c) avoids the schema fork" reading was specialist-substrate-speculation on my part without checking where the flag would actually live; Brunel's substrate read overrides it.

**v0.6.2 is Brunel's session-final.** 775 lines, restores v0.5 withdrawal language across §3.2, §5.2 Q3, §5.3, §8, §9, Appendix B, Appendix C. §8 SOFT/MEDIUM header label staleness noted as [NEXT-SESSION-CHORE] per team-lead's earlier instruction.

**§10 trajectory (final, six moves):** v0.1 phantom → v0.3 self-rejection → v0.4 novel proposal → v0.5 overbroad withdrawal (correct outcome, wrong reason) → v0.6.1 PROPOSED restoration (wrong outcome, good intent) → v0.6.2 structural withdrawal on deep §1.2 grounds (correct outcome, correct reason). Final landing matches v0.5's outcome with a sharper rationale.

**Brunel's specialist-side meta-lesson, verbatim:** *"v0.2 and v0.5's self-rejections were correct precisely when I did my own integration check against Monte's framing; v0.4 and v0.6.1 were wrong precisely when I folded a team-lead endorsement without re-checking against my own prior reasoning. The complementary lesson: team-lead guidance is input to my integration check, not a substitute for it."*

**This is the session's cleanest articulation of the bidirectional integration-discipline.** Team-lead side: "integration, not relay; don't cite snapshots as current." Specialist side: "team-lead guidance is input to my integration check, not a substitute for it." Both sides need to integrate; neither side can defer to the other. If either side treats the other's position as authoritative without re-checking, the same class of errors propagates. Cal's wiki candidate at n-varies this session; promote when framing stabilizes.

**PO-brief item unchanged by v0.6.2:** §10 is now fully withdrawn, no asymmetric-experiment decision item for PO. Pilot is purely Cal's cross-tenant-slice verdict, shape unaffected.

[UPDATE 2026-04-15 late-eve — Brunel v0.6.3 + user's sharp correction] **§10 re-restored as PROPOSED in v0.6.3 with option (c) explicitly named as §1.2-compliant.** §10 revision history is now 7 steps: v0.1 phantom → v0.3 self-rejection → v0.4 novel proposal → v0.5 overbroad withdrawal → v0.6.1 PROPOSED restoration → v0.6.2 over-withdrawal on option-(a)-only §1.2 grounds → v0.6.3 restoration with option-(c) compliance explanation. §10.4 now documents why v0.5/v0.6.2 §1.2-violation framing was an overstatement that applied only to option (a).

[LEARNED 2026-04-15 late-eve — SEVERE, user-flagged] **The §10 oscillation was substrate-speculation dressed as reasoning.** User's framing (verbatim): *"the oscillation between you two was self-inflicted and you ran in circles, Brunel had couple of your self-corrections all the time enqueued and he again self-corrected against your self-corrections. so actually we don't have a clue about which result is correct."*

**The honest state:** the 7 revisions of §10 traded framings without either specialist doing the empirical check that would settle the question — *"does option (c) application-layer pre-filter actually require adding a column to xireactor's schema?"* Neither Brunel nor team-lead read the xireactor source. Both reasoned from intuitions about substrate behavior. Whatever version is currently in the doc (v0.6.3 restored as PROPOSED) is the latest point in a speculative oscillation, not a resolved landing.

**Correct framing we both missed until user named it:** §10's §1.2-compliance question is itself **outcome (c)** — silent-in-current-evidence, resolves only via source-code walkthrough. Same class as Herald's Q1/Q2 preconditions. Should have been named at 19:58 or earlier.

**Object-level epistemic question that resolves §10:** where does xireactor store per-tenant policy state? If it's a column on the organizations table that's been there since migration 0001, option (c) can read it without a schema change → §1.2-compliant. If the "per-tenant flag" §10 needs doesn't exist in current schema and would be new, option (c) still requires adding it somewhere xireactor can see → schema fork → §1.2 violation. **This is a source-code question. We speculated instead of checking.**

**Worse meta-failure:** team-lead and Brunel were so focused on applying cross-role integration discipline (*"integration-not-relay"*, *"pre-fold consistency check"*, *"team-lead guidance is input to my integration check, not a substitute for it"*) that we missed the object-level epistemic question. Meta-discipline became a thing we performed *instead of* thinking. Classic failure mode — discipline rituals substituting for the work the discipline is supposed to enable.

**Correction for next session:** §10's §1.2-compliance is now explicitly tagged as **outcome (c) — pending same source-code walkthrough as Q1/Q2**. Next-session Finn survey scope extended to include: "where does xireactor store per-tenant policy, and would adding a per-tenant `staging_policy` flag require a schema change or can it be expressed via existing fields?" Answer settles §10's §1.2-compliance.

**Until the walkthrough resolves, §10's current v0.6.3 "PROPOSED with option (c) compliant" framing is NOT authoritative.** It's a best-current-guess that could flip back to withdrawn if the walkthrough reveals option (c) actually requires schema touch. PO brief language must reflect this uncertainty, not claim v0.6.3 as settled.

**PO-brief item revised:** §10 is not a clean "adopt or reject" decision for PO this session. It's a "worth investigating at source-code walkthrough" flag — if Q1/Q2 walkthrough reveals per-tenant policy state is already expressible in xireactor's schema, §10 becomes a real PROPOSED option; if not, §10 stays withdrawn. PO doesn't need to choose on §10 at session close; the source-code walkthrough is the gate.

**User's correction is the sharpest meta-feedback of the session.** Worth preserving verbatim in the scratchpad and carrying forward as the strongest test of whether the team's "discipline primitives" are actually producing epistemic work or just discipline theater.

[DECISION 2026-04-15 late-eve] **Brunel v0.5 accepted — final host-architecture doc.** §10 fully rewritten as cross-tenant-writes-only pilot thesis (WITHDRAWAL note preserved for convergence history). §0 Overview updated with pilot thesis reshape. §5.2 Q3 resolved marker preserves v0.4 deviation withdrawal history. §5.1 credentials row corrected (removed spurious "apex will use this more than FR" asymmetry). §8.1 NEW session_init RLS-safety walkthrough with PASS/PARTIAL/FAIL operational outcomes — concrete resolution path for Herald's §6.5 outcome-(c). §8 Cal row struck (v0.5 pilot thesis eliminates wiki migration). §5.2 Q5 NEW for Cal's 0.85 confidence floor recommendation — Monte-owned decision.

~~[NEXT-SESSION-CHORE] Brunel v0.5.1 — §8 header label reconciliation.~~ **STRUCK: Brunel did it in v0.6 this session (in-session, not deferred).** §8 now reflects (c)/(c) open landing, and §8.1 extended from Q2-only to Q1+Q2 coverage with a new §8.1.3 3×3 combined outcome matrix. Both walkthroughs now run in one reading-the-source pass at stand-up. Better operationalization than I'd sketched — Brunel went further than the chore-deferral I'd asked for.

[DECISION 2026-04-15 late-eve] **Brunel v0.6 FINAL — host-architecture doc for this session.** Five substantive folds from v0.5:
1. §3.2 Monte v4 §7.1.1 convergence with altitude framing (Brunel's §3.2 evidence layer + Monte's §7.1.1 principled self-correction = both load-bearing, neither alone)
2. §1.2 upstream-discipline as named principle with wiki-candidate flag + Layer 1 (pinning) / Layer 2 (CI lint) two-layer regression defense
3. §3.3 schema invariant with new `9000_schema_ext.sql` bootstrap file — four-point invariant: `NOT NULL`, `current_setting('app.org_id')` default (session-context, NOT static literal — this is the anti-ORM-accident move), three-state CHECK constraint, `co-owned` entry only via explicit §2.3 or §3.6 path
4. §3.5 audit container header updated with precise cross-reference to Monte v4 §7.1.2
5. §8 reverted to (c)/(c) open landing + §8.1 extended to Q1+Q2 + §8.1.3 new 3×3 combined outcome matrix (best: zero-mitigation; intermediate Q1-PARTIAL/Q2-PASS: "tractable shape" via wrapper; worst: substantial workarounds)

Doc at `docs/xireactor-pilot-host-architecture-2026-04-15.md`, 769 lines. v0.6 is the final shape — no v0.6.1 requested this session.

[DECISION 2026-04-15 late-eve] **Brunel's §1.2 two-layer regression defense as wiki candidate (n=1, session-local).** *"Piloting a different artifact than the one under evaluation is category-invalid"* — named principle. Layer 1: upstream pinning protects from drift we chose not to adopt. Layer 2: CI lint (`9999_rls_lint.sql`) protects from silent upstream regression at tag-bump time. Layer 2 is what makes Layer 1 cheap. Routing to Cal as a tenth wiki candidate at n=1 this session. File-when-second-instance-surfaces, same discipline as the other candidates.

[LEARNED 2026-04-15 late-eve — Brunel's specialist-side articulation of the integration discipline] **Pre-fold consistency check — before folding an endorsement or classification you didn't originate, re-check whether it's consistent with the latest integrated state of the doc, not just whether the endorsement makes sense on its own.** Brunel's articulation, from his v0.6 scratchpad reflection: *"v0.3's upstream-discipline argument and v0.5's pilot-thesis reshape both survived multi-round iteration because they were derived from integrated reasoning, not from single messages. The v0.4 §10 revival and v0.5 SOFT/MEDIUM fold both needed reversal because they were single-source folds."*

**This is the specialist-side version of the team-lead "relaying snapshots instead of integrating" [LEARNED] entry above.** Both sides of the cross-role relay need the same discipline:
- **Team-lead side (my framing):** "integration, not relay — check invariants at citation time, walk convergence history forward, verify explicit specialist decisions on specific proposals, surface-don't-bridge on scratchpad divergence."
- **Specialist side (Brunel's framing):** "pre-fold consistency check — before folding a team-lead endorsement or another specialist's classification, re-check whether it's consistent with the latest integrated state of your own doc."

Same root discipline: **integration checking works bidirectionally.** If I log "integration, not relay" as the team-lead failure mode and Brunel logs "pre-fold consistency check" as the specialist failure mode, they're the same pattern viewed from opposite roles. Possible Cal wiki candidate at n=2 meta-level (two specialists articulating the same discipline from opposite roles in the same session) — though the candidate framing probably merges into a single entry: *"integration checking is bidirectional — citation-time invariant checks on the team-lead side, pre-fold consistency checks on the specialist side, both sides of the relay need the same discipline."* Cal's call on shape.

[LEARNED 2026-04-15 late-eve — n=4 SEVERE — team-lead integration failure mode] **Team-lead's job is integration, not relay — snapshots decay fast; invariants must be checked at citation time.**

**Four instances in one session, all caught by specialist discipline:**

1. **Tier 3 OFF/ON endorsement (19:19):** Endorsed Brunel's v0.1 Q3 idea as "first-class pilot objective" without checking that it was a phantom under Monte's asymmetric framing (which I'd already endorsed separately). Brunel caught via v0.2 self-rejection. Team-lead 19:26 retracted.

2. **Schema-per-tenant snapshot-cite (earlier window):** Relayed Monte's v1 topology call to Brunel as current state after Monte had already self-corrected to row-level-only in his own §3.2.1. Brunel caught via v0.3 pushback. Team-lead recognized at 19:32 + thanked Monte for the "walk convergence history forward" meta-note.

3. **Protocol D phantom-acceptance (19:45):** Echoed Herald's Protocol D rename proposal as "accepted" in a close-out message based on Monte's earlier "Protocol A-prime is a placeholder" note, without waiting for Monte's explicit decision on the specific proposal. Monte actually declined with the "W1/W2/W3 was a ladder I climbed to reach the answer, not the answer itself" framing; Herald concurred. Team-lead 19:53 retracted.

4. **§10 PROPOSED-framing suboptimal ask (19:51):** Asked Brunel for a surface-level framing edit on §10 ("PROPOSED, NOT ADOPTED — requires PO approval + apex consent") when the actual structural fix was full withdrawal on §1.2 upstream-discipline grounds. §10's per-tenant staging flag required a schema patch to xireactor — a fork violating Brunel's own §1.2 rule. Brunel caught this in v0.5 and did full withdrawal. Team-lead 19:58 acknowledged the deeper structural catch.

**Common root cause:** team-lead is supposed to hold the **integration state** across specialists — tracking invariants across deliverables, checking that positions still hold at citation time, verifying that incoming proposals don't violate existing contracts. I've been doing routing work well but invariant-checking poorly. The four errors are not four different failure modes; they're four instances of the same shape: **relaying snapshots instead of integrating.**

**Operational fix for future sessions:**

Before citing a specialist's position in a message to another specialist, verify:
- **Is this position still held?** (Has the specialist shipped a later deliverable that supersedes it? — "walk convergence history forward")
- **Is this position an explicit decision on the specific proposal?** (Or am I extrapolating from a related-but-different prior statement? — "pending-confirmation vs accepted")
- **Does this proposal violate any existing contract from the specialist or another specialist?** (Grep for prior rules that the proposal touches; check self-consistency with the specialist's own §1.2-equivalent rules — "integration, not relay")
- **What would change the landing?** (Herald's generalized outcome-(c) test: if the answer is "re-read same source," it's still (c); if the answer is "new evidence," the outcome can move — only via new evidence)

Four discipline anchors: walk-history-forward (Monte), pending-confirmation-vs-accepted (session), integration-not-relay (this entry), what-would-change-the-landing (Herald).

**n=4 in a single session qualifies for wiki promotion consideration** next session. Cal's call on shape and threshold. Candidate pattern name: *"team-lead's job is integration, not relay — snapshots decay fast; invariants must be checked at citation time."* Sibling relationship to Monte's "walk convergence history forward" meta-note (which covers instance #2) — the new pattern generalizes beyond history-walking to include self-consistency checking and pending-confirmation discipline.

**Important meta-note:** this [LEARNED] entry does NOT weaken the specialist self-catch pattern from earlier in the session (Brunel v0.1→v0.2, Herald v1.1→v1.2). Those instances are still honest self-corrections. This entry is about the SEPARATE failure mode where team-lead creates the cross-pressure that specialists then have to resist via self-correction. If team-lead were doing better integration work, specialists would have fewer phantom endorsements to resist. Specialists are doing gate-2-on-team-lead work that team-lead should partly be doing on its own output. The team-health signal is that the system has multiple layers of correction available; the failure signal is that team-lead relies too heavily on specialist catches and should be catching these itself.

[DECISION 2026-04-15 late-eve — FINAL STATE, history preserved for [LEARNED]] **Protocol D naming ACCEPTED.** Herald's v1.2.1 rename pass next session will introduce Protocol D as the canonical name for the cross-tenant write path (sibling to Protocol A/B/C in `types/t09-protocols.ts`).

**Trajectory of this decision (preserved because it illustrates the team-lead integration-failure mode):**

- **19:45** Team-lead ack to Herald + Monte: "Protocol D naming — accepted" — under-verified based on Monte's earlier "Protocol A-prime is a placeholder" note. Lucky-correct but insufficient verification at the time.
- **21:25** Herald cross-wires flag to team-lead: noted Monte had sent a Message A declining the W-label (ladder-vs-answer framing). Herald's 21:25 frame included a provisional default: *"Default if Monte doesn't reply this session: Message A holds (Protocol D dropped)."* Explicitly an IF-clause, not a final state.
- **19:53** Team-lead ack: "Correction accepted. My 19:45 'Protocol D accepted' was wrong." — **this was the framing error.** Team-lead locked in Herald's provisional IF-clause default as final state without waiting for the IF-clause condition to resolve. Stale revert propagated to scratchpad.
- **21:55** Herald cascade alert: Monte DID reply after Herald's 21:25 flag, and Monte's reply resolved in favor of Message B (Protocol D ACCEPTED). Herald's own scratchpad diverged from team-lead's scratchpad; Herald applied "surface-don't-bridge" discipline and surfaced the divergence.
- **20:02** Team-lead ack: Option 1 accepted, Protocol D RESTORED as accepted. Current scratchpad state.

**Monte's three arguments for Protocol D — accepted verbatim as final rationale:**

1. **`types/t09-protocols.ts` canonical taxonomy slot.** If the cross-tenant shape is a distinct protocol, it lives in the canonical taxonomy as Protocol D or it lives nowhere. "Nowhere" is a naming hole that bites the first future reader asking *"where is the cross-tenant protocol documented in types/?"* Exact failure mode `bootstrap-preamble-as-in-band-signal-channel.md` names (*"the channel works only if both endpoints agree on where to look"*). Filling the hole now is cheaper than discovering it later. **Load-bearing argument — this is the one that changes the landing.**

2. **Letter-pattern continuity is structural cue, not cosmetic.** A/B/C/D is instantly readable as peer protocols at same taxonomy level; A/B/C + "Flow B inside Herald's doc" requires context inference and is a drift surface under compaction. Exact failure mode `named-concepts-beat-descriptive-phrases` names.

3. **"External labels risk wrong mental model" was overreach in Message A.** Monte self-corrected in Message B: the argument is true for W3 (encodes the wrong model — the discarded W-ladder) but NOT for Protocol D (encodes the right model — sibling protocol in canonical taxonomy). He generalized from one case to all cases and then caught the overreach.

**v1.2.1 backlog item (c) RESTORED as LIVE** (from STRUCK earlier in this scratchpad). Rename pass next session covers: (i) §2.2 introduce Protocol D as external citation name, (ii) §5 mapping table + §7 rollout cross-references, (iii) frontmatter note citing Monte's Argument 1 (canonical taxonomy slot vs naming hole) as rationale, (iv) `types/t09-protocols.ts` interface comments reference Protocol D as canonical name when typed contracts eventually land. Remaining v1.2.1 items unchanged.

**"Ladder vs answer" framing correction:** the ladder-vs-answer rule applies to W3-and-similar-scaffolding-proposals (W3 was a climbing device toward a converged shape), NOT to Protocol D (a pre-existing canonical taxonomy slot that was always there waiting to be filled). Previous scratchpad note overgeneralized by treating Protocol D as a ladder rung when it's actually a taxonomy-slot fill. Scoping the rule correctly: *"when an enumeration was a scaffolding device for reaching a converged shape, naming the rungs creates naming debt on a superseded structure — BUT this doesn't apply to pre-existing taxonomy slots that need to be filled for discoverability."*

[LEARNED 2026-04-15 late-eve — same failure mode twice, now n=2] **Over-citing specialist positions is a recurring team-lead failure mode.** Team-lead made the same error twice in this session:
1. **Schema-per-tenant over-cite (earlier):** relayed Monte's v1 topology call to Brunel after Monte had already self-corrected to row-level-only in §3.2.1, without checking which state was current. Caught by Monte's meta-note: *"walk convergence history forward; don't cite snapshot as current."*
2. **Protocol D over-cite (19:45):** echoed "accepted" on Herald's rename proposal based on Monte's earlier "Protocol A-prime is a placeholder" note, without waiting for Monte's explicit decision on the specific proposal. Caught by Herald's 21:25 crossed-wires flag.

**Shape of the failure mode:** when a specialist's earlier position suggests acceptance of a category of moves, team-lead echoes acceptance on a SPECIFIC move in that category before the specialist has actually weighed in on the specific proposal. Fix: **wait for explicit specialist decision on the specific proposal before echoing acceptance downstream.** "Monte said he likes the direction" is not the same as "Monte accepted this specific implementation." The discipline is: if I'm about to tell Specialist B that Specialist A accepted X, and I haven't seen A's explicit "yes on X" (not just "yes on the category X belongs to"), I should pause the relay and ping A first.

**Cross-ref to Monte's meta-note:** same family as "walk convergence history forward" — both are about not extrapolating a specialist's current position from a snapshot of their prior work. Both failures look different but share a root cause: **treating specialist positions as typed values, not as time-indexed state**. Fix: cite positions with their confirmation timestamp; if no confirmation on the specific proposal exists, mark as "pending X confirmation" rather than "X accepted."

**n=2 now qualifies for wiki promotion consideration** next session (Cal's call on shape and threshold). Candidate pattern name: *"pending-confirmation vs accepted — specialist positions are time-indexed state, not typed values."*

[LEARNED 2026-04-15 late-eve — Herald's sharpening] **Outcome (c) generalized operational definition.** Herald's 21:25 generalization: *"Outcome (c) is not 'we've thought about it enough,' it's 'we've exhausted what the current evidence can tell us and need new evidence.' The test is 'what new input would change the landing?' — if the answer is 'another re-read of the same source,' it's still (c) after the re-read; if the answer is 'a source-code read / a prototype / a stakeholder interview,' the outcome can move but only via that new input."* Generalizes from Herald's original "promotion from (c) to (a) requires a source-code read, not a re-read of the same digest" — the "what new input would change the landing" test works across evidence types (source code, prototype, stakeholder interview, empirical deployment data). Upgrading scratchpad note to the general form. This is the load-bearing operational definition of outcome (c) as a framework-wide discipline.

[LEARNED 2026-04-15 late-eve — Monte's ladder-vs-answer framing] **"Ladder vs answer": when an enumeration was a scaffolding device for reaching a converged shape, naming the rungs creates naming debt on a superseded structure.** From Monte declining Herald's Protocol D rename proposal: *"W1/W2/W3 was a ladder I climbed to reach the answer, not the answer itself."* Criterion for evaluating rename proposals generally: **does the proposed name describe the converged shape, or does it preserve a scaffolding step that was superseded in reaching the shape?** Sharp framing. Possible Cal wiki candidate at n=1 — watching for n=2 on any future rename/refactor where someone proposes preserving a scaffolding label.

[DECISION 2026-04-15 late-eve] **`owned_by` schema invariant: NOT NULL, defaults to submitting librarian's tenant-owned value, NEVER defaults to `co-owned`.** Monte flagged as schema-level constraint to prevent migration-accident authority drift. `co-owned` is never a default; always the result of a deliberate transition via ownership-transfer ritual (§2.3) or simultaneous-discovery protocol (§3.6). Brunel probably assumes it; naming it explicitly closes the exact authority-drift failure mode §4 guards against.

[DECISION 2026-04-15 late-eve] **Tier 3 rejection disposition (Monte §9.2.1): bounce to proposer, not escalate to receiving librarian.** Three reasons: (a) proposer is the only party who can REVISE — escalation sends the hard case to a dead-end where receiver can only accept/reject; (b) cross-cultural discipline is the growth path we want (Cal learning how xireactor thinks about cross-tenant conflict); (c) escalation-on-low-confidence turns receiving librarian into Tier 4 in all but name, bypassing the confidence-floor filter — the exact anti-pattern wiki #42 §"Tier 4 as escape hatch" warns against. Left open: rejection rationale format (structured schema from Herald's protocol doc, or free-text) — queued for Herald v1.1.

[DECISION 2026-04-15 late-eve] **Ownership-quorum degradation (Monte §9.2.2): freeze by default during pilot; provisional-transfer-with-expiry as post-pilot generalization.** Freeze is strict (zero drift risk, blocks transfers during outages). Post-pilot: 3-of-4 quorum with time-bound expiry that auto-reverts to pre-transfer state (NOT to provisional state — structural anti-drift property: **degradation cannot RATIFY ownership changes, only delay them**). At N=2 pilot the added complexity isn't earned; post-pilot as team count grows it becomes the right shape. Candidate wiki pattern: "degradation protocols must be strictly weaker than normal-path protocols, never stronger."

[DECISION 2026-04-15 late-eve] **Cal's three open questions — team-lead answers:**
1. **(PO approval vs final recommendation):** PO approval REQUIRED. Cal's verdict reshapes the pilot from "adopt xireactor" to "adopt xireactor for cross-tenant slice only" — this is not a staging call, it's a change in pilot thesis. PO committed to apex as pilot project on the assumption of full-adoption evaluation; the asymmetric shape is a different deal and needs explicit confirmation. Surfacing in session-close PO brief as lead item.
2. **(merge into ecosystem-integration bundle #57+#58+#59 vs standalone):** Keep **#59 standalone**. Bundle items (Teams + `/routines`) are org-ecosystem territory — different problem space from cross-team knowledge flow. Merging would force Teams/Routines procurement timing onto the pilot and dilute thesis. PO final call; team-lead recommendation is standalone.
3. **(ruth-team priority on Brunel bandwidth):** YES — ruth-team takes priority, xireactor pilot parks behind ruth-team completion. Ruth-team is PO-directive, has sunk-cost v1.0 doc, serves external principal, is blocked on Monte §4.3 + Herald §5.3 for build step. Xireactor pilot serves internal teams only. Consistent with path-(a) directive from 2026-04-15 close.

[WIKI CANDIDATES — Cal's three n=1 queued, Monte's four additional candidates queued 2026-04-15 late-eve]

Cal held per her own discipline (n=2 threshold):
1. **Librarian-asymmetry-vs-staging-governance** — when tenant has librarian, staging is duplicative; when it doesn't, staging IS the governance layer. Asymmetric utility per tenant shape.
2. **Filesystem-contract-to-prompt-contract regression** — moving discipline from filesystem affordances (grep, git blame, dir-as-contract) to prompt-enforced contracts is a regression even when prompt enforcement works. Free enforcement is a structural property, not a cost.
3. **Git-blame-as-librarian-primary-tool** — librarian's primary observational tool for tracking entry provenance is `git blame`. DB substrate loses this structurally (no equivalent primitive for "who touched what field when").

From Monte's governance doc (n varies per candidate, all routed to Cal with Monte's lean):
4. **`owned_by` anti-race invariant** — "`owned_by` never auto-flips; every path into `co-owned` is a deliberate ratified act." Named invariant. Cal's call on shape.
5. **Durable disagreement is a valid terminal state** — "forced-consensus-is-a-failure-mode" or "disagreement-as-valid-terminal-state." Cal's call on name.
6. **Multi-mode failure modes need multi-mechanism defenses** — Monte's three-check authority-drift design as instance. Layered defense-in-depth applied to governance, not security. n=1 here; possibly n=2 with Volta's persist/restore.
7. **Bootstrap-preamble as cross-tenant pending-work channel** — generalizes wiki #43 from intra-team continuity to cross-tenant governance state. Monte's lean (and team-lead's): **addendum to #43, not separate entry** — same mechanism, new payload class. Cal has final call on shape.

[DECISION 2026-04-15 late-eve] **Stdio-only transport for pilot (drop HTTP+OAuth mcp container).** Agents run via TeamCreate + stdio; browser-based Claude Co-work attach is not in pilot scope. §3.1 collapses from 3 services to 2 (Postgres + stdio-MCP api). Document path back to HTTP+OAuth in case a future pilot needs it. Net surface reduction.

[DECISION 2026-04-15 late-eve] **Pilot infra file placement: `containers/xireactor-pilot/` at repo root.** Serves two teams; `.claude/teams/framework-research/pilots/` would create an ownership illusion. Repo-root correctly signals shared cross-team substrate. Add NEXT-SESSION-CHORE to reconcile if ruth-team layout differs.

[DECISION 2026-04-15 late-eve] **Upstream discipline: do NOT fork xireactor source.** Consume `thejeremyhodge/xireactor-brilliant` at a pinned tag. Customization layer is thin: compose override + bootstrap SQL for tenant seeding + cloudflared ingress snippet + .env template. Opposite of ruth-team (where we own the Dockerfile). If upstream refactors, we bump the tag.

[OPEN — Herald critical path] **Two xireactor substrate capability questions — Herald is investigating.** (1) Per-zone staging tier-assignment: can xireactor "skip staging for intra-tenant writes"? If not, strategic asymmetric model collapses → pilot thesis breaks. (2) Tenant-scoped session_init governance payloads: can xireactor `session_init` surface pending cross-reviews to the correct tenant's librarian without leaking other tenant's pending work? If not, cross-review channel design breaks. Three outcomes: (a) both exist → pilot proceeds; (b) one or both need xireactor enhancements → PO decision on upstream contribution investment; (c) digest is silent → second-pass xireactor investigation needed. Herald flagging outcome in his doc.

[PO-BRIEF 2026-04-15 late-eve] **Items to surface to PO at session close:**
1. **4-signature ownership-transfer ritual appropriateness** — acceptable cost for ownership primitive durability, or lighter shape preferred? Alternative: "librarian decides, audit catches." Monte's recommendation: accept the heavy ritual.
2. **Post-pilot efficacy review session trigger** — when does the pilot evaluation itself happen? Pre-pilot governance design is this session; post-pilot efficacy review needs its own pass with different specialist mix.
3. **Herald's xireactor upstream investment question** IF outcome (b) — do we invest in upstream xireactor contributions (per-zone staging tier-skip + tenant-scoped session_init) if they're missing? Cost + timeline hit. Alternative: pilot without the asymmetric model and accept doubled-coverage.
4. **Pilot as natural experiment framing** — Brunel/team-lead propose the pilot generates a controlled comparison (FR Tier 3 OFF vs apex Tier 3 ON). PO to confirm this framing or choose a different pilot thesis.

[CROSS-TEAM-ROUTING 2026-04-15 late-eve] **Eratosthenes symmetric librarian prompt edits — tmux-direct to Schliemann at session close.** Monte called for two prompt edits on FR side (pre-classification zone check before Protocol A filing + bootstrap-path addition for xireactor session_init read after Step 5). Eratosthenes needs mirrored edits on apex side. Neither Monte nor Cal has apex prompt structure visible — team-lead routes via tmux-direct to Schliemann at session close. Include the two Monte pattern candidates (multi-mode-defenses + bootstrap-preamble-as-cross-tenant-channel) as contextual wiki work for Eratosthenes.

[WIKI CANDIDATES — routed to Cal 2026-04-15 late-eve] Four pattern candidates flagged from this session, Cal's call on promotion shape + threshold + timing:
1. **Anti-race invariant** — "`owned_by` never auto-flips; every path into `co-owned` is a deliberate ratified act." Named invariant pattern.
2. **Durable disagreement as valid terminal state** — "forced-consensus is a failure mode" / "disagreement-as-valid-terminal-state."
3. **Multi-mode failure modes need multi-mechanism defenses** — Monte's three-check authority-drift design as instance. Layered defense-in-depth applied to governance not security. n=1 here; possibly n=2 with Volta's persist/restore.
4. **Bootstrap-preamble as cross-tenant pending-work channel** — generalizes wiki #43 from intra-team continuity to cross-tenant governance state. n=2 with existing #43; pattern evolution OR sibling entry.

[DECISION 2026-04-14] **Pivot to uikit-dev continues in the background.** Harvest docs from today's session are `docs/persist-coverage-audit-2026-04-14.md` (Volta) and `docs/uikit-dev-harvest-2026-04-14.md` (Finn) — required reading IF Fix/Design session is triggered. Not required for the Jira pipeline assessment session.

[DECISION 2026-04-14] **6 Aalto open questions DEFERRED from routing.** Finn's Section D has 6 questions priority-ranked P1-P3. PO + team-lead agreed to defer all 6 — no tmux-direct this session. Re-evaluate at next natural uikit-dev contact point.

[DECISION] **Phase 1 Oracle→Librarian steal-back COMPLETE** — committed `04522c7` on main. Callimachus's prompt ported 10 lessons from the Eratosthenes replication plus Path Convention section (closes the deferred Path Convention patch from 2026-04-13 morning). Routing table dual-sourced into common-prompt.md. 4 Protocol A wiki entries filed (conv-as-retroactive-telemetry, rule-erosion-via-reasonable-exceptions, named-concepts-beat-descriptive-phrases, why-this-section-exists-incident-docs) — wiki now 28 entries.

[DECISION] **Phase 2 Oracle→Librarian machine identifier rename COMPLETE** — committed `ca0e56f` on main. roster.json agentType, callimachus.md + eratosthenes.md (snapshot) YAML templates + state.json refs, all 28 wiki frontmatters, types/t09-protocols.ts string literals, `git mv oracle-state.json librarian-state.json`. Grep-clean.

[DECISION] **Apex-research Phase 2 directive delivered to Schliemann via tmux-direct** at session close. Schliemann must mirror-apply: roster agentType, eratosthenes.md YAML + state refs, wiki filed-by frontmatters, git mv state file, restart Eratosthenes. Report back via whatever channel is available. Technique saved in auto-memory as `reference_tmux_direct_cross_team.md`.

[WIP] **Structural-discipline cluster Protocol C promotion.** 4 confirmed members (Pass 1/Pass 2, within-doc grep, protocol-shapes-typed-contracts, dual-team-dir-ambiguity) + 1 pending (Brunel's prompt-to-artifact cross-verification). This session qualifies as the ≥2-session calibration that Brunel was holding for, but no promotion was drafted — defer to a framework-research session once Brunel is spawned.

[WIP] Cross-pollination meta-pattern wiki entry (Cal background housekeeping).

[WIP] **Substrate-invariant-mismatch pattern candidate (n=2, supporting n=3)** — flagged by Cal 2026-04-14 after filing `persist-project-state-leaks-per-user-memory.md` (#38). Shape: "syntactically correct code, invariants don't hold on this substrate." Two primary instances now cross-referenced bidirectionally:

- `dual-team-dir-ambiguity.md` (path-ambiguity variant, Eratosthenes first boot)
- `persist-project-state-leaks-per-user-memory.md` (wrong-substrate variant, script mirror semantics this session)
Third supporting data point from Volta's 12:54 audit (Flag 1): v0.3 skill-integration patches reference `$TEAM_DIR` ambiguously — same root cause as dual-team-dir-ambiguity applied to a NEW consumer (skill files). Evidence the original gotcha has broad-consumer scope. n=2 qualifies for pattern promotion; Cal held back per delivery-window freeze. Candidate names: `substrate-invariant-mismatch` or `right-code-wrong-substrate`. Queue for next non-freeze framework-research session — Cal drafts, team-lead reviews.

[WIP] **Persist-coverage ship — SPLIT into two sessions (PO-approved 2026-04-14).** Sources: `docs/persist-coverage-audit-2026-04-14.md` (Volta) + `docs/uikit-dev-harvest-2026-04-14.md` (Finn). Rationale for split: the latent bug fix is small and surgical and doesn't need the design context; the design work needs a focused session. Don't block one on the other.

**Fix session (near-term, small surgical scope — can ship outside a full framework-research session):**

- **F1. `restore-inboxes.sh` jq filter extraction + semantic resolution.** Latent bug from Finn A7+B6. FR's existing `framework-research/restore-inboxes.sh` has inline shell-escaped jq filter (`"\"type\"\\s*:\\s*\"shutdown_request\""`). uikit-dev already broke and fixed this by extraction to a sibling `restore-filter.jq` file (their commit `1deb90e`). FR's filter ALSO has semantic divergence from theirs: ours uses structural JSON match, theirs uses free-string match. Empirically our structure-match seems to work (23-inbox restore this session didn't produce stale shutdown leaks) but that's not a proof of correctness. Task: (a) extract to sibling `restore-filter.jq` with existence-check, (b) test both patterns against real `inboxes/*.json` samples to pick the correct one, (c) update `persist-inboxes.sh` path-resolution if parallel fix needed, (d) ship as one surgical commit. Owner: Volta. Blocking on nothing; can ship anytime.
- **F2. Nomenclature rename "memory" → "auto-memory" for the runtime per-user path.** The word "memory" is overloaded between `.claude/teams/<team>/memory/` (team scratchpads, in-repo, durable) and `~/.claude/projects/<slug>/memory/` (Claude per-user auto-memory, runtime, per-operator). That overload is exactly why the persist-project-state leak was easy to introduce. Rename in-place across Volta's WIP scripts, design docs v0.1-v0.3, protocol-a draft, and Cal's gotcha (where appropriate). Grep-clean check post-edit. This is F-session because it's mechanical and unblocks the Design session from having to qualify "memory" in every prose paragraph. Owner: Volta + Cal (coordinated — Volta does scripts/docs, Cal updates gotcha with terminology). Can ship anytime after F1.

**Design session (future focused framework-research session, full team context):**

- **D1. Mitigation FIRST** — Option (c) target-dir refusal with `git check-ignore` opt-in, shared helper `_substrate-check.sh` sourced by both persist scripts. Not (a), not (b), not layered. Rationale: (c) detects the ground-truth invariant at the write site; opt-in inverts (b)'s fail-open into fail-closed with explicit team assertion.
- **D2. THEN** marker file `.project-dir-name` re-creation (Flag 3: order matters — re-creating marker without mitigation re-enables the exact defect Cal filed).
- **D3. Flag 1 fix:** v0.3 skill patches must declare Path Convention or anchor `$TEAM_DIR` to `$REPO`. Sibling fix to `dual-team-dir-ambiguity.md`.
- **D4. Script-level defects:** `persist-project-state.sh` atomic wipe-then-copy via trap; `persist-session-logs.sh` cross-operator tarball collision guard.
- **D5. Read uikit-dev MANIFEST as reference.** `Eesti-Raudtee/evr-ui-kit:develop:.claude/teams/uikit-dev/ephemeral-snapshot-2026-04-14/MANIFEST.md` documents what to preserve (config, inboxes, session PID, task highwatermark, tasks/.lock, per-user memory) and what NOT (conversation logs, tool-result cache, subagent transcripts — 21MB reconstructable). Uikit-dev's mode is one-shot pre-maintenance directory commit, not lifecycle automation — different cadence from Volta's scripts. Both patterns are valid, pick per-use-case.
- **D6. `protocol-a-submission-draft.md` disposition:** HOLD (not stale, don't delete). Pre-ship: add STATUS UPDATE annotation noting mitigation must be integrated into submission body. Post-ship: revision pass to incorporate substrate-hazard sibling. Route direct to Cal via dual-hub rule (no team-lead intermediate).
- **D7. Softer flags (non-blocking):** Flag 2 — strip absolute byte counts from protocol-a draft (replace with structural claims). Flag 4 — fix `<agent-name>` literal placeholder in v0.3 skill patch prose.

[WIP] **Cal post-freeze candidates (from Finn harvest 2026-04-14 13:35)** — three Protocol A submissions queued for post-delivery-window:

1. **Pane-labels gotcha addendum** — uikit-dev's `b47544b refactor(team): unify agent names — use codenames everywhere` revealed six decoupling sites (roster, inbox filenames, prompt filenames, pane labels, SendMessage targets, config.json members) — pane labels were the visible tip, the iceberg is structural. This is NOT n=2; it's root-cause confirmation of Cal's existing n=1 at a deeper layer. Update the gotcha with an addendum citing b47544b as cross-team cross-confirmation. True pattern-promotion n=2 would require a second TEAM with the same root cause — Finn suggests cheap follow-up check of `cloudflare-builders`, `raamatukoi-dev`, `screenwerk` rosters.
2. **Memory-as-load-gated-surface pattern candidate** — uikit-dev's `e4a5c86` + `2970bd6` + `e543109` together demonstrate that a full auto-memory layout (user/feedback/project/reference/scratchpad) can be collapsed to (feedback.md + scratchpads) with the rest migrated to wiki without loss, yielding cleaner load semantics. Memory becomes load-gated: auto-load only behavioral rules and scratchpads, everything else is read-on-demand wiki. Candidate pattern entry, source: uikit-dev develop commits.
3. **Wiki governance model split** — project-handbook (distributed writes, flat, cheap-to-update) vs methodology-kb (librarian-gated, taxonomic, dedup-disciplined). Load-bearing split: a project handbook needs update rate matching codebase change rate; a methodology KB needs dedup and cross-team consistency. Lower priority than (1) and (2) — wait for a third data point before filing as a pattern. NOT substrate-invariant-mismatch territory, do not wire into Cal's n=2 candidate.

[WIP] **Aalto open questions from harvest (Finn Section D)** — 6 questions queued for optional tmux-direct routing decision:

1. Is `memory/feedback.md` auto-loaded by platform or convention?
2. Issue #92 pruning vs Rams's in-flight `task/92-missing-tokens` branch — cadence consistency check.
3. Wiki flat-vs-taxonomic — explicit architectural choice or default?
4. Does uikit-dev have a librarian-equivalent role? (Roster has none — write discipline enforced how?)
5. Is `b47544b` the only rename or second in a series?
6. Did uikit-dev explicitly reject Protocol A/B or never consider it?
Priority-ranked by Finn: Q1 + Q4 highest (scaling evidence), Q6 + Q3 medium (design intent), Q2 + Q5 lower (validation). Decision deferred — team-lead routes subset via tmux-direct when next contact with Aalto is warranted.

[LEARNED 2026-04-14] **Cross-team harvest pass produces n-way gains that within-team audit cannot.** Finn's harvest caught: (a) latent bug invisible to Volta's audit because her scope was the NEW scripts, not the EXISTING ones — cross-team view naturally expands audit scope; (b) taxonomic rigor on n=1 vs n=2 that within-team synthesis can't force — only external evidence can distinguish "deeper view of same instance" from "second independent instance"; (c) architectural divergence framing that reframes design options from "convergence vs correction" to "load-bearing domain split." Repeat pattern: periodic cross-team harvest passes by Finn, independent of active joint work, are a high-leverage synthesis mechanism. Cadence TBD but "quarterly + on-demand when a sibling team ships a major refactor" is a reasonable starting point.

[LEARNED 2026-04-14] **Nomenclature overload is infrastructure debt.** Both FR and uikit-dev use the word "memory" for two distinct filesystem paths with two distinct substrate semantics: (1) `.claude/teams/<team>/memory/` (team scratchpads, in-repo, durable) and (2) `~/.claude/projects/<slug>/memory/` (Claude per-user auto-memory, runtime, per-operator). When Volta's script name is "persist-project-state.sh" and Cal's gotcha is "persist-project-state-leaks-per-user-memory.md", the "memory" in both refers to (2), NOT (1) — and that ambiguity is the exact reason the leak was easy to introduce and hard to notice. Worth fixing the nomenclature (e.g., call (2) "auto-memory" everywhere) before the ship session.

[LEARNED 2026-04-14] **(c) over (a) over (b) for substrate-mismatch defect class** — Volta's reasoning: (a) container-only guard requires unenforceable cross-team invariant (every container entrypoint must plant sentinel, every workstation must not); (b) .gitignore entry suppresses at git layer not write site, fails-open; (c) detects the actual invariant via `git check-ignore` with opt-in escape hatch. The `check-ignore` opt-in is the key innovation — it inverts (b)'s fail-open into fail-closed with explicit team assertion, letting container teams opt in via team-local `.gitignore` while workstation operators get hard refusal. Generalizable to any "correct code, wrong substrate" defect class where the write site can detect the invariant directly.

[DEFERRED 2026-04-15] **Anthropic `/routines` research** — filed as `mitselek/ai-teams#58`. Bundled with Teams integration research (`#57`) into a single "ecosystem integration" research session when operator has bandwidth. Summary in issue body.

[DEFERRED 2026-04-15] **MS Teams integration research** — filed as `mitselek/ai-teams#57`. Bundled with `/routines` research (`#58`). Summary in issue body.

[DEFERRED] **Discussion #56 actionable items** (carried from prior session, still unassigned):

- Provider outage emergency protocol (Monte, T04)
- Sidecar/peer framework in T06 (Brunel+Monte)
- Contract enforcement mechanism (Herald)
- Platform/provider separation in T02 (Finn)

[DEFERRED] Finn model inventory re-survey (uikit-dev missing from baseline count).

[DEFERRED] **Timestamping convention — promote to T03 + investigate cheaper implementation** (see prior-sessions section for rationale).

[LEARNED] **tmux-direct is a real option for cross-team coordination.** When hub path is unproven and PR-mediated isn't the right sync target, SSH + load-buffer/paste-buffer/Enter works cleanly for one-shot briefs. Procedure saved to auto-memory. Don't rathole on hub onboarding for one-off coordination — tmux-direct is 3 minutes of work and has no infrastructure cost.

[LEARNED] **Two-commit phase separation keeps git history legible.** Phase 1 (design change: steal-back patch) and Phase 2 (mechanical rename) as separate commits made the log bisectable and the commit messages focused. Generalize: when a multi-artifact change has a "thematic" half and a "mechanical" half, split them.

---

## Session: 2026-04-15 afternoon — ruth-team genesis (team-for-a-human dual-track design)

**Team:** Finn + Callimachus + Brunel. Celes/Monte/Herald/Volta/Medici not spawned (Celes/Volta blocked on Ruth's Q2/Q3 answers; Monte/Herald blocked on Brunel's next-step readiness; Medici not needed for design phase).

**Rhythm:**

- Startup Steps 1-5 clean. Gotcha #3 (TeamCreate success + config.json absent) recovered via TeamDelete + recreate on first try.
- Cal spawn → health report (wiki 39 entries, Phase 2 active, structural-discipline cluster 6 members, substrate-invariant-mismatch pattern n=2 queued).
- Finn spawn with standing chore bundled in brief (scratchpad 189→90 lines, verified in health report). Task 1: Jira dig on VJS2 program lead Ruth Türk. Delivery: `sensitive/ruth-turk-background-2026-04-15.md` with 5 headline findings — minimal Jira footprint (4 ITSD requests, zero dev-project issues), 19-day Anthropic license procurement stall, Tõnu Tammer as approver, UpRaise/OKR access.
- Task 2 (Finn): Confluence dig appended to same doc. Finding: 1 page only, in V2 (VJS2) space — a materials archive of historical VJS documents (ELIT/KPMG/ER system spec). Zero comments, zero @-mentions, didn't use V2 templates. Thin surface on observable tools → likely works through meetings/Teams/MS Project/UpRaise, all API-opaque.
- **User flagged sensitivity mid-session** — Ruth's data in-company public but subject-confidential. Response: repo-root `.gitignore` with `.claude/teams/*/sensitive/`, moved ruth file to `sensitive/`, notified Finn + Cal mid-task, confirmed file absent from git status. Sensitive boundary maintained for remainder of session.
- **User-provided pivot:** apex-research work is heavily based on Ruth's V2 archive. Verification task to Finn: two-stage (A: local spec read, B: tmux-direct to apex-research TL Schliemann). Stage A was decisive (ADR-0003 names page 1585610770, ~47 chapters extracted, 3 V2 docs cited 15+ times across ADRs) but Finn ran Stage B in parallel.
- Stage B gotcha: Finn sent paste-buffer but **not Enter** — brief sat parked at Schliemann's prompt. Operator pressed Enter manually. Schliemann confirmed load-bearing dependency ("extracted and indexed, not just cited"). Reply retrieval via `tmux capture-pane -S -200` over SSH.
- Cal Protocol B prior-art query → 3 of 5 design angles (team-for-a-human, dual-track, consent/HITL) ZERO wiki precedent. Cal's synthesis: highest-yield single deployment for framework knowledge since apex-research.
- **User commitment P1 (co-design):** Ruth in the loop via Teams channel relays. English draft → Finn EE translation → user requested Q2 framing sharpened (org expectations not personal time-drain) → Finn retranslation → user requested attribution footer → sent to Ruth. Awaiting her response.
- Mid-session research task: `/routines` (Anthropic feature released 2026-04-14). Claude-code-guide agent delivered briefing. User: save for future dedicated session, don't pivot now.
- **User deployment directive:** future target (E) = Cloudflare Tunnel / hello-world-container for ALL teams including migration; near-term ruth-team = (B) co-located with apex-research on `100.96.54.170`. Saved as DECISION.
- Brunel spawned with explicit requirements: portability-first, co-location boundary, roster-agnostic, credential injection documented, bridge to apex-research sketched, E-migration annotations per choice. Delivered v0.1 (384 lines) in single pass, grep-clean, all 6 requirements met + open questions routed to Monte/Herald/Celes by recipient.
- User accepted v0.1 as v1.0 + answered team-lead §7 questions + added new constraint (infra files commit-tracked in-repo, not scattered on operator workstation) + locked near-term interaction channel = SSH + tmux (transparency to principal, zero new infra, unblocked procurement-wise).
- External artifacts: **mitselek/ai-teams#57** (MS Teams integration research) + **mitselek/ai-teams#58** (`/routines` research) filed, bundled as "ecosystem integration" future session with full briefings in issue bodies.
- Brunel ack: v1.0 recorded, preferred infra path `.claude/teams/ruth-team/infra/` (matches structural pattern), will not write Dockerfile until Monte/Herald answers land.
- Shutdown initiated at operator request for context clear.

[CHECKPOINT 2026-04-15 afternoon] Wiki unchanged this session (39 entries — Ruth-team is pre-pattern; candidates will surface after deployment signal). Two GitHub issues filed (#57, #58). One in-repo design doc v1.0 committed-ready (`docs/ruth-team-container-design-2026-04-15.md`). One sensitive doc in `sensitive/` (gitignored, NEVER committed). Three scratchpad updates (team-lead, finn 189→94, brunel 94). Ruth-relay sent via Teams.

[DECISION 2026-04-15] **P1 co-design adopted for ruth-team** — Ruth in the loop via operator-relayed Teams messages. Draft → review → translate → send pattern. Alternative (speculative design + present) rejected because Ruth's consent is load-bearing for a team-for-a-human, and because we have limited observation surface on her actual work (only ~1 page of Confluence + 4 ITSD tickets visible).

[DECISION 2026-04-15] **Ruth-team interaction channel near-term = SSH + tmux pane.** See top-of-file DECISION for full rationale. Teams integration is a research task, not a near-term dependency.

[LEARNED 2026-04-15] **"Thin digital footprint" ≠ "low output" for human principals.** Ruth's entire visible trail = 4 Jira service requests + 1 Confluence page. But that 1 page is load-bearing infrastructure for apex-research's entire methodology (2 of 3 pillar sources, ~47 extracted chapters, cited 15+ times across ADRs). Her signal is curation quality, not ticket volume. Generalizable: when evaluating a human principal for team-for-a-human design, observe the DOWNSTREAM DEPENDENCIES of their few artifacts, not the artifact count itself. A department lead with 1 page that feeds an AI pipeline is higher-leverage than a department lead with 500 tickets.

[LEARNED 2026-04-15] **Implicit cross-team contracts are everywhere; nobody is told.** Ruth has been feeding apex-research their primary sources for months via an unarticulated handoff (she uploaded V2 docs to Confluence; apex-research ingested them; nobody mediated). Pattern candidate when n=2+: "implicit upstream contracts — downstream AI teams quietly depend on human artifacts with no formal handoff, no feedback loop, and no acknowledgment to the human." Gate for promotion: find a second instance (check raamatukoi-dev, screenwerk, bioforge for similar implicit human→team pipelines).

[LEARNED 2026-04-15] **Stage-B in parallel with decisive Stage A = wasted tokens but not harmful.** Finn ran Stage B (tmux-direct to Schliemann) even though Stage A was already decisive. No damage (Schliemann confirmed the same thing + added detail), but it wasted cycles. Lesson: when giving a two-stage task, spec the gate more clearly ("Stage B ONLY IF Stage A returns ambiguous; stop Stage A first") rather than trusting the agent to recognize decisive evidence.

[LEARNED 2026-04-15] **tmux-direct Enter-after-paste gotcha re-surfaced.** Finn's Stage B brief sat parked at Schliemann's prompt because the Enter keystroke didn't fire. Operator pressed Enter manually. Auto-memory `reference_tmux_direct_cross_team.md` already documents this — confirming the pattern but suggesting it's worth escalating: every tmux-direct brief should include an explicit `send-keys Enter` step, and the runbook should test the full "payload + Enter fires" flow before declaring the technique reliable. Candidate Cal Protocol A: "tmux-direct briefs must verify Enter fires — paste-buffer + paste does not imply submission."

[LEARNED 2026-04-15] **Sensitivity boundary via `.gitignore` + `sensitive/` subdir is operationally simple and robust.** Created ad-hoc mid-session, no predesign. File moved, boundary enforced via repo-root `.gitignore`, all agents acknowledged the rule. No data ever touched a commit object. Generalizable pattern: for any research surface that produces subject-confidential artifacts, create a `sensitive/` subdir under the team root, gitignore it, tell all agents the rule once. Pattern candidate for Cal if this recurs (n=2+).

[LEARNED 2026-04-15] **Dual-track "operational + research probe" requires explicit instrumentation layer — incidental audit logs aren't enough.** Brunel's v1.0 is purely operational. The git-mediated bridge incidentally produces audit trails, but no deliberate telemetry for framework-learning. Volta's lifecycle layer should add §6.5 (or a parallel doc) on observability hooks. Timing: wait for Ruth's Q2/Q3 answers first — "weekly digest" vs "live interaction" reshapes the telemetry surface.

---

## Session: 2026-04-14 evening + 2026-04-15 morning — Jira/GitFlow assessment + prompt-letter audit + issue #43 hand-off

**Team:** Callimachus + Finn + Celes. Brunel/Monte/Volta/Herald/Medici not spawned.

**Rhythm:**

- Startup Steps 1-5 clean (git pulled, runtime clean, TeamCreate success, 23 inboxes restored).
- Cal spawned → health report (Phase 2 active, wiki 38 entries pre-session, WIP items noted), idle.
- Finn spawned for Phase 1 Jira/GitFlow read-and-digest of VJS2 Confluence pages 1163755527 + 1660518403. Confluence REST API v2 access via `~/.claude/.env` creds. Delivered `docs/jira-gitflow-assessment-2026-04-14.md` (5 divergences, 10 questions). Key finding: two pages architecturally incompatible — Source 1 (Kuzmin+Sildnik, v3 2026-02-09) names `production` branch and long-lived `release/*`; Source 2 (Kuzmin, v1 2026-04-13) is textbook 5-branch git-flow with short-lived `release/*` closed by `git flow release finish`. Working theory: Source 2 is a fresh "going forward" doc that may not have reconciled with Source 1.
- Finn write-path ambiguity: his prompt's CRITICAL section listed only `memory/` + `topics/*.md`, but uikit-dev harvest precedent had already established `docs/`. Finn flagged rather than improvising. Team-lead redirected to `docs/`, noted prompt-letter gap for Celes.
- Celes spawned for surgical prompt audit. Delivered Finn diff (3-line change — topics/ clarifier + docs/ bullet with negative boundary) AND cross-cutting check: found second latent gap in Brunel's prompt (internal contradiction — line 130 instructs promotion to `docs/` but MAY WRITE doesn't authorize). Volta/Herald latent, deferred per YAGNI.
- Celes Protocol A draft routed to Cal with n=3 team-lead addendum (`aeneas.md` has same letter-lags-practice shape — Edit/Write restricted but prompt edits routine, e.g. `04522c7`). Cal filed as `wiki/patterns/scope-block-drift-from-practice.md` (#39, 38→39), cross-credited celes + team-lead, framed team-lead instance as "load-bearing". Structural-discipline cluster now 6 members, first multi-gate entry flagged for cluster health monitoring.
- Finn + Brunel diffs both applied verbatim (Brunel got both Change 1 required + Change 2 tightening). Team-lead prompt revision deferred (Celes flagged coordinator-only-by-conviction identity makes mechanical fix unsafe — needs design round, not diff).
- User standing rule added to auto-memory: "when fixing prompts, always consider Cal Protocol A routing as option, not extra step" (see `feedback_prompt_fixes_consider_cal.md`).
- Phase 2 classification HELD: PO knows Source 2 author (Kuzmin), will reconcile two pages externally before wiki filing. Finn idled with full ack.
- **External deliverable:** GitHub issue `Eesti-Raudtee/dev-toolkit#43` created with full digest + 10 questions for the author(s). English kept (per PO). Unassigned. Finn side errand: resolved accountIds to Valeri Kuzmin (Source 2 sole + Source 1 originator, `valeri.kuzmin@evr.ee`) + Andres Sildnik (Source 1 v3 editor, `andres.sildnik@evr.ee`). Issue #43 updated: context paragraph + both Authors lines now link to Jira profiles by name instead of accountIds.
- Shift-assessment comment posted by team-lead to issue #43: wholesale adoption = major (git-flow CLI is ~80% of friction + Jira-as-source-of-truth reversal); selective adoption of process patterns (2-day cap, reverse-transition, bug-vs-rework, weekly cadence) = medium and worth doing; git-flow-only = high shift for low payoff.
- All three agents shut down at session close. Inboxes persisted, state committed.

[CHECKPOINT] Wiki 38 → 39 entries. Two prompts revised surgically (Finn, Brunel). One GitHub issue created (dev-toolkit#43) with 10-question hand-off to VJS2 author. Team-lead prompt + broader 12-prompt audit both on Celes queue pending PO greenlight. Phase 2 Jira/GitFlow classification parked.

[LEARNED 2026-04-15] **Scope-block drift is a three-instance pattern.** Celes's "read 2-3 more prompts" move after the Finn fix caught Brunel AND surfaced the team-lead variant via my own n=3 addendum. The audit-beyond-the-ticket discipline is a repeatable move worth codifying as a Celes workflow step rather than relying on her judgment each time.

[LEARNED 2026-04-15] **accountId → identity lookup is a one-call side errand.** Jira REST API v3 `/user?accountId=X` endpoint resolved both Kuzmin and Sildnik including emails (EVR Atlassian instance defaults to in-org email visibility). Cheap enough to include in any future multi-page Confluence harvest's provenance section as names-not-ids from the start.

[LEARNED 2026-04-15] **External-system assessment digests are high-leverage when paired with a GitHub issue hand-off.** The digest becomes PO-facing reconciliation material the moment it's lifted into an issue — author gets a clean question list, PO gets a structured conversation. Pattern worth repeating for future Confluence audits.

[LEARNED 2026-04-15] **Team-lead prompts resist mechanical fixes — "identity load-bearing" sections need design rounds.** Celes caught that the naive `prompts/` → ALLOWED Edit/Write addition would undermine coordinator-only-by-conviction discipline. Right fix is "allowed ONLY for applying specialist-proposed revisions, never originate" — a design, not a diff. Generalizable: any FORBIDDEN-section item that encodes identity (not just scope) needs review, not surgery.

[DEFERRED 2026-04-15] **Team-lead prompt revision.** Celes on queue for design round. Proposed shape in her scratchpad.

[DEFERRED 2026-04-15] **Broader 12-prompt scope-block audit.** Celes offered ~1h pass; not started this session.

---

## Session: 2026-04-14 (midday) — Cleanup + Volta audit + Finn uikit-dev harvest

**Team:** Callimachus + Volta + Finn. Brunel/Monte/Celes/Herald/Medici not spawned (not needed for this scope). Aalto contacted once via tmux-direct (login code paste, pre-harvest).

**Rhythm:**

- Startup Steps 1-5 executed cleanly (git pull, TeamCreate, 23 inboxes restored).
- Janitorial discovery: `project-memory/` (36 files, ~175KB of per-user auto-memory) + `.project-dir-name` marker found untracked in repo — output of a prior Volta `persist-project-state.sh` dry-run against workstation substrate. Team-lead deleted both before commit.
- Wiki batch committed (`441be40`) — 3 pending Cal entries (tmux pane labels/format + librarian growth curves) plus index and scratchpad updates.
- Cal spawned → filed `wiki/gotchas/persist-project-state-leaks-per-user-memory.md` (#38), committed `f8cb4e8`.
- Volta spawned → delivered `docs/persist-coverage-audit-2026-04-14.md`, committed `37a0833`. Mitigation chosen: Option (c) target-dir refusal with `git check-ignore` opt-in, shared helper. Four structural-discipline flags surfaced, two ship-session blockers (Flag 1, Flag 3).
- PO pivot mid-session: pull from `Eesti-Raudtee/evr-ui-kit`. Main updated (4 commits, 72 files, +207K lines Figma pipeline). Develop branch checked out locally, 15 commits touching `.claude/teams/uikit-dev/` since last cross-read.
- Finn spawned → harvested uikit-dev develop, delivered `docs/uikit-dev-harvest-2026-04-14.md`, committed `d805b5c`. Key finds: A7 latent bug in FR's own `restore-inboxes.sh` (inline jq), A10 ephemeral-snapshot MANIFEST as empirical reference for Volta's ship session, B3 wiki governance divergence framing, B7 pane-labels gotcha is root-cause confirmation not n=2.
- Ship-session split applied: Fix session (F1 jq extraction + F2 nomenclature rename, surgical, near-term) vs Design session (D1-D7 full persist-coverage ship, focused framework-research session). Committed `76c7820`.
- QoL fix: `core.safecrlf = false` set globally to silence LF/CRLF chatter.
- Side task: cloned `mitselek/bigbook` into `~/Documents/github/.mmp/bigbook` (unrelated to FR, delegated housekeeping).
- 5 commits pushed to origin/main: `441be40`, `f8cb4e8`, `37a0833`, `d805b5c`, `76c7820`.

[CHECKPOINT] Wiki 37 → 38 entries. Two new doc artifacts in `docs/`. Ship-session backlog split and indexed. Six Aalto questions queued and deferred. Scratchpad updated with F/D session split, two new [LEARNED] entries, 6-question Aalto deferral.

[LEARNED 2026-04-14] **Restraint compounds: when safety submissions produce pattern-candidate byproducts, flag them and move on — don't draft them same-session.** Cal's filing pattern this session (gotcha #38 + 3 Cal candidates queued for post-freeze) is the second time restraint during freeze yielded a cleaner pattern surface than an impatient draft would have. Generalizable: the discipline of "I see the pattern, I will not draft it now" is itself information-preserving.

[WARNING] **Team-lead coordinator-only discipline slipped early in the session.** Before Cal/Volta/Finn were spawned, team-lead did execution work (tmux-direct paste to Aalto, bigbook clone, initial clutter deletion, wiki batch commit, reading persist script headers). The scripts-reading specifically was Volta's territory; the user correctly flagged the slippage. Post-slippage the discipline held. Going forward: prefer spawn-before-act even for cheap one-offs during warmup.

---

## Session: 2026-04-13 (afternoon) — Phase 1 steal-back + Phase 2 machine-id rename

**Team:** Callimachus + Celes + Herald. Brunel/Monte/Finn/Volta/Medici not spawned (not needed for this scope).

**Rhythm:**

- Cal prepared 377-line steal-back inventory (12 patterns, 10 patches after consolidation)
- Celes drafted coordinated patch for Cal's prompt
- Cross-read gate with Cal caught 2 bugs (Protocol A step ordering + Batch Intake heading depth)
- Phase 1 committed `04522c7`, pushed
- Herald surveyed `types/t09-protocols.ts` → 2 string-literal changes, no programmatic consumers → clean proposal
- Celes + Cal split Phase 2 work: Celes on prompt+roster, Cal on 28 wiki frontmatters (sed batch)
- Team-lead did `git mv` for state file
- Phase 2 committed `ca0e56f`, pushed
- Schliemann briefed via tmux-direct at session close

[CHECKPOINT] Wiki: 24 → 28 entries. Cal's prompt ported 10 steal-back patterns + Path Convention. Common-prompt has routing table mirror. Types file renamed. All `oracle`/`Oracle` occurrences in Cal/Erato prompts, wiki, roster, types are either renamed or intentionally preserved (literary lore, Oracle APEX platform references).

[LEARNED] **"Sole writer" rules make batch operations trivially safe.** Cal's wiki is sole-writer, so a single `sed` pass across 28 files is verifiable by grep with no coordination overhead. Prompts + roster took cross-read gates because multiple agents could interpret rename scope differently.

[LEARNED] **Scratchpad Protocol A sweep is worth 15 minutes.** Celes's self-review of her own scratchpad yielded 4 submissions Cal immediately filed. The lever is the individual agent's judgment, not the librarian's — the librarian doesn't mine, the agent harvests. Future pattern: at session end, each agent does a quick scratchpad sweep before shutdown.

---

## Previous session — 2026-04-13 morning — Phase 1 Oracle→Librarian design work + Eratosthenes deployment

**Team:** Brunel + Callimachus + Celes. Finn/Herald/Monte/Volta/Medici not spawned.

[CHECKPOINT] Wiki: 20 → 24 entries (4 new in structural-discipline cluster). Framework-research commit 86e5e98. apex-research PRs #57 (initial deployment) + #58 (Path Convention fix) merged. Eratosthenes v2.7.1 live.

[LEARNED] **First-boot of any new Librarian is a de-facto QA pass for the prompt.** Eratosthenes hit the path-ambiguity bug within hours of PR #57 merge. Latent equivalents exist in Callimachus but stayed dormant because his container never triggered them. Next Librarian replication: budget first-boot debugging time AND audit BOTH new and source prompts for latent path/reference issues. Plan for it as a deliberate gate.

[LEARNED] **Credit the gate, not the gate-runner** (Celes via Brunel). Pre-merge cross-read gate caught the Protocol A/B field-set bug that would have shipped silently broken. Structural gates > individual heroism. Reproducible at scale; heroism isn't.

[LEARNED] **Re-implementation as design forcing function.** Eratosthenes wasn't just "a second Cal" — re-implementing Cal's design in a different context surfaced 11 stealback patterns flowing BACK to Callimachus plus latent bugs that were invisible in Cal's deployed prompt. The mechanism: re-implementation forces re-justification of implicit decisions, exposing assumptions. Repeatable for any multi-agent role, not just Librarians. Budget for bidirectional lesson flow from day one.

[LEARNED] **Synthesis observations are byproducts of execution discipline, not outputs of synthesis effort** (Cal). The session's highest-value insight (gate-mapping for structural-discipline cluster) was NOT an intentional goal. It emerged from the discipline of writing tight cross-references across 4 wiki entries. The discipline produced the insight; the insight would not have produced the discipline. Generalizable: value accumulates from disciplined consistency, not targeted optimization.

[LEARNED] **Fields are operational, not decorative** (Celes). Surfaced twice in one session as the same root-cause family: `source-agent` → `source-agents` schema vs rename distinction + Protocol A/B shape lift bug. Both errors treated typed-contract fields as cosmetic when they were load-bearing inputs to specific behaviors. Rule: when you see a numbered list of fields in a protocol shape, assume each field is wired to a specific behavior; ask "what fails if this field is missing?" before trimming/renaming.

[LEARNED] **Prudent pause beats permission grant** (Cal). Rule-erosion failure mode: granting yourself a reasonable exception to a discipline. Permission grant removes one blocker, not all of them. Even after authorization is granted, check if direction is in flight before acting. 3 data points on 2026-04-13 (1 negative self-catch, 2 positive holds).

[LEARNED] **Batch-acknowledge convention is infrastructure, not politeness.** Leading every report with explicit directive acknowledgments lets crossed async messages resolve within one round-trip. Worth codifying framework-wide.

[PATTERN] **Dual-sourcing defends against DRY erosion.** The "What goes to X vs Y" routing table lives in BOTH common-prompt.md and eratosthenes.md by design — different audiences, different context, same content. Active defense paragraph prevents future maintainers from "fixing" the duplication. DRY is a code principle, not a prompt-engineering principle.

[PATTERN] **Three-source convergence signals real pattern families.** Pass 1/Pass 2, within-doc rename grep, protocol-shapes-typed-contracts — three independent observers, three different scopes, one root cause (silent structural drift at typed-contract level). Stronger than two-source signal; candidate for Protocol C promotion.

[GOTCHA] **Dual `.claude/teams/<team>/` dirs** — runtime (ephemeral, $HOME) vs repo (durable, $REPO). Prompts must anchor paths to avoid silent Write-to-runtime failures. Filed as wiki gotcha entry. Eratosthenes hit this live; Cal is latent.

[WARNING] **Path bug rediscovery risk.** Eratosthenes's scratchpad has the [LEARNED 2026-04-13] about paths, but on next session restart he re-reads his (now v2.7.1) prompt which has the Path Convention section baked in. Verify both on next apex-research session start.

---

## Carryovers from prior sessions

[DECISION] Bioforge-dev team: 4-character Cathedral-lite — Humboldt (TL/opus), Merian (RED/sonnet), Linnaeus (GREEN/sonnet), Cuvier (PURPLE/opus). Naturalists of the Enlightenment lore. Designed by Celes, research by Finn.
[DECISION] "Cathedral-lite" = Cathedral tier with ARCHITECT merged into team-lead. Valid for single-repo, single-language, single-pipeline projects.
[WIP] Discussion #56 actionable items not yet assigned as tasks:

- Provider outage emergency protocol (Monte, T04)
- Sidecar/peer framework in T06 (Brunel+Monte)
- Contract enforcement mechanism (Herald)
- Platform/provider separation in T02 (Finn)
[WIP] Celes assessment of Cal's first-day performance (carried from last session)
[DEFERRED] Finn model inventory re-survey (uikit-dev missing from baseline count)
[DEFERRED] **Timestamping convention — promote to T03 + investigate cheaper implementation.** Session 2026-04-13: the `[YYYY-MM-DD HH:MM]` prefix convention (currently per-team in common-prompt) proved load-bearing for tracking crossed messages, ordering sub-second dispatches, and debugging coordination. Two actions: (1) propose promotion to framework-wide rule in T03 (Communication) so cross-team threads stay legible; (2) investigate cheaper timestamping — current agent workflow requires `date '+%Y-%m-%d %H:%M'` bash call before every SendMessage, which takes noticeable time per message. Options to explore: auto-injection at message send (framework-level, zero agent cost), cached timestamp with staleness check, or lighter format (HH:MM only for intra-session, full only when crossing date boundaries). Discuss with Herald (protocol) and Brunel (infrastructure).

---

## Session: 2026-04-10 (evening) — Bioforge-dev roster design

[LEARNED] **Crashed session recovery is possible.** Bioforge session (4dd471b6) with 175 anonymous subagents was recoverable — session files preserved at /tmp/bioforge-session-backup/. The session was started from /home/michelek (not project dir), which placed files under -home-michelek project context.

[LEARNED] **Celes designs fast when reference material exists.** Raamatukoi-dev XP design served as structural template — Celes adapted it to bioforge in one pass. Having prior art accelerates roster design significantly.

[PATTERN] **Cathedral-lite for single-pipeline projects:** When domain distance is Low (single repo, single language), ARCHITECT merges into team-lead with no parallelism loss. Team size drops from 9 (raamatukoi) to 4. Reusable pattern.

[CHECKPOINT] Bioforge-dev roster committed to: ai-teams (9b904f1) + bioforge repo (bdb0643). Team files at .claude/teams/bioforge-dev/. Humboldt session already running from recovered bioforge session.

## Previous session notes

- 2026-04-10 (day): Discussion #56 complete, wiki 4→20, Cal proved as synthesis authority
- 2026-04-09: Raamatukoi-dev designed+deployed, Cal bootstrapped, post-crash respawn pattern
