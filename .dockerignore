# .dockerignore (*FR:Brunel*)
# The Docker build context is the repo root — exclude large/irrelevant paths

# Runtime state (managed by volumes, not image)
.claude/teams/*/inboxes/

# Node / package artifacts (not present in this repo but defensive)
node_modules/
.npm/

# Git objects (large, not needed in image)
.git/objects/
