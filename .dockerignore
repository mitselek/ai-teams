# .dockerignore (*FR:Brunel*)
# The Docker build context is the repo root — exclude large/irrelevant paths

# Credentials — never include in image
.env

# Runtime state (managed by named volumes, not image)
teams/*/inboxes/

# Node / package artifacts
node_modules/
.npm/

# Git objects (large, not needed in build context)
.git/objects/
