# Hopper — scratchpad (*FR:Hopper*)

## Summary (lines 1-15 — always read on startup)
- **S51 (2026-06-15):** Task #1 HUB HEALTH CHECK DONE — hub UP+HEALTHY on prod-llm. Customer-channel evidence: ping ok+identity=framework-research; SSH host-key SHA256:CNcFjOxr8vREOueOS8nxJN8W3LaQHet62du+PHyK13U EXACT match (genuine, no MITM); uptime_s=233293 (~2.7d, continuous since S50, restart:unless-stopped held); forced-command isolation intact (echo PROBE ignored); state PERSISTED across CLI bump 2.1.175→2.1.177 (sm-state vol): grants_in=[fr-test, apex-research], grants_out=[apex-research, fr-test]. FLAGGED to TL: waiting_for_me={apex-research:2} (2 uncollected — needs courier collect/ack); apex-research fresh grant_in 2026-06-15T06:48:26Z (ephemeral-key re-route, expect Task #2 re-register). NOTE: ping-response "fingerprint" field (SHA256:nkmhWN...DpyQ) is protocol-level §5.1, NOT the SSH host key. Could NOT host-probe docker (michelek@) — sm_framework-research is forced-command-bound, no host-admin key staged in agent ctx; customer-channel evidence conclusive alone.
- **S51 HARD GATE (PO Mihkel via TL 10:33):** I am executor for Task #5 (apex container hardening: service supervisor + build-time courier key) but MUST NOT execute anything that restarts apex's container or docker-exec's changes onto it — BLOCKED pending explicit PO approval + scheduled window. Await Brunel's design + TL's explicit go (carries PO sign-off). EXCEPTION still permitted: apex key-rotation = hub-side `sm-register apex-research <new-key>` (no apex-container restart) — that I may still do (Task #2). Standing by; gate acknowledged to TL.
- **S51 Task #4 FR courier daemon — PERMITTED (TL 10:39):** running the FR courier daemon is FR's OWN infra on our dev box, NOT apex → hard gate does NOT apply. Brunel will hand me a run dispatch. RUN SEQUENCE: (1) live `--ping` (pure, read-only) → (2) confirm outbox clean (Herald) → (3) `--once` → (4) persistent loop, with Brunel checkpointing to TL BEFORE the persistent loop starts. Operate per Brunel's dispatch. (Task #5 apex hardening stays hard-gated.) **LIVE 2026-06-15 11:02 — PO GO via Brunel run dispatch 11:00 (Tier-M). Launched `./start-fr-courier.ps1` (PowerShell, ghost-bridge dir). FR-side GO-LIVE milestone. VERIFIED 4/4: (1) pidfile=35568; (2) Get-Process ALIVE name=python; (3) lock held ~/.stationmaster/framework-research/courier.lock; (4) first + 2nd poll cycle clean (banner "FR session courier up: team=framework-research target=sm@10.100.136.162 interval=30s outboxes=['apex-bridge'] default_inbox=team-lead", no WARN/ERROR; CPU climbed across cycles = polling). NOTE: daemon logs to STDERR → fr-courier.log.err (stdout fr-courier.log empty by design; ref.info/warn/err all stderr). Quiet cycles emit no log line — only events log. STOP PATH: `./stop-fr-courier.ps1` (drain-on-shutdown final cycle + atexit lock release, no zombie); guaranteed drain = `python fr-courier-daemon.py --config fr-courier.config.json --drain-once` then stop. apex courier DOWN (their S52 wrap) → any FR→apex deposit queues on hub at-least-once = EXPECTED not error. Reported pid+log+lock to Brunel+TL.** **RESTART 11:15 (Brunel dispatch 11:14, Tier-M) — routing fix: ghost_outboxes "apex-bridge"→"apex-research-bridge" (strips to apex-research, the registered team; old stripped to "apex"→E_UNKNOWN_TEAM). Config-only, no wrapper change. stop(35568)+start(NEW pid 20664). VERIFIED: alive; lock REACQUIRED via stale-lock-reclaim-on-tasklist ("WARN lock: reclaiming stale lock" — confirms no-zombie self-heal works); banner now outboxes=['apex-research-bridge']. **LIVE ROUTING PROOF (better than banner): a RETAINED spool consignment 20260615T080907604798-2f36d48d.json that had been looping on E_UNKNOWN_TEAM under the old map got "deposited 1/1 (accepted/duplicate); removed" on first patched cycle → spool now EMPTY. Fix proven end-to-end vs live hub.** TWO CORRECTIONS to Brunel's dispatch premise I flagged: (a) spool was NOT empty (he said it was) — there was a real retained reject; no loss (retained-not-dropped held it), fix resolved it; (b) on STOP, atexit lock-release did NOT fire (terminate killed mid-drain → stale lock left) — harmless because next start reclaims via tasklist, but the "clean atexit release on terminate" claim didn't hold on Windows-terminate path. Both reported.**
- **Current state:** S50 — ALL Hopper operator work DONE. Tasks #3 (T6.a gate) + #7 (deploy) complete+accepted. #4 operator portion (all 5 registrations) DONE. Hub LIVE+healthy on prod-llm, 5 keys: alpha/beta (scratch), framework-research (production, MY key), fr-test (Herald's key, registered per Aen intercept — his .pub comment says framework-research but routing=fr-test via forced command), apex-research (cross-team, PO-sanctioned, ephemeral-rotate). WRAP-READY.
- **Active items:** none for me — Herald drives the courier round-trip (#4 acceptance, his ownership). Awaiting session-end shutdown.
- **Key decisions this session:** Aen re-adjudicated T6.a gate-of-record = prod-llm host-fs (customer substrate), in-container = confirmatory; volume REUSE (no Tier-D clear, gate intent = unknown-provenance, mine was enumerated-inert); Task #4 constraint-lifted back ON (17:28, reversing the deferral).
- **Carry-forward:** ALL my operator work DONE — WRAP-READY. (1) Task #4 acceptance (courier deposit/collect/ack round-trip) is HERALD's, in flight; he has framework-research (my key) + fr-test (his mrx7 key ~/.ssh/sm_fr_test) + apex-research as targets, hub sm@10.100.136.162:2222 real-ssh verified, lazy-registry sequencing note relayed. (2) smoke-test.sh test-ordering bug (E_NOGRANT vs E_UNKNOWN_TEAM) — Brunel owns, post-freeze; NOT a hub defect. (3) apex-research ephemeral key ROTATES on their container restart → expect re-registration requests (routine idempotent sm-register); sturdier infra-mount queued for PO. (4) prod-llm ~/stationmaster (HEAD 909bbe9); image stationmaster:1.0.0 healthy; FR private key ~/.ssh/sm_framework-research on this box. (5) Cal filed both gotchas (wiki 130 ssh-keygen-A-ignores-f; 132 nologin-defeats-forced-command). **LESSONS:** (a) lazy-registry — a team is a valid deposit target only after it connects once, not at sm-register time [Brunel S51-doc nuance: "registered" = operator-registered vs registry-known; deposit keys off the latter]. (b) **Cal-distilled testing rule (1 instance from pattern-promotion, watching for n=3): a forced-command-over-ssh service needs an OVER-REAL-TRANSPORT acceptance test — build-green AND direct-unit-test-green both pass straight through protocol-layer defects (host-key-gen + nologin both surfaced ONLY at real-ssh smoke).**

---

First-spawn 2026-05-20. Maintain under 100 lines; prune stale entries; promote pattern-grade to Callimachus via Protocol A.

## Substrate facts — apex-research

[LEARNED — substrate, apex-research] Host-user SSH: `ssh -T dev@100.96.54.170` (default key `~/.ssh/id_ed25519`, port 22). Source-of-truth: `~/bin/rc-deployments.json` entry `num:"1"`. `-T` is load-bearing for non-interactive ops (suppresses TTY allocation, keeps stdout clean for parsing).

[LEARNED — substrate, apex-research] Container-user SSH: `ssh -i ~/.ssh/id_ed25519_apex -p 2222 ai-teams@100.96.54.170`. Source-of-truth: `~/bin/rc-deployments.json` entry `num:"2"`. Same physical host as the host-user SSH (`num:"1"`); two SSH shapes for host vs container access.

[LEARNED — substrate, apex-research] `$COMPOSE_DIR` = `/home/dev/github/apex-migration-research`. Discovered via P1.2 find-probe (Tier R); confirmed authoritatively via `docker inspect` label `com.docker.compose.project.working_dir`, NOT via FR's `designs/deployed/apex-research/container/` path (that's design-as-shipped, not operational substrate).

[LEARNED — substrate, apex-research] Operational compose-yml diverges from FR design: substrate `docker-compose.yml` exposes `SSH_PUBLIC_KEY_3` slot; FR's `designs/deployed/apex-research/container/docker-compose.yml:47-60` shows only SLOTS 1+2. Per PO via Aen 2026-05-20 17:37: SLOT 3 reserved for future key, never populated — though backup `.env` (frozen 2026-04-29) had `SSH_PUBLIC_KEY_3=...rc-connect` (apparent contradiction; documented in ops-log, not adjudicated).

[LEARNED — substrate, apex-research] Host user `dev`: uid 1000, gid 1000, in groups `sudo + docker + ollama + plugdev + ...`. `docker inspect` and `docker compose config` work unprivileged.

[LEARNED — substrate, apex-research] AS OF 2026-05-21T09:18 (post-Phase-2 recreate success): **original PO ask ACHIEVED**. Container recreated; 3 SSH keys (PO + Aleksandr + rc-connect) installed by entrypoint Step 7 from canonical `.env`; KEY_COUNT=3 confirmed in entrypoint logs; sshd on port 2222 accepting all 3 keys. Operational compose-yml amended (P4.05) to declare `GH_TOKEN=${GH_TOKEN:-}` in apex-research env block — GH_TOKEN preserved through recreate per PO Option B direction. All declared tokens in Config.Env (GITHUB_TOKEN, GH_TOKEN, ATLASSIAN_*, ANTHROPIC_API_KEY-empty). All 3 named volumes preserved across recreate. Container is recreate-safe — next maintenance recreate installs same keys cleanly from same .env. Backup `.env` at $BACKUP_DIR + compose-yml backup `docker-compose.yml.bak.20260521-091347` both untouched as rollback artifacts. **The "any recreate = multi-system credential loss" gotcha is fully RESOLVED post-Phase-2.**

[GOTCHA — substrate, apex-research, historical] Pre-2026-05-20T19:17 the substrate was in degraded state: no `.env` at `$COMPOSE_DIR`; container survived on Config.Env baked pre-2026-04-29-fresh-clone; any recreate = full SSH lockout + credential cluster loss (GITHUB_TOKEN, ATLASSIAN_API_TOKEN, TUNNEL_TOKEN). Original r3 Phase-2 sanction was rescinded by Aen 17:34 due to this. **Fully RESOLVED 2026-05-21T09:18 post-Phase-2 recreate** — substrate now in canonical state with .env + amended compose-yml + recreate-safe Config.Env. Historical reference for the dispatch-arc audit trail; do NOT treat as current.

[LEARNED — substrate, apex-research] Operational compose-yml's apex-research env block (post-2026-05-21T09:18 P4.05 amendment) declares 15 vars: HOME, REPO_URL, SOURCE_REPO_URL, GITHUB_TOKEN, **GH_TOKEN (added P4.05)**, ANTHROPIC_API_KEY, SSH_PUBLIC_KEY, SSH_PUBLIC_KEY_2, SSH_PUBLIC_KEY_3, NODE_EXTRA_CA_CERTS, CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS, TEAM_NAME, ATLASSIAN_EMAIL, ATLASSIAN_API_TOKEN, ATLASSIAN_BASE_URL. FR-design `designs/deployed/apex-research/container/docker-compose.yml:47-60` has fewer slots (no SLOT 3, no GH_TOKEN). Sub-shape E drift still exists between Layer 1 (FR design) and Layer 2 (operational); future FR-design update may want to absorb the operational SLOT 3 + GH_TOKEN additions.

[GOTCHA — substrate, apex-research] Backup `.env` at `$BACKUP_DIR/.env` (frozen 2026-04-29) has `SSH_PUBLIC_KEY_3=ssh-ed25519 ... rc-connect` UNQUOTED (other SSH_PUBLIC_KEY entries are double-quoted; SLOT 3 is the only unquoted multi-word value). With `set -e` enabled, bash `source` of this file fails (parses `SSH_PUBLIC_KEY_3=ssh-ed25519` as assignment then tries to exec the rest). Workaround: use `grep+cut+strip-quotes` per-token instead of `source` — substrate-truthful and robust to malformed backup syntax. Pattern shape from P3.6 amendment-1.

## Generalizable patterns

[LEARNED — discipline, revert-on-invalidated-premise] Mid-conversation 2026-05-20: PO surfaced apparent wrong-host concern via Aen at 17:47-17:58 (his transient mis-read of `~/bin/rc-deployments.json`). I executed the discipline-honoring cleanup amendment when instructed by Aen — relabeled scratchpad substrate entries, appended ops-log correction at 18:05 (append-only, not in-place). PO re-checked registry himself at 18:08 and confirmed the original 17:09 substrate-target was correct. Aen instructed full revert at 18:19. **Lesson: even after discipline-honoring artifact updates (append-only ops-log + scratchpad amendments), be willing to revert when the original premise is invalidated. Discipline serves accuracy, not consistency-with-prior-correction.** The append-only ops-log preserves the full audit-trail of confusion-and-resolution (17:09 original + 18:05 first correction + 18:XX revert-correction); scratchpad reverts to pre-amendment state because scratchpad is working-memory, not append-only audit log. Sub-shape F (registry-entry-choice-from-first-match) was withdrawn from the catalog — no valid instance from this dispatch.

[LEARNED] **Within-dispatch-agency JSON-labels dump pattern:** when a single-label `docker inspect ... --format '{{index .Config.Labels "key"}}'` probe returns empty, run `docker inspect ... --format '{{json .Config.Labels}}'` as a Tier R follow-up. Disambiguates three failure modes in one round-trip: (a) label-key-missing, (b) container-not-running, (c) command-malformed. Substrate-truth-cheap, well within Tier R within-dispatch-agency scope.

[LEARNED] **Substrate-truth probe sequencing for env-source diagnosis:** container Config.Env > docker compose config (rendered output) > filesystem search (`.env*`, `*.env`, `find` in host home). Cheap-first, online-truth-first. Each layer reveals what the next can't: Config.Env shows what running container actually has; compose config shows what NEXT compose-up would resolve; filesystem search catches env-file-via-flag or alt-naming variants.

[LEARNED — generalized discipline] **On any first-ever dispatch against a new substrate**, scratchpad-capture as `[LEARNED — substrate, <team-name>]` entries the durable operational facts the probes surfaced: host-side filesystem paths, SSH connection shapes (cite `~/bin/rc-deployments.json` entry), file-ownership and writability defaults, named-volume layout, entrypoint-specific behaviors, naming conventions. On subsequent dispatches against the same substrate, READ scratchpad BEFORE probing; only re-probe if a fact looks stale or the dispatch explicitly directs.

[LEARNED — discipline gap, candidate Hopper-Amendment-4] My existing `hopper.md` Diagnostic Discipline "Read Deployed Artifacts Before Executing" section reads **Layer 1 only** (FR design-as-shipped). Insufficient for FR-shipped substrates consumer teams operationalize. **Three-layer substrate-truth discipline:** (1) FR design-as-shipped (`designs/deployed/<team>/container/*`), (2) consumer team operational copy (substrate host's compose dir), (3) running container state (Config.Env, mounted volumes). Each can drift from neighbor. First-dispatch against any substrate → run Tier R probe-suite surfacing all three; subsequent dispatches → read scratchpad first, only re-probe if stale. **Surface to Celes at session wrap as Hopper-Amendment-4, cross-link to ops-log-2026-05.md catalyzing-incident entry.**

[LEARNED] **Discriminator anchored on sub-canonical source** (Brunel-phrasing; A.1/A.2 distinction locked with Brunel 2026-05-21 09:30). Filter regex / lookup key / literal-string anchored on inferred convention, template stub, or assumed-outer-layer-pass-through rather than substrate-truth-anchored grammar. **Sub-shape A.1 (identifier-grammar mismatch):** discriminator regex on identifier names anchors on inferred/template variants instead of canonical identifier grammar (POSIX envvar, Docker Compose label-key, etc.). Instances 1-3 in this dispatch arc: P1.1 michelek (template stub), P1.2a label-key underscore (inferred convention), P3.6 `[A-Z_]+` (digit exclusion missing). Recovery: substrate-truth-diagnostic surfaces actual identifier-grammar via cheap Tier R round-trip (JSON-dump-on-empty + alt-regex-on-discriminator-fail). **Sub-shape A.2 (transit-chain mismatch):** literal traverses multiple parsing layers; inner-layer grammar reinterprets a character outer layers passed through intact. Instance 4: P4.05 awk `}` parsing in PowerShell→base64→bash→awk chain. Recovery: string-concatenation refactor at innermost parser + post-fail diff-vs-backup. **Shared lesson:** verify-substrate-truth-anchored-grammar at the innermost parser BEFORE relying on outer-layer pass-through. Innermost-parser-truth dominates outer-layer-assumption. Both share same recovery posture: surface-back-with-substrate-truth-evidence before silent re-attempt. Cal-Protocol-A submission `wiki/patterns/discriminator-anchored-on-sub-canonical-source.md` staged for session-end (Brunel-phrasing + n=4 catalog + Hopper-defense-pattern + this dispatch arc as catalyzing-incident).

## Local-side gotchas (Windows dev workstation)

[GOTCHA — local dev] PowerShell-on-Windows quoting of nested Go-template format strings via `ssh "remote-cmd"` mangles `\"` escapes. **Workaround used through this dispatch: base64-transit** — build the literal remote command locally, base64-encode, send as `ssh "echo '<b64>' | base64 -d | bash"`. Same pattern Brunel specified for P1.4; generalizes to any remote command with shell metacharacters or nested quotes. (Pattern itself was Brunel-authored for transit safety; deployment lesson here is its second-use generality.)

[GOTCHA — local dev] PowerShell 5.x file-loaded scripts: non-ASCII characters (em-dash `—`, etc.) in `Write-Host` literal strings cause parser failure when file is UTF-8-no-BOM. **Workaround: use ASCII hyphens in script messages.** `(blank-line)` etc. for separators. This is a Windows session-local friction per `feedback_no_windows_substrate_findings.md` — do NOT file as wiki-grade.

[GOTCHA — local dev] PowerShell 5.x lexer mis-parses `[STRING-BRACKET-LITERAL]` inside `Write-Host` strings under some states. **Workaround: parentheses/no-brackets in script messages.** Same windows-substrate-friction class as the em-dash gotcha.

## In-flight dispatches

[OUTCOME — Task #3 + #7 S50 CLOSED 2026-06-12. FULL AUDIT in ops-log-2026-06 (6 entries).] **Task #3 (T6.a gate):** harness `poc/ghost-bridge/t6a-race-harness.py` (Python open('x') + bash set -C, 50-round 2-proc race, mirrors courier.py:773-785). PASS on prod-llm: host-fs ext4-on-LVM (load-bearing, gate-of-record per Aen re-adjudication = prod-llm IS customer substrate) + in-container sm-state volume mount (confirmatory) — 50/50 both primitives both runs, 0 anomalies. Cal gotcha filed: tmpfs-vs-deployment-fs trap (wiki 125). **Task #7 (deploy):** stationmaster hub LIVE+healthy on prod-llm (Debian 13, ext4, sshd:2222, host key on sm-state vol). Caught 2 Brunel artifact defects mid-deploy (NOT patched, surfaced w/ Layer-3 evidence, Brunel fixed both): (1) entrypoint host-key `ssh-keygen -A -f` lands 0 keys OpenSSH 9.2p1 → fixed f022fed (direct ed25519); (2) `sm` shell nologin blocks forced-command → fixed 909bbe9 (/bin/sh). Smoke-test 14/15 — the 1 fail is a TEST-ORDERING ARTIFACT (expects E_NOGRANT, hub correctly returns E_UNKNOWN_TEAM pre-first-connect; registry is lazy-populated on connect not at sm-register) = Brunel test-script bug, NOT hub defect. Volume REUSE (Aen ruling: gate intent=unknown-provenance, mine enumerated-inert). 2 scratch keys alpha/beta left registered+inert for S51. **[GOTCHA×2 candidate Cal]:** ssh-keygen -A+-f prefix fails OpenSSH 9.2p1; nologin shell defeats authorized_keys forced-command (Windows unit-smoke missed — tested sm-shell direct, not via sshd login-shell). **3 layered-gate catches this arc: EXIT-0≠success, transport-green≠protocol-OK, smoke-fail-triage=test-artifact-not-defect.**

## Prior-session archive (pruned at S50 shutdown per 100-line discipline)

[ARCHIVED] S35/S36/S37 dispatch transcripts (apex db-tunnel #6, fr-cma-pilot CF bypass #1, secret-rotation #2) pruned — full records in docs/operations-log-2026-05.md. Durable lessons already promoted: wiki/patterns/discriminator-anchored-on-sub-canonical-source.md + three-layer-substrate-truth-discipline.md (Cal-filed); Layer-0 library-first + probe-design-as-sub-canonical-source (A.3) + L2-unreachable-graceful-degradation captured in those patterns. apex-research substrate facts retained above (host-networked, ssh-keygen behaviors, , etc.).
