# Hopper -- scratchpad (*FR:Hopper*)

## Summary (lines 1-15 -- always read on startup)
- **S56 (2026-06-18) -- SPAWN-UP ONLY; NO DEPLOY EXECUTED. npm flip 2.1.177->2.1.181 PAUSED by PO (autoupdater-vs-pin decision pending).** Absorbed team-lead's 8 FINAL DECIDED DIRECTIONS (read-once, did NOT reopen). Cleared S55-owed Protocol-B read-back to Callimachus on `decisions/courier-must-runtime-discover-team-name` -> CONFIRMED (I was final co-author; Cal folded my 3 confirmations + promoted Brunel's pending-refinement into body; that was the team's LAST open stage-2 gate -> ALL decisions/ now confirmed). **CLI still 2.1.177; FR courier pid 38044 UNTOUCHED on explicit config. Nothing deployed, nothing to roll back.** **CARRY-FORWARD to S57: the unpin (CLI 2.1.177->2.1.181) is STILL PENDING -- gated now on PO's autoupdater-vs-pin decision (NEW gate, on top of the prior PO-go). When it lands it is a Tier D dispatch: I require exact command + reason + expected outcome quoted verbatim. Lifecycle flip-ready on branch fr/unpin-2.1.181-lifecycle (applied AT unpin, not before). Resolver process-liveness must-fix already MERGED on main.**
- **S54 (2026-06-17) -- TASK #4 COMPLETE: 2.1.179 teams-migration probe (throwaway container, P1-P6).** Built+ran disposable `teams-migration-probe:2.1.179` on rc host (dev@100.96.54.170), drove via docker-exec+tmux, auth=(c) `claude login` (OAuth relayed via Aen, code never logged). **6 probes, evidence in docs/teams-migration-probe-findings-2026-06-17.md (+ ops-log 2026-06 18:30 entry; both LOCAL-ONLY, need Aen commit).** P1 (LOAD-BEARING) FAIL: team_name=framework-research IGNORED on disk -- on-disk team = `session-<id>` (random), config.name+member agentIds all session-<id>; NO framework-research dir => courier hardcoded path MUST become runtime name-discovery (THE migration cost). P2/P3 PASS (SendMessage exists+round-trip). P4 PASS (external members[]-injection still honored -> SendMessage routes to ghost member). P5 PASS (real SendMessage writes inboxes/<member>.json; 2.1.179 adds `type` field). P6 (LOAD-BEARING) PASS+CORRECTED: external inbox-file write PROACTIVELY WAKES+delivers to a bare idle session (clean re-test woke ~15s no-nudge; harness drains to []); my FIRST "inert" read was WRONG -- machinery just wasn't armed (inboxes/ didn't exist yet). PO ideal WORKS. NET: members[]-injection + inbox-write-delivery BOTH survive 2.1.178+; only hardcoded team-name breaks. BUILD DEFECT (-> Brunel): ubuntu:24.04 base ships stock `ubuntu`@uid1000 -> `useradd -u1000` collides; fixed w/ `userdel -r ubuntu` prepend. CLEAN TEARDOWN: down -v (container+vol gone), image rmi'd (leaf), on-host repo copy restored, snapshot rm'd; apex UP 28h untouched, host :2222 only apex. Submitted both findings to Cal (Protocol A). **LESSON [catch-of-session]: disprove-your-own-report held AGAIN -- "P6 inert" reversed once inbox machinery armed; delivery was DEFERRED not absent.** **DISCIPLINE: refused bridge-build (egress 000) + refused host-net-WITH-sshd (collides apex :2222); host-net + no-sshd + docker-exec was the only sound path, Aen-approved.**
- **S53 (2026-06-16) ~11:25Z -- CCR REFERENCE INSTANCE #1 (apex) EXECUTED. DONE_WITH_CONCERNS: rebuild succeeded, NO rollback, all 9 stateful paths survived.** Route = `dev@100.96.54.170`(rc host)→`docker exec apex-research` (the empirical S52 path; container-side tailnet/`:2222`/10.200.13.114 are DOWN -- supersedes briefing candidate list). Build-source DRIFT caught AGAIN (rc clone on stale `fr/apex-container-hardening-s52`, 756 behind, no `deploy/`) → `git checkout main && git pull --ff-only` to 4d68b88 (PR#165 5168e68 + #166 2d6fce1). Tagged rollback `apex-research-claude:pre-20260616-1417`(=old img df21b51) BEFORE build; new img 1786c34 (0 key bytes in history -- secret via `--mount=type=secret`); `up -d --force-recreate` (host-net, volumes preserved, NEVER down -v/git clean). RebuildReport: 9/9 stateful survived=true (6 reported in JSON + 3 -- mcp.json/stationmaster-state/inboxes -- MANUALLY confirmed survived), identity_stable+e2e_ok=true, status=DEGRADED (only warn=data:ea-model, pre-existing /tmp issue) → per playbook NOT FAILED → no rollback. Courier key fp NBq5a/...QsaE unchanged → no re-register. **3 DEPLOY-SURFACE DEFECTS found (health-report.sh+entrypoint @4d68b88, NOT rebuild faults; recommend follow-up CCR PR on apex):** (1) report emits only 6 path_checks vs manifest's 11 -- NOT derived one-check-per-manifest-path, drifts from contract (Component 4 violation); (2) trigger=session-start wrong -- heuristic `uptime<120s` reads HOST uptime (7704064s) under network_mode=host, never sees container boot; (3) entrypoint-apex.sh never calls deploy/startup → on headless `up -d` the report is NEVER auto-emitted (only fires on SSH-login .bashrc auto-tmux), so manifest's "runs on every container start" is FALSE for a quiescent rebuild -- I generated it by running `deploy/health-report.sh --send` directly (deterministic, no Claude launched, apex stayed quiescent). PO-requested follow-up: author host-side `ccr-rebuild.sh` (/home/dev/bin/) mechanizing the playbook. Rollback tag retained. Reported to team-lead; awaiting call on follow-ups.
  - **S53 follow-up A -- PO-attach path VERIFIED:** PO connects `ssh -p 2222 ai-teams@100.96.54.170` (or @10.200.13.114 LAN) with their `mihkel.putrinsh@evr.ee` key (authorized; 3 keys in authorized_keys). sshd on host-net :2222 (0.0.0.0), host key fp SHA256:z1/fXAE...4vA (persistent from S52#5, unchanged). **CATCH:** apex image has NO auto-tmux/auto-claude .bashrc block -> SSH lands in PLAIN BASH, Claude does NOT auto-launch. PO must run `cd ~/workspace && claude` or `tmux new-session -A -s apex -c ~/workspace \; send-keys "claude" Enter`. docker-exec sets no $SSH_CONNECTION so nothing auto-fires that route either. The missing auto-tmux block = 4th follow-up-CCR-PR item (session-launch companion to report-concern #3). Verified mechanically, apex kept quiescent (no long session started).
  - **S53 follow-up B -- ccr-rebuild.sh DONE + APPROVED:** operator tooling mechanizing the proven rebuild sequence. rc host `/home/dev/bin/ccr-rebuild.sh` + FR repo `poc/ccr/ccr-rebuild.sh` (main @ 37a7f2b, byte-identical sha ba92430d), NOT pushed (commit-only). Signature `ccr-rebuild.sh <repo-dir> <compose-service> <image>`. Steps a-f (build-source ff-only gate / tag / build / up --force-recreate / explicit health-report.sh --send + print + rollback one-liner). HARD GUARDS: never down -v, never git clean, abort-on-dirty-tracked, abort-if-no-:latest, abort-if-deploy-absent; set -euo pipefail; bash -n PASS both copies. Overridable CCR_REPORT_USER/CCR_REPORT_WORKDIR. team-lead APPROVED at S53 close.
- **S52 (2026-06-15) 14:31 -- apex hardening COMPLETE + all 5 gaps CLOSED. Image 862c799f / branch c0fb2bac / PR#165 6 commits.** Full arc in ops-log (12:11 route → 12:14 OQ-1 → 12:34 abort-transit-gap → 12:47 abort-GH_TOKEN-regression → 12:58 rebuild#1 → 13:06 hub-register → 13:11 rebuild#2 → 13:17 stale-lock → 13:23/13:28 supervise-defect → 13:39 rebuild#3 set+e VERIFIED → 13:48 5d deposit → 13:52 host-key sourced → 14:01 rebuild#4 collect-transport VERIFIED + inject-CAS → 14:15 sshd-fp-verify-for-PO → 14:31 rebuild#5 sshd-persist VERIFIED). **SCORECARD ALL GREEN:** ASK-1 supervisor both halves (set+e fix verified); ASK-2 courier key e2e (fp NBq5a/...QsaE, hub-registered); inbox-dir; GH_TOKEN-preserve; dashboard 200; lock-pre-clean; host-key-pin→collect-transport (fp CNcFj...K13U; PROVEN by 08:15 consignment full collect→inject→ack); round-trip 5d GREEN (08:15 delivery); container-sshd-host-key PERSISTED (rebuild#5 Step-6c: NEW permanent fp **SHA256:z1/fXAE8gRCfUrKnIJ8PM5bcO/pBhAMrZESNHbmh4vA root@apex-research**, on ~/.claude/ssh-host-keys st_dev=65024 root:root700, STABILITY PROVEN 2nd-recreate-fp-unchanged). **5 FINDINGS all surfaced-not-improvised + routed:** transit-gap, GH_TOKEN-regression, supervise-set-e (Brunel fixed), inject-CAS-down-agent (Herald gate-4, QUEUED courier-ref + boot_id, NOT this session), sshd-host-key-ephemeral (Brunel fixed rebuild#5). + lock-reclaim pid-alias gotcha (Herald n=2, honest caveat). **PO ACTION PENDING: `ssh-keygen -R '[100.96.54.170]:2222'` + accept new fp z1/fXAE...4vA.** **NEXT (Aen): apex-startup cutover (drop startup.md 4e/5) + wake apex.** OP-NOTE: a55da27 redelivers+fails-inject every 30s (harmless) till apex agent up drains team-lead.json. apex DOWN throughout (docker-exec read-only; recreates don't wake agent). KEEP STASH@{0} RECOVERABLE. HUB-ADMIN=`ssh -i ~/.ssh/id_ed25519_apex michelek@10.100.136.162` /home/michelek/stationmaster; FR-deposit=`ssh -i ~/.ssh/sm_framework-research -p 2222 sm@10.100.136.162`. **STOOD DOWN -- all dispatched apex work complete; idle pending cutover/wake ask or session-end.**
- **S52 (2026-06-15) -- apex Tier D rebuild dispatch ABORTED PRE-EXECUTION (hard-gate-on-drift, L1↔L2 transit gap).** Brunel dispatched the rebuild (PO greenlight + quiescence + key-name=stationmaster_apex all OK). My Tier D pre-flight (Tier R grep-probes on rc-host build source `/home/dev/github/apex-migration-research`) found his hardening edits NOT THERE: supervise()/seed-copy/`id=courier_key`/compose-secret all grep=0; rc last commit d9074bb. Edits live ONLY as 5 UNCOMMITTED ` M` files in LOCAL Windows checkout `~/Documents/github/apex-migration-research/`. Build now = green build of OLD image, zero hardening. STOPPED, ran no mutating cmd, did NOT push (Aen's git) or scp (silent-broadening). Surfaced to Brunel+Aen w/ evidence. **STATE 12:40: dispatch FORMALLY SUSPENDED by Brunel; Aen confirmed abort correct + owns git transit resolution (PO call -- apex's repo).** Both corrections folded: corrected 4-step seq = (1)`[ -f ./stationmaster_apex ] || ssh-keygen -t ed25519 -f ./stationmaster_apex -N "" -C "apex-research"` (2)`DOCKER_BUILDKIT=1 docker compose build` (3)`docker compose up -d --force-recreate` (4)`cat ./stationmaster_apex.pub`+report generated-new-vs-reused → Brunel sm-registers NEW-only. Durability CONFIRMED (Tier R 12:38): build ctx = ext4 LVM (durable), key absent now→first build generates, NO git clean in flow, no wrapper scripts. ORDERING FLAG to Brunel: .gitignore edit must land BEFORE keygen (naturally safe, keygen=step1 post-transit). **NEXT: Brunel RE-ARMS only after Aen confirms (1) edits on rc-host build source + (2) 5-marker grep gate passes → then I re-run grep gate → 4-step seq → 4-point verify.** PO greenlight+window NOT spent. ops-log 12:34. STOOD DOWN, holding.
- **S52 (2026-06-15) -- OQ-1 RESOLVED (Tier R, Aen part-3, read-only via host exec route).** Live courier.json = SINGLE file `/home/ai-teams/workspace/teams/apex-research/stationmaster/courier.json` (no alts; find-swept) = the supervisor `--config` arg for Brunel §3. Shape: ssh_target=sm@10.100.136.162(FR hub), **ssh_key=`~/.ssh/stationmaster_apex`** (NOT plan §4's `sm_apex-research` -- naming mismatch, Brunel's bake must reconcile), inboxes_dir+state_dir under `~/.claude/`(persistent), ghost_outboxes=[framework-research-bridge]. ASK-2 finding: ssh_key on ephemeral `~/.ssh`(F5) confirms rebuild-loses-key; state on persistent `~/.claude` survives. Surfaced to Aen+Brunel, not patched. ops-log 12:14. AFTER OQ-1: hold for window per Aen; gates (b)PO-go +(c)window still block -- Aen surfacing (b) to PO. Do NOT originate further apex work.
- **S52 (2026-06-15) -- apex host-route probe DONE (Tier R, Aen-dispatched), §1(b) gate of hardening plan.** Host-side docker-exec route CONFIRMED LIVE: `ssh -T dev@100.96.54.170` (rc:22, registry num:"1", default key) → host `paarisprogemis-fyysiline` reachable (apex *container's* tailscale logged-out is irrelevant; the RC HOST's tailnet addr answers fine). `docker ps` → apex-research Up 3 weeks. `docker exec apex-research id` → **uid=0(root)** = exec route live AND lands root (matches F4: root needed to edit root-owned entrypoint). This is plan §1(b)(ii); gate (b) satisfiable via host route -- tailnet re-login NOT required. **DRIFT SURFACED to TL (not patched):** live NetworkMode=`host`, NOT the FR-design bridge+cloudflared (L1↔L3 drift) → closes plan GAP F10 / OQ-5 as host-net. Logged ops-log-2026-06 12:11 entry. Touched NOTHING; hardening hard gate fully intact (PO go + window + apex online still required). NEXT: await TL on whether to schedule window or close probe.
- **S51 (2026-06-15):** Task #1 HUB HEALTH CHECK DONE -- hub UP+HEALTHY on prod-llm. Customer-channel evidence: ping ok+identity=framework-research; SSH host-key SHA256:CNcFjOxr8vREOueOS8nxJN8W3LaQHet62du+PHyK13U EXACT match (genuine, no MITM); uptime_s=233293 (~2.7d, continuous since S50, restart:unless-stopped held); forced-command isolation intact (echo PROBE ignored); state PERSISTED across CLI bump 2.1.175→2.1.177 (sm-state vol): grants_in=[fr-test, apex-research], grants_out=[apex-research, fr-test]. FLAGGED to TL: waiting_for_me={apex-research:2} (2 uncollected -- needs courier collect/ack); apex-research fresh grant_in 2026-06-15T06:48:26Z (ephemeral-key re-route, expect Task #2 re-register). NOTE: ping-response "fingerprint" field (SHA256:nkmhWN...DpyQ) is protocol-level §5.1, NOT the SSH host key. Could NOT host-probe docker (michelek@) -- sm_framework-research is forced-command-bound, no host-admin key staged in agent ctx; customer-channel evidence conclusive alone.
- **S51 HARD GATE (PO Mihkel via TL 10:33):** I am executor for Task #5 (apex container hardening: service supervisor + build-time courier key) but MUST NOT execute anything that restarts apex's container or docker-exec's changes onto it -- BLOCKED pending explicit PO approval + scheduled window. Await Brunel's design + TL's explicit go (carries PO sign-off). EXCEPTION still permitted: apex key-rotation = hub-side `sm-register apex-research <new-key>` (no apex-container restart) -- that I may still do (Task #2). Standing by; gate acknowledged to TL.
- **S51 Task #4 FR courier daemon -- DONE+ACCEPTED; STOPPED CLEAN at session-end.** FR's own infra on dev box (not apex → no hard gate). GO-LIVE 11:02 (PO GO via Brunel Tier-M dispatch); routing-fix restart 11:15 (ghost_outboxes "apex-bridge"→"apex-research-bridge"; old stripped to "apex"→E_UNKNOWN_TEAM, new→apex-research = registered; config-only). **STOPPED CLEAN at S51 session-end 11:59** (was pid 3300) via hardened `./stop-fr-courier.ps1` -- INDEPENDENTLY VERIFIED 5/5: process GONE, lock RELEASED (no courier.lock), spool EMPTY, pidfile removed, NO orphan python daemon proc. Outbound flushed before TL commit. **NEXT SESSION: relaunch with `./start-fr-courier.ps1`** (config already carries the routing fix outboxes=['apex-research-bridge']; a fresh start should show NO stale-lock-reclaim WARN since this stop released cleanly). Daemon logs to STDERR → `fr-courier.log.err` (stdout empty by design); quiet cycles emit no line.
  - **[OPS NOTE -- Windows stop-path, scratchpad-not-wiki per TL]:** `Stop-Process` = `TerminateProcess` = HARD KILL → Python signal handler + atexit do NOT fire → original `stop-fr-courier.ps1` ran NO drain and left a STALE lock (proved via _sig_probe.py: handlers-installed but no signal/drain/atexit marker). No data loss ever (reference journals to spool BEFORE deposit = spool-durable; next start re-ships). **FIX (Brunel patched, I re-verified PASS 11:45): stop→wait-for-exit→`--drain-once`→verify, baked into stop-fr-courier.ps1; CTRL_BREAK declined (Windows-fragile; composition is portable). drain-once's NORMAL exit DOES release the lock. ACCEPTANCE PROOF: post-patch fresh start showed NO "reclaiming stale lock" WARN (every prior start this session had it) = clean release not reclaim.** Order matters: stop-FIRST-then-drain-once (reverse fails -- drain-once can't grab the lock while daemon live, FileExistsError). Corroborates v2 stale-PID-36772 (reclaim = real safety net; handlers/atexit best-effort, SIGKILL bypasses on any OS). Prod=Linux where SIGTERM+atexit behave → Windows-dev-only friction. SESSION-END: `./stop-fr-courier.ps1` (one command, now honestly clean) → confirm + commit; leftover lock then is inert. Deposit path proven live earlier this session -- named artifact: Herald's "FR-S51-WINDOWPREP" auto-deposited "1/1; removed" on the routing-fix restart (same deposit code path as drain-once); declined fabricating apex-bound junk-test (would pollute apex's hub queue w/o consent -- TL+Brunel endorsed; if a repeatable deposit proof is ever wanted, use a self-route / fr-test throwaway target, NOT apex). Both TL+Brunel ACCEPTED + CLOSED 11:46-11:49; at idle per TL stand-down. **LESSON [catch-of-session per TL]: controlled-probe beats artifact-inference; disprove your OWN report when probe contradicts it.**
- **Current state:** S50 -- ALL Hopper operator work DONE. Tasks #3 (T6.a gate) + #7 (deploy) complete+accepted. #4 operator portion (all 5 registrations) DONE. Hub LIVE+healthy on prod-llm, 5 keys: alpha/beta (scratch), framework-research (production, MY key), fr-test (Herald's key, registered per Aen intercept -- his .pub comment says framework-research but routing=fr-test via forced command), apex-research (cross-team, PO-sanctioned, ephemeral-rotate). WRAP-READY.
- **Active items:** none for me -- Herald drives the courier round-trip (#4 acceptance, his ownership). Awaiting session-end shutdown.
- **Key decisions this session:** Aen re-adjudicated T6.a gate-of-record = prod-llm host-fs (customer substrate), in-container = confirmatory; volume REUSE (no Tier-D clear, gate intent = unknown-provenance, mine was enumerated-inert); Task #4 constraint-lifted back ON (17:28, reversing the deferral).
- **Carry-forward:** ALL my operator work DONE -- WRAP-READY. (1) Task #4 acceptance (courier deposit/collect/ack round-trip) is HERALD's, in flight; he has framework-research (my key) + fr-test (his mrx7 key ~/.ssh/sm_fr_test) + apex-research as targets, hub sm@10.100.136.162:2222 real-ssh verified, lazy-registry sequencing note relayed. (2) smoke-test.sh test-ordering bug (E_NOGRANT vs E_UNKNOWN_TEAM) -- Brunel owns, post-freeze; NOT a hub defect. (3) apex-research ephemeral key ROTATES on their container restart → expect re-registration requests (routine idempotent sm-register); sturdier infra-mount queued for PO. (4) prod-llm ~/stationmaster (HEAD 909bbe9); image stationmaster:1.0.0 healthy; FR private key ~/.ssh/sm_framework-research on this box. (5) Cal filed both gotchas (wiki 130 ssh-keygen-A-ignores-f; 132 nologin-defeats-forced-command). **LESSONS:** (a) lazy-registry -- a team is a valid deposit target only after it connects once, not at sm-register time [Brunel S51-doc nuance: "registered" = operator-registered vs registry-known; deposit keys off the latter]. (b) **Cal-distilled testing rule (1 instance from pattern-promotion, watching for n=3): a forced-command-over-ssh service needs an OVER-REAL-TRANSPORT acceptance test -- build-green AND direct-unit-test-green both pass straight through protocol-layer defects (host-key-gen + nologin both surfaced ONLY at real-ssh smoke).**

---

First-spawn 2026-05-20. Maintain under 100 lines; prune stale entries; promote pattern-grade to Callimachus via Protocol A.

## Substrate facts -- apex-research

[LEARNED -- substrate, apex-research] Host-user SSH: `ssh -T dev@100.96.54.170` (default key `~/.ssh/id_ed25519`, port 22). Source-of-truth: `~/bin/rc-deployments.json` entry `num:"1"`. `-T` is load-bearing for non-interactive ops (suppresses TTY allocation, keeps stdout clean for parsing).

[LEARNED -- substrate, apex-research] Container-user SSH: `ssh -i ~/.ssh/id_ed25519_apex -p 2222 ai-teams@100.96.54.170`. Source-of-truth: `~/bin/rc-deployments.json` entry `num:"2"`. Same physical host as the host-user SSH (`num:"1"`); two SSH shapes for host vs container access.

[LEARNED -- substrate, apex-research] `$COMPOSE_DIR` = `/home/dev/github/apex-migration-research`. Discovered via P1.2 find-probe (Tier R); confirmed authoritatively via `docker inspect` label `com.docker.compose.project.working_dir`, NOT via FR's `designs/deployed/apex-research/container/` path (that's design-as-shipped, not operational substrate).

[LEARNED -- substrate, apex-research] Operational compose-yml diverges from FR design: substrate `docker-compose.yml` exposes `SSH_PUBLIC_KEY_3` slot; FR's `designs/deployed/apex-research/container/docker-compose.yml:47-60` shows only SLOTS 1+2. Per PO via Aen 2026-05-20 17:37: SLOT 3 reserved for future key, never populated -- though backup `.env` (frozen 2026-04-29) had `SSH_PUBLIC_KEY_3=...rc-connect` (apparent contradiction; documented in ops-log, not adjudicated).

[LEARNED -- substrate, apex-research] Host user `dev`: uid 1000, gid 1000, in groups `sudo + docker + ollama + plugdev + ...`. `docker inspect` and `docker compose config` work unprivileged.

[LEARNED -- substrate, apex-research] AS OF 2026-05-21T09:18 (post-Phase-2 recreate success): **original PO ask ACHIEVED**. Container recreated; 3 SSH keys (PO + Aleksandr + rc-connect) installed by entrypoint Step 7 from canonical `.env`; KEY_COUNT=3 confirmed in entrypoint logs; sshd on port 2222 accepting all 3 keys. Operational compose-yml amended (P4.05) to declare `GH_TOKEN=${GH_TOKEN:-}` in apex-research env block -- GH_TOKEN preserved through recreate per PO Option B direction. All declared tokens in Config.Env (GITHUB_TOKEN, GH_TOKEN, ATLASSIAN_*, ANTHROPIC_API_KEY-empty). All 3 named volumes preserved across recreate. Container is recreate-safe -- next maintenance recreate installs same keys cleanly from same .env. Backup `.env` at $BACKUP_DIR + compose-yml backup `docker-compose.yml.bak.20260521-091347` both untouched as rollback artifacts. **The "any recreate = multi-system credential loss" gotcha is fully RESOLVED post-Phase-2.**

[GOTCHA -- substrate, apex-research, historical] Pre-2026-05-20T19:17 the substrate was in degraded state: no `.env` at `$COMPOSE_DIR`; container survived on Config.Env baked pre-2026-04-29-fresh-clone; any recreate = full SSH lockout + credential cluster loss (GITHUB_TOKEN, ATLASSIAN_API_TOKEN, TUNNEL_TOKEN). Original r3 Phase-2 sanction was rescinded by Aen 17:34 due to this. **Fully RESOLVED 2026-05-21T09:18 post-Phase-2 recreate** -- substrate now in canonical state with .env + amended compose-yml + recreate-safe Config.Env. Historical reference for the dispatch-arc audit trail; do NOT treat as current.

[LEARNED -- substrate, apex-research] Operational compose-yml's apex-research env block (post-2026-05-21T09:18 P4.05 amendment) declares 15 vars: HOME, REPO_URL, SOURCE_REPO_URL, GITHUB_TOKEN, **GH_TOKEN (added P4.05)**, ANTHROPIC_API_KEY, SSH_PUBLIC_KEY, SSH_PUBLIC_KEY_2, SSH_PUBLIC_KEY_3, NODE_EXTRA_CA_CERTS, CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS, TEAM_NAME, ATLASSIAN_EMAIL, ATLASSIAN_API_TOKEN, ATLASSIAN_BASE_URL. FR-design `designs/deployed/apex-research/container/docker-compose.yml:47-60` has fewer slots (no SLOT 3, no GH_TOKEN). Sub-shape E drift still exists between Layer 1 (FR design) and Layer 2 (operational); future FR-design update may want to absorb the operational SLOT 3 + GH_TOKEN additions.

[GOTCHA -- substrate, apex-research] Backup `.env` at `$BACKUP_DIR/.env` (frozen 2026-04-29) has `SSH_PUBLIC_KEY_3=ssh-ed25519 ... rc-connect` UNQUOTED (other SSH_PUBLIC_KEY entries are double-quoted; SLOT 3 is the only unquoted multi-word value). With `set -e` enabled, bash `source` of this file fails (parses `SSH_PUBLIC_KEY_3=ssh-ed25519` as assignment then tries to exec the rest). Workaround: use `grep+cut+strip-quotes` per-token instead of `source` -- substrate-truthful and robust to malformed backup syntax. Pattern shape from P3.6 amendment-1.

[GOTCHA -- substrate, apex-research] **Live network mode = `host`, NOT bridge** (S52 probe 2026-06-15, `docker inspect apex-research --format {{.HostConfig.NetworkMode}}`). FR-shipped design (Layer 1) describes bridge + cloudflared sidecar; runtime (Layer 3) is host networking. L1↔L3 drift -- closes Brunel plan GAP F10. Consequence: container has NO per-container NetworkSettings IP (inspect returns empty / "invalid IP"); it shares the host netns. The plan's "container IP 10.200.13.114" is host-stack addressing, not a separate bridge IP. Dashboard `--host 0.0.0.0:5173` and courier egress both ride the host's network directly. Surfaced to TL; not adjudicated.

[GOTCHA -- substrate, apex-research] **Cloudflared proxy (`ssh-apex-research.dev.evr.ee`) is NOT a docker control-plane route.** ~/.ssh/config carries `Host ssh-apex-research.dev.evr.ee / ProxyCommand cloudflared access ssh --hostname %h` and cloudflared is installed (scoop). BUT registry entry "a" user = `node` -- that tunnel fronts the hello-world APP/dashboard, lands you as `node` in app context, NOT on the rc host with docker access. Tailnet-independent but WRONG LAYER for `docker exec apex-research`. Only relevant if reaching the dashboard itself. (Brunel route-recon S52; he floated it as candidate iii -- ruled out.)

[LEARNED -- substrate, apex-research] **Host-side docker-exec route (the tailnet-down fallback):** `ssh -T dev@100.96.54.170` (rc host:22, registry num:"1", default `~/.ssh/id_ed25519`, key:null in registry = default) → `docker exec apex-research <cmd>` lands as **root**. This bypasses the apex container's own tailscale entirely (the container's tailscale being logged-out does NOT break this -- the RC *host's* tailscale serves 100.96.54.170). Use this route for any future apex docker-exec when the container-user tailnet path (num:"2", rc:2222 ai-teams) is down. `docker exec ... id`/`docker ps`/`docker inspect` all Tier R.

[LEARNED -- discipline, candidate Cal Protocol A] **Build-source-on-build-host pre-flight gate for any rebuild dispatch.** When a Tier D dispatch's diff was authored in a LOCAL workspace checkout but the image builds on a REMOTE host from that host's own checkout, the edits do NOT automatically exist on the build host. Authoring-path ≠ build-source-path. Before ANY `docker compose build`, grep the rebuild's signature markers ON THE BUILD HOST (rc-host build source), not just trust the dispatch/diff-doc. If markers grep=0 → L1↔L2 transit gap; the build would be green-but-empty (delivers none of the ask, secret mounts have no consumer). Recovery: STOP, surface back, transit-then-re-verify; do NOT git-push (team-lead's domain) or scp (silent-broadening) on own initiative. S52 apex catch: Brunel's 5 edits were uncommitted in the Windows checkout; rc-host build source had none. The grep-the-build-host pre-flight is now my standing step-1 gate for rebuild dispatches. (Generalizes the S51 probe-beats-inference lesson to the build-substrate.)

## Generalizable patterns

[LEARNED -- discipline, revert-on-invalidated-premise] Mid-conversation 2026-05-20: PO surfaced apparent wrong-host concern via Aen at 17:47-17:58 (his transient mis-read of `~/bin/rc-deployments.json`). I executed the discipline-honoring cleanup amendment when instructed by Aen -- relabeled scratchpad substrate entries, appended ops-log correction at 18:05 (append-only, not in-place). PO re-checked registry himself at 18:08 and confirmed the original 17:09 substrate-target was correct. Aen instructed full revert at 18:19. **Lesson: even after discipline-honoring artifact updates (append-only ops-log + scratchpad amendments), be willing to revert when the original premise is invalidated. Discipline serves accuracy, not consistency-with-prior-correction.** The append-only ops-log preserves the full audit-trail of confusion-and-resolution (17:09 original + 18:05 first correction + 18:XX revert-correction); scratchpad reverts to pre-amendment state because scratchpad is working-memory, not append-only audit log. Sub-shape F (registry-entry-choice-from-first-match) was withdrawn from the catalog -- no valid instance from this dispatch.

[LEARNED] **Within-dispatch-agency JSON-labels dump pattern:** when a single-label `docker inspect ... --format '{{index .Config.Labels "key"}}'` probe returns empty, run `docker inspect ... --format '{{json .Config.Labels}}'` as a Tier R follow-up. Disambiguates three failure modes in one round-trip: (a) label-key-missing, (b) container-not-running, (c) command-malformed. Substrate-truth-cheap, well within Tier R within-dispatch-agency scope.

[LEARNED] **Substrate-truth probe sequencing for env-source diagnosis:** container Config.Env > docker compose config (rendered output) > filesystem search (`.env*`, `*.env`, `find` in host home). Cheap-first, online-truth-first. Each layer reveals what the next can't: Config.Env shows what running container actually has; compose config shows what NEXT compose-up would resolve; filesystem search catches env-file-via-flag or alt-naming variants.

[LEARNED -- generalized discipline] **On any first-ever dispatch against a new substrate**, scratchpad-capture as `[LEARNED -- substrate, <team-name>]` entries the durable operational facts the probes surfaced: host-side filesystem paths, SSH connection shapes (cite `~/bin/rc-deployments.json` entry), file-ownership and writability defaults, named-volume layout, entrypoint-specific behaviors, naming conventions. On subsequent dispatches against the same substrate, READ scratchpad BEFORE probing; only re-probe if a fact looks stale or the dispatch explicitly directs.

[LEARNED -- discipline gap, candidate Hopper-Amendment-4] My existing `hopper.md` Diagnostic Discipline "Read Deployed Artifacts Before Executing" section reads **Layer 1 only** (FR design-as-shipped). Insufficient for FR-shipped substrates consumer teams operationalize. **Three-layer substrate-truth discipline:** (1) FR design-as-shipped (`designs/deployed/<team>/container/*`), (2) consumer team operational copy (substrate host's compose dir), (3) running container state (Config.Env, mounted volumes). Each can drift from neighbor. First-dispatch against any substrate → run Tier R probe-suite surfacing all three; subsequent dispatches → read scratchpad first, only re-probe if stale. **Surface to Celes at session wrap as Hopper-Amendment-4, cross-link to ops-log-2026-05.md catalyzing-incident entry.**

[LEARNED] **Discriminator anchored on sub-canonical source** (Brunel-phrasing; A.1/A.2 distinction locked with Brunel 2026-05-21 09:30). Filter regex / lookup key / literal-string anchored on inferred convention, template stub, or assumed-outer-layer-pass-through rather than substrate-truth-anchored grammar. **Sub-shape A.1 (identifier-grammar mismatch):** discriminator regex on identifier names anchors on inferred/template variants instead of canonical identifier grammar (POSIX envvar, Docker Compose label-key, etc.). Instances 1-3 in this dispatch arc: P1.1 michelek (template stub), P1.2a label-key underscore (inferred convention), P3.6 `[A-Z_]+` (digit exclusion missing). Recovery: substrate-truth-diagnostic surfaces actual identifier-grammar via cheap Tier R round-trip (JSON-dump-on-empty + alt-regex-on-discriminator-fail). **Sub-shape A.2 (transit-chain mismatch):** literal traverses multiple parsing layers; inner-layer grammar reinterprets a character outer layers passed through intact. Instance 4: P4.05 awk `}` parsing in PowerShell→base64→bash→awk chain. Recovery: string-concatenation refactor at innermost parser + post-fail diff-vs-backup. **Shared lesson:** verify-substrate-truth-anchored-grammar at the innermost parser BEFORE relying on outer-layer pass-through. Innermost-parser-truth dominates outer-layer-assumption. Both share same recovery posture: surface-back-with-substrate-truth-evidence before silent re-attempt. Cal-Protocol-A submission `wiki/patterns/discriminator-anchored-on-sub-canonical-source.md` staged for session-end (Brunel-phrasing + n=4 catalog + Hopper-defense-pattern + this dispatch arc as catalyzing-incident).

## Local-side gotchas (Windows dev workstation)

[GOTCHA -- local dev] PowerShell-on-Windows quoting of nested Go-template format strings via `ssh "remote-cmd"` mangles `\"` escapes. **Workaround used through this dispatch: base64-transit** -- build the literal remote command locally, base64-encode, send as `ssh "echo '<b64>' | base64 -d | bash"`. Same pattern Brunel specified for P1.4; generalizes to any remote command with shell metacharacters or nested quotes. (Pattern itself was Brunel-authored for transit safety; deployment lesson here is its second-use generality.)

[GOTCHA -- local dev] PowerShell 5.x file-loaded scripts: non-ASCII characters (em-dash `—`, etc.) in `Write-Host` literal strings cause parser failure when file is UTF-8-no-BOM. **Workaround: use ASCII hyphens in script messages.** `(blank-line)` etc. for separators. This is a Windows session-local friction per `feedback_no_windows_substrate_findings.md` -- do NOT file as wiki-grade.

[GOTCHA -- local dev] PowerShell 5.x lexer mis-parses `[STRING-BRACKET-LITERAL]` inside `Write-Host` strings under some states. **Workaround: parentheses/no-brackets in script messages.** Same windows-substrate-friction class as the em-dash gotcha.

## In-flight dispatches

[OUTCOME -- S55 (2026-06-18) ~13:27 -- LIVE FR courier "auto"-flip restart ABORTED-PRE-EXECUTION (Tier M). Courier UNTOUCHED.] Brunel dispatched: restart live FR courier on flipped inboxes_dir:"auto" config to validate runtime discovery on 2.1.177 (WS3 unpin-prep). I validated **Tier M** (live FR<->apex comms, reversible via rollback). Ran all 3 verify-before-restart gates (Tier R): GATE1 PASS (working-tree courier has "auto" branch L99 + null-pid guard L949; daemon imports it via importlib+ref.Config -- FR courier runs LOCAL from this working tree, no rc-host clone/pull); GATE2 PASS (config flipped, rollback value in _unpin_flip_note); GATE3 PASS (hub ping ok, grants intact, no stuck mail). **CAUGHT 2 things before any restart:** (a) restart-MECHANISM divergence -- dispatch said S54 "Stop-ScheduledTask->Task relaunches" but the Task is Ready/idle (NOT the launcher); live daemon = plain python.exe pid 38044 -> correct mechanism is FR's stop/start-fr-courier.ps1 script pair (surfaced, held for confirm); (b) uncommitted ` M` on the courier = task#10 --resolve-team-dir shim (COMPLETE, confined to main(), daemon code paths unaffected, py-compiles -- informational). **THEN Brunel+Aen HOLD: bare "auto"+team_dir_name:null FAIL-FASTS on this box -- 11-12 TeamCreate-NAMED team dirs, lone live session-<hex> matches NONE -> liveness=[] -> resolve_team_dir RAISES at Config-load -> courier would CRASH on startup.** I had NOT restarted (was holding for the mechanism confirm) -> NOTHING to roll back; courier stays pid 38044 on pre-flip explicit-path config, live comms uninterrupted. Brunel reverts the config (his edit -- I do NOT also touch fr-courier.config.json, cross-agent contention); "auto" flip moves to the AT-unpin (2.1.178+) step w/ a team_dir_name:"framework-research" disambiguator (Aen deciding). Corroborates my V2 probe finding (glob+liveness fail-fast on multi-dir = SAFE degradation, working as designed). **LESSON [catch-of-session, candidate Cal]: gates that prove CODE-handles-X + CONFIG-is-flipped are NECESSARY-NOT-SUFFICIENT for a config-RESOLUTION change -- add a DRY-RUN RESOLUTION gate (`--resolve-team-dir` rc=0 check) BEFORE restarting any daemon whose config resolves at load-time. The crash is at RESOLVE time, not import/config-presence time; gates 1+2 structurally can't catch it. Brunel supplied the missing gate. This generalizes my S52 build-source-pre-flight discipline to config-resolution-pre-flight.** ops-log: aborted-pre-execution entry pending (asked Aen: log now or after revised-config resolves).

[OUTCOME -- S55 (2026-06-18) 12:34 -- migration-validation probe V1-V5 RUN 2 COMPLETE on 2.1.181. Task #5/WS3b DONE.] After PO reclaimed ~13.6G (apt cache 9.8G + journal 3.8G -> root 100%->76%), re-ran clean. Auth = team-driven tmux pipeline (I drove send-keys theme/login-method, captured OAuth URL, relayed to Aen->PO, PO authed in browser, I injected the returned code via send-keys -l -- code never logged). **FINDINGS:** **V3 (LOAD-BEARING) = WORST CASE:** `sessions/<pid>.json` NOT GC'd on exit (graceful + kill-9); lingers `status:"idle"` -- SAME as a live idle session (verified: kill -0 pid344=DEAD but entry persists idle; pid81 ALIVE also idle). **Status CANNOT distinguish dead from live -> WS1 `_has_live_session` `dead/exited/stopped` allowlist is WRONG, must use process-liveness `kill -0` on the `pid` field (procStart guards PID-reuse). The one must-fix before unpin.** **V4:** config.json FIRST, sessions/<pid>.json only at interactive-ready (early-discovery window exists). **V1 PASS** (clean-isolated): resolver glob->live session-<id>, inboxes_dir:"auto"->discovered dir not framework-research. **V2:** pid-keyed PASS (selects live dir); glob-only FAILS-FAST safe on stale idle entries = the V2b<->V3 coupling CONFIRMED. **V5a/P4 PASS** (members[]-injection routes, ghost-courier.json got the SendMessage). **V5b/P6 INCONCLUSIVE-leaning-NEGATIVE:** external write did NOT wake bare idle session in 35-90s (differs from S54 ~15s); CONFOUND = headless never-attached pane (focus-events off) -- did NOT call it pass OR regression (disprove-own-report discipline); needs attached-pane re-test. **HARNESS DEFECTS (dogfood, surfaced to Brunel):** drive-phase check-isolation -- V3/V4 leave stale dirs contaminating V1/V5/V2 in-sequence verdicts (true verdicts via direct re-probe), V2 false-PASS on empty live_dir, V4 poller newsess-diff bug; build/up/teardown/preflight all CLEAN (2.1.181 assert fired). NET: V1/V2-pid/V5a VALIDATE; unpin proceeds once (a) liveness=process-not-status (V3) + (b) V5b re-confirmed attached-pane. CLEAN teardown (down -v + rmi leaf 212a04ab, WARP edit reverted); apex UP 46h; Docker garbage left for PO. findings doc `migration-validation-probe-findings-2026-06-18.md`; ops-log 2026-06 12:34. evidence snapshot host /tmp/migration-probe-v1v5-evidence-20260618/. **LESSONS [candidate Cal]: (1) `df -h /` Tier-R pre-flight before build on any host (Run-1 abort catalyzed Brunel's phase_preflight guard de2f24e). (2) disprove-own-report held AGAIN on V5b/P6 -- refused to call it regression given the headless-pane confound, exactly the S54 P6 reversal pattern. (3) scripted-harness check-isolation: spawning checks (V3/V4) MUST clean up before resolver checks (V1/V2/V5) or run after them.**

[OUTCOME -- S55 (2026-06-18) 11:55 -- migration-validation probe V1-V5 ABORTED-MID-EXECUTION (RUN 1): rc host root FS 100% full.] Task #5/WS3b, sibling of S54. Dispatch arc was noisy (10:34 V1-V4 -> Aen HOLD -> 10:38 amend +V5/version-pin -> 10:47 scripted-harness amend -> **10:49 CONSOLIDATED RE-ROUTE = authoritative**). Got through ALL 3 gates (harness pushed e52077c, version 2.1.181 PO-confirmed, PO auth-go) + Tier-R pre-flight + `git pull --ff-only` (CLEAN clone /home/dev/github/mitselek-ai-teams chosen over dirty /home/dev/mitselek-ai-teams, build-source discipline) + on-host WARP uncomment (backup .bak-prewarp, reverted at abort -- committed file untouched) + BUILD (**assert installed==2.1.181 PASSED -- version invariant VERIFIED at substrate**) + UP (host-net, claude 2.1.181, files staged, creds ABSENT). **HARD-GATE STOP at auth step: `tmux new-session` -> "No space left on device" /tmp/tmux-1000.** Tier-R diagnosis: `df -h /` rc host root `/dev/mapper/...-root` 58G **100% full / 0 avail**; container overlay = same root LV so container /tmp full -> NO session can launch. `docker system df`: 19.35GB images (18.75GB reclaimable!), 11GB volumes, 4.26GB build-cache (3.7GB reclaimable); my probe img 1.02GB tipped it but host was already critically full (garbage predates run). **V1-V5 NOT executed -- ZERO substrate findings (no V3 GC status, no V4 order, no V1/V2 resolver, no V5 P4/P6); host must be remediated first.** Did NOT prune the 18.75GB Docker garbage (cortex/uikit/backlog-triage/entu/polyphony all UP -> cascade risk per feedback_docker_rmi_cascade; PO/Aen call, surfaced not remediated). REVERSED own footprint CLEAN: `./harness.sh teardown` = down -v (container+vol gone) + rmi LEAF 96356db (no cascade, only my container ref'd it) + compose edit reverted; apex UP 46h untouched; no probe artifact lingering. **Probe re-runnable AS-IS once disk freed** -- harness + pin (2.1.181) + envelope all proven up to the auth wall; only host disk blocks. ops-log 2026-06 11:55 (8 fields, aborted-mid-execution). Result logs: designs/new/migration-probe-harness/results-2.1.181-*.log. Reported Brunel(diag)+Aen(role-of-record). **LESSON [catch-of-session, candidate Cal]: `df -h /` belongs in my Tier-R substrate-readiness pre-flight BEFORE build on ANY host -- a full-disk host wastes a build+up cycle and dead-ends at the auth wall; cheap to check up front, expensive to discover late. S53 already flagged a pre-existing apex /tmp issue on this host -> the host-fullness was foreshadowed.**

[OUTCOME -- Task #3 + #7 S50 CLOSED 2026-06-12. FULL AUDIT in ops-log-2026-06 (6 entries).] **Task #3 (T6.a gate):** harness `poc/ghost-bridge/t6a-race-harness.py` (Python open('x') + bash set -C, 50-round 2-proc race, mirrors courier.py:773-785). PASS on prod-llm: host-fs ext4-on-LVM (load-bearing, gate-of-record per Aen re-adjudication = prod-llm IS customer substrate) + in-container sm-state volume mount (confirmatory) -- 50/50 both primitives both runs, 0 anomalies. Cal gotcha filed: tmpfs-vs-deployment-fs trap (wiki 125). **Task #7 (deploy):** stationmaster hub LIVE+healthy on prod-llm (Debian 13, ext4, sshd:2222, host key on sm-state vol). Caught 2 Brunel artifact defects mid-deploy (NOT patched, surfaced w/ Layer-3 evidence, Brunel fixed both): (1) entrypoint host-key `ssh-keygen -A -f` lands 0 keys OpenSSH 9.2p1 → fixed f022fed (direct ed25519); (2) `sm` shell nologin blocks forced-command → fixed 909bbe9 (/bin/sh). Smoke-test 14/15 -- the 1 fail is a TEST-ORDERING ARTIFACT (expects E_NOGRANT, hub correctly returns E_UNKNOWN_TEAM pre-first-connect; registry is lazy-populated on connect not at sm-register) = Brunel test-script bug, NOT hub defect. Volume REUSE (Aen ruling: gate intent=unknown-provenance, mine enumerated-inert). 2 scratch keys alpha/beta left registered+inert for S51. **[GOTCHA×2 candidate Cal]:** ssh-keygen -A+-f prefix fails OpenSSH 9.2p1; nologin shell defeats authorized_keys forced-command (Windows unit-smoke missed -- tested sm-shell direct, not via sshd login-shell). **3 layered-gate catches this arc: EXIT-0≠success, transport-green≠protocol-OK, smoke-fail-triage=test-artifact-not-defect.**

## Prior-session archive (pruned at S50 shutdown per 100-line discipline)

[ARCHIVED] S35/S36/S37 dispatch transcripts (apex db-tunnel #6, fr-cma-pilot CF bypass #1, secret-rotation #2) pruned -- full records in docs/operations-log-2026-05.md. Durable lessons already promoted: wiki/patterns/discriminator-anchored-on-sub-canonical-source.md + three-layer-substrate-truth-discipline.md (Cal-filed); Layer-0 library-first + probe-design-as-sub-canonical-source (A.3) + L2-unreachable-graceful-degradation captured in those patterns. apex-research substrate facts retained above (host-networked, ssh-keygen behaviors, , etc.).
