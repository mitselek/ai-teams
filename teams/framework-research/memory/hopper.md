# Hopper -- scratchpad (*FR:Hopper*)

## Summary (lines 1-15 -- always read on startup)
- **S67 (2026-08-03) -- Task #2 RC-host "keeps shutting down" -- MECHANISM NAILED = s2idle SUSPEND (Tier R, NO fixes; fix PROPOSED awaiting Tier D sanction).** Host=`100.96.54.170` CONFIRMED (`199`=typo), physical Dell Pro Max Slim desktop (`paarisprogemis-fyysiline`, virt=none), reached via **Cloudflare WARP** (NOT Tailscale -- Aen; 100.64/10 shared-range trap). **ROOT CAUSE: the box s2idle-suspends ~3x/session under active remote load. `/sys/power/suspend_stats/success=3` since 09:39 boot (kernel counter, authoritative); `mem_sleep=[s2idle]`; suspend.target fingerprint 10:58:56; journal GAP window B 10:28-10:33 totally empty.** RAM-preserved resume (apex mid-conversation) + no reboot (boot0 unbroken, RestartCount=0) = suspend not shutdown, not bare network-flap. **HYPOTHESIS EVOLVED 3x this task, each on evidence: power-fault -> network-flap -> s2idle-suspend. Owned 2 of my own misreads: 10:59 "fault firing" (disproven by boot-list) + "journal continuous" (disproven by the 5-min void).** **TRIGGER SURPRISE: standard paths all OFF** (logind IdleAction default-ignore, GNOME AC=nothing) yet it suspends -> trigger unidentified, flagged-not-guessed; likely platform s2idle policy the desktop knobs don't govern. FIX = `systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target` (mechanism-block, robust to trigger since suspend routes through systemd; rollback=unmask). **SANCTIONED Tier D by Aen 11:32; PO EXECUTES it (sudo-password stays w/ PO, no NOPASSWD); my role = read-only VERIFY.** **TRIGGER NAMED: headless box (monitors removed) -> sits at GDM GREETER on seat0, all dev sessions seatless SSH -> greeter's OWN idle policy suspends under remote-only load; the `sleep-inactive-ac-type=nothing` I checked = WRONG SCOPE (user session, not greeter) = why obvious knob looked done.** **TASK #2 CLOSED (COMPLETE):** PO applied mask 11:23; `suspend_stats/success` held FLAT at 4 from 11:23->11:58 (35min, 2-3 would-be cycles) = mask bit, suspends STOPPED. Verified by counter-going-quiet, not mask-command-success. Framework finding FINAL text handed to Aen (scratch cal-submission-5; headless->GDM-greeter-scope-trap->s2idle, +EDID-wrong-lead, +genus-link to SUB 1/4). **TASK #3 CLOSED (COMPLETE): all 3 apex tunnels LIVE END-TO-END.** Live Windows ssh pid5040 (respawned 12:19 post-drops) carries all 3 -R flags -> **11443 survived every suspend/respawn cycle**; RC listeners 11443/11521/11522 up v4+v6; **11443 GitLab = L7 302** (CF Access end-to-end); **11521/11522 Oracle = HELD_OPEN_alive** (/dev/tcp hold-open: full path to live listener, no sshd reset -- honest bound: TCP-path not TNS-handshake, no oracle client in container, auth apex-side). TYPE 2 (historical cold reboots) still separate+non-urgent-open; SEL moot (no reboot). ops-log 2026-08 = 5 entries, uncommitted since `6655d51`. Tasks #2+#3 COMPLETE.
- **S66 (2026-07-24) -- GH #104 (apex GitLab forward) DONE + DELIVERED (superseded snapshot; full arc in In-flight dispatches + ops-log 2026-07 + Cal queue).** Substrate = operator-side autossh reverse-SSH bridge on PO's Windows box, owned by apex-migration-research repo (NOT stationmaster). Added `-R 11443:gitlab.evr.ee:443` to the one shared ssh session. apex's CF-Access premise was FALSE (gitlab=CF anycast) but ask survives via WARP device identity (proven, verify3 302 + CF_Authorization). See Current-state/Active-items below for delivery + courier-outage detail.
- **Current state:** **S67 SESSION-END (shutdown 2026-08-03 ~14:37). ALL THREE tasks this session COMPLETE: #104 (apex GitLab 11443 forward, delivered), #2 (RC-host suspend fix -- headless GDM-greeter s2idle, masked, verified holding 64min+), #3 (all 3 apex tunnels live end-to-end, 11443 survived cycles).** No mutations by me except the #104 one-line diff (S66). RC host stable post-mask.
- **Active items:** NONE -- session closed, all assigned tasks done, closing report sent, shutdown approved.
- **Key decisions this session:** (0) **CAL CATALOGUE SPLIT (mine, Aen accepted + called it better than his own proposal):** the session's misses are TWO genera, not one. *Verification narrower than it appears* (SUB 1: `-R` TCP probe, my pre-send courier check, Aen's stdout-vs-stderr startup check) vs *control narrower than its name* (SUB 4: running-loop-ignores-edit, `Stop-ScheduledTask`-orphans-tree). Cross-link, do NOT merge -- merging collapses the observe/act distinction, per Cal dedup rule 3 on over-merging claims that merely look alike. **n=2 agents across the verification genus kills the reading that this is one operator's discipline problem; it is a property of how these systems report health.** (1) tier pre-classification WITHDRAWN -- locate reports carry facts + verbatim command + blast radius, never a tier label. (2) Refused to assert WARP-through-ssh on inference; verify step 3 is the proof. (3) Refused to ship `pkill -f` to the PO (cross-MSYS silent no-op) -- CIM/Stop-Process substituted. (4) Cal submission 1 filed `confidence: medium` on purpose, with a written confirming experiment; **Aen: do not let anyone talk it up to `high` without running that experiment.**
- **Carry-forward to NEXT SESSION:** (1) **Cal queue = 5 submissions (1 verification-genus, 2 nc-guard, 3 CF/WARP, 4 control-genus, 5 headless-suspend framework finding), Cal NOT spawned -> all ride to next session as COPIES (do not rewrite).** SUB 5 full text in scratch `cal-submission-5-framework-finding.md` (temp -- if lost, reconstruct from ops-log-2026-08 Task#2 entries); Aen places SUB 5 in `docs/findings.md`+wiki (I can't write those). (2) **TYPE-2 historical cold reboots = the one open thread** (weekend boot-list gaps, cause unevidenced, SEL read deferred -- non-urgent, no recurrence; only revisit if a real cold-boot recurs). (3) **Suspend-fix durability watch:** if `suspend_stats/success` ever climbs past 4 on the RC host, the mask was bypassed (firmware modern-standby) -> escalate to `mem_sleep` kernel param / BIOS toggle. (4) BIOS "Restore on AC Power Loss=Power On" still worth flagging PO (resilience for any future real power-off). (5) ops-log-2026-08 (5 entries + Task#3 addendum) committed by Aen per shutdown; #104 committed `5e8840a`/`2db020c`. (6) **My standing operator disciplines validated hard this session: verify-the-thing-not-its-neighbours (caught the courier + the s2idle misreads via the queued check); hold-the-close-on-the-mechanism's-own-counter; flag-don't-guess (trigger left unnamed, mask made it moot).**

---

First-spawn 2026-05-20. Maintain under 100 lines; prune stale entries; promote pattern-grade to Callimachus via Protocol A.

## Substrate facts -- apex-research

[LEARNED -- substrate, apex-research] Host-user SSH: `ssh -T dev@100.96.54.170` (default key `~/.ssh/id_ed25519`, port 22). Source-of-truth: `~/bin/rc-deployments.json` entry `num:"1"`. `-T` is load-bearing for non-interactive ops (suppresses TTY allocation, keeps stdout clean for parsing).

[LEARNED -- substrate, apex-research] Container-user SSH: `ssh -i ~/.ssh/id_ed25519_apex -p 2222 ai-teams@100.96.54.170`. Source-of-truth: `~/bin/rc-deployments.json` entry `num:"2"`. Same physical host as the host-user SSH (`num:"1"`); two SSH shapes for host vs container access.

[LEARNED -- substrate, apex-research] `$COMPOSE_DIR` = `/home/dev/github/apex-migration-research`. Discovered via P1.2 find-probe (Tier R); confirmed authoritatively via `docker inspect` label `com.docker.compose.project.working_dir`, NOT via FR's `designs/deployed/apex-research/container/` path (that's design-as-shipped, not operational substrate).

[LEARNED -- substrate, apex-research] Operational compose-yml diverges from FR design: substrate `docker-compose.yml` exposes `SSH_PUBLIC_KEY_3` slot; FR's `designs/deployed/apex-research/container/docker-compose.yml:47-60` shows only SLOTS 1+2. Per PO via Aen 2026-05-20 17:37: SLOT 3 reserved for future key, never populated -- though backup `.env` (frozen 2026-04-29) had `SSH_PUBLIC_KEY_3=...rc-connect` (apparent contradiction; documented in ops-log, not adjudicated).

[LEARNED -- substrate, apex-research] Host user `dev`: uid 1000, gid 1000, in groups `sudo + docker + ollama + plugdev + ...`. `docker inspect` and `docker compose config` work unprivileged.

[LEARNED -- substrate, apex-research] AS OF 2026-05-21T09:18 (post-Phase-2 recreate success): **original PO ask ACHIEVED**. Container recreated; 3 SSH keys (PO + Aleksandr + rc-connect) installed by entrypoint Step 7 from canonical `.env`; KEY_COUNT=3 confirmed in entrypoint logs; sshd on port 2222 accepting all 3 keys. Operational compose-yml amended (P4.05) to declare `GH_TOKEN=${GH_TOKEN:-}` in apex-research env block -- GH_TOKEN preserved through recreate per PO Option B direction. All declared tokens in Config.Env (GITHUB_TOKEN, GH_TOKEN, ATLASSIAN_*, ANTHROPIC_API_KEY-empty). All 3 named volumes preserved across recreate. Container is recreate-safe -- next maintenance recreate installs same keys cleanly from same .env. Backup `.env` at $BACKUP_DIR + compose-yml backup `docker-compose.yml.bak.20260521-091347` both untouched as rollback artifacts. **The "any recreate = multi-system credential loss" gotcha is fully RESOLVED post-Phase-2.**

[GOTCHA -- substrate, apex-research, historical] Pre-2026-05-20T19:17 the substrate was in degraded state: no `.env` at `$COMPOSE_DIR`; container survived on Config.Env baked pre-2026-04-29-fresh-clone; any recreate = full SSH lockout + credential cluster loss (GITHUB_TOKEN, ATLASSIAN_API_TOKEN, TUNNEL_TOKEN). Original r3 Phase-2 sanction was rescinded by Aen 17:34 due to this. **Fully RESOLVED 2026-05-21T09:18 post-Phase-2 recreate** -- substrate now in canonical state with .env + amended compose-yml + recreate-safe Config.Env. Historical reference for the dispatch-arc audit trail; do NOT treat as current.

[LEARNED -- substrate, apex-research] Operational compose-yml's apex-research env block (post-2026-05-21T09:18 P4.05 amendment) declares 15 vars: HOME, REPO_URL, SOURCE_REPO_URL, GITHUB_TOKEN, **GH_TOKEN (added P4.05)**, ANTHROPIC_API_KEY, SSH_PUBLIC_KEY, SSH_PUBLIC_KEY_2, SSH_PUBLIC_KEY_3, NODE_EXTRA_CA_CERTS, CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS, TEAM_NAME, ATLASSIAN_EMAIL, ATLASSIAN_API_TOKEN, ATLASSIAN_BASE_URL. FR-design `designs/deployed/apex-research/container/docker-compose.yml:47-60` has fewer slots (no SLOT 3, no GH_TOKEN). Sub-shape E drift still exists between Layer 1 (FR design) and Layer 2 (operational); future FR-design update may want to absorb the operational SLOT 3 + GH_TOKEN additions.

[GOTCHA -- substrate, apex-research] Backup `.env` at `$BACKUP_DIR/.env` (frozen 2026-04-29) has `SSH_PUBLIC_KEY_3=ssh-ed25519 ... rc-connect` UNQUOTED (other SSH_PUBLIC_KEY entries are double-quoted; SLOT 3 is the only unquoted multi-word value). With `set -e` enabled, bash `source` of this file fails (parses `SSH_PUBLIC_KEY_3=ssh-ed25519` as assignment then tries to exec the rest). Workaround: use `grep+cut+strip-quotes` per-token instead of `source` -- substrate-truthful and robust to malformed backup syntax. Pattern shape from P3.6 amendment-1.

[GOTCHA -- substrate, apex-research] **Live network mode = `host`, NOT bridge** (S52 probe 2026-06-15, `docker inspect apex-research --format {{.HostConfig.NetworkMode}}`). FR-shipped design (Layer 1) describes bridge + cloudflared sidecar; runtime (Layer 3) is host networking. L1↔L3 drift -- closes Brunel plan GAP F10. Consequence: container has NO per-container NetworkSettings IP (inspect returns empty / "invalid IP"); it shares the host netns. The plan's "container IP 10.200.13.114" is host-stack addressing, not a separate bridge IP. Dashboard `--host 0.0.0.0:5173` and courier egress both ride the host's network directly. Surfaced to TL; not adjudicated.

[GOTCHA -- substrate, apex-research] **Cloudflared proxy (`ssh-apex-research.dev.evr.ee`) is NOT a docker control-plane route.** ~/.ssh/config carries `Host ssh-apex-research.dev.evr.ee / ProxyCommand cloudflared access ssh --hostname %h` and cloudflared is installed (scoop). BUT registry entry "a" user = `node` -- that tunnel fronts the hello-world APP/dashboard, lands you as `node` in app context, NOT on the rc host with docker access. Tailnet-independent but WRONG LAYER for `docker exec apex-research`. Only relevant if reaching the dashboard itself. (Brunel route-recon S52; he floated it as candidate iii -- ruled out.)

[LEARNED -- substrate, apex-research] **Host-side docker-exec route (the tailnet-down fallback):** `ssh -T dev@100.96.54.170` (rc host:22, registry num:"1", default `~/.ssh/id_ed25519`, key:null in registry = default) → `docker exec apex-research <cmd>` lands as **root**. This bypasses the apex container's own tailscale entirely (the container's tailscale being logged-out does NOT break this -- the RC *host's* tailscale serves 100.96.54.170). Use this route for any future apex docker-exec when the container-user tailnet path (num:"2", rc:2222 ai-teams) is down. `docker exec ... id`/`docker ps`/`docker inspect` all Tier R.

[LEARNED -- discipline, candidate Cal Protocol A] **Build-source-on-build-host pre-flight gate for any rebuild dispatch.** When a Tier D dispatch's diff was authored in a LOCAL workspace checkout but the image builds on a REMOTE host from that host's own checkout, the edits do NOT automatically exist on the build host. Authoring-path ≠ build-source-path. Before ANY `docker compose build`, grep the rebuild's signature markers ON THE BUILD HOST (rc-host build source), not just trust the dispatch/diff-doc. If markers grep=0 → L1↔L2 transit gap; the build would be green-but-empty (delivers none of the ask, secret mounts have no consumer). Recovery: STOP, surface back, transit-then-re-verify; do NOT git-push (team-lead's domain) or scp (silent-broadening) on own initiative. S52 apex catch: Brunel's 5 edits were uncommitted in the Windows checkout; rc-host build source had none. The grep-the-build-host pre-flight is now my standing step-1 gate for rebuild dispatches. (Generalizes the S51 probe-beats-inference lesson to the build-substrate.)

## Generalizable patterns

[LEARNED -- discipline, revert-on-invalidated-premise] Mid-conversation 2026-05-20: PO surfaced apparent wrong-host concern via Aen at 17:47-17:58 (his transient mis-read of `~/bin/rc-deployments.json`). I executed the discipline-honoring cleanup amendment when instructed by Aen -- relabeled scratchpad substrate entries, appended ops-log correction at 18:05 (append-only, not in-place). PO re-checked registry himself at 18:08 and confirmed the original 17:09 substrate-target was correct. Aen instructed full revert at 18:19. **Lesson: even after discipline-honoring artifact updates (append-only ops-log + scratchpad amendments), be willing to revert when the original premise is invalidated. Discipline serves accuracy, not consistency-with-prior-correction.** The append-only ops-log preserves the full audit-trail of confusion-and-resolution (17:09 original + 18:05 first correction + 18:XX revert-correction); scratchpad reverts to pre-amendment state because scratchpad is working-memory, not append-only audit log. Sub-shape F (registry-entry-choice-from-first-match) was withdrawn from the catalog -- no valid instance from this dispatch.

[LEARNED] **Within-dispatch-agency JSON-labels dump pattern:** when a single-label `docker inspect ... --format '{{index .Config.Labels "key"}}'` probe returns empty, run `docker inspect ... --format '{{json .Config.Labels}}'` as a Tier R follow-up. Disambiguates three failure modes in one round-trip: (a) label-key-missing, (b) container-not-running, (c) command-malformed. Substrate-truth-cheap, well within Tier R within-dispatch-agency scope.

[LEARNED] **Substrate-truth probe sequencing for env-source diagnosis:** container Config.Env > docker compose config (rendered output) > filesystem search (`.env*`, `*.env`, `find` in host home). Cheap-first, online-truth-first. Each layer reveals what the next can't: Config.Env shows what running container actually has; compose config shows what NEXT compose-up would resolve; filesystem search catches env-file-via-flag or alt-naming variants.

[LEARNED -- generalized discipline] **On any first-ever dispatch against a new substrate**, scratchpad-capture as `[LEARNED -- substrate, <team-name>]` entries the durable operational facts the probes surfaced: host-side filesystem paths, SSH connection shapes (cite `~/bin/rc-deployments.json` entry), file-ownership and writability defaults, named-volume layout, entrypoint-specific behaviors, naming conventions. On subsequent dispatches against the same substrate, READ scratchpad BEFORE probing; only re-probe if a fact looks stale or the dispatch explicitly directs.

[LEARNED -- discipline gap, candidate Hopper-Amendment-4] My existing `hopper.md` Diagnostic Discipline "Read Deployed Artifacts Before Executing" section reads **Layer 1 only** (FR design-as-shipped). Insufficient for FR-shipped substrates consumer teams operationalize. **Three-layer substrate-truth discipline:** (1) FR design-as-shipped (`designs/deployed/<team>/container/*`), (2) consumer team operational copy (substrate host's compose dir), (3) running container state (Config.Env, mounted volumes). Each can drift from neighbor. First-dispatch against any substrate → run Tier R probe-suite surfacing all three; subsequent dispatches → read scratchpad first, only re-probe if stale. **Surface to Celes at session wrap as Hopper-Amendment-4, cross-link to ops-log-2026-05.md catalyzing-incident entry.**

[LEARNED] **Discriminator anchored on sub-canonical source** (Brunel-phrasing; A.1/A.2 distinction locked with Brunel 2026-05-21 09:30). Filter regex / lookup key / literal-string anchored on inferred convention, template stub, or assumed-outer-layer-pass-through rather than substrate-truth-anchored grammar. **Sub-shape A.1 (identifier-grammar mismatch):** discriminator regex on identifier names anchors on inferred/template variants instead of canonical identifier grammar (POSIX envvar, Docker Compose label-key, etc.). Instances 1-3 in this dispatch arc: P1.1 michelek (template stub), P1.2a label-key underscore (inferred convention), P3.6 `[A-Z_]+` (digit exclusion missing). Recovery: substrate-truth-diagnostic surfaces actual identifier-grammar via cheap Tier R round-trip (JSON-dump-on-empty + alt-regex-on-discriminator-fail). **Sub-shape A.2 (transit-chain mismatch):** literal traverses multiple parsing layers; inner-layer grammar reinterprets a character outer layers passed through intact. Instance 4: P4.05 awk `}` parsing in PowerShell→base64→bash→awk chain. Recovery: string-concatenation refactor at innermost parser + post-fail diff-vs-backup. **Shared lesson:** verify-substrate-truth-anchored-grammar at the innermost parser BEFORE relying on outer-layer pass-through. Innermost-parser-truth dominates outer-layer-assumption. Both share same recovery posture: surface-back-with-substrate-truth-evidence before silent re-attempt. Cal-Protocol-A submission `wiki/patterns/discriminator-anchored-on-sub-canonical-source.md` staged for session-end (Brunel-phrasing + n=4 catalog + Hopper-defense-pattern + this dispatch arc as catalyzing-incident).

## Local-side gotchas (Windows dev workstation)

[GOTCHA -- local dev] PowerShell-on-Windows quoting of nested Go-template format strings via `ssh "remote-cmd"` mangles `\"` escapes. **Workaround used through this dispatch: base64-transit** -- build the literal remote command locally, base64-encode, send as `ssh "echo '<b64>' | base64 -d | bash"`. Same pattern Brunel specified for P1.4; generalizes to any remote command with shell metacharacters or nested quotes. (Pattern itself was Brunel-authored for transit safety; deployment lesson here is its second-use generality.)

[GOTCHA -- local dev] PowerShell 5.x file-loaded scripts: non-ASCII characters (em-dash `—`, etc.) in `Write-Host` literal strings cause parser failure when file is UTF-8-no-BOM. **Workaround: use ASCII hyphens in script messages.** `(blank-line)` etc. for separators. This is a Windows session-local friction per `feedback_no_windows_substrate_findings.md` -- do NOT file as wiki-grade.

[GOTCHA -- local dev] PowerShell 5.x lexer mis-parses `[STRING-BRACKET-LITERAL]` inside `Write-Host` strings under some states. **Workaround: parentheses/no-brackets in script messages.** Same windows-substrate-friction class as the em-dash gotcha.

## Cal submission queue -- Protocol A, SUBMITTABLE AS-IS (copy, do not rewrite). Queued S66; Cal not spawned.

**SUBMISSION 1 (Aen-ranked FIRST -- the cross-cutting one). GENUS: *verification narrower than it appears*. Aen 17:34 ruling: this entry carries the `-R` probe as its canonical TECHNICAL instance PLUS the two live health-check misses below as instances of the same genus. Sibling entry = SUBMISSION 4 (*control* narrower than its name); cross-link the two, do NOT merge (over-merge would collapse the observe/act distinction -- Cal dedup rule 3).**
`## Knowledge Submission / From: Hopper (instance 3 co-reported by Aen) / Type: gotcha / Scope: cross-team / Urgency: standard / Related: references/rc-host-db-tunnel-architecture.md, patterns/windows-user-context-persistent-bridge.md, SUBMISSION 4 / Confidence: medium on the -R mechanism, high on the genus (n=3 live misses across 2 agents + a signal-design instance, 1 session)`
**GENUS STATEMENT:** *A health check that verifies the NEIGHBOURS of a thing -- that a process exists, that a port answers, that a log is not full of errors -- is routinely mistaken for verifying the thing itself. The failure is silent and self-consistent: the check passes, so the substrate is declared healthy, and the disconfirming evidence sits unread in an artifact you already had the path to.* **Instance 2 (Hopper, 17:04):** before dispatching cross-team mail I checked Scheduled-Task state, live daemon process, hub `ping` and hub `status` -- all green -- and concluded the link was live. I never opened the courier's OWN log, which held 83 consecutive transport failures. The mail spooled undelivered. **Instance 3 (Aen, 16:24, root cause of instance 2):** at startup Step 3.5 Aen checked process-alive, lock-held, ledger-has-entries, and opened `fr-courier.log` -- **which is stdout and was 0 bytes** -- while every failure line went to `fr-courier.log.err`. An empty log beside a live process read as healthy. **AEN'S PHRASING, keep verbatim in the entry: "Absence of error is not evidence of function when you are reading the wrong stream." The check that would have caught both is *does this process's log show a SUCCESSFUL operation*, not *does this process exist*.** Corroborating detail: the 2-minute tool timeout that orphaned the daemon (parent killed at the deadline) is visible as courier-up 13:24:35Z -> first failure 13:26:37Z, 2m02s later. **Instance 4 (Hopper, 17:36, Aen 17:34 nominated it as the BEST teaching example -- trap is in the SIGNAL'S DESIGN, not in reading the wrong artifact): the hub `deposited_uncollected:{}` field reads IDENTICALLY before a deposit and after a collection.** A signal that cannot distinguish "nothing sent yet" from "sent and collected" is not a state gauge, it is a coin flip read as a gauge -- I caught it while confirming the fixed delivery and only called delivery confirmed on the CONJUNCTION of (deposit log line + emptied spool + hub view), never the hub field alone. Sub-lesson: a green reading that does not distinguish success from a DIFFERENT state entirely is the same genus even when you are reading the right artifact.
**CANONICAL TECHNICAL INSTANCE (instance 1) -- Content:** A TCP-connect probe against a reverse-SSH (`-R`) forwarded port proves only that **sshd still holds the remote listener** -- it does NOT prove the forwarded path can carry traffic. The remote `accept()` is performed by sshd locally and completes BEFORE sshd attempts to open the channel to the originating client, so a connect-only probe (`nc -z`, `timeout N bash -c "echo -n > /dev/tcp/host/port"`) returns success even when the far side cannot reach the forward target at all. Two distinct failure windows. **Lead with the permanent one -- a reader who meets the timing case first assumes that is all this means, and a probe that lies forever is a different and worse defect than one that is 90 seconds stale** (Aen framing, S66). **(a) PERMANENT -- the sharp claim:** ssh session perfectly healthy, but the *originating* host cannot reach the forward target (target down, DNS gone, auth/policy rejection at the target's edge). Probe reads OPEN indefinitely; the path never works; waiting does not correct it. **(b) STALENESS -- secondary:** originating host asleep/off-network but TCP not yet timed out -- sshd keeps the listener until keepalives fire (`ServerAliveInterval` x `ServerAliveCountMax`, e.g. 30x3 = ~90s, longer or never if keepalives are unset), so the probe reads OPEN for that window and then self-corrects. Only once the ssh session fully dies does sshd tear the listener down and the probe finally read CLOSED. **Consequence: a green TCP check on a `-R` forward is not evidence the tunnel works, and monitoring built on it reports healthy through a real outage.** Use an L7 probe that exercises the whole path instead (for HTTPS: `curl --resolve <host>:<port>:127.0.0.1 https://<host>:<port>/` and assert the expected status). Applies to every reverse forward FR ships or consumes, not just apex's.
**Evidence:** S66 GH #104. Substrate `apex-migration-research/.claude/bin/autossh-db-tunnels.sh:46-53` (`ServerAliveInterval=30`, `ServerAliveCountMax=3`); consumer `designs/deployed/apex-research/container/entrypoint-apex.sh:365-372`. Observed in-container baseline `11521 OPEN / 11522 OPEN / 11443 CLOSED`. **HONESTY CAVEAT -- confidence is `medium` not `high` on purpose: case (a) is derived from OpenSSH accept-then-channel-open semantics and is NOT yet field-demonstrated by me; I observed working forwards and an absent forward, never an OPEN-but-dead one.** Cheap confirming experiment for whoever gets there first: establish `-R <p>:<unreachable-host>:<port>`, then TCP-probe `<p>` on the remote -- predict OPEN. Promote to `high` on that result; treat as disputed if it reads CLOSED.

**SUBMISSION 2.**
`## Knowledge Submission / From: Hopper / Type: gotcha / Scope: cross-team / Urgency: standard / Related: none / Confidence: high`
**Content:** The idiom `if command -v X >/dev/null 2>&1 && X <args>; then OK else WARN fi` **silently collapses two different facts into one branch**: "tool X is not installed" and "the thing X tests is broken" both land in the else branch, and the else branch's message almost always blames the thing under test. The guard is written to make the check *safe* when the tool is missing, and instead makes it *dishonest* -- the check can never report OK on a host lacking X, and the operator is told the substrate is down. Failure is silent, permanent, and self-consistent: it never errors, never changes, and reads as a real finding. **Fix: make the three states distinguishable -- `if ! command -v X; then echo "SKIP: X unavailable, check not performed"; elif X <args>; then OK; else WARN; fi`.** Prefer a probe with no external dependency where one exists (bash `/dev/tcp` instead of `nc` for TCP liveness) so the guard is unnecessary. Generalises past shell to any `capability-present && capability-says-yes` conjunction.
**Evidence:** S66 GH #104, found while answering an unrelated network-path question. `designs/deployed/apex-research/container/entrypoint-apex.sh:368` = `if command -v nc >/dev/null 2>&1 && nc -z -w3 127.0.0.1 11521; then ... else echo "WARN: DB tunnel down"`. Probe: `docker exec apex-research sh -c 'command -v nc'` -> empty; `curl` present at `/usr/bin/curl`. **`nc` is absent from that container, so the gate has printed `WARN: DB tunnel down` on every boot regardless of true state and has never been capable of reporting OK** -- while the tunnels it "checks" were in fact UP (independently verified: `11521 OPEN / 11522 OPEN` via `/dev/tcp`). apex was about to duplicate the idiom for port 11443, which would have shipped a second permanently-dead probe.
**SUBMISSION 4 (Aen-ranked most immediately useful; absorbs the S66 bash-loop gotcha as instance (a) -> n=2). GENUS: *control narrower than its name*. Sibling of SUBMISSION 1; cross-link, do not merge.**
`## Knowledge Submission / From: Hopper / Type: gotcha / Scope: cross-team / Urgency: standard / Related: SUBMISSION 1, patterns/windows-user-context-persistent-bridge.md / Confidence: high (both instances directly observed)`
**GENUS STATEMENT:** *An operation whose name implies a total teardown is frequently scoped narrower than the name says. It succeeds, reports success, and leaves the part you actually needed changed still running.* **Instance (a) -- editing a script does not change a running loop.** Bash re-reads a script file between TOP-LEVEL commands; a `while true; do ... done` is ONE top-level compound command, fully parsed into the process's memory, that never completes. So an on-disk edit cannot reach it, and killing the SUPERVISED CHILDREN is not enough -- the interpreter process holding the loop must itself restart. Observed S66 16:53: killed all four autossh/ssh pids, respawn returned fresh pids carrying the OLD argv; wrapper bash pid 18832 alive from 10:48:05 throughout. **Instance (b) -- `Stop-ScheduledTask` orphans the descendant tree, it does not reap it.** On a wscript-launcher -> `WshShell.Run wait=True` -> bash-supervisor -> autossh -> ssh shape, Stop terminated ONLY the top-level `wscript.exe` and left **seven orphans running**, including the `ssh.exe` still holding the remote port binds. A straight Stop-then-Start would have hit `ExitOnForwardFailure` against its own predecessor's binds and churn-looped. **RULE: any stop-then-start of a Task-Scheduler-supervised tree needs an explicit straggler sweep in between; for a task holding network binds that sweep is the difference between a clean restart and a churn loop.** **TRAP FOR THE NEXT OPERATOR: sweep by EXPLICIT PID, never by command-line filter** -- my own probe shells matched the filter (their argv contained the search strings) and a filter-based kill would have killed the shell I was standing in.

**SUBMISSION 3** = the `[GOTCHA -- substrate, gitlab.evr.ee / CF Access]` entry below (already in submittable prose; Type: gotcha / Scope: cross-team). Confidence split, **RESOLVED 17:00 -- the 302 landed, standing instruction now APPLIED**: `high` on the DNS observations + the decoded `CF_Authorization` JWT (both directly measured). The claim "WARP device identity carries through an ssh `-R` leg" is now `high` **but SCOPED EXPLICITLY AND IN THE ENTRY BODY to: this workstation, this WARP enrolment, this CF Access policy** -- it is NOT a general mechanism finding. **Per Aen 16:52, the 302 does not license generalisation: a successful production path is not an experiment.** Evidence at that scope: from inside the apex container, `curl --resolve gitlab.evr.ee:11443:127.0.0.1 https://gitlab.evr.ee:11443/` -> `302 -> https://gitlab.evr.ee:11443/users/sign_in`, `x-gitlab-meta`/`x-request-id`/`x-runtime: 0.020972` origin headers, `set-cookie: CF_Authorization=<JWT>`, `cf-ray: a20372d8587b543c-TLL`. Generalising would need a deliberate experiment across a second workstation/enrolment, same as submission 1's `-R` mechanism. **Also record the measured redirect correction: GitLab echoes the received `Host`, so Host-derived redirects PRESERVE the port (`:11443/users/sign_in`); only `external_url`-generated URLs (UI clone URLs, notification email links, some API absolutes) are portless. An earlier FR draft claimed all redirects were portless -- that was a workstation-side probe taken before the tunnel existed and is SUPERSEDED.**
**SUBMISSION 5 (S67 Task #2, Aen 11:08 adopted my reframe + told me to draft+file as `finding`).** FULL TEXT in scratch `cal-submission-5-framework-finding.md` + reported verbatim to Aen 11:06 (Aen places it -- I can't write findings/ or wiki). Claim: *a non-persistent host reached over an overlay network is disqualified as a shared deployment substrate -- not only because it can be powered off, but because the overlay access path is itself a SPOF that presents IDENTICALLY to a host outage.* Worked example = RC host, 2 incident classes: (type2) real weekend cold-boot gaps; (type1) 2026-08-03 live ~2-3min WARP flap, host STAYED UP (boot unbroken, apex RestartCount=0, warp-cli healthy) yet apex saw it as a shutdown. Same genus as SUBMISSION 1 (a signal that can't distinguish two states read as a gauge), on the availability axis. Confidence high.

## In-flight dispatches

[DISPATCH -- S66 (2026-07-24) 16:34 -- GH #104 apex GitLab forward. Tier R locate DONE; Steps A+B HELD pending team-lead ruling. NOTHING MUTATED.] Aen-dispatched. Ask = add container-local `11443 -> gitlab.evr.ee:443` to the tunnel serving apex's 11521/11522. **MECHANISM IS NOT STATIONMASTER** (apex's issue text is wrong). It is an operator-side reverse-SSH bridge on the PO's **Windows workstation**, owned by the **apex-migration-research repo**. L1 = `designs/deployed/apex-research/container/entrypoint-apex.sh:365-372` (FR ships only the soft `nc -z 127.0.0.1 11521` probe; its own comment disclaims tunnel ownership). L2 = `apex-migration-research/.claude/bin/{autossh-db-tunnels.sh,run-tunnel-hidden.vbs,register-tunnel-task.ps1}` + Task Scheduler task `ApexResearch-DBTunnels` (Running; user-ctx Interactive/Limited; triggers at-logon + Power-Troubleshooter EventID1; IgnoreNew). apex repo on main, `.claude/bin/` CLEAN @183de33b. L3 = single chain wscript11120 -> bash19300 -> bash18832 -> bash15612 -> autossh15136(shim) -> autossh17200(supervisor) -> autossh11588 -> **ssh14728 (Windows-native OpenSSH)**; **ONE ssh session carries BOTH -R forwards**. RC `ss -lnt`: 11521+11522 LISTEN on 127.0.0.1 AND [::1]. Log = 40 lifetime wrapper respawns + ssh-restart count 30 (matches apex's status line exactly). NO L1/L2/L3 drift; wiki `references/rc-host-db-tunnel-architecture.md` + `patterns/windows-user-context-persistent-bridge.md` still accurate. **PORT: 11443 FREE on RC** (only other 11xxx = 11434 Ollama). **TIER D rationale (the catch): `-o ExitOnForwardFailure=yes` is SESSION-WIDE -- a bind failure on 11443 refuses the whole ssh session and takes 11521+11522 down, then the wrapper loop respawn-fails every ~11s forever. One added line, blast radius = apex's entire Oracle path.** Append the new `-R` AFTER 11522 so the cleanup discriminator `pkill -f "ssh.*-R 11521:vjsdbtest"` keeps matching. **2 SCOPE FLAGS surfaced, not crossed:** (a) my MAY-NOT bars commands on the Windows dev workstation and the whole substrate lives there; (b) the file is in ANOTHER TEAM's repo on clean main -- proposed alternative = FR specifies the diff, apex/PO applies.

[GOTCHA -- substrate, apex-research, LIVE DEFECT found S66 16:42] **`nc` is NOT installed in the apex container** (`command -v nc` -> nothing; `curl` present at /usr/bin/curl). The Oracle tunnel gate at `designs/deployed/apex-research/container/entrypoint-apex.sh:368` is `command -v nc >/dev/null 2>&1 && nc -z -w3 127.0.0.1 11521` -- with nc absent the guard SHORT-CIRCUITS and always falls through to `WARN: DB tunnel down` regardless of real state. **That probe has never been able to report OK**; apex's "11521 UP" status line comes from somewhere else. Their #104 plan to mirror it as `nc -z 11443` would ship a second probe that also never runs. VERIFIED nc-free replacement (ran it, works in-container): `timeout 3 bash -c "echo -n > /dev/tcp/127.0.0.1/$p"` -> baseline `11521 OPEN / 11522 OPEN / 11443 CLOSED`. **Second-order: TCP liveness on a `-R` forward is WEAK regardless of tool -- the RC-side listener is held by sshd, so it accepts even when the Windows-side leg is dead; you see OPEN on a tunnel that cannot carry traffic.** For 11443 recommend L7: `curl --resolve gitlab.evr.ee:11443:127.0.0.1 https://gitlab.evr.ee:11443/` expect 302. [candidate Cal, folds into the CF/WARP gotcha above]

[GOTCHA -- local dev, Windows] **My OWN 16:34 draft carried the bad version and Aen had already relayed it to the PO before my 16:42 apply-pack landed; Aen retracted it downstream 16:48. The CIM version is authoritative.** Do NOT hand the PO `pkill -f "autossh.*..."` as a cycle command for this bridge. Git Bash's procps `pkill` vs scoop's msys2 `autossh.exe` is the SAME cross-MSYS boundary `wiki/gotchas/cross-msys-argv-mangling.md` documents for this exact binary pair -- if it matches nothing it exits 0 and the operator believes the change applied while the OLD argv still runs. UNVERIFIED either way; I refused to ship it. Deterministic substitute given to PO: `Get-CimInstance Win32_Process -Filter "Name='autossh.exe' OR Name='ssh.exe'" | Where-Object { $_.CommandLine -like '*-R 11521:vjsdbtest*' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }` (matches the true Windows cmdline, cannot silently no-op, and kills the ssh child too so the respawn rebinds clean instead of colliding with a survivor holding the ports).

[DECISION -- S66 16:37, Aen correction TAKEN] My 16:34 report pre-classified the change "Tier D" and the feasibility probe "Tier M". **Aen: the tier call is the TASKER's at dispatch time; mine is to VALIDATE it, not assign it.** Both labels WITHDRAWN and restated as evidence instead. Correct shape when reporting a locate: describe the mechanism and the blast radius, answer the tasker's own stated discriminator, supply the exact command verbatim -- and let the tasker attach the tier. My prompt already says this ("the tasker classifies the tier at dispatch time; you validate the classification") -- I inverted it because the locate came back before any dispatch existed to validate against. **Standing rule for future locate-then-change dispatches: a locate report carries facts + verbatim command + blast radius, NEVER a tier label.** (Also confirmed no PO-held S60 infra value was needed -- host/key are hardcoded at `autossh-db-tunnels.sh:18-19`, no tmux in this substrate at all; launcher is wscript + Task Scheduler.)

[GOTCHA -- substrate, gitlab.evr.ee / CF Access] **apex's stated premise for #104 is FALSE, but the request survives for a different reason.** Oracle works because `vjsdbtest.evr.ee` -> **10.100.34.42** (internal RFC1918) -- CF is genuinely not in the path. `gitlab.evr.ee` from this workstation -> **172.66.43.48 / 172.66.40.208 / 2606:4700:3108::ac42:28d0 = Cloudflare anycast**; GitLab is CF-fronted, there is NO internal IP to forward to, and a TCP forward cannot route around CF Access. What actually passes Access is the **WARP device identity of the originating machine**: Tier-R `curl -I https://gitlab.evr.ee/` from this box returns **302 -> /users/sign_in** (GitLab origin headers `x-gitlab-meta`/`x-request-id`/`x-runtime`), NOT the cloudflareaccess.com 302 apex got, and sets `CF_Authorization` whose JWT carries `iss: eestiraudtee.cloudflareaccess.com`, `email: mihkel.putrinsh@evr.ee`, `device_id: 95fd90cf-...`, **`warp_as_auth: true`**. So feasibility hinges on whether ssh.exe's outbound leg is WARP-intercepted like curl.exe's (per-machine interception => very likely, NOT proven -- refused to report it working on inference; Step A throwaway `-R 11444` probe designed to prove it). **Second-order requirement apex has NOT accounted for: the forward is plain TCP and TLS terminates at the CF edge, so connecting to `https://127.0.0.1:11443` sends SNI/Host `127.0.0.1` and CF will not route it. `127.0.0.1 gitlab.evr.ee` in the container's /etc/hosts + `https://gitlab.evr.ee:11443/...` is a PRECONDITION, not a follow-up.** Also: `nc -z` on a `-R` forward is a weak liveness signal (the RC-side listener is sshd's, so it accepts even when the Windows leg is dead) -- their existing 11521 check has the same weakness; recommend an L7 curl probe for 11443. [candidate Cal: "reverse-forward through a CF-Access-fronted host carries the ORIGINATING machine's device identity, not the consumer's -- and nc -z cannot see a half-dead -R forward"]

[OUTCOME -- Task #3 + #7 S50 CLOSED 2026-06-12. FULL AUDIT in ops-log-2026-06 (6 entries).] **Task #3 (T6.a gate):** harness `poc/ghost-bridge/t6a-race-harness.py` (Python open('x') + bash set -C, 50-round 2-proc race, mirrors courier.py:773-785). PASS on prod-llm: host-fs ext4-on-LVM (load-bearing, gate-of-record per Aen re-adjudication = prod-llm IS customer substrate) + in-container sm-state volume mount (confirmatory) -- 50/50 both primitives both runs, 0 anomalies. Cal gotcha filed: tmpfs-vs-deployment-fs trap (wiki 125). **Task #7 (deploy):** stationmaster hub LIVE+healthy on prod-llm (Debian 13, ext4, sshd:2222, host key on sm-state vol). Caught 2 Brunel artifact defects mid-deploy (NOT patched, surfaced w/ Layer-3 evidence, Brunel fixed both): (1) entrypoint host-key `ssh-keygen -A -f` lands 0 keys OpenSSH 9.2p1 → fixed f022fed (direct ed25519); (2) `sm` shell nologin blocks forced-command → fixed 909bbe9 (/bin/sh). Smoke-test 14/15 -- the 1 fail is a TEST-ORDERING ARTIFACT (expects E_NOGRANT, hub correctly returns E_UNKNOWN_TEAM pre-first-connect; registry is lazy-populated on connect not at sm-register) = Brunel test-script bug, NOT hub defect. Volume REUSE (Aen ruling: gate intent=unknown-provenance, mine enumerated-inert). 2 scratch keys alpha/beta left registered+inert for S51. **[GOTCHA×2 candidate Cal]:** ssh-keygen -A+-f prefix fails OpenSSH 9.2p1; nologin shell defeats authorized_keys forced-command (Windows unit-smoke missed -- tested sm-shell direct, not via sshd login-shell). **3 layered-gate catches this arc: EXIT-0≠success, transport-green≠protocol-OK, smoke-fail-triage=test-artifact-not-defect.**

## Prior-session archive (pruned at S50 shutdown per 100-line discipline)

[ARCHIVED] S35/S36/S37 dispatch transcripts (apex db-tunnel #6, fr-cma-pilot CF bypass #1, secret-rotation #2) pruned -- full records in docs/operations-log-2026-05.md. Durable lessons already promoted: wiki/patterns/discriminator-anchored-on-sub-canonical-source.md + three-layer-substrate-truth-discipline.md (Cal-filed); Layer-0 library-first + probe-design-as-sub-canonical-source (A.3) + L2-unreachable-graceful-degradation captured in those patterns. apex-research substrate facts retained above (host-networked, ssh-keygen behaviors, , etc.).
