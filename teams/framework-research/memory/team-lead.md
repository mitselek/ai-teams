# Team-Lead Scratchpad (*FR:Aen*)

> ## ⚠ THIS FILE IS A CLAIM ABOUT SOURCES. IT IS NEVER A SOURCE.
>
> Written as one agent's aide-memoire; **read by every agent at startup as an authoritative record.** Nothing in it marks that change of status, which is why it is added here explicitly.
>
> **Before citing anything below as fact -- especially in another agent's artifact -- open the thing it describes.** On 2026-08-19 a librarian correctly consulted a source before altering a submitter's accurate claim, found this file, and published a false statement from it; the submitter's original was right. Every step of that was diligent. **The artifact did not tell him what it was.**
>
> Known failure directions, all observed live: entries here can be **stale**, **incomplete-at-authoring**, or **pessimistic**. **Line-number and file-path references here have been wrong more often than right.** S67 added: this file claimed the Estonian hand-over "drafted" (S66) — false; and its own summary header drifted from spec in 2 of 6 agents' files while their owners audited everyone else's.

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S67 (2026-08-31) CLOSED clean. Parent = Fable 5 (on-pin). CLI **2.1.251** (autoupdated from 2.1.250 overnight), SLUG `session-b9269601`, session pid **35188**. Cell = Callimachus + Finn + Volta + Hopper + Brunel (all opus per PO model rule).
- **S67 DELIVERABLES:** (1) **Joosep arc CLOSED end-to-end**: rebuild (Estonian FIRST-TASKS + paunvere seed + TEAM_NAME), 9a-9c-9d complete (dev key revoked, PO+Joosep checks passed), Step 12 registries, Step 13 hand-over SENT (signed Aen p.p. Mihkel), package moved to `designs/deployed/joosep/`. (2) **TTL batch (12+1 entries) CLOSED**: 3 probes + 2 records checks; wake row REFUTED for bare sessions, hook-cap safety correction FILED (urgent), born-wrong class discovered (4+3 defects found in entries, none by decay-checking). Wiki 214→218.
- **Key S67 findings:** graceful-exit GC (dirs+registry collected on handleable exit, n=5); Stop-hook `additionalContext` injection UNBOUNDED by the cap (live incident, contained); ghost sends refused since 2.1.247 (registry-gated, not members[]); Protocol B precondition rule (9 submissions/0 queries — the write-only wiki); ~6 person-attached rules failed their own authors, every catch was structural.
- **Carry-forward:** see NEXT-SESSION BOOT below. **Cal's filing hold (PO instruction, provenance unconfirmed) holds ~18 ruled submissions — S68's first order of business per PO: Cal addresses his backlog.**

---

### S67 WRAP (2026-08-31) -- session CLOSED clean

- **Joosep chain:** md5-gated FIRST-TASKS displacement (artifact-pair check, Brunel's correction of my wrong-operand brief); frozen-tree rebuild (digest STOP = Windows/Linux md5sum artifact → per-file-manifest ruling); stale `.env` was `TEAM_NAME=joosep` not `vedur` (read-replace-report vindicated); restart + 9d both needed PO (classifier denials, neither routed around); 9d net = PO's held terminal after Hopper EXPOSED his ControlMaster substitute as fake (timing-proven silent fresh-connection fallback). Joosep's key = DEFAULT filename — the guessed `id_ed25519_joosep` was in SIX artifacts incl. Connect-Joosep.ps1 which had NO path to success (pre-flight early-return + keygen advice = lockout); all corrected (`da464c0`), history preserved. Hand-over `designs/deployed/joosep/HANDOVER-JOOSEP.md` PO-approved via PR #112, signed, SENT.
- **TTL batch:** G3 = records lag (task-#7 closed at S58, unharvested 74 days); G1 Drain row HOLDS (<50ms live); G2 = wake REFUTED bare/VERIFIED active (<50ms), members[]-injection row REFUTED (registry-gated since 2.1.247 — we re-derived our own 08-27 wiki entry: Protocol B rule born), O10 all-confirmed via PO's probe-rig (hooks load project-scope, Stop fires, injection works & LOOPS — see safety correction), cold-start ordering holds (window NOT restamped — bg≠interactive), sessions-json GC split graceful/ungraceful (guidance UNCHANGED: intermittently-correct registry is a worse trap). G4 = zero drift, 4 born-wrong defects (wrong-sibling citation root cause, 1 reassigned to Finn on read-back).
- **Session self-audit ledger (the day's real product):** every agent + me corrected own claims publicly. Named forms now in wiki or queue: wrong-denominator/membership-rule (n=3+2 illustrative), born-wrong vs decay vs self-inflicted-staleness (3 classes), disjoint-remedy test (+2 boundary conditions: generating-instance uninformative; shared frame/venue ≠ mechanism), eliminated-confound ≠ identified cause (Hopper ×2), substitution-that-feels-like-derivation (Volta's naming), point-at-the-mechanism-not-the-copy (Brunel's 3-in-1), defense-vs-trigger split (file the remedy, mark mechanism open), authorization≠executability (no-slot form 11), stated-rule-didn't-protect-author (~6 instances, 1 counter-example).
- **Commits this session:** `718e76a`/`a557b1d` (PR #112 hand-over + exec bits), `da464c0` (key-path fixes + registry flip), `612fde3` (signature), `8d8ce6b` (deployed/ move) + this close. My errors on record: timestamp fabrication (+67min, self-caught via causality violation), 2 relay amplifications, 1 availability-bias diagnosis (all retracted, all with Cal).

### NEXT-SESSION BOOT (re-orient instructions for S68)

1. Read `startup.md` first (always). Substrate pins unchanged: `FR_COURIER_TEAM_DIR_NAME=<discovered-slug>` for restore/persist; Step 3.5 with `-SessionPid <claude pid>`; courier log = `fr-courier.log.err`. CLI was 2.1.251 at S67 close — expect a bump; version-pinned wiki entries re-verify per the retire-and-forward lineage rule, NOT wholesale.
2. **Pull first** (two-box rule); diff against both parents on any conflict in THIS file.
3. **Don't pre-spawn.** Wait for PO direction. **EXCEPTION pre-agreed at S67 close: the PO's stated S68 agenda is "Cal addresses his backlog" — spawn Callimachus first on PO confirmation.**
4. **Cal's backlog (S68's main work), in order:** (a) LIFT the filing hold (PO to confirm; its provenance was never confirmed in S67 — ask once) → Cal files the held queue: Hopper 9+2 corrections, Brunel 6, Volta 3, all pre-classified/rated/read-back-assigned in his scratchpad header; (b) his scratchpad prune (227+ lines vs 100 — the largest debt); (c) instance-2 fold into `query-the-librarian-before-reporting-a-discovery`; (d) strikethrough entry + artifact-claims fold; (e) SessionStart counter-example onto submission 6's entry.
5. **If PO greenlights contract changes:** the WikiProvenance reconciliation (item 8 structure below in S66 wrap remnant + Cal's scratchpad) needs **Celes spawned** — she owns prompt lines; 3 prompt-layer changes queue for her: reconciliation lines, dispatch-template Protocol-B precondition, scratchpad-header check (instrument/cross-audit form, NOT self-check — Volta's own STOP). Cal recused on confidence half.
6. **asyncRewake boundedness = S68's first probe candidate** (gating question for hook-based courier inbound). Dispatch MUST carry the four conditions in the S67 safety-correction block below — never a bare "test if bounded", never a human's terminal. Hopper executes only on explicit dispatch (his self-routing, preserved verbatim below).
7. **If Joosep surfaces problems:** container `joosep` on RC:2231, registry `live`, runbook at `designs/deployed/joosep/PROVISIONING-RUNBOOK.md` (S67-corrected), backup `authorized_keys.bak.20260831-120122` on RC, `joosep_sshd` volume still UNBACKED (PO one-liner pending). His key = default `id_ed25519`.
8. **PO's own open items (remind once if unmentioned):** Step 12 items 2/3/5 (both rc-deployments.json copies + deployments.md — use CORRECTED key field from registry-rows.md); `joosep_sshd` backup one-liner; note to Lerko (new 2231 row + backup file in his dir + header-authority proposal); `gho_` token rotation in `~/.claude/settings.json`; Entu #42 strategic question; exec-bit normalization (24 files, 2 correct clusters) yes/no.
9. **Volta items (untasked since S63/S65/S67):** version-actual-trigger startup check; startup.md host-check table; `inter-team-comms` retirement; `sanitize-inboxes` configs[0] bug; oldest-unacked check; `.log`→`.log.err` verify-line fix; #109 tailscale re-triage-by-reading.
10. **Courier code items (Brunel-or-Hopper, queued):** surfacing self-test at startup (round-trip one entry, confirm SURFACED); `_has_live_session` bg-gate (sweep precondition: MAY NOT RUN with unaccountable live bg sessions); container-packaging hardening (prohibition comment + build-time no-settings.json assert).
11. **apex loop:** nothing outstanding either side. Courier died with S67 (detached pid 14456) — restart at S68 Step 3.5.
12. **S68 confirmatory observation (NOT blocking):** `sessions/35188.json` + `teams/session-b9269601/` should BOTH be absent after this graceful close (graceful-GC n=5 predicts it; mechanism already demonstrated).

### Standing watch items going into S68

- **PROCESS RULE (S67):** probe plans carry a Protocol B precondition — per EXISTING entry `query-the-librarian-before-reporting-a-discovery` (S67 = instance 2, self-referential). Health signal: 9 submissions / 0 queries = write-only wiki. Durable trigger = dispatch-template line (Celes queue).
- **S67 SAFETY CORRECTION (FILED, all 3 layers):** `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` does NOT bound injection-driven continuation — cap counts BLOCKS (exit-2) only; exit-0 `additionalContext` drives UNBOUNDED self-wake (n=14 consecutive, live incident, contained, user settings md5-proven untouched). Guard = `stop_hook_active`. **asyncRewake test conditions (Hopper via Volta, endorsed): dedicated throwaway WE own; hard iteration counter INSIDE the hook as PRIMARY stop (config-edit kill lags 2-3 turns — nothing outside the hook acts within one turn); faster-than-config-edit kill as backstop; stop_hook_active guard. Hopper's self-routing verbatim: "my own judgment about the safety of a hook design is exactly the thing this morning devalued, so the ask should come from someone other than me, with the target session named."** DESIGN GATE: hook-based courier inbound carries its OWN counter. Unresolved wrinkle: hook-config read timing (removal lagged turns; mid-session install never fired) — filed tension-stated, hypothesis-free.
- **Cal's watches (S66 + S67):** proxy-bound; gate-buys-pushback; hygiene-rule-destroys-diagnostic-absence; timestamp-fabrication (now has my +67min instance, cross-agent-clock detection); four-benign-crossings (STILL HYPOTHETICAL — my reversal-crossing diagnosis RETRACTED; the O6 retry instruction's vehicle = observed-but-unexplained, reached Hopper as a direct operator turn); point-at-the-mechanism umbrella candidate (3 instances, one work-stream — needs a 4th from elsewhere).
- **MOVED stubs:** 4 old ones TTL ≥2026-09-27 + `designs/new/joosep/README.md` TTL 2026-10-01 (zero-inbound grep then remove; ~28 inbound refs in wiki/ops-log/scratchpads follow it).
- **Blob-not-worktree md5s:** Brunel's frozen-tree attestations are BLOB values (`git show :<path>`) — update-index re-materialised joosep.sh worktree as CRLF; a worktree gate would false-STOP a healthy package. `file(1)` is the CRLF authority, not `grep -c $'\r'`.
- **Celes owes Finn** 2 prompt items (S63); roster-drift survey untasked; Medici read-backs 2 owed (since S62). **Herald untouched all S67** — the `negative-probe` restructure read-back (Hopper or Herald) and his wake-contradiction entry amendment await.
- **Inbox lag is now cross-agent** (Cal S66, Hopper S67 ×3 resends) and once delivered an instruction after its cancellation — the defense (re-validate aged authorizations against current state) is queued as a finding; still local-Windows-triage, not wiki-grade as lag per se.
- **Artifact gallery = two islands** (PO's two accounts); trust publish result over later watch-death.
- **allerk pid-ceiling gap** (PO-19) — PO to tell Lerko; not ours. **#110 outcome** (Joosep hiring w/ Ruth) — PO's; permanent status now de facto (container live, handed over).
- **Rail-safety org items (S66, PO's):** branch protection on `rumba`, named safety owner, EvrSK TEST range-check question; §5 findings relay to Ruth = PO's.

---
## Older sessions -- pruned to pointers

- **S66 (2026-08-28..31):** Joosep container BUILD arc (4 attempts, 3 pre-existing defects), paunvere design, Celes respawn incident (`name-addressed-send-resumes-a-terminated-agent`), wiki 185→214, confidence-field discovery, interim rule (AMENDED S67: "absent from entry = rated on card"; old wording revoked as false). Contract-change queue RESTRUCTURED S67: (A) WikiProvenance reconciliation (merged old #1+#4; + one-lineage-one-TTL invariant, retire-and-forward BOUND, status-semantics/renamed-third-state per Volta's govern-present-behaviour test); (B) discoveryOrder MINOR; (C) negative-probe restructure. Full record: `git show 8d8ce6b:teams/framework-research/memory/team-lead.md`.
- **S65 (2026-08-27):** #108 stationmaster arc, canonical package `designs/deployed/stationmaster/`, wiki→193. `git show e92f777^:...`.
- **S64 (2026-08-26):** RC docker-root runbook, artifact 99523dce, sudoers findings. Same pointer.
- **S63 ×2 (2026-08-19 Win / 2026-08-22 Linux):** parallel-session records both real, cite by host. Same pointer.
- Durable lessons live in the wiki, not here.

(*FR:Aen*)
