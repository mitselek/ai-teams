# Team-Lead Scratchpad (*FR:Aen*)

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S63 CLOSED (2026-08-22). Ran on the **HOME LINUX BOX** (not the Windows FR host) on **fable-5** -- roster pin matched for the first time in 4 sessions. SLUG `session-d0108b23`. Cell = **Celes + Hopper**.
- **S63 KEY RESULT: Passepartout** -- the PO's private-life personal assistant, full arc in one session: brainstormed section-by-section with PO -> Celes design package -> 6 third-party review findings folded (rev 2, all PO-sanctioned) -> name RATIFIED (Passepartout; Kratt runner-up) -> deployed `designs/deployed/passepartout/` (rev 4) -> Hopper installed `~/passepartout` -> test-run PASSED (`passe` alias launches with initial prompt so the PA introduces itself; `pp` bare).
- **Key decisions:** solo Claude Code project, NOT a team (YAGNI, growth path noted). Authority: organize+draft / box sudo (changelog-logged) / hub comms autonomous; send / book / spend confirm-first, backstopped by a settings.json DENY list (send_message/reply/forward/respond_to_event) so headless runs *cannot* cross the line. Hub egress rule: instructions only from Mihkel, peers get coordination data never household contents. Freshness rule = our S62 TTL ruling applied to PA memory. **Cron deliberately NOT installed** -- PA writes its own playbook bodies first (its backlog #8, with verified path `/home/michelek/.local/bin/claude`).
- **CARRY-FORWARD:** ALL S62 items untouched by S63 -- VEO-78 (oldest commitment), Finn's 3 tasks, Cal's 2 defects, 7 stage-2 gates, ask-Schliemann, roster-drift survey. PA's own next steps live in `~/passepartout/backlog.md` (8 items); PO settles §11.2-6 with the PA directly -- NOT an FR task.
- **LEARNED:** (1) **Spawn-by-reference** (PO feedback, saved to auto-memory): never inline a roster prompt into a spawn -- send roster identity + "read your own prompt file" + task. (2) **Anchoring:** I floated name candidates in Celes's brief despite the PO delegating the creative choice to her -- she picked my #1 and the PO called it out; the unanchored candidate (Kratt) was the genuinely-hers one. Leaner briefs protect delegated judgment. (3) Sessions never speak first -- an alias carrying an initial-prompt arg is the clean auto-intro fix.
- **Substrate notes (home box):** startup Steps 1-3 clean; resolver worked with bare `$PPID` inside lifecycle scripts (no `FR_COURIER_TEAM_DIR_NAME` override needed, unlike the Windows box); Step 3.5 courier N/A here (PowerShell, Windows-only) -- skipped legitimately; Gmail/GCal/Drive MCP live on this box; artifact published for the design (claude.ai/code/artifact/bd596e27, rev 2 content -- rev 4 delta is alias-only).

---
## S63 transcript (2026-08-22, home Linux box)

- Startup: model matched roster (fable-5) after PO switch; pull brought S62 batch; `git status` before trusting closing record (S62's own rule) -- tree clean.
- PO scope: personal assistant for private deeds (sibling of po-team, but private side). Brainstorm rulings: 5 domains (mail+calendar, paperwork+finances, household+errands, personal projects, the Ubuntu box itself); both modes (on-demand + cron); local-only home -> revised to git-with-non-GitHub-remote (backlog #1); English; ~ launch -> revised to dedicated dir + alias after CLAUDE.md-walks-up-the-tree leak hazard surfaced.
- Home-dir survey as design input (PO directive): 235 entries/96 loose files, /home 93% full, literal `~/` dir, 35 dir-name collisions -- all folded into design §1 + backlog seeds.
- Third-party review (Verne-literate): 6 findings, all verified by me, all sanctioned by PO -- mail-suppression gate (contacts-only burial + archived-list visibility), draft provenance (confirm the pair), hub egress, memory freshness, restore drill, confirm semantics (explicit + quotes item + expires).
- Deploy: git mv new->deployed AT THE SAME TIME as Hopper's read -- his verify caught a 0-byte extraction and he re-ran from the new path (verification-discipline win; the race was my sequencing, avoid parallel mv-and-read next time).
- Commit 42e61d4 (package rev 3 + move). Rev 4 (alias purpose-split: `passe` carries startup prompt, `pp` bare) committed at wrap.

(*FR:Aen*)
