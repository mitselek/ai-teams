# Team-Lead Scratchpad (*FR:Aen*)

> ## ⚠ THIS FILE IS A CLAIM ABOUT SOURCES. IT IS NEVER A SOURCE.
>
> Written as one agent's aide-memoire; **read by every agent at startup as an authoritative record.** Nothing in it marks that change of status, which is why it is added here explicitly.
>
> **Before citing anything below as fact -- especially in another agent's artifact -- open the thing it describes.** Entries here can be **stale**, **incomplete-at-authoring**, or **pessimistic**; **line-number and file-path references have been wrong more often than right.**
>
> **S68 supplied the sharpest instance yet, and it was mine:** I built Cal's brief from this file's unverified header and **manufactured three tasks that did not exist** (a 7th Hopper submission, a "casualty 3", a 227-line debt that was 46 lines and compliant) -- after reading this very warning, and while telling him in the same message to verify against the tree. Filed as instance (c) of `coordinator-supplied-material-anchors-the-delegation`. **A summary decays; a relay laundering it into an instruction is what gives it authority.**

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S69 (2026-09-02) CLOSED clean. Parent = **Opus 5** (OFF-PIN; roster says `claude-fable-5[1m]` -- PO override, roster NOT updated, Step 0.5 will re-trip). CLI **2.1.258** on PATH (session process ran 2.1.257; S67/S68 recorded 2.1.251 -- **revision trigger HAS fired**). SLUG `session-6425b920`, session pid **27176**. Cell = Callimachus + Brunel.
- **S69 DELIVERABLES:** (1) Courier found DEAD (pid 14456, stale lock, last act a failed collect 2026-09-01T05:25Z); **2 unconsumed apex-research messages recovered** via collect-only peek, archived verbatim to `docs/cross-team-inbound-2026-09.md`, committed `0b8633e`, THEN acked -- hub verified empty. Courier restarted (pid 22056, bound `session-6425b920`), then STOPPED at S4 (see below). (2) **PR #113 merged** (`446251f`) on Brunel's GREEN: base-image CLI 2.1.217 -> 2.1.258.
- **S69 KEY FINDING (Brunel, verified by me):** `engines.node` crossed **>=18 -> >=22 exactly at 2.1.217**, while `ai-teams-claude:latest` takes Node **18.19.1** from ubuntu:24.04 apt -- the base image has installed an engine-incompatible CLI since `03985fb` (#103). **Latent** (every live consumer overrides Node to 22.14.0 first); **the build cannot report it** (`Dockerfile:53` trailing pipe to `tail -5` makes the layer exit status tail's, always 0). Fix already exists at `Dockerfile.probe:59-63` and never propagated.
- **Carry-forward:** see NEXT-SESSION BOOT. **Two open decisions the PO deferred to S70: Brunel's line-53 fix PR, and Brunel's read-back of Cal's 5 `stage-2: pending` entries.**

---

### S69 WRAP (2026-09-02) -- session CLOSED clean

- **This was a RESUMED session across days.** S68's work happened 2026-08-31 (commits 13:22-14:44); resumed 2026-09-02 09:05 under new pid 27176. **I briefly mis-flagged this as a clock / timestamp-fabrication problem -- it was not.** Cal's entries dated `2026-08-31` are CORRECT. `sessions/<pid>.json` `startedAt` is the current PROCESS start, not the conversation's.
- **Courier discipline, NEW and load-bearing:** I **stopped the courier at S4** rather than leaving it up. Reason: on 2.1.178+ a graceful exit GCs the team dir, so a courier left running would collect from the hub, inject into a **deleted** `inboxes/`, and **ack** -- converting the hub's two-phase durability into silent loss. `startup.md` says restart-at-session-start handles rotation; it does not address this window. **Candidate finding, not filed.**
- **The courier log misled me twice in one session, in opposite directions.** 4 transport failures / ~270 cycles are transient (WARP flap + one `0xC0000142`); the daemon logs `inbound: injected+acked` **only when it injects**, so **silence = healthy**. I first read the silence as breakage. Mirror of S68's `lifecycle-bridge-reports-success-over-empty-payload` -- there a success line over nothing, here nothing over success. Both times the log's shape did not map to the state.
- **My published error, retracted with apex on the PR:** I told them "FR can validate independently" -- **false, there is no docker on this box** (not on PATH, no `C:\Program Files\Docker`). Also retracted my "aligns containers with the host" argument: the container/host delta is the **runtime**, not the version, so host exposure is not evidence about a Node-18 container. **Both merge gates I proposed were blind to the real defect** (the probe harness installs Node 22; host exposure runs on host Node).
- **The dispatch framing worked and is worth repeating.** I briefed Brunel explicitly AGAINST my own review -- my comments labelled a claim never a source, every assertion listed for him to verify not inherit, questions declared "a floor, not a ceiling", and my framing named as the likeliest hiding place for a defect. He then overturned 2 of my 4 claims and found a defect neither of us had asked about, in his own file. **Per `ask-the-specialist-dont-prefetch`: the anti-frame is what bought the finding.**
- **Commits this session:** `0b8633e` (inbound archive), `446251f` (PR #113 squash-merge), `7b8e325` (Brunel scratchpad) + this close. Rebased onto the merge to keep `main` linear.
- **Heredoc gotcha hit again, live:** writing this file via a bash heredoc failed on a `$'\r'` literal. Cal's filed gotcha (*heredocs with backticks/quotes fail -- write a file instead*) is correct and now has another instance.

### NEXT-SESSION BOOT (re-orient instructions for S70)

1. Read `startup.md` first (always). **Step 0.5 WILL trip:** roster pins team-lead to `claude-fable-5[1m]`; S68/S69 ran on **Opus 5** by PO override with the roster left unchanged. Either update the roster or expect to re-raise it. `config.json` now shows `"model": null` rather than a stamped parent-model string -- **the premise of the Step 0.5 gate may no longer hold; verify before trusting it.**
2. **Substrate pins:** `FR_COURIER_TEAM_DIR_NAME=<discovered-slug>` for restore/persist AND for any courier call. **`$PPID` is 1 under the Bash tool on this box -- the resolver's pid tiebreaker is UNUSABLE from bash.** Identify the slug from the session UUID instead (`sessions/<pid>.json` -> `sessionId`), or pass `--session-pid <claude pid>` explicitly. Courier log = `fr-courier.log.err`. **Two live session dirs existed this session**, so bare auto+liveness is ambiguous -- the Herald sole-migrated-team precondition no longer holds on this box.
3. **CLI is 2.1.258** (was 2.1.251 at S67/S68 close). The **revision trigger has fired** on version-coupled entries -- re-verify **per the retire-and-forward lineage rule, NOT wholesale**. This is Cal's, and it is untasked.
4. **THE TWO PO-DEFERRED DECISIONS (S70's first order of business):**
   (a) **Brunel's line-53 base-image fix** -- replace the exit-status-swallowing pipe with the `Dockerfile.probe:59-63` assertion, and either add Node 22 to the base or state in the Dockerfile that consumers MUST override it (today's undocumented truth). One-line PR, his to write on request. **Take this before any base rebuild.**
   (b) **Brunel's read-back of Cal's 5 filed entries** (md5-scoping amendment, `agreement-across-copies`, exec-bit, merged-vs-shipped, growth sub-shape). All at `stage-2: pending`; his read-back is what promotes them. **Volta owes read-backs on his 3 too.**
5. **Rebuild gate, if a base rebuild is sanctioned:** `docker run --rm ai-teams-claude:latest claude --version` must print `2.1.258` -- derived from the artifact, not a remembered value. **Hazards:** `:latest` is a floating tag, so rebuilding retags it and *any* later apex rebuild silently adopts it (delayed, invisible blast radius); a rebuilt image needs `--force-recreate` (S52); volumes survive. **FR cannot build -- no docker on this box.** Any build is RC = Hopper/PO.
6. **(e) STILL BLOCKED on Hopper** -- one line naming "submission 6's entry" for his `SessionStart` counter-example. Unidentifiable from surviving artifacts; Cal correctly refused to guess a plausible neighbour. His 2 unfiled submissions ship as an explicit `[GAP]` until then.
7. **G3 + the `resolved`-enum is MINE, unopened:** two courier entries' revision-trigger prose promises a `status` transition `WikiProvenance` cannot express (enum `active | disputed | archived`; corpus holds 426 `active`, 2 `disputed`, **0 `resolved`**). Protocol C proposal, adjacent to deferred change (A). **Revision-trigger prose deliberately left unpatched -- the mismatch IS the evidence.**
8. **Card `related:` base -- HOLD, and it is the STANDING DECISION, not my judgment.** Finn's entry already ruled repo-root canonical on 2026-08-12; measured over 831 links it resolves **0%**, entry-dir 70%, card-dir 58%, and 28% resolve under no base at all. The card layer is not a population that drifted -- it is one the rule never reached. Remedy is **normalise on touch, NO sweep** (a mass rewrite over durable citations is the pathology that decision prevents). Do NOT re-frame this as "settle the base".
9. **Apex onset discrepancy, unreconciled by design:** ghost-name `SendMessage` refusal -- FR measured **2.1.247**, apex says **2.1.217+**. Neither is bisected; both may be "the version we were on when we noticed". Recorded on the inbound archive; **confidence on the onset claim deliberately NOT upgraded.** If apex's figure is a measurement, it moves the onset by 30 releases.
10. **Contract-change queue, still PO-gated:** (A) WikiProvenance reconciliation (**needs Celes spawned** -- she owns prompt lines; Cal RECUSED on the confidence half), (B) `discoveryOrder` MINOR, (C) `negative-probe` restructure (Cal recused; read-back Hopper or Herald, NOT Brunel).
11. **Volta items (untasked since S63/S65/S67):** version-actual-trigger startup check (**now overdue -- item 3 above is exactly it**); startup.md host-check table (**the `$PPID`=1 finding belongs in it**); `inter-team-comms` retirement; `sanitize-inboxes` configs[0] bug; oldest-unacked check; #109 tailscale re-triage. Also **the lifecycle-script half of the durable-store finding is his.**
12. **Cal's own carry:** scratchpad 56 lines and compliant; his S68 self-audit ledger (4 self-corrections, all caught by checking) is the model. **Health signal still live: 9+ Protocol A submissions / 0 Protocol B queries.**

### Standing watch items going into S70

- **Crossed-message watch stays at FOUR, unupgraded.** S68's pair was ruled **not an instance of anything**: my "index.md still reads 218" was **true when written** (verified on disk at ~14:28; `git log -S` puts the 229 string first in `947ec12` at 14:35:57) and decayed in transit. Cal read my commit timestamp as my report timestamp. **A retracted instance must not quietly upgrade a watch.**
- **The decayed-vs-false distinction needs an INSTRUMENT, not attention.** Cal drew it, defended it against my framing, then misapplied it one instance later. *A stale claim ARRIVING feels like a false claim BEING MADE* -- the reader meets the claim and the present state simultaneously with no evidence of the authoring moment. Re-deriving on read protects **content**, never **classification**.
- **Three rule-firing counter-examples now, and the honest bound:** (1) externally prompted, credit declined; (2) unprompted but LATE (10 files already changed); (3) unprompted and PREVENTIVE, on the cheapest possible action -- reading a number someone else asserted. **None generalised**: Cal's note protected the fact and not the interpretation in the same hour.
- **Cal's watches (S66-S68):** proxy-bound; gate-buys-pushback; hygiene-rule-destroys-diagnostic-absence; timestamp-fabrication (has my +67min instance); four-benign-crossings (STILL HYPOTHETICAL); point-at-the-mechanism umbrella candidate (3 instances, one work-stream -- **needs a 4th from elsewhere**).
- **Brunel's md5 authority:** frozen-tree attestations are BLOB values (`git show :<path>`); `file(1)` is the CRLF authority, not a carriage-return grep. md5 discriminator is scoped to **md5-of-a-FILE** -- aggregate digests hash a tool's *presentation*.
- **Inbox lag is cross-agent** and once delivered an instruction after its cancellation. Local-Windows triage, not wiki-grade as lag per se; the defense (re-validate aged authorizations against current state) is queued.
- **Celes owes Finn** 2 prompt items (S63); roster-drift survey untasked; Medici read-backs 2 owed (since S62). **Herald untouched since S66** -- `negative-probe` restructure read-back and his wake-contradiction amendment await.
- **PO's own open items:** Step 12 items 2/3/5; `joosep_sshd` volume backup one-liner; note to Lerko; `gho_` token rotation; Entu #42; exec-bit normalization yes/no. **Joosep container live on RC:2231, handed over, runbook at `designs/deployed/joosep/`.**

---
## Older sessions -- pruned to pointers

- **S68 (2026-08-31):** Cal's held-queue backlog (a)-(e). **The queue's source text was GONE** -- inbox messages are consumed on delivery AND a clean exit deletes the whole team dir including a non-empty `inboxes/` (*reading deletes the message; exiting deletes the mailbox*, two composing mechanisms). Filed as TWO entries on the disjoint-remedy test. Wiki **218 -> 229**, parity across all 5 layers; 11 new entries, 9 amended; index drift repaired (all self-inflicted by batching index rows). Volta 3/3 + Brunel complete + Hopper **4/6**. Full record: `git show b23e251:teams/framework-research/memory/team-lead.md`.
- **S67 (2026-08-31):** Joosep arc CLOSED end-to-end (hand-over signed + SENT, `designs/deployed/joosep/`); TTL batch closed; graceful-exit GC (n=5); Stop-hook `additionalContext` UNBOUNDED by the block cap (guard = `stop_hook_active`). `git show 8d8ce6b:...`.
- **S66/S65/S64/S63:** Joosep container BUILD arc; #108 stationmaster; RC docker-root runbook; 39 cards relabelled `legacy-unaudited`. Same pointer form.
- Durable lessons live in the wiki, not here.

(*FR:Aen*)
