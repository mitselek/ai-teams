# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S60 2026-07-14 -- control-semantics filing; Protocol-A from Aen, PO-originated Mihkel):** Wiki **160->161**. **Filed 1 process entry (3 lessons folded as named sub-lessons).** `process/control-signal-semantics-at-authority-boundaries` [aen,herald] **stage-2: pending** (filed-on-behalf). process 9->10; process gate 5conf/4pend->**5conf/5pend**. On-disk verified consistent (10 entries==10 cards==INDEX rows==main-index listings==process-count-10; total 160->161; gate 5/5 real, grep shows 6/6 = 2 known false-positives: INDEX.md audit-literal + stage-2-confirms card body quotes "confirmed" while own frontmatter pending). All edits UNCOMMITTED (team-lead git). [S62 base: patterns=90 gate 53conf/2part/35pend; gotchas=36, contracts=5.]
- **[S60 CURATION CALL]** 3 PO lessons (musing-is-not-commission / stop-is-not-revert / control-acts-at-boundaries) = ONE parent, three named sub-lessons -- NOT 3 entries and NOT gotchas/patterns. Same mechanism (control language executed literally+eagerly -> imprecision resolves toward more/sooner/more-destructive action), divergent loci (read input / write stop-content / time stop). Passes team divergent-mechanism test (shared mechanism, divergent triggers = parent+named-variants, cf coordination-loop-self-correction). process/ (not gotchas/, despite task hint) because they shape how coordination works + are promotion-bound to po-team design + common-prompt (Protocol C follow-on). "cut-coordination-churn" entry Aen named DOES NOT EXIST on disk -- closest is patterns/coordination-loop-self-correction, cross-linked.
- **[S62 3 ARTIFACTS filed -- all cross-team from apex #176, principles-only NO apex-artifact critique]:**
  - **L1** `patterns/detection-is-upstream-of-recovery` [finn] **stage-2: CONFIRMED (Finn read-back)**. Recovery-without-detection = dominant contamination risk at scale (all-external triggers -> undetected-never-corrected dominates; detection belongs UPSTREAM in observability/knowledge-health). Dedup-3 vs documentation-vs-substrate-truth-divergence (MECHANISM vs LOOP-STRUCTURE). Self-cert gap section = apex S59->S60 R1-R5.
  - **L2** `patterns/provenance-coverage-percent-as-knowledge-health-metric` [medici] **stage-2: pending** (filed-on-behalf). Provenance-coverage % PER SEGMENT (global hides the rot; value=gradient); LEADING indicator. Specifies the metric L1 only names (different altitude, cross-ref). telemetry-from-convention. Scope: metric-as-principle, NO threshold/authoring-rule (separate PO call).
  - **L3** `patterns/authoring-time-prevention-vs-audit-time-detection` [medici] **stage-2: pending** (filed-on-behalf). The DISTINCTION ONLY (per team-lead scope): prevention(write-time, never enters) vs detection(post-ship, caught downstream); neither subsumes other. Places full axis prevention->detection->recovery (this=prev/det boundary, L1=det/rec boundary). Explicit Scope section: does NOT adopt an authoring rule (PO call -> common-prompt/topic-file).
- **[S62 TASK #3 verdict, relayed to team-lead]** apex contamination-recovery = BOTH genres: keep PLAYBOOK (procedure, playbook-mature) + extract SHORT ADR (durable decision "recovery mandatory + both modes required") POINTING to it. n=1 = playbook-grade not ADR-frozen (mechanics evolving); ADR=commitment, playbook=mechanics. FR precedent: decisions/ terse + HOW-in-playbooks.
- **[S62 TASK #3 verdict, relayed to team-lead]** apex contamination-recovery = BOTH genres: keep the PLAYBOOK (procedure, playbook-mature) + extract a SHORT ADR (durable decision "recovery mandatory + both modes required") that POINTS to it. Maturity: n=1 battle-test = procedure playbook-grade but NOT ADR-frozen (mechanics still evolving); ADR records the commitment, playbook keeps the mechanics. FR precedent: decisions/ terse + HOW-in-playbooks.
- **[S58 6 ARTIFACTS filed -- all observation-based/version-stamped 2.1.181, ref teams-substrate-2.1.179 as 2.1.181 DATAPOINT cross-ref-not-fold; src=[aen live-diag]+owner]:**
  - **F1** `gotchas/cold-start-discovery-false-negative-config-before-sessions-json` [aen,hopper,herald] CONFIRMED -- S57-halt root cause (config.json eager, sessions/<pid>.json +10-25s = V4 window; too-early probe reads absence-in-window as lazy-create/fail). Herald's Protocol-A submission CONVERGED = dedup-outcome-2 + his read-back; folded his "ANY substrate-state probe not just discovery" generalization as Scope section. SIBLING of sessions-pid-json-not-gc (BIRTH absent/false-neg vs DEATH lingers/false-pos), separate not merged.
  - **F2** `gotchas/courier-restart-needs-inboxes-dir-step25-before-step3` [aen,volta] CONFIRMED (Volta) -- Bug A runbook-order (Step2.5 binds inboxes_dir before Step3 mkdir's it; inboxes/ lazy). Both fixes LANDED: reorder->Step 3.5 + courier self-mkdir(validate_startup). Slug kept = names FIXED defect (link stability). NOT the stale-pidfile gotcha.
  - **F3** `gotchas/orphan-courier-holds-lock-across-sessions-wrapper-cannot-reclaim` [aen,brunel] CONFIRMED (Brunel) -- Bug B LIVE orphan holds courier.lock cross-session, wrapper can't reclaim non-owned lock->FileExistsError. **KEEP-SEPARATE** from courier-scheduled-task-restart-vs-stale-pidfile (dead-PRESENCE vs LIVE-orphan/ownership). Both fixes LANDED: identity stop-sweep + Scheduled-Task DISABLED (Hopper 16:06, pid-38044 class closed).
  - **F4** `gotchas/explicit-courier-config-hardcoded-path-stale-on-2.1.181` [aen,brunel] CONFIRMED (Brunel) -- Bug C explicit cfg hardcodes dead `framework-research\inboxes`->no delivery+drain rc=1; breaks Direction#4. Same root-cause as courier-must-runtime-discover, diff site. Rejected-alt (session-aware explicit=duplicate resolver). Fix LANDED: drain option-1; Direction#4 amendment RATIFIED+CLI-version-split.
  - **CONTRACT** `contracts/courier-rotation-contract-has-teardown-half-reap-before-acquire` [herald] CONFIRMED (author-solo) -- rotation is SYMMETRIC: reap-prior(kill+drain+release lock) THEN resolve THEN acquire; Step2.5 was only the acquire half. Mechanism(lock-staleness PID-1-starttime spec=WHEN reapable) vs requirement(this=reap MUST happen). + **v2-at-scale OPEN co-located as TRACKED-GAP subsection** (single-point Config-load resolution not self-healing; NOT v1 blocker per Aen; owner TBD at 2nd-team migration) -- NOT a standalone findings/ entry (dir doesn't exist + MIGRATION-STALE would mis-fire).
- **All 4 S58 gotchas held `status: active`** (NOT resolved) pending **task#7** end-to-end validation -- fixes LANDED but not yet e2e-validated. Offered team-lead: flip to resolved at #7 close, or keep as historical record. (status edit, not a count change.)
- **[CHECKPOINT] TTL scan:** 2026-09-17 teams-substrate-2.1.179 + hook-primitives + no-teamdelete-stale-dirs; 2026-09-18 sessions-pid-not-gc + ALL 4 S58 gotchas (co-stamped). Older: model-inventory 2026-07-10; inbox-drained 2026-08-07; substrate-property refs 2026-11-12/14/19; edit-tool-trap+inverted-trigger 2026-11-27.
- **Carry-forward (next session): [2 OPEN stage-2 gates] = L2 `provenance-coverage-percent-as-knowledge-health-metric` + L3 `authoring-time-prevention-vs-audit-time-detection`, both pending Medici read-back (filed-on-behalf S62).** (L1 detection-is-upstream CONFIRMED via Finn read-back -- closed.) [LESSON S62] team-lead corrected my deferral: the L2/L3 submissions DID arrive (I'd reported queue-clean prematurely -- they were unread in queue, not absent). Processed all 3 + Finn's read-back end-to-end; held principles-only + distinction-only scope on L3. [V5b-WATCH] proactive-external-write-wake INCONCLUSIVE-leaning-negative on 2.1.181 (Hopper, headless-pane/focus-events confound) -- if attached-pane re-test lands a wake finding it may touch inbox-file-write-as-wake-mechanism / 2.1.179 sheet External-write-wake row. [TASK#7] when it closes, the 4 S58 gotchas can flip status:active->resolved (fixes already landed). [VERSION] CLI now UNPINNED at 2.1.181; autoupdater ENABLED (S56 PO) -> a future boot may be >2.1.181, re-validate lifecycle via migration-probe-harness before trusting Step2'/2.5.
- **[DURABLE KEY CALLS, still live]** version-coupled-vs-version-stable (revision-trigger=version change NOT n+1); *mega biblion* (version datapoint=compact pointer/cross-ref not 2nd full sheet); decision/protocol=POINTER not copy; companion-pair (two cross-linked entries) only when claim-types differ, else one entry; resolution of a confirmed gotcha=status update keeps stage-2 confirmed; arch-fact gotchas don't gain confidence from n+1; **author-submitted Protocol-A = author-solo = file CONFIRMED (no separate read-back round)**; **convergent submission on a prepped-on-behalf entry = dedup-outcome-2 + serves as the owner's read-back**; **index-reconcile IS the gated filing act (hold it until read-back so the live index never implies an unratified entry)**; **fixes-landed updates ride the confirmed entry as amendments, status:active held until e2e-validated**. [GOTCHA-self, RECURRED S58 every edit] scratchpad/INDEX Read-state expires on edit-after-intervening-calls (deferred-tool system-reminders reset it) -- re-read recovery held every time; my own entry warns.
- **Cal-Protocol-A queue (pre-S50 backlog, still DEFERRED):** Routing-by-action / Stage-0-contribution / Candidate-B n=1; C3 / E2 / E3 / A.3 / Companion-Pair / Producer-staleness. [POSSIBLE-PATTERN n=2-watch] Hopper "build-green+unit-green but protocol-broken, caught only by over-real-ssh acceptance" (3rd→promote). [ANOMALY n=3] task-state-loss S42+S44+S50 (Aen boot). [UNADDRESSED] none.

---
## Session transcript (prune beyond line 100)

## Session 60 -- 2026-07-14 (control-semantics lessons; Protocol-A from Aen, PO-originated)
Wiki 160->161 (1 process entry). S60 designed po-team; during it three coordination failures fired in sequence (musing tasked as lane; HOLD "checkpoint and stop...doc parked" -> Herald REVERTED finished protocols.md §7 + banners = multi-round retraction; HOLD raced Herald's completed work off PO "please don't rush"). Mihkel formulated 3 lessons + directed filing.
- [DECISION] Folded 3 lessons into ONE parent process entry `control-signal-semantics-at-authority-boundaries` with 3 named sub-lessons (musing-is-not-commission / stop-is-not-revert / control-acts-at-boundaries-not-mid-flight). Rationale: single shared mechanism (control language executed literally+eagerly, imprecision -> over-action), divergent loci only -> parent+named-variants per team divergent-mechanism test (cf coordination-loop-self-correction precedent). Honors mega-biblion + fold-over-standalone + coherent promotion unit (Mihkel wants all 3 promoted to po-team design together). Rejected: 3 separate entries (over-fragment); gotchas+patterns split (6 entries, violates mega-biblion; the discipline IS the lesson, failure-instance is its evidence not a separate gotcha).
- [PLACEMENT] process/ not gotchas/ (despite task hint "likely gotchas/patterns"). Decision-matrix: "shapes how the team works" + promotion-bound (po-team design + common-prompt Protocol C) = process/. Sibling precedent standby-agent-fix-then-flag-discipline (coordination discipline in process/).
- [STAGE-2] pending (filed-on-behalf: Aen submitted via Protocol A, Mihkel originated). source-agents [aen,herald] (failure-instance co-authors who can read back). PO formulation is authoritative source; note in provenance. Advances on Aen/Herald read-back.
- [CROSS-LINK] coordination-loop-self-correction (healthy vs failure case of acting in a live loop), timestamp-crossed-messages (racing-control-message = crossed-messages mechanism), standby-agent-fix-then-flag (sibling on-behalf axis: this=what coordinator SAYS, that=what agent TOUCHES), rule-erosion-via-reasonable-exceptions. "cut-coordination-churn" (Aen's named related entry) NOT on disk -- coordination-loop-self-correction is closest.
- [NOTE] Promotion into po-team design carried separately by Herald+Celes; noted in entry + card.

## Session 62 -- 2026-06-26 (apex #176 methodology review; CLOSED)
Wiki 157->160 (3 patterns). Deliverables:
- [TASK#3] apex Discussion #176 Q2 (playbook vs ADR). Verdict = BOTH-but-unbundle: keep playbook (procedure, playbook-mature at n=1), extract a short ADR (durable decision: recovery mandatory + both modes required) that POINTS to the playbook. Maturity argument = deliberate-vs-evolving test: commitment is ADR-grade, mechanics still evolving so DON'T freeze the step-list into an ADR at one battle-test. Cross-checked against FR's own playbook/decision line (no-alternatives=>procedure; decisions/ terse + HOW-in-playbooks). Methodology rec to apex, NOT a filing in our wiki.
- [PROTOCOL-A x3] Finn L1 `detection-is-upstream-of-recovery` (CONFIRMED via Finn read-back) + Medici L2 `provenance-coverage-percent-as-knowledge-health-metric` (pending) + Medici L3 `authoring-time-prevention-vs-audit-time-detection` (pending). All cross-team from apex #176, all dedup-3 cross-linked into one family (prevention->detection->recovery axis). principles-only, NO apex-artifact critique. L3 = distinction-only per team-lead scope (authoring rule = separate PO call, explicit Scope section says so).
- [LESSON -- corrected] At first shutdown I reported queue-clean on L2/L3 believing they were never submitted. Team-lead corrected: they WERE in my queue, UNREAD. Real lesson is NOT "held submitter-trust" -- it's CHECK THE QUEUE before declaring it clean. The instinct (don't author another agent's knowledge on his behalf) was right IF absent, but they weren't absent. Processed all 3 + the read-back end-to-end on the re-prompt; pre-commit consistency verify passed (90==90==90, sum=160, gate 53/2/35==grep). [GOTCHA-self RECURRED] Read-state reset by deferred-tool system-reminders on index.md + 2 cards mid-batch -- re-read recovery each time, as my own entry warns.

## Session 58 -- 2026-06-18 (S58 courier-lifecycle + cold-start knowledge curation; CLOSED)

Wiki 152 -> 157 (4 gotchas + 1 contract; + 1 OPEN as a subsection). The big curation pass of the 2.1.181 unpin. Process detail lives in the entries; load-bearing lessons:
- [WORKFLOW] Prepped all 4 gotchas on-behalf BEFORE owner read-backs (Aen scope-confirmed separate-not-umbrella), held index-reconcile until each CONFIRM -- the index never showed an unratified entry. Filed F1 first (highest value = S57-halt root cause) per Aen priority.
- [DEDUP] Herald's Protocol-A #1 CONVERGED with my prepped F1 -> treated as dedup-outcome-2 (cross-credit) AND his Stage-2 read-back in one. Saved a round-trip. His later formal CONFIRM was redundant (already done) -- no re-edit triggered.
- [CURATION] F3 KEEP-SEPARATE (Brunel) from the stale-pidfile gotcha: same singleton-guard abstraction, different root cause (dead-PRESENCE vs LIVE-orphan/ownership). Folding would bury the live-contention corner. Three-artifact triad kept distinct: contract(requirement) / Bug-B gotcha(incident) / lock-staleness(mechanism).
- [CURATION] Herald #3 v2-OPEN filed as a tracked-gap SUBSECTION on the contract, NOT a standalone findings/ entry -- findings/ dir doesn't exist (0-count) and its 3-session MIGRATION-STALE rule would mis-fire on a correctly-deferred v2 gap. Co-located as the contract's at-scale hardening. No count change.
- [FIXES-LANDED] All 3 owners reported their fixes landed during read-back (reorder+self-mkdir, stop-sweep+Task-disabled, drain option-1). Folded as amendments; held status:active until task#7 e2e-validation (consistent across all 4).
- [SELF-CATCH at shutdown] Gate-count off-by-one: I'd written "32conf/3pend" but the real pre-S58 pending = 4 (grep returns 5 incl. INDEX.md's own prose-mention of the literal). Fixed to 32/4 + annotated the grep-caveat BEFORE approving -- caught it on the pre-commit consistency verify, exactly when it mattered.
- [LESSON] Verify on-disk count consistency (entries==cards==index-total, gate-line==grep-minus-INDEX) before a team-lead commit. A hand-maintained count drifts; the grep audit has a known false-positive (the INDEX file itself). The pre-commit verify is the catch-point.

## Session 56 -- 2026-06-18 (unpin-prep boot + last S55 gate closed; CLOSED)

- [GATE-CLOSED] Hopper read back `decisions/courier-must-runtime-discover-team-name` -> CONFIRMED (final co-author). Folded Brunel's S55 pending-refinement blockquote->body (glob PRIMARY/pid TIEBREAKER + liveness-filter + CRITICAL process-liveness). decisions/INDEX 7conf/1part->8conf/0part. Wiki UNCHANGED 152 (amendment).
- [LESSON] folding a held pending-refinement on final co-author confirmation = canonical Stage-2 endgame: blockquote-while-partial, body-on-confirmed.

## Session 55 -- 2026-06-18 (WS1/WS2/WS3b migration intake; CLOSED) -- pruned to load-bearing
Wiki 147->152 (5 new). Herald WS2 batch (3, all CONFIRMED same session): dec `lifecycle-release-evaporates-under-implicit-teams` (S5 DELETED, 5->4 phases) + `startup-create-collapses-to-discover` (Step2->Step2' Discover) + gotcha `no-teamdelete-stale-session-dirs-accumulate`. WS3b: `sessions-pid-json-not-gc-status-idle-lingers` (hopper/brunel/herald, 2.1.181, CONFIRMED, RESOLVED OQ2 worst-case -> amended no-teamdelete confidence MED->HIGH + liveness status->process). S52-sibling: `lockfile-pid-staleness-false-refuse-across-container-recreate` (brunel/herald, field-22 sibling family now spans 3 gotchas). SUPERSESSION (version-coupled NOT archived): teamcreate-leadership got 2.1.177- banner. Declined Herald's synthetic umbrella (dense cross-links instead).

## Sessions 1-54 (heavily pruned -- load-bearing only; full detail in git history + the entries)
- [S50] Wiki 120->129 stationmaster intake. *mega biblion* on T-entries (23 atomic->2 wiki). standby-discipline one-entry-two-clauses. INDEX-DRIFT fix (process count). Spawn-brief-path gotcha (3rd catcher).
- [S44] Entu competency-index schema SHIPPED+COMMITTED (`contracts/entu-competency-index-schema.md`). evidence-TYPE vs verification-METHOD on separate axes. "mirror don't invent." Derived-view (roll-up) must validate against source bodies not hand-count.
- [S40] Epic #67: 118 cards + card-INDEX system (#68); scratchpad summary-header convention (#69); Stage-2-confirms filing gate defined (#70, `wiki/process/stage-2-confirms-filing-gate.md`). Held scope-line on common-prompt edits (propose, team-lead writes L1).
- [S39] roster.model non-load-bearing on Agent-tool teams (Instance-4 n=4). card-tier writing is extractive not transformative.
- [S1-37] Phase 2 activated S6. Protocol C promotions (substrate-invariant-mismatch, worktree-isolation, semver-strict, relay-fidelity). git-blame for disputes; sibling-entries-beat-variants; cross-pollination unit = idea not file. Stage-2-feedback typology (5 shapes, S36). 9 entries S37.

(*FR:Callimachus*)
