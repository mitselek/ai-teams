# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S73, 2026-09-04, 10:46):** wiki **240 entries = 240 cards** (was 235). **5 filed + 3 amended.** Truth-loop batch **COMMITTED by Aen at `487285c`**; the Lelle entry + 4 files are **uncommitted**.
- **[FILED 5, 10:0x] `decisions/lelle-gen-3-evr-island-comms` (team-lead relaying a PO ruling, high, PENDING).** Codename **Lelle** = gen-3 EVR-island comms method AND its hub instance, over **Tapa** and **Koidula**, for **disambiguation**. Lineage ghost-bridge -> stationmaster v1.0.0/`prod-llm` -> Lelle; **sagres out of scope**. Ledger **#116**, Herald spec, Brunel/Hopper/Volta impl, PO gates.
- **[GATE CALL AGAINST THE SUBMISSION'S OWN LABEL] Sent as `author-is-filer`; I filed it `pending`.** **The decision's author is the PO, the filer is me, and Aen is a WITNESS relaying it** -- filed-on-behalf, fail-closed by the three-bucket rule. Read-back owed from Aen; **a PO confirmation would be stronger.**
- **[I VERIFIED THE FINDING INSTEAD OF RELAYING IT, and it paid twice.]** #107 = 0 comments / 22 days, #111 = 0 comments / 5 days, zero repo mentions -- confirmed by `gh api` + `grep`, and **#107's body matched Aen's summary exactly, which is worth saying because it means the relay was faithful.** **THEN THE COUNT CHANGED UNDER ME: 0 early in the session, 1 twenty minutes later** (Aen's pointer comment). **So "zero comments" is only true of a window with two ends, and I stated both.** `verification-certifies-a-moment-not-a-session`, live, on the very claim I was filing.
- **[GENUS RULING, asked for and made] `precondition-without-an-owner-is-no-precondition` = NEAR-INSTANCE, cross-linked, NOT counted.** **A precondition is a CONTROL that fails to fire when its condition comes true -- there is a moment something should have happened. A proposal has NO triggering condition; it just sits.** Remedies disjoint: *owner + moment to evaluate* vs *a routing decision -- issue or declined on the record*. **Only the OWNER third of the three-part rule is shared, and a shared third part is not a shared mechanism.** **Promotion condition written while I had the reasoning: a third instance, an input-to-work artifact in a venue with no assignment step, AND a demonstrable cost.** **n=2 today is ONE CIRCUMSTANCE SAMPLED TWICE** (same repo, same lane-occupied period), not two sightings.
- **[TWO THINGS I RAISED, NEITHER A DISPUTE] (1) The ruling assigns ONE TOKEN to TWO THINGS -- the method AND its instance -- which is the same shape as the ambiguity it fixes** (`singular-convention-plural-instances`). Harmless while one island runs one instance; **the coincidence is contingent**, so offered to Herald as a one-sentence spec question. **(2) `Koidula`, a rejected alternative, is ALREADY an agent persona name in 11 repo files** (esl-legal deployed + esl-suvekool). **Offered with NO inference about what the PO knew** -- the relevance is only that a name meant to remove ambiguity was already bound.
- **[FILED 1-4 earlier] 4 entries + 2 amendments, all cross-team from apex-research's truth-loop playbook (`ec0fc76b`).** Counts re-measured on disk per subdir AND in total, together, not derived from each other. **Files changed, NOT committed -- team-lead commits.**
- **Active items (S73):**
  - **[THE SESSION'S REAL OUTPUT] I checked the filing brief's factual claims instead of transcribing them, and TWO were wrong.** (1) The brief said the three `.claude/` engine files are NOT on apex `origin/main`; **they are, byte-identical at `ec0fc76b`** (6373 / 12222 / 5090). **I reproduced team-lead's false negative myself** -- `git cat-file -s origin/main:.claude/...` fails as *"Not a valid object name"* on this shell because of `cross-msys-argv-mangling`; `MSYS_NO_PATHCONV=1` returns sizes. **A tooling false negative read as an absence = `negative-probe-result-underdetermined-absence-read-as-permanent`. TWO of our own gotchas composed, and the product would have entered the wiki as a PROVENANCE FACT.** (2) The brief said *we* hold `confirmation-method-diversity-over-repetition`; **we do not -- it is in THEIR wiki.** Zero hits here for *diversity*, *method diversity*, *register boundary*, *sender-declared*. **[LESSON] A brief from the coordinator is a relay, and the relay-fidelity discipline applies to it exactly as to any other relay.**
  - **[FILED 1] `patterns/gated-answer-loop-with-reader-owned-exit`** (schliemann, `source-team: apex-research`, medium, **pending**). The 9-station gated design + verification table separating read from relayed. **The load-bearing claim: "done" is not a state the sender can enter.**
  - **[FILED 2] `gotchas/sender-declared-done-closes-on-the-attempt`** (medium, **pending**). Filed as the **GENUS of three narrower entries we already hold** (merged-vs-shipped, holding-a-measurement, bridge-success-over-empty-payload) rather than folded in, **because its remedy is structural -- move the close to the other party -- and none of the three can do that inside its own substrate.** **Audit: 4 of our 5 protocols close by the producer; the Stage-2 gate is the one exception and it is our highest-yield mechanism.**
  - **[FILED 3] `gotchas/a-thorough-probe-is-not-an-independent-check`** (high on mechanism, **pending**). **Kept OUT of `agreement-across-copies-...` on the disjoint-remedy test RUN, not assumed:** document-source -> *record the mechanism not the value*; method-source -> *name the confirming method and check it differs*. **Neither remedy does the other's work.** Written as a pointer to their canonical entry, not a copy.
  - **[FILED 4] `decisions/truth-loop-shape-tightened-v1-to-v4`** (medium, **pending**). The design trail; **rejected alternatives ARE the payload**, which is why decisions/ and not patterns/. **Explicit Stage-1 relay fold:** v2/v3/v4 primary-verified from the playbook header; **v1's baseline + the two PO questions are `[FLAG] relay-only` -- and they are the reason the PO singled the material out.** Artifact section 6 **supersedes** them, not corroborates.
  - **[AMENDED 1] `process/protocol-a-has-no-durable-store-...`** -- their *ephemeral evidence* is the same mechanism and the same remedy SHAPE (durable by a STEP, not a habit). **They chose shape 1, we prefer shape 2, and the discriminator is now stated: whether the protocol has a single point that sees everything.** That **narrows** our original preference to a scope condition. **Their fix does not reach ours: their tripwire terminates in Protocol A.**
  - **[AMENDED 2] `contracts/speculative-marker-for-cross-team-drafts`** -- their *tag decay*. **They adopted the SAME token for the SAME job with no contact.** It exposes a hole invisible from inside: **a marker dropped in transit makes the survival count go DOWN, which we read as speculation resolving -- the healthy signal and the worst failure produce the same number.** Fix = count on BOTH SIDES of any rewrite; **three rewrites in our own pipeline qualify (entry->card, session->scratchpad header, relay->entry) and none does it.**
- **[SECTION 6 FOLDED 09:32 -- and the primary artifact CHANGED what I filed, in four places] Aen read the full 283-line HTML and quoted section 6 VERBATIM.** Both `[FLAG] relay-only` rows CLOSED. **Provenance stated exactly and NOT collapsed: primary-QUOTED by a reader who saw it, not primary-verified by me -- a verbatim quotation can still be a quotation of the wrong section.** **(1) The mapping is established AND it narrows my own claim: challenges 1 and 2 dissolved the two nodes, but a SELF-RUN adversarial pass -- not a stakeholder -- found the six inefficiencies.** My *outside review did the work* was too strong; corrected in place. **(2) CREDIT CORRECTED against myself: I filed *the retro produced the material, the challenges produced the shape* as my reading; section 6 shows they said it first** (*"Encapsulating the loop exposed design debt in the retro shape"*). Recorded as a restatement. **(3) TWO discrepancies between apex's OWN artifacts, recorded and NOT resolved** -- section 6 names THREE challenges where the playbook header says TWO; the header credits v3 with *stumble-translation added* while section 6's v1 AND v2 boards already draw that path. **(4) There is NO v3 board** -- v1, v2, v4 only. **The version carrying the two most consequential positional changes is the one the trail never drew.** **[THE METHOD NOTE] Both discrepancies came from putting two of THEIR artifacts side by side -- not an independent method, but a DIFFERENT one, which is the cheap half of the entry I filed this morning.**
- **[WHAT THE v2 BOARD ADDED, and it sharpens edit 3] In v2 the GATE sat UPSTREAM of the register crossing with `translate + PUBLISH` collapsed into ONE node -- so NO human saw the reader-facing text before publication at all.** v3's repositioning is therefore **not a choice between two documents; it introduces a review point v2 did not have.**
- **[GATES] Both amendments CLOSED at 09:32 -- Aen read back both, accepted, no edits.** protocol-a: the narrowing is what they meant when co-confirming, and *does the protocol have a single point that sees everything* is the right discriminator. speculative-marker: the hole is real, **accepted as Protocol C and explicitly NOT adopted this session** -- recorded so acceptance of the finding is not misread as adoption of the fix. **Referent notes LEFT STANDING, not deleted: a closed gate that erases what it was open about hides that it was ever open.** **The four NEW entries stay `pending` -- supplying a source is not a read-back of the argument built on it.**
- **[CLOCK SKEW, recorded not smoothed] Aen's read-back message is stamped 09:32; my `date` at the time of folding it read 09:31.** The stamps invert by one minute across agents. **Their stamp is recorded on the entries as they gave it and mine is measured; neither is adjusted to fit the other**, per `gotchas/fabricated-timestamps-destroy-ordering-not-just-accuracy`. A later reader should not take the inversion for a causality error.
- **[MSYS FINDING CORROBORATED BY A THIRD SOURCE OF A DIFFERENT KIND] Artifact section 5 lists all three engine files as links to the apex repo at `main`** -- their presentation, not their git tree. Aen accepted the correction and recorded the argv path-conversion as the cause in their own scratchpad.
- **Key decisions:** **3 of the 5 named anti-patterns were FOLDED, not filed** -- ephemeral-evidence into process/, tag-decay into contracts/, density-repeat into the pattern + decision (it is structurally excluded, so it has no independent remedy). **2 filed as gotchas.** **Both amendments carry a GATE REFERENT note** naming what the `confirmed` was earned against, per the axis-2 defect -- both are librarian edits to jointly-confirmed entries and team-lead is owed both read-backs.
- **[PROTOCOL C -> TEAM-LEAD, two new, both recorded on entries and NOT acted on]** (1) **Record the confirming METHOD when a claim falls** -- their register has a *Confirmed by (who/when/how)* column so the METHOD accumulates a track record; our `status: disputed` marks the claim and **no method here has one.** Touches Protocol A + WikiProvenance. (2) **Both-sides marker count across any rewriting transition.** Touches the card procedure + the scratchpad header format.
- **[THE PREDICTION I WOULD MOST WANT TESTED] Our Stage-2 read-back is TIER 1 WITH TIER 2 MISSING** -- a co-author re-reads against the same evidence, so it is the same method run by a different person. **It cannot catch an error in the evidence itself, and the record agrees: substrate-truth errors here were caught by fresh measurement, never by read-back.**
- **Carry-forward:**
  - **[WARNING, still true] submissions >> Protocol B queries** (S71: 6 in, 0 queries; S73: 0 queries).
  - **[DEFERRED, PO go] (A) WikiProvenance reconciliation (RECUSED on confidence half); (B) `discoveryOrder`; (C) `negative-probe` restructure (recused).**
  - **[UNADDRESSED] apex `Dockerfile.apex` fix is deferred at Brunel's call and lives in the apex repo, not ours.**
  - **[PRONOUN SWEEP] 9 occurrences normalised on touch in `process/protocol-a-...`. Remainder of the 140-file sweep untouched.**
  - **[CARD `related:` PATHS] 6 touched cards normalised `../` -> `../../`; corpus-wide issue still HELD, no sweep.**

## S73 transcript (2026-09-04) -- truth-loop filing

- Task: file apex-research's Truth Loop in our wiki, dual-perspective. Sources exported to scratchpad by team-lead; artifact URL cited but unfetchable by me.
- **Verified both source files against the apex repo before writing** (`truth-loop.md` 8209 B matches the export exactly; `truth-revisions.md` at `docs/`, 10134 B).
- **Overlap sweep BEFORE filing, per Dedup Protocol:** zero hits in our corpus for *method diversity*, *confirmation-method-diversity*, *register boundary*, *sender-declared / declared done*, *tripwire*. Two strong existing homes found instead, and both took an amendment rather than a new entry.
- **Files touched: 8 new (4 entries + 4 cards), 4 amended (2 entries + 2 cards), 6 index files.** All internal links machine-checked; all resolve. **Final count re-measured on disk: 239 entries = 239 cards, every subdir at parity.**
- **09:32 section-6 fold:** decision entry + card rewritten (boards, mapping, two discrepancies, credit correction, claim narrowed), pattern entry + card gained the section-5 corroboration, both amendment gate referents closed, 4 card INDEXes + `index.md` updated. **Links re-checked after; all resolve.**
- **[THE SESSION'S SECOND LESSON, and it is about my own filing] I filed a Stage-1 relay fold with the flags marked, and the primary artifact then corrected me in FOUR places -- including one credit that was not mine and one claim that was too strong.** **The flags were not ceremony; every one of them was load-bearing.** The reason the corrections were findable at all is that **Aen QUOTED section 6 instead of summarising it** -- a summary would have carried the mapping and lost the self-run adversarial pass, which is the fact that narrowed my claim.

---
## Session transcript (prune beyond line 100)

## 2026-09-02 (S71) -- 5 filed, 5 amended, queue cleared

- **[FILED 1] `patterns/state-the-match-set-before-trusting-the-instrument`** (hopper, `pending`). An instrument's MATCH SET is the states producing a verdict; wider than the question -> a clean answer to a question it did not ask. **Rule: confirm BOTH pass and null are reachable on the substrate it runs against.** n=4, one day: self-matching `pgrep -af`; merged auth+transport grep count 86; a version gate that cannot pass for ANY image; **`Config.Image` -- a DATA FIELD, so the genus is not confined to commands you invoke.** **The CONTROL is the general form** (answers 1, 3, 4). Process-check rule: EXCLUDE THE CHECKER.
- **[FILED 2] `patterns/live-is-not-the-same-as-discriminating`** (brunel principle + hopper measurement, `pending`). **Freshness and evidential value are independent.** Test: *what would this field read in the case I am ruling out?* `Config.Image` is a creation-time STRING, so it reads the same in the unsafe case; `.Image` is the discriminator, and the two diverged live. **The conclusion survived, the reasoning did not -- a withdrawn leg is not a refuted conclusion.** **Bites us directly: `last-verified` is a FRESHNESS field; re-running a non-discriminating check yields a fresh timestamp over an unchanged non-answer.**
- **[FILED 3] `contracts/tier-taxonomy-has-no-slot-for-undeclared-in-place-change`** (hopper + brunel, `pending`). Not R/M/D by their own definitions. **The taxonomy's own asymmetry heuristic fires correctly and recommends the WRONG escalation.** Distinguishing property is **durability, not risk-at-execution.** Cost is in the **reporting**, not the execution: clears on Tier M sanction, **must never be reported as done** until the durable path lands. **No-slot form 12**, registered in the hub; sibling of form 11 (both authorization-schema gaps).
- **[FILED 4] `gotchas/courier-no-ack-on-vanished-inbox-dir-is-incidental-not-designed`** (me + aen, `confirmed`). **S69 claim REFUTED from source.** `inject_tmp_dir` = `state_dir/inject-tmp`, NOT under `inboxes_dir`, so nothing on the inject path recreates the parent; `open(path,"x")` raises `FileNotFoundError`; **the loop catches only `FileExistsError` (so `raise InjectError` is never reached) and the caller only `InjectError`** -> escapes before `cmd_ack`; `run_once`'s "never let one bad cycle kill the loop" keeps the daemon alive. **DURABLE HALF: the safety is unprotected** -- adding `target.parent.mkdir(...)` (the obvious defensive fix) would CREATE the failure mode. Log/blast-radius both differ from the designed path.
- **[FILED 5] `references/teams-substrate-2.1.258-implicit-teams`** (me rows 1-3, aen row 4, `pending`). Version-POINTER per retire-and-forward; 4 rows measured. **The NOT-re-measured table is the load-bearing half** -- `inboxes/` lazy was NOT MEASURABLE (restore wrote 46 files before members joined), drain never watched, cold-start unobserved. **GC-split row recorded as a census that cannot attribute kill type**, precisely so it is not later read as a measurement. TTL 2026-11-30, aligned with its two 2.1.251 siblings so the lineage re-verifies in one pass.
- **[AMENDED] `gotchas/trailing-pipe-...`** dedup outcome 2: instances 4+5 = `Dockerfile.apex` #16 and #10. **Discharges the entry's own frequency caveat on both counts at once -- different repo, file no team member authored, second observer.** Confidence unchanged (`high`); n+1 on a shell fact moves frequency, not confidence.
- **[AMENDED] `patterns/roster-drift-...`** dedup outcome 2: family-only `model` re-confirmed at 2.1.258, `fable` now in the set. **I caught my own framing error before filing** -- the `null`-for-team-lead half was already on file at 2.1.235 in `dual-team-dir-ambiguity`; what is new is the CONTRAST (null lead / populated members) and the 3-version bracket.
- **[AMENDED] `gotchas/dual-team-dir-ambiguity`** items 1+3 re-confirmed at 2.1.258. **Both defects have survived three CLI versions unfixed = a fact about OWNERSHIP, not the substrate; neither is version-coupled, so no version change retires them.** Volta's.
- **[AMENDED] `gotchas/command-v-multi-operand-...`** submission-6 identification recorded + ops-log timing correction + the opposite-direction sibling table.
- **[GOTCHA-self, FOUND WHILE INDEXING] card drift, and worse than the entry drift.** `dual-team-dir-ambiguity`'s CARD never absorbed the 2026-08-19 additions and its `last-verified` sat at **2026-04-13 while the entry read 2026-08-19 -- four months behind the page it summarises, in the layer Protocol B surfaces FIRST.** Its index.md row was equally behind. Both repaired. **The entry was maintained; the two derived layers were not.**
- **[GOTCHA-self] index.md Card Tier per-subdir column was stale in EVERY row, summing to 193 against a total line reading 229 -- the correct number.** **The aggregate stayed right because it is the number people QUOTE; the breakdown rotted because it is the number people SKIM.** Re-measured: 234/234. Gate lines in `patterns/` and `gotchas/` cards INDEX were stale too and did not sum; re-measured and appended rather than rewritten.
- **[LEARNED] Ask the index before scanning the corpus -- applied, and it worked.** Delegated the dedup sweep over 229 entries to a subagent reading `cards/INDEX.md` first. It returned two DIRECT matches I would have filed as new entries (pipe-masking, model-pin) and one already-covered sub-item (`$PPID`). **Three of seven candidates did not need an entry; that is the sieve doing its job.**
- **[GOTCHA-self, THE SHARPEST OF THE SESSION -- I filed a claim that was FALSE, and the submitter corrected it against their own interest.]** On `trailing-pipe`, I wrote that the two apex instances **discharged the entry's correlation flag** "on both counts at once", asserting `Dockerfile.apex` was authored outside the team. **Both lines are Brunel's.** Worse: **the apex line PREDATES the joosep one, so instance 3 was probably copied from it** -- which makes the correlation stronger, evidencing **propagation, not independent frequency.** Brunel wrote *"please do not let me weaken it."* **The flag is reopened, my error left visible.** **I over-read a correlation flag while maintaining it, in the direction that made my own filing look better.** I assumed authorship from repo location and never checked.
- **[GOTCHA-self, SECOND] My `dual-team-dir` amendment asserted a DURABLE property from a minutes-old snapshot -- "no version change will retire them" -- and it was FALSE within the hour.** startup.md was rewritten mid-session: **items 1, 2 and 3 all CLOSED** (Step 0.5 no longer prescribes the unperformable check; Step 3.5 passes `-SessionPid`; `restore-inboxes.sh:44` honours `FR_COURIER_SESSION_PID` with `$PPID` only as fallback). Only item 4, the skills, is open. **`stale-snapshot-trusted-as-current` committed inside a note about ownership** -- and **a claim about WHY something has not been fixed decays fastest of all, because it is a claim about someone else's queue.** Verified against the files before correcting, not against the claim.
- **[LEARNED] Both submitters improved on my filings, and both corrections came from the producer side.** Hopper flagged a missing provenance as **THEIR** error (buried it *"where it read well, not where a filer would need it"*); Brunel corrected a flag in the direction that cost them. **The read-back is not a formality -- it caught two real defects in one window.**
- **[LEARNED] Heredoc/quoting: `python -c '...'` dies on an apostrophe; `python - <<'EOF'` dies inside a `&&` chain.** Write the script file. **Also: my own cards mix `--` and em-dash `—`, so any scripted string match must try both.** Fourth quoting cost this session -- write the file FIRST.

## 2026-08-31 (S68) -- pruned to the calls that still govern
- **DECAYED vs FALSE-AT-AUTHORING:** a stale claim ARRIVING feels like a false claim BEING MADE; re-deriving on read protects CONTENT, never CLASSIFICATION -- needs an instrument, not attention. Watch stays at FOUR.
- **Rules that fire are NARROWER, MORE MECHANICAL ones attached to an ACTION, not an INTENTION.**
- **Protocol A has no durable store: reading deletes the message, exiting deletes the mailbox.**
- **entry -> card -> INDEX row -> index.md, THEN the next entry.** Held all session; zero drift introduced.
- **Before reporting an edit as done, grep the artifact. Test the whole candidate space before "neither wins".**
- **A header is a CLAIM ABOUT a state and decays like any derived layer** -- every count carries its membership rule inline.

## S67 and earlier -- pruned; the entries carry the detail
- **DISJOINT-REMEDY TEST** standing (+3 boundary conditions). **A datapoint may not file without a `ttl`.** **`confidence` interim rule REVOKED; I am RECUSED** on that half.
- *presence is self-certifying, absence is not.* **`confirmed` certifies a PROCEDURE, not CORRECTNESS.** **`stage-2` = CARDS-ONLY.** **A scratchpad summary is a CLAIM ABOUT A SOURCE, never the source.**
- **[DURABLE]** version-coupled vs version-stable (trigger = version change, NOT n+1); pointer, never a 2nd sheet; arch-fact entries gain nothing from n+1; author-submitted = confirmed at filing, librarian-on-relayed = PENDING; pin confidence to the weakest load-bearing claim; **run `date` before EVERY send**; cwd resets between Bash calls -- absolute paths; verify after every scripted edit.
- **[SESSION-NUMBERING FORK]** Aen per continuous session, Hopper and I per working day. **Dates are canonical.**

(*FR:Callimachus*)
