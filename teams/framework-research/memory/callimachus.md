# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S67, 2026-08-31, PO HOLD in force):** wiki **218 = entries = cards = declared**, MEASURED; CLI **2.1.251**; tree has uncommitted wiki work (git is Aen's). **PO instructed 11:16: do not start new filing work that is not urgent** -- hold held for the rest of the session. **ONE urgent exception, on Aen's explicit word (11:39): the `claude-code-hook-wake-primitives` safety correction, filed 11:41 in all 3 layers** (the block cap governs BLOCKING hooks only; `asyncRewake` demoted to an OPEN GATING QUESTION because the entry had been recommending the escape from the sole bound). **That is the only thing written since the hold.** Nothing else urgent outstanding; nothing half-written. Also filed today: `patterns/state-the-membership-rule-of-the-set-you-counted` (high, **confirmed**) + `process/disjoint-remedy-test-for-umbrella-versus-cross-link` (medium, **confirmed**), both read back by Brunel; `contracts/entu-competency-index-schema` amended (4 born-wrong defects, TTL ->2026-11-30, §3->§2 renumber, 17 targeted edits); audit tool's false `absent="unrated"` removed.
- **Active items -- HELD QUEUE, classified/deduped/rated/acked but UNWRITTEN. This is the largest thing I carry:**
  - **Hopper: 9 submissions + 2 corrections.** Intake corrections MUST be applied before writing: casualty 3 of sub-7 **is wrong, do not file as written**; gate is the **live agent registry NOT `members[]`**; injection row files **REFUTED not untested**; the ~3.2s cold-start figure **must not go in** (bg-measured; would re-arm the S57 false halt).
  - **Brunel: 3 + a pre-filing amendment + an offered sub-shape.** md5 discriminator must be **scoped to md5-of-a-FILE** (aggregate digests hash a tool's *presentation*); sub-1 link goes **INSIDE sub-3's transport paragraph** (co-location is load-bearing, not cosmetic); growth sub-shape ruled a **distinguished sub-shape** of `verification-step-goes-stale`.
  - **Volta: 2 + 4 corrections + a sharpening.** Wake amendment: **cell RESOLUTION eliminated, cell MECHANISM neither confirmed nor eliminated**; closure reason = **2.1.179 is gone, permanently unfalsifiable**; **Brunel's finding (team size was in the Rule and every analyst used team-dir presence) is HIS, credit him.** Form 11 must foreclose the friction-reading or it becomes **a manual for permission laundering assembled from two people refusing to launder**.
- **Key decisions this session:**
  - **BOUND to retire-and-forward** (Volta Ruling A) **with an instrument, not a promise**: a datapoint may not file without a `ttl`; *one lineage, one TTL* becomes a value-space invariant in the audit extension.
  - **Adopted Finn's DISJOINT-REMEDY TEST as standing** (*two findings whose corrective actions do not overlap are not one mechanism*), + 2 boundary conditions: a shared **frame** or **venue** is not a shared mechanism; a **deferral** carries an obligation to **co-locate**.
  - **Interim `confidence` rule REVOKED as false on my measurement** -- 214/214 cards rated, 112 entry mirrors, zero disagreement -> *absent from the entry means rated on the card*. Change (A) **HELD FOR REDRAFT**; **I am RECUSED** on the confidence half.
  - **D4 adopts Finn's reuse-composition** + my stance-vs-maturity refinement (stance is DIRECTION; rule 2 is precedent, not the field to reuse). Read-back **Hopper**.
- **Carry-forward:**
  - **[UNADDRESSED, mine, LIVE] This header had drifted from its own spec** -- 4 invented fields, **`Active items` and `Key decisions` both MISSING**, and `Current state` stale at wiki 214 while the tree held 218. Repaired 11:23 **only because Volta reported the identical drift in his own file.** Transcript is **193 lines against a 100-line limit** -- still unpruned, shutdown work.
  - **[DEFERRED] Contract changes, one at a time, on PO go:** (A) WikiProvenance reconciliation (Aen+Celes shape it; carries the entry-set definition + one-lineage-one-TTL + status-semantics); (B) `discoveryOrder` on KnowledgeSubmission; (C) `negative-probe` restructure (**I am recused**; non-author read-back Hopper or Herald, NOT Brunel).
  - **[WARNING] Retrieval gap, not a filing gap: 9+ submissions, ZERO Protocol B queries today.** My corpus held both the finding Hopper re-derived AND the rule about querying it (`process/query-the-librarian-before-reporting-a-discovery`, high, confirmed, filed 08-28) and **neither reached a reader.** Phase-2 health signal, owed to Aen's PO checkpoint. Fix adopted = a **precondition in the dispatch shape**, not another rule.
  - **[WARNING] My own inbox lags under load** (n=2 agents now; Aen re-sent to Hopper 3x). If a reply is overdue, read the inbox FILE. Local Windows friction, not wiki-grade.
  - **[OWED TO ME] Hopper: Defect-4 read-back + form-11 read-back. Brunel: wake-amendment read-back at render.**
---
## Session transcript (prune beyond line 100)

## 2026-08-31 (Aen: S67 / mine: S73) -- wiki 214 -> 218. **Queue mechanics live in the HEADER; this is durable knowledge only.**
- **[THE SESSION'S FINDING ABOUT ME, and it outranks every entry filed] 9+ submissions, ZERO Protocol B queries.** My corpus held **both** the finding Hopper re-derived **and** the rule about querying it (`process/query-the-librarian-before-reporting-a-discovery`, high, confirmed, filed 08-28) -- **and neither reached a reader.** Filing was COMPLETE; gates closed. **This is a RETRIEVAL gap, and retrieval is my half of the dual-hub contract.** Volta's reframing, which I had not seen: ***a corpus sweep is not only deduplication -- it is how a NEW MEASUREMENT GETS ATTACHED TO THE QUESTION IT SETTLES.*** Dedup only saves effort; that direction **creates knowledge neither artifact contained alone.** His sweep was the session's ONLY query and produced its only resolution of a standing contradiction.
- **[ADOPTED AS STANDING -- Finn's DISJOINT-REMEDY TEST, now filed]** *Two findings whose corrective actions do not overlap are not one mechanism.* Beats *"do they share a mechanism?"*, which is won by whoever thought about the genus longest = **stamina, not adjudication.** **THREE boundary conditions, all earned the same day:** (1) a shared **FRAME** is not a shared mechanism -- **nor is a shared VENUE**; (2) when A's remedy **DEFERS** to a condition B supplies, disjointness holds **AND carries an obligation to CO-LOCATE** -- *worse than silence, because silence prompts a question and a named-but-unsupplied obligation closes one*; **a seam between two CORRECT entries cannot be seen from inside either**; locator = **is the supplying entry's question as wide as the deferring entry's obligation?**; (3) **overlap on the GENERATING instance is uninformative** -- it sits inside both remedies by construction, so **always run the test against the OTHER cases each remedy claims.** It ruled **in both directions in one day**: *a discriminator that only ever says yes is a rationalisation.*
- **[FILED] `patterns/state-the-membership-rule-of-the-set-you-counted`** [finn+hopper+callimachus, high, confirmed] -- *a count is taken over the set that was EASY TO ENUMERATE and reported as a count of the set the claim NAMES.* **The danger is the asymmetry: a wrong denominator produces a PLAUSIBLE number.** **5 occurrences in one day, 4 agents**; occ 4-5 illustration-only (correlated). **Clause one demonstrably paid off** -- a stated rule made an error reconcilable in ONE step. **[FILED] `process/disjoint-remedy-test-for-umbrella-versus-cross-link`** [finn, medium, confirmed] -- the bound is on the **APPLICATIONS** (one adopter), not the idea.
- **[HELD FOR REDRAFT -- change (A)'s premise is REFUTED; see header] MEASURED: cards 214/214 carry `confidence`, entries 112/214, ZERO disagreement** = a half-populated mirror. **No entry in this wiki is unrated.** `absent = "unrated"` is false, and documenting it **formalises a mirror** -- my own S63 `stage-2` CARDS-ONLY ruling argues against that at 4 mirrors; this is 112. **I am RECUSED.** No era correlation (Apr 49/9, May 4/56, Aug 19/30): ad hoc for the corpus's whole life, which is what an undocumented field does.
- **[THE DAY'S GENUS, 4 instances + 1 counter-example] *STATING THE RULE IS NECESSARY BECAUSE KNOWING IT IS NOT SUFFICIENT.*** Brunel x2, Volta, Hopper -- **his the most expensive: he TRANSCRIBED the shipped hook guidance verbatim to a colleague, then wrote a hook that ignores it four hours later**, having guarded the *blocking* axis and never considered the *injection* axis. **The one counter-example: Hopper's `SessionStart` positive control fired** -- submission 6's own pattern, applied by its author an hour after violating it -- **and he declined the credit, because the prompt came from OUTSIDE.** **Three people declined credit on externally-prompted-vs-self-triggered today** (Volta, me, Hopper); that distinction is becoming the team's default honesty move.
- **[TRANSFERABLE FORM, 3 hands one day, CORRELATED (one idea travelling)] When the MECHANISM is contested but the REMEDY is not, FILE THE REMEDY and mark the mechanism open.** My hook correction (*wrong under either explanation, so ship it*), Hopper's either-mechanism observation, Aen's **defense-vs-trigger** split (*re-validate an aged authorization against current state* -- works under every candidate mechanism).
- **[MY OWN FAILURES, the ones to carry]** (1) **I stated a count and enumerated a different number** -- said 8, enumerated 11, actual 12; the S63 slip repeating, and **Aen turned it into a work order within a minute.** [RULE] *an enumeration and its count are two claims; the error is invisible from either alone.* (2) **My VERIFIER failed twice running**, both the membership-rule shape -- an awk that did not stop at the frontmatter boundary, then one assuming a single resolution base. **I did NOT report the flood**: a corpus-wide `related:` audit cannot run until the base is settled (Finn's open defect). (3) **My scratchpad header had drifted from its own spec** -- 4 invented fields, `Active items` and `Key decisions` MISSING, `Current state` stale at 214 while the tree held 218, **and the entire held queue unrepresented.** Repaired only because Volta reported the identical drift in his; **prompted-by-a-colleague is not the remedy working.** (4) **I hardcoded a count into the audit tool and it was stale within the hour** -- *a DATED measurement does not rot; an undated current-state claim does.*
- **[RULINGS THAT WILL BE CITED]** `[URGENT]` hook-cap correction FILED under Aen's exception: **the cap governs BLOCKING hooks only**, and **`asyncRewake` is an OPEN GATING QUESTION -- the entry had been recommending the escape from the sole bound.** D4 = **Finn's composition ADOPTED** plus my *stance is DIRECTION, maturity is a different axis* (Hopper verified that is the table's own semantics); the chain is **rule 5 -> `unverified` -> §4 table -> `[GAP]`**, two steps, and the seam is that it rides a **catch-all.** Volta sub-3 -> files **with Hopper's *an eliminated confound is not an identified cause***, NOT form 11 (**artifact-with-no-field vs speaker's-reasoning**). Volta sub-2 = **no-slot FORM 11**, outside the umbrella (umbrella covers forms 5/9/10 ONLY -- my carried note said otherwise and was STALE). Brunel's key-path finding: **the copies NEVER DIVERGED, so every drift remedy we hold would have passed** -- *agreement across copies is worth nothing when they share one source.*
- **[CANDIDATE UMBRELLA, recorded NOT filed] Brunel's three findings share ONE remedy: stop maintaining a recorded copy; point at the thing that already computes it** (exec bit in git / `grep '^COPY' Dockerfile` IS the list / `ssh` resolves the identity). **My revisit trigger may have fired.** BOUND: all three his or Hopper's, one session, one work-stream -- discounted as everything else was.
- **[STILL HYPOTHETICAL, do not upgrade] Four crossed-message pairs, all benign** -- and all benign **because the later message was a RESTATEMENT, not a contradiction.** Aen briefly supplied a reversal-carrying instance and **retracted it on Hopper's evidence.** *A retracted instance must not quietly upgrade a watch.*

## 2026-08-28 (S66) and earlier -- PRUNED 2026-08-31; the entries carry the detail
- **[S66]** wiki 193 -> 214; Protocol-A queue drained (Brunel 8, Hopper 3+5, team-lead 3, Celes 1). **9 of my calls reversed, EVERY ONE because a specialist opened the ARTIFACT rather than reading my ack.** Shipped `wiki/tools/schema-population-audit.py` (7th layer) -- **six layers checked the layers against EACH OTHER and never once against the SCHEMA.** Tier-2 drift again: 5 index rows stale 2.5 months, and **the total summed either way, which is why audit-by-sum never caught it.** [MY WORST] built a headline on someone else's inference **while holding the refuting evidence**, then shipped it as an ack pin twice.
- **[S65]** `negative-probe-result-underdetermined-absence-read-as-permanent` filed [aen, medium] -- *presence is self-certifying, absence is not.* **[LESSON] a relayed EVENT gets the same check as a relayed claim; when a rule already stands on its own ground, do not bolt an event onto it** -- every added prop is one more thing that can turn out false, and it contaminates a true rule's provenance.
- **[S63, the largest batch]** 39 pre-gate cards relabelled `legacy-unaudited` (audit noise 49-hits/10-real -> 9/9; **the data fix and the query fix were BOTH needed**). **`confirmed` certifies a PROCEDURE, not CORRECTNESS** -- the confirmer held the falsifier and never connected it. **`stage-2` ruled CARDS-ONLY** (a mirror with no writer). **A promotion criterion that needs a ruling to apply is not a criterion.** [MY WORST] **I corrected Finn's ACCURATE claim into a FALSE one, from a source that was wrong when I read it** -- *a scratchpad summary is a CLAIM ABOUT A SOURCE, never the source.* Every number I was handed that day was wrong, each for a different reason.
- **[S62/S61]** **Both defects were in TIER 2, not the entries** -- *the derived layer is where staleness accumulates, precisely because nobody re-reads it.* A closing record describes intent at close; **the working tree describes what happened.**
- **[DURABLE KEY CALLS, still live]** version-coupled vs version-stable (revision trigger = version change, NOT n+1); *mega biblion* -- a pointer, never a 2nd sheet; decisions/protocols = POINTER not copy; arch-fact entries do not gain confidence from n+1; author-submitted Protocol-A = confirmed at filing, **librarian-authored-on-relayed-candidate = PENDING**; **evidence must not depend on a prunable store -- QUOTE it**; **awareness of a pattern is not protection -- only a check with a defined trigger is**; **pin confidence to the weakest load-bearing claim, never an average**; **run `date` before EVERY send** (fabrication n=5 across 3 agents; it INVERTS ORDER, not merely accuracy). [GOTCHA-self] cwd resets between Bash calls -- absolute paths; verify file tails after Write; **heredocs with backticks/quotes fail -- write a script file** (hit again today).
- **[SESSION-NUMBERING FORK, standing]** Aen numbers per continuous session; Hopper and I per working day. **Dates are canonical; S-numbers never travel alone.** ~875 bare S-number strings corpus-wide are legacy notation, NOT a bulk-rewrite target.

## Sessions 1-62 (heavily pruned -- full detail in git history and the entries)
- [S56] Folding a held pending-refinement blockquote into the body on final co-author confirmation = the canonical Stage-2 endgame. [S55] SUPERSESSION without archival (version-coupled banner). [S50] *mega biblion* on T-entries (23 atomic -> 2 wiki); first INDEX-DRIFT fix. [S44] Entu schema; evidence-TYPE vs verification-METHOD on separate axes; "mirror don't invent."
- [S40] Epic #67: 118 cards + the card-INDEX system (#68); scratchpad summary-header convention (#69); Stage-2-confirms gate (#70). **Held the scope line on common-prompt edits -- propose; team-lead writes L1.** [S39] Card-tier writing is extractive, not transformative. [S1-37] **Phase 2 activated S6.** Protocol C promotions (substrate-invariant-mismatch, worktree-isolation, semver-strict, relay-fidelity); cross-pollination unit = idea, not file.

(*FR:Callimachus*)