# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S70, 2026-09-02, 15:30):** wiki **229 entries = 229 cards** (membership rule: `wiki/<subdir>/*.md` excl `INDEX.md` case-insensitively AND `README.md`; card = same name under `cards/`). **NOTHING FILED this session** -- S70 closed by PO after ~15 min; **zero Protocol A received** (Hopper's submission-6 did NOT reach me). CLI **2.1.258** (from `~/.claude/sessions/30620.json` `version`, not memory). PO directive 15:17: **no chatter -- message team-lead only with a finished deliverable or a blocker.**
- **Active items:**
  - **[WIP, NOT STARTED AS FILING] Task 1 revision trigger 2.1.251 -> 2.1.258.** Inventory + seat-observations done (below), **no entry touched**. Plan was ONE compact version-pointer datapoint for the implicit-teams lineage (2.1.173 shape: measured rows + explicit NOT-re-measured rows, own TTL), everything else LEFT with its TTL governing and named for Hopper's bg probe.
  - **[WIP] S69 courier-past-graceful-exit claim: PLAUSIBLE, NOT CONFIRMED.** Decisive unread code = `stationmaster-courier.py:674-755` (`inject_batch`) -- see below. Do not file from Aen's WRAP.
  - **[OPEN] Brunel read-back of 5 pending** (named below); never prompted -- his plan had not landed when S70 closed. **[OPEN] G3 `resolved`-enum** Protocol C (Aen's). **[BLOCKED] backlog (e)** Hopper sub-6 -- still needs his one line; do NOT guess.
  - **[HELD BY RULING] card `related:` paths -- normalise on touch, NO sweep** (2026-08-12 ruling).
- **Key decisions this session:** none filed. Two expired-TTL entries (`inbox-drained-on-spawn` 08-07, `subagent-harness-blocks-curator-role-repo-write` 08-26) are already `[TTL-EXPIRED]`-flagged in-body -- **deliberately left as flagged**, not re-verified, not archived.
- **Carry-forward:**
  - **[WARNING, still true] submissions >> Protocol B queries** (S70: 0 / 0).
  - **[DEFERRED, PO go] (A) WikiProvenance reconciliation (RECUSED on confidence half); (B) `discoveryOrder`; (C) `negative-probe` restructure (recused; read-back Hopper/Herald, NOT Brunel).**
  - **[OWED TO ME] Read-backs: Volta x3, Brunel x5 (see list), Hopper x4 reconstructed (load-bearing).**

---
## Session transcript (prune beyond line 100)

## 2026-09-02 (S70, ~15 min, closed by PO) -- inventory only, nothing filed

- **[TASK 1 INVENTORY] Version-coupled entries with a TTL (16 + 2 datapoints), nearest expiries first:** `inbox-substrate-properties-2.1.170` + `inbox-retention-flip-pending-only-queue` **09-10**; `drain-on-delivery-datapoint-2.1.173` **09-15**; `teams-substrate-2.1.179` + `claude-code-hook-wake-primitives` + `no-teamdelete-stale-session-dirs` **09-17**; `cold-start-discovery`, `courier-restart-needs-inboxes-dir`, `explicit-courier-config-hardcoded-path`, `orphan-courier-holds-lock`, `sessions-pid-json-not-gc` **09-18**; the two 2.1.251 datapoints + `bg-session-registry` + `protocol-a-has-no-durable-store` **11-30**; `inbox-file-write-as-wake` 11-12, `members-array-edit` 11-14, `inbox-slot-vs-members` 11-19, `inverted-trigger-primitives` 11-27. **Retire-and-forward rule as filed (2.1.179 sheet §Revision trigger): a sighting on a new version = NEW datapoint, filed as a version-pointer mega-biblion-style (2.1.173 shape), not a fresh sheet, never folded.** Both 2.1.251 sheets are `stage-2: pending` (reconstructed, Hopper unread).
- **[SEAT OBSERVATIONS on 2.1.258, NOT FILED -- direct, n as stated]** (1) **Team-dir slug = `session-<sessionId[:8]>` HOLDS, n=2 interactive** (`session-97b61440` <-> `sessions/30620.json` sessionId `97b61440-…`; `session-b65192ba` <-> `sessions/8496.json`; both stamp `version: 2.1.258`). (2) **`config.json` eager: present** (createdAt ≈ 15:07:21 = registry `startedAt`). (3) **`inboxes/` lazy: NOT MEASURABLE** -- Aen's Step-3 restore wrote 46 inbox files at 15:12, before members joined 15:14-15:15; the pre-restore state is gone. (4) **`~/.claude/sessions/` holds ONLY the two live pids while 15 `session-*` team dirs exist** -- consistent with S68's *graceful exit GCs `sessions/<pid>.json`* half AND with team-dir accumulation, but NOT attributable to kill type from a snapshot; do not file as a cell measurement. (5) **Drain: NOT measured** (plan was to piggyback on the real send to Brunel and watch `brunel.json` size 2 -> content -> 2; never sent). (6) **Observed, not compared:** spawned `members[]` entries now carry `model` AND the full spawn `prompt` (config.json 10KB) -- unknown whether new since 2.1.251; a question for Hopper, not a claim.
- **[COURIER CLAIM -- what the SOURCE says so far]** Source = `poc/ghost-bridge/fr-courier-daemon.py` (Brunel's wrap) over `stationmaster-courier.py` (Herald's reference). **Confirmed from code:** (a) `inboxes_dir` is resolved **ONCE at Config load** (`auto` -> `resolve_team_dir`, daemon.py:331-332) and **never re-resolved per cycle** -- the loop (284-311) carries the launch-time dir; (b) per-agent guard (3) `candidate.exists()` false -> **falls back to `default_inbox` = team-lead** (116-145), so a deleted dir routes everything to a team-lead path that no longer exists; (c) ordering is inject FIRST, ledger SECOND, ack ALL collected (164-237); `InjectError` removes the id from the ack set -> hub redelivers. **UNVERIFIED, and it decides the claim:** what `inject_batch` (reference 674-755) does when the PARENT DIR is gone -- `open(path,'x')` on a missing parent raises `FileNotFoundError`; if that surfaces as `InjectError` the ack is withheld and **Aen's claim is FALSE (safe)**; if any path `mkdir(parents=True)`s the target or swallows the error, the ack fires over a resurrected orphan dir and **the claim is TRUE (silent loss)**. Note `validate_startup` mkdirs `inboxes_dir` at STARTUP only (1203), and `inject_tmp_dir.mkdir(parents=True)` runs every inject (686) -- check where `inject_tmp_dir` lives (139: "same volume as inboxes"). **One read settles it. Read it before writing a word.**
- **[COURIER STATE today]** pid **29816**, started 15:09:23 on `fr-courier.config.auto.json`, `interval=30s`, outboxes `['apex-research-courier']`; **bound dir NOT logged** and **two sessions were live at bind time** (97b61440 + b65192ba) -- the resolution is unknown, which is exactly Aen's BOOT-item-2 ambiguity. Scheduled Task `FrameworkResearch-Courier` = **Disabled**. Ledger last id 2026-08-28. Aen said the courier is stopped at S4 on 2.1.178+; S70 startup restarted it anyway (15:09) -- record, not a finding.
- **[BRUNEL'S 5 PENDING, identified from cards]** `agreement-across-copies-is-worthless-when-they-share-one-source`, `exec-bit-is-tracked-by-git-use-update-index-chmod`, `merged-and-shipped-are-different-claims-git-show-stat-is-the-check` (his solo, `source-agents: [brunel]`), the **md5-scoping amendment** on `process/runbook-source-tree-drifts-under-active-authorship-pin-by-checksum`, and the **growth sub-shape** on `gotchas/verification-step-goes-stale-invisibly-because-it-passed`. **Total `stage-2: pending` cards = 16** (Hopper 4 incl. both 2.1.251 sheets; Finn/Aen 4; Medici 2; me+Finn 2; Volta+Hopper 2).
- **[GOTCHA-self, hit again]** a recursive grep over the whole `github/` tree for the courier source blew the 120s tool timeout and produced nothing; the skill file (`~/.claude/skills/inter-team-comms/SKILL.md`) named the path in one line. **Ask the index before scanning the corpus.**

## 2026-08-31 (S68) -- pruned to the calls that still govern
- **DECAYED vs FALSE-AT-AUTHORING:** a stale claim ARRIVING feels like a false claim BEING MADE; re-deriving on read protects CONTENT, never CLASSIFICATION -- needs an instrument, not attention. Watch stays at FOUR.
- **Rules that fire are NARROWER, MORE MECHANICAL ones attached to an ACTION, not an INTENTION**; three counter-examples, none generalised.
- **Protocol A has no durable store: reading deletes the message, exiting deletes the mailbox.** Recovery rate set by submitter habit -- not a property a protocol should have. Filed as TWO entries (disjoint-remedy test).
- **entry -> card -> INDEX row -> index.md, THEN the next entry.** Batching index rows created all 11 of S68's drift. Order work so an abrupt stop lands on "not started", never "entry without card".
- **Before reporting an edit as done, grep the artifact** (I made a false completion claim to Aen). **Test the whole candidate space before "neither wins"** (repo-root base 0/831 was the real headline).
- **A header is a CLAIM ABOUT a state and decays like any derived layer** -- every count carries its membership rule inline.

## S67 and earlier -- pruned; the entries carry the detail
- **DISJOINT-REMEDY TEST** standing (+3 boundary conditions). **A datapoint may not file without a `ttl`.** **`confidence` interim rule REVOKED; I am RECUSED** on that half.
- *presence is self-certifying, absence is not.* **`confirmed` certifies a PROCEDURE, not CORRECTNESS.** **`stage-2` = CARDS-ONLY.** **A scratchpad summary is a CLAIM ABOUT A SOURCE, never the source.**
- **[DURABLE]** version-coupled vs version-stable (trigger = version change, NOT n+1); pointer, never a 2nd sheet; arch-fact entries gain nothing from n+1; author-submitted = confirmed at filing, librarian-on-relayed = PENDING; pin confidence to the weakest load-bearing claim; **run `date` before EVERY send**; cwd resets between Bash calls -- absolute paths; verify after every scripted edit; heredocs with backticks fail -- write a script file.
- **[SESSION-NUMBERING FORK]** Aen per continuous session, Hopper and I per working day. **Dates are canonical.**

(*FR:Callimachus*)
