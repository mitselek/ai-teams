# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S68, 2026-08-31, 14:30):** wiki **228 active entries = 228 cards, MEASURED** (218 -> 228 today; the 1 archived entry has no card by design). CLI **2.1.251, no bump**. PO hold LIFTED; **the S67 held queue is FILED.** Git is Aen's. **Membership rule for every count here: entry = `wiki/<subdir>/*.md` excluding `INDEX.md` case-insensitively; card = same name under `<subdir>/cards/`.**
- **Active items:**
  - **[OPEN] G3 courier-gotchas status not yet filed** -- gate closed at S58, never harvested into the wiki (records lag, not an open technical question). Its **`resolved`-enum flag is a Protocol C proposal for Aen, not a wiki edit**: both courier entries promise a `status` transition `WikiProvenance` cannot express (`active|disputed|archived`; corpus 426/2/**0**).
  - **[OPEN] backlog (c) S67 as instance 2 into `process/query-the-librarian-before-reporting-a-discovery`; (d) strikethrough entry + artifact-claims fold; (e) Hopper's `SessionStart` positive control as the counter-example on his submission-6 entry.**
  - **[BLOCKED ON A RULING] card `related:` resolution base.** 156 of 225 cards carry >=1 path that will not resolve from the card's own dir (356 links). Measured on untouched cards: **card-dir base 58%, entry-dir base 68% -- neither wins.** This is Finn's `frontmatter-reference-field-without-enforced-resolution-base` at corpus scale. **Do not bulk-fix without his base ruling.**
- **Key decisions this session:**
  - **The durable-store finding filed as TWO entries, on the disjoint-remedy test:** an archive for Protocol A does not fix a success line counting files; fixing the line does not create an archive. **Shared venue + shared frame, NOT a shared mechanism.** Co-location note in both.
  - **Everything re-enveloped from a scratchpad files `stage-2: pending`** -- librarian-authored on a relayed candidate, fail-closed. Hopper's are weaker still (**reconstructed**, never relayed) and structured so an author-submitted `confirmed` lands on top without a rewrite.
  - **Filed the honest `[GAP]` over a plausible entry** for the npm-mask framing and the phantom "casualty 3 / sub-7".
  - **G1+G2 filed as NEW version-stamped datapoints** (`2.1.251`), forwarding from the 2.1.179 sheet, each with a TTL -- retire-and-forward, not a silent fold.
- **Carry-forward:**
  - **[WARNING, still true] Retrieval gap: submissions >> Protocol B queries.** Fix adopted = a precondition in the dispatch shape (Celes's prompt-layer work, not mine to write alone).
  - **[DEFERRED, PO go needed] (A) WikiProvenance reconciliation -- I am RECUSED on the confidence half; (B) `discoveryOrder`; (C) `negative-probe` restructure (recused; non-author read-back Hopper or Herald, NOT Brunel).**
  - **[OWED TO ME] Read-backs: Volta x3, Brunel x3+amendment+sub-shape, Hopper x4 (reconstructed -- his read-back is load-bearing, not courtesy).**

---
## Session transcript (prune beyond line 100)

## 2026-08-31 (Aen S68 / mine S74) -- the queue filed, and the loss that outranked it

- **[THE SESSION'S FINDING] Protocol A has no durable store. I went to file the queue and the source was gone.** Inbox messages are **consumed on delivery** (measured: Aen's 13:16 wake was already absent from my own inbox); and **a clean exit deletes the whole `session-<id>` team dir, non-empty `inboxes/` included** (Hopper's GC measurement -- which is exactly why S67's `session-b9269601` and its 45 inbox files are not on disk). **Two mechanisms compose: reading deletes the message, exiting deletes the mailbox.**
- **My standing rule needed a stronger word.** *Evidence must not depend on a prunable store -- QUOTE it.* **The store was not prunable, it was SELF-DRAINING**: prunable loses old things under pressure; self-draining loses everything on the normal path, immediately. **Reading IS the deletion.** The rule was written imagining eviction; no eviction was required.
- **[THE ROW THAT IS THE WHOLE ARGUMENT] Volta and Brunel lost nothing; Hopper lost a third of his batch -- and the difference was NOT care taken over the submissions.** It was an unrelated personal habit of writing to a scratchpad. **The recovery rate of this failure is set by how conscientious each submitter happened to be. That is not a property a protocol should have.**
- **[MY OWN, and it is the day's sharpest] I made a structural change on an assumption I had not verified, in the session I filed two entries about exactly that.** Found broken card links, "fixed" 10 cards `../` -> `../../`, **then checked and reverted.** Neither base wins (58/68). **My carried note -- *a corpus-wide `related:` audit cannot run until the base is settled* -- is what stopped me shipping a 156-file change on a false premise.** A carried note firing correctly, for once, instead of a rule that failed to fire.
- **[COUNT ERRORS IN MY OWN HEADER, n=3 today, all in the file I repaired yesterday for exactly this]** (1) Active-items said *Volta: 2* while the decisions line ruled on *sub-3*; actual 3. (2) Said *Hopper: 9*; his own count is **6**, and **no seventh submission or "casualty 3" exists in any surviving artifact** -- I carried a trap about filing something that was never there. (3) Said the transcript was **193 lines against a 100-line limit**; **it was 46 and already compliant.** Aen relayed (3) to me as my largest carried debt and it did not exist. **[RULE, earned three times] A header is a CLAIM ABOUT a state, and it decays exactly like any other derived layer -- including the claims it makes about itself.**
- **[GENUS CONFIRMED, and I am now an instance] *Stating the rule is necessary because knowing it is not sufficient.*** Instance: I filed `state-the-membership-rule-of-the-set-you-counted` yesterday at high/confirmed, then produced three miscounts inside the artifact that rule governs. **Recorded on that entry, not as a new one** -- the genus is named; this is evidence for it.
- **[MEASUREMENT DISCIPLINE THAT PAID] Every count in this session's header carries its membership rule inline**, because two of the day's three count errors were denominator errors. The `INDEX.md`-uppercase trap is the concrete one: a case-sensitive `find` excluding `index.md` counts it as a card and shifts every total by 9. **Aen's 219/218 came from that; the real baseline was 218 = 218.**
- **[RULINGS] Volta sub-2 = no-slot FORM 11, registered in the hub (entry AND card -- the register was stale at 10).** Its **anti-friction foreclosure is in all three layers**: both instances are an agent authorized, blocked, and *correctly declining to route around*, and a friction reading turns a record of two refusals to launder **into a manual for laundering**. Volta sub-3 pairs with **Hopper's *an eliminated confound is not an identified cause***, NOT form 11 -- **artifact-with-no-field vs speaker's-reasoning**; Hopper had asked for form 11 and Volta corrected him, and Hopper's own line on it is the best in the record: ***"I reached for form 11 because the SHAPE matched, without asking whether the CAUSE did -- which is the same move as the error itself."***
- **[BRUNEL] md5 discriminator scoped to md5-of-a-FILE** -- an aggregate digest **hashes a tool's PRESENTATION** (MSYS binary-mode asterisk; locale collation): same tree, three digests. **Hash the BLOB, not the worktree** (his own 12:50 self-correction; a chmod re-materialised `joosep.sh` through autocrlf and a worktree gate would have false-STOPped). **The discriminator that generalises: the hazard is never the FUNCTION, it is the EXPOSURE -- "do the two operands cross a boundary?", never "is it a digest?"** -- which is what keeps the entrypoint's own safe `dir_digest` from being "harmonised".
- **[BRUNEL, co-location honoured] sub-1's link sits INSIDE sub-3's transport paragraph**, not in a neighbours list, because **the seam is invisible from inside either entry**: `pin-by-checksum` says RECORD md5s, `agreement-across-copies` says DO NOT record the value. **Discriminator = what the recorded value is ABOUT** -- a checksum describes a file you hold; a key path describes a machine you cannot see.
- **[UMBRELLA HELD BACK AGAIN] Brunel's three findings share one remedy (*stop maintaining a recorded copy; point at the thing that already computes it*) -- recorded, NOT filed**, discounted for correlation (all his or Hopper's, one session, one work-stream). **A 4th from a different work-stream makes it hard to refuse.** Consistency with every other umbrella call today mattered more than the fresh finding.
- **[TRAPS THAT FIRED CORRECTLY]** the **~3.2s cold-start figure stayed OUT** -- bg-measured n=1, and quoting a figure as a general property is what produced the S57 false halt; the sheet carries **the ordering, not a number**. The **injection row filed REFUTED, not untested** (in-harness `config.json` edit denied by the classifier), with Volta's narrowing kept: **it does NOT say the ghost-courier design is broken** -- that path is out-of-band and still untested. The **gate is the live agent registry, NOT `members[]`** -- the ops-log's own conclusion was wrong and I filed the correction.
- **[HOPPER'S GC NARROWING, the shape worth reusing] "A bare 'refuted' here would have invited someone to relax correct code."** Half refuted, half intact, **guidance unchanged** -- the hard-kill path still leaves a dead entry reading identical to a live idle one. **He also narrowed his own n=5 to three cells**, because counting a window-close as another `claude stop` *"destroyed the distinction it was actually available to draw."* **Frequency changed; the required check did not.**

## 2026-08-28 (S66) and earlier -- PRUNED; the entries carry the detail
- **[S67]** wiki 214 -> 218. Adopted Finn's **DISJOINT-REMEDY TEST** as standing (*two findings whose corrective actions do not overlap are not one mechanism*) + 3 boundary conditions: a shared **frame or venue** is not a shared mechanism; a **deferral** carries an obligation to **CO-LOCATE**; **overlap on the generating instance is uninformative** -- always test the OTHER cases. **BOUND to retire-and-forward with an instrument, not a promise:** a datapoint may not file without a `ttl`; *one lineage, one TTL*. **Interim `confidence` rule REVOKED as false on my measurement** (214/214 cards rated, 112 entry mirrors, zero disagreement -> *absent from the entry means rated on the card*); **I am RECUSED** on that half.
- **[S66]** Protocol-A queue drained. **9 of my calls reversed, EVERY ONE because a specialist opened the ARTIFACT rather than reading my ack.** Shipped `wiki/tools/schema-population-audit.py` -- **six layers checked the layers against EACH OTHER and never once against the SCHEMA.** [MY WORST] built a headline on someone else's inference **while holding the refuting evidence**.
- **[S65/S63]** *presence is self-certifying, absence is not.* **`confirmed` certifies a PROCEDURE, not CORRECTNESS.** **`stage-2` ruled CARDS-ONLY** (a mirror with no writer). **A promotion criterion that needs a ruling to apply is not a criterion.** [MY WORST] **I corrected Finn's ACCURATE claim into a FALSE one, from a source that was wrong when I read it** -- *a scratchpad summary is a CLAIM ABOUT A SOURCE, never the source.*
- **[S62/S61]** **Both defects were in TIER 2, not the entries** -- *the derived layer is where staleness accumulates, precisely because nobody re-reads it.*
- **[DURABLE KEY CALLS, still live]** version-coupled vs version-stable (revision trigger = version change, NOT n+1); *mega biblion* -- a pointer, never a 2nd sheet; decisions/protocols = POINTER not copy; arch-fact entries do not gain confidence from n+1; author-submitted Protocol-A = confirmed at filing, **librarian-authored-on-relayed-candidate = PENDING**; **pin confidence to the weakest load-bearing claim, never an average**; **awareness of a pattern is not protection -- only a check with a defined trigger is**; **run `date` before EVERY send**. [GOTCHA-self] cwd resets between Bash calls -- absolute paths; **verify after every scripted edit -- a python `replace` that misses its anchor prints success and changes nothing** (hit again today, caught on a card); heredocs with backticks fail -- write a script file.
- **[SESSION-NUMBERING FORK, standing]** Aen numbers per continuous session; Hopper and I per working day. **Dates are canonical; S-numbers never travel alone.**

(*FR:Callimachus*)
