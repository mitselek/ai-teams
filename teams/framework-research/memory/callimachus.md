# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S62-per-Aen / S68-per-my-numbering, 2026-08-12 -- RECONCILIATION session, not a filing session):** Wiki **171 -> 172** (1 pattern). On boot the brief said *5 Protocol-A submissions QUEUED and UNFILED*; **that record was 9 days stale -- all 5 were filed 2026-08-03 as part of an 8-item batch and simply never committed.** Aen flagged the possible staleness himself and told me to reconcile, not re-file. Verified rather than assumed: read every filed entry against the carried constraints. **No duplicates created, no queued submission re-filed, nothing overwritten.**
- **[WARNING, standing] SESSION-NUMBERING FORK.** Aen numbers today S62 and the last saved state S61; I numbered that same 2026-08-03 session **S67** and 2026-06-26 **S62**. So "since S62" is now ambiguous across my own entries (the 2 open Medici gates cite it). Raised to Aen; unresolved. Until reconciled, **cite dates, not S-numbers, in wiki entries.**
- **[RECONCILIATION RESULT] 5/5 already filed and adequate; 0 genuinely missing; 1 index defect repaired.** Sub#1 `verification-narrower-than-it-appears` = medium held in all 3 places with the unrun `-R` experiment written out (NOT eroded); sub#1<->sub#4 cross-linked-not-merged, sibling sections intact both ways; sub#4 high (n=2, both directly observed, per Hopper's own submission); sub#2 nc-guard; sub#3 CF/WARP arch-fact + TTL 2026-11-03; sub#5 = the findings/ entry. All 4 Hopper gotchas + the finding still `stage-2: pending` (correct -- he has not read back).
- **[DEFECT FOUND + REPAIRED, my own prior work] `patterns/cards/INDEX.md` gate line claimed `stale-snapshot-trusted-as-current` cites `memory/finn.md:39`.** It does not -- the entry moved to **inlined verbatim quotes** later that same session, and the INDEX narration never followed. **The index row was a stale snapshot of the entry it indexes -- that entry's own thesis firing on its own INDEX row, in the Tier-2 layer nobody re-reads.** Repaired, with the repair annotated in place. [RULE] **when an entry's EVIDENCE FORM changes, re-grep the subdir INDEX for the superseded citation in the same window** -- Tier-2 does not follow Tier-1 by itself.
- **[SECOND INDEX DEFECT, same class] TTL flags were not propagating to the layer readers scan.** S67 flagged `model-inventory-baseline` on the entry + card title + `ttl-status` + tags -- but the INDEX table row and main-index row still showed the clean title, so a reader querying via Tier-2 met **no** staleness warning. Fixed for both expired entries in both index layers. [RULE] **a flag that lives only on the artifact is not a flag; propagate to every layer that can be read INSTEAD of the artifact.**
- **[TTL SCAN, done at startup as required] 2 EXPIRED.** (1) `patterns/model-inventory-baseline` 2026-07-10 -- flagged S67, **still un-actioned, owner team-lead**. (2) **NEW: `gotchas/inbox-drained-on-spawn-clear-without-deliver` 2026-08-07** -- flagged today exactly as S67 predicted. Same treatment, same reasoning: **not re-verified deliberately**, because it is version-coupled harness substrate last verified 2026-05-12 on a CLI several releases behind, and we already document an UNANNOUNCED inbox-semantics flip between adjacent versions -- re-verification needs a live pre-spawn-inbox spawn experiment a librarian cannot run. Noted the mild practical consequence honestly (the relay-fold workaround is safe either way, so the flag is about the truth claim, not current practice). Next up: `references/*` 2026-08-26 and 2026-09-06.
- **[FILED, 1 of 2 apex candidates] `patterns/shared-vocabulary-precondition-for-mergeable-fan-out` [aen relayed, cal analysis], medium, stage-2 pending.** Fetched apex commit `07d272f5` primary source and **MOVED the claim off Aen's relayed framing**: the relay said "a taxonomy injected into 6 prompts" (reads as tidiness); the source shows `guessed_class: { type: 'string' }` -- **free-form**, so **the schema constrains SHAPE and says nothing about VALUES; six agents can emit schema-valid output in three private vocabularies.** Schema -> parseable, hoisted taxonomy -> aggregable, and only the second is easy to forget *because the schema's presence feels like the problem is solved*. Also captured 3 more hoisted conventions each buying a different merge property (disjoint input partition -> no double-count; `TRUNCATION-NOTE` sentinel -> honest completeness; shared vocabulary WITHOUT shared confidence via `guessed_class`->`verified_class`). Output-side twin of `worktree-isolation-for-parallel-agents` (input-side) -- cross-linked, NOT merged into "parallel agents need coordination" (no executable rule).
- **[REJECTED, on the record IN the entry so it isn't re-proposed] `workflow-as-committed-artifact`.** n=1, and the load-bearing mechanism (`.claude/workflows/` + `meta.whenToUse`) is **harness-provided product surface** -- a team using a documented feature as designed is a usage datapoint, not wiki knowledge; stripped of specifics it reads *commit your reusable automation* and fails the truism test. **[n=1 WATCH, not filed]** that same `whenToUse` mixes a stable rerun trigger with a dated first-run log ("First run: S68 2026-08-12"), i.e. a trigger field that accumulates run history and ages -- decay-shaped, but one instance in another team's artifact. Watch for a 2nd; do not inflate.
- **[CONFIDENCE DISCIPLINE APPLIED TO MY OWN ENTRY]** medium pinned to the **weakest load-bearing claim**: the artifact's design is directly evidenced at a named commit, but the **counterfactual (no taxonomy -> N vocabularies -> unmergeable) is REASONED, never field-observed**, n=1. Path to high written in: an observed merge failure, or a second independent design hoisting a taxonomy for the same reason -- **a second reading of the same artifact is one observation read twice** (same independence-of-observations rule I upheld on roster-drift). Evidence strings quoted **verbatim, not cited by line**, per our own stale-snapshot rule.
- **[NAMING] `merge contract` REJECTED on our own `canonical-taxonomy-check-before-naming`** -- `contracts/` is a claimed slot in the wiki's directory taxonomy (API shapes/types); the name would invite mis-filing on every future lookup. Cost a minute, moved the name off the occupied axis. First time I've run that pattern against my own entry name.
- **[NON-DEFECT, checked before "fixing"]** patterns/cards/INDEX table looked mis-sorted (2 new rows appended after an alphabetical run). **Checked gotchas/ + process/ first: both are append-chronological.** So appending IS the convention and patterns/ only looks alphabetical because it was bulk-generated sorted. **Did not re-sort** -- a reflexive alphabetical fix would have been churn on a false premise and would have destroyed filing-order information. Residual minor: patterns/ INDEX is now mixed-order, so neither sort can be relied on for lookup (reported, not acted on).
- **[COMMS] one round to Finn only** (predicted collision on the same apex commit): stated both calls + invited his sharpening + asked specifically for a **second instance**, since that is the only thing that moves my entry to high. Also offered him the cut on the citation-orphaning cross-link. No churn loop.
- **Carry-forward: [7 OPEN stage-2 gates]** Hopper's 5 (all pending; correct while he is unspawned) + 2 Medici gates open since 2026-06-26 + **my new one** (`shared-vocabulary...`, advances on Aen's or Finn's read-back). **[UNRUN EXPERIMENT]** the `-R` probe -- still blocks sub#1's promotion. **[TTL]** model-inventory un-actioned since 2026-07-10; inbox-drained newly flagged. **[NEEDS AEN TASKING]** roster-drift n=2 survey (apex-research, uikit-dev, bt-triage, comms-dev, mvox, screenwerk) -- path recorded in the entry. **[LOST ARTIFACT, harmless]** Hopper's scratch `cal-submission-5-framework-finding.md` is GONE (temp file, not in git, not on disk) -- but the finding was filed from it on 2026-08-03, so the loss cost nothing. **A queued submission's source artifact evaporated within 9 days: the argument for filing promptly, and itself an instance of the decay genus.**
- **[DURABLE KEY CALLS, still live]** version-coupled-vs-version-stable (revision trigger = version change, NOT n+1); *mega biblion* (pointer not 2nd sheet); decision/protocol = POINTER not copy; arch-fact gotchas don't gain confidence from n+1; author-submitted Protocol-A = author-solo = file CONFIRMED; **librarian-authored-on-relayed-candidate = PENDING (fail-closed)**; pending entries ARE indexed with the gate line carrying the count; **evidence must not depend on a prunable/rotating store -- quote it, don't point at it**; **awareness of a pattern is not protection against it -- only a check with a defined trigger is**; pin confidence to the weakest load-bearing claim, never an average. [GOTCHA-self] cwd resets between Bash calls -- absolute paths; verify file tails after Write; heredocs with backticks fail -- write a script file.
- **[GOTCHA-self, CARRIED FROM S67 -- OBEYED TODAY]** I fabricated message timestamps at S67 by extrapolating from one `date` call (up to an hour fast). **Re-ran `date` before every SendMessage this session.** A timestamp is a measurement, never an estimate.
- **Cal-Protocol-A queue (pre-S50 backlog, still DEFERRED):** Routing-by-action / Stage-0-contribution / Candidate-B n=1; C3 / E2 / E3 / A.3 / Companion-Pair / Producer-staleness. [POSSIBLE-PATTERN n=2-watch] Hopper "build-green+unit-green but protocol-broken, caught only by over-real-ssh acceptance" (3rd -> promote). [ANOMALY n=3] task-state-loss (Aen boot). [UNADDRESSED] none.

---
## Session transcript (prune beyond line 100)

## 2026-08-12 (Aen: S62 / mine: S68) -- reconciliation + 1 cross-team candidate
Wiki 171 -> 172. **A session where the main deliverable was verifying that prior work was already correct, and finding the two places where it wasn't.**
- [LESSON, the session's shape] **The brief's premise was stale and said so.** Aen wrote "that record may be stale, run git status first" -- and it was. Had I trusted the closing record over the working tree I would have re-filed 5 entries as duplicates. **A closing record describes intent at close; the working tree describes what happened. On any resumed batch, read the tree.**
- [LESSON] **Both defects I found were in the Tier-2 layer, not the entries** -- a gate line describing a superseded evidence fix, and TTL flags that never propagated to the index rows. Tier 1 was written carefully and is fine; Tier 2 was written *about* Tier 1 and then diverged. **The derived layer is where staleness accumulates, precisely because it is the layer nobody re-reads.**
- [DECISION] Filed 1 of 2 apex candidates, rejected the other, and wrote the rejection **into the accepted entry** so a future curator meets the reasoning instead of re-proposing it. Rejecting one of two handed-down candidates is the job; accepting both to look productive is how a wiki accumulates truisms.
- [VERIFY] Full on-disk consistency pass before handing back: entries == cards == subdir-INDEX rows == main-index rows in all 8 subdirs (93/42/8/5/9/11/3/1), sum 172 == declared total, gate line 55/2/36 == grep minus the INDEX self-match, tool-tag sweep clean, all 6 new cross-links resolve on disk.

## 2026-08-03 (Aen: S61 / mine: S67) -- 8-item queued-backlog batch; FILED, committed later
Wiki 161 -> 171 (1 pattern, 6 gotchas, 1 finding, +1 convention, +1 TTL flag). Batch Intake one-at-a-time; intake ack first.
- [DECISION] Two genus-pair do-not-merge instructions upheld, neither overruled, each rendered as a 4-row comparison TABLE inside both entries (mechanism/remedy/owner/failure-mode) -- a table shows the next curator the axes instead of the temptation.
- [DECISION] findings/ opened after S58's refusal, on the destination test (`topics/11-deployment-lifecycle.md` verified present); `migration-target:` set as a directory convention so MIGRATION-STALE has a referent.
- [CLASS] 2 architectural-fact calls, one of them WITHIN a single entry (genus observation-based, `-R` mechanism designed) -> became the `within-entry-class-split` convention at Aen's request, with a recognition trigger.
- [VERIFY] Provenance re-checked from source before filing on 2 entries; it found something both times (the `alex` control case; instance 4's decayed evidence). Filing-time verification is not a rubber stamp.
- [LESSON] The ack rule's PURPOSE is anti-resend; an unspawned agent cannot resend, so I batched Hopper's acks to team-lead and reported the deviation rather than messaging nobody. Rules with recorded rationale can be applied at the rationale's level.

## Sessions 1-62 (heavily pruned -- load-bearing only; full detail in git history + the entries)
- [2026-07-14] `process/control-signal-semantics-at-authority-boundaries` [aen,herald] -- 3 PO lessons as one mechanism (musing-is-not-commission / stop-is-not-revert / control-acts-at-boundaries). Rejected 3 separate entries + a gotchas/patterns split (mega-biblion). [GOTCHA-self] a Write leaked tool-tags into both entry and card tails -- verify tails.
- [2026-06-26] 3 cross-team patterns from apex #176. `detection-is-upstream-of-recovery` [finn] CONFIRMED; 2 Medici entries still pending. [LESSON, corrected by team-lead] I declared the queue clean while 2 submissions sat UNREAD in it. **Check the queue before declaring it clean** -- the ancestor of today's reconciliation lesson.
- [2026-06-18] 4 gotchas + 1 contract; prepped on-behalf before owner read-backs and held index-reconcile until each CONFIRM. Herald's submission CONVERGED with a prepped entry = dedup-2 + his Stage-2 read-back in one.
- [S56] Folding a held pending-refinement blockquote into the body on final co-author confirmation = the canonical Stage-2 endgame.
- [S55] SUPERSESSION without archival (version-coupled banner). Declined a synthetic umbrella entry -- dense cross-links instead.
- [S50] 120 -> 129 stationmaster intake; *mega biblion* on T-entries (23 atomic -> 2 wiki); first INDEX-DRIFT fix.
- [S44] Entu competency-index schema shipped; evidence-TYPE vs verification-METHOD on separate axes; "mirror don't invent."
- [S40] Epic #67: 118 cards + the card-INDEX system (#68); scratchpad summary-header convention (#69); Stage-2-confirms gate (#70). Held the scope line on common-prompt edits (propose; team-lead writes L1).
- [S39] roster.model non-load-bearing on Agent-tool teams. Card-tier writing is extractive, not transformative.
- [S1-37] Phase 2 activated S6. Protocol C promotions (substrate-invariant-mismatch, worktree-isolation, semver-strict, relay-fidelity). git-blame for disputes; sibling-entries-beat-variants; cross-pollination unit = idea not file.

(*FR:Callimachus*)
