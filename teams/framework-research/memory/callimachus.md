# Callimachus Scratchpad (*FR:Callimachus*)

## Summary (lines 1-15 -- always read on startup)
- **Current state (S71, 2026-09-02, 17:39):** wiki **235 entries = 235 cards**. **6 filed + 10 amended.** Gates measured corpus-wide at close: **confirmed 171 / partial 9 / pending 16 / legacy-unaudited 39 = 235.** **Files changed since team-lead's commit `6995304` -- NEEDS RE-COMMIT.** **Four of my own same-session filings were corrected by others or by measurement; that is the session's real output, not the filings.**
- **Active items (S72 carry-forward):**
  - **[PROTOCOL C -> CELES, S72] tier-taxonomy gap in `prompts/hopper.md`.** Two shapes: a fourth class, or an **orthogonal `representation: durable | unrepresented` flag** -- I read the flag as better (missing DIMENSION, not missing tier; a fourth class makes risk and durability compete for one field = no-slot form 4). **A reading, not a decision.** **[OPEN] G3 `resolved`-enum Protocol C.**
  - **[ALL FOUR GATES CLOSED at 17:39] `state-the-match-set`, `live-is-not-discriminating`, `tier-taxonomy` = `confirmed`** (Hopper then Brunel on all three; **Brunel HELD tier-taxonomy on a numeric inconsistency rather than confirming around it** and released only after verifying the corrected figures in the file against their own independent timestamps -- two sources, same numbers). **`teams-substrate-2.1.258` = `confirmed`** (Aen read back row 4). **Only the new genus entry is `pending`.** **[STILL OWED] Volta: 3 S67 candidates UNFILED + 3 read-backs.**
  - **[NEW, from Brunel's read-back] `state-the-match-set` gained instances 5 and 6** -- `ls -l` on a dotfile-only dir printing `total 0`, and a `tail` of a non-existent path. **Both are the *reports a failure that is not there* direction, which the entry ASSERTED IN PROSE AND NEVER EVIDENCED.** Brunel found it by noticing **the remedy table had FIVE instrument classes and the instance table FOUR rows -- two rules with no instance behind them.** No new observation was needed; only the filer counting their own two tables against each other. **Instance 5 recurred inside the RECOVERY PATH for its own first occurrence** -- a defect that survives its own recovery path beats one that merely repeats.
  - **[FILED AT CLOSE, 6th entry] `patterns/an-attribute-that-correlates-is-not-one-that-determines` (`partial`, n=5 / n=4 on attributes alone, 2 agents, 4 substrates).** **My reversal condition on `state-the-match-set` FIRED and I did not notice; Brunel did, declined to relitigate, left the call with me.** **Resolved by checking the corpus FIRST -- the genus was already named AND its promotion trigger pre-written in Aug** (*"promote on a third, in a different substrate"*). **TWO pre-written conditions fired the same day; neither would have if the decliner had simply declined.** Brunel's read-back gave 3 corrections, **two reducing their own credit**: Brunel is the CONSUMER of instance 1's proxy not its supplier; half of *why the proxy convinces* was already theirs from Aug; **the title says *attribute* and instance 5 is a PROCESS EVENT -- marked a sub-shape, count stated both ways.** **[LESSON] Write the reversal condition every time you decline -- it is what let someone else check me.** **[+CORROBORATION 17:50] Read word by word, the Aug condition's term *an attribute* SELECTS: it fires at instance 3 alone (repo location, 3rd substrate), confirms at 4 (a name, 4th), and does NOT fire on 5 (a process event) -- **exactly where I marked the sub-shape today, by different reasoning and without the condition in view.** **Two decisions 5 days apart agree on core-vs-outlier, neither made with the other in view.** **Cheap repeatable check: run a promotion condition's own terms against the instance list and see if they select the filer's set -- a written condition is not just a trigger, it is a CLASSIFIER you can run against your own judgment.** Of the two counts, **four is the one with a written rule behind it.** **The asymmetry that makes it cheap: a decline costs nothing extra to make checkable, because the condition is ONE SENTENCE written while you still hold the reasoning; writing it later means reconstructing why you declined, which is exactly when it does not get written.**
  - **[PROTOCOL C #3 -- OUR OWN SCHEMA, and it is TWO changes not one. Brunel, 17:36 + 17:47.] `stage-2` is INSTANCE 7 of `state-the-match-set`, blind on TWO INDEPENDENT AXES.** **Axis 1:** reads *is this entry true*, answers *did a second agent read it back* (demonstrated by `teams-substrate-2.1.258` = `confirmed` AND `disputed`). **Axis 2: it carries NO VERSION** -- reads *this entry has been read back*, answers *some earlier version was*. **Demonstrated BY THE MATCH-SET ENTRY ABOUT ITSELF: its gate line said closed from 17:24 on a FOUR-instance version while I added instances 5, 6 and 7 over the next 21 minutes.** **My error, named on the entry: I advanced the gate then changed the artifact underneath it -- a build moving a tag underneath a live container, performed on a wiki entry.** **AXIS 2 IS SHARPER: an amendment silently INHERITS a confirmation it never received, and amending is the ordinary life of an entry.** **Brunel PARTLY WITHDREW their own remedy: renaming to `read-back: complete` is exactly as version-blind, and a reader told it was fixed would trust it FURTHER -- worse than now.** **Full remedy = rename AND record what it was confirmed against** (content marker / revision / last-amended stamp at read-back). **Decide both together; a rename fixing only axis 1 is its own defect.** Filed on `process/stage-2-confirms-filing-gate`. **[REMEDY DEMONSTRATED BY HAND 17:52] Brunel confirmed the match-set entry AGAINST A STATED REFERENT -- md5 `818cc1bf...`, 25494 B, with the delta since declared. I verified the hash before recording it. **Axis 2 had recurred on the very section that FILES axis 2, within two minutes** (21987 B at 17:43 -> 25494 at 17:52), so the 17:47 confirmation was stale before it was written down. **[THE REASON IT IS NOT BELT-AND-BRACES, now on the process page] A read-back and an active amendment CANNOT both proceed against one artifact without a referent -- not because either is wrong, but because `confirmed` is a claim about a VERSION and the version is moving.** Concurrent review and amendment is what a working knowledge base looks like, so **without a referent it is unsafe by construction.** **[DECISION-RELEVANT] The referent half costs ONE COMMAND and is INDEPENDENT of the rename** -- it needs no schema change to start working; the rename fixes axis 1 and can follow at its own pace. **[+17:56] Brunel VERIFIED MY DELTA MECHANICALLY** -- searched every contiguous line range for one whose removal reproduces `818cc1bf`, found lines 209-219, hash AND byte count exact. **Ledger holds; verified by a check needing neither of us trusted.** **[HELD, NOT FILED -- n=2, on the gate's process page + card] *A partial fix to a trust signal RAISES trust rather than leaving it.*** Both instances are fixes to THIS gate within one hour, **each incomplete and each caught by the OTHER party, never its author** (rename left axis 2; my referent left its own recursion -- recording a referent changes the artifact). **An incomplete fix is incomplete in the specific way that READS AS COMPLETE.** **NOT filed: n=2 is one artifact examined twice, not two sightings.** **Promotion condition written: a third instance in a trust signal that is NOT this gate** (test-suite green, health-check OK, lint clean, coverage number). **[FRAMING, reusable] I restated Brunel's *a condition that arises* as *what a working knowledge base always looks like* -- situational reads as avoidable and gets DROPPED as overhead; structural does not. When a practice keeps being dropped, check whether it was written situational when it is structural.** **[+17:58, SHARPENED] Brunel gave a NEAR-INSTANCE that FAILS the condition and improves it: the `grep -qF "${CLAUDE_VERSION}"` fixed *install failed silently* and left *a shorter pin passes as a PREFIX* -- different trust signal, same shape, but NEVER SHIPPED (caught pre-dispatch), so no trust was ever raised. **Does not promote; n stays 2.** **It corrects an over-strong clause of mine** -- I wrote *"otherwise its author would have noticed"*, generalising from two cases where the author did not. **THE BOUNDARY IS RELIANCE, NOT COMPETENCE: a partial fix is catchable by its author right up until the moment it is TRUSTED, and essentially invisible afterwards.** So the condition now reads **shipped AND RELIED UPON**, and the remedy has a deadline: **enumerate what a fix leaves open BEFORE anyone depends on it.**
  - **[DISPUTED, BLOCKS CITATION] `roster-drift-from-reference-capability-register` is `status: disputed`.** Do NOT cite the `config.json` `model` field for anything until a probe settles **which path writes a full version pin** there. Snapshot won't do it.
  - **[NOT YET AN ENTRY] The courier daemon never logs its resolved `inboxes_dir`; its "courier up" line prints the config `team` field, masking the slug** (named in the rewritten startup.md Step 3.5). **Pairs with my courier gotcha, invisible for the same reason.** File S72.
  - **[TRACKED SWEEP, S72] PRONOUNS. Convention: they/them absent a stated preference; NEVER infer from a name.** Now stated in `wiki/CLAUDE.md`. **Brunel found two entries referring to the SAME agent with different pronouns, same corpus, same day** -- an internal attribution-consistency defect, not a disagreement with a reader. **Fixed: today's 8 files + both files Brunel named. Remaining, measured at close: 140 files, 771 occurrences.** **Do NOT standardise on the frequent form -- the frequent form IS the guess.** Some are legitimate (Callimachus of Cyrene, quoted material); each must be read, not pattern-replaced.
  - **[HELD BY RULING] card `related:` `../<subdir>/` paths resolve wrong from `cards/`** -- corpus-consistent, body links fine. **Normalise on touch, NO sweep.**
- **Key decisions:** **S69 courier silent-loss claim FALSE**, settled from source; the latent-robustness warning is the durable half. **Submission-6 CLOSED** (filed 08-28). **Brunel's genus page DECLINED** on their own disjoint-remedy test, both halves folded into `state-the-match-set`; reversal condition recorded (an instance whose subject is **not a checking instrument at all**). **`live-is-not-discriminating` kept OUT of `verification-narrower-than-it-appears` against the submitter's placement** -- the target was CORRECT, so it is discriminating-power-on-a-right-target; cross-referenced there as instance 7.
- **[THE LESSON, and it is mine] FOUR claims I filed today were wrong in the same direction: I read an artifact's shape as the substrate's answer.** Repo location read as authorship (correlation flag); a minutes-old snapshot read as a durable property (ownership claim); **`jq` output read as substrate content (`null` vs an ABSENT key)**; **a name read as a pronoun.** **The third is the genus this session catalogued, committed by the curator inside the entries cataloguing it; the fourth is the same move on a person.** **Measure before amending, and never let the instrument's output -- or the label -- stand in for the thing.**
- **[FIGURE FIX, Brunel's read-back] tier-taxonomy quoted 11 minutes in one sentence and ~40 in another; Brunel withheld confirmation until resolved.** From the ops log: **in-place 16:17 -> durable image 16:28 (+11) -> ADOPTED at recreate 16:44 (+27).** Neither figure was right for the claim -- **11 is the distance to state 2, 27 to state 3, 40 was loose.** **THREE states, not two**, and the 16-minute gap between 2 and 3 is a window in which every reasonable summary is wrong in a different way. Resolved against the log, not by picking one.
- **Carry-forward:**
  - **[WARNING, still true] submissions >> Protocol B queries** (S71: 6 submissions in, 0 queries).
  - **[DEFERRED, PO go] (A) WikiProvenance reconciliation (RECUSED on confidence half); (B) `discoveryOrder`; (C) `negative-probe` restructure (recused).**
  - **[UNADDRESSED] apex `Dockerfile.apex` fix is deferred at Brunel's call and lives in the apex repo, not ours.**

---
## Session transcript (prune beyond line 100)

## 2026-09-02 (S71) -- 5 filed, 5 amended, queue cleared

- **[FILED 1] `patterns/state-the-match-set-before-trusting-the-instrument`** (hopper, `pending`). An instrument's MATCH SET is the states producing a verdict; wider than the question -> a clean answer to a question it did not ask. **Rule: confirm BOTH pass and null are reachable on the substrate it runs against.** n=4, one day: self-matching `pgrep -af`; merged auth+transport grep count 86; a version gate that cannot pass for ANY image; **`Config.Image` -- a DATA FIELD, so the genus is not confined to commands you invoke.** **The CONTROL is the general form** (answers 1, 3, 4). Process-check rule: EXCLUDE THE CHECKER.
- **[FILED 2] `patterns/live-is-not-the-same-as-discriminating`** (brunel principle + hopper measurement, `pending`). **Freshness and evidential value are independent.** Test: *what would this field read in the case I am ruling out?* `Config.Image` is a creation-time STRING, so it reads the same in the unsafe case; `.Image` is the discriminator, and the two diverged live. **The conclusion survived, the reasoning did not -- a withdrawn leg is not a refuted conclusion.** **Bites us directly: `last-verified` is a FRESHNESS field; re-running a non-discriminating check yields a fresh timestamp over an unchanged non-answer.**
- **[FILED 3] `contracts/tier-taxonomy-has-no-slot-for-undeclared-in-place-change`** (hopper + brunel, `pending`). Not R/M/D by their own definitions. **The taxonomy's own asymmetry heuristic fires correctly and recommends the WRONG escalation.** Distinguishing property is **durability, not risk-at-execution.** Cost is in the **reporting**, not the execution: clears on Tier M sanction, **must never be reported as done** until the durable path lands. **No-slot form 12**, registered in the hub; sibling of form 11 (both authorization-schema gaps).
- **[FILED 4] `gotchas/courier-no-ack-on-vanished-inbox-dir-is-incidental-not-designed`** (me + aen, `confirmed`). **S69 claim REFUTED from source.** `inject_tmp_dir` = `state_dir/inject-tmp`, NOT under `inboxes_dir`, so nothing on the inject path recreates the parent; `open(path,"x")` raises `FileNotFoundError`; **the loop catches only `FileExistsError` (so `raise InjectError` is never reached) and the caller only `InjectError`** -> escapes before `cmd_ack`; `run_once`'s "never let one bad cycle kill the loop" keeps the daemon alive. **DURABLE HALF: the safety is unprotected** -- adding `target.parent.mkdir(...)` (the obvious defensive fix) would CREATE the failure mode. Log/blast-radius both differ from the designed path.
- **[FILED 5] `references/teams-substrate-2.1.258-implicit-teams`** (me rows 1-3, aen row 4, `pending`). Version-POINTER per retire-and-forward; 4 rows measured. **The NOT-re-measured table is the load-bearing half** -- `inboxes/` lazy was NOT MEASURABLE (restore wrote 46 files before members joined), drain never watched, cold-start unobserved. **GC-split row recorded as a census that cannot attribute kill type**, precisely so it is not later read as a measurement. TTL 2026-11-30, aligned with its two 2.1.251 siblings so the lineage re-verifies in one pass.
- **[AMENDED] `gotchas/trailing-pipe-...`** dedup outcome 2: instances 4+5 = `Dockerfile.apex` #16 and #10. **Discharges the entry's own frequency caveat on both counts at once -- different repo, file no team member authored, second observer.** Confidence unchanged (`high`); n+1 on a shell fact moves frequency, not confidence.
- **[AMENDED] `patterns/roster-drift-...`** dedup outcome 2: family-only `model` re-confirmed at 2.1.258, `fable` now in the set. **I caught my own framing error before filing** -- the `null`-for-team-lead half was already on file at 2.1.235 in `dual-team-dir-ambiguity`; what is new is the CONTRAST (null lead / populated members) and the 3-version bracket.
- **[AMENDED] `gotchas/dual-team-dir-ambiguity`** items 1+3 re-confirmed at 2.1.258. **Both defects have survived three CLI versions unfixed = a fact about OWNERSHIP, not the substrate; neither is version-coupled, so no version change retires them.** Volta's.
- **[AMENDED] `gotchas/command-v-multi-operand-...`** submission-6 identification recorded + ops-log timing correction + the opposite-direction sibling table.
- **[GOTCHA-self, FOUND WHILE INDEXING] card drift, and worse than the entry drift.** `dual-team-dir-ambiguity`'s CARD never absorbed the 2026-08-19 additions and its `last-verified` sat at **2026-04-13 while the entry read 2026-08-19 -- four months behind the page it summarises, in the layer Protocol B surfaces FIRST.** Its index.md row was equally behind. Both repaired. **The entry was maintained; the two derived layers were not.**
- **[GOTCHA-self] index.md Card Tier per-subdir column was stale in EVERY row, summing to 193 against a total line reading 229 -- the correct number.** **The aggregate stayed right because it is the number people QUOTE; the breakdown rotted because it is the number people SKIM.** Re-measured: 234/234. Gate lines in `patterns/` and `gotchas/` cards INDEX were stale too and did not sum; re-measured and appended rather than rewritten.
- **[LEARNED] Ask the index before scanning the corpus -- applied, and it worked.** Delegated the dedup sweep over 229 entries to a subagent reading `cards/INDEX.md` first. It returned two DIRECT matches I would have filed as new entries (pipe-masking, model-pin) and one already-covered sub-item (`$PPID`). **Three of seven candidates did not need an entry; that is the sieve doing its job.**
- **[GOTCHA-self, THE SHARPEST OF THE SESSION -- I filed a claim that was FALSE, and the submitter corrected it against their own interest.]** On `trailing-pipe`, I wrote that the two apex instances **discharged the entry's correlation flag** "on both counts at once", asserting `Dockerfile.apex` was authored outside the team. **Both lines are Brunel's.** Worse: **the apex line PREDATES the joosep one, so instance 3 was probably copied from it** -- which makes the correlation stronger, evidencing **propagation, not independent frequency.** Brunel wrote *"please do not let me weaken it."* **The flag is reopened, my error left visible.** **I over-read a correlation flag while maintaining it, in the direction that made my own filing look better.** I assumed authorship from repo location and never checked.
- **[GOTCHA-self, SECOND] My `dual-team-dir` amendment asserted a DURABLE property from a minutes-old snapshot -- "no version change will retire them" -- and it was FALSE within the hour.** startup.md was rewritten mid-session: **items 1, 2 and 3 all CLOSED** (Step 0.5 no longer prescribes the unperformable check; Step 3.5 passes `-SessionPid`; `restore-inboxes.sh:44` honours `FR_COURIER_SESSION_PID` with `$PPID` only as fallback). Only item 4, the skills, is open. **`stale-snapshot-trusted-as-current` committed inside a note about ownership** -- and **a claim about WHY something has not been fixed decays fastest of all, because it is a claim about someone else's queue.** Verified against the files before correcting, not against the claim.
- **[LEARNED] Both submitters improved on my filings, and both corrections came from the producer side.** Hopper flagged a missing provenance as **THEIR** error (buried it *"where it read well, not where a filer would need it"*); Brunel corrected a flag in the direction that cost them. **The read-back is not a formality -- it caught two real defects in one window.**
- **[LEARNED] Heredoc/quoting: `python -c '...'` dies on an apostrophe; `python - <<'EOF'` dies inside a `&&` chain.** Write the script file. **Also: my own cards mix `--` and em-dash `—`, so any scripted string match must try both.** Fourth quoting cost this session -- write the file FIRST.

## 2026-08-31 (S68) -- pruned to the calls that still govern
- **DECAYED vs FALSE-AT-AUTHORING:** a stale claim ARRIVING feels like a false claim BEING MADE; re-deriving on read protects CONTENT, never CLASSIFICATION -- needs an instrument, not attention. Watch stays at FOUR.
- **Rules that fire are NARROWER, MORE MECHANICAL ones attached to an ACTION, not an INTENTION.**
- **Protocol A has no durable store: reading deletes the message, exiting deletes the mailbox.**
- **entry -> card -> INDEX row -> index.md, THEN the next entry.** Held all session; zero drift introduced.
- **Before reporting an edit as done, grep the artifact. Test the whole candidate space before "neither wins".**
- **A header is a CLAIM ABOUT a state and decays like any derived layer** -- every count carries its membership rule inline.

## S67 and earlier -- pruned; the entries carry the detail
- **DISJOINT-REMEDY TEST** standing (+3 boundary conditions). **A datapoint may not file without a `ttl`.** **`confidence` interim rule REVOKED; I am RECUSED** on that half.
- *presence is self-certifying, absence is not.* **`confirmed` certifies a PROCEDURE, not CORRECTNESS.** **`stage-2` = CARDS-ONLY.** **A scratchpad summary is a CLAIM ABOUT A SOURCE, never the source.**
- **[DURABLE]** version-coupled vs version-stable (trigger = version change, NOT n+1); pointer, never a 2nd sheet; arch-fact entries gain nothing from n+1; author-submitted = confirmed at filing, librarian-on-relayed = PENDING; pin confidence to the weakest load-bearing claim; **run `date` before EVERY send**; cwd resets between Bash calls -- absolute paths; verify after every scripted edit.
- **[SESSION-NUMBERING FORK]** Aen per continuous session, Hopper and I per working day. **Dates are canonical.**

(*FR:Callimachus*)
