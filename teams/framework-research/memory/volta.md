---

# Volta scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S67 (2026-08-31) **CLOSED clean.** Group 2 lifecycle SEMANTICS for the TTL batch re-verification, live CLI **2.1.251** vs entries pinned 2.1.178-181. Hopper ran probes, I judged semantics. Both deliverables DONE, all 6 entries verdicted, all 10 observations answered. Two Protocol A submissions ruled and queued with Cal; **filing HELD on a PO instruction -- do not chase.**
- **Active items:** **G2 FULLY ANSWERED.** One URGENT open question left by the O10 result: **is `asyncRewake` (exit-2) bounded by the block cap, or ungoverned like the exit-0 injection route?** The entry recommends it *for safety* on the grounds that it escapes the budget -- if it is also unbounded that recommendation is inverted. **Aen ruled it S68-shaped, NOT tested today** (the incident spent the runaway-risk budget in the PO's terminal). **Test preconditions -- Hopper's three, endorsed, routed to Aen for the boot note because he stated them to ME not him:** a dedicated throwaway WE own (never a human's terminal), a hard iteration counter INSIDE the hook, a kill **faster than a config edit** -- plus the `stop_hook_active` guard my scope clause mandates. **AMENDED 11:46 on Hopper's measurement: his hook-config removal took 2-3 FURTHER TURNS to take effect, so a config-edit kill is NOT immediate.** Consequence beyond the spec: **the in-hook counter is not redundant with the external kill -- it is the ONLY control that acts within one turn**, so it is the PRIMARY stop and the kill is the backstop (inverse of how I first stated it). Same correction applies to the design gate: hook-based inbound carries its own counter not merely because the harness ceiling is absent on the measured path, but because **nothing outside the hook can stop it promptly.** A dispatch saying only "test whether asyncRewake is bounded" invites exactly the run that caused the incident. Brunel takes the wake-amendment read-back at render; Hopper takes the classifier-entry read-back.
- **Key decisions this session:** Ruling A retire-and-forward (one lineage, one TTL, on the newest datapoint) -- Cal BOUND, and met my condition with an audit-tool invariant rather than a promise. Ruling B `status` is about the ENTRY not the DEFECT (general half retracted: the "no longer applicable" gap is real, the NAME `resolved` is wrong). Ruling C O8 staging DECLINED (resolver safe either way). Declined O8 but REQUESTED the wake rig -- prose vs delivery path.
- **Carry-forward:** **[UNFILED, hold]** 3 Protocol-A candidates below (stated-guarantee pattern, slug-width gotcha, evidence-window boundary). **[DEFERRED]** OQ10 sweep -- see the hard precondition below, it must land BEFORE the sweep exists. OQ11 partly discharged; team-dir-ABSENT cell + pane-latency still open. Bug C at-scale RfC. 6 untasked S63/S65 items in Aen's scratchpad (out of scope unless re-tasked). **[WARNING]** version-drift tripwire proposed, NOT started.

---
## Session transcript (prune beyond line 100)

## S67 -- Group 2 TTL semantics (2026-08-31) [COMPLETE -- all 10 observations answered]

### Per-entry verdicts (the deliverable)
1. **anchor `teams-substrate-2.1.179-implicit-teams` = MIXED.** Name-on-disk CONFIRMED for interactive (8-hex truncation, `session-` prefix, team_name still ignored on disk) but **REFUTED for `--bg`**; config-eager CONFIRMED; inboxes-lazy CONFIRMED (live witness: `session-2aedf13c` = ALIVE pid 32168, config only); **inbox-shape REVISED** (+`msgV`,`msg_id`); **external-write wake REFUTED, scoped**; **members[]-injection REFUTED**. A 2.1.251 datapoint, never an in-place TTL bump.
2. **`claude-code-hook-wake-primitives` = ALL SIX CONFIRMED + a NEW SAFETY DEFECT (verdict reopened 11:37).** Hopper closed a/b/c: `Stop` fires content-agnostically every turn-end; `additionalContext` injects AND **drives a new turn**; `stop_hook_active` correct (false first, true after). **So the fallback for courier inbound EXISTS.** **But he ran 14 consecutive self-driven continuations with NO cap override** -- the `?? 8` counts **blocks**, his hook exited 0, never blocked, counter never incremented. **`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` does NOT bound `additionalContext`-driven self-wake.** That is a **scope defect across three sections**: the entry presents injection as a wake substrate, presents the cap as its rail, then says "budget your wakes against this cap" -- **the rail does not guard the substrate the entry recommends.** So my restamp needs the clause stated as SCOPE (*the cap governs blocking Stop hooks only; the injection route is ungoverned*), not as a footnote to the number -- **correcting 9->8 without it makes the entry MORE dangerous**, a precise number lending authority to an absent rail. **Second note is possibly worse and I flagged it:** *"Prefer `asyncRewake:true` … does not consume the consecutive-block budget"* is a SAFETY recommendation whose stated reason is the route's ESCAPE from the bound. Its schema wakes on **exit 2 (blocking error)** so it may be counted, unlike Hopper's exit-0 route -- **URGENT OPEN: is the one officially-recommended background wake path bounded?** One hook, one loop-or-not. Hopper's exit-0 result does NOT transfer and someone will assume it does. **Design consequence (mine): if hook-based inbound is ever built, the ceiling must be OURS** -- a counter in our own hook, checked and yielded on -- same shape as the surfacing self-test, a guarantee we assumed the platform provided and it does not. Told Aen not to let that design start before the scope clause lands. **Adopted Hopper's split:** 14 continuations = measured; counter-never-incremented = his inference from a constant, testable (blocking hook alongside non-blocking), untested.
2b. **Wake row now DEFINITIVE:** his watcher spanned the whole episode -- entry written 11:32:14, untouched until the team dir was GC'd 11:34:23, **surviving 15 completed turns** (not delivered, not read-flagged, not rewritten). With n=3 sessions showing no proactive wake, the bare-session refutation is as strong as this substrate allows.
2c. **[FRAMING offered to Hopper, connects to form 11]** He told Aen the hook was *"non-blocking by design, cannot trap the session in a loop"* -- **a claim about the MECHANISM offered as a claim about the CONSEQUENCE.** "Does not use the blocking path" and "cannot run away" are different assertions; the first does not carry the second. Same two-questions-answered-as-one shape as authorization-vs-executability. Sharper than "knew the rule and ignored it", and more useful: the failure was **a substitution that felt like a derivation**, not forgetfulness.
2d. **The morning cap ruling STILL STANDS alongside the new scope defect -- two defects, not one.** `stop_hook_active` survives; `asyncRewake` schema matches exactly. **Cap: the DIGIT is not the defect, the AMBIGUITY is** -- shipped `?? 8` + strict `>` means 8 permitted, override on the 9th; the entry says "caps at 9" AND "nine is the ceiling", which take opposite sides. Restamp the MECHANISM. Harm: an agent budgeting nine gets cut at eight.
3. **`no-teamdelete-stale-session-dirs-accumulate` = REVISED (narrowed).** O9a settled at MECHANISM level by Hopper: `TeamCreate`/`TeamDelete` are name-literals in one Set with no schema/interface/handler -- not a hidden tool, a name that is not a tool. Dirs accumulate from **ungraceful exits only**. My earlier CONFIRMED was too shallow and I downgraded it myself.
4. **`cold-start-discovery-false-negative` = CONFIRMED (ordering) -- but DO NOT restamp the width.** Hopper's ~3.2s is a `--bg` measurement; the entry's ~10-25s is interactive-reaching-interactive-ready. Shortening the guidance would reintroduce the S57 false halt. Ordering confirmed both kinds, interactive width UNMEASURED.
5. **`sessions-pid-json-not-gc` = REVISED (half-refuted): graceful REMOVED, hard-kill LINGERS `status:"idle"`.**
6. **`courier-restart-needs-inboxes-dir` = FINAL: hold `active`** + bump last-verified + Amendments closure. Both S58 fixes verified present (`startup.md:162,164`; `stationmaster-courier.py:1203`) and O4 confirms the gap persists, so they stay load-bearing.

### The sharpest ruling of the batch
**A half-refutation of the GC gotcha makes its warning STRONGER, not weaker.** The old substrate was honestly hostile (registry ALWAYS wrong -> the process-liveness check was obviously load-bearing). The new one is **intermittently correct** -- right after every graceful exit, wrong after every crash. Worse trap: anyone validating a liveness check by exiting cleanly sees the registry behave perfectly, concludes the process check is redundant, and loses in production where sessions die badly. The Casualties section MUST carry this or the entry reads as a relaxation.

### Hard precondition for the OQ10 sweep -- must land BEFORE the sweep exists
Measured 11:15: **12 team dirs, 5 registry entries, only 3 match** -- all three live INTERACTIVE. The two live `kind:"bg"` sessions match NOTHING (their registry `sessionId` is the jobId; the dir is named from `leadSessionId`). Nine dirs unmatched; **if a live bg session owns one, a sweep deletes a live dir, and the linkage to tell does not exist.** So the rule is NOT "handle bg correctly" but **"the sweep may not run on a host with live bg sessions it cannot account for"** -- no care inside the predicate recovers information the substrate never wrote. FR's own path is sound (our subagents are `in-process`, produce no registry entry; the parent matches). Brunel: *"the hazard is never the function; it is the exposure"* -- same predicate fails LOUD for today's caller (`resolve_team_dir` never guesses) and deletes SILENTLY for the proposed one. Four copies exist (po-team sagres + shipyard, probe-harness staging, FR `:1136`).

### Wake row -- refuted, scoped, and it resolves a standing contradiction
Bare idle lone interactive session with a team dir present: **NOT delivered at 190s**, `status` never left `idle`. Active multi-member: **drained <50ms**. So P6's own claim is refuted on P6's own cell. That **eliminates resolution (b)** of `session-wake-on-inbox-write-two-unstamped-claims-contradict` (Brunel's different-cells reading) and **confirms (a)**: substrate moved, P6 true at 2.1.179, false at 2.1.251. Entry goes medium/untested -> resolved/version-bracketed. Two live-doc casualties: `HOW-TO-REPRODUCE.md` + the "courier must create the file itself" caveat are now broken guidance; and **my own startup.md** has a window where Step 3.5 runs before Step 4 spawns, leaving team-lead a bare lone session -- **NOT proposing a reorder** (window small, mail not lost, another reorder carries its own risk after S58's); document it.

### Protocol A -- 2 sent, both ruled, filing HELD
- **11:08 wake-contradiction resolution** + **11:16 corrections** from Brunel's read-back: (i) the cell **RESOLUTION** is eliminated, the cell **MECHANISM** is not; (ii) his better closure reason -- **2.1.179 is GONE, so the mechanism is permanently unfalsifiable at the version where it mattered**; (iii) **his best finding, credited to him**: the entry's own Rule names *"team size"* as a cell axis and the whole analysis above it used team-dir presence, with the three data points falling exactly on the unread axis -- `holding-a-measurement-is-not-having-applied-it`; (iv) the stamped/unstamped clarification. Confidence SPLIT BY CLAIM (resolution `high`, mechanism `speculative`) -- Cal honoured it and said I should not have had to ask.
- **11:13 classifier envelope** -- **authorization and executability are SEPARATE AXES and our planning artifacts have a slot for only one.** Cal ruled it **no-slot FORM 11, outside the umbrella**. My sharpening, which he is adding: the friction-misreading **inverts** the entry -- both instances are an agent authorised, blocked, and correctly declining to route around, so a friction reading turns a record of two refusals to launder into a manual for laundering. Corollary (Aen's): mark classifier-exposed steps as PO-approval AT PLANNING TIME. Scope caution I insisted on: n=2, one session, one CLI, one mode -- instances solid, MECHANISM unexamined.

### Protocol A #3 SENT to Cal 11:39 (routed at Aen's instruction) -- mechanism-offered-as-consequence
**A property of the MECHANISM offered as a property of the OUTCOME.** Hopper assured Aen the Stop hook was *"non-blocking by design, cannot trap the session in a loop"*; it then drove 14 self-wakes. *"Does not use the blocking path"* is true; *"cannot run away"* is what it was offered as; **the first does not carry the second.** Worth an entry because it is **a substitution that FEELS like a derivation** -- nothing announces itself as a leap, so "he knew the rule and ignored it" is the wrong account (the guidance he'd quoted 4h earlier would have caught it; the substitution is what stopped him applying it). **Two candidate family placements given, ruling left to Cal:** (1) beside form 11 -- both two-questions-answered-as-one, **but I doubt it**: form 11's signature is an ARTIFACT with no field, here the collapse is in a SPEAKER'S reasoning with no artifact; folding them recreates the one-token-many-remedies defect Cal refused on the umbrella. (2) **Better fit, and I weighted it:** with Hopper's own *"an eliminated confound is not an identified cause"* -- both are **ruling out the known failure felt like establishing there is none**; n=2 on a pattern whose author supplied both instances by walking into it. Provenance flagged: incident + concession are Hopper's, framing is mine, he is the natural read-back, and **the entry must not read as a finding levelled at him from outside.**

### Three Protocol-A candidates NOT yet filed (hold)
1. **PATTERN "convert a stated guarantee into a checked precondition"** (n=3): S58 `validate_startup` self-mkdir; the courier's surfacing contract at `:765` (asserted once, never checked -- drift found only because a TTL batch happened to run; fix = startup self-test that round-trips one entry and confirms SURFACED not merely drained); Cal's TTL filing rule made an audit-tool invariant. **Canonical form:** when you write "X must always hold", ask what fails if X quietly stops holding -- if the answer is "nothing, until much later, by accident", it needs an instrument, not stronger wording.
2. **GOTCHA slug-width hardcoded twice** (`:1122` `discover_by_session_pid`, `:1136` `_has_live_session`); a width change fails SILENTLY on a clean box because `resolve_team_dir` (`:1163-1170`) returns the lone candidate and only WARNS on mismatch. "Works in testing, fails on hosts with stale dirs." Not broken today (width still 8).
3. **METHOD BOUNDARY on the born-wrong audit: it only works where the ORIGINAL EVIDENCE survives.** Two instances, reached independently: the hook-cap value needs a vanished 2.1.179 binary; the cell mechanism needs a vanished substrate version. `color` was classifiable only because our persisted inboxes survived from March with dates. File with the two pointing at each other.

### Retractions and misses -- mine
- **RETRACTED my O2 bg caveat.** I told Aen the live bg sessions had registry entries and no team dir, so config-eager might be `kind`-scoped. **False** -- I looked under the REGISTRY-derived name and concluded absence, but bg dirs are named from `leadSessionId`. **I hit the jobId bug myself 4h before Hopper named it**, and mine is the encounter that produced a false SUBSTRATE CLAIM rather than a blocked action: the bug's real damage is the plausible wrong conclusion, not the failed lookup.
- **I specced O6 without a Protocol B query** against a corpus that already held a stage-2-confirmed answer (`precondition-without-an-owner-is-no-precondition`, 08-27). Cost: a blocked write, rig time, Hopper's time. **Aen adopted the corrective as a standing rule** -- a probe plan's precondition list includes a Protocol B query. Hopper rightly refused my sole-attribution: he had his own S66 rule and it did not fire either.
- **[LEARNED] Population inference is not controlled observation** -- I inferred GC-on-exit from a dirs-vs-entries count and Hopper's headless finding punched a hole in it; the inference happened to be right and the lesson holds anyway.
- **[LEARNED] The entry we skipped describes exactly why we skipped it.** He had a scratchpad rule, I had a role-prompt instruction; both existed, neither fired, and the entry documents ownerless triggers. Near-controlled demonstration of its own claim, and the argument for a STEP over better intentions. Instances 3+4 for its confidence, Cal's call.
- **[LEARNED, and this file was an instance] A summary is a claim about a source, never the source** -- n=4 today, and only Cal's check fired (he caught his own scratchpad holding a pre-reversal ruling). My own summary header had grown to 45 bullets against a 15-line rule before I restructured it at 11:20.
- **Hopper's formulation, better than mine, use verbatim: "An eliminated confound is not an identified cause."**
- **[CAUGHT MY OWN FIX REPEATING THE FAILURE -- sent to Aen 11:23, last act]** I proposed and Aen ADOPTED a checkpoint-time size check for the scratchpad header. **It is a self-check by the owner on the owner's own artifact -- structurally identical to Hopper's scratchpad rule, my role-prompt instruction, and Cal's carried note, all three of which failed today.** It would file as the day's FIFTH person-attached rule while being sold as the corrective for the first four. Exposed by **Cal's separated observation (his, credit him): the scratchpad is the one artifact nobody audits, because auditing it is the owner's job and the owner is busy auditing everything else.** He found a live SIXTH instance in his own header -- four invented fields, `Active items` + `Key decisions` missing, `Current state` stale at wiki 214 vs 218 in tree, and **fourteen queued submissions with no representation at all**; worse than mine because mine was misshapen and his was wrong on a fact. **Recommendation sent: keep the checkpoint MOMENT (shutdown is too late by construction), move the CHECKER** -- an audit-tool field check (the move Cal made converting my TTL condition into an invariant) and/or cross-audit via Aen's existing startup header-read. Same observability argument I'd made to Cal an hour earlier and then violated: a person-attached rule fails silently and is uncountable; a step- or instrument-attached one either runs or visibly does not.

(*FR:Volta*)

---

## S58 -- Bug A reorder + Bug C residue + Herald sentence (2026-06-18) [condensed]

Booted 2.1.181/Windows; S57's halt was a cold-start false negative. Bug A root cause: `validate_startup` self-created state/spool/inject but NOT inboxes -- the lone non-self-created precondition. BOTH fixes landed split by file-ownership: (a) I reordered startup.md courier Step 2.5 -> **Step 3.5** (after Step 3 restore) + cross-refs + topics/06; (b) Brunel self-mkdir'd in `validate_startup`, I co-signed. Bug C residue amended into a CLI-version-SPLIT note. Herald's cold-start sentence folded verbatim into `startup.md:136`.

[LEARNED] **Single-writer discipline + flag-before-edit** -- held the file until Herald's sentence arrived, applied in ONE pass, surfaced the overlap as Aen's call. [LEARNED] **Validator asymmetry is a smell** -- the reorder alone would have left the latent bug for out-of-runbook launches; the class fix is making the validator consistent.

---

## S56/S55 -- migration cutover + WS2 lifecycle rework (2026-06-18) [condensed; work on main @309dcd8]

S56 = CUTOVER, verify-only, all PASS. WS2 cut over on main @309dcd8; CLI unpinned 2.1.177->2.1.181. S55: applied Herald's WS2 design to 4 files (implicit-teams block, Step 0.5, Step 2'->Discover, Step 2.5 courier-restart, S5 DELETED 5->4 phase; restore/persist-inboxes.sh runtime-discover, fail-closed). MULTI-DIR is the NORM -> always pass a disambiguator. Halted by PO at commit gate for over-churn; committed S56.

(*FR:Volta*)
