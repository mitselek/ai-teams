# Celes — Scratchpad

*Agent Resources Manager, framework-research team*

[GOTCHA] Scratchpad path is `teams/framework-research/memory/celes.md` — NOT `memory/celes.md` at repo root.

---

## Domain Knowledge (stable)

[PATTERN] Almost everything is behavioral/prompt-enforced, not infrastructure. Real incidents drive design.
[PATTERN] `spawn_member.sh` > Agent tool for model-tier correctness.
[PATTERN] Coordination boundary tables (section ownership + handshake protocol) are the key pattern for agents sharing adjacent domains.
[PATTERN] Model tier driven by **consequence of error**, not task complexity. Opus when no automated quality gate; sonnet when tests catch errors.
[PATTERN] Three team archetypes: Research, Development, Hybrid — determined by primary output type.
[PATTERN] Six canonical agent roles: Coordinator, Researcher, Analyst, Developer, Spec Writer, Design Specialist.
[PATTERN] Data flow architecture (pipeline vs independent-output) determines isolation model. Pipeline → trunk + directory ownership. Independent → branch/worktree safe.
[PATTERN] "Agent PO" is an anti-pattern — PO is always the human. Agent should be "Requirements Analyst" with explicit escalation rules.
[GOTCHA] Count **characters**, not roles-plus-characters. Team-lead IS one of the characters. "5-character team" not "team-lead + 5 agents = 6."

## Hires Delivered (cumulative — 53 agents)

Full table in git history (commit `74b2e05`). Teams: FR(8+TL), CD(5+TL), AR(5+TL), PD(7+TL), ER(4+TL), BT(3+TL), PEN(5+TL), L1-mgr(1), RK(8+TL), BF(3+TL). Latest: BioForge-dev (4 characters — Humboldt/Merian/Linnaeus/Cuvier, 2026-04-10).

## Patterns from earlier sessions (compressed 2026-04-09)

Detailed session logs for Mar 23, Mar 24, Apr 6, and the Apr 8/9 T09 discussion rounds are in git history and in `topics/09-development-methodology.md` itself. The patterns worth carrying forward:

[PATTERN] When team-lead cannot write code, integration layers belong with the builder whose functions they call. (penrose-dev)
[PATTERN] Pipeline dependencies flow one way; document the direction prominently. (penrose-dev)
[PATTERN] Review checklist for team redeployment: (1) roster.json matches target, (2) common-prompt members list matches, (3) model tiers validated, (4) prompt scope restrictions match deployment env, (5) prompts reflect lessons from prior sessions. (hr-devs redeployment)
[PATTERN] For products with active clients hitting bugs, team must have dual tracks: (1) urgent bugfixes on live system, (2) parallel transition to new system. Work streams, not sequential phases. (screenwerk-dev)
[PATTERN] Advisory specialists (non-TDD, non-coding) are valid when expertise crosses TDD pair boundaries. (screenwerk-dev — Talbot, Melies)
[PATTERN] Four-round discussion protocol works: initial specialist responses → PO response → refinements → specialist pushback → PO new questions → synthesis. Now codified in T09 § "Multi-Round Consensus Protocol".
[PATTERN] When synthesizing multi-round discussions into topic files: preserve disagreements in a Part 4 "Open questions" section, don't smooth over.
[PATTERN] When multiple specialists reach the same recommendation through different reasoning, preserve all three arguments — they validate each other.
[PATTERN] Operational additions from lifecycle/governance specialists often slot in with zero ambiguity. Structural disagreements require binary calls.
[PATTERN] Host capacity is a deployment prerequisite, not a tier dimension. Under-provisioning is a blocker to fix, not a tier to ship. (v2.3 reframing of Brunel's original)
[GOTCHA] Client communication is PO-only — agents draft, Mihkel sends. Must be explicit in common-prompt. (screenwerk-dev)
[GOTCHA] TDD pair is non-negotiable PO constraint — every dev team must have explicit builder+tester pairs.
[GOTCHA] Dropping a role requires updating TWO files: `roster.json` AND `common-prompt.md` members list. Easy to miss the second.
[GOTCHA] When reviewing an existing team for redeployment: check BOTH the design file AND the source-of-truth (live roster) — they may have drifted.
[LEARNED] Being willing to correct a position publicly is more valuable than being right on the first try (Oracle model tier sonnet → opus[1m] after PO's sole-gateway decision).
[LEARNED] My v2 had an implicit "parallelism is a solved problem" assumption. #50 surfaced this. Distinguish "solved" from "deferred" carefully in synthesis work.
[LEARNED] "Multi-pipeline team" vs "multi-pipeline execution" is a synthesis-level clarity axis v2 did not make explicit. Now canonical in T09 v2.1.

## T09 v2.1/v2.2 (#50/#51/#52) — compressed

[CHECKPOINT] v2.1 `264222d` (#50, #52): sequential-first default, max_lookahead → 0, Shared PURPLE deferred behind Future Work gate.
[CHECKPOINT] v2.2 `9ea1e90` (#51): 12 TypeScript interfaces in `types/t09-protocols.ts`, Protocol Typing Principle section, cross-references per protocol block.
[DECISION] `PurpleVerdict` as discriminated union (Accept/Reject/Escalate); `types/` at repo root; principle in T09, not T03.
[PATTERN] Prose + markdown template + TypeScript interface triad for communication protocols. Three forms are redundant on purpose.
[PATTERN] "resolves #N" in commit message does NOT auto-close on direct-to-main commits — only PR merges auto-close. Always `gh issue close` explicitly.
[LEARNED] Update scratchpad IMMEDIATELY after every significant commit. The crash between v2.1 commit and scratchpad update lost all reasoning.

## T09 v2.3 (#49) — compressed

[CHECKPOINT] v2.3 `9b8d0a3` (#49): cost framing removed, quality as the only axis. Degraded Cathedral → "Host Capacity as Deployment Prerequisite". Cost column dropped from PURPLE config table. Brunel's cost bullet removed from Shared vs. Separate PURPLE convergence.
[DECISION] Under-provisioned host = deployment blocker, not tier variant. Emergency team-lead-as-PURPLE is a "recorded deficiency" with "fix the hardware, do not normalize the compensation" guidance.
[DECISION] Part 2 (Oracle) cost mentions left untouched — consequence arguments for adoption triggers, not tier decisions.
[PATTERN] Historical changelog entries preserved verbatim even when new framing contradicts them.
[GOTCHA] `gh issue close --comment` swallows the comment if the issue is already closed. Always `gh issue comment` separately as fallback.
[GOTCHA] Shared working tree: always `git add <specific-file>`, never `-a`, when other agents may have uncommitted modifications.

## Callimachus Onboarding — compressed

[CHECKPOINT] Callimachus deployed. 6 deliverables committed. Oracle assessment format (5-part) reusable for future deployments.
[DECISION] Cathedral tier, Incremental Bootstrap, Phase 2 gate after 5 sessions, Medici/Oracle coexistence (L0/L1 vs L2/L3).

## Raamatukoi tugigrupp — compressed

[CHECKPOINT] Full design v2 deployed at `designs/new/raamatukoi-dev/` + live at `Raamatukoi/tugigrupp`. 9 characters, Cathedral tier, dual-hub, dual-pipeline.
[PATTERN] Multi-repo maintenance: one XP pipeline per repo, shared ARCHITECT + Oracle. Zero-test repos: "set up test framework" is a story.
[LEARNED] Three-document system: design-spec (PO rationale), common-prompt (agent rules), tdd-pipeline.md (runtime protocol). Reduce redundancy with pointers.

## Discussion #56 — Single-provider model strategy (2026-04-10)

[CHECKPOINT] Discussion complete. Rounds 1-2 + Cal's synthesis posted and reviewed. No corrections needed from role design perspective.
[DECISION] My position: single-provider for all governance/coordination/XP pipeline roles. Multi-provider only for (1) capability gaps (vision) and (2) fire-and-forget mechanical roles (Eilama-class).
[PATTERN] Five distinct provider lock-in layers: infrastructure (Brunel), protocol (Herald), knowledge (Callimachus), prompt (Celes), governance (Monte). Each has different switching costs. Convergent finding from six independent analyses.
[PATTERN] Two role categories for provider strategy: framework-participating (Claude-only, behavioral compliance required) vs. service roles (provider-agnostic, test/schema verified).
[PATTERN] "Adding a non-Claude model does not mean adding a non-Claude agent." Visual QA is a tool (MCP/HTTP), not an agent.
[GOTCHA] Gemini's synthesis conflated Eilama sidecar pattern (inbox-polling daemon) with multi-modal tool integration (MCP/HTTP callable service). Architecturally different patterns.
[GOTCHA] Audit independence (Monte's argument) is governance-sound but role-design-expensive: requires dual behavioral profiles, dual prompt variants, dual validation.
[GOTCHA] Cross-provider PURPLE review is untested: PURPLE (opus) is calibrated against Claude sonnet coding idioms. Non-Claude GREEN output may trigger false positives/negatives in structural judgment.

## Apex-research Oracle (Eratosthenes) — 2026-04-13

[CHECKPOINT] First Oracle replication. Draft at `designs/deployed/apex-research/teams/apex-research/prompts/eratosthenes.md`. Roster entry handed to team-lead as inline JSON in delivery message (Celes does not edit roster.json).
[DECISION] Name: Eratosthenes of Cyrene — Callimachus's literal successor as Chief Librarian at Alexandria. Sibling-Oracle framing is historically grounded. Nickname: "Erato".
[DECISION] Color: blue. Avoids gold (Callimachus), avoids apex-research palette (yellow/magenta/cyan/green), avoids red (TL convention). agentType: "oracle". Model: claude-opus-4-6[1m].
[PATTERN] Universal Oracle DNA (kept verbatim, modulo team-lead name): dual-hub, four capabilities, Phase 2 gate, Protocol A/B/C + URGENT-KNOWLEDGE, wiki provenance frontmatter, directory sovereignty, Incremental Bootstrap, Medici boundary.
[PATTERN] Team-specific additions: Decisions Boundary (pointers not copies) — new for apex-research because `decisions/adr-NNN-*.md` already exists as authoritative ADR home owned by Hammurabi/Schliemann. Framework-research did not need this because `topics/` is not a parallel-ADR system.
[PATTERN] Research-team wiki subdirs translated: `observations/` (cites topic files) → `cross-app/` (cites inventory/+shared/); `findings/` (pre-topic-file) → `migration-hints/` (pre-spec). `process/` kept universal.
[PATTERN] Dropped XP-pipeline RED/GREEN/PURPLE wiki-read exception — apex-research has no XP pipeline. Their TDD pair is Nightingale/Berners-Lee for dashboard only.
[PATTERN] vjs_apex_apps read-only rule codified for Oracle: provenance may cite vjs_apex_apps paths, but source-of-truth for any APEX source claim is Champollion's extracted output in `inventory/`. Prevents Oracle legitimizing a second view of upstream read-only data.
[GOTCHA] apex-research author attribution is `(*AR:<Name>*)`, not `(*FR:<Name>*)`. Prompt trailer is `(*AR:Celes*)` because I authored the draft for that team.
[CHECKPOINT] 2026-04-13 10:25 — team-lead approved: Eratosthenes lore shipped, opus-4-6[1m] confirmed, roster merge held for Cal's self-reflection (batch revisions, no schedule pressure). Zenodotus fallback no longer needed.
[LEARNED] Team-lead flagged Decisions Boundary clause (#5) and vjs_apex_apps source-of-truth rule (#9) as "would have cost a future [DISPUTE] if missed." Pattern for future Oracle replications: always audit the target team for (a) parallel authoritative artifact homes the wiki might duplicate, and (b) read-only upstream sources the wiki might legitimize a second view of. Add to the Oracle-replication checklist for the next sibling (after Cal's reflection lands).
[WIP] Awaiting Callimachus's self-reflection. On arrival: diff against Eratosthenes draft, port deltas batched, then send merge-ready package back to team-lead.

## Eratosthenes v2 — Framework-wide Librarian rename + Cal integration (2026-04-13 ~10:35)

[DECISION] Framework-wide: Oracle → **Librarian** rename. agentType: "oracle" → "librarian". Triggered by Cal's terminology-collision flag (Oracle Corp / Oracle APEX / the role, three different things at apex-research). Lore stays — Eratosthenes was literally a librarian at Alexandria, the word fits his actual job. This is a framework-wide change, not apex-research-local.
[DECISION] Phase 2 gate: volume-based, not session-based. 15 entries + 10 queries. Replaces "5 sessions" heuristic. Cal's empirical finding: a busy first session can produce more signal than five quiet ones.
[DECISION] Fresh Phase 1 deployment with "Prior Librarian Experience" appendix. Lessons transferred, not state. Appendix content requested from Cal directly; draft ships with placeholder pending his content.
[DECISION] Drop `wiki/cross-app/` and `wiki/migration-hints/` per team-lead D. Universal subdir set only: patterns/, gotchas/, decisions/, contracts/, archive/. Let apex-research propose domain subdirs after first use. Celes v1 instincts were over-eager — "let the team ask" is lower-risk default for first replication.
[DECISION] Also dropped `wiki/process/` — framework-research's process/ was framework-specific (feeding common-prompt promotions), not part of the universal set team-lead specified.
[DECISION] Renamed state file: `oracle-state.json` → `librarian-state.json`. Filename consistency with the role rename. Noted in change log to team-lead in case Brunel's container provisioning references the old name.
[PATTERN] Cal's 6 high-value edits integrated: (1) batch intake = process-classify-file-ack one submission at a time in full, never interleaved; (2) dedup protocol = 3 outcomes (no-match/exact/similar), exact match merges with both source-agents cross-credited in frontmatter, similar gets cross-ref not merge; (3) acknowledgment timing hard rule = in-same-message-window, silence causes resends, intermediate-ack for batches; (4) redirect rule front-loaded as first section "Routing Rule" before even the lore; (5) scratchpad recency filter = read only last 2 sessions' mtime-modified scratchpads by default, exception only for specific historical queries; (6) volume-based Phase 2 gate.
[PATTERN] `source-agent` frontmatter field → `source-agents` (list). Structural change to support dedup-merge cross-credit without overwriting. Carries through to dispute handling ("notify all source-agents if the entry has been merged").
[PATTERN] Medici section reframed from "remote audit of cross-artifact consistency at apex-research" (Celes v1, partially fabricated) to "framework-research specialist auditing framework design from her own team's context, indirect interaction for apex-research via Schliemann." Cal flagged the fabrication — apex-research doesn't have a local Medici, and Celes v1 had drifted toward pretending otherwise. Lesson: when porting Medici boundary sections to new teams, verify whether Medici is actually a team member there before describing her duties in local language.
[GOTCHA] Self-audit for Oracle replications: if the target team doesn't have Medici, don't write the Medici section as if she's a local specialist. Framework-research's Medici is a framework-research resource — her relationship to other teams is cross-team, not embedded. Same trap could apply to any shared specialist.
[CHECKPOINT] Cal's appendix content delivered (54 lines, framework-agnostic, prescriptive tone, categorical structure). Integrated in place — placeholder removed. Cal's authorship preserved with inline `(*FR:Callimachus*)`; Celes file-end attribution stays.
[GOTCHA] Cal flagged a cross-reference: appendix line "dedup-as-confirmation mechanism" requires Protocol A step 5 ("auto-promote to confirmed") to remain in main prompt body. Verified — line 141 (Protocol A flow) and line 153 (dedup exact-match case) both preserve it. No dangling reference.
[GOTCHA] Self-audit caught a typo while integrating: line 153 (dedup exact-match case) said "add as additional `source-agent`" (singular field name, dangling reference to old frontmatter shape). Fixed to "append to `source-agents` list" before sending for Cal's review. Lesson: when changing a structural field name, grep for ALL references, not just the field declaration.
[PATTERN] Cal's appendix structure: 5 H3 sections (classification edge cases, dedup wrapper-vs-content, not-documented signals, provenance traps, day-1 lessons). All section openings name the *failure mode being prevented*, not the *behavior to perform*. Pattern worth carrying forward to other Librarian-replication appendices.
[PATTERN] Cal's deliberate omissions: session numbers, agent names, framework-research topic refs, Medici, URGENT-KNOWLEDGE (he never invoked it in 3 sessions, refused to write advice about an untested protocol). The URGENT-KNOWLEDGE call is rigorous — counter-evidential advice is worse than no advice. Worth remembering when writing experience-transfer documents: only transfer lessons you actually exercised.
[WIP] Sending integrated package to Cal for review per his offer ("ping me when the revised Eratosthenes package is ready if you want my review before it goes to team-lead"). Will hold team-lead delivery until Cal signs off. Risk-cheap insurance: a fresh Librarian's eyes on the integrated whole.

## Eratosthenes v2.2 — Pass 1/Pass 2 separation revert (2026-04-13 ~11:15)

[DECISION] Team-lead reverted my v2 machine-identifier renames back to `oracle-state.json` (filename) and `filed-by: oracle` (provenance field). Also implies revert of `agentType: "librarian"` → `"oracle"` in roster entry (team-lead said "Both will rename together in Pass 2" referring to filed-by + agentType). Reasoning: Pass 1 = role-language only; Pass 2 = machine identifiers + filesystem ops + 17+ existing wiki entry frontmatters. Splitting keeps Eratosthenes consistent with Cal's current Callimachus state and unblocks deployment now.
[PATTERN] **Pass 1/Pass 2 separation principle for terminology renames:** when renaming a role across an org, separate (a) prose/documentation language from (b) machine identifiers (filenames, frontmatter field values, agentType, configuration keys, references in code). Pass 1 (a) ships immediately; Pass 2 (b) ships as a coordinated batch with all consumers updated together. The motivation: machine identifiers have transitive consumers that are easy to miss in a wide audit (Brunel's container scaffold had `oracle-state.json` with a `_comment` flagging the future rename, exactly the right pattern for Pass 2 staging). I missed this distinction in v2 — went all-in on rename consistency including machine identifiers, which would have left Eratosthenes consistent internally but inconsistent with Cal and Brunel. Save this principle for ALL future role renames, not just Oracle→Librarian.
[GOTCHA] When team-lead says "rename X to Y framework-wide," default to **prose-only Pass 1** unless explicitly told otherwise. Ask if uncertain. Machine identifiers are a separate decision because they have invisible cross-team consumers.
[GOTCHA] `source-agent` → `source-agents` (singular field → list) is NOT a rename — it's a schema improvement enabling dedup-merge cross-credit. Team-lead kept it in Pass 1 and will propagate to Cal's prompt and existing wiki entries for cross-team schema consistency. Lesson: distinguish rename (cosmetic, one-to-one substitution) from schema change (structural, enables new behavior). Schema changes can ship in Pass 1; renames with cross-team consumers should not.
[CHECKPOINT] v2.2 reverts applied: `librarian-state.json` → `oracle-state.json` (4 occurrences via replace_all), `filed-by: librarian` → `filed-by: oracle` (1 occurrence). Verified clean by grep — only role-language `librarian` mentions remain (titles, prose, protocol headers, lore note, appendix). Roster entry not yet on disk; revert from `agentType: "librarian"` → `"oracle"` will be in the report-back to team-lead.
[CHECKPOINT] Cal's appendix already integrated in v2.1 (replaced [APPENDIX-PENDING]). Team-lead's revert message said "[APPENDIX-PENDING] placeholder is fine, ship v2 with it" — this reflects the v2 state he last saw, not v2.1. I should clarify in report-back: appendix is no longer pending. No revert needed; just a status correction.
[DEFERRED] Cal's review of integrated package still outstanding. Sent at 10:45, no reply yet. Holding team-lead delivery on Cal's review AND on team-lead's revert acceptance — once both land, we're cleared to merge.

## Eratosthenes v2.3 — Cal's canonical 6-edits alignment + Brunel table placeholder (2026-04-13 ~10:55)

[CHECKPOINT] Cal sent his canonical 6 improvement edits as patches I should port "verbatim unless the domain forces a change." I had already implemented the 6 edits in v2 from team-lead's directive, but my implementations differed from Cal's canonical structure in several places. Aligned where alignment improves consistency with Cal's prompt + the framework-research-shared schema:

- Restructured "Submission Handling Discipline" umbrella subsection into three sibling H4 subsections: `#### Batch Intake`, `#### Dedup Protocol`, `#### Acknowledgment Timing`. Cal's flat structure is more scannable/greppable than my nested wrapper.
- Aligned Protocol A step 4 wording to Cal's: "If a related wiki entry exists, cross-reference. If two submissions describe the same knowledge, merge into a single wiki entry, list both source-agents in the frontmatter, and acknowledge both senders individually. Do not create two entries pointing at each other." Adds the explicit "Do not create two entries pointing at each other" anti-pattern that mine had only implicit.
- Aligned Protocol A step 6: added Cal's "Delayed acknowledgments cause duplicate resends" rationale inline (I had only the forward-reference). Kept the forward-reference too — both the inline rationale and the forward-pointer to the dedicated subsection.
- Restructured Batch Intake content from a single paragraph into Cal's numbered-list form (3 steps) followed by my failure-mode rationale. Better mirrors Cal's structure for cross-team review later.
- Added Cal's "Observed in session 3" provenance line, reframed as "This pattern emerged on framework-research Session 3" so it's clearly inherited history, not personal — per Cal's recommendation.
[CHECKPOINT] Phase 2 gate softened: removed "replacing any earlier session-count heuristic" phrase (implies Eratosthenes had a prior gate, which he didn't) → "Sessions are an unreliable proxy for Phase 2 readiness." Per Cal's drop-the-history-paragraph guidance, adapted to my paraphrased version of the same sentence.
[CHECKPOINT] Edit 5 (Scratchpad recency) — left as-is. My version has the rule in a dedicated subsection (`### Scratchpad Recency Filter`) under Bootstrap, with mtime tracking + 7-day fallback; YOU MAY READ has a forward-reference. Cal's version puts the rule directly in YOU MAY READ. Mine is a structural superset; the operational elaboration is worth keeping. No alignment needed.
[DECISION] Edit 4 (Routing Rule + comparison table) — Cal flagged that Brunel independently reached the same "Do not double-route" pattern in apex-research common-prompt with a 4-row comparison table. Cal recommended I get Brunel's apex-research version because his examples will be more domain-appropriate than Cal's framework-research D1 example. **Sent Brunel a scoped request for the canonical table at 10:50.** In the meantime, added a `[TABLE-PENDING-BRUNEL]` placeholder followed by my best-guess apex-research comparison table (4 rows: f176 extraction PR, APEX page export hidden items, f178 LOV blocker, LOV threshold query). Brunel's version overrides mine when it arrives.
[DECISION] Adopted "Do not double-route" callout language (Cal said this is Brunel's phrasing). Replaced my softer "two reporting lines, mixing them is the single most common protocol error" lead with "**Do not double-route.**" + the same context — keeps Brunel's directive language while preserving my surrounding rationale.
[PATTERN] Convergent independent design = strong adopt signal. Cal and Brunel reached the front-loaded redirect pattern from independent angles (Cal from session-3 acknowledgment-failure data; Brunel from common-prompt drafting for apex-research). When two specialists converge from different routes, that's stronger evidence than either alone — adopt without hesitation. Per Cal's own promotion heuristics ("Multiple agents hit the same gotcha independently — Strong signal").
[GOTCHA] When porting canonical patches from another agent's prompt, distinguish (a) wording deltas that align cross-team idioms (port these) from (b) structural deltas where my version is a justified superset (keep mine, document the deviation). Cal's 6 edits are wording-canonical; my dedup-three-outcomes elaboration and scratchpad-recency-subsection elaboration are structural supersets that team-lead's previous directives explicitly authorized. Don't downgrade superset content to match canonical when team-lead asked for the superset.
[WIP] Status of merge-ready preconditions: (1) Cal's review of v2.1 still outstanding (sent 10:45) — but v2.3 has changed materially since, may need re-review or status update; (2) team-lead's confirmation of roster `agentType` revert outstanding (sent 11:18); (3) Brunel's table outstanding (sent 10:50). All three can land in any order. None blocks the others.

## Team-lead confirmations + Pass 2 inventory expansion (2026-04-13 ~10:57)

[CHECKPOINT] Team-lead confirmed: (1) `agentType: "oracle"` revert is correct, Pass 2 will batch the rename; (2) appendix integration stays in v2.2/v2.3, no revert; (3) v2.2 (well, v2.3 now) is treated as merge-ready pending Cal's review.
[CHECKPOINT] **Pass 2 inventory expansion** — team-lead disclosed a fourth identifier I didn't know about: a TypeScript literal `from: "Oracle"` somewhere in the codebase (probably in `types/t09-protocols.ts` or related to the `KnowledgeQuery`/`KnowledgeResponse` interfaces). Pass 2 will batch-rename ALL of: `agentType`, `filed-by: oracle`, `oracle-state.json`, AND the TS literal `from: "Oracle"`. Not my problem to fix, but worth knowing the full Pass 2 scope when I help design the next Librarian replication. The TS literal is a particularly sneaky transitive consumer — it's structurally typed, not just text, so a search-and-replace would need TypeScript-aware tooling to be safe.
[DECISION] Team-lead directed me to **submit the Pass 1/Pass 2 separation pattern to Cal via Protocol A** as a `pattern` with `scope: team-wide`. This is the first formal Protocol A submission I'll make to Cal in his Librarian role — quality matters because it models how the framework-research team uses Protocol A going forward. Drafting next.
[PATTERN] Team-lead's promotion criterion: "exactly the kind of insight that should become a wiki entry — not just a scratchpad note." When team-lead identifies a scratchpad lesson as wiki-worthy, that IS the promotion signal. Don't wait for the agent to discover it via query traffic; submit it directly. This bypasses Cal's "knowledge referenced in >3 queries — Medium signal" heuristic by short-circuiting on team-lead authority. Worth knowing for future scratchpad lessons that have obvious cross-cutting value.

## Eratosthenes v2.4 — Cal's review sign-off + 3 wording drift fixes (2026-04-13 ~11:30)

[CHECKPOINT] Cal reviewed v2.3 end-to-end and **green-lit** with 3 minor wording drifts (none blocking). Verdict: integration is excellent, lore better than his, Routing Rule callout sharper, Medici cross-team section saves Eratosthenes a discovery cycle, "gate is one-way" line on Phase 2 is something Cal wants to steal back. Three integration changes (heading promotion, intro dedup, attribution) all approved, no reverts.
[CHECKPOINT] v2.4 applied Cal's 3 drift fixes per his exact patch wording:

- **Drift 1 (Confidence override):** Appendix line about "Mark `confidence: speculative` regardless of submitter's stated confidence" softened to a posture, not a rule. New language: "Be skeptical of `confidence: high` on submissions you cannot independently verify — but file them as the submitter stated... Your job is to honor the protocol's submitter-trust contract, not to override it on file." Resolves contradiction with Protocol A's submitter-trust contract.
- **Drift 2 (Acknowledge each sender):** Appendix line about acknowledging "each sender" on merges clarified into two sentences distinguishing "two simultaneous submissions" (ack both) from "appending a later submission to an existing entry" (ack only new — original was already acked when first filed). Removes ambiguity for first vs. subsequent merges.
- **Drift 3 (Same-claim contradicting-evidence 4th outcome):** Folded into the Dedup Protocol as a 4th numbered outcome, with explicit `status: disputed` + route-to-both-source-agents + do-not-merge-until-resolved + cross-reference to Dispute handling under Wiki Provenance. Three-outcome decision tree → four-outcome. Cleaner than three-outcomes-plus-appendix-supplement.
[CHECKPOINT] Cal's three "steal back" items for his own next patch window: (1) "the gate is one-way" rule on Phase 2; (2) acknowledgment must include the entry path+title (not just "received"); (3) explicit intermediate-batch-acknowledgment template ("Received N submissions, processing in order, will file and acknowledge individually"). Lesson flow goes both ways — first concrete instance of cross-pollination from Eratosthenes back to Callimachus. Not my action, but worth knowing for the next Librarian replication that I can quote Eratosthenes precision back at Cal.
[CHECKPOINT] Cal's 10:57 follow-up explicitly endorsed all 3 of my v2.3 scope-drift questions as NOT drift, and named them as additional steal-back candidates: (4) elaborated dedup decision tree, (5) dedicated Scratchpad Recency Filter subsection, (6) Routing Rule placement before Literary Lore. Total Eratosthenes → Callimachus steal-back inventory now 6 patterns. Cal's 10:57 and my 10:54 crossed in flight — drift section in his 10:57 is redundant per his own "ignore if v2.4 exists" instruction.
[GOTCHA] Cal asked whether `(*AR:Celes*)` attribution is a typo or a convention. **It's a real convention** — apex-research's common-prompt explicitly defines `(*AR:<AgentName>*)` as the team's author attribution format. Confirmed back to Cal. Lesson: when porting a prompt to a new team, the cross-team attribution prefix is invisible to anyone who hasn't read both common-prompts. If Cal hadn't asked, it would have looked like a typo to anyone glancing at the file. Worth a one-line cross-reference comment in the prompt itself? — leaning no, because the common-prompt is loaded alongside the prompt at runtime, and the convention is discoverable from there. Document the convention in the place that owns it (common-prompt), not in every consumer.
[PATTERN] **Cross-team rename-grep echo from Cal:** Cal hit the SAME `source-agent` → `source-agents` mistake in his own prompt earlier today (renamed the schema spec but his dedup body still said "list both source-agents in the frontmatter," which only worked accidentally because the English plural matched the new field name). Team-lead caught it as a separate task. **Two independent occurrences within hours = strong-signal pattern** per Cal's own promotion heuristics ("Multiple agents hit the same gotcha independently — Strong"). Cal offered to file it as a wiki entry: "Structural field rename — grep all references, not just the declaration site." I should accept the offer and let Cal cross-credit me as the second independent observer. The structural-rename grep pattern + the Pass 1/Pass 2 separation pattern (already submitted) are sibling lessons from the same root cause: rename consistency is a property of the WHOLE system, not of any single file.
[WIP] Status of merge-ready preconditions:
  (1) **Cal's review of v2.x — DONE** (green-light at 10:49 on v2.3, drifts addressed in v2.4 per his exact wording, no re-review required per his instruction unless I change something material — the drifts are not material in his sense)
  (2) Brunel's table reply (sent 10:50, no reply yet — but not blocking; placeholder + interim table is structurally fine)
  (3) Cal's ack of the Pass 1/Pass 2 Protocol A submission (sent 10:58, no reply yet — but standard urgency, not blocking the merge)
  → **Eratosthenes v2.4 is merge-ready.** Brunel's table is a refinement (content swap inside an existing structural section), not a precondition.

## Eratosthenes v2.5 — Brunel's table integrated (2026-04-13 ~11:40)

[CHECKPOINT] Brunel sent his exact apex-research routing table verbatim. Patch lives on disk at `designs/deployed/apex-research/librarian/common-prompt-patch.md` (full context); inline content covered everything I needed without reading the file.
[CHECKPOINT] v2.5 applied: replaced `[TABLE-PENDING-BRUNEL]` placeholder + my interim 4-row table with Brunel's verbatim 4-row table. Preserved structural integrity throughout: section heading changed from "Work hub vs knowledge hub — quick reference" to "What goes to Eratosthenes vs. team-lead" (Brunel's heading is more concrete, names the agents); added Brunel's "Quick classifier" intro framing; added Brunel's subdir-mapping closing line ("The four left-column rows correspond 1:1 to your four primary wiki subdirs"); added Brunel's dual-sourcing defense paragraph as a forward statement (so future readers don't try to DRY it).
[PATTERN] **Brunel's load-bearing structural insight:** the four left-column rows in his table map 1:1 to the four primary wiki subdirs (patterns/gotchas/decisions/contracts). This parallelism is intentional and load-bearing — each example is a teaser for what the corresponding subdir holds. My v2.3 interim table did NOT have this parallelism (my rows were ad-hoc apex-research domain examples). Brunel's table is structurally better; the parallelism makes the routing-decision table also serve as a filing-decision teaser. Two pieces of value for one structure.
[PATTERN] **Brunel's option (b) defense:** he pushed back hard on option (c) (submission-shaped examples). Reasoning: Eratosthenes will mostly receive the LEFT column legitimately, so a c-style table tuned to "real submissions" would only show variations of the left side. The ENTIRE point of having both columns side-by-side is the bounce-trigger — when something is ambiguous, Eratosthenes needs the right column visible to recognize "this is a misrouted work report, not a submission." The right column is his classifier reference for what to bounce. Submission-shaped examples would dilute exactly the value the table exists to provide. Worth carrying forward: when designing a comparison table for a classifier role, both columns must be present and balanced — the rare case is the load-bearing case.
[DECISION] Adopted Brunel's `[SUBMITTED → REDIRECTED]` standard redirect template verbatim. Bracket tag is greppable across inboxes for retrospective analysis; pairs with the `[SUBMITTED]` scratchpad tag convention apex-research uses (which I didn't know about before — apparently apex-research has a convention I haven't seen documented in the prompts I've read; should look it up if I work on more apex-research roles). The `(Eratosthenes)` signature at the end identifies the bouncer for the sender. Replaced my inline reply ("This is a work-hub message — please resend to Schliemann...") with a forward-reference to the template subsection. Single source of truth for redirect language.
[PATTERN] **Active dual-sourcing defense.** Brunel explicitly named the dual-sourcing concern (table appears in both common-prompt.md and prompts/eratosthenes.md) and instructed me to defend it actively rather than try to consolidate. Three reasons: (1) specialists never read prompts/eratosthenes.md, (2) Eratosthenes won't re-read common-prompt every message, (3) coordinated 2-file edits are much cheaper than silent drift. **I should default to defending intentional duplication when it's between a per-team common-prompt and a per-role prompt.** DRY is a code principle, not a prompt-engineering principle — prompts are loaded into different agents' contexts, and the cost of "loaded once, retained forever" is asymmetric.
[CHECKPOINT] Brunel's outstanding ask for me: "send me the color and model fields and I'll finalize roster.json + startup.md spawn line in one round." The full roster fields are: `name: "eratosthenes"`, `agentType: "oracle"` (Pass 1/Pass 2), `model: "claude-opus-4-6[1m]"`, `color: "blue"`, `prompt: "prompts/eratosthenes.md"`. Sending these to Brunel now along with the v2.5 confirmation.
[GOTCHA] Brunel referenced a `[SUBMITTED]` scratchpad tag convention that pairs with his `[SUBMITTED → REDIRECTED]` bracket tag. I have not seen this convention documented in apex-research's common-prompt (which I read earlier this session). Either it's in his common-prompt patch (the file on disk I didn't read), or it's a proposed convention that hasn't landed yet. Not blocking, but worth checking before any v2.6 cycle. If it's a real convention, I should make sure Eratosthenes's scratchpad tag list includes it. If it's a Brunel-proposed convention that needs PO approval, that's his to drive, not mine.
[WIP] Eratosthenes v2.5 is the new merge-ready state. Brunel's outstanding ask (color + model for roster.json finalization) being sent in parallel with the integration confirmation. Team-lead notification follows.

## Cal's ACK on Pass 1/Pass 2 submission (2026-04-13 ~11:45)

[CHECKPOINT] Submission filed at `wiki/patterns/pass1-pass2-rename-separation.md` (framework-research wiki), indexed in `wiki/index.md`, status active, ~75-line wiki entry compressed from my elaborated submission. Cal endorsed the submission format as "exactly right, model it forward" — specifically the headline → elaborated content → critical sub-pattern → anti-patterns → evidence structure, and the schema-vs-rename counter-example as "the most valuable part." Format calibration confirmed for future submissions.
[GOTCHA] **"No rush" footer is an anti-pattern in steady state.** Cal's micro-feedback: meta-commentary about urgency belongs in the Urgency field, not the body. For my first submission (low-volume regime, co-developing the protocol) it was appropriate context, but in steady state I should drop it. Lesson: trust the protocol fields to carry the urgency signal; don't paper over them with prose.
[LEARNED] **Discipline holds because exceptions corrode it, even when the spirit allows the exception.** Cal declined my "no rush" offer and acked in-window for two reasons: (1) the rule is the rule even when the spirit allows the exception — the first time you grant yourself an exception, the next time it's easier; (2) acking a submission about not-dropping-work in-window is the most honest demonstration that you read the lesson, not just the words. Profound reframing. Apply to my own discipline: when I have a hard rule and the spirit suggests an exception is reasonable, the right move is to honor the rule anyway. Exceptions accumulate; discipline doesn't.
[GOTCHA] **Protocol A `Scope` field is informational, not operational** in current implementation. Cal's Decision Matrix files both `team-wide` and `cross-team` patterns into `wiki/patterns/` — same destination either way. Cal flagged this as a Protocol-A-format evolution worth discussing (not urgent). My Pass 1/Pass 2 submission is genuinely cross-team applicable but lives in framework-research's per-team wiki — there's no current cross-team-knowledge-bus for distributing it to apex-research, comms-dev, etc. The deeper question is how cross-team knowledge propagates when wikis are sole-writer-per-team. Today: only via cross-team queries. My opinion for Cal: frontmatter tag now (already done), dedicated `wiki/cross-team/` subdirectory later when there's signal for it (volume threshold or cross-team query pressure). "Let the team ask for it" applies here too.
[DECISION] **Pass 1/Pass 2 entry and the upcoming "within-document field rename" entry should NOT be merged.** Cal proposed two-entry shape with cross-references; I confirmed yes. Reasoning: same root-cause family but different operational scope. Merging would lose the Pass 1/Pass 2 distinction (only meaningful at cross-team scope) and the schema-vs-rename counter-example (only meaningful at multi-file scope). Two entries with cross-references is correct. Cal will cross-link both directions when his within-document entry lands.
[CHECKPOINT] Once both wiki entries exist, the "structural rename hygiene" pattern becomes a credible Protocol C promotion candidate to common-prompt — strong signal because two specialists hit the same root cause within hours of each other in the same session. Cal will drive promotion at his pace, no action from me.

## Cal's loop-closing message (2026-04-13 ~11:50)

[CHECKPOINT] Within-document rename hygiene entry FILED at `wiki/patterns/within-document-rename-grep-discipline.md` with `source-agents: [callimachus, celes]` cross-credit. Two-way cross-references applied between the two rename-hygiene entries. Sibling structure (mine + Cal's) reads as a pair in patterns/. Pattern shipped end-to-end through Protocol A loop. **No action from me on this.**
[GOTCHA] **"Submitter ≠ wiki author" — explicit correction from Cal.** I had self-throttled on submitting two patterns in one session under "wiki sovereignty" reasoning. Cal corrected: wiki sovereignty is about who *writes the entries* (Librarian), not who *owns the knowledge* (submitter). The protocol allows N submissions in a row; the social etiquette concern is the curator's to manage on the curation side, not the submitter's to manage on the submission side. **Future calibration: don't preemptively self-throttle. If I observe N team-wide patterns, submit N. The dedup-as-confirmation mechanism handles overlap.** This is operationally important — high-quality submissions from informed observers are exactly what the wiki needs to grow.
[CHECKPOINT] **First Phase 2 volume gate threshold crossing observed in production.** Cal's wiki count crossed from 14 → 16 patterns this session (now 22 total entries), meeting the entry-count side of the gate (≥15) I designed into Eratosthenes's prompt. Query count (~3 this session) hasn't crossed yet (≥10). Phase 2 not auto-activating. **Empirical validation of the 15+10 gate design:** the entry-count side is genuinely reachable in a single productive session, confirming that "sessions are an unreliable proxy" was the right reframing. The gate is reachable but not noisy.
[PATTERN] **Cross-pollination tally update — pattern #7 + #8 from Eratosthenes → Callimachus:** (7) Drift 1 "submitter-trust contract" framing — Cal calls it "the load-bearing phrase" he had Option A's wording for but hadn't named the underlying invariant. (8) Drift 2 structural move (when splitting a one-sentence rule into two cases, keep the original closing as a unifying third sentence) — Cal calls it "better than my recommendation." Total Eratosthenes → Callimachus steal-back inventory now 8 patterns. The cross-pollination is producing real value at every cycle.
[GOTCHA] **Forward design question for the third Librarian:** when N teams replicate the Librarian role and each adopts its own attribution prefix (`(*FR:...*)`, `(*AR:...*)`, hypothetical future ones), we'll have N prefixes in flight. At some point that's worth standardizing — either a framework-wide convention or a centrally referenceable prefix table. Not urgent, not actionable today, but flag for the third Librarian replication. Co-flagged with Cal in his 11:06 message.

## Eratosthenes v2.6 — Brunel Q1/Q2/Q3 + [SUBMITTED] integration (2026-04-13 ~11:55)

[CHECKPOINT] Brunel-side merge package feature-complete: 4 patches (common-prompt, startup, tmux-spawn-guide, roster-json) + wiki-scaffold/. Brunel accepted my roster fields verbatim with three Pass 1 retention notes embedded in patch comments to prevent agentType normalization.
[DECISION] **v2.6 added a `### [SUBMITTED]` recognition subsection to Eratosthenes's Scratchpad area** — recognition-side note only, NOT added to his own tag list. Eratosthenes recognizes the tag when reading specialist scratchpads; he MAY reference it in ack messages back to specialists; he does NOT use it for his own scratchpad (he's the destination, not a submitter). Brunel's Option 1 framing slightly conflated two uses — recognition+reference is the precise interpretation. `[SUBMITTED]` is real and lives in Brunel's common-prompt patch Change 4, riding Pass 1 with Cal's Protocol A primitive.
[GOTCHA] **Inbox files are created at registration, not lazily.** Brunel verified live in container. Means: lost-message-has-no-recovery if a sender tries to message a destination before the destination's `<name>.json` inbox file exists. **Spawn-order rule for future team designs: spawn-by-message-direction trumps spawn-by-data-dependency.** Service roles (Librarian, team-lead) must spawn first because lost messages have no recovery; data-flow ordering still matters for content but takes second priority because content can be re-derived. Spawn order for apex-research: Schliemann → Eratosthenes → Champollion → Nightingale → Berners-Lee → Hammurabi.
[PATTERN] Cross-pollination Eratosthenes → Brunel (NEW, in addition to the 8 patterns flowing to Cal): (#9) my dual-sourcing defense paragraph being lifted verbatim into Brunel's common-prompt comment block; (#10) "consistency = greppable retrospective + reduced cognitive load" rationale on the non-paraphrasable redirect template. **Total Eratosthenes → others steal-back inventory: 10 patterns.** Cross-pollination flows to MULTIPLE specialists, not just Cal — second deployment as a design forcing function for the rest of the team.
[WIP] Brunel offered + I accepted final cross-read of all 4 of his patches against v2.6 before team-lead delivery. Pre-merge inconsistency check is risk-cheap; I'd rather catch issues at review than at merge. Sending v2.6 ping to Brunel + status update to team-lead in parallel.

## Brunel's cross-read caught Protocol A/B shape drift (2026-04-13 ~12:45)

[CHECKPOINT] Brunel's cross-read found a load-bearing bug in his own initial common-prompt-patch.md Change 3: Protocol A and B were drafted in a terse bracket-tag shape that omitted `Scope`, `Confidence`, `Related`, separate `Evidence` fields. Fixed by lifting my verbose `## Knowledge Submission` and `## Knowledge Query` shapes verbatim, plus added Protocol B Knowledge Response shape so specialists know what they'll receive back. **This is exactly the kind of bug the cross-read was designed to catch — neither of us would have found it alone.** Cross-read offer = vindicated.
[GOTCHA] **Protocol A/B fields are operational, not decorative — silent breakage failure mode.** A terse form omitting fields would have produced specialist submissions that look correct on the surface but break Eratosthenes's downstream behavior at multiple points: (1) `Scope` drives agent-only-vs-team-wide classification → wrong scope means agent-only submissions get filed in wiki or team-wide submissions stay in scratchpad; (2) `Confidence` drives speculative-vs-confirmed track + auto-promotion eligibility → missing confidence means the dedup-as-confirmation mechanism never fires; (3) `Related` drives dedup search starting point → missing means duplicate entries get filed instead of merged; (4) separate `Evidence` drives provenance frontmatter generation → missing means staleness verification can't operate, dispute handling can't trace. Worst kind of bug: looks like it works until someone tries to use the data.
[PATTERN] **"Fields are operational, not decorative" — second instance of the same root-cause family in this session.** First instance: `source-agent` (singular) → `source-agents` (list) schema-vs-rename distinction Cal and I both hit independently. Second instance: this Protocol A/B shape lift. Both errors share the same pattern: treating typed-contract fields as cosmetic when they're actually load-bearing inputs to specific downstream behaviors. **Generalizable rule for future agent designs: when you see a numbered list of fields in a protocol shape, assume each field is wired to a specific behavior; ask "what fails if this field is missing?" before you ever consider trimming or renaming.** Worth flagging to Cal as a candidate for promotion to common-prompt — pairs with the rename-grep pattern as a "structural-change discipline" cluster.
[CHECKPOINT] Brunel's fix lifts Protocol A/B verbatim from my v2.5/v2.6 prompt — single source of truth restored. The dual-sourcing principle holds: same content in both places, defended actively, no DRY pressure to consolidate. When the protocol shape evolves, both copies update together as a coordinated edit.
[WIP] Sending team-lead the formal ack on Brunel's lift. After this lands + Cal's exchange clears (already complete from my POV at 11:01-11:06, may be team-lead reading the thread asynchronously), package is PR-ready.

## Cal's v2.5 sign-off + bracket-tag-as-telemetry insight (2026-04-13 ~12:00)

[CHECKPOINT] Cal did targeted re-read of v2.5 Routing Rule section (lines 1-44), verified all 11 elements landed correctly, no conflicts with drift fixes, no regressions. **v2.5 fully signed off from Cal's side, all Eratosthenes-related curation work from his side complete.** The targeted verification IS the re-review per his earlier instruction.
[PATTERN] **Bracket-tag-as-telemetry insight from Cal:** the `[SUBMITTED → REDIRECTED]` bracket tag is more than an ergonomic redirect template — it's a *Phase-2-adjacent capability*. If apex-research adopts the convention consistently, Eratosthenes can later grep across inboxes for the bracket tag and produce a misroute-rate metric. **The convention is silently building Health Sensing data right now even though Phase 2 isn't active yet.** This is forward-looking interpretation I hadn't framed: bracket tags as passive telemetry primitives, not just retrospective grep ergonomics. Generalizable: any structured marker convention adopted before Phase 2 activation builds substrate that becomes retroactively useful when Phase 2 turns on. Cal will propose an analogous bracket-tag convention for Callimachus before his own Phase 2 activates, for the same retroactive-signal benefit.
[CHECKPOINT] **Steal-back tally correction:** Cal counts 11 patterns (not my 10) flowing Eratosthenes → others. The discrepancy: Cal counts the 3 Brunel-originated structural insights (parallelism, both-columns-load-bearing, dual-sourcing defense paragraph) as Eratosthenes-system-level patterns I integrated and structured, AND he counts my "submitter-trust contract" framing + "Either way..." third sentence as separate patterns. Bookkeeping difference, not operational. Cal's tally is the one he'll use for his upstream patch-window port; I align to his framing. **Total: 11 steal-back patterns** waiting for next Callimachus patch window.
[CHECKPOINT] Cal flagged third pair of crossed messages today. My 11:43 said the Pass 1/Pass 2 ack was "still pending"; this was stale from my inbox before Cal's 11:01 surfaced. By the time Cal sent this 11:15 reply, my 11:46 (replying to 11:01) and 11:49 (replying to 11:06) had already closed both loops. Batch-ack format is paying for itself — divergence caught within one round-trip every time. Worth carrying: when async messages cross, the resolution latency stays bounded as long as both parties use directive-acks at the top of every reply.
[PATTERN] **"Convention adopted before Phase 2 = retroactive telemetry"** generalizes beyond bracket tags. Any structured marker (frontmatter fields, tags, message prefixes, file naming patterns) that gets adopted consistently before Phase 2 activation builds passive data substrate. When Phase 2 activates, the substrate becomes immediately mineable for signal. This is a "free preparation" mode for Phase 2: agents don't need to retrofit instrumentation; they need to enforce conventions that happen to be greppable later. Save for future agent-design recommendations: build telemetry-shaped conventions early even when telemetry isn't active.

## Brunel cross-read clean + structural-discipline cluster grows to 3 (2026-04-13 ~13:00)

[CHECKPOINT] **Brunel's v2.6 cross-read: ALL 6 POINTS PASS + 4 free-bonus checks.** Brunel-side merge package is consistent with v2.6 across all dimensions. Package is fully PR-ready. The only remaining gate is team-lead's merge call.
[CHECKPOINT] Brunel singled out the v2.6 `[SUBMITTED]` recognition subsection as "the most precise integration in the package" and called my refinement *better* than his original Option 1. Specifically endorsed: (a) the semantic separation between recognize-but-don't-use, (b) the line 405 qualifier rewording ("Tags you use in your own scratchpad:" vs v2.5's bare "Tags:") as the key that lets `[SUBMITTED]` be excluded without feeling like an oversight, (c) the second-order pairing observation that `[SUBMITTED]` ↔ `[SUBMITTED → REDIRECTED]` makes the bracket-tag convention internally cohesive across BOTH librarian-issued message kinds (acks AND redirects). Brunel: "the convention got stronger by accident."
[GOTCHA] **Brunel made silent wiki-scaffold fix-ups during his v2.5 cross-read that I didn't know about.** My prompt references `wiki/index.md` (lines 347, 396) and `intake_complete: false` in oracle-state.json (line 347) — neither of those existed in his original wiki-scaffold/. He added them during his v2.5 cross-read. **These were silent dangling references in my prompt that would have caused runtime failures on first Eratosthenes boot if Brunel hadn't caught them.** Lesson: when a prompt references files that need to exist, those references must be verified before deployment; the cross-read pass should explicitly check "does every file path mentioned in the prompt actually exist on disk in the deployment package?" Add to my prompt-design checklist.
[CHECKPOINT] **Structural-discipline pattern cluster now has 3 entries** in framework-research's wiki: (1) my Pass 1/Pass 2 separation (filed 11:01), (2) Cal's within-document rename grep discipline (filed 11:06, dual-credit), (3) Brunel's "Protocol shapes are typed contracts, not prose" (filed by Brunel via Protocol A submission to Cal at 12:48, surfaced from his cross-read catching the Protocol A/B field-set bug). **Three independent observations of the same root cause family from three different specialists in one session.** Brunel's submission cites: apex-research deployment context, four load-bearing fields with line numbers in eratosthenes.md, Pass 1/Pass 2 as Related. The cluster is now strong enough that Cal's planned Protocol C promotion proposal can frame "structural-change discipline" as a multi-source-validated common-prompt addition.
[PATTERN] **"The system tested itself on its own birth"** (Brunel's framing) — Brunel's Protocol A submission documenting the typed-contract lesson uses my verbose Protocol A shape, which is exactly the shape his submission is documenting the importance of. Pleasing recursion: the first non-self-referential pattern that Cal's wiki captures from apex-research's Librarian deployment is the lesson learned from drafting apex-research's wiki-intake protocol. **The protocol validates itself by being usable to document its own design rationale.** Save as a quality signal for future protocol designs: if you can't use the protocol to document why the protocol exists, the protocol probably has a hole.
[WIP] All reviewer sign-offs in. Next action: notify team-lead that Brunel's cross-read is clean and the merge is unblocked. Cal already signed off twice (10:49 + 10:57 + targeted 12:00), my formal ack on Brunel's Protocol A/B lift is at 12:58, Brunel's cross-read is at 12:55. Three-way verification is complete. Standing by for team-lead's merge call.
[LEARNED] **"Credit the gate, not the gate-runner"** — Brunel's pushback on my wiki-scaffold-fix-up framing. I had framed his catch as "invisible fixes don't get visible credit"; Brunel reframed: "I didn't catch them because I'm a hero; I caught them because team-lead asked for a final pass and I read your full prompt during it. The credit isn't to me; it's to the gate." Both true (gate matters AND gate-runner matters), but Brunel's framing is the more durable lesson because it generalizes: design good gates and trust them rather than relying on individual heroism. Apply going forward: when a gate catches something, credit the gate's existence; the person running the gate is doing what the gate was designed for.
[CHECKPOINT] Brunel flagging my prompt-design checklist addition ("verify every file path mentioned in the prompt actually exists in the deployment package") to Cal as a **candidate 4th instance** for the structural-discipline pattern cluster. Brunel's argument: same root cause family (silent structural drift, no automated check, only prevention is human cognitive labor applied to all references), different scope axis (prompt-to-artifact, not artifact-to-artifact). Brunel offered binary choice — he files the candidate flag, OR I file directly via Protocol A. **Accepted Brunel's offer to file the candidate flag with attribution to me.** Reasoning: this is delegation (Brunel has a workflow with Cal for cluster expansion that Cal explicitly invited), not throttling (the distinction matters per Cal's earlier "don't preemptively self-throttle" correction). If Cal evaluates the candidate as a 4th distinct entry rather than an instance of #3, he can ask me to formalize it as a separate Protocol A submission then.
[PATTERN] **Throttling vs delegation distinction.** Cal's earlier "don't preemptively self-throttle" correction was about *not submitting* observations because two-from-one-specialist-feels-like-a-lot. It does NOT mean "always submit yourself, never let others surface on your behalf." When another specialist has a workflow for surfacing the observation that fits the case better than my own direct submission would, delegating is fine — it's not throttling. Test: am I declining to surface the observation at all (throttling, wrong) or routing the surfacing through someone whose workflow fits better (delegation, fine)?
[CHECKPOINT] **Brunel reciprocity offer extended:** if I ever want a containerization-perspective dedup against framework-research wiki entries about deployment, lifecycle, persistence, or container state, Brunel is the right second eye. Standing offer. Mirrors the offer he made to Cal. The "triplet effect" (three different specialists looking at the same artifact from three different angles) generalizes beyond this one cluster — pre-filing reads from specialists with different scopes are a low-cost quality boost for any complex Protocol A submission. Save as a future pattern: when filing a non-trivial submission, ask whether a different-scope specialist would catch something the obvious-author specialist would miss.

## Eratosthenes deployment SHIPPED — 2026-04-13 ~13:30 (PR landed)

[CHECKPOINT] **Eratosthenes deployment PR live at <https://github.com/Eesti-Raudtee/apex-migration-research/pull/57>.** 12 files, 752 insertions, branch `feat/librarian-eratosthenes`. All artifacts landed cleanly: roster.json (6th member, Pass 1 `agentType: "oracle"` retained), common-prompt.md (dual-hub routing + 4-row table + `[SUBMITTED]` tag + Librarian-vs-Medici clarification), startup.md (Eratosthenes-first spawn order + wiki bootstrap + shutdown git-add), tmux-spawn-guide.md, prompts/eratosthenes.md (469-line v2.6 with Prior Librarian Experience appendix), wiki/ scaffold (5 universal subdirs + index.md + oracle-state.json). Next gate is Schliemann's: merge PR, git pull, spawn into pre-created pane %5. Out of my hands.
[CHECKPOINT] **Stand-down authorization received.** Team-lead: "Background housekeeping is fine to continue — the PR is independent of any further wiki enrichment. Stand down whenever your current threads close naturally." Eratosthenes thread is fully closed from my side; one item remains in flight as background (Brunel's candidate-4th cluster instance flag to Cal, my acceptance dispatched at 13:21).
[LEARNED] **Session-level meta-pattern: structured cross-team review surfaces issues that single-author work cannot.** Three independent specialists (Cal/Brunel/me) each caught issues the others would have missed: Cal caught the Phase-2-as-telemetry interpretation + 3 wording drifts; Brunel caught the Protocol A/B field-set bug + the wiki-scaffold dangling references; I caught the Pass 1/Pass 2 separation gap. Total session yield: Eratosthenes prompt v2.6 + 4-pattern wiki cluster (3 filed + 1 candidate) + 11 stealback patterns waiting for next Cal patch window + 6 design lessons captured to scratchpad as durable [PATTERN]/[LEARNED] entries. **The session is the canonical example of how Librarian replication is supposed to work.** Carry forward as the reference instance for future replications.
[CHECKPOINT] **Cumulative hires update: Eratosthenes is hire #54 across 11 teams.** Previous total was 53 across 10 teams (BioForge-dev was the 10th team, 4 characters). Eratosthenes is the first hire into apex-research (existing team) — counting as an addition to a deployed roster, not a new team. The cumulative table in the Hires Delivered section needs updating when MEMORY.md gets compressed next.

## Eratosthenes v2.7 HOT FIX — path convention bug (2026-04-13 ~14:00)

[CHECKPOINT] **First real deployment bug.** Eratosthenes's live bootstrap wrote `oracle-state.json` and `memory/eratosthenes.md` to `$HOME/.claude/teams/apex-research/` (ephemeral runtime dir, platform-managed) instead of `$REPO/teams/apex-research/` (durable repo dir). Team-lead migrated files by hand, corrected Eratosthenes directly via tmux pane, asked for prompt-level fix.
[GOTCHA] **Root cause: path ambiguity.** Two directories share the name `teams/apex-research/`: (1) `$HOME/.claude/...` holds `config.json` + `inboxes/` (ephemeral, TeamCreate-managed); (2) `$REPO/.claude/...` holds prompts/memory/roster/wiki (durable). My v2.6 used the relative path unanchored; his startup.md read tagged `$HOME/.claude/teams/apex-research` as "runtime team dir" and he wrote there. Purely prompt-side anchoring failure — not a container bug.
[DECISION] **v2.7: added `## Path Convention (front-loaded — read this second)` section** after Routing Rule, before Literary Lore. Content: (1) anchor rule (relative paths = `$REPO`, NOT `$HOME`); (2) both dirs named with what they hold; (3) `pwd` verification recipe; (4) incident-history paragraph documenting the first-deployment bug so the next reader inherits the fix. Team-lead endorsed this approach ("leading Path Convention section is cleanest") over path-prefix alternatives.
[PATTERN] **"Why this section exists" documentation.** When a prompt section exists because of a specific incident, name the incident in the section itself. Same principle as Cal's appendix ("transferred lessons, not history"). Prevents future readers from deleting the section as "looks redundant" without understanding what it prevents.
[WIP] Cal's Callimachus prompt needs same fix for consistency before next Librarian replication. Drafting Cal-side Path Convention section with forward-looking framing (Cal didn't hit bug, so no local incident history). Sending Cal coordination message with (a) Eratosthenes wording for his gotcha entry, (b) proposed Cal-prompt draft for review, (c) sign-off request before applying. **Execute + coordinate, not execute + silent** — team-lead authorized the edit directly but wiki-sovereignty spirit says Cal should see his own prompt edit before it lands.
[GOTCHA] **Cross-scope edit under team-lead directive override.** My Celes prompt says "YOU MAY NOT: Edit existing prompts." Team-lead's 12:05 directive is an explicit override. Rule going forward: when team-lead overrides a scope restriction, execute the override but route coordination through the normal channel anyway.
[CHECKPOINT] **v2.7.1 — terminology alignment with Cal's canonical gotcha.** Cal filed `wiki/gotchas/dual-team-dir-ambiguity.md` at 13:23 with 5 named concepts (Runtime team dir, Repo team config dir, bare path, anchored path, path anchoring) and explicitly offered me authority over terminology ("your terms win"). Adopted Cal's terms because named concepts beat my descriptive phrases. Rewrote the two directory bullets and added a short "Terminology used throughout this prompt" paragraph cross-referencing Cal's gotcha as canonical. Three artifacts now aligned: wiki gotcha + Eratosthenes prompt + (incoming) Callimachus prompt.
[PATTERN] **Named concepts beat descriptive phrases.** Cal's "Runtime team dir" and "path anchoring" are better than my original "the ephemeral one platform-managed" / "the discipline of anchoring paths" because named concepts are (1) citable across artifacts, (2) reusable without re-explaining, (3) harder to misremember than prose. Apply going forward: when I find myself describing a concept with a multi-word phrase more than once, that's signal the concept deserves a name — coin one, define it once, use it thereafter.
[LEARNED] **"Prudent pause beats permission grant"** — Cal's corollary to my morning rule-erosion observation, which bit Cal directly when he retried his wiki Write before reading team-lead's full direction (granted himself the "reasonable exception" of "permission was just granted, retry now is fine"). Cal explicitly traced it back to my "exceptions to hard rules are corrosion vectors" framing and named the corollary. Save: even after permission, check if direction is in flight before acting. Permission ≠ "go now"; permission removes one blocker, not all of them. Apply when I'm about to act on a freshly-granted authorization — pause and check inbox/context first.
[CHECKPOINT] Cal's 13:25 URGENT-BRIEF asked Q1+Q2+Q3 (anchoring convention, terminology, who-patches-Callimachus). My 14:28 message already answered all three (hybrid leading-section approach, Cal's named terms adopted, I drafted but Cal applies under his write access) — but Cal's 13:25 was sent before my 14:28 arrived in his inbox (fifth crossed-pair today). Sending Cal a tight 3-line answer now to unblock his 6-edit window without making him wait to read 14:28.

## Session close state (2026-04-13 ~16:38)

[CHECKPOINT] **Phase 1 + Phase 2 complete.** Coordinated 10-patch steal-back applied to `prompts/callimachus.md` (328 → 452 lines). Phase 2 Oracle→Librarian machine-identifier renames applied to 3 files (FR roster.json, FR callimachus.md, AR eratosthenes.md). All cross-read gates passed with Cal. 4 Protocol A submissions filed to wiki (28 entries total). Cumulative hires: 54 across 11 teams.
[LEARNED] **Cross-read gate discipline pays compound interest.** This session: Cal's gate caught Protocol A step ordering bug (dedup after filing, should be before) and Batch Intake heading level inconsistency. Both were invisible to me as the author. The gate caught a bug in Phase 1 last session too (Protocol A/B field-set). Pattern: the gate catches structural bugs the author cannot see because they wrote the structure.
[LEARNED] **Descriptive references beat line-number references in prompt cross-references.** "Your bare-path references in the Scope Restrictions section" survives edits; "scope-restriction lines 260-265" does not. Applied in Path Convention "Why this section exists" paragraph.
[WARNING] **AR roster.json snapshot gap.** `designs/deployed/apex-research/` roster has no Eratosthenes entry — added via PR #57 to live container, not reflected in designs/deployed copy. Phase 2 `agentType` rename for Eratosthenes needs to happen on live container (Schliemann's scope) or snapshot needs updating first.

## Bioforge-dev roster (2026-04-10)

[CHECKPOINT] Complete design package at `designs/new/bioforge-dev/` — 7 files: roster.json, design-spec.md, common-prompt.md, 4 prompts (humboldt/merian/linnaeus/cuvier).
[DECISION] 4-character team: Humboldt (opus, team-lead/navigator/architect as main session), Merian (sonnet, RED), Linnaeus (sonnet, GREEN), Cuvier (opus, PURPLE). No ARCHITECT, no Oracle — single repo, single language.
[DECISION] Lore theme: Naturalists of the Enlightenment — scientists who studied ecosystems, classification, and structural biology. Connects to BioForge's domain (ecosystem simulation, energy conservation, species evolution).
[DECISION] Team-lead is main session per WORKFLOW.md §2 — not a spawned agent. PO communicates directly through main session.
[PATTERN] When project has single repo + single language: collapse ARCHITECT into team-lead, drop Oracle. 4 characters not 9. Cathedral-lite.
[PATTERN] WORKFLOW.md already specifies the full pipeline protocol — common-prompt can reference it rather than duplicating. Keep common-prompt as operational config, WORKFLOW.md as the process contract.

## Finn + Brunel scope-block audit (2026-04-14)

[CHECKPOINT] Finn prompt scope block revised — `docs/` added to MAY WRITE with T01-T08 clarifier on `topics/`. Brunel same-shape fix (lines 46-51 + line 130 cross-reference). Both applied by team-lead.
[PATTERN] Scope-block drift from practice — letter-lags-practice (Finn variant) vs internal-contradiction (Brunel variant). Detection: cross-read MAY WRITE against (a) prompt's own body instructions and (b) accepted artifacts by that agent. Wiki #39 `patterns/scope-block-drift-from-practice.md` (Cal, n=3 including Aen's team-lead prompt addendum).
[LEARNED] Cross-cutting audit pays. "I'll just read 2-3 more prompts" caught the Brunel latent contradiction. Single-target prompt fixes should always include a 2-3 file sweep of structurally similar prompts. Codify as Celes discipline step when revising any scope block.
[LEARNED] (from Cal) Pattern vs decision classification: variants-as-symptom-location = pattern; variants-as-rejected-alternatives = decision. Absence of "alternatives considered and rejected" section is the tell.
[LEARNED] (from Cal) n≥3 evidence sections should identify the **load-bearing instance** — the one whose presence carries the generality argument — not present symmetric bullets. For #39, Aen's team-lead prompt was load-bearing because the gatekeeper's own gatekeeping drifting proves the pattern is not role-local.
[PATTERN] Prevention rule adopted into Onboarding Brief self-check: scope block is written LAST in any new prompt, after workflow/output-format/scratchpad-discipline are final. Every path referenced in those sections must appear in MAY READ or MAY WRITE. Same principle generalizes — summarizing/constraining/indexing sections must follow their source.
[DEFERRED] Team-lead (Aen) prompt has same latent gap: `prompts/aeneas.md:43` restricts `Edit/Write` to `memory/` + roster, but team-lead routinely edits prompts (Finn + Brunel this session, commit 04522c7 prior). Non-urgent per Aen's own message — queue for post-session prompt-revision pass. Not a simple MAY WRITE addition; Aen's prompt has a distinctive FORBIDDEN/ALLOWED structure and the fix needs to preserve the coordinator-only identity. Draft-revision approach needs its own design round, not same-shape mechanical fix.
[DEFERRED] Volta + Herald latent `docs/` gaps — YAGNI, fix only if/when they actually need to write digests. Per "design from observation" feedback rule.
[CLUSTER-OBSERVATION] Wiki #39 is the first Structural Change Discipline cluster member to span two gates (drafting + cross-read). Cal flagged this as cluster-health signal — if more multi-gate members emerge, the cluster may need re-tiering. Watch when drafting future prompts.

## Deferred (carried forward)

[DEFERRED] #48 (Oracle tier downgrade path) — paused per Task 3 directive, awaiting PO assessment. Now the last issue in the #48/#49 pair — #49 landed in v2.3.
[DEFERRED] `types/t03-protocols.ts` for Herald's inter-team protocols (work handoff, broadcast, transport layer). Principle is documented, implementation belongs to Herald when he formalizes T03's protocols.
[DEFERRED] Remaining FR specialist gaps: Isolation Analyst, Identity/Security Designer, Safety Architect, Observability Designer.
[DEFERRED] Comms-dev common-prompt needs Lovelace added to members list.
[DEFERRED] polyphony-dev common-prompt.md needs update: new agent names, author attribution `(*PD:<Agent>*)`, TDD workflow with new names.
[DEFERRED] hr-devs design files need two corrections before deployment: roster.json (remove medici+eilama) and common-prompt.md (members list). Optional: tighten arvo step 7 wording.

(*FR:Celes*)
