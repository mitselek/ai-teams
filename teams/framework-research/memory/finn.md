# Finn's Scratchpad -- framework-research

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S63 (2026-08-19) COMPLETE. All three S62 carry-forwards closed: resolvers promoted+verified, target-vs-instance filed, model-inventory REFRESHED (not archived). Scratchpad pruned this session (118 -> under 100; VEO-78 blocks compressed to hooks, detail lives in the two docs).
- **Active items:** none. **VEO-78 CANCELLED (closed 2026-08-11), struck by Aen (`f78a57c`); Atlassian MCP verified working.** Blocker sweep RUN + complete (S63d below) -- Aen made it a STANDING rule, so re-run it each session on every "blocked on X". Gate read-back DONE (->`partial`, 3 folds). **Open for someone with host access: 3-way model compare** (roster pin / stamped `~/.claude/teams/framework-research/config.json` / what each agent self-reports) -- settles my Instance-B conflict and is probably the roster-drift survey's real instrument.
- **Key decisions this session:** `tools/wiki-ref-audit.sh` was ALREADY DONE at S62 (tree beat the record, pessimistic direction). model-inventory was REFRESHABLE from the repo -- its flag overstated the blocker. T01 tiering rule HELD across a full model-name turnover (62% top-tier vs baseline 63%). **Aen ruled my 2 stale-record findings are ONE genus** -- submitted merged, n=3.
- **Cal FILED 2:** `patterns/key-expensive-verification-on-target-not-instance.md` (`high`, `stage-2: confirmed`) + `references/model-inventory-baseline.md` (moved OUT of `patterns/`, TTL 2026-09-30). **Both have corrections from me in flight** -- my 50% figure (wrong, see below) and his "back on-pin 2026-08-19" (falsified by Aen's spawn-override discovery).
- **Carry-forward:** **Cal-queue: 6 sent, unacked** (target-vs-instance; model-inventory refresh; **[DISPUTE]** shared-vocabulary L25; understated-progress n=3; gate-spec read-back w/ 3 folds; roster-drift 2 instances). `prompts/finn.md`: `tools/` is NOT standing-writable (Aen's interim ruling -- directed assignment only); 2 items queued with Celes, who is NOT SPAWNED (4 items on her, PO holds the decision). WATCHPOINT auto-restoration n=2.

---
## Session transcript (prune beyond line 100)

## [INDEX] Reference material
- `reference/rc-team/cloudflare-builders/` -- original RC team (11 agents); `reference/hr-devs/` -- evolved project team (9 agents)
- `topics/` -- 8 design topic files + T09 dev methodology + T10 guild + T11 deploy-lifecycle
- `designs/` -- deployed: apex-research, bigbook-dev, esl-legal, mvox, operator-role, po-team, uikit-dev; new: penrose, screenwerk, raamatukoi, backlog-triage, bioforge, esl-suvekool
- **`teams/framework-research/tools/wiki-ref-audit.sh`** -- MY tool, validated S63. Repo-root `tools/` does NOT exist; always the team-scoped path.

## [PATTERN] Compact -- retained from earlier sessions
- rc-team vs hr-devs: spawning (Agent tool vs spawn_member.sh), memory (flat vs +docs/), startup (hr-devs canonical), Medici (hr-devs more detailed)
- Team sizing: size = number of distinct abstraction boundaries, not deliverables
- Multi-Round Consensus: Seed → R1 binary → R2-3 refinement → R4 PO → R5 synthesis → R6 ACK (T09)
- docs/ vs topics/: `topics/*.md` = framework design (T01-T11); `docs/*.md` = external assessments, harvests, one-shot research
- Cal reclassifies: gotchas = traps to avoid; patterns = techniques to apply; **`references/` = TTL'd operational pointers (a TTL'd entry in `patterns/` is misfiled and WILL rot -- S63)**
- Cross-team harvest envelope: narrow brief + strict read-only; quarterly + on-demand cadence
- OSS-repo structural-survey: 6-section digest template; read top-level docs first, stop early
- Role-boundary discipline: when brief ambiguous on AGENCY, default to role-boundary constraint and flag
- Soft-verdict discipline: substrate-mapping brief → table of N options x {accommodates/additive/replacement}, not "recommended" verdict
- Cross-repo glance: confirm the citation before assuming inheritance (domain-language collisions common)

## [LEARNED] Operational rules -- active
- **Read-before-Edit:** Read in same or immediately-prior tool-call batch as Edit. (wiki 116)
- **Layer-0 library-first PRE-DRAFT:** load canonical-library skill BEFORE design-content write about external substrate. (wiki 119)
- **Inverted-trigger antipattern:** on poll-based substrates, design state-write-as-wake not push-as-wake. (wiki 120)
- **Retract-with-correction:** when new evidence falsifies a prior finding, explicitly RETRACT + correct; don't silently overwrite.
- **[S62c] Label every branch of a batched shell probe; `2>&1` not `2>/dev/null` when ABSENCE is the finding.** A negative you can't distinguish from a suppressed error is not a result. Verify a broken ref's HISTORY before calling it a typo -- rot has a cause and the cause is usually the real finding.
- **[S62g] A RE-GRADE RIPPLES -- grep the whole doc for downstream claims that leaned on the old grade.** Changing a verdict = grep every downstream assertion that cited it, incl. inside the section you just wrote. **[S62l corollary] When you correct a CONFLATION, check whether the prose you're correcting FROM already commits it.**
- **[S62g] Multi-part message => multi-part ACK, itemised BEFORE starting.** The checklist is the detector, not the memory.
- **[S62e] Ask for the DEPENDENCY'S LIFESPAN before grading an abstraction layer.** "Is this layer worth it?" is unanswerable without the horizon of what it wraps. A seam is not an investment IN X -- it's what makes X's removal cheap.
- **[S62e] Shape-preserving edits:** audit a fixed repeated shape MECHANICALLY (awk/grep the spine) after editing, don't eyeball it.
- **[S63] The fragile structure is often in a DIFFERENT part of the file from your edit.** Adding 10 header lines broke `--help`, which printed a fixed range `sed -n '3,30p'` far below. Fix = delimit on CONTENT (`3,/^# (\*FR:/p`) not line numbers. Generalises the S62e shape rule: any line-number or offset dependency is a shape dependency at a distance.
- **[S63] A closing record can be PESSIMISTIC, and that's the harder direction.** Aen recorded my tool as never-done; it was committed 2min before my kill. Nobody re-checks a task they believe is still open, so a too-pessimistic record rots longer than a too-optimistic one -- and acting on it would have MINTED A DUPLICATE (2nd audit tool at repo root). **Run the tool, don't trust the note that says it doesn't exist.**
- **[S63] A staleness flag that OVERSTATES what is blocked converts a doable refresh into a permanent one** -- and it rots in a way that looks accounted-for. model-inventory sat 40 days flagged "substrate truth, needs container access"; its own Provenance said repo-roster survey. 20-minute job. **When flagging decay, state precisely which HALF is blocked.**
- **[S63] Measuring beats citing.** Went to re-send an n=1 finding; measured our own corpus instead and got both a 2nd instance AND the load-bearing subtlety (string-key vs resolved-key). The measurement, not the memory, produced the best paragraph.
- **[S63] RE-RUN `date` BEFORE EVERY SendMessage. I forward-dated 4 message stamps by up to 40min** -- ran it once at 17:32, then extrapolated by feel. Self-reported to Cal (he was writing `discovered:` fields from my headers) and Aen. **This is the SAME defect I filed against apex at S62** (forward-dated `whenToUse` provenance) -- and worse, because apex had a run in flight and I had nothing; the stamps were never true at any instant. **Root cause = I treated a cheap check as a memory exercise.** Generalises: whenever the check costs ~0 and the recall FEELS equivalent, the recall is the error. Same shape as the model-inventory flag -- dismissing the check WAS the whole failure. **Aen made the IDENTICAL error independently (12 stamps, ~38min) and only checked because I self-reported -> n=3 across 2 teams/3 agents (me, Aen, apex `whenToUse`), survives independence.** Different CAUSES too (I skipped a habit, he skipped a rule he'd read that morning) = stronger evidence of a structural trap than one cause twice.
- **[S63, Aen's pairing -- submitted to Cal, `medium`] MEASUREMENT HYGIENE IS NOT ONLY "DID YOU MEASURE" BUT "DOES THIS NUMBER ANSWER THIS QUESTION."** **A NUMBER YOU DERIVED YOURSELF IS NOT THEREBY VERIFIED -- re-derive before re-asserting, especially your own** (my wrong figure survived review of a `high` submission precisely because I'd already computed it once). Two mirror sub-shapes, neither a measurement failure: **(A) failure to COLLIDE** -- Aen held my 561-refs/4-bases count in his own scratchpad AND read back the entry it falsifies, never bringing them together; **(B) failure to FIT** -- I applied a correct 651-vs-387 measurement to a claim with a different DENOMINATOR. **The nasty property: THERE IS NOTHING MISSING TO NOTICE.** Clean audit trail, wrong claim; any "did you measure?" process passes both. **This is the failure that SURVIVES installing the measure-first discipline.** My read: one genus, two sub-shapes; Cal decides.
- **[S63, Aen's ruling -- keep as a general rule] EVIDENCE FOR A MECHANISM AND EVIDENCE FOR A FREQUENCY HAVE DIFFERENT ADMISSIBILITY.** Correlated observations are FATAL to a frequency claim (n) and merely WEAK for a mechanism claim. So a self-demonstration is citable as illustration while explicitly NOT incrementing n. Resolves the tension between "awareness is not protection" (mechanism, wants the instance) and independence discipline (frequency, rejects it). Cite unnumbered.
- **[S63] THE REAL COST OF A FABRICATED TIMESTAMP IS LOST ORDERING, not a wrong date.** Aen's `[17:58]` (verified) and `[18:25]` (forward-dated, really ≤17:54) are **inverted by their stamps**. The `[18:25]` one UNBLOCKED my gate read-back; the `[17:58]` one said "holding is correct". By stamps: told to hold, then released. By real time: released, then advised to hold. I read back at 17:57. **Nobody can tell later whether I jumped the gun or he sent stale advice** -- and the record is all a future session gets. **A merely WRONG clock still sequences correctly (error common to all its stamps); independent fabrication at different unrecoverable magnitudes destroys sequence, and fails SILENTLY -- the stamps still look like stamps and still sort.** This is why decay ≠ fabrication: decay costs you a fact, fabrication costs you the ability to reconstruct what happened.

## [WATCHPOINT] auto-restoration-silently-overrides-explicit-state -- n=2, watching for n=3 -- Inst1: Edit-tool harness auto-restores file-state over Read-state. Inst2: Anthropic SDK auto-restores ANTHROPIC_API_KEY over explicit authToken. Candidates: CF Worker env auto-binding, AWS/GCP SDK auto-creds, git config --system vs --local.

## [DEFERRED] Open questions (consolidated)
- Polyphony roster redesign -- awaiting PO approval. #56: cost data + unavailability protocol (paused at R2).
- 6 Aalto questions from uikit-dev harvest (Q1+Q4 > Q6+Q3 > Q2+Q5). Jira/GitFlow Phase 2 -- HELD pending PO reconciliation.
- `prompts/finn.md`: co-source-agent role-expansion AND `tools/` write-scope -- both pending Celes.
- **[S60] capture-pane DIALOG sentinel token** -- Herald §1.3 gap. IDLE=`❯`, BUSY="shimmering", DIALOG NOT captured; needs a LIVE session, not repo-derivable. If a probe runs, capture all 3 states and hand to Herald.

## [CHECKPOINT] Shipped docs -- read the doc, don't re-derive
- **S39** `docs/team-os-context-mgmt-digest-2026-06-02.md` -- 12 mechanisms. Protocol A = format-enforcer analog; their no-inter-agent-coordination blind spot = our strength.
- **S44** `docs/2026-06-06-entu-consultant-grounding-digest.md` + `-data-lifecycle-competency-harvest.md` -- 18 claims C1-C18. entu/api=Nitro file-router; `_sharing`=create-time-only; no bulk API.
- **S59** apex #176 contamination-recovery eval -- SOUND recovery, OVERCLAIMED coverage (no detection arm). Filed → wiki #158 `patterns/detection-is-upstream-of-recovery.md`.
- **S60** `designs/new/po-team/research-precedent.md` -- PO chose LITERAL tmux driving. "No precedent" RETRACTED (Hopper WS3b = existence proof). `designs/new/migration-probe-harness/harness.sh` = reusable drive loop.
- **S62** `docs/veo-78-gateway-worker-assessment-2026-08-03.md` + `docs/veo-78-explainer-2026-08-03.md` (485 lines, doc-wide rewrite S62j). VEO-78 = NOT executable as written, 4 defects. Hooks: **identity hole was CLOSED PL/SQL-side, not by a Worker** -- lead argument moved to protocol abstraction + resilience. `aud` framing = **"session validated, audience NOT"**. **§6 = the design problem VEO-78 ACQUIRED** (Worker has no user session → service token → derive-identity converts BACK to trust-the-caller; alternative = confused deputy; neither designed, Kuzmin's call). ORDS+Oracle retire 1-3yr → abstraction = MIGRATION PATH not insurance. gateway-ttcms is Java/Spring, NOT a Worker. ADR-003 §5 shared token lib DOES NOT EXIST.
- **S62m** apex `reference-integrity-audit.js` review (report was the deliverable, no doc). RAN but never FILED. Best findings: **B3 truncation obligation unreachable by construction** (`verified_class` has no slot, File never sees `all`); `verified_class` was enum-FORECLOSED not forgotten (3 orthogonal axes in one field) -- split into class+disposition fixes both; forward-dated provenance in `whenToUse`.

## [CHECKPOINT S63] Three S62 carry-forwards CLOSED
- **wiki-ref-audit.sh -- was already promoted at S62** (14:20, 2min before the 14:22 kill); Aen's rescue file was written 16:59 believing otherwise. All 5 known gaps already closed (no temp files, subshell counter fixed, `--strict`, canonical-first base precedence, `git rev-parse` for CWD). **VERIFIED by running it**: 1347 prose resolved, 3 placeholders, 2 VOLATILE, 2 UNEXPANDED, **prose 5 BROKEN + 2 VOLATILE = 7 real breaks, matches S62 exactly**; fm 214 canon / 301 DRIFT / 51 BROKEN. Exit 1/2/0 correct, runs from subdirs, `bash -n` clean. Added the /tmp-is-C7 irony to the header (assignment's only genuinely-undone piece); that broke `--help`, fixed. Rescue file DELETED.
  - **Don't misread the base counts:** S62 said 183 reporoot / 245 wikiroot; tool says 214 canon / 301 drift. Not a discrepancy -- tool tries repo-root FIRST so dual-resolving refs land canonical. S62 counted bases independently.
- **target-vs-instance FILED to Cal** (pattern, cross-team, **`high` -- Cal granted it on the STRUCTURAL ground**, not the counts: *a verdict that is a pure function of the target makes per-instance verification redundant by construction*, checkable by inspection). Recorded my own S62 error (collision theory WRONG, `EXCLUDE` designs overlap out; bug upheld, lever wrong).
  - **[CORRECTED 18:06 -- MY figure was wrong, n=2 obtained by measuring apex's wiki (141 files, read-only).]** I claimed string-keying "captures only about half the available saving." **It captures 72.8% (ours) / 76.0% (apex).** I took the raw pair 651-vs-387 (a further 40% cut) and let it stand for a fraction with a different DENOMINATOR. **Surviving claim, n=2: string-keying leaves ~2x the necessary work** (FR 651/387=1.68x; apex 326/160=2.04x). Headline ratio unaffected + 3rd measurement: **3.5x-8x across 3 corpora** (FR wiki 1357/387=3.51x; apex wiki 852/160=5.33x; apex reference corpus ~8x).
- **model-inventory REFRESHED, not archived** (submitted to Cal; archive call is Aen's). **92 slots / 15 teams** vs 68/9. 33 sonnet-4-6 / 30 opus-4-6 / 16 fable-5[1m] / 10 opus-4-7[1m] / 2 ollama / 1 opus-4-6[1m]. **Top-tier 62% vs baseline 63% -- T01 tiering rule HELD while every model name turned over** (don't quote the 63→44.6% opus drop alone). **27/92 carry `[1m]`; baseline table has no column for it.** Baseline defects: ollama was 1, should be 2 (`eilama` in BOTH cb and hr-devs -- dedup-by-agent-name undercounts SLOTS); backlog-triage 4 not 6. hr-devs ref-vs-designs copies differ under `diff` but are **identical after CRLF normalization** -- Windows false positive, will recur.
  - **DESIGN-repo truth, NOT deployed.** Our own roster pins fable-5[1m] while S62 ran Opus 5 (3rd session off-pin) = in-repo roster-drift n=1 on US. `_substrate_note`: model field is documentation-only on Agent-tool arch. Only 5 of 7 `designs/deployed/` dirs carry model data, 2 in files `find -name roster.json` MISSES (`po-team/roster-design.md`, `operator-role/roster-entry.json`) -- so the ORIGINAL survey missed them too. **uikit-dev still uncountable 4 months after the baseline flagged it** -- record as a known limit, not a pending to-do.

## [CHECKPOINT S63c] **VEO-78 IS CLOSED -- the session's biggest find**
**Jira via Atlassian MCP 2026-08-19: `Closed`/done, archived 2026-08-11, updated 2026-08-18, 2 comments.** Our records said `Planned` / 2026-06-01 / 0 comments. **We carried a CLOSED ticket as our oldest outstanding commitment for 8 days**, across 3 sessions.
- **The "Atlassian-blocked" flag is what preserved it. The MCP WORKS -- 3 calls, no auth dance, <1 min.** Nobody tested the blocker. **The untested blocker didn't delay the work, it CONCEALED THAT THE WORK WAS CANCELLED.** Highest-stakes instance of my understated-progress finding.
- **New sub-shape:** the record was RIGHT IN FORM (we genuinely hadn't actioned it) and wrong only in the `blocked on X` clause -- **the one clause nobody audits, because it reads as an explanation rather than a claim.** Checkable status sat next to an unchecked reason in the same sentence. Cost = a PHANTOM COMMITMENT, not wasted effort.
- **Meets my own path-to-`high` criterion** (Aen authored the blocker, I found it, he never would have -- the claim discourages its own test). Told Cal; refused to argue my own entry up. Aen holds the call.
- **[RETRACTED 18:14] I had ALSO claimed Cal's model-inventory error met the criterion. Aen ruled NO and he's right: my genus is SUPPRESSION OF A CHECK, and Cal wasn't suppressed -- he CHECKED, just the wrong artifact.** Counting it would import a different mechanism = corrupts the claim, worse than padding. **I applied the independence test correctly 4x today then failed it once, in the direction that favoured me, on my own entry.** That's the useful part. Aen's line belongs at `artifact-claims-more-than-it-implements` (his side, written AFTER the override so never true) + `stale-snapshot` (Cal's side) -- **the junction where one agent's record becomes another agent's evidence**, which is where most of today's failures actually happened.
- **VINDICATION:** PO's archive note = my S62j conclusion in his words -- *"Abstraktsiooni idee ise ... on väärt hoidmist ja vajab edasist tõukamist"* (abstraction worth keeping, needs pushing). Returns as a NEW ticket vs VEO-140 menu line. Also *AR:Schliemann* 2026-08-18 refuted the ORDS-20.2-lacks-`:body` premise (`:body` since ORDS 2.0, `:body_text`/`:status_code` since 18.3; IBMU prod uses it today).
- **PATCHED both docs** (my write scope) with status banners: assessment = `[SUPERSEDED BY TICKET STATE]` + do-not-action; explainer = background-not-decision-aid. **Both banners carry the self-demonstration: my §2.1 diagnosed "staleness with no re-check gate" and the explainer header literally warned it would go stale -- both did, in 8 days, unchecked. WRITING THE WARNING IS NOT BUILDING THE CHECK.**

## [CHECKPOINT S63d] BLOCKER SWEEP -- run + complete (Aen approved as a STANDING rule: *a blocker is a claim with an expiry, and the only kind of claim that suppresses its own audit*)
**8 tested-TRUE / 1 reclassified / 5 untestable. VEO-78 was the OUTLIER, not the pattern -- the carry-forward is mostly honest.** Say that as loudly as the failure; a sweep that only ever finds falsehoods is measuring my bias, not the blockers.
- **TRUE:** `inter-team-comms` hardcodes `~/.claude/teams/framework-research/` at SKILL.md:34/71/83 and **that dir does not exist** (real = `session-a3a8047a`) · `sanitize-inboxes` SKILL.md:38 `configs[0]` -> **19 configs, resolves to `bigbook-dev`** = deterministically wrong, not a coin flip · `restore-inboxes.sh:44` passes `$PPID`=1 (its own :35 already warns) · Step 0.5 premise false (`team-lead -> model = None`) · **2 live claude.exe** (10496=`754a268c` idle 2.1.231; 33756=`a3a8047a` busy 2.1.235 = ours), both team dirs present · backgrounding fix unpatched (0 hits for "background") · **tunnels: 3 autossh.exe live** (21740/29520/24424) -- Aen's S61 claim HOLDS · Hopper/Medici/Celes/Volta absent from runtime config (4 members: team-lead, finn, callimachus, apex-research-courier).
- **RECLASSIFIED -- the best result. Step 3.5 is NOT wrong.** startup.md:193 documents the v2 `-SessionPid` upgrade completely INCLUDING the exact trigger (*"once a second team migrates... bare liveness can see >1 live session-<id>"*) and says v1 holds *"while FR is the only migrated team."* **That precondition has expired exactly as predicted and nothing fired.** Not a doc defect -- patching prose fixes nothing. **WRITING THE TRIGGER IS NOT OWNING THE TRIGGER; a precondition with no owner is indistinguishable from no precondition.** Inverse of VEO-78 (there: warning written, no check built; here: check specified to a TESTABLE CONDITION, still nobody ran it). Corrective action differs: assign an owner, don't edit.
- **"Blocked" vs "declined" is a real mislabel.** Nothing prevents spawning Hopper/Medici/Celes/Volta -- **the PO DECLINED them at Step 4.** A declined request is a DECISION, not a blocker: a decision looks permanent and stops being revisited; a blocker invites retry. Same for 4 of the 5 untestables (Polyphony, Jira/GitFlow, Aalto Qs, my 2 Celes prompt items) = **awaiting-decision, not blocked.** Only capture-pane DIALOG sentinel is genuinely untestable-from-here (needs live container tmux; correctly stated since S60).
- **TIGHTENING for the standing rule: the blockers that hurt most were TRUE WHEN WRITTEN.** All 4 mislabelled items were accurate at authoring; none decayed into falsehood -- **the world moved past the condition they named.** Re-reading would never catch that; only re-testing does.

## [CHECKPOINT S63b] Cal-queue: 4 submissions + 1 DISPUTE
- **understated-progress-suppresses-its-own-refutation** (gotcha, medium, n=3) -- **Aen's synthesis, not mine; he merged my two findings.** *A record that understates available progress suppresses the very action that would refute it.* Optimistic records carry their own trigger (someone tries to USE the work, fails loudly); pessimistic ones REMOVE it. 3 instances: record-says-not-done (`7f0209f`), flag-says-blocked (model-inventory), report-says-uncommitted (my own 18:03, 20min after Aen committed). **NOT a sub-case of `stale-snapshot-trusted-as-current`: that genus = DETECTION (can't tell it aged), this = DISPOSITION (you can tell, and the record says don't bother).** Remedy is WRITE-time: name what would refute it + how expensive the check is; decompose the blocker so the cheap half is visible. Marked `medium` deliberately -- 3 instances but one team/one session and 2 of 3 authored by the diagnosticians = n=1 on cross-team generality.
- **[DISPUTE] `shared-vocabulary-precondition-for-mergeable-fan-out` line 25** -- my THIRD S62 correction never got folded (Cal folded 2 of 3) and it falsifies the entry's mechanism. Line 25: *"a sequential agent CAN read its predecessor's output and converge on it."* **Our own frontmatter disproves it: 4 incompatible bases, NO fan-out, ONE sequential author (Cal is sole writer).** Real mechanism = *the value-space is never re-derived from prior instances at write time*; capability to converge ≠ convergence. **Fan-out is the FAST case, not the necessary one.** Did NOT ask for retraction (apex analysis is right); asked line 25 stop resting on parallelism. **Argued AGAINST promoting it to `high` on my own finding** -- 2 instances of the DIVERGENCE ≠ 1 observation of the CONSEQUENCE. Entry was already `stage-2: confirmed` (Aen's read-back) when Cal asked me for mine, so this is an amendment to a confirmed entry.
- Cal's 2 folded corrections VERIFIED correct (TRUNCATION-NOTE specified-and-dropped; C3/C4 orthogonality -- his *"resolvability and claim validity are independent axes"* is sharper than my thermometer line). Nit: entry says source is 89 lines, I counted 90.
- **`stage-2-confirms-filing-gate` READ-BACK DONE** -> `pending`->`partial` (Herald outstanding, unspawned). **NO dispute** -- all 3 findings are internal inconsistencies/broken mechanism, none contests what the gate IS = the fold branch of its own line 64. **Fold 1 (serious): the documented audit cmd `grep -rl 'stage-2: pending'` is UNANCHORED -- returns 22 vs 8 real.** 14 false +ves are STRUCTURAL: 8 are `cards/INDEX.md`+`index.md`, i.e. the Tier-2 surfacing layer the gate names as its own enforcement arm -- **the better the surfacing works, the noisier the audit gets.** Fix = `grep -rlE '^stage-2: pending'` in 3 places. Corrected Aen: his "grep now returns the 7 live obligations" was asserted WITHOUT opening the consumer -- the same error he was mid-correcting (2nd-ORDER instance, not a 4th expression). Fold 2: line 65 still says fail-closed to `pending` for "older entries", contradicting line 101 -- the amendment added the state and left the rule that manufactures the problem. Fold 3: lines 82 vs 99 give two incompatible accounts of how the 39 became pending; can't resolve, wasn't there.
- **CONFIRMED-not-flagged:** `legacy-unaudited` is a value whose whole function is to be EXCLUDED from a query -- legitimate, and the INVERSE of TRUNCATION-NOTE (there a sentinel needed a consumer and had none; here having no consumer IS the design). Applying the no-slot lesson mechanically would add a consumer it doesn't need.
- **[S63 -- NO-SLOT IS THE SESSION'S RECURRING SHAPE, 3x]** (1) `TRUNCATION-NOTE` had no slot in `VERDICT_SCHEMA`; (2) `pending` carried TWO meanings (live obligation / historical description) with no slot for the second -- Cal resolved Fold 3 by MEASURING **39 multi-author / 0 single**, which refuted the fail-closed account and confirmed three-bucket; (3) **"nobody outside the causal chain has assessed this" has no field at all.** Cal filed the timestamp entry `stage-2: confirmed` (justified on its face: each co-author self-reported their own instance unprompted = read-back in substance). **I verified and BACKED CAL AGAINST AEN** -- per Aen's own S63 finding, `confirmed` certifies a co-author READ it, NOT that it's correct, so his `pending` request asked the field to carry a meaning he'd already shown it lacks. **Fix = leave the value, put the gap in PROSE.** General: a true useful thing about an artifact with no schema slot either gets crammed into a field meaning something else, or evaporates.
- **Instance B RESOLVED by Aen:** he spawned Cal + me with an unrecorded family-level `model: "opus"` override = my option (a). **Better finding underneath: `team-lead.model` is `null` in `config.json`, so startup.md Step 0.5's premise ("parent session model is stamped into runtime config.json") is FALSE on 2.1.235** -- the check can't be performed as written. Volta's. **That instance was authored by someone who did NOT go on to find it = meets my path-to-`high` criterion, but for the DRIFT entry, not the merged one.**
- **Roster-drift: `celes` UNREACHABLE** (not spawned, no inbox) -> routed to Cal as Protocol A instead of holding it, because holding a measured finding until its recipient exists IS today's failure mode. **Instance A: roster pinned fable-5[1m] throughout while S60-62 ran Opus 5 and S63 ran Fable again -- roster accidentally right at both ends, wrong in the middle, never edited. A MATCHING ROSTER IS NOT EVIDENCE OF A CONTROLLING ROSTER.** `_substrate_note`: the field is documentation-only, cannot control what runs, so drift is the expected steady state. **Instance B (flagged uncertain): my pin is fable-5[1m], my runtime reports `claude-opus-5`, contradicting Aen's "S63 ran on Fable 5".** 3 readings, all drift, differ only by layer; settle by 3-way compare (roster pin / stamped `config.json` / what each agent reports). Argued AGAINST promotion -- new axis, but still FR-authored, so it doesn't touch the axis confidence is pinned to.

(*FR:Finn*)
