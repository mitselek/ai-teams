# Finn's Scratchpad -- framework-research

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S62 COMPLETE -- VEO-78 assessment + explainer shipped, source-verified, then FULLY REWRITTEN doc-wide (S62j) on the falsified foundation. 485 lines, 8 sections.
- **Active items:** none blocking. Awaiting Cal's reply on roster-drift confidence + on the 2 new S62m submissions. PO may return with questions on the rewritten explainer. **Scratchpad is at ~108 lines -- PRUNE AT SHUTDOWN** (VEO-78 blocks S62/b-d-e/h-i are the compression candidates; detail lives in the two docs).
- **Key decisions this session:** VEO-78 = NOT executable as written (4 named defects). Aen's hypothesis partly FALSIFIED: Q4 answerable in-repo; 90/10 by effort but ~30/70 by RISK. No deployed team fits. **`aud` finding CORRECTED to "session validated, audience not"** (Aen) -- use that framing everywhere.
- **Carry-forward:** Cal-queue EMPTY -- 3 filed + amendments folded (wiki 171). **DEFERRED to next session (Aen carries in boot block): n=2 roster-drift survey** -- any deployed team whose live roster differs from its own design-repo roster (`apex-research` w/ design-vs-container, `uikit-dev`, `bt-triage`, `comms-dev`, `mvox`, `screenwerk`); one unrecorded delta promotes roster-drift medium->high. **Do NOT start unasked.** Older DEFERRED queue below. WATCHPOINT auto-restoration n=2.

---
## Session transcript (prune beyond line 100)

## [INDEX] Reference material
- `reference/rc-team/cloudflare-builders/` -- original RC team (11 agents); `reference/hr-devs/` -- evolved project team (9 agents)
- `topics/` -- 8 design topic files + T09 dev methodology + T10 guild + T11 deploy-lifecycle
- `designs/` -- deployed: apex-research, bigbook-dev, esl-legal, mvox, operator-role; new: penrose, screenwerk, raamatukoi, backlog-triage, po-team
- `designs/new/po-team/` -- research-precedent.md (mine, §1-6), protocols.md (Herald)

## [PATTERN] Compact -- retained from earlier sessions
- rc-team vs hr-devs: spawning (Agent tool vs spawn_member.sh), memory (flat vs +docs/), startup (hr-devs canonical), Medici (hr-devs more detailed)
- Team sizing: size = number of distinct abstraction boundaries, not deliverables
- Multi-Round Consensus: Seed → R1 binary → R2-3 refinement → R4 PO → R5 synthesis → R6 ACK (T09)
- docs/ vs topics/: `topics/*.md` = framework design (T01-T11); `docs/*.md` = external assessments, harvests, one-shot research
- Cal reclassifies: gotchas = traps to avoid; patterns = techniques to apply
- Cross-team harvest envelope: narrow brief + strict read-only; quarterly + on-demand cadence
- OSS-repo structural-survey: 6-section digest template; read top-level docs first, stop early, never source files unless dir listing insufficient
- Role-boundary discipline: when brief ambiguous on AGENCY, default to role-boundary constraint and flag
- Soft-verdict discipline: substrate-mapping brief → table of N options x {accommodates/additive/replacement}, not "recommended" verdict
- Cross-repo glance: confirm the citation before assuming inheritance (domain-language collisions common)
- Pre-scaling baseline: structurally near-zero at n=1 team; push back, measure post-scaling

## [LEARNED] Operational rules -- active
- **Read-before-Edit:** Read in same or immediately-prior tool-call batch as Edit. Don't trust Read-state across rounds. (wiki 116)
- **Layer-0 library-first PRE-DRAFT:** load canonical-library skill BEFORE design-content write about external substrate. (wiki 119)
- **Inverted-trigger antipattern:** on poll-based substrates, design state-write-as-wake not push-as-wake. (wiki 120)
- **Co-source-agent role:** research coordinator substantively shapes design output + co-authors wiki entries with Cal.
- **Stage-2 author-side correction chaining:** post-submission data broadening mechanism -> surface pre-filing even if Cal accepted prior correction.
- **Retract-with-correction:** when a prior finding is falsified by new evidence (S60 "no precedent" → strong precedent), explicitly RETRACT + correct in the doc, don't silently overwrite. Aen flagged this as exactly right.
- **[S62c] Label every branch of a batched shell probe; use `2>&1` not `2>/dev/null` when ABSENCE is the finding.** Unlabelled `ls A/; ls B/` gave one block I attributed to the WRONG dir -> shipped "different directory" as fact. A negative you can't distinguish from a suppressed error is not a result. **And: verify a broken ref's HISTORY (`git log --diff-filter=D`, `git show <sha>:<path>`) before calling it a typo** -- rot has a cause and the cause is usually the real finding.
- **[S62g] A RE-GRADE RIPPLES -- grep the whole doc for downstream claims that leaned on the old grade.** After promoting protocol abstraction (insurance -> migration path) I left TWO contradictions: §4.1 "timeouts and circuit-breaking yes" and piece-4 "circuit-breaking safe regardless" -- both written when circuit-breaking was recommended, both surviving into a doc that now recommends DROPPING it. Aen asked me to audit; the audit is what caught them, not the editing. **Rule: changing a verdict = grep every downstream assertion that cited it, incl. inside the section you just wrote.** Also killed a stale "= insurance" line in THIS scratchpad the same way. **[S62l corollary, Aen] When you correct a CONFLATION, check whether the prose you're correcting FROM already commits it** -- the digest sentence describing the two-boundary error said "relative to its OWN reference config" about roles that live in cloudflare-builders, i.e. contained the error in miniature. The confusion that produced a finding tends to have shaped how it was first written down.
- **[S62g] Multi-part message => multi-part ACK, itemised BEFORE starting** (common-prompt, Aen enforced). I applied both items of a 2-part msg but acked implicitly; an implicit ack can't surface a drop. The checklist is the detector, not the memory.
- **[S62e] Ask for the DEPENDENCY'S LIFESPAN before grading an abstraction layer.** I graded rumba's protocol abstraction "insurance, not a present need" -- correct given what I knew, WRONG once PO said ORDS+Oracle retire in 1-3yr. Abstraction over a doomed dependency isn't insurance, it's the MIGRATION PATH; re-graded weakest -> 2nd-strongest. **Generalisable: "is this layer worth it?" is unanswerable without the horizon of what it wraps.** Corollary trap: "X is going away" reads as "don't invest in X", but a seam is not an investment IN X -- it's what makes X's removal cheap. Ask the horizon EARLY; it re-grades cost/benefit across a whole doc.
- **[S62e] Shape-preserving edits:** when a doc's value is a FIXED REPEATED SHAPE (5 chunks x same 4 lines), an Edit whose old_string spans a shape line silently drops it. I dropped `**Owner.**` from piece 5. **Audit the shape mechanically after editing (awk/grep the spine), don't eyeball it** -- the whole point of the shape is that a tired reader pattern-matches it, so one missing line costs more than its content.

## [WATCHPOINT] auto-restoration-silently-overrides-explicit-state -- n=2, watching for n=3 -- Inst1: Edit-tool harness auto-restores file-state over Read-state. Inst2: Anthropic SDK auto-restores ANTHROPIC_API_KEY over explicit authToken. Candidates: W3a CF Worker env auto-binding, W3b AWS/GCP SDK auto-creds, W3c git config --system vs --local.

## [DEFERRED] Open questions (consolidated)
- Polyphony roster redesign -- awaiting PO approval. #56: cost data + unavailability protocol (paused at R2).
- 6 Aalto questions from uikit-dev harvest (Q1+Q4 > Q6+Q3 > Q2+Q5). Jira/GitFlow Phase 2 -- HELD pending PO reconciliation. `prompts/finn.md` co-source-agent role-expansion -- pending Celes review.

## [CHECKPOINT S39] Team OS digest -- SHIPPED (pruned) -- `docs/team-os-context-mgmt-digest-2026-06-02.md` (12 mechanisms). Hooks: Protocol A = format-enforcer skill analog; plan persistence = real gap; their no-inter-agent-coordination blind spot = our strength. Read the doc.

## [CHECKPOINT S44] Entu consultant-agents grounding -- SHIPPED (pruned) -- docs/`2026-06-06-entu-consultant-grounding-digest.md` + `-data-lifecycle-competency-harvest.md` (18 claims C1-C18 -> designs/new/entu-consultant-agents/). Hooks: entu/api=Nitro file-router; _sharing=create-time-only; no bulk API; verification = FIVE methods, per-evidence stance field.

## [CHECKPOINT S59] apex #176 contamination-recovery eval -- COMPLETE -- Verdict: SOUND recovery, OVERCLAIMED coverage (no detection arm). 10 failure modes + 8 fixes to team-lead. **Lesson FILED Protocol A -> Cal wiki #158 `patterns/detection-is-upstream-of-recovery.md`** (Stage-2 CONFIRMED). Principle: detection structurally upstream of recovery; recovery-without-detection = dominant contamination risk at scale.

## [CHECKPOINT S60] PO-team research -- SHIPPED (pruned S62; detail in doc + Herald protocols.md) -- `designs/new/po-team/research-precedent.md`. PO chose LITERAL tmux driving. Recall hooks: first-pass "no precedent" = RETRACTED (Hopper WS3b probe = existence proof); crash class = 2 modes, both avoidable driving an IDLE session (residual trigger = send-keys onto a permission dialog, Herald §1.2 covers); `designs/new/migration-probe-harness/harness.sh` = reusable drive loop; tmux>screen. Herald folded all 3 reinforcements into protocols.md.

## [DEFERRED S60] capture-pane DIALOG sentinel token -- acceptance-test deliverable -- Herald §1.3 gap. PROVISIONAL given (probe scope doc `teams-migration-probe-container-scope-2026-06-17.md:89`): IDLE=`❯` prompt; BUSY="shimmering" indicator. DIALOG sentinel NOT captured -- version+dialog-type dependent, needs a LIVE session (over-real-ssh acceptance test / WS3b harness re-run), not repo-derivable. If a probe runs: capture all three exact states (esp. DIALOG permission-prompt `capture-pane -p` dump), hand to Herald.

## [CHECKPOINT S62] VEO-78 Gateway Worker assessment -- SHIPPED
`docs/veo-78-gateway-worker-assessment-2026-08-03.md`. Recall hooks (read the doc, don't re-derive):
- **4 defects:** (1) hidden day-one blocker P3/Q5 = how does the Worker auth TO ORDS? If ORDS is behind CF Access w/ user-session policy, Worker can't call it at all -> Q5 is prerequisite not hardening. (2) DoD#3 "or document why not" = unfalsifiable escape hatch on a HIGH/blocking Anderson finding. (3) DoD#4 below ADR-013/APP-12 (ADR drafted 2026-06-09, EIGHT DAYS AFTER ticket) -- and "add retry" on a non-idempotent Oracle write = double-write trap. (4) DoD#1 silently passable while wrong: both in-org exemplars verify issuer ONLY, no `aud` -> copying org code violates ADR-003.
- **Q4 answered in-repo:** issuer `https://eestiraudtee.cloudflareaccess.com` (dev-toolkit/examples/messageboard/wrangler.jsonc:33), JWKS `${issuer}/cdn-cgi/access/certs` (hr-platform hooks.server.ts:30). Real lookup needed = app AUD tag, which ticket never asks for.
- **gateway-ttcms is Java 23/Spring Boot**, NOT a Worker (role-level analogy only, day-loss trap). **ADR-003 §5 shared UI-kit token lib DOES NOT EXIST** (verified evr-ui-kit) -> VEO-78 = 3rd divergent impl.
- **Team mapping:** no deployed team fits. cloudflare-builders = best roster on paper (has harmony=S2 exactly) but NOT deployed + oversized 3x; apex-research = ORDS supplier not owner. New team = ~4. **APP-12 resilience is UNOWNED org-wide.** **[S62k] Digest §6 row + §8 CORRECTED to the 3-way reading** (harmony/piper=loss, alex=correct specialisation, medici/eilama=2nd boundary) + the mechanical discriminator; also fixed a precision error -- harmony/piper are absent vs `cloudflare-builders`, NOT vs hr-devs' own reference config.

## [CHECKPOINT S62b/d/e] VEO-78 PO explainer -- SHIPPED (detail in the doc; hooks only)
`docs/veo-78-explainer-2026-08-03.md`. Comprehension doc for the PO, written to be interrogated -- no team-competence content, no recommendation.
- **`aud` framing (Aen correction, use everywhere):** "session validated, audience NOT" -- exemplars DO call get-identity (real server-side check). `iss`=WHO signed; `aud`=WHICH APP's door. Missing `aud` => keep authentication, lose PER-APP authorization.
- **Cookie-vs-header is not pedantry:** injected `Cf-Access-Jwt-Assertion` = edge already enforced the door; `CF_Authorization` COOKIE = client-supplied bearer where `aud` is the ONLY binding. Ticket names the COOKIE.
- **"Hiding ORDS names the MECHANISM, not the PROPERTY"** -- hide it perfectly, forward a client-supplied email, fix NOTHING. **DoD discipline:** never write "or document why not"; use artifacts that exist-or-don't (named clauses + named accepter; 2+ consumers; countable list).
- **[S62e] LOAD-BEARING: ORDS+Oracle NOT in target stack, 1-3yr obsolescence** -> abstraction over a doomed dependency = MIGRATION PATH not insurance. "ORDS being doomed is the strongest argument FOR the Worker." Rule must be STACK-NEUTRAL ("system of record") -- a rule written about ORDS dies with ORDS.
- **[CALIBRATION] Verify the commit MESSAGE before ascribing intent from the DIFF** (827f542 de-link = documented tradeoff, not cover-up). Also: cite ADRs BY NUMBER -- IDs stable, paths are not.

## [CHECKPOINT S62h/i] rumba SOURCE VERIFICATION -- §1 claim PARTIALLY HOLDS (1st pass `gh api`; re-done on the local clone @ 5a5030e)
`Eesti-Raudtee/rumba` main, pushed 2026-07-29. **Repo has MOVED HARD since the ticket** -- pnpm monorepo apps/{home,sample,vjs} + packages/shell; "static-assets-only serving ./public" is FALSE on main.
- **HOLDS:** browser->ORDS is still DIRECT cross-origin. `api/menu.ts:34` fetch(apexBase+'vjs_guard/rumba/menu'), apexBase=`https://vjs.evr.ee/nvagun/` (config.ts:8). No gateway.
- **STALE/FALSE:** email-in-URL-path is GONE. No email in ANY constructed URL. Identity rides the CF Access COOKIE (`credentials:'include'`) and is derived SERVER-SIDE by the ORDS handler (vjs_db_vjs_guard `ords/rumba.sql`). README: "Menu is pulled from ORDS by Cloudflare Access identity." **The client-supplied-identity hole §1 is built on was closed -- PL/SQL-side, NOT via a Worker.** `/wrapper/submit` + `/wrapper/menu` not called from rumba at all.
- **Workers ALREADY EXIST but NO gateway:** apps/vjs/wrangler.jsonc:15 main=_worker.js, adapter-cloudflare, ZONE routes rumba.dev.evr.ee/vjs + /vjs/*; home owns /*. But apps/vjs is `ssr=false`, no hooks.server.ts, no +page.server.ts -> Worker serves the shell, does NOT proxy ORDS.
- **P3/World-A-vs-B PARTLY ANSWERED FROM SOURCE:** menu.ts:31-47 `redirect:'manual'` + opaqueredirect => "CF Access on an expired session 302s to the IdP login". **ORDS IS behind CF Access w/ a user-session policy = World B for browser traffic.** Doesn't settle the Worker path (no user session) but makes the service-token question REAL not hypothetical.
- **`aud` argument UPGRADED inference->verified:** config.ts:31-34 "vjs.evr.ee is a SEPARATE CF Access app from the rumba shell", cross-app redirect REJECTED by CF on dev. Multiple Access apps in the team = the precondition my §2.3 hole depends on.
- **Supports the gateway case:** menu.ts:22 hand-rolled `MENU_PULL_TIMEOUT_MS=10_000`+AbortController in the CLIENT = exactly the per-caller resilience duplication APP-12 says to standardise once at the gateway. And `/new-module` is a live strangler-fig demo.
- **[S62i] PER-APP (local clone, all 3 checked):** ORDS/Apex code lives ONLY in **apps/vjs** (= the rumba the ticket means; embeds Apex 600 iframe). apps/home + apps/vjs are `ssr=false`, ZERO server files. **apps/sample HAS server code** (hooks.server.ts + 3 *.server.ts) => an app in this monorepo is ALREADY more than static assets. packages/shell = chrome only (exports RumbaShell + 2 types), NO ORDS. **No `public/` dir exists; all 3 wrangler.jsonc declare main=_worker.js** -> ticket Q1 premise "static-assets-only serveerib ./public" is FALSE, and Q1's A-vs-B framing (one static app vs separate repo) is superseded by a 3rd shape: another app / server-route inside the existing monorepo. **Do NOT resolve Q1 -- Aen/PO call.**
- **[S62i] §2.2 (ui-kit has no auth) HOLDS -- but I nearly shipped it on stale evidence.** My local evr-ui-kit clone = **v0.4.0 / 2026-06-16**; rumba pins **^0.10.0**. Re-verified at evr-ui-kit `main` (v0.10.0): no auth-named files, no jose/jwt dep, code-search jwtVerify / createRemoteJWKSet / CF_Authorization = 0/0/0. rumba itself: zero matches for jose|JWKS|CF_Authorization|aud. UI-kit shell integration brought CHROME not auth (types.ts:42 "identity" = a header SLOT the app fills). **LESSON: a local clone is a SNAPSHOT -- check its version against what the consumer PINS before citing it as current.**
- **VEO-78 not dead, but its LEAD ARGUMENT changed:** identity no longer the driver (already fixed elsewhere); protocol abstraction + resilience now carry it.
- **[S62j] DOC-WIDE REWRITE SHIPPED** (not a §1 patch -- Aen scoped it as a reconciliation triggered by the largest re-grade the doc has taken; my own "a re-grade ripples" rule applied to the foundational fact). 485 lines. New §3 = 3-state trust chain (PoC history / TODAY verified / Worker=open). New **§6 = the design problem VEO-78 ACQUIRED** -- marked REASONING NOT VERIFIED: today's safety DEPENDS on the user's CF cookie reaching ORDS; a Worker has no user session -> service token -> ORDS sees the WORKER not the user -> Worker must pass identity explicitly and ORDS must TRUST it = derive-identity converted BACK to trust-the-caller (same SHAPE as the PoC defect, better anchor). Alternative = Worker forwards user cookie = CONFUSED DEPUTY. Both legit, neither free, **neither designed**. Kuzmin's call, NOT resolved by me. **"VEO-78 was going to close a hole; it now has to avoid opening one."**
- **[S62j] Piece 2 RENAMED** "the security fix" -> "the gateway seam" (it no longer fixes anything) + now BLOCKED on §6. Piece 1 got MORE valuable (ORDS already implements the rule unwritten -> record it before someone regresses it). Piece 5 = only piece unblocked AND stack-independent. §2.3 `aud` hedge DISCHARGED by config.ts:31-34. §4.5 = Q1 premise dead (facts stated, Q1 NOT resolved). Provenance convention [verified]/[reported] + commit 5a5030e stated at top.

## [Cal S62] 3 items FILED + amendments folded -- wiki 171. Read the entries, not this.
`patterns/roster-drift-from-reference-capability-register` (medium) · `gotchas/gap-citation-acquires-hidden-dependency-on-closure` (medium) · `gotchas/citation-orphaning-by-housekeeping-sweep` (HIGH) · Aen's `patterns/stale-snapshot-trusted-as-current` = the GENUS of the last two (my #7/#8 are instances; do-not-merge upheld).
- **Discriminator I contributed (now in the entry):** *does the shed role's own prompt cite the shedding team's domain?* harmony.md:13/15/22 (jose+JWKS, `hr-platform/sync/`, HR group IDs) = LOSS; alex.md:13-15 (apex/vjs paths) = CORRECT SCOPING. Two boundaries: cb(12)->hr-devs ref(9) and ref(9)->DEPLOYED(7, dropped medici/eilama).
- **Confidence held `medium` and I agree:** I was treating "more evidence" and "more confidence" as the same axis; they are not. Both deltas are ONE team's authorship -> n=1 on the axis that matters (cross-team generality).

## [CHECKPOINT S62m] apex `reference-integrity-audit.js` review -- SHIPPED to Aen (no doc; report is the deliverable)
Source: `Eesti-Raudtee/apex-migration-research` `.claude/workflows/reference-integrity-audit.js` @ `07d272f5` (90 lines, *AR:Schliemann*, 2026-08-12 10:43Z). 3 phases Harvest(6 scanners)/Verify(batch 25)/File(1 opus -> GH issue), C1-C8 taxonomy.
- **RUN STATE: launched, never filed.** issue #186 (10:29Z) says audit "(running)"; script committed 10:43Z; at 11:02Z no audit issue exists (searched `reference-integrity` + `audit in:title`, all states). Alive-vs-dead NOT distinguishable from outside. Corrected Aen's "committed but unexercised".
- **`whenToUse` contains `'First run: S68 2026-08-12 -> GH issue'` = FORWARD-DATED PROVENANCE** -- artifact asserts its own run output before the run finished. This replaced Aen's "workflow-as-committed-artifact" candidate, which I REJECTED (same repo committed 3 skills 3wk earlier; promotion-to-repo-asset is an existing habit, not novel).
- **B3 truncation finding (strongest):** producer obligation (`TRUNCATION-NOTE`) + consumer obligation ("silent caps forbidden") both present; `VERDICT_SCHEMA.verified_class` = `'C1..C8, or FALSE_POSITIVE'` has NO SLOT, and File gets only `results`, never `all`. **Obligation unreachable by construction.** S61 gauge lesson one layer out: not "can't distinguish 2 states" but "informative signal can't traverse the pipeline".
- **Enum critique re-diagnosed:** `verified_class` was not enum-FORGOTTEN, it was enum-FORECLOSED -- one field carries 3 orthogonal axes (class / is-it-real / did-we-truncate). **Fix = split into class + disposition, which ALSO fixes B3.** One correction, two defects.
- **Corrected Aen twice:** (a) token -- committed string is `$ATLASSIAN_API_TOKEN`, SHELL expands it, so say "may surface" not "will"; the worse bug is the hardcoded accountId used as an authorship FILTER -> stale ID yields zero candidates = "surface clean" (S61 gauge failure, correctness not secrets). (b) "not idempotent" is the wrong defect -- per-run issue is RIGHT for a point-in-time map-only audit; the real miss is NO RUN-TO-RUN DIFF, so run 2 can't answer "did we fix anything", the only question quarterly cadence exists for.
- **Dedup: cost bug upheld, lever wrong.** key `location+ref_text` almost never collides (cross-scanner overlap designed out via `EXCLUDE`). Real multiplier = **verify keyed on citation INSTANCE not TARGET: ~1900 instances -> ~230 distinct targets = 8x.** Fix = resolve target once, fan verdict back out.
- **B2 taxonomy-const: claim HOLDS, framing too narrow.** Parallelism is not the active ingredient -- my own frontmatter data is the counter-case (4 private bases, SEQUENTIAL authors, no fan-out). Restate: *any field written by many authors + read by one aggregator needs its value-space declared at one point.* Fan-out is the fast case, not the only one.

## [CHECKPOINT S62m] FR wiki measured against apex's own checks (read-only; scripts /tmp/res.sh + /tmp/fm.sh)
- **Aen's premise "FR has [[wikilink]] refs" is WRONG and it changed the verdict: 42 wikilink occurrences / 14 distinct targets, whole wiki.** C4 = apex's flagship class = near-empty here. Our layer is 1347 inline links + 561 frontmatter refs.
- Prose HEALTHY: 1347 links, 10 unresolved, 3 = template placeholders in `wiki-cross-link-convention.md` itself. **7 real breaks** (best: po-team MOVED `designs/new/`->`designs/deployed/`; 2x `file:///Users/...` C7 in `patterns/sub-shape-e-at-design-domain.md`; 4 wrong-subdir; 1 in an UNTRACKED file from this session = we're still minting them).
- **Real defect is the layer apex does NOT scan: frontmatter `related:` has 4 incompatible bases** -- repo-root 183 (33%) / wiki-root 245 (44%) / same-dir 43 (8%) / citing-dir 16 (3%) / unresolvable 50 (9%). **The 33% is the style `wiki-cross-link-convention.md:81` PRESCRIBES**, and :81's durability claim is false for the other 67%. :54 says frontmatter is out of scope while :81 governs it. :81 ends "Audit when the structure shifts" -- **we wrote the trigger, never built the instrument.** ~35 of the 50 point into OTHER repos in a field with no repo slot (2 literal unexpanded `$REPO/` committed).
- **VERDICT: adopt the cheap 20% (2 resolver scripts + ONE base ruling), decline the fan-out.** Do not port a 6-scanner opus pipeline to audit 7 broken prose links.
- **"We found the disease, they built the thermometer" = MOSTLY TIDY, NOT VINDICATING.** Sorted our 2 gotchas against C1-C8: only the sweep gotcha's *already-repaired* in-repo half is catchable. Its load-bearing halves (out-of-repo citations; `C2` naming 3 things) and ALL of cite-the-gap are semantic -- a resolver only finds the announcing kind. Our own entry says it: *"a dead link announces itself; a live link to the wrong thing does not."*
- **LIVE PROOF on our surface:** `decisions/courier-must-runtime-discover-team-name.md:50` says "CLI is currently pinned at 2.1.177" citing `[[project_claude_cli_pinned_2177]]` -- memory file EXISTS, its description now reads "autoupdater ENABLED, CLI=2.1.193 as of S59". **Link resolves, claim is dead.** No C1-C8 scanner flags this. Both our gotchas are this class.
- Filed 2x Protocol A to Cal: truncation-in-transit gotcha; frontmatter-resolution-base gotcha (incl. the :54-vs-:81 self-contradiction he owns as sole writer). 7 prose breaks handed to him, NOT fixed by me.

## [GOTCHA S44 + S62f] Crossed messages -- a VERIFICATION can be stale, not just a task (n=4 in one session)
Classic: task_assignment replays surface late with ORIGINAL timestamp -> check status first, send a no-op confirmation, do NOT redo. **S62f new direction: a teammate GREPS mid-write and reports your finished work MISSING.** Correct response = re-run THEIR check, report evidence, never redo -- for ADDITIVE edits a wrong redo DUPLICATES rather than no-ops.
**Sender-side fix (Aen adopted): point at the message, don't grep the file** -- removes the false negative at source. Keep BOTH: mine catches what his fails to prevent; his only fires when the sender remembers. **Bounded: pointer-over-check only when the check is cheap AND the false negative is expensive.**

(*FR:Finn*)
