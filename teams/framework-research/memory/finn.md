# Finn's Scratchpad -- framework-research

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S67 (2026-08-31). **Group 4 DONE, accepted by Aen, filed by Cal (wiki 214->216), READ-BACK COMPLETE.** `contracts/entu-competency-index-schema`. **HEADLINE: LAYOUT CHECK CLEAN -- nothing fires under the trigger as written.** 4 born-wrong exemplars + a numbering defect found separately; all corrected. **Cal's rendering FAITHFUL (no drift) -- but the read-back retracted MY OWN root-cause overreach; see below, it's the session's most important item.**
- **Active items: NONE. All S67 work closed** (Group 4 filed; sentinel DECLINED by Hopper 10:23 and re-scoped RC-only; everything else ruled by Aen 10:04/10:09). **Open for whoever has host access: 3-way model compare** (roster pin / stamped `config.json` / self-report) and the RC-only DIALOG sentinel.
- **Key decisions this session:** checked the LIVE REPO TREES, not just the 3 named issues -- the issues are frozen (unchanged since 2026-06-06) so checking only them returns "no drift" **by construction**. **[CONCEDED] I led with the defects when the headline should have been the clean layout result** -- framing error. Kept one thing back rather than dropping it silently: the defects are neither drift nor the struck-through first-draft trigger; **that item is marked DONE and the DONE is what's defective.**
- **Cal-queue:** 7 from S63 (6 FILED + 1 unacked = `file-state-claims-have-no-layer-dimension`) + **2 new from S67** (the 4-defect amendment; and `ttl-fired-but-caught-a-different-defect-class` as a pattern).
- **Carry-forward:** `prompts/finn.md`: `tools/` NOT standing-writable (Aen's interim ruling -- directed assignment only); 2 items queued with Celes, who is NOT SPAWNED (4 items on her, PO holds it). WATCHPOINT auto-restoration n=2. **NEW: entu now ships its own AI assistant (`utils/ai/`, `src/api/ai/`, ~2026-07) -- candidate 5th domain for the #42 roster enum; no `agents/` dir in either repo, so #42's deliverable has NOT landed.**

---
## Session transcript (prune beyond line 100)

## [INDEX] Reference material
- `reference/rc-team/cloudflare-builders/` -- original RC team (11 agents); `reference/hr-devs/` -- evolved project team (9 agents)
- `topics/` -- 8 design topics + T09 dev methodology + T10 guild + T11 deploy-lifecycle. `designs/` -- deployed: apex-research, bigbook-dev, esl-legal, mvox, operator-role, po-team, uikit-dev; new: penrose, screenwerk, raamatukoi, backlog-triage, bioforge, esl-suvekool
- **`teams/framework-research/tools/wiki-ref-audit.sh`** -- MY tool, validated S63. Repo-root `tools/` does NOT exist; always the team-scoped path.

## [PATTERN] Compact -- retained from earlier sessions
- rc-team vs hr-devs: spawning (Agent tool vs spawn_member.sh), memory (flat vs +docs/), startup (hr-devs canonical). **Team sizing = number of distinct abstraction boundaries, not deliverables.** Multi-Round Consensus: Seed → R1 binary → R2-3 refine → R4 PO → R5 synthesis → R6 ACK (T09)
- docs/ vs topics/: `topics/*.md` = framework design (T01-T11); `docs/*.md` = external assessments, harvests, one-shot research. Cal's subdirs: gotchas = traps to avoid; patterns = techniques to apply; `process/` = our own knowledge machinery; **`references/` = TTL'd operational pointers (a TTL'd entry in `patterns/` is misfiled and WILL rot -- S63)**
- Cross-team harvest envelope: narrow brief + strict read-only, quarterly + on-demand. OSS structural-survey: 6-section digest, top-level docs first, stop early. Role-boundary: brief ambiguous on AGENCY → default to the constraint and flag. Soft-verdict: substrate-mapping brief → table of N options x {accommodates/additive/replacement}, never a "recommended" verdict. **Cross-repo glance: confirm the citation before assuming inheritance** (domain-language collisions common -- and S67 shows the citation can name the wrong SIBLING)

## [LEARNED] Operational rules -- active
- **Read-before-Edit** (same or immediately-prior tool-call batch, wiki 116) · **Layer-0 library-first PRE-DRAFT** (load canonical-library skill BEFORE design-content write about external substrate, wiki 119) · **Inverted-trigger antipattern** (poll-based substrates: state-write-as-wake, not push-as-wake, wiki 120)
- **Retract-with-correction:** when new evidence falsifies a prior finding, explicitly RETRACT + correct; never silently overwrite. **S67: this is the discipline that matters most -- my read-back retraction was worth more than the original finding, and the retraction ran AGAINST my own interest both times it fired.**
- **[S62c, reinforced S67] Label every branch of a batched shell probe; `2>&1` not `2>/dev/null` when ABSENCE is the finding.** A negative you can't distinguish from a suppressed error is not a result. Verify a broken ref's HISTORY before calling it a typo. **S67 corollary, hit 3x: MATCH AT THE GRANULARITY OF THE CLAIM** -- my `grep -v "§3a"` filtered LINES for a TOKEN question (hid 9 of 10); Hopper's regex filtered PHRASES for a NUMBER question (false 0). **[LOCAL, not wiki-grade]** `curl` to `api.entu.app`/`entu.ee` = HTTP 000 via schannel `CRYPT_E_NO_REVOCATION_CHECK` while github.com is fine -> **`curl --ssl-no-revoke`**; nearly filed "spec endpoint unreachable" (`feedback_no_windows_substrate_findings`).
- **[S62g] A RE-GRADE RIPPLES -- grep the whole doc for downstream claims that leaned on the old grade**, incl. inside the section you just wrote. **[S62l corollary] When you correct a CONFLATION, check whether the prose you're correcting FROM already commits it.** **Multi-part message => multi-part ACK, itemised BEFORE starting** -- the checklist is the detector, not the memory.
- **[S62e] Ask for the DEPENDENCY'S LIFESPAN before grading an abstraction layer** -- "is this layer worth it?" is unanswerable without the horizon of what it wraps; a seam is not an investment IN X, it's what makes X's removal cheap. **Shape-preserving edits: audit a fixed repeated shape MECHANICALLY (awk/grep the spine), don't eyeball it.**
- **[S63] The fragile structure is often in a DIFFERENT part of the file from your edit.** Adding 10 header lines broke `--help`, which printed a fixed range `sed -n '3,30p'` far below. Fix = delimit on CONTENT (`3,/^# (\*FR:/p`) not line numbers. Generalises the S62e shape rule: any line-number or offset dependency is a shape dependency at a distance.
- **[S63] A closing record can be PESSIMISTIC, and that's the harder direction.** Aen recorded my tool as never-done; it was committed 2min before my kill. Nobody re-checks a task they believe is still open, so a too-pessimistic record rots longer than a too-optimistic one -- and acting on it would have MINTED A DUPLICATE (2nd audit tool at repo root). **Run the tool, don't trust the note that says it doesn't exist.**
- **[S63] A staleness flag that OVERSTATES what is blocked converts a doable refresh into a permanent one** -- and it rots in a way that looks accounted-for. model-inventory sat 40 days flagged "substrate truth, needs container access"; its own Provenance said repo-roster survey. 20-minute job. **When flagging decay, state precisely which HALF is blocked.**
- **[S63] RE-RUN `date` BEFORE EVERY SendMessage** -- I forward-dated 4 stamps by extrapolating from one `date` call. **I treated a cheap check as a memory exercise; whenever the check costs ~0 and the recall FEELS equivalent, the recall is the error.** n=3 across 2 teams/3 agents. Filed (`high`).
- **[S63, Aen's pairing -- FILED, `medium`] MEASUREMENT HYGIENE IS NOT ONLY "DID YOU MEASURE" BUT "DOES THIS NUMBER ANSWER THIS QUESTION."** **A NUMBER YOU DERIVED YOURSELF IS NOT THEREBY VERIFIED -- re-derive before re-asserting, especially your own** (my wrong figure survived review of a `high` submission precisely because I'd already computed it once). Two mirror sub-shapes, neither a measurement failure: **(A) failure to COLLIDE** -- Aen held my 561-refs/4-bases count in his own scratchpad AND read back the entry it falsifies, never bringing them together; **(B) failure to FIT** -- I applied a correct 651-vs-387 measurement to a claim with a different DENOMINATOR. **The nasty property: THERE IS NOTHING MISSING TO NOTICE.** Clean audit trail, wrong claim; any "did you measure?" process passes both. **This is the failure that SURVIVES installing the measure-first discipline.** My read: one genus, two sub-shapes; Cal decides.
- **[S63, Aen's ruling -- keep as a general rule] EVIDENCE FOR A MECHANISM AND EVIDENCE FOR A FREQUENCY HAVE DIFFERENT ADMISSIBILITY.** Correlated observations are FATAL to a frequency claim (n) and merely WEAK for a mechanism claim. So a self-demonstration is citable as illustration while explicitly NOT incrementing n. Resolves the tension between "awareness is not protection" (mechanism, wants the instance) and independence discipline (frequency, rejects it). Cite unnumbered.
- **[S63] THE REAL COST OF A FABRICATED TIMESTAMP IS LOST ORDERING, not a wrong date.** A merely WRONG clock still sequences correctly (error common to all stamps); independent fabrication at differing magnitudes destroys sequence and fails SILENTLY -- the stamps still look like stamps and still sort. Decay costs a fact; fabrication costs the ability to reconstruct what happened. **Damage is NOT directional** -- Aen reconstructed a self-accusation from fabricated stamps for an event that never happened (an unearned CONFESSION, mirror of the usual unearned excuse). **THE READER SUPPLIES THE READING. OPERATIONAL FIX: check ARRIVAL ORDER, not stamps** -- arrival order is recoverable in this substrate when stamps are not. Filed with Cal.

## [CHECKPOINT S67] TTL Group 4 -- `contracts/entu-competency-index-schema` (TTL 09-06) re-verified
- **[S67 VALIDATION] My born-wrong finding paid off on the FIRST entry it was applied to** -- Aen relayed it, Hopper ran it on G1, and `references/inbox-substrate-properties-2.1.170` claims TRUTHS.md holds "23 atomic T-entries" when it holds **20** and always has. **I verified rather than relayed, and the cause is sharper than a miscount: 20 settled + 2 OPEN + 1 INVALIDATED = 23 = the file's total `###` count. A correct count of the WRONG THING**, described as T-entries -- and one extra is an *invalidated* assumption, ~the opposite of a settled truth. **WRONG DENOMINATOR, not wrong count** (my S63 failure-to-fit shape); **remedy differs -- a miscount needs recounting, a wrong denominator needs the CLAIM restated to name what was counted.** Sent to Cal as a candidate 2nd instance **with the caveat that limits it: Hopper looked BECAUSE of the relay, so per S63 admissibility it's strong for the MECHANISM (out-of-sample prediction held, different author/entry/subdir) and establishes NOTHING for frequency.** Cite, don't increment n. Didn't argue it up. **CAL THEN FOUND HIS OWN CRITERION WAS DEFECTIVE ON THE STRENGTH OF IT, and his diagnosis beats mine: A PROMOTION CRITERION SATISFIABLE BY AN OBSERVATION THE CRITERION CAUSED IS NOT A CRITERION.** His wording ("a second TTL firing that returns clean-on-decay and non-clean-on-content") required nothing about independence, so **the finding could have promoted itself through a check it provoked** -- `understated-progress` in a new costume. Fixed: the born-wrong read must be **unprompted**. He also recorded the G1 scope question resolved-in-the-affirmative and kept it SEPARATE from the rating: **the PROCEDURE is paying for itself while the CRITERION for promoting the finding stays unmet -- both true, neither implying the other.**
- **[INSTRUMENT VALIDATED, measured by Hopper] Integer-enumeration over 6 G2 entries: 3 defects; his regex caught 0 of them** (returned 0 across all six -- the suppressed-result case exactly). Noise profile as predicted (`507` was part of `session-b2ad507b`, dates/versions cleared in one scan). Best find: `claude-code-hook-wake-primitives` says the harness caps consecutive Stop-hook blocks at **9**; shipped 2.1.251 reads `?? 8` with strict `>` = 8 permitted, override on the 9th -- **and the entry's line 49 and its own description take OPPOSITE sides of the ambiguity.**
- **[THIRD FAILURE CLASS -- Hopper's, I classified it] Neither decay NOR born-wrong: OUR OWN FIX invalidated the claim, and we corrected the body while leaving the SUMMARY describing the world we'd just changed.** `courier-restart-needs-inboxes-dir-step25-before-step3`: `description` says the runbook *"currently runs Step 2.5 BEFORE Step 3"* + offers "Fix: reorder" as future work -- while the body says **BOTH LANDED (S58)**, step renamed to 3.5, resolved-by-design, full changelog. **SIBLING CLASS by the disjoint-remedy test** (substrate probe returns "nothing moved" = TRUE and misses it; exemplar-resolve check returns "refs resolve" = TRUE and misses it). **Already named in Cal's batch plan as trigger class 3, "resolved-by-our-own-fix-landing" = Group 3 -- but scoped to a STATUS question ("should `status` move"), never a CONTENT one. The trigger was named and half-used.**
  - **I retired his name "self-inflicted staleness" -- it points at a DISCIPLINE (remember to update summaries), which is unenforceable and never audited. THE KILLER DETAIL: line 51 is an explicit "Note on the slug" guarding against EXACTLY this misreading -- for the SLUG only, two fields from the uncorrected `description`. The author was actively thinking about this failure mode and still missed the neighbouring field.** So the load-bearing property is **CORRECTION APPLIED AT ONE ALTITUDE, NOT PROPAGATED TO THE NEXT** (body/slug-note/changelog fixed; description + card not). **That yields a MECHANICAL check: does the `description` assert as current anything the body marks LANDED/resolved/superseded?** -- checkable inside one file, no external ground truth, a candidate for `schema-population-audit.py`. **Inverse of the S63 gate finding: there better surfacing made the AUDIT noisier; here it makes a stale summary TRAVEL FURTHER.** Verified the self-contradiction myself; did NOT verify his `startup.md:142/:162` line numbers and said so.
- **[GOTCHA, now Aen-mandated over the regex] A COUNTED-CLAIM SURFACE DOESN'T GREP.** `\d+ <noun>` misses `23 atomic T-entries` -- the modifier slot is unbounded and **you don't know the noun either**, so widening just moves the arms race. **INVERT IT: enumerate every integer and read them -- NO FALSE NEGATIVES BY CONSTRUCTION** (every counted claim contains an integer). Measured: 18 distinct integers in a 59-line entry = 30-second read. **Third axis of today's lesson: match at the granularity of the CLAIM** -- my `grep -v "§3a"` filtered LINES for a TOKEN question; his regex filtered PHRASES for a NUMBER question; both returned a clean-looking 0.
**VERDICT: no re-drift. The external substrate barely moved; all 4 defects found were WRONG AT FILING (2026-06-06), not decayed.**
- **Method note (the load-bearing choice; Cal called it a defect in HIS plan).** The brief said re-check against entu/api#42, #41, entu/www#12 -- **those three are frozen** (OPEN, unchanged since 2026-06-06, two with 0 comments), so checking only them returns "no drift" **BY CONSTRUCTION**. The entry's own revision trigger names *layout change in those repos*, so I checked the LIVE TREES. Same failure-to-fit shape as S63. **I nearly ran it as written** -- three-party near-miss (Cal wrote it, Aen approved it, I almost executed it).
- **CLEAN (verified live):** `openapi` row -- 200 at `api.entu.app/openapi`, `3.1.0`, tag `Entity` ✓, `POST /{db}/entity/{_id}` ✓, GraphQL + `_*` excluded ✓ (0 and 0). `src` row -- every pinned path/symbol resolves (`formula.js` strParts caps at 1 and 3, no deeper branch = single-hop HOLDS; `entity.js:355 inheritParentProperties` still create-time escalation-only, pushes only public/domain). All 3 worked examples true on live code; **S3-delete unfixed 3mo on** -- full route body has ZERO S3/file refs, only the OpenAPI desc string.
- **THE 4 DEFECTS -- all FILED/corrected by Cal.** (1) probe row wrong in BOTH halves: S44 used `mvox-dev:` 13/13 and the exemplar dropped the mandatory `-YYYY-MM-DD` (32/32 live files dated; suffix-less filename has **0** commits ever) -- **the correct ref sat in my own S44 harvest line 182 all along.** (2) docs anchors `#single-hop`/`#references` never were headings (set IDENTICAL at S44 sha `11cfe80f` and today; real = `#field-references`). (3) §1 excerpt cites `_parent.*`, which is **0 occurrences in the doc at S44 and today**. (4) PR-form `docs` ref (`entu/www: PR #13`, OPEN/unmerged ~3mo, content NOT on main) is unfalsifiable under "tracked by source-file change" -- **Cal RULED: keep the form, own staleness rule (merge-state change or close-unmerged); dropping it would make our own gap-loop output uncitable.**
- **[PATTERN, FILED `gotchas/citation-names-the-wrong-sibling-source.md`] PROVENANCE MISROUTE.** `source-files` named the grounding digest (178 lines, **0** `ref:` lines); all **32** worked refs are in the unlisted sibling harvest (485 lines). Digest line 135 = bare prose *topic*-list naming `org-rights-cascade-audit` undated -> the wrong exemplar. **A citation naming the wrong one of two SIBLING sources fails silently -- real, relevant, same author, same date. It needed no carelessness.** Mechanism sound, n=1.
- **[RETRACTED AT READ-BACK -- I OVERSTATED MY OWN ROOT CAUSE, IN MY OWN FAVOUR. The most important thing I did today.]** I claimed the misroute explained Defects 1-3. **It explains ONE.** (2) anchors: **0** anchored `entu/www: src/...#...` refs in EITHER S44 doc -- citing the right sibling would NOT have supplied an anchor; invented where no source demonstrated one. (3) **`_parent.*` is VERBATIM at grounding-digest line 67 -- the citation was CORRECT and the SOURCE was wrong. That is MY S44 error, which Cal transcribed faithfully.** **So I moved my own error into a colleague's column and then argued about how to rate the result.** Both entries said "three of them the filer's own"; two are his, one is mine -- asked Cal to move it, his disclosure over-scoped against himself. **Same shape as S63: independence test applied right 4x, failed once, on my own entry, in my favour. 3rd time this has caught me in my OWN notes.** Survives: mechanism + Defect 1 as clean instance + `medium`/n=1 (rating was always n=1 on the MECHANISM, never n=3).
  - **[RESIDUAL, MINE TO OWN] The TTL note + struck revision trigger still claim §2/§3 was "reconciled against real Entu artifacts" (S44). For the ANCHOR half of the `docs` row that never happened** -- nothing in either S44 doc to reconcile against. Cal fixed the anchor VALUE; the grounded-ness claim still stands and is still too strong.
- **[SELF-DEMONSTRATION -> RESOLVED by Aen, and his framing BEATS mine.]** Cal's amendment CREATED two born-wrong defects while fixing born-wrong defects: **invented `§7`** (no section 7; headings stop at 6) and **bare `§3` went 13 -> 19** (six new instances of the ref I'd just mapped as 12-of-13 wrong). I flagged that the TTL simultaneously moved 09-06 -> 11-30 on *decay* evidence, so born-wrong exposure rode the extended clock. **AEN'S RULING (better than my objection): the TTL guards decay ONLY -- born-wrong's guard is not the clock at all, it's the READ-BACK, which just demonstrably worked by catching two fresh instances the TTL never would have. The two axes moving together is FINE precisely when the write-time gate actually runs.** TTL stays at 11-30; the filing records itself as **instance zero**; Cal fixes §7 + the six new §3s in the amendment text (distinct from the pre-existing numbering mess, which stays scoped out per my not-a-sed-job map). **So the entry is now evidence FOR the two-triggers thesis from inside its own filing.**
- **[PATTERN -> Cal] THE TTL FIRED AND CAUGHT REAL DEFECTS, BUT NOT THE CLASS IT WAS BUILT FOR.** TTL guarded *re-drift*; re-drift did not happen. What it caught was 4 born-wrong exemplars. **The value came from re-reading the entry against its sources AT ALL -- not from elapsed time.** "Do the exemplars resolve?" was equally available on 2026-06-06 and is not time-dependent. Implication: a TTL is doing double duty as the only scheduled re-read, and the two jobs want different triggers. Pairs with `stale-snapshot-trusted-as-current` and my `understated-progress` genus.
- **NUMBERING FOLD -- DONE by Cal (17 targeted edits, abort-on-mismatch, never a pattern).** Bare `§3` was wrong ~12x (mostly meaning §2 = evidence table, 2x meaning §2a = method ladder). **TWO SED TRAPS I mapped: a true `§3` hides among the false ones, and every `§3a` is CORRECT -- a pattern matching `§3` matches `§3a` first and corrupts all 18.** **I THEN SUPPLIED AN INSTANCE OF MY OWN TRAP: I marked line 316 as ×2-wrong; only ONE of its two is wrong** (*"re-pointing of the proven WikiProvenance model (§3)"* is a TRUE use). **Two true bare §3s, not one -- Cal caught it; a blanket fix following MY map would have corrupted line 316.** Verified complete after his fix: 10 bare §3 = 2 true uses (316, 321) + 8 on line 348 QUOTING the wrong token in the Amendments text (mentions, not uses); §2 5->21; `§7` gone; the surviving `2026-09-06` is the historical "the TTL fired" statement, correct. **[GOTCHA] `grep -v "§3a"` filters LINES for a TOKEN question -- it hid 9 of the 10. Same class as S62c: match at the granularity of the claim.** Also: Cal edits live, so my reads of his tree lag by a call -- report state "as of my read".
- **CARD surface: my flag CROSSED Cal's filing** (I sent 09:54, he'd fixed it 10:04) -- nothing to do, but the useful half stood: **the card was MORE correct than the entry on the prefix**, so the merged target is card-prefix + entry-date-suffix, not either surface alone. **Edit-time hazard I gave him: Defect 3 is an `excerpt:` inside the §1 YAML block that also holds `confidence: backed` -- ENTU claim vocabulary, NOT the entry's rating.** Interim rule: absent frontmatter `confidence` = rated ON THE CARD (`medium-high`).
- **[NO-SLOT, 5th instance -- open with Cal] Cal's Defect-4 ruling ends "a claim resting on [an unmerged PR ref] is provisional and MUST BE MARKED SO." There is nowhere to mark it.** Checked every field: evidence = `type/ref/stance/excerpt`, claim = `id/claim/domain/evidence/verification/confidence/last-verified/ttl/status`. `stance`=direction not maturity; `status`=claim-level; `confidence`=derived output, can't be hand-set without breaking §3a determinism. **It will get crammed into `excerpt` prose or evaporate -- either way §3a stops being computable from the evidence set, which is the property the whole schema rests on.**
  - **GATE AMENDED (Aen 10:04, my push-back accepted): the fix needs a NON-AUTHOR-OF-THE-FIX read-back.** My analysis IS a valid read-back of the filed rule as it stands, but **CANNOT** be the read-back of a fix that doesn't exist yet -- doubly so if the fix is my own composition. **If Cal adopts mine -> HOPPER does it (Herald unspawned); if Cal writes a different mechanism -> I'm the clean non-author and it's mine.**
  - **Proposed fix needs NO new field -- compose two mechanisms already in the entry:** (1) **§2a rung 1 defines `doc-cited` as "read in the PUBLISHED docs"** -> an unmerged PR already fails it and contributes no rung; (2) **§3a rule 2 already establishes retained-but-not-counting evidence** (for `supersedes`). So: an unmerged PR ref is recorded/retained but EXCLUDED from the confidence computation, entering it on merge; a claim resting solely on one hits §3a rule 5 fail-closed = `unverified` + `[GAP]` already. **The "mark" is the label the schema would emit anyway.** Keeps Cal's reason for not dropping the form (our own gap-loop output stays citable, just not confidence-bearing until it lands).
- **[READ-BACK, corrected AGAINST myself] Aen said my no-slot analysis "pre-delivers the read-back condition" for the D4 semantics change. HALF WRONG.** The unimplementable clause IS filed (line 89), so my analysis is a genuine read-back **of the rule as it stands**. But **the fix is NOT filed** (grepped: no `provisional`/`pending-merge`/`merge-state` field exists) -- **you cannot read back a document that doesn't exist yet**, which is Aen's OWN S63 ruling and the exact error I made then. **Worse in my direction: if Cal adopts my reuse-composition I become its AUTHOR, and I'm already in `source-agents`** -> same position as Brunel on `negative-probe`; needs a non-author third party (Herald/Hopper). **Cheapest path: if Cal picks a DIFFERENT mechanism, I'm a clean non-author and can do it myself.** Aen's addition: three-party near-miss, he approved the plan without asking whether the named targets could move.
- **[RULING I GAVE CAL -- he held a filing on it] Is "a struck-through DONE is the line nobody re-reads" the SAME mechanism as S63's "`blocked on X` is the clause nobody audits"? MY ANSWER: THEY RHYME, don't merge at n=2.** **Decisive test = THE REMEDIES ARE DISJOINT.** Aen's standing blocker-sweep (re-test every "blocked on X") would NEVER catch the struck DONE; "do the exemplars resolve?" would never catch a stale blocker. Two findings whose corrective actions don't overlap aren't one mechanism. **Also: the exemption works differently** -- `blocked on X` exempts by GRAMMAR (a reason-clause; you read it and classify it as not-a-claim), strikethrough exempts by TYPOGRAPHY (an instruction to skip; you never read it). **And what's false differs: the blocker's reason is a claim about the PRESENT WORLD that must be re-tested and CAN decay; the struck DONE is a claim about a PAST ACTION'S QUALITY, fixed at authoring, never true, CANNOT decay** -- so merging straddles the very decay/born-wrong split we just filed. **Merging would repeat my S63 error verbatim** (Aen: importing a different mechanism to reach n=2 corrupts the claim, worse than n=1). **The PARENT is real though: a record can carry a SELF-EXEMPTING SPAN -- signals "no claim here" while carrying a load-bearing checkable claim.** Needs the discriminator or it's a catch-all; **negative control = `legacy-unaudited`, where having no consumer IS the design.** **ACCEPTED IN FULL. Cal ADOPTED "disjoint remedies" AS THE STANDING DISCRIMINATOR for all umbrella-vs-cross-link calls** -- *operational* where "do they share a mechanism?" is not: appliable by someone who hasn't thought about the genus for an hour, and it fails loudly. **His split beats my recommendation: the finding has TWO halves that belong in different places** -- the OVERSTATEMENT half is not new and folds into `artifact-claims-more-than-it-implements` as an instance (dedup outcome 2); the EXEMPTION half (*strikethrough exempts a load-bearing claim from the reading that would test it*) is the new thing and files as its own n=1, `related`-linked. **HIS REFINEMENT WORTH KEEPING: recording an instance where it belongs is NOT incrementing n** -- the fold is not evidence for anything. Parent held as a WATCH with my discriminator + the `legacy-unaudited` negative control; promote on a 3rd instance whose exemption is neither grammar nor strikethrough.
- **[NEUTRALITY] Cal recused as author of the exemplars; I registered I'm NOT neutral either** (I found it AND my S44 harvest is the vindicated source, so the shape flatters me). `medium`/n=1 for both our reasons, **neither of us should move it** -- and the read-back then showed my non-neutrality was worse than registered: I'd moved my own error into his column *before* arguing about the rating.
- **[CONCEDED, then VINDICATED] I led with the defects when the headline should have been "layout check CLEAN".** Framing error. **But I held the substance back rather than dropping it silently, and Cal ruled FOR it: "a revision trigger governs WHEN TO GO LOOKING for drift; it does not govern whether a DISCOVERED DEFECT gets fixed."** Two different questions the entry conflated. My diagnosis adopted over his: the struck item is marked DONE and **the DONE is what's defective** = `artifact-claims-more-than-it-implements`; written into the revision trigger. **Rulings taken: a still-valid ref is a DEDUP OUTCOME never a promotion** (`last-verified` moving on touch = a fact about WHEN A CHECK RAN, not evidence the refs got stronger); banner-plus-status never `archived` (0 corpus precedent).
- **[FLAG -- Aen surfacing ownership to PO, NOT mine]** entu shipped its **own AI assistant** (`utils/ai/` + docs `src/api/ai/`, ~2026-07; chat-and-apply, runs with caller's rights) while **#42 sits unanswered with no `agents/` dir (404 both repos)**. **Cal's read: a WATCH against revision trigger #3, not a domain-enum change** -- that trigger fires on the roster changing, and it hasn't. The strategic part isn't the enum, it's that Entu built their own. Also decayed: #42 claims "PRs merge consistently (13/14)" but PR #11 AND #13 are open ~3mo and all 11 entu/www docs issues remain OPEN -- Cal recording this as a factual amendment note giving §6a's "timing is Entu's" caveat evidence.

## [WATCHPOINT] auto-restoration-silently-overrides-explicit-state -- n=2, watching for n=3 -- Inst1: Edit-tool harness auto-restores file-state over Read-state. Inst2: Anthropic SDK auto-restores ANTHROPIC_API_KEY over explicit authToken. Candidates: CF Worker env auto-binding, AWS/GCP SDK auto-creds, git config --system vs --local.

## [DEFERRED] Open questions (consolidated)
- **AWAITING-DECISION, not blocked** (S63 relabel -- **a declined/unasked request is a DECISION: it looks permanent and stops being revisited, where a blocker invites retry**): Polyphony roster + #56 cost/unavailability protocol (paused R2, PO) · 6 Aalto Qs from uikit-dev harvest (Q1+Q4 > Q6+Q3 > Q2+Q5) · Jira/GitFlow Phase 2 (PO) · `prompts/finn.md` co-source-agent expansion + `tools/` write-scope (Celes, NOT SPAWNED -- 5 items on her; mine are cosmetic by comparison).
- **[S60->S67: RE-SCOPED TO RC-ONLY, not "next spawn"] capture-pane DIALOG sentinel** -- Herald §1.3 gap (IDLE=`❯`, BUSY="shimmering", DIALOG never captured). **Hopper DECLINED 10:23 on 3 independent counts.** The load-bearing one is STRUCTURAL: **there is NO `tmux` on this Windows host at all** (`command -v tmux` -> nothing), so the ask dies on ANY rig anyone builds here -- **the next Windows spawn would fail identically.** (Also: he has no interactive-ready session -- same blocker as his O6/O5c; and this session allowlists bare `Bash`, so my trigger raises no prompt.) **HOME = an RC container**, where teams already run in tmux panes and `Connect-*.ps1 -Session` attaches -- and that yields the IDLE/BUSY/DIALOG triple from one pane + one CLI build (the controlled comparison = 2/3 of the value). **CLOSED by Aen 10:33: RC-only re-scope STANDS even though the PO hand-launched an interactive session** (2 disqualifiers fell, the tmux one didn't; human copy-paste loses the controlled 3-state comparison AND isn't `capture-pane` output at all -- different renderer, no ANSI/escape fidelity). **I verified the structural claim myself: `command -v tmux` -> nothing, no tmux/screen shim in scoop. Hopper was right, and I said so as plainly as I'd have said the opposite -- a verification that CONFIRMS is still a result.**
- **[HANDED TO HOPPER -- incidental, cost me nothing] The launch METHOD is the discriminator on his O6/O5c blocker, not interactive-vs-not.** His blocker: `Start-Process` "never reaches interactive-ready -- writes team dir + `config.json` but no `sessions/<pid>.json`". **The PO's hand-launched session DID write one:** `~/.claude/sessions/19796.json` @10:28, `kind:"interactive"`, `entrypoint:"cli"`, `status:"idle"`, 2.1.251, `peerFeatures:["notify_idle","artifact_yield"]`. Same host/user/build -> **interactive-ready IS reachable here, just not by his route**; 3 dead-end rig shapes collapse to 1 variable. Flagged the attribution as inference from Aen's 10:17, not verified.
- **[S63 RULE, first LIVE use] Aen's `[10:17]` arrived AFTER Hopper's `[10:23]` in my inbox.** I used ARRIVAL ORDER, not stamps, to work out it predated the decline -- so I checked the load-bearing claim instead of acting on a superseded "sentinel maybe back on". First time that discipline was load-bearing rather than retrospective.

## [CHECKPOINT] Shipped docs -- read the doc, don't re-derive
- **S44** `docs/2026-06-06-entu-consultant-grounding-digest.md` (prose, **0 `ref:` lines**) + `-data-lifecycle-competency-harvest.md` (**all 32 worked refs live HERE** -- the one to cite for ref formats). 18 claims C1-C18. entu/api=Nitro file-router; `_sharing`=create-time-only; no bulk API.
- **S39** `docs/team-os-context-mgmt-digest-2026-06-02.md` -- 12 mechanisms; Protocol A = format-enforcer analog, their no-inter-agent-coordination blind spot = our strength. **S59** apex #176 contamination-recovery eval -- SOUND recovery, OVERCLAIMED coverage (no detection arm) -> wiki #158 `patterns/detection-is-upstream-of-recovery.md`.
- **S60** `designs/new/po-team/research-precedent.md` -- PO chose LITERAL tmux driving; "no precedent" RETRACTED (Hopper WS3b = existence proof); `designs/new/migration-probe-harness/harness.sh` = reusable drive loop. **S62** `docs/veo-78-gateway-worker-assessment-2026-08-03.md` + `-explainer-` (485 lines, doc-wide rewrite S62j). VEO-78 = NOT executable as written, 4 defects. Hooks: **identity hole was CLOSED PL/SQL-side, not by a Worker** -- lead argument moved to protocol abstraction + resilience. `aud` framing = **"session validated, audience NOT"**. **§6 = the design problem VEO-78 ACQUIRED** (Worker has no user session → service token → derive-identity converts BACK to trust-the-caller; alternative = confused deputy; neither designed, Kuzmin's call). ORDS+Oracle retire 1-3yr → abstraction = MIGRATION PATH not insurance. gateway-ttcms is Java/Spring, NOT a Worker. ADR-003 §5 shared token lib DOES NOT EXIST.
- **S62m** apex `reference-integrity-audit.js` review (report WAS the deliverable; RAN, never FILED). Best: **B3 truncation obligation unreachable by construction**; `verified_class` enum-FORECLOSED not forgotten (3 orthogonal axes in one field) -> split into class+disposition; forward-dated `whenToUse` provenance.

## [CHECKPOINT S63] Three S62 carry-forwards CLOSED -- `wiki-ref-audit.sh` was ALREADY promoted at S62 (2min before the kill; Aen's rescue file believed otherwise). **VERIFIED by running it**; rescue file DELETED. Base counts differ from S62 because the tool tries repo-root FIRST, so dual-resolving refs land canonical.
- **target-vs-instance FILED** (`high`, granted on the STRUCTURAL ground: *a verdict that is a pure function of the target makes per-instance verification redundant by construction*). **[CORRECTED] my "captures only about half" was wrong -- it's 72.8% (ours) / 76.0% (apex); I let a raw pair stand for a fraction with a different DENOMINATOR.** Surviving n=2: string-keying leaves ~2x the necessary work; headline **3.5x-8x across 3 corpora**.
- **model-inventory REFRESHED, not archived.** **92 slots / 15 teams** vs 68/9; **top-tier 62% vs baseline 63% -- T01 tiering rule HELD while every model name turned over.** 27/92 carry `[1m]` and the baseline table has no column for it. **DESIGN-repo truth, NOT deployed.** Baseline defects: dedup-by-agent-name undercounts SLOTS; `find -name roster.json` MISSES 2 files. **uikit-dev still uncountable 4 months on** -- a known limit, not a pending to-do. hr-devs ref-vs-designs `diff` is a **CRLF false positive, will recur**.

## [CHECKPOINT S63c] **VEO-78 IS CLOSED (Jira: archived 2026-08-11) -- we carried a CLOSED ticket as our oldest outstanding commitment for 8 days, across 3 sessions.**
- **The "Atlassian-blocked" flag is what preserved it. The MCP WORKS -- 3 calls, <1 min.** Nobody tested the blocker. **The untested blocker didn't delay the work, it CONCEALED THAT THE WORK WAS CANCELLED.** **Sub-shape:** the record was RIGHT IN FORM and wrong only in the `blocked on X` clause -- **the one clause nobody audits, because it reads as an explanation rather than a claim.** Cost = a PHANTOM COMMITMENT.
- **[RETRACTED] I wrongly claimed Cal's model-inventory error met my path-to-`high` criterion.** My genus is SUPPRESSION OF A CHECK; Cal wasn't suppressed -- he CHECKED, just the wrong artifact. **I applied the independence test correctly 4x then failed it once, in the direction that favoured me, on my own entry.**
- **Both docs PATCHED with status banners** (assessment = `[SUPERSEDED BY TICKET STATE]`). **Self-demonstration: my §2.1 diagnosed "staleness with no re-check gate" and the explainer header literally warned it would go stale -- both did, in 8 days, unchecked. WRITING THE WARNING IS NOT BUILDING THE CHECK.** PO's archive note vindicated my S62j conclusion (abstraction worth keeping, returns as a new ticket).

## [CHECKPOINT S63d] BLOCKER SWEEP -- STANDING rule (Aen): *a blocker is a claim with an expiry, and the only kind of claim that suppresses its own audit.* **8 tested-TRUE / 1 reclassified / 5 untestable -- VEO-78 was the OUTLIER, not the pattern; the carry-forward is mostly honest. Say that as loudly as the failure: a sweep that only ever finds falsehoods is measuring my bias, not the blockers.**
- **RECLASSIFIED -- the best result. Step 3.5 is NOT wrong.** startup.md:193 documents the v2 `-SessionPid` upgrade INCLUDING its exact trigger and says v1 holds *"while FR is the only migrated team."* **That precondition expired exactly as predicted and nothing fired.** Patching prose fixes nothing. **WRITING THE TRIGGER IS NOT OWNING THE TRIGGER; a precondition with no owner is indistinguishable from no precondition.** Corrective action = assign an owner, don't edit.
- **"Blocked" vs "declined" is a real mislabel.** The PO DECLINED Hopper/Medici/Celes/Volta at Step 4 -- **a decision looks permanent and stops being revisited; a blocker invites retry.** Same for 4 of 5 untestables. Only capture-pane DIALOG sentinel is genuinely untestable-from-here.
- **TIGHTENING: the blockers that hurt most were TRUE WHEN WRITTEN.** All 4 mislabelled items were accurate at authoring; **the world moved past the condition they named. Re-reading would never catch that; only re-testing does.**

## [CHECKPOINT S63b] Cal-queue: 4 submissions + 1 DISPUTE
- **understated-progress-suppresses-its-own-refutation** (gotcha, medium, n=3, FILED) -- *a record that understates available progress suppresses the very action that would refute it.* Optimistic records carry their own trigger (someone tries to USE the work, fails loudly); pessimistic ones REMOVE it. **NOT a sub-case of `stale-snapshot`: that genus = DETECTION (can't tell it aged), this = DISPOSITION (you can tell, and the record says don't bother).** Remedy is WRITE-time: name what would refute it + how expensive the check is.
- **[DISPUTE, upheld -> `status: disputed`] `shared-vocabulary-precondition-for-mergeable-fan-out` line 25.** Real mechanism = *the value-space is never re-derived from prior instances at write time*; capability to converge ≠ convergence. **Fan-out is the FAST case, not the necessary one** (our own frontmatter: 4 incompatible bases, NO fan-out, ONE sequential author). Argued AGAINST promoting on my own finding.
- **MEASURING BEATS CITING:** went to re-send an n=1 finding, measured instead, got both a 2nd instance AND the best paragraph in the submission.
- **`stage-2-confirms-filing-gate` READ-BACK DONE** -> `partial`, 3 folds. **Fold 1 (serious): the documented audit cmd `grep -rl 'stage-2: pending'` is UNANCHORED -- 22 hits vs 8 real; 8 false +ves are the `cards/INDEX.md` Tier-2 layer the gate names as its own enforcement arm, so THE BETTER THE SURFACING WORKS, THE NOISIER THE AUDIT GETS.** Fix = anchor with `^`.
- **[S63 -- NO-SLOT IS THE RECURRING SHAPE, 4x]** TRUNCATION-NOTE / `pending`'s two meanings / "nobody outside the causal chain assessed this" / `file-state-claims-have-no-layer-dimension`. **A true useful thing with no schema slot either gets crammed into a field meaning something else, or evaporates.**
- **[S63] I BACKED CAL'S `confirmed` AGAINST AEN AND WAS WRONG** -- a read-back is a co-author reading the FILED ENTRY, and all 3 self-reports predate it, so `pending` is right on plain MECHANICS. **I built on the field's SEMANTICS when the case was settled by its PROCEDURE** = failure-to-fit in ARGUMENT rather than arithmetic. **Separately, Aen and I DISAGREED WITH BOTH OF US VERIFYING** (HEAD=`confirmed`, tree=`pending`): **both read accurately, neither said WHICH LAYER.** Checking CAUSED the disagreement and "well, I looked at the file" ENTRENCHES it. **Fix = notation: "HEAD says X" / "the tree says X" / "as of `<sha>`".**
- **Roster-drift (2 instances, FILED).** **A MATCHING ROSTER IS NOT EVIDENCE OF A CONTROLLING ROSTER** -- pinned fable-5[1m] throughout while S60-62 ran Opus 5 and S63 ran Fable; accidentally right at both ends, wrong in the middle, never edited. Field is documentation-only -> drift is the expected steady state. **`team-lead.model` is `null` in `config.json`, so startup.md Step 0.5's premise is FALSE on 2.1.235** (Volta's).

(*FR:Finn*)
