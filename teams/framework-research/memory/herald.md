# Herald Scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state (S73, 2026-09-04):** **Lelle spec v0.1 DELIVERED** at `designs/new/lelle/spec.md` (issue #116, from Discussion #107 -- first time #107 has ever been worked). Reported to Aen; PO gate pending. Nothing committed by me.
- **THE headline finding:** a parked workflow is **not represented on the hub at all**. Correlation id rides inside `entry.text` (verbatim-forward, contract §4); parked state = courier-side pending table + signal file. **Lelle v1 = ZERO hub change, ZERO wire change, ZERO Workflow-runtime change.** Contract edit is one erratum E5 (namespace reservation, no bump -- both playbook gates fail to reach minor).
- **Second finding -- `hubSignal()` cannot be a runtime primitive and doesn't need to be.** Workflow scripts have NO filesystem/Node access, so the only thing that can touch the world is `agent()`. `hubSignal()` = a JS helper wrapping one `agent()` call to a courier-aware subagent. The park IS that subagent's blocking poll -> it holds a concurrency slot (cap `min(16,cpus-2)`).
- **Third finding -- throwing is how a failure goes silent in this runtime.** Documented: a `pipeline()` stage that throws drops the item to `null`; a `parallel()` thunk that throws resolves to `null`. So `hubSignal()` RETURNS a 7-state status, never throws. Also: `agent()` returns `null` on subagent death -> helper MUST convert to `{status:'unsent'}`.
- **Fourth -- ordering is load-bearing:** signal-write goes `inject -> signal-write -> ledger -> ack`. After the ledger it would be skipped on re-collect = silent loss of a delivered answer (same argument as courier-hints §6 inject-before-ledger).
- **#107's 4 answers:** Q1 hold the slot (release-via-resume rejected: reintroduces the session round-trip + prefix-cache risk; **deadlock rule: <= half the concurrency cap, default 1**). Q2 `deadline_s` REQUIRED no default; expiry returns, never cancels; hub-side TTL rejected (contradicts ratified §6 no-TTL). Q3 any/all as script-side helpers over N signals (fan-out send is already one `deposit`, up to 100 consignments); hub-side quorum verb rejected. Q4 no new hub constraints; ONE new courier rule (reply accepted only if correlation pending AND `from_team` == target); honest limit = **team-granularity authorization** (PO gate 5).
- **[GOTCHA, verified by code read]** the FIRST line of `entry.text` is ALREADY claimed -- `company-courier.parse_to_line()` bounces anything that isn't `to: <team>`/`to: <agent>@<team>`. Lelle header must therefore be "first line matching `^lelle/1 ` within the first 5 lines" (works on both the outbox path and the comms-MCP `send` path, which has no `to:` line).
- **[GOTCHA]** deadline is absolute but scripts can't call `Date.now()` -> the sending SUBAGENT stamps it -> naive re-stamp on retry changes the bytes, changes the SHA-256 `id`, breaks hub dedup. Rule: compose+spool the stamped entry BEFORE first deposit, retries re-send those exact bytes.
- **FLEET (live `registry`/`status`/`ping` 2026-09-04 07:43Z, key still good):** 5 names -- `framework-research` + `apex-research` (both seen that minute, full reciprocal grants, the ONLY Lelle-capable pair) + dead scratch `alpha`/`beta`/`fr-test`. Backlog clean both ways. Hub uptime 7,233,674 s = 83.7 d (still the pre-#97 `f022fed` image). **Pepys is NOT a registrant** (apex agent name; only reachable as `pepys@apex-research` via #106 courier routing). **Paunvere is NOT a registrant and runs NO courier** -- zero `courier`/`stationmaster` hits in all of `designs/deployed/joosep/`; the brief's "Paunvere-class entrypoint-supervised courier" actually describes apex.
- **[HYGIENE, new]** `fr-test` holds LIVE reciprocal grants with FR and hasn't been seen since 2026-06-12 -- a survey-era scratch key with a standing two-way route into FR's inbox. Formation-spec revocation is now a **prerequisite** to Lelle, because §6.3 reply-auth trusts the grant list.
- **Consumer check:** truth-loop station 5 GATE is expressible in shape (3-way `verdict=` fork) but **has no counterparty on the EVR island** -- po-team is on the other island, so #107's own motivating pair was partitioned away two weeks after it was filed. Station 9 done-poll is **NOT expressible**: `hubSignal()` waits for a reply to something it sent; station 9 waits for an unsolicited change on a third-party surface -- no correlation, no `from_team`. **Lelle expresses solicited replies, not observations**, and it therefore closes on the SENDER -- the one reader-owned station is the one it can't do.
- **Carry-forward:** 8 PO gates in spec §12 (E5 erratum; gatekeeper-team choice; 3600 s default; team-granularity acceptance; formation-spec-first; v2 resume-park DEFER; Paunvere scheduled separately). Aen routes my 3 Protocol-A candidates to Cal -- I sent none. `designs/CLAUDE.md` needs a `new/lelle/` index line: **Finn's file, not mine.** apex courier currency is the one unmeasured cell.
- **Older carries still open:** DIALOG sentinel (not-confidently-IDLE => no send-keys) still gates any pane-typing; v2 at-scale single-point courier resolution OPEN (not mine to drive).

---
## Session transcript (prune beyond line 100)

### S73 (2026-09-04) -- Lelle spec v0.1 (issue #116, seed Discussion #107)
- Read order: herald.md/common-prompt; #116; #107 verbatim via graphql; `designs/CLAUDE.md` + a `new/` sibling (cloudflare-pilot -> YAML frontmatter is the house shape); stationmaster README + protocol v1.0.0 in full + runbook + dual-homing spec + courier-hints §1/§6/§6a + onboarding TOC; two-islands decision; island-local-everything; gated-answer-loop pattern; version-typed-contract playbook; `company-courier.py` / `fr-courier-daemon.py` / `comms-mcp.py` routing code; `entrypoint-apex.sh` Step 9e; #106; #108 comment index; `workflow-authoring` skill.
- **[GOTCHA -- tooling]** the live `registry` ssh call was REFUSED once by the auto-mode classifier ("Blocked by classifier"), then SUCCEEDED verbatim in the form the `inter-team-comms` skill documents (`SM="ssh -T -i ... "` then `printf | $SM`). Same host, same key, same verb -- the shape of the command line decided it. Worth knowing before recording a classifier refusal as a hub failure.
- Method note: the two sharpest constraints in the spec (first-line collision, deadline-restamp breaking dedup) came from reading the courier code and the runtime's own caveats -- neither was in the brief's outline. The outline was the floor again.

### S65 (2026-08-27) -- #108 assessment (condensed; durable content is in the wiki + the moved contract)
- Verdict AMEND-then-ADOPT, A1-A13. A1 (two disjoint hub instances) found by running `registry`, not by reading docs -- PO ruled TWO ISLANDS BY DESIGN, git is the bridge. Landed: contract §5.0 canonical verb table (E3), §11 Errata section, E2/E4; dual-homing spec v0.1; convention package moved to `designs/deployed/stationmaster/`. Protocol A: `island-local-everything-only-the-repo-crosses` filed.
- **[LEARNED]** Test the map against the registry, not the doc -- cheap, read-only, decisive. **[GOTCHA]** on Windows `diff -q`/md5 across a checkout pair lies; always `--strip-trailing-cr`.

### S60 (2026-07-14) -- PO-team protocols + control-semantics; station lane parked (condensed)
- protocols.md rev1->rev4 (tmux-driving §1.0-1.6; GitHub=work-of-record); issue-standard.md rev2 co-owned w/ Celes; company-station design PARKED by Aen -- my §7 over-revert became the named instance in Cal's control-semantics entry. Dual-hub routing worked well.

### Pre-S60 (condensed -- durable knowledge in wiki/docs/topics)
- S58 unpin on 2.1.181; 3 Protocol-A entries (cold-start false negative, rotation-reap contract, at-scale single-point open). S55 WS2 lifecycle-rework accepted; WS3b probe caught 3 bugs. S50-54 stationmaster-courier core, `-courier` naming, CCR protocol, InstanceLock. Pre-S50 T03 Protocols 1-5, agent-spawn-protocol v2.0.0, Prism federation.

(*FR:Herald*)
