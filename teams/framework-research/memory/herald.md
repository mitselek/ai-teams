# Herald Scratchpad

## Summary (lines 1-15 -- always read on startup)
- **[S56 CLOSE 2026-06-18 ~14:57] BRIEF session. Came online, absorbed S55 DECIDED DIRECTIONS (team-lead.md lines 10-20, FINAL), sent intro confirming all 8 + readiness. PO halted cutover in S55 for cosmetic churn -> S56 plan = brief-once-then-unpin. I was NOT assigned a work task before shutdown_request arrived (TL closed S56). CLI STILL 2.1.177 -- the unpin did not execute in my lane this session.** All my S55 code remains MERGED on main (resolver + _pid_alive procStart + entry-guard + "auto" sentinel + --resolve-team-dir shim + FR_COURIER_TEAM_DIR_NAME env). Nothing new written/changed by me in S56.
- **NEXT-SESSION RESUME (unchanged from S55 close): unpin gates only on Aen's config-flip (`inboxes_dir`->"auto" in live config) + CLI updater unfreeze + v1 single-live-team precondition.** Lifecycle flip-ready on branch `fr/unpin-2.1.181-lifecycle` (startup.md + restore/persist scripts); apply at unpin via `git checkout fr/unpin-2.1.181-lifecycle -- ...` then commit main. My WS2 design `docs/lifecycle-rework-implicit-teams-2026-06-18.md` (on main) grounds it.
- **DECIDED DIRECTIONS (S55, FINAL -- do NOT reopen):** (1) target 2.1.181; (2) resolver liveness = process-based not status; (3) rotation = mode-(b) bare restart for v1, -SessionPid = v2; (4) courier config = launch-override (explicit default + .auto.json behind V4 dry-run); (5) V5b/P6 non-blocking; (6) env name FR_COURIER_TEAM_DIR_NAME keep; (7) lifecycle WS2 flip-ready on branch; (8) known constraints documented not blockers.
- **OPEN carry-forwards (non-blocking, not mine to drive):** (a) §4.3-vs-Volta ownership of script-invocation note [asked Aen S55, unanswered]; (b) #9 P6 attached-pane re-test (RfC-scope, deferred); (c) Aen V4-guard-placement decision (Brunel #11 wrapper).
- **Cal knowledge: all S55 entries FILED + read-backs CONFIRMED** (wiki ~150; lifecycle decisions x2, stale-dir gotcha med->HIGH, sessions-pid-json-not-gc gotcha HIGH, plus S51 trust-triangle set). No outstanding Cal obligations.
- **LESSON BANKED (S55, durable):** evidence-by-execution caught 3 real production-gating bugs an equivalence-argument shipped past (_pid_alive Linux-only collision, null-pid crash, multi-team ambiguity). Run the literal artifact against its real consumer/substrate; "byte-identical on the part I diffed" != validated.
- **[CARRY] courier-ref implement-next queue (PARKED on TL/PO go):** (1) lock-staleness boot_id/PID-1-starttime fix per `docs/courier-lock-staleness-fix-spec-2026-06-15.md`; (2) inject down-agent occupied-inbox B+A. Both PARKED.

---
## Session transcript (prune beyond line 100)

### S56 (2026-06-18)
- Startup read order done: herald.md, common-prompt, scratchpad, team-lead.md lines 10-20. Sent intro (14:44) confirming 8 DECIDED DIRECTIONS absorbed + ready, no work-start until assigned.
- No task assigned. shutdown_request received 14:57 (TL closed S56). Saved scratchpad, closing report, approve.

### S55 (2026-06-18) -- CONDENSED (durable detail in docs + Cal wiki)
- **WS2 lifecycle-rework design (#86) ACCEPTED-FINAL + probe-folded:** `docs/lifecycle-rework-implicit-teams-2026-06-18.md`. Core: STARTUP TeamDelete+TeamCreate -> Step 2' Discover (team auto-exists; session discovers `session-<id>` via shared resolver `resolve_team_dir(claude_home,*,session_pid=None,explicit_dir_name=None)` -- ONE fn, TWO callers, caller-relative: lifecycle passes pid -> pid-tiebreaker canonical; detached courier omits -> glob-`.name`+liveness-filter canonical). SHUTDOWN S5 leadership-release EVAPORATES (implicit property of live session, released by process exit) -> 5->4 phases. Step 3 restore + S4 persist KEEP+STRENGTHEN (agent-name-keyed durable bridge survives rotating team name). §6 stale `session-*` dirs accumulate -> pid-guarded sweep PROMOTED to recommended (V3: status useless). OQ2 (sessions/<pid>.json GC-on-exit) RESOLVED worst-case.
- **WS3b probe (Hopper, `docs/migration-validation-probe-findings-2026-06-18.md`, v=2.1.181):** V1 PASS; V2-pid PASS, V2b glob-liveness FAIL-FAST-SAFE; **V3 WORST CASE = sessions/<pid>.json NOT GC'd on exit (graceful + kill-9), dead entry lingers `status:"idle"` byte-identical to live; status CANNOT distinguish dead/live;** V4 = config.json first, sessions/<pid>.json ~10-25s later (early-discovery window real); V5a PASS; V5b INCONCLUSIVE-leaning-NEG (headless confound, attached-pane re-test = task #9 deferred).
- **V3 liveness fix MERGED + VALIDATED into stationmaster-courier.py:** extended cross-platform `_pid_alive(pid,procstart=None)` (backward-compat, /proc field-22 PID-reuse guard Linux-only/degrades); new discover_by_config_glob / discover_by_session_pid / _has_live_session(keys on _pid_alive NOT status) / resolve_team_dir; Config inboxes_dir "auto" sentinel. Win 11/11 + 4/4 InstanceLock + 17/17 integration + py_compile; Linux 12/12 (Hopper). **CRASH BUG caught by literal-merged-file run: null/non-int pid -> TypeError; FIXED via int-coerce-or-dead guard at _pid_alive head.**
- **#10 `--resolve-team-dir` shim** added to main() (Volta scripts): bare dir / +`--name` slug; session_pid precedence `--session-pid` then FR_COURIER_SESSION_PID env. 9/9. **FR_COURIER_TEAM_DIR_NAME env-fallback** added to Config("auto") 3/3.
- **Multi-team-host (WS2 §8):** 12 named dirs; bare resolve fail-fasts CORRECT; pre-unpin use --team-dir-name framework-research, post-unpin --session-pid; v1 precondition = FR is ONLY live 2.1.178+ team at cutover.
- **P6/V5b verdict (#9):** Step-3 = SEED-THEN-PROCEED (own startup writes inbox while ACTIVE = P4-class active-delivery), NOT rely-on-woken-recipient. P6 does NOT gate Step-3/unpin. UNPIN = GO from lifecycle side.

### Pre-S55 (condensed -- durable knowledge in wiki/docs/topics)
- **S54:** `-courier` channel-naming adopted (rewrite_attribution + _outbox_to_team backward-compat loop); closes apex reply dead-letter class. 17/17. GOTCHA: Win _pid_alive verify via CIM CommandLine not grepped tasklist.
- **S53:** `topics/11-deployment-lifecycle.md` authored (CCR protocol); caught 09/10 slot collision -> 11. Monte legislative/judicial/executive framing.
- **S52:** apex stationmaster two-customer round-trip (deposit GREEN, host-key provisioning = leg-3 trust triangle); InstanceLock pid-only-staleness bug spec'd (`docs/courier-lock-staleness-fix-spec-2026-06-15.md`, PID-1-starttime epoch not host-scoped boot_id); inject-occupied down-agent loop. All -> Cal.
- **S50-51:** stationmaster-courier.py (8/8 + 14->17 integration); per-consignment inject; fan-out per-destination-outboxes (CR4 decision); consignment body-in-text-not-content; spool route-deferred self-heal. Docs FINAL rev S51.
- **Pre-S50:** S40 playbooks extracted; T03 Protocols 1-5; agent-spawn-protocol v2.0.0; Prism federation (13 PRs); cloudflare-pilot comms design; T03/T06 boundary Herald=shapes/Volta=state-machine.
