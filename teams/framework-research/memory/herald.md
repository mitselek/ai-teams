# Herald Scratchpad

## Summary (lines 1-15 -- always read on startup)
- **[S60 rev2 2026-07-14 -- PO-TEAM PROTOCOLS DELIVERED + REVISED] `designs/new/po-team/protocols.md` (*FR:Herald*).** PO DECISION (via Aen): channel = **literal tmux/screen CLI driving** (send-keys direction + capture-pane read-back), NOT message-passing. Rev2 rewrote §1 as critical-path net-new spec. KEY CONTRACT PIECES: (a) §1.0 launch-vs-drive rule sidesteps #60 crash class -- PO NEVER launches the CLI (remote startup runs claude as fg process of named tmux session) and NEVER send-keys onto a permission DIALOG; (b) §1.3 capture-pane read-back = poll classifying pane IDLE/BUSY/DIALOG/DEAD by sentinel tokens (heuristic, tokens pinned empirically in acceptance test); (c) TWO-LEVEL completion: pane read-back = "turn finished", GitHub = "work done" (never report done off pane alone); (d) send-keys uses `-l` literal + SEPARATE Enter; one-shot ssh-exec is agent default over interactive attach; (e) §4.7 over-real-ssh acceptance test incl. DIALOG-path exercise = go-live GATE (retires #60 risk per-pair on real substrate, not assumed away). GitHub still work-of-record; local clone RO. Message-passing (stationmaster/ghost-member) demoted to §7 documented v2 fallback only. 8 open Qs (multiplexer=tmux, sentinel-token owner=librarian, remote CLI permission posture [biggest lever], task-issue owner, attach-vs-ssh-exec, issue lang, RO-enforce+keys, writer arbitration). **rev2b: folded Finn's 3 evidence reinforcements** -- §1.0 now cites BOTH crash modes (#60 launch+dialog, runbook§16 shell-into-Claude) + Hopper WS3b OAuth-drive as EXISTENCE PROOF (framing: proven-safe-in-envelope, not just prudent); §1.2 adds multi-line 3-invocation rule (chaining paste-buffer+Enter in one ssh SILENTLY swallows newline) + `-l` as no-echo login primitive + cites harness.sh tmux_send/tmux_capture (== my pattern, verified); §4 new item 8 = provision tmux-direct-brief skill (NOT in local ~/.claude/skills, rc-host env). Checklist now 1-10, gate 1-8. Remaining empirical gap: exact IDLE/BUSY/DIALOG capture-pane sentinel tokens (pin during acceptance test). **rev2c: Finn provisional-pinned IDLE=`❯` clean / BUSY="shimmering" (WS3b, migration-probe-scope:89) -> §1.3 table upgraded from TBD to provisional-usable; DIALOG stays OPEN (no stable sentinel, dialog-type-dependent, load-bearing for the §1.2 gate -> must capture live). Safe fallback: not-confidently-IDLE => no send-keys. The one artifact that closes §1.3 = a live `capture-pane -p` dump of the permission-prompt screen on target CLI version.**
- **[S60 rev3 2026-07-14 -- AEN DECISIONS FOLDED + ISSUE STANDARD WRITTEN]** Aen ratified 2-substrate split + 4 decisions: Q1=tmux, Q3=ALLOWLIST-TUNED permission mode (dialogs rare-but-possible -> observe-before-inject gate now explicitly MANDATORY-not-best-effort, residual-dialog = first-class path; folded into §1.0 rule 2), Q5=GitHub-only/NO courier v1 (§7 = v2 fallback, ratified), Q6=English issues. §6 restructured: Resolved(Aen) / Resolved-as-design-calls(me: Q4 task-owner=remote-lead, Q5b driving=one-shot-ssh-exec, Q8 writer=single-writer-convention-v1) / Still-open-Mihkel (Q2 DIALOG sentinel=acceptance-test deliverable, Q7 keys/enforcement/infra). **TASK 2: wrote `designs/new/po-team/issue-standard.md`** (epic/task standard, English, identical across 4 repos) -- 5 core labels (epic/task/ready/blocked/needs-po; `ready`=durable dispatch handshake so tmux "pick up #N" only for ready-labeled), optional priority + `product:*` (org-board-only), milestone=release-grouping-epics (NOT epic-as-milestone), GitHub-native epic->task checklist decomposition (`- [ ] #NNN` auto-progress + `Closes #` auto-check), 2 issue templates (.github/ISSUE_TEMPLATE/epic.md+task.md). Aligned to Celes roster (Henry lead; POs Gama/mvox,Pacheco/bigbook,Albuquerque/ad-auto,Magellan/field-network; Nunes lib). SENT to Celes for naming-alignment review (product-registry short-names). Open: org-board y/n, priority-now-or-defer, GH write scopes (Mihkel).
- **[S60 rev3b 2026-07-14 -- Aen Q2/Q5 ratified + Celes label draft reconciled]** Aen: Q2=Nunes owns capture-pane sentinel-token card (versioned per CLI ver, re-pin at each acceptance-test + CLI upgrade) -> wired into protocols §4 item7 + §6 Q2 marked RESOLVED; Q5=one-shot ssh-exec agent default stands. Confirmed to Aen all 4 (22:02) decisions folded in rev3. **Celes reconciliation into issue-standard.md (rev2):** kept LEAN status vocab, REJECTED her full `status:*` ladder (duplicates GitHub-native state, desyncs) -- native open/closed+linked-PR carries backlog/in-progress/review/done; only non-native signals get labels (`ready`/`blocked`/`needs-po`). ADOPTED from her: `priority:P0/P1/P2` naming, `epic:<slug>` optional grouping, per-product `area:*` (non-standardized). Her Q2 cross-product dep = NOT a param label (can't carry #N) -> `blocked` label + body `Blocked on owner/repo#N` + escalates to Henry per protocols §3. Her Q3 FR-collision: none (separate repos). Still pending: Celes product-registry short-names for `product:*`; Mihkel org-board/priority calls. **rev3c: Aen correction -- `priority:P0/P1/P2` PULLED from adopted set (Mihkel's call, not team's) -> moved to issue-standard §7 "Pending Mihkel (proposal)" block, preserved verbatim. Std = 5 core labels + optional `epic:<slug>`/`area:*` only. Told Celes for po-template. Consistent.** DELIVERABLES FINAL: `protocols.md` (rev3, tmux-driving critical path) + `issue-standard.md` (rev2, co-owned w/ Celes). Only truly-open = Mihkel infra values / GH scopes / org-board / priority. **rev3d: `product:*` slugs LOCKED (mvox/bigbook/ad-auto/field-network; field-network=slug, mikrotik=domain-context) -- Celes confirmed from product-registry, no roster/prefix clash. issue-standard §7 Celes-item CLOSED. Reviewed Celes's PO-prompt citations of my contracts (po-template/gama cite issue-standard+protocols §1; ownership/ready-as-M/ssh-exec/§1.0-rules/§1.3-gate/Nunes-card/registry.json-ref) -- ALL FAITHFUL, no misreads. MY S60 LANE COMPLETE.** Truly-open = Mihkel-only (infra, GH scopes, org-board, priority).
- **[S60 NEW LANE 2026-07-14 -- COMPANY STATION DESIGN] Wrote `designs/new/company-station/station-design.md` (*FR:Herald*).** Mihkel direction: durable async = stationmaster as a DEDICATED company-grade station (not FR's poc). THESIS: hub already built+smoke-tested (Brunel S50) + protocol v1.0.0 RATIFIED (S49) + lifecycle-independent by construction (sm-shell per-conversation, ssh-only persistent proc, sm-state volume) -> "dedicated" = PROMOTE from FR-poc to company infra (own host/state/operator/tenancy), protocol UNCHANGED, only positioning+ownership+tenancy change. TENANCY: team-level addressing (agent-level @-defer), isolation = channel-is-identity + partitioned spool + grant-at-deposit + from_team-stamped-not-content; registration = human station-keeper step (v1); OPERATOR = dedicated `station-keeper` role (NOT a consumer team; a la Strabo/Hopper). **S59 DELIVERY GAP as REQUIREMENT INPUT (Aen): the courier once-at-load resolution gap is COURIER-SIDE; station must not inherit silently -> 3 mandates: (1) hub `deposited_uncollected` aging = monitored dark-courier signal (detection-upstream-of-recovery), (2) courier continuous-resolution MANDATORY at onboarding (not once-at-load), (3) identity-scoped single-launcher singleton-guard + reap-before-acquire.** MIGRATION: hub NOT yet deployed + ghost-bridge v2 decommissioned/no-traffic -> clean slate, company station = FIRST deploy, single-station-of-record, no dual-instance. PO-team v1 UNCHANGED (GitHub+tmux); station = the §7 v2 substrate -> UPDATED protocols.md §7 to POINT at station-design (not restate). 5 OQs for Mihkel: host slot (rec prod-llm dedicated port; RC:2221 reserved slot candidate), station-keeper seat, first-onboard order (phased FR->PO+products->existing), repo relocate to infra/, self-service-registration horizon. Did NOT need Finn/Brunel pull (had material); flagged Brunel(image)/Hopper(deploy) for execution. **[HELD 2026-07-14, Aen] Lane RETRACTED -- was PO thinking-out-loud, NOT commissioned. Stood down: reverted protocols.md §7 pointer back to self-contained fallback text; BANNERED station-design.md as PARKED/uncommissioned (do-not-build/deploy/cross-ref, retained as checkpoint). Doc content is complete + sound IF recommissioned -- pick up from the banner. Nothing to advance until Mihkel actually commissions.**
- **[S60 COMMISSIONED 2026-07-14 -- CONTROL-MSG SEMANTICS] Added protocols.md §1.6 (rev4), cross-ref'd from §3.** Mihkel-commissioned control-message rules: (1) STOP≠REVERT -- cease not destroy; untouched uncommitted tree = deferred decision, reverted = work lost; undo is a separate explicit later instruction never a rider on stop; (2) control msgs act at BOUNDARIES not into the running thing -- reroute at the seam (next `ready` dispatch/issue thread), change what's next not what's already happening; (3) NEVER race a control msg to a working session -- composes w/ §1.3 BUSY (BUSY pane gets NO sends incl stops, except Tier-D emergency under sanction). Provenance = Mihkel's verbatim S60 lesson; the station-lane §7 revert IS the named instance of rule 1 (stopped + preserved, not reverted). Cal filing wiki entries in parallel -- left provenance line to cite entry names once he reports. protocols.md now rev4.
- **[S58 CLOSE 2026-06-18 -- UNPIN LIVE ON 2.1.181, SHIPPED] CLI is now 2.1.181; the lifecycle rework is the LIVE protocol.** S57's halt was OVERTURNED as a COLD-START FALSE NEGATIVE (probed inside the V4 window: config.json eager but sessions/<pid>.json ~10-25s later). Live truth confirmed: eager `session-<sessionId[:8]>` dir, resolver resolves bare + via --session-pid, Step 3 restore PASSES, Step 2.5 courier UP after 3 bug fixes.
- **[S58 MY WORK -- ALL DONE]** TASK (cross-check design vs live): eager-write + `session-<id>` naming both now LIVE-CONFIRMED (was probe-only); design docs CORRECT, no claim edits. 3 courier bugs triaged: A=impl (runbook order, Volta); B=impl + design-silence (Brunel); C=genuine protocol gap (Brunel).
- **[S58 TASK #5 COMPLETED]** 3 sub-items: (1) Step 2' cold-start sentence -> `startup.md:136` (ROUTED to Volta, he applied it attributed to me + a lead-in carrying my "any existence/liveness probe" generalization); (2) rotation-contract teardown half `reap-prior (kill+drain+release lock)->resolve->acquire` -> lifecycle doc Section 3; (3) Bug-C at-scale OPEN -> lifecycle doc Section 9 OQ#6. All in `docs/lifecycle-rework-implicit-teams-2026-06-18.md`.
- **[S58 CAL -- BATCH OF 3 FULLY CLOSED]** F1 GOTCHA cold-start-false-negative: FILED + stage-2 CONFIRMED (my read-back) + indexed; source-agents [aen,hopper,herald]; my generalization folded as attributed Scope section. CONTRACT courier-rotation-reap-prior: FILED + confirmed (author-solo); mechanism-vs-requirement + 3-artifact triad preserved. OPEN at-scale-single-point: filed as attributed tracked-gap subsection ON the contract (co-located, not standalone findings/). No outstanding Cal obligations.
- **[LESSON BANKED S58, durable]** A cold-start FALSE NEGATIVE (probe inside the V4 write-order window) can masquerade as a permanent substrate property and HALT a migration (S57 did exactly this). Rule now in startup.md + wiki: within ~25s of cold start, "nothing here" must AWAIT/RETRY before concluding absence -- never lazy-create. Generalizes beyond pid-tiebreaker to ANY existence/liveness probe.
- **[LESSON BANKED S58, durable]** Rotation contracts have a teardown half. "Re-resolve on restart" (acquire) is incomplete without "reap prior process + release lock" (teardown). Requirement (the contract) vs mechanism (lock-staleness PID-1-starttime spec) are distinct: requirement-without-mechanism reaps blindly; mechanism-without-requirement detects-but-never-acts.
- **[CARRY -- NOT mine to drive]** v2/at-scale OPEN (NOT a v1 blocker, Aen): single-point Config-load inbox-dir resolution is not self-healing; at 2nd-team migration needs poll-loop re-resolution OR per-delivery inboxes_dir liveness check. Owner TBD at v2 (likely WS1/courier). Discoverable via the contract subsection + `v2-open`/`self-healing` card tags.
- **[CARRY -- S55, still open, non-blocking]** #9 P6 attached-pane re-test (RfC-scope, deferred -- proactive-wake is a latency not delivery property; courier poll-loop guarantees delivery). Not mine to drive.
- **[CARRY -- courier-ref impl queue, PARKED on TL/PO go]** (1) lock-staleness boot_id/PID-1-starttime fix per `docs/courier-lock-staleness-fix-spec-2026-06-15.md` (now the MECHANISM cited by the new reap-contract); (2) inject down-agent occupied-inbox B+A.
- **[S58 board state]** Tasks #1-#5,#8 completed; #6 (catalog -- Cal, F2/Bug-A still pending Volta), #7 (#86 closure -- Aen) in_progress, not mine.

---
## Session transcript (prune beyond line 100)

### S58 (2026-06-18) -- unpin shipped; my lane closed
- Spawned by Aen to cross-check the two design docs vs the live 2.1.181 truth (S57 halt overturned). Reoriented (startup/common/herald-prompt/scratchpad + both docs), sent intro + findings BEFORE editing (scope gate).
- Scope confirmed -> Task 1 routed to Volta (startup.md single-writer), Task 2 (both additions) applied to lifecycle doc by me. Then submitted 3 Protocol-A entries to Cal; F1 read-back confirmed; batch closed across several windows (Cal processes 1-at-a-time; my batch sent 16:04-16:06 so each "send #N" was already queued -- timing-crossed, harmless).
- Note for next time: when I send a Cal batch in one window, tell him up front the whole batch is already in his inbox so he doesn't prompt "send #N" per item.

### S57 (overturned) -- DO NOT trust S57's "lazy-create" claim
- S57 reported "no eager team dir, no session-* dirs, lazy-create hypothesis" and HALTED. That was a cold-start false negative (checked inside the V4 window). S58 live-validated the eager dir IS written. The design docs were right all along; only S57's report was wrong. Captured as wiki gotcha cold-start-discovery-false-negative.

### S55 (2026-06-18) -- CONDENSED (durable detail in docs + Cal wiki)
- WS2 lifecycle-rework design (#86) ACCEPTED-FINAL: STARTUP TeamDelete+TeamCreate -> Step 2' Discover; SHUTDOWN S5 leadership-release EVAPORATES (5->4 phases); Step 3 restore + S4 persist KEEP+STRENGTHEN (agent-name-keyed bridge survives rotating team name). Resolver = ONE fn `resolve_team_dir(claude_home,*,session_pid,explicit_dir_name)`, TWO callers (lifecycle passes pid; detached courier omits -> glob+liveness).
- WS3b probe (Hopper, 2.1.181): V3 worst-case = sessions/<pid>.json NOT GC'd, lingers status:"idle" -> liveness MUST be process-based not status (fix MERGED into stationmaster-courier.py). V4 = config-first, sessions late (the cold-start window). V5b/P6 inconclusive-non-blocking.
- LESSON (S55): evidence-by-execution caught 3 production-gating bugs an equivalence-argument shipped past. Run the literal artifact against its real substrate.

### Pre-S55 (condensed -- durable knowledge in wiki/docs/topics)
- S54: `-courier` channel-naming (closes apex reply dead-letter). S53: `topics/11-deployment-lifecycle.md` (CCR protocol). S52: apex two-customer round-trip + InstanceLock staleness spec. S50-51: stationmaster-courier.py core (per-consignment inject, fan-out per-destination-outboxes). Pre-S50: S40 playbooks; T03 Protocols 1-5; agent-spawn-protocol v2.0.0; Prism federation; T03/T06 boundary Herald=shapes/Volta=state-machine.
