# Herald Scratchpad

## Summary (lines 1-15 -- always read on startup)
- **[DEFERRED -> NEXT SESSION scope, Aen 2026-06-17 S54-close]:** the **2.1.178 unpin** -- Claude CLI is pinned to 2.1.177 because 2.1.178 removed TeamCreate/TeamDelete (FR lifecycle depends on them). Next session's protocol-domain work = **courier runtime name-discovery + lifecycle rework** so FR no longer hard-depends on the removed lifecycle primitives. MY angle: the courier's channel-naming (just standardized on `-courier` this session) and the InstanceLock/daemon-supervision discipline are the protocol surfaces that touch runtime name-discovery -- start from how the daemon discovers its team/outbox names at runtime vs. compile-time config. Also carry my still-open courier-ref **implement-next queue** (lock-staleness boot_id/PID-1-starttime fix per `docs/courier-lock-staleness-fix-spec-2026-06-15.md`; inject down-agent occupied-inbox B+A) -- both PARKED on TL/PO implementation-go, may intersect the lifecycle rework.
- **[S54 DONE 2026-06-17] `-courier` channel-naming convention adopted in shared courier + spec docs (Task #1).** Closes apex-found reply dead-letter class (reply targets inbound `from`; courier watched a differently-named outbox → silent dead-letter). apex (Schliemann) flipped their side live + kept compat; PO ratified "adopt now." **Code (stationmaster-courier.py):** (1) `rewrite_attribution` L664 `from = <from_team>-courier` (was `-ghost`) + docstring rationale; (2) `_outbox_to_team` L808 strips `-courier` FIRST then `-bridge` (backwards-compat loop `("-courier","-bridge")`) so live `apex-research-bridge` config keeps routing through staged flip; bare name passes through; multi-outbox still None (refuse-and-retain). Also fixed 2 inline-comment drifts (config example L67, deposit_spool NOTE L470). **Spec docs:** hints §1/§3/§4 (PRIMARY doc) + onboarding §routing/§worked-example/§revision-changelog (added S53 revision note, did NOT rewrite S51 historical record). **Consumers updated (Gate-2 surfaced):** integration-test.py anti-spoof assert → `hr-devs-courier` + outbox → `hr-devs-courier` + 3 new routing regression checks (17/17 PASS, was 14); fr-courier-daemon.py L206 stale comment `-ghost`→`-courier` (Brunel's file, comment-only, flagged to him). **4 gates PASS.** Dry-validate: `--ping` GREEN (hub sees framework-research, fp SHA256:nkmhWN..., proto 1); **`--once` correctly REFUSED by InstanceLock — live persistent daemon (pid 34660 = fr-courier-daemon.py, confirmed via CIM CommandLine) holds the lock. Did NOT kill it (task forbade disturbing live link). Live daemon runs OLD in-memory code (started 06-15) → runtime flip = Brunel's gated restart; my on-disk code is ready.** **GOTCHA banked (→ Cal):** Windows `_pid_alive` is NOT conservative-only — on this Estonian-locale box `tasklist /FI "PID eq N" /NH` listed live python.exe 34660 correctly; my earlier shell `grep` falsely showed empty (pipe ate it), nearly misdiagnosed a live lock as stale. Lesson: verify pid liveness with CIM CommandLine, not a grepped tasklist, before assuming a lock is reclaimable. **5 files changed, NO git (TL's session commit), NO config touched (Brunel's). Reported to Aen, code-ready (unblocks Brunel flip).**
- **[S53 DONE 2026-06-16] CCR protocol doc shipped -- Task 1 of the Coordinated Container Rebuild plan (`docs/ccr-protocol-plan-2026-06-16.md`, spec `docs/ccr-protocol-spec-2026-06-16.md` v2 PO-approved).** Authored `topics/11-deployment-lifecycle.md` (NOT 09 -- plan picked wrong slot; 09=dev-methodology, 10=guild-specialists already taken; I CAUGHT the collision, TL confirmed 11). Four sections, all spec Components mapped: (1) Purpose & principle = Monte's "teams own content; FR gates activation" + Montesquieu legislative(team proposes in repo)/judicial(FR reviews vs checklist)/executive(FR rebuilds in window), no branch acts alone [sign `(*FR:Monte*)`]; (2) deploy-surface convention = `deploy/` thin surface, Dockerfile pins STABLE entrypoint path + files NOT relocated (Dockerfile stays put, e.g. Dockerfile.apex at root), versioned startup payload, `deploy/MANIFEST.md` YAML frontmatter validated vs vendored `deploy/manifest.ts` (copy of canonical `types/t10-ccr-contracts.ts`); frontmatter fields VERBATIM = schema_version(str), startup_units[](name,command,long_running:bool), persistent_paths[](path,kind:generate-able|stateful), maintenance_contact(str); generate-able=regen-if-absent vs stateful=preserve-never-reset (mis-tag = `.claude.json`-wipe class); ownership split FR=bootstrap(Dockerfile,base img,vol wiring) team=startup units; (3) change->rebuild flow = team PRs on OWN repo + sends FR hub `rebuild-request` JSON {seq,repo,pr,summary,urgency:routine|soon|urgent,requested_window} VERBATIM from spec, PR is the contract / hub msg coordination only; (4) pointers to ccr-review-checklist.md + ccr-rebuild-execution.md + types/t10-ccr-contracts.ts [sign `(*FR:Herald*)`]. Style: ASCII " -- ", no em-dashes/placeholders. **2 commits on branch `fr/ccr-protocol`:** c49725e (initial author at 09) then 17bdd15 (git mv 09->11 R100 + topics/CLAUDE.md index line). TL's commit 4ac0c8a (plan/spec ref-fix 09->11) sits between mine. Working tree clean. **CARRY: my other CCR deliverable involvement** -- Task 2 typed contracts `types/t10-ccr-contracts.ts` lists me as report-shape co-owner (RebuildReport interface) w/ Brunel; Tasks 3/4 are Monte+Brunel/Hopper. Reference instance #1 (apex courier-bake) DEFERRED by PO until full protocol design done. **The S52 courier-lock + inject-occupied fixes (below) are the persistence discipline this protocol now formalizes (manifest persistent_paths + supervision checklist) -- my implement-next queue still stands.**
- **[S52 DONE 2026-06-15 13:36] FR→apex deposit-shape handed to Hopper (verify-3c round-trip prep, Aen path-(a)).** Same protocol-designer handoff as register: I resolve wire shape, Hopper executes (no operator sm-deposit; it's a sm-shell protocol request). Shape (verbatim protocol §3/§4/§5.2 + hints §3): conversation = envelope line `{"v":1,"cmd":"deposit"}` + ONE consignment line `{"to":"apex-research","entry":{from,type,summary,text,timestamp,read}}` over `ssh -T -i ~/.ssh/sm_framework-research -p 2222 sm@10.100.136.162`. TEST marker `[FR-S52-ROUNDTRIP-TEST]` in BOTH `text` (rendered body) + `summary` (chip). **Two traps flagged (hand-crafted-deposit-specific):** (1) body in `text` NOT `content` (my CR-7 errata; else renders undefined on apex); (2) SUCCESS = per-consignment DATA LINE `{"status":"accepted"}` after `ok:true` -- `ok:true` w/ no data line = NOTHING landed = re-send (hints §3 rule; her accept-vs-silent discriminator). PRECONDITION = apex-research grant of FR (E_NOGRANT else); gave one-line `status` check for `grants_out=[apex-research]` (present S51, persists on vol). **[OUTCOME 13:51] DEPOSIT LEG GREEN** -- Hopper fired it; my shape was correct: `{"id":"a55da2736257086c","to":"apex-research","status":"accepted"}` durable, marker in text+summary, no subject. **COLLECT LEG BLOCKED (3rd provisioning gap, courier-config domain):** apex courier (pid 1846) fails collect: `No ED25519 host key is known for [10.100.136.162]:2222...strict checking`. **MY DOMAIN VERDICT [13:53]: config CORRECT not buggy** -- onboarding Step 3 + Appendix A (I authored) require host-key pinned OUT-OF-BAND by operator before first send; courier ref passes ssh_opts verbatim (L199), agnostic to trust provisioning, does NOT seed known_hosts BY DESIGN. So = missing-provisioning-step (same class as courier-key seed), NOT ref defect / config error. FIX = seed KNOWN-authentic host key (fp SHA256:CNcFjOxr8vREOueOS8nxJN8W3LaQHet62du+PHyK13U, FR-pinned + Hopper-no-MITM-verified S51), NOT ssh-keyscan (keyscan=trust-whatever=defeats MITM gate); hub host keys persist on sm-state vol → fp stable → seed stays valid. Bake (Brunel) or repoint UserKnownHostsFile to persistent vol. **PO decided: rebuild-#4 bakes it** (Hopper source from FR-box known_hosts → Brunel bake → Aen 5th commit). No data loss (both consignments durable on spool). **[MY LANE]: (a) doc note to onboarding Appendix A -- DONE+LANDED 14:02** (Aen reviewed+approved as-is; 4 gates run, gate-4 substrate-invariant the operative one). Added lesson (iv) [host-key MUST be provisioned; ephemeral-~/.ssh trap; bake-or-persistent-vol; no-TOFU/no-keyscan; symptom + apex-S52 ref] + config-line inline comment on L157. Aen commits w/ session state (git=his). **(b) Cal trust-triangle structure APPROVED [14:02]** -- queued (umbrella `patterns/courier-trust-triangle-three-provisioning-legs` + leg-3 gotcha `gotchas/courier-strict-hostkey-needs-provisioned-known-hosts` + cross-links; sibling-not-stuffing call endorsed). Reusable canonical hand-crafted-deposit shape also → Cal reference candidate. **STANDBY: host-key ground-truth fallback if Hopper pings** (unlikely -- her FR-box source + fp match cover it). PO closing collect via rebuild-#4 bake. apex DOWN. Standing by.
- **[S52 DONE 2026-06-15 13:08] apex courier pubkey REGISTERED hub-side as `apex-research` -- GATE A cleared.** Brunel's "needs the hub/protocol path" flag RESOLVED: registration = OPERATOR inside-container path `docker compose exec stationmaster sm-register apex-research '<key>'` (protocol §2 line 23; sm-register idempotent-replace; runbook §5). FR hub key forced-command-locked to `sm-shell framework-research` → CANNOT self-register; channel IS identity. OWNER=Hopper (operator, prod-llm docker-exec; standing PO exception). I drove request-shape + verify-plan; Hopper executed. Full fp = `SHA256:NBq5a/r3GsTuIGME1BzsklC9Sr+6VrltsxoSfW4QsaE`. All 3 verify PASS: (1) register line OK; (2) --list shows apex-research [idempotent REPLACE of stale S51 ephemeral binding]; (3) authorized_keys line-5 readback == fp. Pubkey reconciled byte-identical to live rc-host .pub. Key identity verified whole chain: build-secret seed→entrypoint Step-7b→~/.ssh/stationmaster_apex→hub authorized_keys. Hub-side only, apex container untouched, hard gate clear. Closed loop to TL. ROUND-TRIP still gated on GATE B (Brunel's inboxes_dir bake + TL 3rd commit -- NOT mine; courier exits at validate_startup before dialing). Hub admin access needs registry key `~/.ssh/id_ed25519_apex michelek@... num:9`; compose dir /home/michelek/stationmaster. Task COMPLETE; standing by.
- **[S52 WIP 2026-06-15 13:22] courier-reference InstanceLock bug -- Brunel found, my domain (`stationmaster-courier.py` is mine).** BUG CONFIRMED vs code: lock rec `{pid,ts}` only (L859); `_is_stale` (L863) = `not _pid_alive(pid)` pid-liveness ONLY; `ts` unused. Container recreate resets PID namespace → prior-container pid aliases live proc in fresh container → false-negative staleness → courier REFUSES start ("another instance running"). Triggers on ANY ungraceful death (SIGKILL/OOM/docker kill) + persistent state_dir; defeats apex supervisor relaunch. NOT blocking apex now (Hopper one-time-clears test lock). **MY REFINEMENT:** Brunel's `boot_id` fix has a granularity bug -- `/proc/sys/kernel/random/boot_id` is HOST-kernel-scoped (containers share host kernel; `--force-recreate` does NOT change it) → misses the observed recreate-on-live-host case. Counter: CONTAINER-INSTANCE discriminator = PID-1 starttime (`/proc/1/stat` field 22), record in lock, `_is_stale`=True if epoch differs (diff container⇒stale), pid-liveness fallback only within same epoch. Windows degrade: keep conservative tasklist path (/proc absent). Could NOT empirically test (/proc absent on this Win box -- reasoning from documented Linux semantics). **[FIX-SPEC WRITTEN 13:24]** `docs/courier-lock-staleness-fix-spec-2026-06-15.md` -- staged diff §3 (_container_instance() helper + cinstance in lock rec + _is_stale epoch-compare; backward-compat: old lock w/o field → pid fallback; non-Linux empty epoch → fallback), alternatives §5, test plan §6 (unit Windows-runnable + Linux container-recreate repro), OQ §7. **TL HOLD (13:22): do NOT modify shared stationmaster-courier.py (apex+FR+others blast radius) until PO scope call (this-session-vs-follow-up).** I ACK'd HOLD, did NOT request fast-track (apex finish independent). **[PO SCOPE 13:24 = spec-now/implement-next.]** **[BRUNEL CONCUR 13:26 -- gate (b) CLEARED]**: agreed PID-1-starttime over boot_id (admitted boot_id was his error); his substrate-check CLOSED my OQ-1 (gosu→courier sees own PID 1) + OQ-2 (/proc/1/stat readable, no hidepid; hidepid=2 future-risk covered by degrade) -- both folded into spec §7. **SPEC LOCKED + IMPLEMENTATION-READY 13:27.** Remaining gate = (a) TL/PO implementation go (PENDING -- after Hopper closes apex round-trip; this session if time else next). HOLD still held (shared reference untouched). CO-FILE Protocol-A gotcha (Brunel=finder, me=owner): `gotchas/courier-lock-pid-only-staleness-false-negative-across-container-recreate` (root cause + boot_id-host-scoped trap = load-bearing) -- in Cal queue item 4, for Cal spawn; Brunel does finder read-back to advance Stage-2. Sibling to S51 spool/lock + Hopper's v2 stale-PID-36772. PARKED on TL implementation go; NOT idle.
- **[CAL QUEUE -- Protocol A, 4 items, Cal NOT spawned yet → submit when routed or next session]:** (4) GOTCHA `gotchas/courier-lock-pid-only-staleness-false-negative-across-container-recreate` [co-author: Brunel=finder, Herald=owner; PO scope-call 13:24 = spec-now-implement-next]: courier InstanceLock reclaims stale locks by pid-liveness ONLY; PID namespace resets on container recreate → a prior-container lock's pid aliases a live unrelated proc in the fresh container → false-alive → courier REFUSES start. Triggers on ANY unclean exit (SIGKILL/OOM/docker kill/crash) + persistent state_dir. **LOAD-BEARING TRAP: the obvious fix `/proc/sys/kernel/random/boot_id` is HOST-kernel-scoped (shared by all containers on a host; unchanged by --force-recreate) → MISSES the recreate-on-live-host case. Correct discriminator = container-instance epoch (PID-1 starttime, /proc/1/stat field 22).** Fix-spec: `docs/courier-lock-staleness-fix-spec-2026-06-15.md`. **OBSERVED n=2 [Hopper, authoritative 13:44]:** lock rec `{"pid":1691,"ts":"20260615T101014057235"}` (1691 = courier kill-9'd 13:10); SAME rec, OPPOSITE outcomes across recreates. DP1 (13:17, ctr 13c94177 fresh-from-78377bcb) REFUSE `RuntimeError: another courier instance is already running...refusing`. DP2 (13:23+13:28+13:39) RECLAIM `WARN lock: reclaiming stale lock`→up. **MEASURED vs INFERRED (Hopper honesty caveat -- HONOR IT): DIRECTLY OBSERVED = same-pid/same-rec → opposite outcomes + pid 1691 ps-confirmed DEAD by 13:21 (~4min post-refuse). INFERRED, NOT measured = os.kill(1691,0) value AT the 13:17 refuse instant (the aliasing-alive condition). Do NOT cite as measured.** Mechanism (deterministic-mechanism / nondeterministic-outcome via pid-alias) = BEST EXPLANATION, backed by (a) observed opposite-outcomes-same-pid + (b) Brunel code read (_pid_alive=os.kill only, rec {pid,ts} no boot-id, ts unused). Per-instant os.kill = the one inferred link; optional Tier-R confirmatory probe (kill-9→recreate→os.kill/ps lockpid at boot pre-check) available from Hopper to upgrade to measured -- tasker's call, touches apex (spec §6.4). **EXTRA: neither SIGKILL nor clean SIGTERM releases lock on exit (no self-cleanup) → ANY unclean exit leaves stale lock.** Fix makes both deterministically reclaim (epoch always differs). Folded into spec §1+§6.4. Sibling: S51 spool/lock + Hopper v2 stale-PID-36772 + FR stop-script drain+release workaround.
- **[CAL QUEUE -- Protocol A items, TL-endorsed 13:10; Cal NOT spawned yet → submit when routed or next session]:** (1) GOTCHA `gotchas/hub-admin-access-needs-registry-key-not-default`: hub-side operator ops (sm-register etc.) need registry key `~/.ssh/id_ed25519_apex` (registry num 9) for `michelek@10.100.136.162`; the default key gets Permission denied. Compose dir `/home/michelek/stationmaster`. (2) PATTERN `patterns/keyed-service-identity-verify-fp-at-every-hop`: a build-time courier key verifies as ONE identity end-to-end by checking the SAME fp at each hop (build-secret seed → entrypoint copy step → ~/.ssh → hub authorized_keys); reusable verification method for any keyed-service hardening. Instance: apex stationmaster_apex, fp SHA256:NBq5a/r3GsTuIGME1BzsklC9Sr+6VrltsxoSfW4QsaE. (3) CONTRACT/GOTCHA `contracts/forced-command-lock-makes-registration-operator-domain`: a stationmaster customer's hub key is sshd-forced-command-locked to `sm-shell <team>` (protocol §2) → it CANNOT run sm-register; registration is structurally operator-domain (docker-exec inside container), a courier key can never self-register. Clean §2 confirmation. (5) GOTCHA `gotchas/courier-strict-hostkey-needs-provisioned-known-hosts` [leg-3 of trust triangle]: courier.json ships StrictHostKeyChecking=yes + UserKnownHostsFile but NOTHING provisions that known_hosts; on ephemeral-~/.ssh container → courier authenticates ITS key fine but REFUSES to trust hub (`No ED25519 host key known...strict checking`) → collect blocked. Config CORRECT (onboarding Step3 = operator pins host-key out-of-band before first send; courier ref passes ssh_opts verbatim L199, agnostic by design). FIX = seed KNOWN-authentic host key (fp SHA256:CNcF...13U, FR-pinned+verified), NOT ssh-keyscan (=trust-whatever=defeats MITM gate). Bake or persistent-vol known_hosts. DOC GAP: my onboarding Appendix A ships strict config w/o provisioning step (awaiting Aen OK to add note). (6) PATTERN `patterns/courier-trust-triangle-three-provisioning-legs` [UMBRELLA -- Aen 13:51 knowledge prompt]: provisioning a keyed courier = 3 distinct legs, each own provisioning+durability story, each its own failure mode: (1) own private key [no key=can't auth] → links [[keyed-service-identity-verify-fp-at-every-hop]] (leg-1 verify method, stays scoped, do NOT stuff host-key in it -- that conflates client-identity-propagation w/ server-auth); (2) hub registration of pubkey [no reg=Permission denied] → links [[forced-command-lock-makes-registration-operator-domain]]; (3) hub host-key in known_hosts [no host-key=strict refuses] → links the leg-3 gotcha above. Mutual auth = legs 1+3 are OPPOSITE directions. Instance: apex S52. (7) GOTCHA `gotchas/courier-inject-occupied-inbox-loops-when-target-agent-down` [substrate-invariant-mismatch instance; 4th apex finding, Hopper 14:01]: inject_batch (L582-661) occupied-inbox else-branch (L631-637) does `sleep(0.2);continue` WAITING for the harness to drain native entries before exclusive-create. Correct for LIVE agent (~0.8s drain); but a DOWN agent NEVER drains → inject to a non-empty inbox loops 50× (~10s) → exhausts → won't ack (SAFE: no clobber, hub redelivers, no data loss). NOT contention, NOT CAS-bug, NOT T6.a (that's empty-file race). 1st consignment CREATES (empty path OK), 2nd APPENDS-to-occupied (loops, since 1st sits undrained). Invariant relied on = "non-empty inbox ⇒ live harness drains shortly"; fails on down-agent substrate (= apex round-trip test condition). FIX OPTIONS to Aen: A=extend rename-aside-merge to occupied case (HAZARD: races live harness mid-drain -- needs down-vs-live distinction), B=cap-and-surface honest "agent likely down" after N stable rounds (safe), C=file-as-finding (real traffic targets live agents). MY REC: B+C now, A queued WITH lock fix (not rebuild-#5 rush). **SEVERITY = HARD STICK while apex DOWN, does NOT self-heal on redeliver (08:15 never leaves the inbox → every a55da27 redeliver re-loops); auto-resolves ONLY when apex agent comes UP + drains team-lead.json → next redeliver hits empty→create path.** Adjudicated Aen's 3 hypotheses (14:06): NOT (i) same-poll-stale-CAS [the 2 consignments were diff polls -- 08:15 earlier, a55da27 later], NOT (ii) shape-specific, NOT (iii) general CAS defect. **Round-trip ALREADY GREEN via 08:15 WINDOWPREP collect→inject→ack on fixed image -- a55da27 is redundant 2nd probe; host-key fix worked.** **[Aen 14:09 -- SCOPE SETTLED]: C now (file+Cal), A+B QUEUED with lock fix (spec-now-implement-next, HOLD held, his go). LOW-SEVERITY for apex (self-resolves operationally): while down, a55da27 redelivers+fails-inject every 30s = harmless log noise, durable on spool, NO spool-delete needed; when apex next UP → inbox drains → a55da27 injects → apex gets clearly-marked test msg they discard = self-cleans (delivered-then-discarded). No data loss/clobber throughout.** My courier-ref implement-next QUEUE (2 items): (1) lock-staleness boot_id/starttime, (2) inject down-agent occupied-inbox (B cap-and-surface + A safe-append; B's content-stable-across-rounds heuristic = candidate down-vs-live signal for A).
- **Current state (S51 close, 2026-06-15):** Stationmaster two-customer (FR⇄apex) fully operational + documented. FR courier daemon LIVE + owns FR↔hub (Brunel/Hopper, pid 20664); I am OFF manual hub deposit/collect. apex courier also LIVE (their side). **All FR→apex traffic now goes via the `apex-research-bridge` ghost outbox** (SendMessage to that name; daemon ships it ~30s). Hub `sm@10.100.136.162 -p 2222`, FR key `~/.ssh/sm_framework-research`, host-key fp SHA256:CNcFjOxr8vREOueOS8nxJN8W3LaQHet62du+PHyK13U.
- **Docs FINAL (rev S51), NO git yet (team-lead's one session commit):** `stationmaster-onboarding.md` status FINAL -- CR-1..7 folded + Appendix A/B apex worked-examples. `stationmaster-protocol.md` §4 errata pins renderable-body=`text` (no major bump, §5.5-errata posture). `stationmaster-courier-hints.md` Outbound §3 = routing + text-field + data-line rules. `fr-courier.config.example.json` fixed to apex-research-bridge + caution. `poc/ghost-bridge/README.md` DECOMMISSIONED banner (v2 dead).
- **[RESOLVED] Fan-out amendment (my long-standing [DEFERRED -- PO ratification]):** PO RATIFIED **candidate A -- per-destination outboxes** (`<team>-bridge`→`<team>`, N teams=N outboxes; single-outbox fan-out OUT OF SCOPE, refuse-and-retain). B/C rejected (kept as fallbacks if a real consumer forces). Filed `decisions/fan-out-routing-per-destination-outboxes-cr4.md`; resolved the `gotchas/courier-originates-routing-protocol-leaves-undefined.md` carry. NO LONGER DEFERRED.
- **Cal knowledge CLOSED:** 8 S51 entries, wiki 132→141, all my read-backs/co-author obligations green. (contracts/consignment-body-in-text-field-not-content [CR-7]; gotchas/deposit-ok-without-data-line; references/drain-on-delivery-datapoint-2.1.173; gotchas/cf-access-apex-sso-header-trust; decisions/fan-out-routing-per-destination-outboxes-cr4; decisions/text-field-pin-clarifying-errata-no-bump; gotchas/v2-ghost-bridge-restart-redelivery-dupe-motivates-hub-ledger; patterns/spool-stores-raw-entry-reresolve-on-deposit.)
- **apex-Q shipped once:** my window-prep question (SEQ FR-S51-WINDOWPREP: Q1 network-mode/cloudflared + Q2 docker-exec route) crossed a routing-bug + hold; spool SELF-HEALED on Brunel's config-fix+restart (deposited 1/1, one copy queued for apex-research, waits apex's NEXT session -- they're in S52 wrap/offline). I did NOT re-send (would dupe). DONE.
- **[CARRY → Task #5] apex container hardening (FR-owned: Brunel designs, Hopper docker-exec).** PO-gated + unscheduled. HARD PRECONDITION before exec: a CONFIRMED FR→apex docker-exec route at window-time (apex tailscale LOGGED OUT → registry `rc`/100.96.54.170 stale; live IP 10.200.13.114). Either apex logs tailscale IN, or ssh `rc` HOST then local docker exec. Approach = (b) build-time key provision (interim relocate declined). Folded into Brunel's #5 plan; my route-Q is in the shipped apex-Q. NO container touch till gate clears. Closed on my side till window scheduling.
- **[LEARNED] Two substrate properties banked this session:** (1) consignment body MUST be in `text` not `content` (harness renders `text`; verbatim-forward → fix at sender, no receiver remap). (2) courier spool is ROUTE-DEFERRED (re-resolves dest at deposit time from current config) → config-fix+restart RETROACTIVELY self-heals stuck mis-routed mail; do NOT manually migrate/re-send (re-send = new envelope-id = dupe).
- **[CARRY] apex standing watch (Task #2 open):** apex is a live cross-team partner. New apex traffic now auto-collected by FR daemon (I no longer manually courier). Relay any new drain/CLI datapoints to Cal; route apex key-rotation re-registration to Hopper. apex offline (S52 wrap) -- expect their window-prep answers next apex session.

---
## Pre-S50 (condensed -- durable knowledge promoted to wiki / docs / topic files)

- **S40 (issue #71):** extracted 4 procedural recipes from common-prompt into `playbooks/` (verify-structural-change, version-typed-contract, relay-fidelity, shutdown-agent). Extract=procedure, keep=behavioral-contract.
- **S39 (Team OS protocol-lens):** 14 transferable mechanisms delivered to Aen. Their adoption-first design vs FR's rigor-first; "skill-as-protocol-contract" + three-tier-context-loading-as-flow-control as named candidates.
- **S35 (Cloudflare pilot comms):** DO-as-per-recipient-mailbox primitive; `idFromName` identity + KV roster; retain FR SendMessage envelope (envelope IS framework-state, not substrate); Sub-shape E n=3 at comms layer. Design at `designs/new/cloudflare-pilot/comms.md`.
- **S26-27 (Prism federation Phase A+B):** 11 PRs merged (Phase A) + PR #12 (federation-authority-record v0.1.1) + PR #13 (02 §4 R9-rule, SemVer-MAJOR v2.0.0). Symmetric-envelope-mode-by-content-category; strict-SemVer-for-typed-contracts; discriminator `kind` locked; 8 discipline surfaces dogfooded. Worktree-isolation n=7 cumulative.
- **S59-60 (xireactor + agent-spawn):** xireactor pilot protocol v1.2 (`docs/xireactor-pilot-protocol-2026-04-15.md`); Protocol D canonical-taxonomy slot. `agent-spawn-protocol.md` v2.0.0 -- Agent-tool persistent spawn default, `team_name+name` discriminator; session-lifetime caveat load-bearing. T03/T06 boundary: Herald=shapes, Volta=state machine.
- **Pre-S59:** T03 Protocols 1-5 in `topics/03-communication.md`; T09 dev-methodology (XP message types, three-strike escalation, three Librarian protocols A/B/C); single-provider as protocol-level requirement; Entu API BFF pattern for SvelteKit.
- **S50 (stationmaster courier):** `stationmaster-courier.py` (8/8 unit) + `integration-test.py` (14/14, transport stubbed); 3 orthogonal test layers (courier-logic / ssh-transport / inbox-race). Per-consignment inject (ledger 1:1 with hub id); partial-deposit retain-non-accepted; anti-spoof `<from_team>-ghost`. Filed `patterns/same-volume-startup-gate-for-rename-atomicity.md` + the courier-routing gotcha (now RESOLVED S51).
