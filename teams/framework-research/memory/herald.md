# Herald Scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state (S65, 2026-08-27, resumed 14:51 after session-limit kill at 13:33):** #108 verdict POSTED by Aen (issuecomment-5437728323 + A13 addendum -5437761255 + second addendum 14:53). **Stage 1 GO items DONE 14:53:** protocol.md §1 "(prod-llm)" struck (E2); §11 Errata/Changelog added (E1 S51 `text`, E2; open backlog: `waiting_for_me.oldest` minor, verb table, §9 agent-addressing wording, two binding layers); Step 2 operator/steward + A13 Step 6 block + hints §1 clause sent VERBATIM to Brunel (he owns those files) -- INSERTED by him 14:56, commit-ready per Aen (onboarding L46 / A13 block L128-138 placed after the Step 6 bullet list, my call accepted / hints L24; provenance sentence on the §1.1 six-weeks precedent added); §6 inventory row to Aen via Brunel packet. Cal: solo-wake cell addendum RE-SENT; po-team §1.1 six-weeks precedent sent for Aen's dead-SendMessage entry. HELD: A1 (PO gate), A2 verb table, A11 move, code. Original verdict 13:25: **AMEND, then ADOPT**, A1-A13, A1 BLOCKING.
- **A1 [BLOCKING, empirical]:** TWO disjoint hub instances. FR's key against `sm@10.100.136.162:2222` (prod-llm) -> registry {framework-research, apex-research, alpha, beta, fr-test}, uptime ~75 d since 2026-06-12. Sagres hub (100.102.133.125, 2026-07-15) holds po-team/mvox/passepartout. Proposal §3 knows only sagres; decision card excludes relay. Recommend FR+apex re-register on sagres, decommission prod-llm hub, revoke 3 scratch names. Operator executes, FR specs.
- **Other amendments (short):** A2 verb inventory = 3 docs/3 subsets (`revoke` absent from onboarding entirely; protocols.md §1 omits ping/revoke/registry; comms MCP exposes 1 of 8 verbs) -> canonical table + spec `hub_status()` MCP tool. A3 contract §1 hardcodes "(prod-llm)" -> remove + add Errata section. A4 age alarm: `waiting_for_me` has no `oldest` (receiver-side age not computable) -> minor bump 1.1.0 + detection-arm spec. A5 decision card "mail never over MCP" superseded by deployed `send`. A6 `entry.to` agent routing undocumented (2 couriers, 2 behaviours). A7 solo-wake claim contradicts P6 (2.1.179) -> version-stamp, treat like T1.b. A8 courier->tmux nudge needs §1.0-1.3 driving discipline; DIALOG sentinel still OPEN -> not adoptable yet. A9 courier conformance checklist (3 couriers in field). A10 name `inject_patience_s` (=50x0.2 s=10 s). A11 home = `designs/deployed/stationmaster/` + redirect stubs. A12 onboarding conflates operator/steward.
- **A13 (13:28, from Aen's 2.1.247 measurement):** SendMessage-to-ghost-outbox enqueue is DEAD on 2.1.247 (harness resolves live agents, not `members[]`); po-team recorded the same 2026-07-16 (protocols.md §1.1) and FR docs never absorbed it. Amend onboarding Step 6/hints §1/skill: comms MCP `send` = primary enqueue; harness-native path = per-version datapoint (T1.b-style). Brunel + I aligned on wording; adopted his A8 alternative (courier drains when no live harness) over the tmux nudge.
- **(e) Runbook = REWRITE** (status is the least stale field; ~9 stale facts; shape: per-instance reality / operator procedures / build-from-source kept / closed-questions LEDGER with evidence). **(d) FR §3 row drafted**, Brunel to correct. **STRUCK (Aen 13:28):** "fr-courier.log 0 bytes n=2" -- courier logs to stderr, launcher splits streams, real log is `fr-courier.log.err` (populated). Aen's false alarm, refuted by Brunel A9.
- **STAGE 2 (un-held 15:30 -- PO answered A1: TWO ISLANDS BY DESIGN, #108 issuecomment-5439161208; git = inter-island bridge; a1.1 FR MAY opportunistically join sagres from home box):** item 1 (A2) DONE 15:32 -- contract §5.0 canonical verb table (E3; "who's on THIS hub instance" on registry), onboarding mirrors (table + Step 4 revoke) to Brunel, po-team handoff sentence+table to Aen for Henry. Item 2 DONE 15:33 -- a1.1 dual-homing spec v0.1 delivered as message to Aen (one name two keys never shared / operator grant round / systemd+linger home courier, configs gitignored both boxes / RULE: no cross-island mail or failover ever, git or human / Step 1 host-check decision table, fail-closed third row, Volta patches / merge hazard now structural -> pull-first, diff-against-both-parents / N islands = N rows). Item 3 SETTLED 15:38 in the one exchange -- **`designs/deployed/stationmaster/`**, Brunel conceded (docs/CLAUDE.md ground + operator-role precedent), 4 conditions confirmed: package README (first line disarms per-instance misreading + my authority line), 4 docs only (formation spec stays docs/, code+TRUTHS/SPEC-v3 stay poc), link fix-list (MY CORRECTION: protocol.md:5 `../../playbooks/` has NO repo-root target -- must become `../../teams/framework-research/playbooks/...`), MOVED stubs per relocation pattern. Runbook-timing precision moot in the good direction: Brunel rewrote it IN PLACE (`3111cf5`) before the move, so the rewritten runbook moves; dual-homing spec v0.1 (+§8 rulings) lands in the SAME move batch, Aen hands it to Brunel. Move EXECUTED 15:41 (4 docs + README at `designs/deployed/stationmaster/`; my uncommitted §5.0/§11 traveled intact; A2 inserts in, mirror diff identical). **E4 + Canonical-home header line WRITTEN 15:42** at the moved contract -- edit protocol.md at the NEW path only, old path is a stub. My Stage 2 = fully landed; Brunel and I closed for the session. **[CARRY -- one open]:** dual-homing spec doc NOT yet in the package dir (Aen hands v0.1+rulings to Brunel); when it lands, ping Cal with its path for `island-local-everything` source-files. Other carriers not mine: Volta (host-check table, Step 3.5 detached, skill retirement), PO (formation execution). Items 1+2 ACCEPTED by Aen (§5.0 committed `5a98d53`; handoff posted #108 issuecomment-5439197981; spec §8 rulings: i accepted / ii deferred / iii yes-host-check-owns-courier-arming, Volta shapes). Protocol A to Cal 15:35: island-local-everything pattern (only the repo crosses) -- FILED 15:37 as `patterns/island-local-everything-only-the-repo-crosses` (high, confirmed, designed-discipline class, cross-linked both ways with Aen's two-islands decision). **[CARRY]** when the dual-homing spec doc lands at the settled home, PING CAL to add it to that pattern's `source-files` (his caveat: spec wasn't in the tree at filing).
- **Aen's 3 notes all TRUE**; note 2 is bigger than stated (FR is on the other hub); note 3 genus = `stale-snapshot-trusted-as-current`, 2 fresh instances (contract §1 host; deployments.md:48 ":22 only" yet :2222 answered).
- **Wiki (Cal filed 15:08, all uncommitted, read back by me 15:09 -- faithful, no corrections):** `gotchas/singular-convention-plural-instances-enumerate-from-the-registry` (high, confirmed; no-slot shape, NOT stale-snapshot); AMENDMENT on `decisions/stationmaster-post-office-model` (bullet 4 marked superseded for OUTBOUND leg; retained invariant = inbound never over MCP, courier owns collect->inject->ack; I'm on source-agents); `gotchas/session-wake-on-inbox-write-two-unstamped-claims-contradict` (medium, **stage-2 CONFIRMED 15:15** -- Brunel read back, his watcher-registration mechanism hypothesis + re-test scope folded). Cal's sharpening: the 2.1.179 substrate sheet IS stamped; RFC + protocols.md dropped the stamp when restating. Steward-drift evidence (po-team §1.1 six weeks) folded into Aen's `precondition-without-an-owner-is-no-precondition` entry -- I'm on source-agents, Aen reviews the wording, drift window dated closed 2026-08-27 (A13 remedy live at hints:24/onboarding:133). Brunel touch-points (13:24) all closed by his 13:30/14:53.
- **[LEARNED S65]** Test the map against the registry, not the doc: `registry` was the one call that falsified "the hub" -- a convention described in the singular can have N instances and every per-instance doc reads true. Cheap, read-only, decisive.
- **[LEARNED S65 -- CORRECTED by Brunel, verified by me]** ALL SEVEN deployed sagres hub files are identical to the poc reference under `diff --strip-trailing-cr`; my "sm-register/Dockerfile/compose differ" was CRLF noise from the Windows checkout. Instance customization lives only in sagres' `docker-compose.override.yml`. §7 split holds cleaner than reported. Third ownership category §7 misses: vendored reference implementations (sagres courier 64 lines behind poc, no #106). **[GOTCHA]** on Windows, `diff -q`/md5 across a checkout pair lies -- always `--strip-trailing-cr` before claiming drift.
- **Corrections folded 13:32:** A1 needs a tailnet-reachability PRECONDITION (sagres ListenAddress = tailnet IP only; FR box on WARP, apex on EVR LAN); per-agent `entry.to` routing is in the REFERENCE courier (#106), not FR-only -- fr-courier-daemon carries older GAP-A, po-team vendored has neither (3 currency states); `$PPID=1` observation is Aen's (n=2), not mine; Scheduled Task DISABLED S58; A7 cell stamp (P6 had team dir, Passepartout none) sent to Cal as addendum; A10 cites Brunel's hints §4.5. Brunel's A10 FR row supersedes my draft.
- **[CARRY -- DIALOG sentinel]** still the one empirical gap gating any pane-typing (A8 too): not-confidently-IDLE => no send-keys.
- **[CARRY -- older, durable in wiki/docs]** S60 po-team protocols rev4 / issue-standard rev2 delivered; company-station lane PARKED; control-semantics (stop != revert) filed; S58 unpin lessons banked; v2 at-scale single-point courier resolution OPEN (not mine to drive).

---
## Session transcript (prune beyond line 100)

### S65 (2026-08-27) -- #108 assessment, protocol side
- Read order: issue #108 + Aen's ack comment; proposal (actual path `teams/framework-research/docs/...`, not `docs/...` as briefed); contract v1.0.0; onboarding S53; courier-hints; runbook; protocols.md rev5 §1; decision card; age-alarm gotcha; FR lifecycle material (startup.md courier steps, restart wrapper, .auto.json, rotation contract, Bug A/C cards, cold-start card); RFC teamless-courier (P6); Passepartout design (London-time phrase origin); sm-shell/comms-mcp/inject_batch code; inter-team-comms skill (stale path + prod-llm hub).
- Empirical: ping/status/registry on FR hub (read-only) -> two-hub finding. `diff` poc vs sagres hub artifacts -> sm-shell identical.
- Method note: Aen's outline was the floor; A1/A5/A6/A7/A9/A10 came from reading the code and the registry, not the outline.

### S60 (2026-07-14) -- PO-team protocols + control-semantics; station lane parked (condensed)
- protocols.md rev1->rev4 (tmux-driving §1.0-1.6; GitHub=work-of-record); issue-standard.md rev2 co-owned w/ Celes; company-station design PARKED by Aen -- my §7 over-revert became the named instance in Cal's control-semantics entry (sub-lesson 2). Dual-hub routing worked well.

### Pre-S60 (condensed -- durable knowledge in wiki/docs/topics)
- S58 unpin on 2.1.181; 3 Protocol-A entries (cold-start false negative, rotation-reap contract, at-scale single-point open). S55 WS2 lifecycle-rework accepted; WS3b probe caught 3 bugs. S50-54 stationmaster-courier core, `-courier` naming, CCR protocol, InstanceLock. Pre-S50 T03 Protocols 1-5, agent-spawn-protocol v2.0.0, Prism federation.

(*FR:Herald*)
