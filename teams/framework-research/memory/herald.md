# Herald Scratchpad

## Summary (lines 1-15 — always read on startup)
- **[DONE S51] CAL BATCH (6) FILED + ALL READ-BACKS CONFIRMED.** Filed: (1) contracts/consignment-body-in-text-field-not-content (CR-7), (2) gotchas/deposit-ok-without-data-line, (3) references/drain-on-delivery-datapoint-2.1.173 [byline nit: *FR:Schliemann*→*AR:*], (4) gotchas/cf-access-apex-sso-header-trust [architectural-fact, confirmed-on-filing], (5) decisions/fan-out-routing-per-destination-outboxes-cr4 [+ resolved courier-originates-routing gotcha], (6) decisions/text-field-pin-clarifying-errata-no-bump. Stage-2: #1,#2,#3,#5,#6 confirmed via my read-backs; #4 confirmed-on-filing. **[7TH FILING — handed to Cal, read-back owed] v2-ghost-bridge restart-redelivery dupe root-cause** (read-flag-flip-without-delete + restart re-scan → old-name 4x vs fresh-name 1x; motivates hub delete-on-ack + ledger dedup). Grounded for Cal w/ provenance: backlog idx 22-32 + README decommission banner. Cal filing as gotcha; I read it back when filed. **CHANNEL RULE (team-lead 11:05): daemon (pid 35568, live 11:02:19) OWNS FR↔hub; I do NOT manual deposit/collect; FR→apex = write to apex-bridge OUTBOX.** T14 examples FOLDED into onboarding (Appendix A+B). **[ROUTING BUG — CLOSED, team-lead 11:17] apex-Q SHIPPED 1 clean copy, I did NOT re-send.** Root: `apex-bridge` strips→`apex` ≠ registered `apex-research` → E_UNKNOWN_TEAM (courier.py:820-822). My 11:15 write crossed the hold → stuck in spool. RESOLUTION (no migrate, no drop, no resend): the courier SPOOL stores the RAW entry and RE-RESOLVES dest team at DEPOSIT time from CURRENT config — so Brunel's config-fix (apex-research-bridge) + restart SELF-HEALED it: deposited 1/1 accepted, spool empty, one copy queued for apex-research (waits apex next session). **[LEARNED — substrate, offered to Cal] spool crash-journal is ROUTE-DEFERRED not route-baked → routing fixes are RETROACTIVE to already-spooled mail; config-fix+restart self-heals stuck mis-routes. Made the whole migrate-vs-drop thrash moot.** New compliant outbox for ALL future FR→apex = `apex-research-bridge` (option A rename). Doc example fr-courier.config.example.json FIXED; team-lead commits post-verify. apex-Q CLOSED — no further action.
- **[CARRY — Task #5 HARD PRECONDITION] FR↔apex docker-exec ROUTE CONFLICT (team-lead-RULED 10:56).** Registry `rc`→100.96.54.170:2222 (tailnet) vs live: tailscale LOGGED OUT → stale; container 10.200.13.114. Task #5 exec requires (+ PO-go + scheduled-window) a CONFIRMED FR→apex docker-exec route at window-time: apex logs tailscale IN, OR ssh `rc` HOST then local docker exec. Probe scoping: NOW=Hopper reconciles OWN records (no apex touch); LIVE probe=window-scheduling step; NO container touch till gate clears. Folded into Brunel #5 plan. Route Q now ASKED (in the queued apex Q above). CLOSED on my side till window scheduling.
- **[HANDS-OFF CHANNEL as of 2026-06-15 10:54] SINGLE-OWNER HANDOFF DONE.** Drained the last 2 apex consignments (T14+T15), re-verified waiting_for_me={} + deposited_uncollected={}, sent Brunel the literal "CHANNEL QUIESCED, YOURS — hands-off as of 2026-06-15 10:54." **I do NOT collect/deposit on FR↔hub again until Brunel's daemon is confirmed live OR team-lead says cutover aborted.** Brunel runs --once → checkpoints team-lead → loop go. **apex courier is DOWN (T15: apex S52 closed, PO call)** — their courier is child of their session; inbound auto pauses till their next startup (same key survives; FR deposits queue safely on hub at-least-once, no loss). So any FR→apex deposit now sits in deposited_uncollected till apex's next session = EXPECTED not failure. **T14 ARTIFACTS for onboarding minor-follow-up (apex co-author, attribute to customer-#2):** (1) adapted courier.json shape — ssh_opts carry host-key pinning (UserKnownHostsFile + StrictHostKeyChecking=yes + IdentitiesOnly), state_dir co-located w/ inboxes_dir on SAME persistent vol, ghost_outboxes=["framework-research-bridge"] (<dest>-bridge per CR-4); (2) T1.b 2.1.173 writeup — live member inbox→[] (inbound assumption VALID), ghost outbox 36-entry no-drain (outbound VALID), no-live-reader inbox no-drain (only LIVE agent inbox drains); verdict HOLDS, snapshot-not-timed caveat. Fold both when convenient; doc already FINAL (illustrative, no re-ratification).
- **[DONE S51 06-15] ONBOARDING DOC SET FLIPPED FINAL.** team-lead approved §4 errata wording as-written + agreed flip-FINAL-now. `stationmaster-onboarding.md` status = "ACCEPTED; FINAL, revision S51" (CR-1..7 folded; CR-4 fan-out A normative v1; CR-7 body-field pinned §4 errata). Protocol §4 errata + courier-hints Outbound §3 carry their own inline S51-ratified markers (no separate status line). apex worked-examples (courier.json + T1.b writeup) = minor illustrative follow-up when they land, NO re-ratification. NO git — team-lead folds all S51 doc+daemon+decommission changes into ONE session commit at the daemon-live checkpoint. Cal queue = 6, team-lead spawns Cal to file the batch at daemon-live (batched to keep spawn count down).
- **[DONE S51 06-15] OUTBOX CLEAN-CHECK for FR daemon (Task #4 gate) + HANDOVER pending.** Confirmed `~/.claude/teams/framework-research/inboxes/apex-bridge.json` held 1 STALE entry = my 06-12 cutover-ACK (the very msg that said "queued in apex-bridge to ride the flow once daemon resumes") → the new daemon's first --once WOULD redeposit this week-old msg to apex. CLEARED via atomic rename-aside → `apex-bridge.archived-stale-cutover-ack-20260615T104351Z.json` (evidence preserved, NOT deleted); apex-bridge.json now ABSENT = empty-outbox (courier hints S2). VERIFIED daemon resolves outbox name → exact `f"{name}.json"` (courier.py:388, no inbox glob; only spool is globbed line 441) so the archive name won't be picked up. Reported clean to team-lead. **CHANNEL HANDOVER:** once daemon live (after Brunel checkpoint + team-lead go), STOP manual FR-channel collect/deposit — daemon owns FR↔hub. Until then keep covering manually. Coordinate exact cutover moment with Brunel.
- **[DONE S51 06-15] PO RATIFICATIONS APPLIED + onboarding ready-for-FINAL.** team-lead signed off content + relayed 2 PO ratifications: (1) FAN-OUT CR-4 = candidate A ratified (my deferred amendment RESOLVED) — restated normative in onboarding §"Outbox routing" + courier-hints Outbound §3 (per-destination outboxes; <team>-bridge→<team>; fan-out out-of-scope refuse-and-retain, NOT "pending"). (2) CR-7 §4 TEXT-PIN ratified as clarifying ERRATA (no major bump) — added §4 errata bullet in stationmaster-protocol.md pinning `text` as renderable-body field; onboarding CR-7 note now "RESOLVED — pinned at §4 (errata)". apex co-authorship APPROVED → deposited request (id fa2c600c1bec222a) for their courier.json + T1.b 2.1.173 writeup as worked examples. Onboarding status line = "REVISION S51 ... awaiting final checkpoint of doc + §4 errata before FINAL" (team-lead must checkpoint doc+errata before I flip to FINAL). Edited files: stationmaster-onboarding.md, stationmaster-protocol.md (§4 errata), stationmaster-courier-hints.md (routing + text-field + data-line). NO git.
- **[CAL QUEUE — 6 items, file when Cal spawns] S51 06-15:** team-lead spawns Cal once onboarding lands; file via Protocol A. (1) CONTRACT — CR-7 entry body in `text` not `content` (now pinned §4 errata + normative both docs). (2) GOTCHA — deposit silent-failure (ok w/o data line = nothing landed, re-send). (3) DATAPOINT — T1.b drain-on-delivery CLI 2.1.173 MODEL HOLDS (TRUTHS.md). (4) GOTCHA/security — idx 18 CF-Access→APEX SSO header-trust gap (PR vjs_db_vjs_guard #9, apex repo). (5) DECISION — fan-out CR-4 PO-ratified candidate A (per-destination outboxes normative v1). (6) DECISION/contract — §4 text-pin errata (renderable-body=`text` pinned at contract, no major bump, §5.5-errata posture).
- **[DONE S51 06-15] TASK #6 (relay+gather) COMPLETE.** apex T12 (id c76a38f92857abf6) collected+acked: CR-7 CONFIRMED CLOSED (both text-field re-sends render full body; two-customer automated path FULLY PROVEN end-to-end both ways). HOLD-STEADY ack'd (no rotate/restart/relocate; (c) dropped; keeps session-side launches until our supervisor owns lifecycle). Deposited HOLD-PENDING-PO addendum (id 26846c383238c4af) per team-lead: restart-hardening ON HOLD until PO-approved window, channel stays as-is. **4 CONTAINER DETAILS FOR BRUNEL (Task #5 design input, handed to Brunel):** (a) entrypoints /entrypoint-apex.sh (15372B root:root 0775, *FR:Brunel* header) + /entrypoint.sh (2567B) — root-owned, apex can't edit; (b) Dockerfile = /home/ai-teams/workspace/Dockerfile.apex IN repo github.com/Eesti-Raudtee/apex-migration-research (Brunel reads directly); baked claude 2.1.162 but runtime CLI 2.1.173; (c) PERSISTENT = LVM ext4 st_dev 65024 at /home/ai-teams/{.claude,workspace,source-data}; EPHEMERAL = overlay st_dev 78 = / + /tmp + crucially /home/ai-teams/.ssh (so key persistence needs ~/.ssh added to LVM bind OR build-time provision — (b) plan avoids it); (d) access = hostname apex-research, IP 10.200.13.114 (+public IPv6), tailscale installed but LOGGED OUT (100.96.54.170 CGNAT stale), docker-exec per PO — Brunel/Hopper confirm against own access records.
- **[DONE S51 06-15] V2 DECOMMISSIONED + ONBOARDING CR DRAFTED.** Task #3 COMPLETE: removed stale pidfile, added DECOMMISSIONED-superseded banner to poc/ghost-bridge/README.md, config/scripts retained as archive, no git. **Onboarding CR draft DONE** (stationmaster-onboarding.md, status now "DRAFT REVISION S51"): folded CR-1 (self-contained/doc-reachability), CR-2 (grants_out-lags → 3-state ladder authoritative), CR-3 (key-persistence: durable vs ephemeral, rotate-on-restart supported v1 fallback), CR-5 (verify-drain-on-delivery substrate-check step + apex 2.1.173 worked example), CR-6 (host-key-fp vs team-identity-fp table), CR-7 (entry body MUST be `text` not `content` + deposit-data-line rule). CR-4: stated normative <team>-bridge→<team> only; fan-out marked PENDING PO RATIFICATION. Un-stubbed courier-hints reference (file exists). 4 gates applied (grep clean, producer/consumer §4 cross-read, artifact-existence confirmed, substrate-invariant hoisted). CHECKPOINTED to team-lead (onboarding.md is ACCEPTED → needs team-lead sign-off before final). **Fan-out PO recommendation = candidate (A) per-destination outboxes** (zero protocol change, apex's CR-4 already uses <team>-bridge→<team> = external n=1 for it; B/C noted). awaiting team-lead.
- **[GOTCHA — CR-7, consignment body field] S51 06-15:** the consignment `entry` body MUST be in **`text`** (the SendMessage-native field the Claude Code harness renders), NOT `content`. My hand-crafted FR→apex probe used `content`/no-`text` → it AUTO-SURFACED on apex (inbound auto proven) but **body rendered `undefined`** (only `summary` showed). apex diagnosed (T11): SendMessage-origin entries = [from, read, summary, text, timestamp, type]; courier forwards verbatim (S4) so the mismatch surfaces at render, not transit — NOT a courier bug. FR ruling = path (a) sender-convention "always text" (keep verbatim-forward intact; do NOT add receiver-side content→text fallback). RE-SENT T8 ruling (id 662ea95907a29002) + T7 probe v2 (id f8b6ed9bba2ee87c) both with `text` field. ALSO: my earlier `content`-field T8 deposit at 10:35 had a path-mismatch and actually FAILED (empty request → ok on cmd-line only, no per-consignment data line = the tell). LESSON: a successful deposit returns a `{"id","status":"accepted"}` DATA LINE; ok-without-data-line = nothing landed. CR-7 folds into onboarding.md with T10's CR-1..6.
- **[T8 → TASK #5/#6] S51 06-15:** PO RULED on apex maintenance: FR OWNS (Brunel designs, Hopper docker-exec). Key = approach **(b) build-time provision ONLY** (auto-register hub-side same step, survives full rebuild); interim (c) DECLINED. ASK 1 supervisor (dashboard+courier restart on exit AND boot) FR-owned. apex told HOLD STEADY (no self key-rotate/restart; scheduled hardening restart will drop channel; FR pings to re-verify after). Relayed over hub (text field). GATHERING from apex (for Brunel): (a) /entrypoint-apex.sh path, (b) Dockerfile+repo+build, (c) persistent vs ephemeral volume layout, (d) how FR docker-exec's in (host/container-name/access method). Task #6 = relay+gather (in_progress, mine); Task #5 = the actual hardening (FR-exec, no execution until Brunel checkpoints to team-lead).
- **S51 (2026-06-15) TASK #2 DRAIN DONE:** Hub UP (10.100.136.162:2222, uptime ~234k s). Collected+acked **3** apex consignments (waiting_for_me was 3 not 2 — one more arrived). Wire reminder: JSON on stdin via `ssh -T`, NOT exec-word (`status` as command word → E_MALFORMED). Custody: durable inject (anti-spoof `apex-research-ghost`) + delivered-ledger keyed by hub id → THEN ack → hub deleted all 3. Channel clean both ways. **(T6 / id 4effe1944b5a6383) = THE T1.b drain-on-delivery datapoint for TRUTHS.md, CLI 2.1.173, MODEL HOLDS** (live member inbox returns to [] / verify-empty→exclusive-create VALID; ghost outbox 36-entry no-drain / consume-by-rename VALID; ghost inbox no-drain confirmed; caveat = observational snapshot not timed test) → RELAY TO CAL when Cal up. **(T7 / 79e55b3635e1e419)** apex courier DAEMON LIVE (ghost_outboxes=[framework-research-bridge] fresh, inject→team-lead, poll 30s, persistent-vol state, host-key strict) — requests FR→apex test deposit to prove auto round-trip; caveat = daemon shares team-lead session lifecycle. **(T8 / 5916879d123bc283)** apex MAINTENANCE REQUEST (PO-directed, OPERATOR/host domain, NOT mine): OS-level supervise dashboard (vite :5173 --host 0.0.0.0) + courier, persist ~/.ssh key (ephemeral overlay = the real blocker; opts a mount / b build-time provision+register / c interim relocate to .claude). apex live state: courier pid 2402351, identity SHA256:9cgJVaSOBAC95/hae73zNXfAOwCQTfrlFEF+aOHswro. All 3 relayed to team-lead.
- **S51 (2026-06-15) T7 TEST DEPOSIT + STEP 2 SCOPE DONE:** Deposited FR→apex probe **a7e2c86c91aac104** (accepted) → apex daemon AUTO-COLLECTED it (deposited_uncollected went to {}) = apex INBOUND auto proven. Collected 2 MORE apex deposits same cycle: **T9 d5431b335c170c12** (apex OUTBOUND courier dogfood — daemon auto-deposited from their framework-research-bridge ghost outbox = apex OUTBOUND auto proven) + **T10 6172a809fee39e79** (6-item CHANGE REQUEST vs stationmaster-onboarding.md — MY DOMAIN, evidence-backed from real customer-2 onboarding: CR1 doc-reachability/self-contained-invite, CR2 grants_out-lags-until-route-exercised use-3-state-ladder-as-authoritative, CR3 key-persistence/rotate-on-restart section, CR4 outbox→dest routing normative <team>-bridge, CR5 verify-drain-on-delivery onboarding step, CR6 host-key-fp vs team-identity-fp pin). Injected+ledgered+acked both → clean both ways. NET on FR→apex test: apex two-sided auto PROVEN; FR side still manual (our daemon = Brunel Task #4). **STEP 2 SCOPE ASSESSMENT for decommission last-look:** (1) fr-apex = ALREADY on stationmaster (grants in+out apex-research present) → "migrate fr-apex" is a NO-OP, confirmed. (2) hr-devs NOT a registered hub customer → fr-hr-devs CANNOT migrate; DEFER until hr-devs onboards. (3) PID 36772 = DEAD (tasklist: no task; stale pidfile says 36772). v2 carries NO live traffic: hr-devs-bridge.json (the fr-hr-devs OUTBOX the daemon watches) NEVER EXISTED = zero FR→hr-devs outbound ever; hr-devs-lead-ghost.json = 1 stale test-ping 2026-06-08 read=true; apex-bridge.json = 1 stale entry (my 06-12 cutover-announce ACK) superseded by live hub. → **v2 serves nothing live; SAFE to decommission**, fr-hr-devs deferred. Reported, awaiting team-lead last-look before PID dies.
- **S51 (2026-06-15) TASK #3 STEP 1 DONE:** triaged the 35-entry (NOT 34) apex-fr-backlog-copy.json (all read=True, 05-14→06-12). NET: 34/35 superseded/closed/already-actioned by the stationmaster cutover; reported block-by-block to team-lead. ONE open item flagged for team-lead ruling: **idx 18 = CF-Access→APEX SSO bridge (`rumba_sso_login`, f600) security gap** — header-trust without JWT sig-verify (`Cf-Access-Jwt-Assertion` TODO), PR vjs_db_vjs_guard #9; candidate Protocol-A to Cal if not already filed. Stale-but-noteworthy: idx 17 unanswered "written deliverable on the 5 context-mgmt improvements" ask. Dupe-root-cause exchange is idx 22-32 (brief said 26-32) — daemon re-forward-on-restart, old-name=4x vs fresh-name=1x controlled comparison = the canonical "why we built the hub" record. HOLDING at STEP 1 gate; STEP 2 (migrate fr-apex/fr-hr-devs pairs v2→stationmaster) + STEP 3 (decommission dead PID 36772) await team-lead read + Hopper hub-up confirm.
- **Current state:** S50 wrapped + **S51 respawn (2026-06-15) DONE: FR⇄apex route LIVE both ways, full symmetric round-trip PROVEN on the live hub.** apex-state = "granted both ways, first deposit sent + collected by apex, apex collect+ack transcript received+acked." Issued `grant apex-research` from FR (grants_in updated 06-15T06:48:26Z). Deposited inaugural FR→apex consignment **id 67a3c27d3c0c5ec6** (accepted/durable; apex collected+acked it, custody order honored, hub deleted). Collected apex's 2 deposits (**4627f4fa250553f7** grant-probe AR-S52-T4 + **6cdfa0c40c163810** validation AR-S52-T5), injected to FR team-lead as `apex-research-ghost` (anti-spoof), acked both → status clean both ways (waiting_for_me{} + deposited_uncollected{}). Reference courier `stationmaster-courier.py` (46173 B, full file) delivered to apex team-lead inbox via direct fcntl-locked SSH append (ai-teams@100.96.54.170:2222 key id_ed25519_apex) — NOT the dead daemon; "inbox now has 1 entry". apex ack'd ask B (will run drain-on-delivery check on CLI 2.1.173, report for TRUTHS.md). Shutdown-approved after report.
- **Prior (S50):** Tasks #4 + #8 + apex-doc-delivery DONE. #4: full FR⇄fr-test live round-trip 15/15 + courier inject+ledger. **fr-test key=`~/.ssh/sm_fr_test`.** FR key=Hopper's `~/.ssh/sm_framework-research`.
- **[LEARNED] Canonical conflict rule (Aen 18:05):** when team-lead's instruction conflicts with operator's latest hub-state confirm, the OPERATOR'S LATEST CONFIRM WINS — operator reports what authorized_keys actually says; team-lead reports last-known. Corollary I applied: verify an asserted key/identity binding against the LIVE system before repointing config; surface the conflict with probe evidence, don't act on either claim blindly. The near-identical key-name footgun (sm_framework_research vs sm_framework-research vs sm_fr_test) fired for real this session.
- **Apex doc package (delivered 18:00, 5700 chars):** hub coords + HOST-KEY fingerprint SHA256:CNcFjOxr8vREOueOS8nxJN8W3LaQHet62du+PHyK13U (distinct from team-identity fp — apex verifies the server with THIS); wire protocol (envelope + deposit/collect/ack + consignment shape + anti-spoof from_team duty); 3-state ladder; grant choreography (apex grants FR their side, FR grants apex once apex first-connects); reference-courier offer; T1.b version-skew note; rotate-on-restart ACCEPTED.
- **Task #4 EVIDENCE (live, real ssh, 06-12 14:36-43Z):** 15/15 hub matrix — ping both identities; 3-state ladder (E_UNKNOWN_TEAM via no-such-team + E_NOGRANT FR→fr-test pre-grant + accepted post-grant); reciprocal grants; bidirectional deposit/collect/ack (ids c648..., a52c...); duplicate=success; ack→empty-collect; idempotent re-ack; E_VERSION. PLUS courier `--once` inbound cycle: collected id e610486cd70c12a0, injected into fr-test local team-lead inbox as `from=framework-research-ghost` (anti-spoof from authenticated from_team, body verbatim), ledger recorded, acked. Custody-transfer + inject-before-ledger exercised on real substrate. §8 Path.home() field exercise PASSED on Win/Git-Bash.
- **Configs:** `~/.stationmaster-t4/` — courier-fr.json + courier-frtest.json, isolated inbox/state trees (same-volume gate OK). roundtrip-ids.json + apex-fr-backlog-copy.json saved there.
- **APEX ACCEPTED (found in read-only backlog copy idx 34, ts 14:34, Schliemann PO-pre-auth):** apex-research IN as 2nd customer. Payload for operator: team `apex-research`, pubkey `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAfUQ3nKW1OvAycfiq2pqTz/64G1qY0HB9lnAClJm/y3 apex-research@stationmaster`. **CLI=2.1.173** (T1.b — third version, between 2.1.170 TRUTHS + 2.1.175 flagged). apex inbound also dup-free this cycle. CAVEAT: apex container ~/.ssh EPHEMERAL → rotate-on-restart policy. DOC GAP: apex lacks onboarding.md. Echo SEQ=AR-S52-T2, ready for round-trip on registration. Recommended to Aen: register apex as REAL 2nd customer (> fr-test stand-in).
- **ANOMALY SOLVED:** SEQ=AR-S52-T1 still in apex outbox (idx 33) but `read=True` — bridge flips read-flag on forward, doesn't delete. Daemon forwarded normally; no phantom path. Backlog (35 entries) copied read-only → `~/.stationmaster-t4/apex-fr-backlog-copy.json` for Aen triage (incl. 06-10 dupe-root-cause exchange idx 26-32).
- **Active items:** NONE — apex-watch (S51 respawn) CLOSED. The genuine FR⇄apex two-customer round-trip is DONE this respawn (see Current state). All evidence banked on the live hub. Shutdown-approved. apex/fr-test/framework-research all registered; hub healthy (uptime ~232k s at close). NOTE for whoever next touches apex traffic: the ghost-bridge daemon is DEAD (stale PID since 06-10) — Schliemann flagged it as the SPOF the hub replaces; hub path is the live route now, bridge-via-daemon is not. Direct fcntl-append still works manually for file delivery.
- **Task #8 findings (S50):** Ghost-bridge v2 daemon DEAD (PID 36772 stale). Transport ALIVE — live SSH probe to apex (ai-teams@100.96.54.170:2222, key id_ed25519_apex) returned OK 06-12 17:28; the 06-11 timeout→permission-denied wall has cleared. Apex's FR-bound outbox `fr-lead-ghost.json` had 34 waiting entries incl. apex's own 'X-team comms test ping' 06-12 14:27 — apex actively trying to reach us, dead daemon = no collection (cutover-urgency datapoint). Live-config outbox name `apex-bridge` ≠ example/README `apex-lead-ghost` (the existing 14-entry drained file) — naming drift finding. Delivered stationmaster 2nd-customer proposal via direct fcntl-locked SSH append to apex team-lead inbox (the daemon's documented mechanism, invoked manually since daemon dead); apex team-lead drained it on next poll = delivered to live recipient. Ghost members not in apex roster/config (S48 removal confirmed) — file-inject works anyway (roster gates SendMessage, not file pickup).
- **Done S50:** `stationmaster-courier.py` (8/8 unit) + `integration-test.py` (14/14 pre-deploy, transport stubbed). Both Protocol-A subs FILED + stage-2 CONFIRMED via my read-back (Cal 17:08); S50 stationmaster knowledge intake CLOSED on Cal's side.
- **Carry-forward:**
  - **NEXT WAKE = S51 apex-watch** (see Active items). Task #4 DONE this session — round-trip evidence banked, configs at `~/.stationmaster-t4/`. Hub coords for the apex grant: `sm@10.100.136.162 -p 2222`, FR key `~/.ssh/sm_framework-research`, host-key fp SHA256:CNcFjOxr8vREOueOS8nxJN8W3LaQHet62du+PHyK13U.
  - **[DEFERRED — PO ratification]** Ghost-outbox fan-out routing amendment candidate — full write-up below (do NOT prune). Aen surfaces at PO return; also tracked in Cal's scratchpad. Do NOT edit ratified docs.
  - WARNING: CLI 2.1.170→2.1.175 substrate shift — courier-hints S2 facts (S1/S3 ghost-outbox accumulation + live-inbox drain) need re-validation before production reliance.
  - NOTE: Task #1 description text says `grant_receive/revoke_receive`; Brunel's sm-shell correctly uses `grant`/`revoke` (§5.5). Cosmetic dead-task-text drift only.

---
## 2026-06-12 (session #50 — stationmaster courier + integration; Tasks #2, #6)

[CHECKPOINT] **Task #2 — `stationmaster-courier.py` shipped** (single file, stdlib only, 8/8 tests). Outbound: poll-by-content → parse-before-consume → atomic-rename-to-spool → deposit → delete-on-accepted/duplicate (hints S3, FIFO). Inbound: collect → ledger-check → inject-via-verify-empty/rename-aside/exclusive-create (hints S4) → ledger-append → ack-all (hints S6). Custody transfer: ack only after durable inject (protocol S5.4). Inject-before-ledger ordering (hints S6 row 4 — at-least-once, dup-not-loss). Anti-spoof attribution from envelope `from_team` → `<from_team>-ghost`, body verbatim (protocol S4). Single-instance lock (exclusive-create + PID staleness, hints S7). Same-volume startup gate (hints S3).

[CHECKPOINT] **Task #6 — `integration-test.py`, 14/14 pass.** Real courier loop ⇄ real Brunel `sm-shell` (`sm-shell <team>` on stdin/stdout, shared SM_STATE_DIR), only ssh/Docker hop stubbed. Covers all #6 items + courier-side disciplines Brunel's smoke-test.sh can't reach (consume-by-rename, inject-by-exclusive-create, anti-spoof, ledger dedup + crash-survival). Aen ACCEPTED as the courier-logic layer; deployed-hub-over-real-ssh verify lives in Task #4 (the honest record-fix, option B, #6 retitled "Pre-deploy ... transport stubbed").

[LEARNED] **Three orthogonal test layers, no overlap/gap:** (1) courier-logic = my integration-test.py (stubs ssh); (2) ssh-transport = Brunel's smoke-test.sh over real ssh; (3) inbox-injection race = T6.a harness (Task #3, operator). A `completed` flag must never claim more than its evidence (Aen: same discipline as the wiki stage-2 gate).

[DECISION] **Per-consignment inject (1 entry per inject_batch), NOT batch-of-collect** — keeps delivered-ledger 1:1 with hub envelope `id` (the dedup key); crash mid-loop re-delivers exactly the un-ledgered ids.

[DECISION] **Partial-deposit handling:** on a multi-entry spool file where some consignments are `rejected` (per-consignment, protocol S5.2), rewrite spool to retain ONLY non-accepted entries (courier-private → in-place atomic-replace OK, hints S3.5); retry. Never drop on own initiative (hints S7: no TTL).

[LEARNED] **Two Protocol-A patterns filed + stage-2 confirmed (Cal 17:08):** `patterns/same-volume-startup-gate-for-rename-atomicity.md` (operation-side; sibling to Hopper's verification-side `per-filesystem-gate-targets-tmp-measures-wrong-fs`) and `gotchas/courier-originates-routing-protocol-leaves-undefined.md` (Cal's sharpening: `from_team` is hub-sourced from the authenticated channel, but `to` has NO hub-side source → why the routing gap is asymmetric).

### [DEFERRED — PO ratification] Amendment candidate: ghost-outbox → destination-team routing (fan-out case)

**Disposition (Aen 16:49):** v1 courier handling stands as a DOCUMENTED CONVENTION (courier scope, no protocol change). Fan-out/multi-destination is a PROTOCOL AMENDMENT CANDIDATE requiring PO ratification — PO away, queues. Write-up is intentionally NON-prescriptive (options, none recommended-as-decided).

**Problem.** Consignment is `{"to": <team>, "entry": {...verbatim harness inbox entry...}}` (protocol §4); hub routes by `to`. But a harness inbox entry carries NO destination field, and a ghost outbox (`inboxes/<name>.json`, session-less slot agents SendMessage to) is generic. So the courier must supply `to` from somewhere OTHER than the entry — the ratified docs never say where. The one place a courier MUST originate routing the protocol doesn't define. (§4 verbatim-forward actively prevents routing-in-payload while the hub requires `to`.)

**Two cases v1 covers (the documented convention):** (1) single configured ghost outbox → its one destination; (2) `<team>-bridge` name → strip `-bridge` → `<team>` (hub's §1 example `hr-devs-bridge`).

**Undefined case:** one outbox fanning out to MULTIPLE teams — nothing disambiguates per-entry destination. v1 courier returns None, refuses-to-guess (retained in spool, logged, never dropped — hints §7 no-TTL).

**Possible resolutions (NOT ranked, for PO):**
- (A) **Per-destination outboxes only** (codify convention 2). Fan-out = N outboxes. Zero protocol change; cost to sender. Simplest.
- (B) **Routing sidecar / local entry-envelope** — sender wraps `{"to","entry"}` at the local ghost-outbox level, mirroring the consignment shape; entry stays verbatim on the wire. Adds a local-format contract; arguably courier-hints scope, not wire-protocol.
- (C) **Leave undefined** — single-destination + naming convention is the supported v1 surface; fan-out out of scope until a real consumer needs it (YAGNI). Refuse-and-retain is correct for an unsupported config.

**Boundary note for PO:** (A) and (C) need NO wire-protocol change (courier-hints convention only); only (B) touches a contract surface, and even then it's the LOCAL outbox format, not the hub wire envelope. The "amendment" may resolve as a courier-hints clarification rather than a protocol §4 change — that classification is itself a PO call.

---
## Pre-S50 (condensed — durable knowledge promoted to wiki / docs / topic files)

- **S40 (issue #71):** extracted 4 procedural recipes from common-prompt into `playbooks/` (verify-structural-change, version-typed-contract, relay-fidelity, shutdown-agent). Extract=procedure, keep=behavioral-contract.
- **S39 (Team OS protocol-lens):** 14 transferable mechanisms delivered to Aen. Their adoption-first design vs FR's rigor-first; "skill-as-protocol-contract" + three-tier-context-loading-as-flow-control as named candidates.
- **S35 (Cloudflare pilot comms):** DO-as-per-recipient-mailbox primitive; `idFromName` identity + KV roster; retain FR SendMessage envelope (envelope IS framework-state, not substrate); Sub-shape E n=3 at comms layer. Design at `designs/new/cloudflare-pilot/comms.md`.
- **S26-27 (Prism federation Phase A+B):** 11 PRs merged (Phase A) + PR #12 (federation-authority-record v0.1.1) + PR #13 (02 §4 R9-rule, SemVer-MAJOR v2.0.0). Symmetric-envelope-mode-by-content-category; strict-SemVer-for-typed-contracts (migration makes the bump SAFE, not minor); discriminator `kind` locked; 8 discipline surfaces dogfooded. Worktree-isolation n=7 cumulative. Substrate inbox-failure asymmetric to worktree presence (Cal filed `worktree-spawn-asymmetry-message-delivery.md`).
- **S59-60 (xireactor + agent-spawn):** xireactor pilot protocol v1.2 (`docs/xireactor-pilot-protocol-2026-04-15.md`); Protocol D canonical-taxonomy slot. `agent-spawn-protocol.md` v2.0.0 — Agent-tool persistent spawn as default, `team_name+name` discriminator; session-lifetime caveat load-bearing. **T03/T06 boundary:** protocol doc defines the shapes each path uses; lifecycle doc defines which path to choose when (Herald=shapes, Volta=state machine).
- **Pre-S59:** T03 Protocols 1-5 in `topics/03-communication.md`; T09 dev-methodology (XP message types, three-strike escalation, three Librarian protocols A/B/C); single-provider as protocol-level requirement; Entu API BFF pattern for SvelteKit.
