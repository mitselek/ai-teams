# Brunel scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S67 2026-08-31, **JOOSEP ARC DONE END-TO-END.** Container LIVE on RC:2231 with the Estonian FIRST-TASKS + `paunvere` team package + `TEAM_NAME=paunvere`; Joosep authenticated 9a-9d on his own key; host dev-key revoked; registry `live`; hand-over note delivered by the PO. **Package now at `designs/deployed/joosep/`** (moved 12:45, `git mv`, 21 files, clean renames, exec bit preserved; stub left at the old path). Design of record `docs/joosep-container-design-2026-08-28.md` v3.6. Only tail item: `joosep_sshd` has **no backup** -- PO keyboard item in Aen's checkpoint list.
- **ARC CLOSED + VERIFIED ON MAIN 12:40.** `da464c0` (5 files) + `612fde3` (note signature) + `a557b1d`/PR#112 (note + exec bits). Verified at source, not accepted: main's `Connect-Joosep.ps1` md5 = **`0bb687d1…`** = my corrected copy, `id_ed25519_joosep` down to **1** occurrence (the correction comment), registry `"status": "live"`, working tree clean.
- **[LEARNED S67, the near-miss I caught 30s AFTER "arc closed" was declared] PR #112 shipped only 3 files** -- the note + two MODE-ONLY changes -- leaving the BROKEN script and `status: designed` on main while everyone believed it was done. **`M ` (staged) vs ` M` (worktree) is ONE CHARACTER and it silently decided what shipped:** the PR was cut for the note, the exec-bit changes rode along BECAUSE already staged, the content fixes never were. **"Merged" and "shipped" are DIFFERENT CLAIMS and nothing in a merge message distinguishes them -- `git show --stat <merge>` is the only check.** Generalises: **a completion announcement is not evidence of completion, including when I am the one being told.**
- **CHAIN COMPLETE 11:15.** All 4 entrypoint EXPECTs verbatim, neither STOP fired, host key `SHA256:C8qVyjSQuy…` **unchanged**, 10/10 paunvere files exact in `~/work/paunvere/`, `TEAM_NAME=paunvere` live, image `sha256:95191f2ee20f`, **both seed stamps written -- the hand-displacement was genuinely the last one.** Phase 3 found `.env` = **`TEAM_NAME=joosep`** (older than `vedur`; silently overriding the compose default since 08-28) -> read-replace-report, not literal-match. Phase 5 was briefly blocked by the **permission classifier** (not an authorisation gap -- Hopper correctly did NOT route around it); PO switched to manual mode and retried. **9b/9c green-lit, Joosep invited back.** **9d COMPLETE; Steps 12+13 DONE 12:26.** `registry.json` flipped `designed -> live` (+`updatedAt`/`updatedBy`/`sshKey`; validated, parses, 12 rows) -- the row was ALREADY there from 08-28, so the task was a flip, not an add. Its `_note` had said "flip on first successful up"; the up was 08-28 and it never fired -- **a to-do in a data file's comment field has no owner and no trigger.** Step 13 note drafted at `designs/deployed/joosep/HANDOVER-JOOSEP.md` (Estonian; **the "drafted Estonian note" Aen's record promised DID NOT EXIST** -- what I had written was English guidance to the DELIVERER; 3rd accepted-vs-implemented divergence here). **Fingerprint deliberately NOT in it -- if the value and the thing it verifies share a channel, the comparison checks nothing.** PO reviews the Estonian (Celes not spawned). Still owed: `joosep_sshd` backup one-liner (Aen's checkpoint list).
- **[GOTCHA S67, the near-miss that mattered most] `Connect-Joosep.ps1` would have HARD-FAILED Joosep's first connection and given advice that made it worse.** It `Test-Path`-ed a GUESSED key filename (`id_ed25519_joosep`) and **returned early** when absent -- against a healthy container -- then told him to **generate a new keypair the container does not know**. `IdentitiesOnly=yes` would have blocked ssh's fallback anyway. He only got in at 9d.5 by running raw `ssh` (default-identity lookup). **Fix was NOT to swap in the other filename** (same mistake, new guess): `-KeyPath` now unset by default, `-i`+`IdentitiesOnly` passed ONLY when explicitly named, pre-flight fails only on an explicit path that is missing. **A pre-flight may only fail on something it actually knows.** The assumption was in **6 artifacts** -- script, 5 runbook commands, fingerprint line, README keygen, **and both `registry-rows.md` rows (worst: `rc-connect.ps1` builds `-i` from that field)** -- all copies of ONE guess made once in the design doc and spread by transcription. **Where a key file lives is the user's business, not ours to assert about a machine we cannot see.** **[FILED to Cal 12:28, the durable form] Six artifacts asserted one unobservable fact; ALL SIX AGREED and that agreement is why nobody checked. Every remedy we hold for duplicated facts detects DRIFT -- there was none. Six synchronised copies of ONE guess is one piece of evidence echoed, not six.** Remedy is not "check the assumption" but **do not record it: where a value describes something outside your observation boundary, record the MECHANISM that resolves it** (`ssh` default lookup; `git update-index --chmod=+x`; `grep '^COPY' Dockerfile`). **Where you cannot observe, do not assert -- delegate.** **Cal's correction, take the BROADER form: all THREE of today's findings share ONE remedy -- "stop maintaining a recorded copy; point at the thing that already computes it" -- and it applies wherever a mechanism already resolves the value, OBSERVABLE OR NOT** (exec bit and build-input list are both fully observable and the same move works). My "cannot observe" framing was the narrow case. **Candidate umbrella, NOT filed** -- his revisit trigger (two forms converging on one remedy) arguably fired, but all 3 are mine/Hopper's in one session/work-stream, so he discounted it for correlation as he has all day; **a 4th instance from a different work-stream would make it hard to refuse.** Dedup upheld over Aen's "composes with wrong-denominator": disjoint on non-generating cases, shared FRAME only. I flagged the merge argument I was declining, on Cal's own grounds, so he could overrule me knowingly.
- **Runbook FULLY corrected, Aen-ratified, 14.1/14.3 RAN-stamped** (Steps 1 digest-ruling · 13 Joosep note · 14.1 artifact-pair gate · 14.2 chmod+read-replace+no-`--no-cache` · 14.3 three seeding lines + host key). **14.3 had verified LESS than the rebuild shipped** -- written before the team package existed, would have passed clean on a container missing all of `paunvere`. **A verification step cannot notice that its subject GREW** (amendment leaves a trace; growth leaves a clean pass) -> re-derive EXPECTs from the source artifact, never edit the previous list. Corollary: **a verification that exists only in a dispatch is not part of the runbook.**
- **[GOTCHA S67] The exec bit IS tracked by git (`100755` vs `100644`) -- I first wrote "not in git", which is FALSE and would have condemned us to a `chmod` in every procedure forever.** These files are just committed `100644`. Real fix, one command, **Aen's to run**: `git update-index --chmod=+x designs/deployed/joosep/joosep.sh`. **Measured (rule: every git-tracked `*.sh` under `designs/`, mode from the INDEX): 24x `100644`, 6x `100755` -- and the 6 are TWO DIRECTORY CLUSTERS** (`esl-legal/` 2, `po-team/operator/` 4). **So it is NOT scattered inconsistency: the correct practice exists here twice and did not propagate** (Cal's sharper reading; my "mixed convention" was the weaker one). That makes it worse -- **two deliberate clusters make the other 24 look like a considered choice, so the working examples make the broken ones look deliberate.** 2nd instance armed NOW: `smoke-test.sh` (644) is called `./smoke-test.sh` by a1.2 spec §7, saved only by §4's blanket chmod -- accident, not guard; drop §4's chmod as "redundant" and §7 breaks.

## FROZEN TREE -- **these are BLOB md5s (`git show :<path>`), NOT worktree md5s.** Package moved to `designs/deployed/joosep/` 12:45.
**[CORRECTION 12:50 to my own 11:27 claim, which I gave Aen as settled]** I said a `--chmod=+x` "changes what git records about a file without changing the file, so an attested package can be chmod-fixed mid-flight without invalidating its gate." **True of the BLOB, FALSE of the WORKING TREE.** Measured after the move: 5 of 6 files blob==worktree, but **`joosep.sh` worktree = `2f9d0005…` vs blob `83eda299…`, and `file(1)` says CRLF** -- it is the ONLY file in this package that got the chmod, so the mode change re-materialised it through autocrlf. **A worktree-md5 gate WOULD have false-STOPped.** Not a shipped defect (blob is LF; RC stages from a Linux checkout; container verified live), but **verify against the blob, never the worktree, on this box.** This is my own submission-1 discriminator (worktree==blob) catching my own overclaim -- and note `grep -c $'\r'` returned 0 while `file(1)` said CRLF: **`file(1)` is the authority, the CR-count metric is a Git Bash artifact.**

```
186781db879f521c178299bfcb4b5974  Dockerfile
cb8405a00b5ebae8931f2202fadea02b  entrypoint.sh
5375d9b8314adcc9642e290be0f636d9  docker-compose.yml
2c80186c3e6322b4338cd01f61d50a2a  .env.example
530329c2a04adf1dd0c4411e2439b06d  FIRST-TASKS.md
83eda2994bfcaf3a15ab95914c93a05e  joosep.sh
7b4474e7dfcd55681a216ab64f5bbd33  warp-ca.pem      <- HOST-sourced, NOT in the repo
e78b95ab74a7a233854ba097a909c9b2  teams/paunvere/  <- WINDOWS-ONLY digest, DO NOT ship (see below)
```
**[GOTCHA S67, STRONG -- cost a live STOP] The 2-level dir digest is NOT PORTABLE. The md5 of a FILE is portable; the md5 of `md5sum`'s OUTPUT is not.** Two invisible platform deps: (1) Windows/MSYS prints `<hash> *<path>` (binary-mode asterisk), Linux prints `<hash>  <path>`; (2) `sort` collation is locale-dependent (`README.md` is 8th under UTF-8, 1st under `LC_ALL=C`). Same tree, 3 digests: `e78b95ab` / `f7bd4961` (sep fixed) / `c6612097` (sep + `LC_ALL=C`). **USE THE PER-FILE MANIFEST AS THE GATE** -- it catches everything the digest does AND names the offender. **THE HAZARD IS NEVER THE FUNCTION, IT IS THE EXPOSURE** -- ask "do the two operands cross a boundary", never "is it a digest". The entrypoint's own `dir_digest` (Step 9c) is SAFE and must NOT be "harmonised": both operands, same code, same box. **3rd instance same day: `_has_live_session` (Volta) -- in `resolve_team_dir` a false-negative drops a candidate and FAILS LOUD ("never guesses"); in the proposed OQ10 stale-dir sweep the identical false-negative DELETES a live session's dir, silently. Same predicate, inverted blast radius.** **Build-input list is DERIVED, never enumerated: `grep -E '^(COPY|ADD) ' Dockerfile`.** Host key that must NEVER change: **`SHA256:C8qVyjSQuyiSXPzEBcIOh2tfUwlk9EJtU2WxhAEbO3U`**. [GOTCHA] Wrong cwd makes `find|xargs md5sum` hash an EMPTY stream -> `d41d8cd98f00b204e9800998ecf8427e`, valid-looking hex on total absence (Hopper).

- **Pending rebuild delivers (one operation, Step 14):** Estonian FIRST-TASKS + team-package seeding + `TEAM_NAME=paunvere` in `.env`. Pre-step: **`mv` (never `rm`) `~/FIRST-TASKS.md`**, gated on mtime == first boot. TEAM_NAME alone needs no rebuild (compose default, `.env` override, recreate only).
- **Closed by PO:** PO-1 allerk-base · PO-2 host-net (measured: bridged container cannot resolve DNS at all) · PO-3/4 container starts under-credentialled, PAT + EVR connector are the team's own first tasks · PO-10 `deploy.resources.limits` IS enforced · PO-12 revoke Joosep's `dev` key (Step 9d, **Tier D**, held until 9a-9c) · PO-15 permanent · PO-18 no provisioning-side connector step.
- **Open / routed elsewhere:** **PO-19 `allerk` has NO pid ceiling** (CPU+mem enforced, `PidsLimit` unset) -- one line in Lerko's compose, routed to Aen. PO-16 roster size. PO-17 (deferred) bridge via explicit `dns:`. PO-9 registry drift. Cal's entry restructure (permanence/causation sub-shapes) -- next session, needs a NON-author read-back.
- **[LEARNED] -- the lesson in one line, now 8 instances:** I keep erring one line from a rule I had just written, filed, or was actively verifying. **The checks that caught things were the ones that did not depend on anyone being alert** -- a parse gate, a build assertion, an md5 pin, reading the artifact instead of the ack. **Awareness of a pattern is not protection against it.**
- **[LEARNED S67, the sharpest instance yet] I committed the miscount genus TWICE while READ-BACK-VERIFYING the entry that names it.** (4th) reported corpus `220` -- I stated my membership rule but it did not match the NOUN I was comparing against (216 = entries; my 220 = files, incl `index.md` + `archive/README.md`). Real figure **218**. (5th) verifying that, my per-dir sum came to 217 because the filter `grep -Eiv 'INDEX|README'` dropped **`contracts/entu-competency-index-schema.md`**, a real entry whose FILENAME contains "index" -- **a name is not a membership test** (`scope-bound-identifier` reaching the counting layer). **I had flagged that exact false positive seconds earlier in my own output and did not connect it to the identical filter one line away.** Silver lining, and it is real: because I STATED the rule inline, Cal reconciled the error exactly in one step. Clause 1 (state it) paid off even where I failed clause 2 (check it against the noun).
- **[LEARNED] Hopper's, the best structural fix here:** derive the staging list from the Dockerfile (`grep '^COPY'`), never maintain one beside it -- **two lists that must agree will eventually disagree.** That makes the failure unrepresentable rather than merely caught.
- **[LEARNED] "Proven working" can be an artifact of the mask.** I exempted the Node layer from change BECAUSE it was proven-working; it was the one broken line. A mask does not just hide a failure -- it manufactures the evidence you use to decide not to look.
- **[WARNING] Three divergences between ACCEPTED and IMPLEMENTED in this package, all caught by others, none by me.** Acceptance produces a message; implementation produces a file; nothing compares the two. Before any future "as agreed", grep the artifact.
- **[CORRECTION S67] The FIRST-TASKS gate: my attested `530329…` is the WRONG SIDE.** That is the **Estonian** file we are shipping; the container holds the **English** seed from 08-28. Comparing them mismatches BY DESIGN and fires a false STOP. Correct gate needs no remembered value -- `~/FIRST-TASKS.md` vs `/opt/FIRST-TASKS.md` **inside the container**, equality proves untouched. **Must run BEFORE the build**, which destroys the comparison. Same genus as the derived-build-input-list fix: derive the comparison from the artifact pair, never from a value written down beside it.
- **[GOTCHA S67] The staged `.env` on RC may carry a STALE `TEAM_NAME=vedur`** -- copied from `.env.example` 08-28, before the PO's rename (commit 9999c5c). Compose already defaults `${TEAM_NAME:-paunvere}`, so **absent is correct and stale is worse than nothing**. Replace the line, never append beside it. The task as briefed ("add TEAM_NAME=paunvere") would have appended.
- **[LEARNED S67] Read the entrypoint, not the runbook's EXPECT lines.** Verified from source that the seed-stamp discriminator (added 08-31) post-dates this container, so the stamp file does not exist -> post-rebuild boot would print `NOTE: … differs` and leave English in place forever. That is WHY the manual displacement is unavoidable, and it makes this the LAST hand-run of it (the boot writes the stamp).
- **[DEFERRED] `[PO-17]`** bridge-via-explicit-`dns:` -- Hopper's resolver-scoping hypothesis (embedded DNS 127.0.0.11 -> WARP DoH on 127.0.2.2/.3 = the CONTAINER's loopback inside a bridged netns). Exact 3-command test in design doc 2.4. Only if real isolation is ever wanted for a named reason.

---
## Session transcript (prune beyond line 100)

### 2026-08-28 S66 -- Joosep Madar container design (RC), infra track w/ Hopper
- Delivered `docs/joosep-container-design-2026-08-28.md`: debian-from-scratch + native Claude installer; dropped Python/Playwright/2nd-sudo-user/courier-key/dashboard-port; ADDED cpu+mem+pids limits. Port 2231, menu char `j`. Role-split held: no host reads, no writes outside my doc + scratchpad, no git; classified the bridge probe Tier M rather than calling it a read to ease approval. (S66 narrative pruned S67 -- it is in the design doc.)
- [LEARNED S66] I designed off the artifact the REQUEST named (apex) instead of asking what already exists on the host. Hopper's inventory found `allerk` -- a 10-day-old container that was already the answer. **Ask the executor "is there a closer precedent on the box?" before templating off the named one.** Cost: a full v1->v2 rewrite and one retracted section.
- [LEARNED S66, TWICE-WRONG -- my worst error this session] I claimed a "2230 collision" (v2), then when corrected **ESCALATED it to a three-way divergence (v2.1) instead of asking whether there was a claim at all**. There was none: **TCP ports are a PER-HOST namespace**, so screenwerk@shipyard:2230 and allerk@RC:2230 are both true. I had imported a shipyard row into an RC port question. **Compounding-on-correction is worse than the original mis-read** -- when corrected, re-derive from scratch before reframing. Also: **there is NO fleet-wide port-uniqueness invariant** (prod-llm AND RC both run :2226), so "claimed on another host" never disqualifies a port.
**[S66 gotchas -- FILED, read the wiki not this]** ~15 entries at wiki 214: auto-tmux-bare-shell, cleartext-creds-bashrc, host-net-zero-isolation, image-tag-not-identity, per-host-port-namespace, verification-step-goes-stale, trailing-pipe-exit-status, authorized-keys-comment-not-evidence, pids_limit-one-setting, record-lives-where-claim-is-made, scope-bound-identifier (umbrella), container-control-as-only-independent-check, warp-cgnat-misread, hub-fsync-false-accept, smoke-test-throwaway-identities. Plus `capability-guard` + `verification-certifies-a-moment` amendments.
[GOTCHA S66] apex's live `.env` is neither superset nor subset of what the container consumes: carries `TUNNEL_TOKEN` (no consumer, dead cloudflared-sidecar leftover) and OMITS `REPO_URL`/`SOURCE_REPO_URL`/`TEAM_NAME` (ride compose defaults). Never inherit a `.env.example`; derive it from the compose `environment:` block, which is the actual contract.
[FACT S66] RC has NO host ingress filtering -- `iptables -L` INPUT policy ACCEPT with **zero rules**, ufw/nftables/firewalld inactive. **Port choice is not a security control on this host.** Also: `ai-teams-claude:latest` IS on RC (07-22, 1.73GB); `allerk:latest` derives from nothing (773MB). Compose creates a `<name>_default` bridge even for `network_mode: host` services -- an artifact, not a misconfiguration.
[DECISION S66] WARP CA: prefer apex's **bind-mount** of the host cert over allerk's baked-in copy. A stale baked CA fails SILENTLY after rotation and reads as a network fault (needs rebuild); a moved host path fails LOUDLY at boot. Trade a silent failure for a loud one.

---
## STANDING DECISIONS (carry forward)

[DECISION] Single-provider default for agent runtime; multi-provider = sidecar, not peer (seams: peer/sidecar/MCP). `GatewayPorts yes` on RC NOT recommended -- loopback-only default; host-networking is the consent mechanism.
[PATTERN] `network_mode: host` simplification window: tunnels/sockets/local listeners free; tradeoff breaks E-deployment portability (Swarm cannot host-network). Plan b-host vs e-swarm profiles in compose from day one.
[PATTERN] Author attribution: bold `(*FR:Brunel*)` per common-prompt. Never italic.
[DECISION -- S50, Aen-ratified] Standby-agent hot-fix discipline. Fix-then-flag correct ONLY when ALL FOUR hold: (1) artifact in active deployment + time-pressure; (2) fix in MAY-WRITE, bounded, no protocol/design surface; (3) flag immediately w/ reasoning + verification; (4) explicitly on standby for that failure class. Remove any -> default reverts to surface-before-fix. Filed Cal `process/standby-agent-fix-then-flag-discipline`.
[DECISION -- S55/S56] Courier cutover = LAUNCH-OVERRIDE two-config: explicit fr-courier.config.json (gitignored, always-safe default, holds hub key) + fr-courier.config.auto.json (gitignored as of S56, loaded ONLY by restart-fr-courier-with-pid.ps1 -Config default, behind V4 dry-run guard). "auto" activates ONLY via the wrapper; rollback = stop calling wrapper (explicit untouched). Resolver liveness = PROCESS-based (os.kill + /proc field-22 procStart guard + null-pid guard), cross-platform _pid_alive (Windows tasklist / POSIX os.kill).

## CARRY-FORWARD GOTCHAS (all containers)

[GOTCHA -- S66, STRONG] TWO attach mechanisms exist in the fleet and they CONFLICT. (A) `.bashrc` auto-tmux hook (`entrypoint-backlog-triage.sh:286-299`, runbook §18) fires on `[ -z "$TMUX" ] && [ -n "$SSH_CONNECTION" ]` -> `exec tmux attach`: hijacks EVERY login, a bare shell is UNREACHABLE. (B) remote-command launcher (`/usr/local/bin/tmux-apex`, `entrypoint-apex.sh:428-432`) + rc-connect appending the tmux cmd to `$sshArgs` (`rc-connect.ps1:162-165`): bare ssh untouched. Any "shell OR session" requirement REQUIRES B and forbids A. rc-connect already encodes the distinction as the registry `tmux` field.
[PATTERN -- S66] **Person-shaped vs team-shaped container.** Team-shaped (apex) names volumes after the TEAM (`apex-research_apex-claude-home`) -> the team name is baked in, rename = volume migration losing OAuth + scratchpads. Person-shaped (`allerk`) names them after the PERSON (`allerk_home`, explicit `name:` keys) + whole-$HOME vol + separate `work` vol mounted OVER $HOME -> the team name lives only in a dir inside the home vol, rename = `mv`. **Pick person-shaped whenever the occupant is one human**; it decouples the container from whatever team runs inside it. (I asserted the bake-in as a blocking ordering constraint in v1, then had to retract it -- it is a property of the BASE, not of containers.)
[PATTERN -- S66] `authorized_keys` as a `:ro` BIND MOUNT re-read every start (allerk) beats `SSH_PUBLIC_KEY{,_2,_3}` env vars (apex): adding a key = edit file + restart, no recreate, no rebuild.
[GOTCHA -- S66] `-Session`-style flags run their launcher as an ssh REMOTE COMMAND = non-interactive non-login shell, never sources `.bashrc`. Native Claude lives in `~/.local/bin` -> `claude: command not found`, but ONLY over `ssh host <cmd>`. Fix = `SetEnv PATH=/home/<user>/.local/bin:...` in sshd_config (allerk does this).
[GOTCHA -- S66] A container is NOT a security boundary, and specifying it carefully does not make it one. Check the HOST's `authorized_keys` + `docker` group membership BEFORE writing any "must not reach" list: `docker` group == root-equivalent (`docker run -v /:/host`). Lerko's allerk README says it best: *"separates configuration, not privilege"*; the only real boundaries are **rootless Docker or separate Linux accounts**.
[DEFECT -- S66, unfixed, `[PO-9]`] apex registry row says `"tmux": "apex"` but `/usr/local/bin/tmux-apex` manages session `apex-research`. Two sessions can coexist; the menu can land in an empty one.

[GOTCHA] PO edits live files in parallel during a Brunel pass. ALWAYS re-read before each Edit batch -- Edit tool's "File modified since read" catches it.
[GOTCHA -- S52] Rebuilt image w/ UNCHANGED compose config NOT adopted by plain `docker compose up -d`. MUST use `--force-recreate`. [GOTCHA -- S52] Test supervisor relaunch by killing ONLY the leaf service pid (`pkill -f <svc>`), NOT a group/subshell kill (takes the loop too = test artifact).
[GOTCHA -- S52] Single-instance lockfile on PERSISTENT volume + pid-only staleness = false-refuse across container recreate (pid namespace resets, recorded pid aliases a live unrelated process). Needs CONTAINER-INSTANCE discriminator: PID-1 starttime (`/proc/1/stat` field 22, parse from last `)`), NOT boot_id (host-kernel-scoped, unchanged by recreate).
[GOTCHA -- S52, STRONG] `( while true; do svc; done ) &` restart-loop under `set -e` MUST `set +e` inside the subshell -- errexit inherited, first non-zero exit kills the loop before relaunch -> restart-on-exit silently fails (restart-on-boot still works, masking it). Reproduce the REAL env (set -e + terminal `exec`).
[GOTCHA -- S52] Keygen in a build flow MUST be generate-if-absent (`[ -f key ] || ssh-keygen`), else every rebuild regenerates -> pubkey churn -> forced re-register.
[GOTCHA -- S52, CORRECTED S53] Ephemeral-home paths don't survive rebuild: `~/.ssh` (courier key, known_hosts), `/etc/ssh` (host keys). Fix-split: KEYS -> generate-if-absent or build-secret-seed->copy-each-start on persistent path; STATEFUL CONFIG -> persistent volume + guarded `[ ! -f ]` create. Private keys ONLY on persistent vol, NEVER baked into a layer.
[CORRECTION -- S53] `~/.claude.json` ($HOME ROOT, ephemeral) = CONFIRMED NON-ISSUE (onboarding/trust state, regenerates). The real stateful files are `~/.claude/settings.json` + `~/.claude/mcp.json` + `~/.claude/.credentials.json` (under the persistent volume, guarded-create). One char of path = opposite persistence semantics.
[GOTCHA] WARP TLS interception: `network_mode:host` + `NODE_EXTRA_CA_CERTS=/opt/warp-ca.pem` + system CA. WARP 2nd-container: bridge can't egress + host-net collides sibling ports (`ports:` remap no-op under host-net) -> host-net + docker-exec drive.
[GOTCHA] Named volumes created as root -> `chown 1000:1000` in entrypoint. ubuntu:24.04 ships stock `ubuntu`@uid1000 -> `useradd -u 1000` fails -> `userdel -r ubuntu` first.
[GOTCHA -- S50, OpenSSH 9.x] `ssh-keygen -A -f <prefix>` does NOT create `<prefix>/etc/ssh` and FAILS if absent. Fix: generate single ed25519 directly with explicit `-f <path>`. Smoke-test host-key assertion.
[GOTCHA -- courier S51] ghost_outbox name resolves to team by stripping `-bridge`; MUST be `<registered-team>-bridge` or deposit hits E_UNKNOWN_TEAM, consignment retain-loops. Spool re-resolves dest at DEPOSIT time -> config-fix+restart auto-heals stuck entries.
[GOTCHA -- Windows daemon stop, S51] PowerShell Stop-Process == hard kill; Python gets NO signal -> in-process drain + atexit don't fire. Drain must be external: stop->wait-exit->`--drain-once`. (POSIX delivers the signal; only Windows kill path affected.)
[GOTCHA -- S54, UPDATED S58] stop/start-fr-courier.ps1 manage the PID-FILE lifecycle; the FR courier = SCRIPT-PAIR-launched python.exe. The `FrameworkResearch-Courier` Scheduled Task is now DISABLED (S58, Hopper 16:06; was "Ready/dormant" but its logon+resume triggers DID fire across session boundaries -> orphan pid 38044 holding the lock = Bug B). Registration retained for 2.1.177 rollback (Enable-ScheduledTask). On 2.1.178+ the Task is HARMFUL (pins stale session-<id>, races the wrapper) -- the courier is per-session via the wrapper. LESSON: a dual launcher (script-pair + Task) with no shared pid tracking = the orphan-lock class; stop must reclaim by IDENTITY (CIM cmdline match), not just its own pidfile.
[GOTCHA] CRLF from Windows autocrlf breaks entrypoints. Fix: `sed -i 's/\r$//'` then rebuild.
[GOTCHA] Inbox files created at agent registration time. Specialist -> unregistered agent = message LOST. Spawn service-role agents BEFORE message senders. [GOTCHA] Container reference-memory path: `~/.claude/projects/-home-ai-teams/memory/` ($HOME-dir-encoded), NOT `~/.claude/memory/`.

## DEFERRED (future surfaces)

- V5b/P6 attached-pane proactive-wake re-test (RfC #9, only if latency shows); courier-side CLI re-validation (hub insensitive, courier sensitive); OAuth on hr-devs PROD-LLM (PO manual step); hub container as standalone image; raamatukoi-dev VPS container; MCP pattern for visual QA; provider-outage behavior in containers; external audit container spec
- [S66] `[PO-17]` bridge-via-explicit-`dns:` (resolver-scoping hypothesis, exact test in joosep design §2.4); `[PO-9]` registry drift; allerk README's other ~35 sections (heading-map first, only on a named question)
- **[CLOSED S67] a1.2 DELIVERED to Cal** (3 submissions 10:44/10:47, dedup pre-run; all adopted, read the wiki not this). **The one ACTION item they surfaced: `joosep_sshd` has NO BACKUP** -- it holds the host key we told Joosep to verify out of band, and my design never provided one; goes in Step 13 hand-over (PO keyboard item, one `docker run --rm -v joosep_sshd:/state:ro ... tar czf`). Same omission on `sm-state` (11 weeks, zero backups).
- **[LEARNED S67, the durable one -- SEAM FAILURE]** My 3 submissions were each correct inside their own scope and the failure fell **between** them: sub-3 said "a recorded value is legitimate as transport, its validity must itself be checkable"; sub-1 supplied a validity check; I ran it, it PASSED, and the value was still non-portable -- **sub-3's deferral asked a broader question than sub-1's answer covered, and nothing compared the two.** **A remedy that LICENCES a failure is visible from inside itself; a remedy that DEFERS to a condition is not.** Both entries read as complete -- one names an obligation, the other satisfies one. **A seam between two correct entries cannot be seen from inside either.** Consequence: when A defers to a condition B supplies, cross-linking is NOT enough -- **co-locate**, because a named-but-unsupplied obligation is worse than silence (silence prompts a question; a named obligation closes one).
- **[CLOSED S67] Two non-author read-backs done 10:53** (Cal routed; I was the only non-instance reader). `state-the-membership-rule` CONFIRM -- verified both claims at source (`wiki/CLAUDE.md` has no frontmatter; the body-level match is `contracts/entu-competency-index-schema.md:54`, unindented, different vocabulary) and the defect **still reproduces today: 116/220, 117 with the false match**. Flagged: instance 2 says 112, instance 3 says 114, same noun, nothing reconciling them (they are different moments -- 112/214 -> 114/216 -> 116/220); date the figures. `disjoint-remedy-test` CONFIRM w/ **structural defect: `## Instance zero` sits INSIDE the `## Related` list (line 66) and orphans the last bullet** -- a third slip in the lineage of an entry about a fix reproducing its own defect. Honest datapoint given: I hit the merge/cross-link question today and did NOT reach for the test.

## INFRA REFERENCE

- Stationmaster hub: `sm@10.100.136.162 -p 2222`; FR key `~/.ssh/sm_framework-research`; FORCED-COMMAND endpoint (structured JSON, not a shell). FR grants both ways with apex-research.
- FR courier runs LOCAL on this Windows dev box FROM this repo's working tree (`teams/framework-research/poc/ghost-bridge/`). Commits from this tree are immediately on-disk where the courier imports them -- NO git pull (that's the rc-host-clone pattern = apex/probe, DIFFERENT substrate).
- Two LOCAL configs (both gitignored as of S56): fr-courier.config.json (explicit, hub key, always-safe default) + fr-courier.config.auto.json (inboxes_dir:"auto", loaded only by the wrapper). Wrapper restart-fr-courier-with-pid.ps1: -Config defaults to .auto.json, -SessionPid optional (mode-a multi-team), V4 dry-run pre-flight guard.
- apex-research container: live exec route = ssh `dev@` the rc HOST then `docker exec apex-research`. Entrypoint `/entrypoint-apex.sh` (MY artifact). Compose `network_mode: host`, no cloudflared sidecar. Designs repo: `mitselek-ai-teams/designs/deployed/<team>/container/`.
