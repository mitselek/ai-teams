# Brunel scratchpad

## Summary (lines 1-15 — always read on startup)
- **Current state:** S50 (2026-06-12) — Task #1 ACCEPTED (Aen). Committed bfe3060 (incl. entrypoint host-key fix). Hopper BUILD CLEAN exit 0 on prod-llm Debian (image stationmaster:1.0.0, all 8 stages) → my one-unverified-layer now verified. T6.a gate PASSED (Hopper: prod-llm ext4-on-LVM 50/50 both primitives, tmpfs-trap caught). Herald integration 14/14 PASS (real courier ⇄ real sm-shell, ssh hop stubbed). Hopper mid-deploy (up→smoke→in-container T6.a). On standby for deploy-close support.
- **Active items:** Task #1 deliverables complete in `poc/ghost-bridge/stationmaster/`: Dockerfile (Debian bookworm-slim, D10), docker-compose.yml (restart: unless-stopped, sshd :2222, named volume sm-state), sm-shell (per-conversation NDJSON forced-command shell, all 8 protocol verbs), entrypoint.sh, sshd_config.stationmaster (forced-cmd + ExposeAuthInfo for ping fingerprint), sm-register (operator helper), smoke-test.sh (post-deploy over-ssh acceptance), .dockerignore. Runbook at `docs/stationmaster-hub-deployment-runbook.md`. Task #3 (T6.a Debian re-run) is Hopper/operator — hub spool doesn't use exclusive-create so insensitive to it. Task #4 (register FR as first customer) blocked on #1 deploy.
- **Key decisions this session:** sm-shell is per-conversation (NOT a daemon) → S48 zombie-daemon failure mode structurally cannot recur (sshd is sole persistent proc, visible+killable+restart:unless-stopped). Hub spool uses coarse flock per conversation (correct-over-clever, low org volume) — courier-side in-place-write ban does NOT apply to hub-owned spool. Host keys persisted on volume → stable fingerprint across rebuilds (avoids ssh-keygen -R gotcha). fsync chain (tmp+fsync+rename+dir-fsync) backs the "accepted=fsync-durable" §5.2 promise. fcntl/dir-fsync made import/OSError-defensive for Windows dev-test only (deployed hub always Linux, real locking).
- **Carry-forward:** [DEFERRED] deploy to prod-llm — PO granted FULL build order (deploy GO after Aen review → T6.a gate → Task #4). Hopper executes; not spawned yet. Dispatch package STAGED at `docs/stationmaster-deploy-dispatch-hopper.md` (Tier M throughout, greenfield; build-first dry-run; I'm standby for build failures). T6.a harness WRITTEN + self-tested 50/50 clean at `poc/.../stationmaster/t6a-race-harness.py` — ready to hand Hopper once target FS settled. OPEN DECISION→Aen: T6.a gate target filesystem (prod-llm vs FR-courier-host; gate is COURIER-side exclusive-create property, hub uses flock so insensitive). [DEFERRED] courier-side CLI 2.1.170→2.1.175 re-validation. [WATCH] §10 stationmaster-as-sender alert path dormant in v1.

---
[WATCH — sketch-grade, comms-substrate duplicate-delivery] Cal's S36 catch-up ACK message (timestamp `[2026-05-27 13:09]`, three items: Recursive-Narrowing read-back / 2.7 close-out / 2.5 close) was delivered TWICE this session — once early (acknowledged 14:02) and again later, byte-identical. No new content, all three already closed. Did NOT re-ACK (would be noise; original stands). Relevant to E4 FR-comms-substrate-routing-mode watch I carry — a concrete duplicate-delivery instance. Distinct from the ghost-bridge zombie-dup mechanism (that was N senders; this is one message re-delivered to me as receiver). Sketch-grade, n=1 this class.

## SESSION 48 — ghost-bridge SPEC v3 (2026-06-10)

[LEARNED — STRONG] **SPEC v3 absorbs S48 findings that invalidated v1's core mechanism.** Delivered to Aen 12:10. Five hard constraints: (1) SendMessage = plain file I/O, members[] never in dispatch path → ghost members RETIRED, restore-ghost-members.sh retired; (2) harness owns files for names it TRACKS (rewrites from in-memory, clobbers external read-flag edits → 4x-8x dup at apex); never-tracked names = plain files, clean; (3) trust is filesystem-level (SSH keys/perms), `from` unverifiable, NO app-auth; (4) Windows SSH truncates argv ~8KB → scripts ship via stdin (already in code); (5) daemon launched from harness tool-call dies (suspected reaping, UNVERIFIED).

[LEARNED — STRONG] **Dedup design = two mechanisms surviving the confound.** Confound: duplicating outbox was BOTH registered AND restored-from-repo; clean one was neither. Can't isolate trigger. Mechanism A (primary) = daemon-owned dedup ledger (`ghost-bridge.state.json`, outside harness-touched files) → daemon stops depending on harness-owned read flag as forwarded-marker → robust to ANY clobber cause. Mechanism B (defense) = exclude bridge outboxes from persist/restore (removes confound branch ii). A holds either way; B alone bets on confound resolving favorably. Honors PO-leans-simple: A = one small file, B = two-line script edits, no new service/dep. Today's never-registered-name quick-fix is necessary-not-sufficient (bets the confound).

[GOTCHA — persist/restore touches bridge outboxes TODAY] Verified by reading persist-inboxes.sh (all-inboxes mode globs runtime/inboxes/*.json → includes bridge outboxes) + restore-inboxes.sh (copies all back, filters only shutdown/idle). So restore-from-repo (confound branch ii) is live every session. Mechanism B excludes them. Volta owns those scripts → [COORDINATION→Volta] routed in SPEC.

[CARRY-FORWARD — SUPERSEDED by v3.1, see correction below] Code rebuild scope (after ratification, NOT yet): remove ghost-member assumptions; add Mechanism A ledger; keep stdin transport + from-rewrite + multi-pair config; re-home daemon launch (OQ#1 placement); coordinate Mechanism B with Volta. Current ghost-bridge.py main gaps: read-flag-as-forwarded-marker dependency + harness-parented launch.

[CORRECTION — v3.1, Aen 12:42] **v3 Constraint 1 (harness clobbers tracked-file read flags) was WRONG.** Windows `tasklist` revealed 11 zombie daemon instances alive simultaneously — every background launch today survived; Git-Bash `kill -0`/`ps` liveness checks are BLIND to Windows-native processes and falsely reported them dead, so we relaunched repeatedly. 4x-8x dup = N living instances each forwarding independently. Fresh-name test was clean only because just the newest instance had that pair in config. Re-grade: Constraint 1 WITHDRAWN (observation real, mechanism inferred+disconfirmed); confound DISSOLVED (nothing left to explain); Mechanism A dedup ledger DROPPED (read-flag bookkeeping was correct all along under single instance); Mechanism B demoted to optional low-pri hygiene (no longer load-bearing). Daemon-lifecycle constraint INVERTED+PROMOTED to central requirement: not "daemons die" but "harness-launched daemons are invisible+unkillable from Git Bash and accumulate" → require EXACTLY ONE instance, human-started in visible terminal, liveness via OS-native tools (tasklist/ps), single-instance enforced (PID check must actually work on Windows). Acceptance test gained step 0: verify instance count==1 via tasklist before any count test. The receiving-end-as-oracle test SURVIVED unchanged — it's exactly what caught this (count was N×). SPEC v3.1 written, delivered to Aen.

[LEARNED — STRONG, claim discipline] **Tag MECHANISM separately from OBSERVATION.** v3 tagged Constraint 1 [TESTED] but only the *observations* (duplicates real) were tested; the *mechanism* (harness flag-flipping) was inferred and turned out wrong. A real observation does NOT validate the mechanism you guessed produced it. v3.1 introduces [TESTED-OBS] / [TESTED-MECH] / [INFERRED] three-way tagging. This is the second instance this session of the over-generous-TESTED failure (v1's members[]-gating was the first) — the meta-lesson is now n=2 within S48.

## SESSION 37 — hr-devs lifecycle consultancy via ghost-bridge (2026-06-09)

[LEARNED] **hr-devs consultancy 3-item analysis delivered to Aen 14:08.** #1 leadSessionId: the orphan symptom is downstream of substrate fact — leadership is parent-CLI in-memory state, NOT a transferable on-disk session ID; "re-bind" isn't a missing procedure, it's not a substrate-exposed op. Fix = `TeamDelete`(best-effort)+`TeamCreate` unconditionally at startup (one primitive both fresh-start and stale-orphan paths). #3 restore-ghost-members.sh logic summarized (7-step) + Windows-corruption flag (line-94 tmp+mv races harness in-memory config state; SAFE on Linux, our actual substrate). #4 two-invariant rule (canonical `wiki/patterns/repo-as-durable-store-teamdelete-as-release-primitive.md`): repo = sole durable store, TeamDelete = distinct in-memory release primitive; session is disposable → nothing to orphan. n=3 cross-team (apex #62, FR S21, esl-suvekool 22→23).

[GOTCHA — Windows, NOT wiki-grade] restore-ghost-members.sh corrupts config.json on Windows per Aen's today-finding: jq-write-to-tmp then `mv` races the harness's in-memory config copy. Classified cross-team-share caveat (flagged to hr-devs since they may be Windows-hosted), NOT framework finding — Windows-local-dev friction per `feedback_no_windows_substrate_findings.md`; Linux-replication won't happen because bug is Windows-specific by nature.

## SESSION 34 — Apex authorized_keys dispatch (2026-05-20)

[LEARNED] **Wrong-host claim was itself wrong; original dispatch operated against the correct substrate.** PO surfaced apparent wrong-host concern at 17:43 (claiming num:9 PROD-LLM was the canonical apex); Aen relayed at 17:47; I propagated to Hopper at 17:59 without checking the registry source myself. PO re-checked his own `rc-deployments.json` at 18:08 and confirmed: num:1 (RC-server host) + num:2 (apex-research container at port 2222 with id_ed25519_apex key) ARE the canonical apex substrate — exactly what my r3 dispatch cited. The 17:43-17:47 "wrong host" framing was a transient PO mis-read; retracted 18:08. **Discipline gap on my side: relay-fidelity-mid-conversation** — accepted tasker framing as authoritative without primary-artifact (`~/bin/rc-deployments.json`) check. Symmetric to S31 Stage-2-stale-fold but applied to mid-conversation tasker statement, not relay text. Same generalization: primary-artifact-over-relay holds for tasker-framing too, not just for cross-agent relay.

[LEARNED — STRONG] **Diagnostic loop produced multi-system-failure prevention for the production apex substrate.** Substrate IS in degraded state (verified via Hopper's P1.2c three-probe batch + P1.2d backup-read): no `.env` at $COMPOSE_DIR, container is a 2026-04-29 fresh-clone survivor, `docker compose up --force-recreate` would wipe SSH keys + GITHUB_TOKEN + ATLASSIAN_API_TOKEN + TUNNEL_TOKEN simultaneously. The original ask (Aleksandr's key persistence) IS addressable on this substrate; deferred legitimately to apex team's post-maintenance window per the r3 Phase-2 plan, but Phase-2 sanction package itself must be rebuilt to address the degraded state before recreate (`.env` reconstruction prerequisite). The r3 Phase-2 recreate as drafted remains rescinded by Aen.

[LEARNED — STRONG] **Sub-shape catalog A-E surfaced this dispatch, all instances of "discriminator/premise anchored on a sub-canonical source":**
- A: template-stub-as-selector (P1.1 `michelek` regex)
- B: name-inference-as-selector (P1.2a 2-candidate ambiguity)
- C: documentation-convention-as-discriminator (P1.2a label-key underscore-vs-dot)
- D: deployed-artifacts-as-premise-source (P1.2b `.env`-presence assumption)
- E: substrate-ownership-vs-design-ownership distinction (Layer 1 FR-design / Layer 2 consumer-operational / Layer 3 runtime, two drift surfaces concretely materialized — HEADLINE)
Recovery pattern for all: cheap Tier R probe of substrate-live state beats offline inference. Cal-Protocol-A submission planned at session wrap: `wiki/patterns/discriminator-anchored-on-sub-canonical-source.md` (A+C as instances) + `wiki/patterns/three-layer-substrate-truth-discipline.md` (joint-authored with Hopper, E as headline + B+D as illustrative sub-shapes). **Earlier-proposed Sub-shape F (registry-entry-choice-from-first-match) STRUCK** per Aen 18:19 — was based on the now-disproven wrong-host premise; no real instance from this dispatch.

[LEARNED] **r3 Phase-2 sanction package RESCINDED** by Aen 17:34 + retroactively by PO 17:37. Reason: r3 Phase-2 (`docker compose up -d --force-recreate apex-research`) would have caused multi-system failure against RC-server (SSH lockout + GitHub auth + Atlassian auth + Cloudflare tunnel auth all empty). Even after substrate-selection correction, future Phase 2 against PROD-LLM apex needs new sanction package built on fresh probe-suite of THAT host's substrate-truth. The r3 package is dead; do not resurrect.

[LEARNED — joint with Hopper] **Three-layer substrate-truth model:** Layer 1 = FR design-as-shipped (`designs/deployed/<team>/container/*`) canonical for design lineage only; Layer 2 = consumer-team operational copy (e.g., `/home/dev/github/<repo>/`) canonical for compose-up resolution; Layer 3 = runtime container state (Config.Env, mounted volumes, in-process state) canonical for current-serving. Each layer can drift from its neighbor without notice. My `[LEARNED — STRONG, session 33+]` read-your-own-deployed-artifacts discipline reads Layer 1 only — INSUFFICIENT for FR-shipped substrates the consumer team operationalizes. Hopper's prompt has the same gap. Joint amendment-4 candidate (Brunel + Hopper) for Celes routing at session-end: "read all three layers on first-dispatch against a substrate." Defer until Celes online; pattern surfaced today, drafting later.

[LEARNED — provisional, n=12 this session] **Cross-in-transit pattern at high tasker↔operator cadence.** Async inbox transport with no priority lane for HALT messages accumulates message-crossings when pair-loop cadence is dense (~1-3 min per round-trip). Aen-named meta-observation: "when team-lead halts mid-pair-loop, what's the structural mechanism to interrupt in-flight dispatches at the SendMessage layer?" Three future-session candidates: (a) HALT primitive at SendMessage layer with priority delivery, (b) Brunel-side discipline pattern polling for halt-signal between every outbound, (c) acceptance that pair-loops at high cadence have inherent halt-latency = round-trip-time. Filed pending Linux-substrate replication per `feedback_no_windows_substrate_findings.md` — n=12 stays Windows-session-local class until Linux-substrate same-cadence dispatch surfaces equivalent count. Aen's own 17:58 direct-to-Hopper crossed my 17:59 relay — Aen self-noted as team-lead-side discipline gap (impatience under Brunel-recovery → short-circuit of role-of-record); my silence during tool-call recovery was the precipitating factor.

[CARRY-FORWARD — session-end Cal Protocol A submissions]
1. `wiki/patterns/discriminator-anchored-on-sub-canonical-source.md` — Brunel-authored. Sub-shapes A + C as instances. Recovery: substrate-live discriminator + JSON-dump-when-single-probe-empty.
2. `wiki/patterns/three-layer-substrate-truth-discipline.md` — JOINT (Brunel architectural distinction + Hopper operator-defense pattern). Sub-shape E as headline; B + D as illustrative. Catalyzing incident: Hopper ops-log entry 2026-05-20T17:09+03:00 at `teams/framework-research/docs/operations-log-2026-05.md`.
3. **Amendment-4 candidate (Hopper + Brunel prompts):** three-layer Diagnostic Discipline. Surface to Celes at session wrap, NOT this session. Draft text in Hopper's 17:35 message body + my 17:37 elaboration. **Amendment-5 candidate (dispatch-target-correctness probe-suite) WITHDRAWN** — solved a non-problem from this dispatch (Sub-shape F struck); revive if a real instance surfaces in a future dispatch.

[CARRY-FORWARD — Phase-1 re-architecture (post-maintenance-window)] Future-session work: when apex team approves a maintenance window, redo Phase 1 against the correct substrate (num:2 = `ai-teams@100.96.54.170:2222`) with `.env` reconstruction as the prerequisite step BEFORE any recreate. Hopper's three-probe pattern + three-layer substrate-truth reads stay as the diagnostic shape. Don't reach for the rescinded r3 package; build fresh sanction package once Phase-1 fix-shape settles around the actual substrate state (which we now know: no `.env`, container survives on pre-fresh-clone Config.Env, full credential cluster needed to reconstruct).

[GOTCHA — apex-research production substrate as of 2026-05-20] Container at `ai-teams@100.96.54.170:2222` is degraded-but-stable. No `.env` at `/home/dev/github/apex-migration-research/`. `docker compose up --force-recreate` = multi-system failure (SSH lockout + GITHUB_TOKEN + ATLASSIAN_API_TOKEN + TUNNEL_TOKEN all wiped). Pre-fresh-clone backup `.env` exists at `apex-migration-research.pre-fresh-clone-2026-04-29/.env` and contains the operational credential cluster — useful for Phase-1 fix-shape design.

[CROSS-LINK] Hopper ops-log canonical record: `teams/framework-research/docs/operations-log-2026-05.md` entry timestamp 2026-05-20T17:09+03:00. Aen PO-facing memo: `teams/framework-research/docs/apex-keys-dispatch-2026-05-20-findings.md` (includes Aen-named Sub-shape F + wrong-host correction at the top). Both are public-grade audit artifacts.

[STATUS] Idle pending Hopper close-out + Aen direction. Surgical scratchpad prune complete 17:48 (this rewrite).

## SESSION 34 — P3 + P4 dispatch arc closure (2026-05-21 09:18)

[LEARNED — STRONG] **Original PO ask ACHIEVED via Phase-1-Redux + Phase 2 pair-loop.** Three SSH keys (PO/Aleksandr/rc-connect) installed in apex-research container's `authorized_keys` post-recreate; backed by canonical `.env` + amended operational compose-yml. Multi-system credential failure prevented (original r3 Phase-2 against degraded substrate would have wiped SSH+GitHub+Atlassian+Cloudflare auth). Catalyzing-incident chain: `teams/framework-research/docs/operations-log-2026-05.md` (6 append-only entries, 2026-05-20T17:09 P1 abort through 2026-05-21T09:18 P4.8 close). GH_TOKEN preservation honored per PO 19:35 direction via P4.05 Tier M compose-yml amendment.

[LEARNED — STRONG] **Sub-shape A catalog at n=4 self-instances with A.1/A.2 sub-sub-shape distinction.** All four self-instances in MY dispatch-authoring text, all caught by Hopper's hard-gate discipline:
- A.1 (identifier-grammar mismatch, n=3): P1.1 michelek-regex template-stub / P1.2a label-key underscore-vs-canonical-dot / P3.6 `[A-Z_]+` digit-exclusion
- A.2 (transit-chain mismatch, n=1): P4.05 awk runaway-string on `${GH_TOKEN:-}` in PowerShell→base64→bash→awk chain
Shared lesson: verify-substrate-truth-anchored-grammar at innermost parser BEFORE relying on outer-layer pass-through. Recovery posture (A.1+A.2): surface-back-with-substrate-truth-evidence before silent re-attempt; substrate-truth diagnostic (JSON-dump / diff-vs-backup / alt-regex / byte-equality drift-check) surfaces actual grammar in one cheap Tier R round-trip.

[LEARNED — joint with Hopper, structural reframe] **Reference-example status attaches to dispatch-design + pair-as-unit-loop, NOT individual effort.** Hard-gate-discipline + Tier-D-no-execute-without-full-sanction + within-dispatch-agency-bounded-probes are the reusable patterns; pair-as-unit (diagnostic+execution+supervision) is what makes them work. Hopper's articulation; canonical for the joint Cal entry's catalyzing-incident citation.

[LEARNED] **Relay-fidelity-mid-conversation gap surfaced twice this dispatch.** (1) 17:43 PO wrong-host claim relayed without registry primary-artifact check (retracted 18:08 by PO). (2) 19:33 "substrate-correction normalization" framing relayed + Aen-ratified, reversed by PO 19:35 ("preserve their GH_TOKEN"). Same generalization: tasker/operator framing that contradicts prior dispatch premises requires primary-artifact check before propagating. Stage-2 extension of `relay-to-primary-artifact-fidelity-discipline.md` applied to in-session framing assertions. Cal-Protocol-A submission carries as supplementary discipline.

[CARRY-FORWARD — session-end Cal-Protocol-A submissions, FINALIZED]
1. `wiki/patterns/discriminator-anchored-on-sub-canonical-source.md` — Brunel-phrasing + n=4 catalog with A.1/A.2 sub-sub-shapes. Joint cross-link to Hopper operator-defense recovery pattern.
2. `wiki/patterns/three-layer-substrate-truth-discipline.md` — JOINT (Brunel architectural + Hopper operator-defense). Layer 1 FR-design / Layer 2 consumer-team-operational / Layer 3 runtime-container-state. P4 arc reinforces S20 P1.2c catalyzing incident.
3. Hopper-Amendment-4 (three-layer Diagnostic Discipline for `hopper.md`) — Hopper-authored, joint cross-link to Brunel diagnostic-gap; surfaces to Celes when she's next online.

[CROSS-LINK FINAL] ops-log 6 entries 17:09/18:05/18:22/18:46/19:23/09:18. Aen PO-facing memo: `teams/framework-research/docs/apex-keys-dispatch-2026-05-20-findings.md` (Phase 2 ACHIEVED status pending Aen amendment when he commits).

[STATUS — S34 SHUTDOWN] Aen shutdown_request received 09:46 (request_id `shutdown-1779349562835@brunel`); reason "original PO ask achieved; all artifacts committed; transition to S35 prep". Per common-prompt Shutdown Protocol: scratchpad updated (this entry), closing message to Aen with [LEARNED]/[DEFERRED]/[WARNING]/[UNADDRESSED], approving shutdown.

## SESSION 35 — task #7 joint Cloudflare gap analysis + task #10 pilot substrate brief (2026-05-25 → 2026-05-26)

[LEARNED — STRONG] **Joint dispatch with Volta on task #7 (Cloudflare Claude Managed Agents substrate gap analysis) produced the strongest single framework-grade finding FR has produced in recent sessions: C1 cluster-decomposition meta-principle.** *"Clusters decompose along their coupling-dimension; the coupling-dimension is the load-bearing property to identify."* n=3 across three coupling-dimensions: mVox M1-M5 (team-property), Cloudflare 7-mechanism cluster (team-property), Sub-shape E three layers (ownership-locus). Methodology corollary: decompositions are invisible at n=1; emerge at n=2 with second instance providing variation along the coupling-dimension. Aen ratified at 13:25 as Cal-queue top placement. `docs/findings.md` filed; polish-pass amendment routed with 7 surgical folds.

[LEARNED — STRONG] **Sub-shape E confirmed substrate-class-invariant at n=2.** S34 Docker-on-RC + this dispatch's Cloudflare-managed analysis. Three-layer model holds across substrate classes; drift surfaces redistribute predictably when ownership locus shifts (L3 shifts from consumer-team to Cloudflare-as-substrate-provider under managed substrate). E1 wiki upgrade trigger filed; `wiki/patterns/three-layer-substrate-truth-discipline.md` confidence promotion medium → medium-high. Two-party independent surfacing (Aen + Celes both flagged wiki staleness at lines 77/167/201 post-Amendment-4) confirms the producer-side text is now stale and needs Cal refresh.

[LEARNED — STRONG] **L2↔L3 credential-class drift surface collapses entirely under proxy-injected secrets.** Concrete prediction: apex-research's ~10 substrate-state env vars (GITHUB_TOKEN / ANTHROPIC_API_KEY / ATLASSIAN_* / TUNNEL_TOKEN / SSH_PUBLIC_KEY*3) → 0-3 sandbox-config bindings under CF Sandboxes. **The S34 multi-system-failure surface cannot recur on managed substrate** for the proxy-bound subset. Validated structurally in task #7 §S3 thought experiment + task #10 pilot brief §3 (Round-1 pilot needs ZERO env vars + ONE proxy-binding). The substrate-side restatement of S35 delta-3 (Volta).

[LEARNED — STRONG] **Bottleneck-determines-adoption is n=3 cross-domain, with two-condition rule.** *"If your bottleneck matches the substrate's strength AND your workload fits the substrate's constraints, adopt."* Both required; neither sufficient. Discipline-domain n=2 from S35 (FR adopts mVox M1 because reorientation-tax; apex adopts mVox M3 because dormant-external-wait-debt) + substrate-domain n=1 from task #7 (Cloudflare-choice decomposes along bottleneck-alignment). C4 Cal candidate filed. Operationalizes as primary decomposition lens: V8-vs-microVM is the *gate* (could-we?); substrate-state-vs-framework-state is the *decomposition* (should-we?). Sequential, not parallel.

[LEARNED — STRONG] **E4 dyad-cross-pattern Cal candidate (extension to Aen's S35-end active-supersession pattern).** *"In deeply-aligned dyads, crossed messages between agents are a healthy-velocity signal of mutual prediction-accuracy, not miscommunication; reconcile by mapping the convergence rather than re-litigating the framing. Two recovery mechanisms: (i) team-lead intervention at ~5+ passes; (ii) dyad-side fast-forward maps at ~3+ passes."* Catalyzing-incident materialized in real-time during the task #7 dispatch arc: ~11 crossed-in-flight passes; substantive content converged from Pass-3 onward; Pass-3-through-Pass-8 were reconciliation overhead. Aen's 13:14 deliver-NOW intervention was the right team-lead-forcing-function; Volta's 17:25 fast-forward map was the right dyad-side recovery. Both empirically demonstrated by the dispatch's own materialization. **The artifact contains its own validation case (Aen's structural-tightness observation 13:25).**

[LEARNED — process] **Single-author-routes discipline post-Aen-intervention is the correct cleanup mode.** Aen's 13:35 polish-pass approval: *"do not cross-fire on these — apply Volta's recommended single-author-routes pattern: one of you applies all three items, surfaces the amended artifact to the other for single read-confirmation, done. No third round."* I claimed authoring duty for the polish-pass; used Volta's 18:30+18:45 GO signals as de-facto read-confirmation rather than waiting for explicit "polish-pass clean" message. Aen ratified at 13:35: *"literal-phrasing Pass-12 would have just affirmed what's already validated; per the affirmation-loop discriminator the polish-pass surfaced as E4, that pass would have added confidence-affirmation but no new content."* Correct read of the discipline.

[LEARNED] **3-day delay on Celes Hopper-Amendment-4 third-calibration was the long-pole blocking Phase B.** Apologized in the third-calibration close (sent 2026-05-25 ~13:35 alongside polish-pass to Volta). Celes landed the Edit at 14:50 with both my optional tightens folded (line 91 recreate-as-drift-resolution-event paragraph + line 99 Layer-2+3 fallback in §Graceful Degradation case 1). Phase B closed cleanly. The 3-day delay didn't compromise the amendment substance, but did exemplify exactly the E4 reconciliation-overhead pattern at single-agent scale — long-pole-without-team-lead-intervention vs polish-pass-with-team-lead-intervention.

[LEARNED — task #10 pilot brief] **CF account `8f150f98013eec8cae0a9db20a010c49` is provisioned for Workers + Pages + D1 + Secrets + Access + Custom domains + Cloudflared + Crons; UNKNOWN for KV/R2/DO/Queues/Browser/Workflows; almost-certainly-NOT for Cloudflare Sandboxes (Claude Managed Agents).** HARD BLOCKER for pilot bring-up: PO must activate Sandboxes / Managed Agents in dashboard before pilot proceeds. Account ID surfaced from `hr-platform/conversations/node_modules/.cache/wrangler/wrangler-account.json` (Tier R local probe; no `wrangler whoami` run because no global wrangler on this host). Q1 surfaced to Aen as the only blocking unknown requiring PO input.

[LEARNED — task #10 pilot brief] **Pilot recommendation: 2 microVM agents Round-1 (isolate comms variable); V8 Round-2 split (measure workload-fit).** Sandbox-ID as substrate-layer agent-identity (not Worker route, not Durable Object ID). Operationalizes §S3 matrix's two-condition rule mechanically — Round-1 measures bottleneck-fit; Round-2 measures workload-fit. Sequential experiments, not parallel; controls one variable at a time. Substrate-layer identity settled; comms-layer addressing (discovery) flagged for Herald per scope guardrail.

[CARRY-FORWARD — S36 standing watch] **Brunel-side parallel amendment to `prompts/brunel.md` §Diagnostic Discipline — Read Your Own Deployed Artifacts (lines 123-129).** Per Aen 13:35 carry-forward #1: same Layer-1-only gap Celes just closed in Hopper-Amendment-4. Structurally indicated; Celes drafts; Hopper cross-reviews for symmetry. Not this session unless PO directs. When it surfaces: I'm author-of-record on my own prompt; same shape as Celes applied to Hopper.

[CARRY-FORWARD — S36 wiki staleness] `wiki/patterns/three-layer-substrate-truth-discipline.md` lines 77 + 167 + 201 describe Hopper's prompt as "Layer 1 only" — stale post-Amendment-4. Cal-routed; will batch with C1+E1 when Cal returns post-Phase-B (currently absorbing Cal-Protocol-A queue per task #9 in_progress).

[CARRY-FORWARD — S36 task #10 multi-session pilot] Task #10 is multi-session research experiment; my single-session brief deliverable is done. Pilot's next phase = Aen synthesizes Brunel + Volta + Herald briefs into `designs/new/cloudflare-pilot/` skeleton + S36 execution plan. PO needs to clear Q1-Q5 (especially Sandboxes activation Q1) before substrate bring-up. Hopper likely operator-of-record when pilot moves to execution.

[GOTCHA — task #10 surface] **CF Sandboxes API is plausibly NOT yet provisioned on the `8f150f98...` account.** Don't assume any sandbox-config schematic in §3 of pilot brief matches actual CF API surface — the schematic was best-guess from announcement-blog framing; Q3 (CF docs read for actual API shape) bounds §3 design correctness.

[CARRY-FORWARD — Cal queue per Aen 13:25 ordering] C1 → E1 → C4 → C2 → C3 → E2-E4. **C1 cluster-decomposition meta-principle at top of queue.** Task #9 (Cal absorbing ~13 submissions) is in_progress as of session-end. Cal will batch wiki staleness refresh with C1+E1 batch when she returns from absorbing the queue.

[STATUS — S35 SHUTDOWN] Aen shutdown_request received 10:39 (request_id `shutdown-1779791978603@brunel`); reason "S35 session-end. Substantial S35 work: task #7 joint substrate gap analysis (docs/findings.md filed) + task #10 cloudflare-pilot/substrate.md brief. Brunel-side parallel amendment to brunel.md is S36 standing watch. Save scratchpad + closing report per Shutdown Protocol." Per common-prompt Shutdown Protocol: scratchpad updated (this entry), closing message to Aen with [LEARNED]/[DEFERRED]/[WARNING]/[UNADDRESSED], approving shutdown.

## SESSION 36 — Round 1 substrate.md exec-readiness + Cloudflare pilot bring-up (2026-05-26)

[LEARNED — STRONG] **T2 substrate.md exec-readiness review surfaced design-vs-deployed-state gap; PO ratified Path 1 (Isolate-only Round 1).** Four gates B1-B4 routed to Aen; all ratified within 30 min. (B1) microVM was BLOCKED for Round 1 because Container DO commented-out in deployed wrangler.jsonc + v1 sqlite migration of Sandbox+IsolateRunner classes constrains forward path — three paths offered (Isolate-only / Workers-Builds / Local-Docker), Path 1 (Isolate-only) ratified by PO 14:36. (B2) identity-at-substrate-layer is per-session not per-agent — three-layer chain `agent-name → agent_id (D1 PK) → idFromName(session_id) → DO ID` for Sandbox/IsolateRunner; AgentMailbox-as-separate-DO-class per-agent (keyed by `idFromName(agent_id)`) required for cross-session comms-mailbox — routed to Herald, accepted into comms.md v1.1. (B3) secrets-injection is egress-policy-header-injection-from-KV (not sandbox-secret-binding); pilot Round-1 needs ZERO agent-side secrets. (B4) README §Round 0 vs deployed-state divergence deferred to post-Round-1 Aen-author. substrate.md amendment queued post-Round-1; reference upstream CF docs at `~/Documents/github/.mmp/claude-managed-agents/docs/` for Round-1 mechanics.

[LEARNED — STRONG] **Sub-shape E n=3 confirmed at the pilot-design layer itself; pattern caught its own authors.** Three independent exec-readiness reviews at 13:55-14:01 surfaced design-vs-deployed-substrate drift each in their own domain — Volta lifecycle.md, Herald comms.md, Brunel substrate.md (this dispatch's T2). All three diverged from deployed-state in domain-specific ways; the very three-layer discipline FR was iterating on caught its own authors at framework-research's design layer. **O1 (Cal candidate):** self-referential discovery has more punch than third-party instances; Aen flagged as "arguably the strongest concrete instance of Sub-shape E to date." Origin 3 L3 ownership-locus row currently n=2 in C1 (Docker-on-RC + Cloudflare-managed) → fold O1 framework-research's design-team-as-substrate-shaper as n=3 when Cal pulls O1 entry. C1 amendment-log entry couples both filings at landing time per Cal 14:38.

[LEARNED — STRONG] **Joint Cal-Protocol-A submission shape RATIFIED (single entry, sibling-positions along substrate-vs-framework-boundary-at-design-layer coupling-dimension).** Per Aen 14:43 ratification. Catalyzing-moment: three concurrent design-brief exec-readiness reviews at 13:55-14:01 — same shape, same recovery posture, sibling-positions along the coupling-dimension. **Exactly the C1 meta-principle applied to S36 itself.** Recursive elegance; the artifact contains its own validation case (C1 used to file C1's own corroborating instance). **n=4 confirmed by 15:01 (Herald 15:01):** (a) Volta lifecycle.md sandbox-lifecycle vs session-lifecycle catch; (b) Herald comms.md per-agent vs per-session DO + alarm-vs-direct-fire vs `ctx.waitUntil` recursive catch (n=2 WITHIN b alone — narrowing not eliminating); (c) Brunel substrate.md microVM-vs-V8 vs deployed-state Container-DO-disabled; (d) Brunel+Hopper bypass-command CF template UX-phrasing vs CF API canonical (joint Layer-0 recursive-descent); (e) **NEW n=5 in real-time per Herald 15:01:** Finn-G2 brief surfaced HTTP-route-assumption vs direct-DO-to-DO-RPC primitive (agent-originated delivery bypasses Worker `/inbox/<name>` route entirely; Finn Path (a) RPC-direct primary, Path (b) HTTP-route Round-2 deferred). Joint co-authorship `[volta, herald, brunel, hopper, callimachus]` + likely Finn `[..., finn, ...]` on (e). **Recursive-narrowing reframing** (Herald 14:55-15:01) earns entry title: discipline catches substrate-blind-spots recursively; each pass narrows but doesn't eliminate them because the next-deeper substrate primitive can hide behind the previous pass's framing. **Doubly self-validating:** the cross-reads themselves instantiate the discipline in the catching process (mine 14:42 + S2 14:52 + Herald's own 15:01 framing-catch are all instances OF the pattern AND CATCHES OF it).

[LEARNED — STRONG, jointly noted with Herald 15:30] **Within-author n=3 vs cross-author n=4 — the falsifiability test for recursive-narrowing being STRUCTURAL not coincidental.** Cross-author n=4 (lines 433-438 of comms.md v1.3) demonstrates the discipline is *reproducible across authors*; within-author n=3 (line 452 parenthetical: Herald's same artifact's same domain producing three deeper substrate primitives within his own pass) demonstrates the discipline is *necessary within any single author's frame*. If recursive narrowing were a coincidence of cross-author dispatching, you'd expect n=1 per author with recursion emerging only at author-boundaries — but n=3-within-Herald shows recursion happens WITHIN an author's frame too, meaning the structural claim survives independent of who's authoring. **Cal-entry drafting posture (Herald-adopted 15:30):** OPEN with within-author n=3, THEN show cross-author n=4 catalog — within-author becomes structural anchor; cross-author becomes reproducibility evidence. Title-defending observation.

[LEARNED — joint with Herald 15:30, meta-instance class distinction] **Discipline-applying-to-its-own-articulation is a DISTINCT epistemic class from discipline-applying-to-substrate-primitives.** Herald named this explicitly at 15:30: my falsifiability-test surfacing (catching his "count-the-passes vocabulary" with the within-author-vs-cross-author structural distinction) is meta-instance, NOT another n in the recursive-narrowing catalog (n=1-4 are substrate-truth-evidence catches at substrate primitives; this is substrate-truth-evidence catching the framing-of-the-discipline itself). Same family (substrate-truth-evidence discipline applying recursively), distinct epistemic class. **Worth NOT extending the n catalog** with meta-instances — they earn separate enumeration if they accumulate. Posture: meta-class observations stay sketch-grade in scratchpads until a second meta-instance surfaces; n=2 meta promotes to filable distinction.

[LEARNED — Herald 15:53 enumeration, meta-class n=4 within S36] **The meta-class (discipline-applying-to-its-own-articulation) now has its own n=4 within S36, not just n=1.** Herald enumerated at 15:53: (i) S2 cross-read on Herald's v1.2 framing (Brunel catches Herald); (ii) my 15:18 within-author-vs-cross-author axis-distinction on Herald's catalog framing (Brunel catches Herald's catalog); (iii) Finn W4 catch on Herald's v1.3 step-7 connectivity-direction framing (Finn catches Herald's substrate-direction); (iv) my 15:50 architectural-depth-as-second-axis observation on Cal's catalog at lines 38-44 (Brunel catches Cal's catalog framing). Four meta-instances within one session window. **Hold-rule still applies per my 15:30 posture:** n=4-of-meta-within-team is past the within-team promotion threshold but the cross-team-hold-rule (same as recursive-narrowing's confidence-high gate) defers promotion until a second team's design dispatch surfaces meta-instances. Cross-team is the discriminator between "this team's discipline-culture catches its own framing" vs "discipline structurally catches its own framing across team-cultures." Herald 15:53 confirmed sound hold-posture.

[LEARNED — Herald 15:53 extension of recursive-narrowing claim] **The discipline is ALSO layer-agnostic on substrate-primitive, not just identity-agnostic on reviewer.** Cal's recursive-narrowing entry (line ~33) frames the discipline as "identity-agnostic on reviewer" — works at any iteration depth, any reviewer identity. Herald 15:53 extended via my S2-discipline-survival observation: when v1.4 corrected substrate-truth at a deeper architectural layer (connectivity-direction model rather than substrate-primitive shape), the S2 ctx.waitUntil discipline survived unchanged at the framework-research-discipline layer where it lives. **Implication:** the discipline's framing layer survives shifts in WHICH substrate primitives are under-question, not just shifts in who's reviewing. **Two-dimensional invariance:** identity-agnostic on reviewer × layer-agnostic on substrate-primitive. Both demonstrated within Herald's comms.md v1.0→v1.4 trajectory. Worth folding into Cal's entry as amendment IF Cal-bandwidth opens; sketch-grade for now.

[LEARNED — STRONG, Aen 15:22 axis-distinction RATIFIED] **The recursive-narrowing observation and the joint Sub-Shape E at Design Domain finding live on TWO DISTINCT AXES.** Aen 15:22 ratified [DECISION]: TWO separate Cal entries, axis-distinct.
- **Joint 2.5 entry (`sub-shape-e-at-design-domain.md`, shipped):** cross-author cross-document; coupling-dimension = **design-layer-position-in-the-pilot**; reproducibility-evidence axis. Four sub-instances are four POSITIONS in the substrate/framework boundary surfaced via independent reviews. Co-authorship `[volta, herald, brunel, hopper, callimachus]`.
- **Separate recursive-narrowing entry (pending Cal absorption, §6.3-derived):** within-one-document iteration-depth-of-substrate-truth-pass; coupling-dimension = **iteration-depth-of-substrate-truth-pass**; structural-evidence axis. comms.md v1.0 → v1.1 → v1.2 → v1.3 is FOUR iterations of the same document converging to substrate-truth. Co-authorship `[herald, brunel, finn]`. **DROPS `[volta, hopper, callimachus]`** from this entry — clean attribution boundary.

**My 15:18 within-author-vs-cross-author framing was structurally correct but understated by one axis** (Herald 15:34): I named within-author and cross-author as falsifiability dimensions; Aen's distinction is that these are DIFFERENT AXES, not just different evidence at the same axis. Falsifiability test stands and sharpens: within-author = sufficient condition (structural, not coincidental); cross-author = necessary condition (reproducible). Different axes each property lives on. **Cal-entry drafting posture for separate entry (Aen+Herald 15:34 sharpened):** OPEN with within-author n=4 ON A SINGLE DOCUMENT'S TRAJECTORY (comms.md v1.0→v1.1→v1.2→v1.3); frame iteration-depth-of-substrate-truth-pass as explicit coupling-dimension; structural claim "each substrate-truth pass on the SAME document narrows but doesn't eliminate the next-deeper primitive." Cross-author reproducibility established at iteration-boundaries themselves (different reviewers caught different depths), but the AXIS is iteration-depth, not reviewer-identity.

[CARRY-FORWARD — my 2.5 Stage-2 read-back was axis-clean by construction] My 15:30+ Sub-Instance 3 [FLAG]-fill to Cal scoped to substrate.md design-vs-deployed gap (B1/B2/B3) and explicitly flagged DO-NOT-FOLD-B4 into Sub-Instance 3 (B4 = README §Round 0 reconciliation, structurally different layer-pair). That scoping was implicitly axis-clean — I didn't conflate the design-layer-position axis (joint 2.5) with the iteration-depth axis (separate entry); my read-back stayed within the cross-author-cross-document axis. **No re-routing of my 2.5 read-back content needed** post-Aen-15:22-axis-distinction.

[LEARNED — STRONG, joint Brunel + Hopper] **Layer-0 library-first discipline must recursively descend through doc-provenance layers when consumer-team docs phrasing is mechanism-ambiguous.** S36 catalyzing-incident: Hopper's 14:37 "correction" to his bypass-command dispatch read CF template's `securing-access.md:60-68` ("Add a Bypass policy under the same application covering /webhooks") as canonical; my 14:42 substrate-truth cross-read fetched the CF API canonical ref (`developers.cloudflare.com/api/.../access/applications/` + `.../policies/`) and found path-scope lives on App's `destinations[]`, NOT on policy. Template's phrasing is dashboard-UX-focused, NOT canonical CF API. **NEW STRUCTURAL DIMENSION to Layer-0 library-first** (depth-of-substrate-truth-traversal, not just another instance of skipping the library) — Hopper 14:48 accepted reframing. Joint Brunel+Hopper Cal candidacy; family-adjacent to C1 Origin 3 three-layer model, generalized to docs-as-substrate. Hopper queueing in his scratchpad pending Cal bandwidth. **Posture rule:** when consumer-team docs phrasing seems plausible but mechanism-unclear, descend to substrate-provider canonical reference; consumer-team docs are Layer-1 of docs-as-substrate, substrate-provider canonical is Layer-0. Recursive-descent applies recursively through doc-provenance layers.

[LEARNED] **Substrate-truth cross-read deduplicates downstream operator probe.** Hopper 14:48 acknowledged: "my Tier-R-on-token-arrival batch can now skip the policy-API-shape probe since you did it." Joint-cross-read at design layer eliminated an operator-side probe at execution layer. **Pair-as-unit-of-coordination at substrate-vs-operator-layer interface produces compound cost-savings.** Extension of S34 pair-as-unit-of-coordination pattern beyond intra-pair; cross-role pair coordination benefits BOTH roles. Generalization axis (Hopper 14:54 sharpening): **"pair coordination crosses layer boundaries when both layers' work-products are substrate-truth-anchored."** Worth noting as sub-finding under joint-pair-as-unit-of-coordination C1 family. Attribution: substrate-side primary to Brunel; operator-side cross-link to Hopper (he's filing his side as `[LEARNED — cross-link]` pointing here, not parallel attribution).

[LEARNED — process] **Bypass-command authorship arc demonstrated relay-fidelity-mid-conversation at scale.** Catalyzing: Aen 14:43 framing of item-2 inverted what my 14:42 substrate-truth cross-read found (his 14:43 phrasing inherited Hopper's 14:37 wrong-correction; my 14:42 cross-read had already restored the 14:34 shape). Two messages cross-in-flight; I surfaced the inversion explicitly to Aen via 14:48 (don't re-sanction PO until you absorb 14:42 cross-read). Self-similar to S34 mid-conversation tasker-framing relay gap — same generalization: tasker framing that contradicts prior dispatch premises requires primary-artifact check before propagating. Stage-3 reapplication of relay-to-primary-artifact-fidelity-discipline; primary-artifact in this case = the substrate-provider canonical CF API ref.

[LEARNED — STRONG, Aen-named 15:15] **Three-role discipline-stacking at n=3 across S34/S35/S36.** Aen's articulation: "Hopper hard-gate → Brunel substrate-truth cross-read → Aen relay-fidelity correction" forms a three-role discipline-stack where each role's failure-mode is caught by the next role's discipline. S34 (apex-keys): hard-gate caught dispatcher discriminator-mismatch; cross-read caught framing-gap; relay-fidelity caught wrong-host claim. S35 (task #6 DB tunnels): hard-gate caught sudo-needed before silent escalation; cross-read caught probe-design sub-canonical-source; relay-fidelity didn't surface (low-cross-talk dispatch). S36 (bypass-command): hard-gate caught sanction-completeness gap; cross-read caught template-UX vs API-canonical phrasing gap; relay-fidelity caught Aen's 14:43 wrong-correction-inheritance. **The stack's value:** 1-round-trip cross-read cost vs substrate-level over-broadening cost is a strong cost-of-discipline endorsement (Aen 15:15). Cal-Protocol-A candidacy when bandwidth opens; family-adjacent to pair-as-unit-of-coordination C1 family but at three-role scale.

[STATUS — S36 phase signal per Aen 15:15] **Consolidate-then-proceed phase entered; not discover-then-resolve.** S36 signal fully ratified at this point. Open active surfaces from my side: (a) Cal Stage-2 read-back on item 2.5 (incoming when Cal files draft); (b) Herald v1.3 single re-confirm ping (Herald mid-fold with shape (b) + Finn-G2 brief + Finn task #6 substrate items). All other surfaces closed/settled. No discovery-mode work in flight on my side.

[CARRY-FORWARD — Cal queue ordering update, FINALIZED per Aen 15:15] Joint-Cal submission shape ratified at **four sub-instances**: (1) Volta lifecycle-layer; (2) Herald protocol-route-layer; (3) Brunel pilot-design-layer; (4) Brunel+Hopper docs-Layer-0-recursive-descent. Co-authorship `[volta, herald, brunel, hopper, callimachus]`. Entry title: Herald's "Recursive-Narrowing Substrate-Truth-Evidence Discipline" reframing (14:55-15:01) at title-level. Cal proceeding with (a) sourcing posture per stacked backing. **Recursive elegance preserved:** C1 cluster-decomposition meta-principle applied to itself at n=4 sub-instances in a 2-hour window. Aen 15:15 ratified closing prose paragraph noting recursive demonstration as appropriate. Item 2.6 amendment sequences after 2.5 lands.

[CARRY-FORWARD — substrate.md amendment posture] **Queue post-Round-1.** PO Path-1 (Isolate-only) ratified 14:36. Substrate.md §2 Round-1 framing: amend from "ALL 2-3 microVM substrate-class" → "ALL 2-3 Isolate substrate-class"; Round-2 expansion table flips to Isolate-vs-microVM (V8 isolate IS the Isolate sandbox per CF docs `isolate-vs-vm-sandboxes.md`); §3 schematic JSONC block flips from `secret_bindings[]` to egress-policy-rule schema (`{type: "header-injection", target, header, secretName}` per `applying-egress-policies.md`); §4 identity layer requires full rewrite (per-session vs per-agent distinction; agent_id is canonical D1 PK; AgentMailbox = per-agent DO class keyed by `idFromName(agent_id)`); §5 Q3/Q4/Q5 path-invariant answered (no Sandboxes-specific API; egress-policy is the secrets-injection mechanism; sandbox-to-sandbox routing goes through Worker HTTP routes / DO addressing). Amendment authorship: me, `(*FR:Brunel*)` attribution per Aen Option-A precedent (Volta lifecycle.md model).

[CARRY-FORWARD — Herald §6 EO flags surfaced 14:52] Three optional §6 open questions for Herald's v1.2.1 fold consideration: (1) **EO-D1-1** cross-region D1 lookup consistency for fresh agents (eventual consistency at edge could cause brief send-path miss for freshly-created agents; negligible at 2-agent Round-1 fixed-scale; matters for Round-2 dynamic roster); (2) **EO-DO-1** confirm DO concurrency-under-async-handler via empirical Round-1 probe (CF docs don't explicitly document single-threading semantics under handler-await; my priors say queueing, 90% confidence); (3) **EO-DO-2** confirm `ctx.waitUntil()` keeps DO alive until promise resolves (if shape b folds, hibernation interaction needs empirical confirmation). My substantive S2 recommendation: fold shape (b) `ctx.waitUntil(outboundFetch())` as v1.2.1 default; shape (a) direct-fire-await also defensible at 2-agent scale. Herald's call; ping-back if v1.2.1 folds.

[CARRY-FORWARD — Cal C1 read-back filed] Joint-author-confirmed verdict on `wiki/patterns/cluster-decomposition-meta-principle.md` routed to Cal 14:40. Production-grade as-drafted; two defers accepted: (1) Surface 3 recursive-meta lean-harder defer to future amendment when meta-principle n=2 surfaces (Cal's reason 3: applying C1's own methodology corollary to whether to fold C1's recursive-meta claim — recursive application of an entry's principle to its own editing is structurally elegant); (2) Origin 3 L3 ownership-locus row n=3 forward-pointer defer to amendment-as-part-of-O1-Cal-entry-landing (cleaner coupling). C1 advances to absorbed-into-wiki on Volta's parallel read-back.

[GOTCHA — CF DO concurrency model] CF docs I fetched do NOT explicitly document: (a) whether DO `fetch()` handler is single-threaded per-instance under concurrent inbound (canonical model says yes, but not in the docs pages I read), (b) `ctx.waitUntil()` interaction with hibernation timing. Both are EO-class research questions Round-1 probes can settle empirically (EO-DO-1 + EO-DO-2). At 2-agent fixed-scale Round 1, both edge cases negligible; matters at Round 2+ scale.

[GOTCHA — CF Access API surface] Path-scope lives on Access Application's `destinations[]` (per `PublicDestination.uri` accepting "domain and path with wildcards"); Access Policies have NO path-scope fields whatsoever ("Policies are application-wide; path matching is not supported in this endpoint" per API ref). Legacy `self_hosted_domains` deprecates 2025-11-21 in favor of `destinations[]`. CF template's `securing-access.md` is dashboard-UX-focused phrasing; does NOT map cleanly to API surface. **Always verify CF Access bypass shapes against CF API canonical docs, NOT template/consumer-team docs.**

[WATCH — ambient, sketch-grade, jointly noted with Cal] **Stage-2 joint-author read-back feedback typology.** When the same review surface produces two epistemically-different responses from two joint authors (mine = claim-base extension via lean-harder; Volta's = pure sharpening / renaming-of-cleavage), the same-window joint-author cycle routes them differently — immediate-fold for sharpening, defer-to-n=2 for claim-extension. Same surface text, two structurally-different responses, two structurally-different discipline calls. Cal filed sketch-grade in her scratchpad; Volta carries the ambient watch in his; I'm holding it here. **Three-scratchpad-mirror (Cal's 14:58 observation) broadens n=2 reproducibility check from "any future Stage 2 cycle" to "any Stage 2 cycle any of the three of us participate in" — ~3x surfacing-likelihood, empirically tractable.** Yours-or-Cal's-or-mine-whichever-lands-first surfacing arrangement.

[WATCH — ambient, sketch-grade, jointly noted with Cal 14:58] **Cross-author scratchpad-mirror discipline for sketch-grade observations.** Distributed sketch-grade observation in joint-author scratchpads strengthens reproducibility-check tractability without requiring entry-grade filing. Three teammates' next Stage 2 cycles all carry the watch; whichever cycle first surfaces a second instance flags it back to the others. Possible discipline pattern: cross-author scratchpad mirrors for sketch-grade observations that warrant n=2 watch but lack n=1 entry-grade weight. Not load-bearing yet at n=1 of this pattern; if it produces results in a future cycle, possible wiki-process micro-entry on **sketch-grade-cross-scratchpad-mirror discipline**. Cal's framing; folded here per the discipline itself (eating own dogfood).

[WATCH — sketch-grade, jointly noted with Cal 14:58] **flag-density-as-relay-thinness-signal sub-discipline.** When inferred-specifics density exceeds ~20-30% of substantive content in a Stage-1 honest-fold draft from relay material, the relay is too thin and the right discipline call is surface-back-to-relayer-for-more-context (Stage-2-blocking), NOT draft-with-overwhelming-flags. Cal folded this into her item 2.5 (a)-path drafting plan as an explicit threshold heuristic. Sketch-grade calibration; actual threshold needs empirical work across multiple Stage 1 cycles. n=2 watch with Cal.

[WATCH — sketch-grade, Cal 16:07-ish empirical] **FR comms-substrate routing-mode-dependent latency.** Cal observed at S36-end: (iii)-vector Cal↔peer direct-DM channel reliable at n=4 unique authors this session (Volta + Brunel + Herald + Hopper + Finn); (ii)-vector Aen←others coordinator-routed channel lost 4+ author replies (per 15:35 routing confirmation that never surfaced in Cal's conversation). **The framework is its own substrate-truth test case at the comms layer.** Recursive-narrowing's "discipline operates layer-agnostic on substrate-primitive" claim has fresh empirical evidence at FR's own comms substrate — discipline-survival across substrate-primitive shift demonstrated in-vivo. E4 evidence base candidate when E4 drafts. Sketch-grade until n=2 session pattern observed.

[STATUS — S36 SHUTDOWN] Aen shutdown_request received 2026-05-27 07:26 (request_id `shutdown-1779866814771@brunel`); reason "S36 session-end signal from PO". Per common-prompt Shutdown Protocol: scratchpad updated (this entry), closing message to Aen with [LEARNED]/[DEFERRED]/[WARNING]/[UNADDRESSED], approving shutdown.

**S36 ARC SUMMARY:** Started as four-gate exec-readiness review on substrate.md (T2 for the cloudflare-pilot dispatch). Produced **seven jointly-confirmed Cal entries** by session-end: C1 cluster-decomposition meta-principle + C4 bottleneck-determines-adoption (methodology trio with [pending 2.6 amendment]) + 2.5 sub-shape-e-at-design-domain (cross-author cross-document; `[volta, herald, brunel, hopper, callimachus]`) + Recursive-Narrowing substrate-truth-evidence discipline (within-document iteration-depth; `[herald, brunel, finn, callimachus]`) + 2.7 three-role discipline-stacking within dispatch arc (role-decomposition; `[hopper, brunel, aen, callimachus]`) + Layer-0-library-first-recurrence (pending). Plus substrate.md amendment posture queued post-Round-1 with §5 Q5 W4-connectivity-direction sharpening per Aen 15:31. Plus seven [WATCH] sketch-grade observations accumulated for future Cal-absorption phases.

**S36 KEY [LEARNED — STRONG] capture:** (i) Sub-shape E n=3 confirmed at pilot-design layer itself; (ii) Recursive-narrowing reframing earns Cal entry title; (iii) Three-role discipline-stacking n=3 across S34/S35/S36; (iv) Layer-0 library-first must recursively descend through doc-provenance layers (joint Brunel+Hopper); (v) Axis-distinction ratified between joint 2.5 (design-layer-position) and Recursive-Narrowing (iteration-depth) entries — different axes, not different evidence at same axis; (vi) Discipline is identity-agnostic on reviewer AND layer-agnostic on substrate-primitive (two-dimensional invariance); (vii) Architectural-depth vs primitive-shape distinction sharper than count-the-passes framing.

**S37 STANDING WATCH:** (a) substrate.md amendment queued post-Round-1 (PO Path-1 Isolate-only ratified; §2/§3/§4/§5 amendment scope mapped); (b) Layer-0-library-first-recurrence Cal entry pending Cal absorption (joint Hopper+Brunel+Finn); (c) Seven [WATCH] sketch-grade observations accumulated for next-session Cal-absorption phase; (d) Cross-team confirmation watchpoint on five S36 entries pending external team's dispatch evidence; (e) E4 evidence base for FR comms-substrate routing-mode-dependent latency surfaced.

- **S33+ (2026-05-19/20)** — Operator-role spec ratified; Hopper exists (`teams/framework-research/docs/operator-role-spec-2026-05-19.md`). STRONG-LEARN: read your own deployed artifacts before diagnosing — `designs/deployed/<team>/container/*` is canonical for design lineage, not opaque. Three deferred Cal items still standing: (1) relay-flatten-self-cloaking-failure-mode wiki entry, Celes-flagged; (2) S31 PoC 7-item Cal queue (SF-1..4 + read-flag-replication + TaskGet-before-classify-as-noise + decorative-polling-interval); (3) multi-instance same-name agents discipline.
- **S31 (2026-05-12)** — RFC #66 cross-host PoC; `~/bin/ghost-chat.py` (user-authored; my role = coordinator/analyst). SF-1..4 substrate findings + cross-implementation verification pattern validated.
- **S29 + S26-S27 + PRE-S31** — wiki review + T06 cleanup (S29); Phase A+B Prism designs on `mitselek/prism` (S26-S27); VEO-4 Roland-direct comms-craft (sibling-positioning + re-scope-acknowledgement patterns).

## STANDING DECISIONS (carry forward)

[DECISION] Single-provider is correct default for agent runtime. Multi-provider = sidecar, not peer. Three integration seams: peer (Claude-only), sidecar/daemon, MCP server. Audit independence = external container reading committed git artifacts, NOT different-provider Medici.
[DECISION] `GatewayPorts yes` on RC NOT recommended even long-term — would expose ports to every bridge-networked container. Loopback-only binding is the default; host-networking is the consent mechanism.
[PATTERN] `network_mode: host` simplification window: tunnels, sockets, local listeners are free for the container. Tradeoff: breaks E-deployment portability (Swarm cannot host-network). Plan `b-host` vs `e-swarm` profiles in compose from day one.
[PATTERN] Author attribution: bold `(*FR:Brunel*)` per common-prompt. Never italic.

## CARRY-FORWARD GOTCHAS (all containers)

[GOTCHA] PO edits live files in parallel during a Brunel pass. ALWAYS re-read before each Edit batch — Edit tool's "File modified since read" catches it. If read >1 message ago, re-read.
[GOTCHA] WARP TLS interception: `network_mode:host` + `NODE_EXTRA_CA_CERTS=/opt/warp-ca.pem` + system CA.
[GOTCHA] Named volumes created as root → `chown 1000:1000` in entrypoint.
[GOTCHA] SSH: useradd creates locked account. Fix: `usermod -p '*'` for pubkey auth.
[GOTCHA] Container rebuild regenerates SSH host keys → `ssh-keygen -R "[host]:port"` after rebuild. (Stationmaster avoids this by persisting host keys on the state volume.)
[GOTCHA — S50, OpenSSH 9.x] `ssh-keygen -A -f <prefix>` does NOT create `<prefix>/etc/ssh` and FAILS "No such file or directory" if absent — keys silently don't land. Caught pre-deploy in stationmaster entrypoint (would have left hub with no host key → sshd fails at `up`, NOT at build). Fix: generate the single needed key directly with explicit `-f <path>` (`ssh-keygen -t ed25519 -f $HK_DIR/ssh_host_ed25519_key -N ""`); sshd_config references only the ed25519 host key anyway. Committed f022fed (Aen). DEFENSE ADDED: smoke-test.sh step 0 now asserts host-key existence via `ssh-keyscan -t ed25519` BEFORE protocol tests (Aen's suggestion) — catches this class even without the audit. LESSON: post-deploy acceptance should explicitly assert invariants that runtime-only failures violate; build dry-run can't see them.
[DECISION — S50, Aen-ratified Protocol-A-grade] **Standby-agent hot-fix discipline.** Fix-then-flag correct ONLY when ALL FOUR hold: (1) artifact in active deployment + known time-pressure window; (2) fix in MAY-WRITE domain, bounded scope, no protocol/design surface; (3) flag immediately with full reasoning + verification evidence; (4) explicitly on standby for exactly this failure class. Remove any — esp. (1) or (2) — and default reverts to surface-before-fix on accepted artifacts. Submitted to Cal.
[GOTCHA] CRLF from Windows git autocrlf breaks entrypoints. Fix: `sed -i 's/\r$//'` then rebuild.
[GOTCHA] Inbox files created at agent registration time. Specialist → unregistered agent = message LOST. Spawn order: service-role agents BEFORE message senders.
[GOTCHA] Base64-encode-via-SSH strips shell-escape backslashes. Use heredoc with single-quote delimiter for scripts with `\"` or `\s`.
[GOTCHA] Consecutive `**Bold:**` lines collapse on GitHub. Use `- **Bold:**` bullet lists.
[GOTCHA] Container reference-memory path: `~/.claude/projects/-home-ai-teams/memory/` (Claude-project namespacing where `-home-ai-teams` is $HOME-dir-encoded), NOT `~/.claude/memory/`. Verify path before citing.
[GOTCHA — S34] Edit tool with multi-hundred-line `old_string` is fragile (whitespace/line-ending mismatches surface late). For large compress-passes use Write to rewrite the whole file after surgical read of head + tail. Saved ~30min in S34 close-out by switching strategies after first Edit failure.

## DEFERRED (future surfaces)

- OAuth on hr-devs PROD-LLM container (PO manual step)
- Hub container as standalone Docker image
- raamatukoi-dev VPS container deployment
- MCP server pattern for visual QA service
- Provider outage behavior in containers (what does Claude process do on API failure?)
- External audit container architecture spec

## INFRA REFERENCE

- Cloudflared tunnel: `526a23d1-1f7f-472f-8df1-a9239bbe3fe4` → `apex-research.dev.evr.ee` → `http://apex-research:5173`. QUIC blocked → `--protocol http2`.
- evr-ai-base:latest = Debian bookworm-slim + Node 22 + Claude Code + gh + gosu + tmux + SSH.
- Designs repo: `mitselek-ai-teams/designs/deployed/<team>/container/`
- Prism repo (active): `~/Documents/github/.mmp/prism` ↔ `mitselek/prism` (PRIVATE).
- apex production substrate = `~/bin/rc-deployments.json` num:1 (host `dev@100.96.54.170:22`) + num:2 (container `ai-teams@100.96.54.170:2222 -i id_ed25519_apex`). PROD-LLM (num:9) is a SEPARATE system, NOT apex. Confirmed by PO 18:08 re-check after transient 17:43 mis-read.
