# Brunel scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S72 2026-09-03 CLOSED. **BOTH DELIVERABLES LANDED.** (1) **Amendment A1 is on disk in the live `paunvere` TEAM_ROOT** (Hopper ops-log `T14:34`, outcome success): five files byte-exact against my manifest, `644 joosep:joosep`, uncommitted by design, working tree showing exactly 4 modified + 1 untracked and **nothing else** -- `git diff --shortstat` = **`4 files changed, 14 insertions(+), 11 deletions(-)`**, split README 1/1, common-prompt 2/2, saxby 3/3, startup 8/5, **my prediction to the line on all four**. Staging removed, confirmed by positive test. (2) **PR #115 merged.**
- **[PROVENANCE, do not garble it] The apply was NOT executed by Hopper and NOT by me.** The permission classifier denied it in her session; **the PO changed the regime in the open and authorised team-lead to run my verbatim command, once, unchanged.** Neither of us supplied a narrower variant or re-shaped it. **A denial is about the ACTION, not the transport; routing it to another agent is the same evasion with an extra step.** Routing blocked work back to the human decision-maker is the sanctioned path and the only thing that clears a classifier denial.
- **[CORRECTION to my own record] PR #115 landed as SQUASH commit `2fde59b`, not `e1e6111`.** My branch commit is not an ancestor of main; the content is -- verified in main at `entrypoint.sh:398`. Worktree removed, branch gone.
- **[THE ONE THING NOT MEASURED -- Hopper flagged it and it is MINE, because my design rests on it]** Her apply script `stat`-ed inode/mode/owner **before** the writes to prove `cat >` preserves them -- **but that script is the one the classifier denied, so the before-reading never happened.** After-state is correct on all five; **inode preservation was never demonstrated by before-and-after comparison.** It follows from `cat >` truncating rather than replacing. Not a defect, a hole in the audit surface -- **re-measure it the next time a `cat >` transit runs.**
- **[FOUR INSTRUMENT FAULTS TODAY -- three mine, one hers, ONE signature: the instrument reported on ITSELF, not on the substrate. EVERY ONE FAILED CLOSED, turning a healthy substrate into a STOP that reads as diligence.]** (a) G1 gated on the socket FILE: **a unix socket outlives its server**, so "socket absent" could never go true; fix = DELETE my clause and ask the server (`tmux -S <sock> has-session` rc). (b) G2 `pgrep -f claude` **matched its own wrapper** -- `-f` reads full command lines and the command line carrying the pattern contains it; it then reported "a live claude with no session record", **my own STOP branch firing on a premise that was the wrong thing entirely**; fix = `pgrep -x` (matches process NAME, structurally cannot match a bash wrapper) + an independent `ps` sweep. (c) I pinned `git rev-parse HEAD` to a RECORDED VALUE and #115 merged mid-flight. (d) Hers: **a zero-match `grep -c` as the LAST command makes a wholly successful probe exit 1.**
- **[THE FIXES AT SOURCE, not this session's corrections]** **Never write a `HEAD` equality as an EXPECT** -- it goes in as an observation to record. **A stale PIN is not a stop; a stale MANIFEST is** -- proceed only when the pin is an ANCESTOR of HEAD, `git diff --stat <pin>..HEAD -- <manifest tree>` is EMPTY, and every POST md5 matches; rewritten history fails the first = hard stop. **Make every probe emit its own labelled value**, so a chain's exit status is never the thing anyone reads. **Ask whether the instrument is inside the set it is measuring.**
- **[HERS, STRONGER THAN MINE -- adopt]** `git diff --stat` over the **manifest tree** returning empty proves nothing was ADDED, REMOVED or RENAMED. **N matching hashes cannot: they say the files I NAMED are right, not that they are still the WHOLE SET.** Identity of members vs identity of the set. Also: **re-run gates at moment of use on resume -- a PASSED gate goes stale silently**; assert a file's ABSENCE explicitly; and **READ a new file before installing it into another team's root -- that is not a hash problem** (a correct md5 is equally true of a file that says something else).
- **[DESIGN RULINGS from this arc]** **A full redeploy does NOT deliver a package amendment**: Step 9c finds DST != SRC != STAMP and takes the NOTE branch, so hand transit is the only path once a team has worked. **Rejected forging the stamp to make the re-seed fire** -- silent-no-op failure mode, and the branch's premise is "whole tree untouched", so it would clobber prompts the team edited. **Faking a stamp to make a guard fire is defeating the guard.** **Anything we put in a team's root must exist upstream FIRST** -- a container file with no repo counterpart is reverse drift and the next rebuild silently reverts it. **Leave transits UNCOMMITTED in a team's own repo**: a dirty tree is loud where a commit is silent, and their shutdown-commit step absorbs it under their own authorship.
- **[LIFECYCLE STAMP]** Goes in `registry.json`, NOT `startup.md` (inside the package the team owns; drifts by design) and NOT `deployments.md` (measurably stale: no joosep, allerk or uikit-dev). **Load-bearing field is `teamPackageCommit`, not a CLI version -- what bit us was package-provenance drift.** Container half = `/opt/teams/.provenance` from a build ARG that `joosep.sh build` fills in so it cannot be forgotten. Celes's amendment-level `A1` at `startup.md:5` is the better instrument at the TEAM layer and composes with it.
- **[CLOCK SKEW]** This box's `date` read ~15 min BEHIND Aen's message stamps all session; my 14:15 dispatch answered a 14:26 message and my correction crossed Hopper's execution. **My timestamps cannot be used for ordering. The RC host clock is the authority for ops-log entries.** Unresolved -- check it next session.
- **CARRY-FORWARD:** (a) **joosep rebuild D1-D4 DEFERRED until a post-transit health read; RE-STAGE `entrypoint.sh` FIRST.** Measured drift is **L1 `4379dcf2` (pruned) vs L2 = L3 `cb8405a0` (defective), byte-identical to each other** -- ONE boundary, not two; one re-stage L1->L2, after which L2 and L3 diverge until a recreate lands the image. `/home/dev/joosep` is a STAGED COPY, so a pull never reaches it. (b) **Manifest-seed design (fix c) NOT sanctioned** -- the `.git` prune only helps in the provisioning window; `memory/` and `drafts/` live in the seeded dir, so **there is no update channel for a shipped package after a team's first session.** (c) `Dockerfile.apex` two failure-masking layers -> DELETE both, apex repo, own PR. (d) Base image still ships **Node 18**; every derived team image gets it. (e) **Entrypoint-owned `~/.claude.json` restore, absent-only guard** (adjudicated S71, not written). (f) `joosep_sshd` + `sm-state` have ZERO backups. (g) `git update-index --chmod=+x designs/deployed/joosep/joosep.sh` (24 `.sh` at 100644 vs 6 at 100755). (h) `[PO-9]` apex registry `tmux` drift; `[PO-17]` bridge `dns:`; `[PO-19]` allerk no pid limit. (i) S69 `stage-2: pending` read-back backlog -- count NOT verified by me, re-derive from Cal.

---
## Session transcript (prune beyond line 100)

### S72 -- paunvere amendment A1. Lessons are in the header; these are the substrate facts.

- **[GOTCHA -- line endings, and it took three tries to state correctly]** `core.autocrlf=true` here, so **a FRESH CHECKOUT gives CRLF** (the worktree's `entrypoint.sh` came out 27998 B against a 27479 B blob = 519 CRs) -- **but tool-written files (Write/Edit) stay LF and are never re-checked-out.** So "is this box CRLF?" has no single answer: **it depends on whether each file was CHECKED OUT or TOOL-WRITTEN.** **A fresh clone here would have shipped CRLF into the container.** Sourcing a transit from the target-side Linux checkout removes the question entirely. `file(1)` + `stat -c %s` are the authority; **`grep -c $'\r'` is NOT a valid CR detector under MSYS.**
- **[GOTCHA] MSYS `md5sum` prints `<hash> *<path>`; Linux prints `<hash>  <path>`.** This invalidated my first known-good test of the `dir_digest` fix -- the function's own `sed` never matched, full paths survived, and the FIXED function read as broken. **The prune was working the whole time; the TEST was invalid on this platform.** I reproduced the cause before touching anything. **Had I chased the red test into the code I would have shipped a defect to close a platform artifact.**
- **[SUBSTRATE] joosep is NOT apex, and my own artifact settles it** -- `entrypoint.sh:241-243`: *"the whole $HOME is a volume … unlike apex, where it sat outside the .claude volume and a recreate wiped it."* So `~/.local/bin/claude`, `.claude.json`, `gh`'s token and the JetBrains caches are all on `joosep_home`: **a recreate does NOT regress the CLI, and a rollback does not roll it back either.** TEAM_ROOT and both repos are on `joosep_work`. **Ask which side of the mount the binary is on before predicting either direction.**
- **[SUBSTRATE] `docker diff <container>` minus the mount paths IS the recreate destroy-list** -- volumes never appear in it. One read-only command answers "what does a recreate cost". Joosep has `NOPASSWD:ALL`, so anything he installed outside the mounts dies with the overlay. `~/.tmux.conf` is rewritten unconditionally at `entrypoint.sh:266`.
- **[SUBSTRATE] Step 9c's defect, sharper than "the re-seed branch is dead":** `dir_digest` walked a tree Step 9c itself `git init`s, so **the "already current" branch was dead too and the NOTE branch fired unconditionally** -- it had stopped carrying information. Fixed in `2fde59b`.

### S71/S69/S67 -- FILED, read the wiki not this
- S71: apex CLI 2.1.217 -> 2.1.258 end to end, zero service interruption. Twelve of my own defects, one genus: **failed and working states produced identical observations from where I stood -- a null with no observable state.** Filed as `patterns/state-the-match-set-before-trusting-the-instrument` (7 instances), `patterns/live-is-not-the-same-as-discriminating`, `patterns/an-attribute-that-correlates-is-not-one-that-determines`, `contracts/tier-taxonomy-has-no-slot-for-undeclared-in-place-change`, `gotchas/trailing-pipe-...` inst. 4+5.
- **[THE FIVE RULES -- my practice, not wiki]** (1) A Tier D package goes to the EXECUTOR in full, from its author, at sanction time -- never extracted from an approval thread. (2) State what a probe's NULL means and establish BOTH null and PASS are reachable. (3) Run every gate against a KNOWN-GOOD and a KNOWN-BAD subject. (4) A failure branch carries a PREMISE that can be the wrong thing; "STOP and surface" is the only default. (5) `ls -la`, never `ls -l`, when the subject may be a dotfile.
- S69: PR #113 merged. [LEARNED] "aligns X with the host" is not a risk argument unless host and X differ ONLY in the aligned thing.
- S67: joosep/paunvere shipped. **A dir digest is NOT portable across hosts** (MSYS `*` + locale sort) -- a per-file manifest is the gate. A verification step cannot notice its subject GREW. A seam between two correct entries is visible from neither. **Derive a comparison from the artifact pair, never from a value written down beside it.**
- S66: apex live `.env` neither superset nor subset of compose. RC has NO ingress filtering. Ask the executor "is there a closer precedent on the box?" before designing. TCP ports are per-host.

---
## STANDING DECISIONS (carry forward)

[DECISION] Single-provider default for agent runtime; multi-provider = sidecar, not peer. `GatewayPorts yes` on RC NOT recommended.
[DECISION -- S71] **NO PRUNING, AT ALL.** No `docker rmi`, no `image prune`, not even for dangling images a tag move creates. 2026-04-16 incident; disk is not a constraint.
[DECISION -- S72] **Tag the current image BEFORE any rebuild** (`docker tag <img>:latest <img>:pre-<date>`). Without it the old image is dangling and addressable only by digest. **That one step is what makes a rebuild reversible.**
[DECISION -- S50] Standby hot-fix ONLY when all four hold: active deployment + time-pressure; fix in MAY-WRITE, bounded; flag immediately; explicitly on standby for that class.
[DECISION -- S55/S56] Courier cutover = LAUNCH-OVERRIDE two-config. Liveness = PROCESS-based.
[PATTERN] `network_mode: host` window: tunnels/sockets free; breaks Swarm portability. Plan b-host vs e-swarm profiles from day one.
[PATTERN] Author attribution: bold `(*FR:Brunel*)`. Never italic.

## CARRY-FORWARD GOTCHAS (all containers)

[GOTCHA -- S72] **A `$HOME` named volume MASKS everything the image put under `$HOME`.** Populated once from the image at first attach, authoritative forever after. A rebuild can never change the CLI; a recreate can never regress it. Exact inverse of apex, where `~/.local` was on the overlay.
[GOTCHA -- S71] **A build layer that cannot fail proves nothing about what it installed.** `RUN <install> 2>&1 | tail -N` makes the RUN exit the pipe's last stage; `curl --insecure | bash` does not pass `--insecure` to the fetched script's inner curl. Every install layer needs a postcondition assertion in the same RUN; under dash that means `&& <cmd> --version | grep -qF <expected>`. joosep's `Dockerfile:225-228` gets this right (bash + explicit pipefail + `test -x` + `--version`) but **has no version pin**, so any rebuild bakes an unmeasured version.
[GOTCHA -- S70] A native `~/.local` install on the OVERLAY is ephemeral: recreate = back to the baked version. Same for any in-container `npm install -g`. Two claudes on one PATH = which version you get depends on how you logged in; diagnostic `type -a claude` + `readlink /proc/<pid>/exe`.
[GOTCHA -- S66, STRONG] TWO attach mechanisms conflict: `.bashrc` auto-tmux hook vs remote-command launcher + rc-connect `tmux` field. "shell OR session" REQUIRES the latter.
[GOTCHA -- S66] ssh REMOTE COMMAND = non-login shell, never sources `.bashrc`; `docker exec` without `bash -lc` hits the same case -- **rc=127 is the PATH, not a missing binary.** Fix `SetEnv PATH=` in sshd_config. `/proc/<pid>/exe` fails across uids for want of ptrace, not because the substrate lacks it -- **read it as the owning user.**
[GOTCHA -- S66] A container is NOT a security boundary; `docker` group == root.
[PATTERN -- S66] Person-shaped volumes (named after the PERSON) vs team-shaped (rename = migration). Pick person-shaped for one human. `authorized_keys` as a `:ro` bind re-read every start beats `SSH_PUBLIC_KEY*` env vars.
[GOTCHA -- S52] Rebuilt image + unchanged compose = NOT adopted by `up -d`; MUST `--force-recreate`. Lockfile on a persistent volume + pid-only staleness = false-refuse across recreate; discriminator = PID-1 starttime. `( while true; do svc; done ) &` under `set -e` MUST `set +e` inside the subshell. Keygen in build MUST be generate-if-absent; private keys NEVER in a layer.
[GOTCHA] WARP TLS: `network_mode:host` + `NODE_EXTRA_CA_CERTS=/opt/warp-ca.pem`. Base `docker build` on WARP needs `--network=host`. Named volumes are created as root -> `chown 1000:1000` in the entrypoint; ubuntu:24.04 ships `ubuntu`@1000, rename first.
[GOTCHA] PO edits live files in parallel -- re-read before each Edit batch. CRLF from autocrlf breaks entrypoints: `sed -i 's/\r$//'`. Container memory path is `~/.claude/projects/-home-ai-teams/memory/`.

## DEFERRED (future surfaces)

- V5b/P6 attached-pane wake re-test; courier-side CLI re-validation; OAuth on hr-devs PROD-LLM; hub as standalone image; raamatukoi VPS container; MCP visual-QA pattern; external-audit container spec.
- Cal's entry restructure (permanence/causation sub-shapes) needs a NON-author read-back.

## INFRA REFERENCE

- Stationmaster hub: `sm@10.100.136.162 -p 2222`; FR key `~/.ssh/sm_framework-research`.
- FR courier runs LOCAL on this Windows box from `teams/framework-research/poc/ghost-bridge/`.
- apex-research: RC `100.96.54.170`, ssh `ai-teams@ -p 2222`. Build source = RC clone `/home/dev/github/apex-migration-research`; `designs/deployed/apex-research/container/` is a MIRROR that drifts.
- **joosep: RC `100.96.54.170` port 2231, container+user `joosep`, TEAM_ROOT `/home/joosep/work/paunvere` (local git, branch `master`, head `834d55e`, A1 uncommitted in the working tree). Deploy dir `/home/dev/joosep` = STAGED COPY holding `.env`, `authorized_keys`, `warp-ca.pem`, none of which are in the repo. FR checkout on RC `/home/dev/github/mitselek-ai-teams`. Volumes `joosep_{home,work,sshd}`. Image `joosep:latest`.**
- This box has NO docker. Any build/exec is RC = Hopper/PO.
