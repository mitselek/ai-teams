# Brunel scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S72 2026-09-03. **BOTH SANCTIONED DELIVERABLES ARE OUT.** (1) **Live-path dispatch SENT to `hopper-2`** -- Tier M, amendment A1 into the live `paunvere` TEAM_ROOT, manifest pinned to Aen's commit `8424088`: **4 replaced files + 1 new** (`common-prompt`, `README`, `prompts/saxby`, `startup`, + Celes's `AMENDMENTS.md`), staged in `/tmp/fr-a1`, md5-gated both directions, `cat >` in place, left UNCOMMITTED. **BLOCKED ON THE PO. ALL FOUR GATES PASS, all five files STAGED at `/tmp/fr-a1` with correct md5s/sizes/owner/mode -- and the APPLY was DENIED by the permission classifier (Hopper ops-log `2026-09-03T14:24+03:00`, outcome partial). TEAM_ROOT byte-for-byte as found; nothing half-applied; NOTHING TO ROLL BACK. My `4 files changed, 14 insertions(+), 11 deletions(-)` prediction held to the line.** **I supplied NO reworded variant and told Hopper not to re-shape it: a denial is about the ACTION, not the transport, and routing it to another agent is the same evasion with an extra step.** Resume = re-run G4 at moment of use (a passed gate goes stale silently), then apply. Cleanup either way: `rm -rf /tmp/fr-a1`. (2) **PR #115 OPEN** -- `fr/s72-joosep-entrypoint-digest`, commit `e1e6111`, the `dir_digest` `.git`-prune fix, one file one hunk. Worktree at `C:/Users/mihkel.putrinsh/Documents/github/.wt-s72ep` -- **remove it after merge (`git worktree remove`)**. NOT sanctioned: rebuild (deferred pending a post-transit health read), recreate, per-file manifest design.
- **[CLOCK SKEW -- flagged to Hopper for the ops log]** This box's `date` reads ~15 min BEHIND Aen's message stamps. My 14:15 dispatch answers Aen's 14:26 message, so **my timestamps cannot be used for ordering.** The RC host clock is the authority for ops-log entries.
- **[EVIDENCE CHAIN CLOSED -- the load-bearing measurement]** I md5'd the git BLOBS. All six of Hopper's independently-measured values reproduce exactly: container == `929ba8a^` (`f0969ce1` / `56bd792c` / `94cba7a3`), repo == `929ba8a` == HEAD (`5adad74f` / `36455e70` / `01114426`). **So the container is byte-exact at the parent commit, the delta is exactly this one commit, and nothing has moved since.** `startup.md` `e29fe123` and `roster.json` `64366be6` matched on both sides pre-Celes.
- **[MY OWN ERROR, corrected to Aen 14:12 -- I published an INFERENCE as a MEASUREMENT, in the section I had labelled load-bearing.]** I saw `core.autocrlf=true` + `* text=auto`, inferred CRLF worktrees, wrote "the worktree is CRLF and its md5s are meaningless", and `grep -c $'\r'` (117) appeared to confirm it. **All false.** `stat -c %s` = 11540 = exactly the blob size and `file -b` reports no CRLF terminators -- **the worktree is LF and its md5s equal the blob md5s today.** `grep -c $'\r'` is NOT a valid CR detector under MSYS; **`file(1)` is the authority -- which is my OWN S67 gotcha, walked into four hours after I argued that checks needing alertness are the ones that fail.** Downstream is unaffected (every table value came from `git show`). **Still source the transit from the RC Linux checkout -- not because this box is CRLF, but because that removes the dependency on this box's line-ending state, which is true today by accident and guaranteed by nothing.**
- **[MANIFEST NOW FIVE FILES.]** Celes filled the slot: `AMENDMENTS.md` (new, 2703 B) + `startup.md` (4132 B, and she REMOVED the 2.1.250 pin, closing W7 without a separate line from me). Both UNCOMMITTED, so their md5s are provisional and **must not be gated on until team-lead commits and I re-derive from `git show <commit>:<path>`**. Gate 4 gains a fifth pre-check: container `startup.md` must still be `e29fe123…` at moment of use. **Her amendment-level `A1` at `startup.md:5` is a better instrument than a version stamp at this layer** -- a package-level counter, not a CLI version; self-bootstrapping (no scratchpad carries a level, so the first read is a mismatch and routes every agent through `AMENDMENTS.md` once); and it sits inside the file they already read first. It COMPOSES with `teamPackageCommit` in `registry.json`: same fact, two readers, either one alone would have exposed this drift.
- **[DECISION] Live path = Tier M, whole-file replacement, md5-gated, LEFT UNCOMMITTED.** Do NOT commit on the team's behalf: both existing commits are authored `Joosep Madar`, a dirty tree is loud (their startup Step 1 runs `git status -sb`) where a commit is silent, `git checkout --` reverses it, and Minot's shutdown-commit step demonstrably runs (`28da42a`, 13 min before Hopper's probe) so the designed mechanism absorbs it.
- **[THE DECISIVE ANSWER to the 14:06 addendum] A full redeploy does NOT deliver the amendment.** Rebuild puts it in `/opt`; Step 9c then finds DST != SRC != STAMP, takes the NOTE branch, and leaves TEAM_ROOT alone. **The live path is not the cheap substitute for a redeploy -- it is the only thing that delivers it.** Recreate is ORTHOGONAL, not alternative. **Rejected the clever fix (forge the stamp so the re-seed branch fires): failure mode is a silent no-op, and the branch's premise is "whole tree untouched", so forging it would also clobber any prompt the team edited. Faking a stamp to make a guard fire is defeating the guard.**
- **[RECREATE = REAL OPTION, Tier D package written 14:08.]** Sequence: live path -> `docker tag joosep:latest joosep:pre-20260903-1408` (**the step that makes it reversible; without it the old image is dangling and addressable only by digest**) -> D1-D4 rebuild -> recreate. Live path FIRST so the amendment lands regardless and the two events stay bisectable. **GATE: no `GITHUB_TOKEN` into `/home/dev/joosep/.env` in this window** -- Step 5 would rewrite both repos' `origin` to embed it and would make `entrypoint.sh:68`'s `rm -rf` reachable. Honest reason for the recreate: **not the amendment. Only "make the new image live", plus the fact that this container has NEVER exercised its restart path and doing the first one deliberately beats discovering a boot defect in an incident.**
- **[CORRECTION to the brief's premise -- and my OWN artifact settles it, `entrypoint.sh:241-243`: "the whole $HOME is a volume ... unlike apex, where it sat outside the .claude volume and a recreate wiped it."]** `~/.local/bin/claude` is on `joosep_home`, so **a recreate does NOT regress the CLI** and rollback does not roll it back either -- the inverse of apex on both counts. Same for `.claude.json`, `gh`'s token, `.bashrc`, JetBrains caches. **Falsifiable prediction stated to Aen: the boot log will read 2.1.259+; if it reads 2.1.250 my analysis is wrong and the right response is to RECORD it, not act.**
- **The one thing I cannot measure from here:** Joosep has `NOPASSWD:ALL` (`Dockerfile:159`), so 3 days of `sudo apt install` or writes to `/usr/local` would die with the overlay. **`docker diff joosep` minus the mount paths IS the destroy list** -- volumes never appear in it. One read-only command answers the whole question. Also `~/.tmux.conf` is rewritten unconditionally (`entrypoint.sh:266`).
- **Recreate does NOT clean the harness layer:** `~/.claude/{teams/session-*,sessions/<pid>.json}` are on `joosep_home` and survive as orphans. **Record the four names, do not delete them** -- a stale record named by a pid a fresh container may reuse is an instrument hazard for the next reader, and deleting volume state for tidiness is the 2026-04-16 class.
- **[THE REAL DEFECT, bigger than the one I was handed]** Step 9c `git init`s inside TEAM_DST and `dir_digest` is a `find -type f` over the whole tree, so `.git` alone puts DST off both SRC and STAMP from boot 2. **Consequence sharper than "the re-seed branch is dead": the "current" branch at :406 is dead too, so the NOTE branch fires UNCONDITIONALLY and stops being a signal.** **But the `.git` fix would NOT have prevented this incident** -- `memory/` and `drafts/` live in the same directory, so DST != SRC forever once the team works. The `.git` fix only helps in the provisioning window (it WOULD have worked on 08-31). **There is no update channel for a shipped package after a team's first session.** Real fix = per-file manifest seed, which is the runbook's own "manifest strictly dominates the digest" ruling applied one layer down.
- **[DECISION] Anything we put in TEAM_ROOT must exist upstream first.** A container file with no repo counterpart is REVERSE drift, and reverse drift is worse than forward drift because the next rebuild silently reverts it. So Celes's note + any `startup.md` pointer commit to `designs/deployed/joosep/teams/paunvere/` BEFORE the transit, and the transit ships from that commit.
- **[DECISION] Lifecycle stamp goes in `registry.json`, NOT `startup.md`.** startup.md is inside the package the team owns and drifts by design -- a version line there recreates the W7 defect one line lower. `deployments.md` is measurably stale (no joosep, no allerk, no uikit-dev). The load-bearing field is **`teamPackageCommit`**, not a CLI version: what bit us is package-provenance drift, and a version field would not have caught it. Container half = `/opt/teams/.provenance` written from a build ARG that `joosep.sh build` fills in, so it cannot be forgotten.
- **W7 measured:** the 2.1.250 prose pin is `startup.md:16` (brief said :15). Binary is 2.1.259, autoupdater live, symlink Sep 3 01:48; boot log recorded 2.1.250. Fix = delete the number, keep "2.1.178+", which is the only load-bearing part.
- **Carry-forward:** (a) `Dockerfile.apex` two failure-masking layers -> DELETE both, apex repo, own PR. (b) Base image still ships Node 18; every derived team image gets it. (c) entrypoint-owned `~/.claude.json` restore, absent-only guard (adjudicated S71, not written). (d) S69 `stage-2: pending` read-back backlog -- count NOT verified by me, re-derive from Cal. (e) `joosep_sshd` + `sm-state` have ZERO backups. (f) `[PO-9]` apex registry `tmux` drift. (g) `[PO-17]` bridge `dns:`. (h) `[PO-19]` allerk no pid limit. (i) `git update-index --chmod=+x designs/deployed/joosep/joosep.sh` still owed (24 `.sh` at 100644 vs 6 at 100755).

---
## Session transcript (prune beyond line 100)

### S72 -- paunvere amendment transit design

- **[TWO GATE DEFECTS, BOTH MINE, Hopper aborted on them 14:18 -- instances 13 and 14 of the S71 genus, and BOTH are instrument-not-substrate]** **G1: I gated on a FILE instead of on the server.** A unix socket **outlives the process that created it** unless that process unlinks it on exit, so requiring the socket's ABSENCE was a clause that could never go true. I took a correct instrument (`list-sessions` exit status, which asks the server) and bolted on a clause that guaranteed a false STOP. **Fix = DELETE my clause, not add one.** Re-issued as `tmux -S <socket> has-session` rc + `pgrep -x tmux`, socket file not consulted. **G2: `pgrep -f claude` MATCHED ITS OWN WRAPPER** -- `-f` reads full command lines and the command line carrying the pattern contains the pattern. Run verbatim it reported *a live claude with no session record*, which my own dispatch classified as a STOP: **a failure branch whose premise was the wrong thing entirely = my S71 rule 4, in a gate I wrote AFTER writing the rule.** Fix = `pgrep -x` (matches the process NAME, structurally cannot match a bash wrapper) + an independent `ps` sweep.
- **[THIRD gate defect, 14:24, and the most instructive] I pinned `git rev-parse HEAD` to a RECORDED VALUE (`8424088`).** PR #115 merged, main moved to `2fde59b`, and my EXPECT would have STOPped Hopper a SECOND time on a healthy substrate. **This is the runbook's own Step-14 lesson verbatim: derive the comparison from the artifact pair, never from a value written down beside it** -- and it fails CLOSED, so it reads as diligence. Corrected: **gate on the five file md5s (artifact-derived, cannot go stale); keep HEAD as an observation, not a pass condition.** Measured that all five payload files are byte-identical at `8424088` and `2fde59b` (the range touches `designs/deployed/joosep/` in exactly one file, `entrypoint.sh` = my own fix), so the manifest survived. **Also withdrew my "the pull is provably payload-only" licence -- true when written, false after the merge.**
- **[ENTRYPOINT DRIFT -- Hopper MEASURED it and my shape was WRONG]** I called it "fresh drift between the two", implying a two-way divergence. Measured: **L1 FR checkout `4379dcf2` 28352 B (pruned) vs L2 `/home/dev/joosep/entrypoint.sh` `cb8405a0` 27479 B (defective) vs L3 in-image `cb8405a0` -- L2 AND L3 ARE BYTE-IDENTICAL, so there is NO L2->L3 drift at all.** It is ONE boundary: **L1 vs (L2 = L3)**. Her sequencing, which is the form the next rebuild dispatch needs: **one re-stage L1->L2, after which L2 and L3 diverge until a recreate lands the new image.** The image was built from exactly the deploy copy on disk now. Harmless today; no build/recreate/restart sanctioned and the file is not in the A1 manifest.
- **[THE FIX AT SOURCE, not just the correction] STOP WRITING A `HEAD` EQUALITY AS AN EXPECT.** It goes in a dispatch as an OBSERVATION TO RECORD. **All three of my gate defects today failed CLOSED -- they turn a healthy substrate into a STOP, which reads as diligence** (the runbook's own words). Standing rule I gave Hopper: **a stale PIN is not a stop; a stale MANIFEST is.** Proceed only when all three hold -- pin is an ANCESTOR of HEAD, `git diff --stat <pin>..HEAD -- <manifest tree>` is EMPTY, every POST md5 matches. Force-pushed/rewritten history fails the first = hard stop.
- **[HERS, and STRONGER than mine -- adopt it] `git diff --stat` over the MANIFEST TREE returning empty proves nothing was ADDED, REMOVED or RENAMED. Five matching hashes cannot: they say the five files I NAMED are right, not that they are still the WHOLE SET.** I verified identity of members; she verified identity of the set.
- **[ADOPTED from her execution]** (1) **Re-run the gates at moment of use on resume -- a PASSED gate goes stale silently.** (2) Assert a file's ABSENCE explicitly; never read it off a listing. (3) **READ a new file before installing it into another team's root -- that is not a hash problem.** `4360afc2` would have been equally true of a file saying something else entirely.
- **[HERS, same family as my two -- a THIRD way a probe lies about itself] A zero-match `grep -c` as the LAST command in a chain makes a wholly successful probe exit 1 and READ AS FAILED.** The last command sets the status. Her trailing-pipe gotcha in a new hat. Nothing was lost because each reading printed its own value -- **which is the remedy: make every probe emit its own labelled value, so the chain's exit status is never the thing you read.**
- **[LESSON, the durable one] A gate that contains its own probe can match itself.** Ask whether the instrument is inside the set it is measuring. Both defects made a TRUE condition read as a STOP -- **they failed CLOSED, which is why they looked like diligence** (the runbook's own words about the Step 14 md5 gate). **Neither was caught by me re-reading my reasoning; both by an executor running them against real output.**
- **[Hopper's judgement was right on the meta-point too]** She could have substituted her better instruments and been correct. She stopped because **patching a sanctioned gate in flight leaves the defect live in the next gate its author writes.** Producer-vs-consumer pointed back at the producer.

- **[GOTCHA, new] The `/home/dev/joosep` deploy dir is a STAGED COPY, not a checkout** (runbook Step 1), and it holds three files that are NOT in the repo: `.env`, `authorized_keys`, `warp-ca.pem`. Any re-stage is per-file against the Step 1 manifest, **never `rsync --delete`**. The exec bit on `joosep.sh` is still absent from the index, so any mode-preserving stage yields `Permission denied` at build.
- **[GOTCHA] The runbook says "do NOT harmonise `dir_digest` -- it is correct as it stands."** That ruling is about CROSS-HOST digest portability and it is right. It does not cover the tree being hashed. A reader could apply it to my fix and block a correct change, so the fix's comment disarms it explicitly.
- **[PATTERN] The seed target IS the team's working directory.** Any all-or-nothing tree comparison over a directory that is simultaneously "what we shipped" and "where they work" can only answer "untouched" before the first session. Per-file is the only granularity that survives contact.
- **[LEARNED] `git show <rev>:<path>` does NOT apply the CRLF smudge filter on this box.** Proven: my blob md5s reproduce Hopper's Linux-side values exactly.
- **[GOTCHA -- the REFINED line-endings rule, and it took me three tries]** `core.autocrlf=true` here, so **a FRESH CHECKOUT gives CRLF** (proved it: the worktree's `entrypoint.sh` came out 27998 B vs the blob's 27479 = 519 CRs) -- but the `teams/paunvere/` files in the main worktree are **LF, because they were written by Write/Edit and never re-checked-out.** So "is this box CRLF?" has no single answer: **it depends on whether each file was CHECKED OUT or TOOL-WRITTEN.** My first claim (CRLF, inferred) and my correction (LF, measured on one file) were each half right. **This is exactly the accidental dependency I told Aen to design out -- sourcing the transit from the RC checkout is right, and a fresh clone HERE would have shipped CRLF into the container.** Committing back is safe: `text=auto` normalises, and I verified the blob at 0 CR.
- **[GOTCHA -- I walked into the runbook's OWN documented trap while testing the fix]** My first known-good run said the FIXED `dir_digest` still DIFFERs. Cause reproduced before touching anything: **MSYS `md5sum` prints `<hash> *<path>`, Linux prints `<hash>  <path>`**, so the function's own `sed "s| $1/| |"` never matched and full paths survived into the digest. **The prune was working the whole time; the TEST was invalid on this platform.** Fixed the test, not the code. **The lesson is the runbook's: reproduce the cause, do not adopt the story that fits.** Had I "fixed" the code to make the test pass I would have shipped a defect to close a platform artifact.
- **[LEARNED] The honest limit of the live path:** a running claude already holding the old text in context keeps acting on it until it re-reads. We cannot fix that from outside, and we are barred from messaging paunvere agents. So the transit lands the file; the NOTE file plus Joosep's own restart is what lands the behaviour.

### S71/S69/S67 -- FILED, read the wiki not this
- S71: apex CLI 2.1.217 -> 2.1.258 done end-to-end, zero service interruption. Twelve of my own defects, one genus: **failed and working states produced identical observations from where I stood -- a null with no observable state.** Filed as `patterns/state-the-match-set-before-trusting-the-instrument` (7 instances), `patterns/live-is-not-the-same-as-discriminating`, `patterns/an-attribute-that-correlates-is-not-one-that-determines`, `contracts/tier-taxonomy-has-no-slot-for-undeclared-in-place-change`, `gotchas/trailing-pipe-...` inst. 4+5.
- **[THE FIVE RULES -- my practice, not wiki]** (1) A Tier D package goes to the EXECUTOR in full, from its author, at sanction time -- never extracted from an approval thread. (2) State what a probe's NULL means and establish BOTH null and PASS are reachable. (3) Run every gate against a KNOWN-GOOD and a KNOWN-BAD subject. (4) A failure branch carries a PREMISE that can be the wrong thing; "STOP and surface" is the only default. (5) `ls -la`, never `ls -l`, when the subject may be a dotfile.
- S69: PR #113 GREEN + merged `446251f`. [LEARNED] "aligns X with the host" is not a risk argument unless host and X differ ONLY in the aligned thing.
- S67: Joosep/paunvere arc DONE; package at `designs/deployed/joosep/`. **A dir digest is NOT portable** (MSYS `*` + locale sort) -- per-file manifest is the gate. A verification step cannot notice its subject GREW. A seam between two correct entries is visible from neither.
- S66: apex live `.env` neither superset nor subset of compose. RC has NO ingress filtering. Ask the executor "is there a closer precedent on the box?" before designing. TCP ports are per-host.

---
## STANDING DECISIONS (carry forward)

[DECISION] Single-provider default for agent runtime; multi-provider = sidecar, not peer. `GatewayPorts yes` on RC NOT recommended.
[PATTERN] `network_mode: host` window: tunnels/sockets free; breaks Swarm portability. Plan b-host vs e-swarm profiles from day one.
[PATTERN] Author attribution: bold `(*FR:Brunel*)`. Never italic.
[DECISION -- S50] Standby hot-fix ONLY when all four hold: active deployment + time-pressure; fix in MAY-WRITE, bounded; flag immediately; explicitly on standby for that class.
[DECISION -- S55/S56] Courier cutover = LAUNCH-OVERRIDE two-config. Liveness = PROCESS-based.
[DECISION -- S71] NO PRUNING, AT ALL. No `docker rmi`, no `image prune`, not even for dangling images a tag move creates. 2026-04-16 incident; disk is not a constraint.

## CARRY-FORWARD GOTCHAS (all containers)

[GOTCHA -- S72] **A `$HOME` named volume MASKS everything the image put under `$HOME`.** Populated once from the image at first attach, authoritative forever after. So on joosep a rebuild can never change the CLI, and a recreate can never regress it -- the exact inverse of apex, where `~/.local` was on the overlay. **Ask which side of the mount the binary is on before predicting either.**
[GOTCHA -- S70, AMENDED S71] Two claudes on one PATH = which version you get depends on how you logged in. Diagnostic: `type -a claude` + `readlink /proc/<pid>/exe`. Never install both npm-global and native in one image. apex is the SIBLING failure: the second claude was attempted and silently never landed.
[GOTCHA -- S71, the durable one] **A build layer that cannot fail proves nothing about what it installed.** `RUN <install> 2>&1 | tail -N` makes the RUN exit the pipe's last stage; `curl --insecure | bash` does not pass `--insecure` to the fetched script's inner curl. Every install layer needs a postcondition assertion in the same RUN; under dash that means `&& <cmd> --version | grep -qF <expected>`, not `set -o pipefail`. **joosep's `Dockerfile:225-228` gets this RIGHT** (bash + explicit pipefail + `test -x` + `--version`) -- but has no version pin.
[GOTCHA -- S70] A native `~/.local` install on the OVERLAY is ephemeral: recreate = back to the baked version. Same for any in-container `npm install -g`. Pin at build + assert, or accept drift.
[GOTCHA -- S66, STRONG] TWO attach mechanisms conflict: `.bashrc` auto-tmux hook vs remote-command launcher + rc-connect `tmux` field. "shell OR session" REQUIRES the latter.
[PATTERN -- S66] Person-shaped (volumes named after the PERSON) vs team-shaped (rename = migration). Pick person-shaped for one human.
[PATTERN -- S66] `authorized_keys` as `:ro` bind mount re-read every start beats `SSH_PUBLIC_KEY*` env vars.
[GOTCHA -- S66] ssh REMOTE COMMAND = non-login shell, never sources `.bashrc`. Fix `SetEnv PATH=` in sshd_config. `docker exec` without `bash -lc` hits the same case -- rc=127 is the PATH, not a missing binary.
[GOTCHA -- S66] A container is NOT a security boundary; `docker` group == root.
[GOTCHA] PO edits live files in parallel -- re-read before each Edit batch.
[GOTCHA -- S52] Rebuilt image + unchanged compose = NOT adopted by `up -d`; MUST `--force-recreate`. Lockfile on persistent volume + pid-only staleness = false-refuse across recreate; discriminator = PID-1 starttime. `( while true; do svc; done ) &` under `set -e` MUST `set +e` inside the subshell.
[GOTCHA -- S52] Keygen in build MUST be generate-if-absent. Private keys NEVER in a layer.
[CORRECTION -- S53] `~/.claude.json` ($HOME root) regenerates; stateful config is `~/.claude/{settings,mcp,.credentials}.json` on the volume.
[GOTCHA] WARP TLS: `network_mode:host` + `NODE_EXTRA_CA_CERTS=/opt/warp-ca.pem`. Base `docker build` on WARP needs `--network=host`.
[GOTCHA] Named volumes created as root -> `chown 1000:1000` in entrypoint. ubuntu:24.04 ships `ubuntu`@1000 -> rename first.
[GOTCHA -- courier] ghost_outbox must be `<registered-team>-bridge`. Windows `Stop-Process` = hard kill. CRLF from autocrlf breaks entrypoints: `sed -i 's/\r$//'`. Container memory path is `~/.claude/projects/-home-ai-teams/memory/`.

## DEFERRED (future surfaces)

- V5b/P6 attached-pane wake re-test; courier-side CLI re-validation; OAuth on hr-devs PROD-LLM; hub as standalone image; raamatukoi VPS container; MCP visual-QA pattern; external-audit container spec.
- Cal's entry restructure (permanence/causation sub-shapes) needs a NON-author read-back.
- Base compose `x-team-base` build block `network: host` -- WRITTEN S71, joosep's compose already has it.

## INFRA REFERENCE

- Stationmaster hub: `sm@10.100.136.162 -p 2222`; FR key `~/.ssh/sm_framework-research`.
- FR courier runs LOCAL on this Windows box from `teams/framework-research/poc/ghost-bridge/`.
- apex-research: RC `100.96.54.170`, ssh `ai-teams@ -p 2222`. Build source = RC clone `/home/dev/github/apex-migration-research`; `designs/deployed/apex-research/container/` is a MIRROR that drifts.
- **joosep: RC `100.96.54.170` port 2231, container+user `joosep`, TEAM_ROOT `/home/joosep/work/paunvere`. Deploy dir `/home/dev/joosep` (STAGED COPY). FR checkout on RC `/home/dev/github/mitselek-ai-teams`. Volumes `joosep_{home,work,sshd}`. Image `joosep:latest`.**
- This box has NO docker. Any build/exec is RC = Hopper/PO.
