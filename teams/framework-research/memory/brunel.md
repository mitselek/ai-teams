# Brunel scratchpad

## Summary (lines 1-15 -- always read on startup)
- **Current state:** S71 2026-09-02 17:53, CLOSED. **TASK COMPLETE END TO END. apex-research runs `2.1.258 (Claude Code)` on the image the build produced** -- digest == tag, 3 SSH keys, courier depositing, dashboard serving 200. **Fast path (F0-F2) delivered it at 16:17 with ZERO service interruption** (courier/vite/PO tmux re-verified UP *after* the mutation, not assumed). **Rebuild D0-ii..D9 all GREEN**; new base `ebef9b7b8ed9`, new apex `ca42f7787444`; HEAD `057fab4` -- team-lead committed my 2 files mid-dispatch and **MY assertion ran in base build layer #8 and PASSED**, independently closing the trailing-pipe mask that started this whole task. **`.claude.json` RESTORE VERIFIED 17:00 byte-identical** (md5 `6f3c1370...`, 55734 B, `ai-teams:ai-teams`; all 6 backups intact). **Final report + both adjudications sent to team-lead 17:00; 2 Protocol A submissions filed 16:53/16:54. Shutdown approved 17:05, then RESUMED by Cal 17:22 for read-backs -- delivered 17:24 (below). **FOUR READ-BACKS DONE. Three confirmed 17:33** (tier-taxonomy figures corrected against the ops-log and verified by me in the file, agreeing with my own independent stamps). **4th: `patterns/an-attribute-that-correlates-is-not-one-that-determines` read back 17:41 -- mechanism/remedy/vantage-limit CONFIRMED, 3 corrections sent. **5th: re-read `state-the-match-set` at its SEVEN-instance version 17:47 and confirmed it -- my 17:24 confirmation had covered only FOUR.** **KNOWLEDGE LAYER ALSO CLOSED: 4 entries read back (3 confirmed, genus `partial` pending Hopper), `stage-2` axis-2 raised, match-set confirmed against md5 `818cc1bf...`. NOTHING OF MINE IS OPEN IN EITHER LAYER.**
- **[#10, and it would have put a FALSE entry in the L1/L2 record] `KEY_COUNT` is a shell VARIABLE, not a logged literal.** L1 line 261 sets it, 267 increments, **281 emits `[entrypoint] ${KEY_COUNT} SSH public key(s) installed...`** -- so the log prints `3 SSH public key(s)` and the string `KEY_COUNT` **never appears in any version's log output.** My expected outcome could not be satisfied. **Where it came from: Hopper's OWN May-2026 scratchpad line "KEY_COUNT=3 confirmed in entrypoint logs" -- shorthand for *the count was 3*, which I read as a log literal and turned into a grep string six months later.** Her inference (L1-vs-L2 drift) was reasonable, explains the observation, and is WRONG -- **and the conclusion does not depend on L2's content, since the string cannot appear from L1 either.** **This is D4 exactly, in the verification step for the destructive operation: an expected outcome that could never be satisfied.** She verified the underlying condition (3 keys in `authorized_keys`, listener up) and did not roll back -- **twice today my broken check met her judgement instead of an automatic branch.**
- **[ADJUDICATION owed to team-lead -- should the ENTRYPOINT own the `.claude.json` restore? YES, in the L1->L2 transit follow-up.]** L1's Step 9d2 BACKS UP and nothing RESTORES. **A backup with no restore path is half a mechanism: it converts a silent loss into a recoverable loss that still requires a human to notice and act** -- which is exactly what happened here, and it only got noticed because D9 looked. Fix: at start, if `$HOME/.claude.json` is ABSENT and a backup exists on the volume, restore the newest. **Absent-only guard, never overwrite a live file -- the same `[ ! -f ... ]` shape Step 7b already uses for the courier key**, and the same generate-if-absent pattern as the SSH host keys (S52). Pairs the two halves and makes the container self-healing across recreates for this file.
- **MECHANISM (settled, Hopper live recon):** apex runs ONE claude, `/usr/local/bin/claude`, npm-global ROOT-owned from the BASE image -> 268 MB `claude.exe`. **No `~/.local/bin/claude` and no `versions/` dir** -- `Dockerfile.apex`'s native-install RUN is 20.5 kB and empty, double-masked (`--insecure` never reaches install.sh's inner curl; `2>&1 | tail -5` makes the RUN exit `tail`'s). **So #113 IS the lever and base `Dockerfile:53` IS on apex's runtime path -- the opposite of what I told Aen in S70.**
- **ALL GATES CLEARED (Hopper, R-A/R-B/R-C):** (1) L2 entrypoint DOES clear the courier lock (`rm -f` :513 before `supervise courier` :516) -> no entrypoint change, no manual lock removal. (2) Env-key set difference is exactly `PATH`, image-supplied -> nothing lost at recreate. **This discharges team-lead's stated D8 precondition (D0-iv) ALREADY -- do not let the PO wait on it.** (3) Courier key fingerprint `SHA256:NBq5a/r3GsTuIGME1BzsklC9Sr+6VrltsxoSfW4QsaE` IDENTICAL across build-source/image-seed/live, and the hub ACCEPTS it (0 key rejections in 203 log lines, 32 deposits). (4) `claude.json` absent from L2 -> **D7 manual backup REQUIRED.**
- **[DECISION] L1 vs L2 entrypoint drift: L2 SHIPS UNCHANGED, and that is a no-op** (RC already holds it). A version bump must not also be the first-ever run of L1's never-transited 7 KB -- one recreate, one variable, or a bad recreate cannot be bisected. Seam accepted knowingly and covered by D7. L1->L2 transit is a separate later change with its own recreate.
- **[DECISION] Fast path = root `npm install -g`, NOT `claude install`.** The fast path must PREVIEW the durable path, not diverge from it. `claude install` would create `~/.local/bin/claude` (login PATH prefers it) which the rebuild then WIPES -> a later recreate reads as a spontaneous version regression. Root's npm uses `/root/.npm`, so the root-owned `/home/ai-teams/.npm` defect (cause of the PO's "Unable to fetch latest version") is not in the way; separate one-line fix, deliberately unbundled.
- **[DECISION] NO PRUNING, AT ALL.** 5th image `apex-research-claude:pre-20260616-1417` = Jun 16 pre-change snapshot; LEAVE it. No `docker rmi`, no `docker image prune`, **not even for the dangling images D3/D5 create when they move `:latest`.** Pruning without walking the parent chain caused the 2026-04-16 incident -- and `ai-teams-claude:rollback-pre-2.1.217` is dated exactly that day. 246 G free on /home; disk is not a constraint.
- **[FILES WRITTEN -- mine, team-lead commits, both deliberately OFF the critical path]** `Dockerfile:52-53` += `&& [ "$(claude --version | cut -d' ' -f1)" = "${CLAUDE_VERSION}" ]` (NO `set -o pipefail`, /bin/sh is dash; exact field compare, not `grep -F` which a shorter pin would satisfy as a prefix; no `$`-anchored regex, Dockerfile RUN pre-expands `$`). `docker-compose.yml` x-team-base build += `network: host` (S60 TODO, now on-path; apex compose is the precedent). Runtime gate D4 covers the same failure with zero file changes, so the upgrade never waits on commit+push+RC-pull.
- **[RETRACTED 16:32 -- I issued this warning THREE times and it is now FALSE; do not carry it forward]** I said "until the rebuild lands, any `--force-recreate` silently downgrades apex to 2.1.217." **The hazard closed at D5, not at D8.** The compose launches from `apex-research-claude:latest`, which now resolves to the new image `ca42f7787444` -- so a recreate brings up 2.1.258 and a restart keeps the overlay copy, also 2.1.258. **Safe under every lifecycle event.** MEASURED by Hopper (I gave two supporting legs and **one was worthless -- see the lesson below**): operational compose declares `image: apex-research-claude:latest` at raw line 36 with **NO `pull_policy`**, so `up` uses the local tag; that tag resolves to `ca42f7787444` which reports 2.1.258; and the running overlay independently reports 2.1.258. **Consequence: D8 is COMPLETING, not protective** -- the PO picks the moment freely, though **not costlessly: see the divergence below.**
- **[OPEN INSPECTION TRAP until D8 -- Hopper]** `Config.Image` = `apex-research-claude:latest`, but running `.Image` = `sha256:fb99aee1887c` (OLD) while the tag now resolves to `ca42f7787444` (NEW). **D2/D5 moved the tag underneath a live container, so the container is NOT running the image its own config names.** Benign for the version (both routes deliver 2.1.258, old-image-plus-overlay vs new-image-natively) but a real trap for anyone diagnosing apex in this window -- **and the window is open-ended precisely because we made D8 discretionary.** A mild argument for doing D8 sooner; **explicitly NOT a safety one.** D8 ends the divergence.
- **Verification, AS ai-teams, never root:** `docker exec -u ai-teams apex-research bash -lc 'claude --version; type -a claude'` -> `2.1.258 (Claude Code)` + **TWO identical `/usr/local/bin/claude` lines** (PATH carries `/usr/local/bin` twice) + **ZERO** mentioning `~/.local/bin`. **The pass condition is the ABSENCE of a second path, not a line count.**
- **[CAL RESOLVED] `callimachus` reachable again; my 2 Protocol A submissions SENT 16:53/16:54.** Both as INSTANCES of existing entries, not new pages: (1) `Config.Image` + *live is not discriminating* -> `verification-narrower-than-it-appears` instance-4 sub-shape, **flagged to MERGE with Hopper's same-observation submission from her 16:48 batch**; (2) two masked apex layers -> `trailing-pipe-...` instances 4+5, answering that entry's OWN revision trigger. **I asked Cal to DECIDE whether the genus earns its own page and argued AGAINST it** -- his call as classifier, not mine to argue in. Draft retained at scratchpad `brunel-cal-submissions-S71.md`.
- **Carry-forward / DEFERRED (details in the S71 body):** **(a) `Dockerfile.apex`'s TWO failure-masking layers -> DELETE both, apex repo, own PR + own build + own recreate.** (b) Base image still ships Node 18 (`EBADENGINE`); apex hides it by overlaying Node 22, so **every other team image FROM this base gets Node 18.** (c) **ENTRYPOINT-OWNED `~/.claude.json` RESTORE in the L1->L2 transit -- ABSENT-ONLY guard** (same shape as Step 7b's courier key), preferring a pre-recreate capture and falling back to the newest CLI-written backup, which always trails by one write. **Adjudicated and reported 17:00; not written.** (d) **[COUNT NOT VERIFIED BY ME -- do not carry the number forward] the S69-deferred `stage-2: pending` read-back backlog.** It was recorded as 5 at S69 and Cal's S71 close listed 2 owed from me; **I have not measured it and cannot stand behind either figure.** **Distinct from today's four, which are DONE** -- match-set, live-is-not-discriminating and the tier-taxonomy contract confirmed, the genus entry `partial` pending Hopper. **Whoever picks it up should re-derive the backlog from Cal's state rather than from this line.** (e) `joosep_sshd` has no backup (PO keyboard). (f) `[PO-9]` apex registry `tmux` drift. (g) `[PO-17]` bridge `dns:`. (h) `[PO-19]` allerk no pid limit.

---
## Session transcript (prune beyond line 100)

### S71 -- apex CLI 2.1.217 -> 2.1.258 DONE. TWELVE of my own defects, one genus. **ALL FILED -- read the wiki, not this.**

**FILED, do not re-derive:** `patterns/state-the-match-set-before-trusting-the-instrument` (7 instances; 1,3,4,5,6 mine; row 7 = the wiki's OWN `stage-2` field, both axes) -- confirmed by me against md5 `818cc1bf...`, 25494 B, and his declared delta verified mechanically (removing lines 209-219 reproduces it exactly). `patterns/live-is-not-the-same-as-discriminating` (Cal split it out of `verification-narrower-...` against my placement and was RIGHT -- my target was correct, so it is discriminating-power-on-a-right-target). `patterns/an-attribute-that-correlates-is-not-one-that-determines` (my Aug phrase is the title; n=5; `partial`, Hopper owed). `contracts/tier-taxonomy-has-no-slot-for-undeclared-in-place-change` (3 states: 16:17 in-place / 16:28 declared-not-adopted / 16:44 adopted). `gotchas/trailing-pipe-...` instances 4+5.

- **[THE FIVE RULES -- MY practice, not wiki, and the real output]** (1) **A Tier D package goes to the EXECUTOR in full, from its author, at sanction time** -- never extracted from an approval thread. (2) **State what a probe's NULL means and establish BOTH null and PASS are reachable.** (3) **Run every gate against a KNOWN-GOOD and a KNOWN-BAD subject** -- needs no alertness, which is why it fires. (4) **A failure branch carries a PREMISE that can be the wrong thing** -- mine said "restore" on a gate failure and would have destroyed a good build; destructive branches need the premise independently confirmed, and "STOP and surface" is the only default. (5) **`ls -la`, never `ls -l`, when the subject may be a dotfile.**
- **[THE GENUS, in Hopper's words, hers]** *"It would look exactly like the executor going quiet, which is indistinguishable from the executor working. **That is a null with no observable state.**"* **All twelve had it: failed and working states produced IDENTICAL observations from where I stood.** **The location kept moving** -- commands, a gate, cited evidence, message routing, three verification steps -- **which is why hardening each one never caught the next.** Every one caught by someone reading real output; **none by me re-reading my reasoning.**
- **[NOT FILED, deliberately -- partial-fix finding held at n=2]** *A partial fix to a trust signal does not leave trust where it was; it RAISES it, and is incomplete in the way that READS AS COMPLETE.* Instances: my rename (fixes axis 1, leaves axis 2) and Cal's referent (fixes version, leaves its own recursion). **Held because both are fixes to the SAME signal = one artifact examined twice.** Promotion condition written: **a third instance in a trust signal that is NOT that gate, shipped AND RELIED UPON.** **My `grep -qF` build assertion is a NEAR-instance that FAILS it (caught pre-ship, never relied on) but QUALIFIES the mechanism: a partial fix is catchable by its author right up until the moment it is TRUSTED, and invisible to them after.**
- **[TWO THINGS OF HIS WORTH REUSING]** **Recording a referent CHANGES the artifact, so confirming invalidates the confirmation; only the DECLARED DELTA keeps the ledger true.** And the decay diagnostic: **when a practice keeps being dropped, check whether it was written as SITUATIONAL when it is STRUCTURAL** -- a testable question about a document, not a judgment about the people ignoring it.
- **[OPERATIONAL, keep]** **Rolling back a recreate is NOT free:** the rollback image carries the old version and the recreate destroys any overlay copy, so rollback loses the whole upgrade. Right answer to a BROKEN CONTAINER, wrong answer to a version surprise; never pre-authorise it as a branch. **`~/.claude.json` is at `$HOME` root = OVERLAY; the `~/.claude` VOLUME survives. Claude Code writes its OWN backups into `~/.claude/backups/` on the volume, but ALWAYS TRAILS BY ONE WRITE**, so a pre-recreate capture is the only copy of the final state. `cp -a` preserves the SOURCE mtime -- a backup whose filename time and mtime disagree is evidence the copy is faithful.

### S69/S67 -- FILED, read the wiki not this
- S69: PR #113 GREEN + merged `446251f`; base image NOT rebuilt. [LEARNED] "aligns X with the host" is not a risk argument unless host and X differ ONLY in the aligned thing.
- S67: Joosep/paunvere arc DONE; package at `designs/deployed/joosep/`. Lessons filed: `M `/` M` one char decided what shipped (`git show --stat <merge>` is the only check); six agreeing copies of one guess = one datapoint (record the MECHANISM, not the value); exec bit IS tracked (`git update-index --chmod=+x`), two correct clusters make 24 wrong ones look deliberate; dir digests are NOT portable (MSYS `*` + locale sort) -- per-file manifest is the gate; verify against the BLOB not the worktree on this box (`file(1)` is the authority, not `grep -c $'\r'`); a verification step cannot notice its subject GREW -- re-derive EXPECTs from the artifact; a seam between two correct entries is visible from neither -- co-locate. **8 instances: I err one line from a rule I just wrote. Checks that do not depend on alertness are the ones that catch things.**
- S66: apex live `.env` neither superset nor subset of compose `environment:` (dead `TUNNEL_TOKEN`) -- derive `.env` from the compose block. RC has NO ingress filtering. WARP CA: bind-mount (loud) beats baked (silent). Ask the executor "is there a closer precedent on the box?" before designing. TCP ports are per-host.

---
## STANDING DECISIONS (carry forward)

[DECISION] Single-provider default for agent runtime; multi-provider = sidecar, not peer. `GatewayPorts yes` on RC NOT recommended.
[PATTERN] `network_mode: host` window: tunnels/sockets free; breaks Swarm portability. Plan b-host vs e-swarm profiles from day one.
[PATTERN] Author attribution: bold `(*FR:Brunel*)`. Never italic.
[DECISION -- S50] Standby hot-fix ONLY when all four hold: active deployment + time-pressure; fix in MAY-WRITE, bounded; flag immediately; explicitly on standby for that class.
[DECISION -- S55/S56] Courier cutover = LAUNCH-OVERRIDE two-config (explicit `fr-courier.config.json` + `.auto.json` via wrapper only). Liveness = PROCESS-based.

## CARRY-FORWARD GOTCHAS (all containers)

[GOTCHA -- S70, AMENDED S71] **Two claudes on one PATH = which version you get depends on how you logged in** (allerk hit it; joosep `Dockerfile:194-200`). Diagnostic: `type -a claude` + `readlink /proc/<pid>/exe`. Never install both npm-global and native in one image; if the base forces npm, the derived image's gate must check the path the TEAM runs, not root's. **apex is NOT an instance -- I mis-filed it as one in S70. apex is the sibling failure: the second claude was ATTEMPTED and silently never landed, so the hazard there is a dead layer plus a PATH entry pointing at nothing.**
[GOTCHA -- S71, this is the durable one] **A build layer that cannot fail proves nothing about what it installed.** `RUN <install> 2>&1 | tail -N` makes the RUN exit the pipe's last stage; `curl --insecure … | bash` does not pass `--insecure` to the fetched script's own inner curl. Both masks were on the SAME apex line, so a 20.5 kB empty layer read as a successful native install for six weeks. **Every install layer needs a postcondition assertion in the same RUN, and under dash that means `&& <cmd> --version | grep -qF <expected>`, not `set -o pipefail`.**
[GOTCHA -- S70] **A native `~/.local` install is on the container OVERLAY, not a volume: the CLI version is EPHEMERAL state.** Recreate = back to the baked version. Same is true of any in-container `npm install -g`. Pin at build + assert, or accept drift.
[GOTCHA -- S66, STRONG] TWO attach mechanisms conflict: `.bashrc` auto-tmux hook hijacks every login (bare shell unreachable) vs remote-command launcher (`tmux-apex`) + rc-connect `tmux` field. "shell OR session" REQUIRES the latter.
[PATTERN -- S66] Person-shaped (volumes named after the PERSON, whole-$HOME vol) vs team-shaped (apex: team name baked into volume names, rename = migration). Pick person-shaped for one human.
[PATTERN -- S66] `authorized_keys` as `:ro` bind mount re-read every start beats `SSH_PUBLIC_KEY*` env vars.
[GOTCHA -- S66] ssh REMOTE COMMAND = non-login shell, never sources `.bashrc`; native claude in `~/.local/bin` -> not found ONLY over `ssh host <cmd>`. Fix `SetEnv PATH=` in sshd_config.
[GOTCHA -- S66] A container is NOT a security boundary; `docker` group == root. Only rootless Docker or separate Linux accounts are.
[DEFECT -- S66, `[PO-9]`] apex registry `"tmux": "apex"` vs launcher session `apex-research`.
[GOTCHA] PO edits live files in parallel -- re-read before each Edit batch.
[GOTCHA -- S52] Rebuilt image + unchanged compose = NOT adopted by `up -d`; MUST `--force-recreate`. Test supervisor relaunch by killing the leaf pid only.
[GOTCHA -- S52] Lockfile on persistent volume + pid-only staleness = false-refuse across recreate; discriminator = PID-1 starttime (`/proc/1/stat` f22), not boot_id.
[GOTCHA -- S52, STRONG] `( while true; do svc; done ) &` under `set -e` MUST `set +e` inside the subshell.
[GOTCHA -- S52] Keygen in build MUST be generate-if-absent. Ephemeral-home paths (`~/.ssh`, `/etc/ssh`) don't survive rebuild: keys -> seed+copy-each-start; stateful config -> volume + guarded create. Private keys NEVER in a layer.
[CORRECTION -- S53] `~/.claude.json` ($HOME root, ephemeral) regenerates; apex Step 9d2 backs it up anyway. Stateful: `~/.claude/{settings,mcp,.credentials}.json` on the volume.
[GOTCHA] WARP TLS: `network_mode:host` + `NODE_EXTRA_CA_CERTS=/opt/warp-ca.pem`. Base `docker build` on WARP needs `--network=host` (S60; hidden by layer cache).
[GOTCHA] Named volumes created as root -> `chown 1000:1000` in entrypoint. ubuntu:24.04 ships `ubuntu`@1000 -> rename/delete first.
[GOTCHA -- OpenSSH 9.x] `ssh-keygen -A -f <prefix>` fails if `<prefix>/etc/ssh` absent; generate ed25519 with explicit `-f`.
[GOTCHA -- courier] ghost_outbox must be `<registered-team>-bridge`; deposit re-resolves dest. Windows `Stop-Process` = hard kill, drain must be external. `FrameworkResearch-Courier` Scheduled Task DISABLED (S58), kept for rollback only.
[GOTCHA] CRLF from autocrlf breaks entrypoints: `sed -i 's/\r$//'`. Inbox files exist only after registration: spawn service roles BEFORE senders. Container memory path is `~/.claude/projects/-home-ai-teams/memory/`.

## DEFERRED (future surfaces)

- V5b/P6 attached-pane wake re-test; courier-side CLI re-validation; OAuth on hr-devs PROD-LLM; hub as standalone image; raamatukoi VPS container; MCP visual-QA pattern; provider-outage behaviour; external-audit container spec.
- `[PO-17]` bridge via explicit `dns:` (3-command test in joosep design 2.4); `[PO-9]`; allerk README remaining ~35 sections only on a named question.
- Cal's entry restructure (permanence/causation sub-shapes) needs a NON-author read-back. `joosep_sshd` + `sm-state` have ZERO backups (one `docker run --rm -v <vol>:/state:ro … tar czf`).
- Base compose `x-team-base` build block still lacks `network: host` (S60 durable TODO).

## INFRA REFERENCE

- Stationmaster hub: `sm@10.100.136.162 -p 2222`; FR key `~/.ssh/sm_framework-research`; forced-command JSON endpoint.
- FR courier runs LOCAL on this Windows box from this repo's working tree (`teams/framework-research/poc/ghost-bridge/`); two gitignored configs (explicit + `.auto.json` via `restart-fr-courier-with-pid.ps1`).
- apex-research: RC `100.96.54.170`, ssh `ai-teams@ -p 2222` (registry row 2), exec route = `ssh dev@RC` then `docker exec apex-research`. Real build source = RC clone `/home/dev/github/apex-migration-research` (repo root `Dockerfile.apex`, `entrypoint-apex.sh`); `designs/deployed/apex-research/container/` is a MIRROR that drifts. Compose `network_mode: host`; image `apex-research-claude:latest` FROM `ai-teams-claude:latest`; volumes `apex-research_apex-{claude-home,research-repo,source-data}`.
- This box has NO docker. Any build/exec is RC = Hopper/PO.
