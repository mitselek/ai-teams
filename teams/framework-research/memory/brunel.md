# Brunel scratchpad

## Summary (lines 1-15 -- always read on startup)
- **[S55 SESSION-END -- PO HALTED THE CUTOVER (over-churn). CLI STAYS 2.1.177. All my work STAGED in the working tree, NOT committed (safe).]** PO called a halt: cutover coordination over-churned. No cutover commit this session; no further cross-agent coordination. Status going into next session: design + harness + wrapper + both configs are ON DISK (verified) but the at-unpin flip did NOT happen and the CLI is still pinned. The launch-override mechanism (line below) is the agreed cutover design when the PO re-greenlights -- nothing to redo, just resume the commit+install+validate sequence. My tracked commit set (restart-fr-courier-with-pid.ps1 + designs/new/migration-probe-harness/* + docs/migration-validation-probe-brief-2026-06-18.md) is staged-but-uncommitted; the two configs stay GITIGNORED/local. NO action needed from me at next boot until PO re-raises the cutover. [Detail of the FINAL agreed mechanism preserved verbatim in the bullet below.]
- **[S55 13:57 -- CUTOVER (DESIGN, halted before exec). Flip-timing FINAL = LAUNCH-OVERRIDE (Aen-approved). RESTORED + VERIFIED ON DISK.]** PO: cutover today -> commit tracked migration -> PO `npm i -g @anthropic-ai/claude-code@2.1.181` -> next session boot validates (rollback: reinstall 2.1.177 + the explicit config is the safe default). MECHANISM = LAUNCH-OVERRIDE (after a crossed-message swap where Aen+I briefly traded to each other's proposal then converged back): TWO config files (both GITIGNORED = local machine-only infra, on disk, NOT committed): `fr-courier.config.json` = PURE EXPLICIT = always-safe fallback (plain start-fr-courier.ps1 defaults here); `fr-courier.config.auto.json` = the "auto" variant, loaded ONLY by `restart-fr-courier-with-pid.ps1` (its -Config default). So "auto" is a config-FILE swap via the wrapper at Step 2.5 (2.1.178+), NEVER an in-place edit; courier stays UP between sessions on explicit (mail flows). VERIFIED ON DISK (ls'd, not claimed -- the 13:51 audit-gap lesson): both configs present + valid JSON, explicit is pure (no auto-keys), wrapper points at .auto.json (L53), both load through Config() (explicit resolves path; auto fail-fasts on this 2.1.177 box = EXPECTED, only resolves on 2.1.178+). [LESSON -- STRONG: my earlier "DONE+verified" was STALE -- I created .auto.json then DELETED it on a crossed pivot, never re-checked disk after. VERIFY-BEFORE-COMPLETION means re-confirm on disk AFTER any pivot, not claim from memory. Aen's pre-shutdown audit caught it.] MY TRACKED COMMIT SET (configs are NOT committed -- gitignored): restart-fr-courier-with-pid.ps1 + designs/new/migration-probe-harness/* + docs/migration-validation-probe-brief-2026-06-18.md. (Herald: stationmaster-courier.py. Volta: restore/persist.) Volta Step 2.5 = `./restart-fr-courier-with-pid.ps1` no-args (wrapper owns config+env+mechanism+V4-preflight). Herald precondition (FR sole live 2.1.178+ team) in both _unpin_notes + boot runbook. **[14:03 LATE COORDINATION:** (1) Aen ruled shared bridge-override env = `FR_COURIER_TEAM_DIR_NAME` (parallels FR_COURIER_SESSION_PID). Routed Herald a 1-line patch: Config "auto" branch `explicit_dir_name=raw.get("team_dir_name") or os.environ.get("FR_COURIER_TEAM_DIR_NAME")` (config-primary/env-fallback). NOT load-bearing for v1 cutover (mode-b=null+liveness); multi-team v2 convenience -- can land now or post-cutover (Herald/Aen call). Volta scripts pass --team-dir-name explicitly so decoupled from timing. (2) Volta wired Step 2.5 as MODE-(a) `-SessionPid <pid>`; I recommended MODE-(b) bare (no pid) for v1 -- FR sole team, drops the Step-2'-pid-emit seam; mode-a = v2 multi-team. His call as startup.md owner. (3) Pid-source confirmed consistent ($PPID=session pid).** CLOSING SEQUENCE -- holding for Aen's shutdown request.
- **[S55 13:29 -- earlier: bare-auto flip CAUGHT 12-team fail-fast (live shim test) -> reverted -> deferred to at-unpin. Superseded by the launch-override line above.]**
- **[S55 13:29 -- COURIER UNPIN-PREP (earlier): flipped bare-auto -> CAUGHT 12-team fail-fast -> reverted to explicit, deferred to at-unpin]** PO chose "prep unpin now". I flipped fr-courier.config.json to bare "auto" -> then CAUGHT (live shim test, verify-before-ship) that it FAIL-FASTS on this box: **12-team host, dirs are TeamCreate-NAMED on 2.1.177**, resolver's glob+liveness keys on `session-<id>` naming (a 2.1.178+ construct) so NO named dir matches the live session's slug -> rc=1 ambiguous -> restarting the live courier on it would CRASH at Config-load. HELD Hopper (twice; added a `--resolve-team-dir` dry-run as the missing gate -- gates 1+2 prove code-handles-auto+config-flipped but NOT that auto RESOLVES). Aen converged on the same finding + ruled: REVERT to explicit path now; the "auto" flip is COUPLED to the AT-unpin (2.1.178+) step, NOT pre-unpin (resolver already validated via shim+12/12, so no live pre-unpin round-trip needed). DONE: fr-courier.config.json reverted to "~/.claude/teams/framework-research/inboxes", valid JSON, auto-only keys removed, _unpin_note rewritten w/ the at-unpin plan. Live daemon (pid 38044, plain python.exe NOT Scheduled-Task this session -- Hopper finding, hold for the real at-unpin restart) was NEVER restarted into "auto" -> undisturbed, FR<->apex intact. Hopper stood down. Volta given matching guidance (pid-keyed on 2.1.178+, --team-dir-name override + fail-closed on the 2.1.177 bridge).
- **[S55 13:31 ROTATION FIX = DESIGN REQ (Aen elevated) -- coordinating w/ Volta; SOLVES both post-unpin items]** Aen's fix: new startup.md restarts the FR courier each session with FR_COURIER_SESSION_PID=<session pid> -> resolver picks FR's session-<id> via pid tiebreaker (handles BOTH multi-team-host disambig AND session-rotation staleness in one move). VERIFIED merged courier reads the env (stationmaster-courier.py L101 Config-"auto" + L1217 shim) -- courier side ready. SENT VOLTA exact invocation + 2 REAL GOTCHAS caught verifying the LIVE launcher: (1) PowerShell-not-bash: start-fr-courier.ps1 uses Start-Process which does NOT inherit a per-invocation env -- must `$env:FR_COURIER_SESSION_PID=<pid>` BEFORE the start call (child inherits parent PS env). (2) needs the CLAUDE-SESSION pid (the one w/ sessions/<pid>.json), NOT the PS launcher $PID/script pid -- Aen's "$$" = right-intent/wrong-process on Win/PS; told Volta have Step 2' emit the session pid (single source of truth) + feed to restart. WROTE `restart-fr-courier-with-pid.ps1` (TASK #11, owner=brunel; teams/framework-research/poc/ghost-bridge/) -- ONE call: `-SessionPid <claude-pid>` -> fail-closed pid check (sessions/<pid>.json must exist) -> $env:FR_COURIER_SESSION_PID -> stop-fr-courier.ps1 -> start-fr-courier.ps1. Encapsulates 3 gotchas (PS env-inheritance, script-pair-not-Scheduled-Task, pid-fail-closed). Verified structurally (scripts exist, -Config accepted, ordering); on-target proof = at-unpin restart. NEEDS AEN COMMIT. OPEN SEAM: Volta's Step 2' must emit the CLAUDE-session pid -> pass to -SessionPid (flagged him). Placement AFTER Step 2' Discover (V4: sessions/<pid>.json at interactive-ready). **HOPPER MECHANISM FACT (folded into wrapper):** live FR courier = directly-launched python.exe pid 38044 via start-fr-courier.ps1; Scheduled Task `FrameworkResearch-Courier` = DORMANT (Ready, LastRun Jun15), NOT the live launcher -> wrapper uses the SCRIPT PAIR not the Task. (Durable: FR courier on this dev box = script-pair-launched; Task is a dormant secondary. SUPERSEDES the S54 Scheduled-Task-relaunch assumption.) Hub baseline clean (grants apex-research+fr-test, no stuck mail). LESSON: verify-before-ship caught the multi-team crash + verifying the live LAUNCHER caught the env-inheritance + pid + mechanism gotchas -- test the actual substrate AND the actual scripts, not the doc.
- **[S55 13:42 STRATEGY CONVERGED ON (b) -- wrapper -SessionPid OPTIONAL + V4 pre-flight; ONE Aen decision pending]** Aen recommended v1 cutover = OPTION (b): bare "auto"+null + PLAIN restart-at-session-start, NO pid. KEY: my V3 process-liveness fix IS what makes bare auto resolve FR's live dir w/o a pid (named-2.1.177 dirs filtered + FR's OWN accumulating stale session-<id> dirs filtered by dead-pid + FR's CURRENT session-<id> = the one live match). Eliminates the Windows-which-pid problem for v1. I CONCUR + made -SessionPid OPTIONAL (mode-b=no pid, mode-a=pid for v2 multi-team) + added a V4 PRE-FLIGHT --resolve-team-dir dry-run that ABORTS-without-touching-courier if resolution fails (too-early/V4-window) -- self-protecting (verified: fires correctly on this 12-team box). MY ONE REFINEMENT to Aen: a RAW stop/start (no wrapper) DROPS the V4 guard -> mistimed restart CRASHES the courier. So v1 should call my wrapper mode-b (`./restart-fr-courier-with-pid.ps1` no args = plain restart + V4 guard, no pid path) NOT raw scripts. PENDING AEN: mode-b-wrapper vs raw-scripts-with-guard-moved-into-start-fr-courier.ps1 (both honor (b); only Q = where the V4 guard lives; I lean wrapper, already built+verified). On his ruling I tell Volta the exact v1 call. NEEDS COMMIT (wrapper). #11 in_progress.
- **[S55 13:19 -- PROACTIVE CATCH: --resolve-team-dir CLI shim MISSING from merge]** Verified: merged stationmaster-courier.py has `resolve_team_dir` FUNCTION (L1045) but NOT the `--resolve-team-dir` __main__ shim -- main() (L1187) wires only --config/--once/--ping. Volta's restore/persist scripts (WS2 T2/T3) need `SLUG=$(stationmaster-courier.py --resolve-team-dir --name)`. Small add: --resolve-team-dir branch in main() -> resolve_team_dir + print dir/--name=slug + exit0/stderr-nonzero, AND --config not-required when --resolve-team-dir given. PROPOSED PATCH routed to Herald (his file). Flagged Aen + Volta (gave Volta the contract to code in parallel: handle non-zero exit + mkdir -p inboxes/). NOT blocking my courier flip (inboxes_dir:"auto" via Config IS wired). IS on unpin path via WS2 -- must land before CLI flip. [TASK #10.] Holding for Herald merge-or-ack.
- **[S55 13:21 SUBSTRATE FACT -- FR courier runs LOCAL]** Aen: FR courier runs LOCALLY on this Windows dev box FROM THIS REPO's working tree (`teams/framework-research/poc/ghost-bridge/stationmaster-courier.py`) -- NOT a remote rc-host clone. Commits FROM this tree (0a79b44, 00510b6) are immediately on-disk where the courier imports them -- NO git pull (that's the rc-host-clone pattern = apex/probe, DIFFERENT substrate). DISTINCTION: FR courier = local-working-tree (this box); apex/probe couriers = rc-host clones (pull pattern) -> different verify-gates. Amended Hopper dispatch gate-1: keep grep defense, drop "pull if stale" (grep-miss = working-tree/committed mismatch I'd diagnose, not a pull).
- **[S55 2026-06-18 -- WS1 of #86 ACCEPTED by Aen; now ON HOLD for Herald]** Design = `teams/framework-research/docs/courier-runtime-team-name-discovery-design-2026-06-18.md`. ACCEPTED -- Aen praised the _outbox_to_team exclusion, fail-fast posture, "auto"-sentinel back-compat. GROUND TRUTH: ONLY fix point = `inboxes_dir` resolution in `Config.__init__` (`stationmaster-courier.py:88`); `_outbox_to_team` is NOT a fix point (resolves REMOTE dest team). Hardcoded `framework-research` is in the config JSON, NOT Python. Fix `inboxes_dir` ONCE -> both inbound + outbound (:388) migrate. DESIGN: Approach A (glob `teams/*/config.json` `.name`) = CANONICAL; B (`sessions/<pid>.json`) = TIEBREAKER ONLY (courier ≠ session process, doesn't own session pid; detached under Scheduled Task). Disambig order: 1 dir->use; 0->fail-fast; multiple->pid-tiebreaker/liveness-filter-via-sessions/`team_dir_name`-override/else fail-fast. BACK-COMPAT: `inboxes_dir` optional ("auto"); explicit path = rollback; land code BEFORE unpin, flip config AT unpin. Shared primitive: standalone `resolve_team_dir(claude_home,*,session_pid,explicit_dir_name)->Path` + `--resolve-team-dir` CLI shim. NB: `stationmaster-courier.py` is HERALD's file -- I propose patch, HE integrates; sequence the patch WITH him, do NOT push myself.
- **[S55 PROBE V1-V5 COMPLETE on 2.1.181 -- migration VALIDATES with 1 must-fix (drafted) + 1 open risk]** Findings = `docs/migration-validation-probe-findings-2026-06-18.md` (Hopper). **V3 LOAD-BEARING, overturned my guess WORST-CASE:** sessions/<pid>.json NOT GC'd on exit (graceful AND kill -9); dead entry LINGERS `status:"idle"` = IDENTICAL to live idle. `status` CANNOT distinguish dead/live; no dead/exited/stopped status exists. My `_has_live_session` status-allowlist was BROKEN. **MUST-FIX DONE+VERIFIED:** rewrote `_has_live_session` -> PROCESS-LIVENESS (`os.kill(pid,0)` on `pid` field, procStart-vs-/proc/<pid>/stat-field-22 PID-reuse guard) in staged resolver; py_compile clean; unit-tested `_pid_alive` (own-pid->True, dead-pid->False, garbage->False). HONESTY: logic verified on Windows but os.kill is POSIX -> END-TO-END validation = harness RE-RUN on Linux. PATCH PROPOSED to Herald for stationmaster-courier.py (his file, WS1 §6.2). OTHER: V1 PASS (resolver glob+inboxes_dir:auto->discovered dir, core mechanism sound). V2-pid PASS; V2b glob+liveness was broken (V3 coupling), now load-bearing post-fix. V4 config-FIRST (early-discovery window real -> Herald WS2 §4.2). V5a/P4 PASS (members[]-injection survives 2.1.181). **V5b/P6 = THE OPEN RISK: INCONCLUSIVE-leaning-NEGATIVE** -- external-write-wake didn't fire 35-90s (S54 woke ~15s); confound = headless docker-exec pane, focus-events off, never attached; needs ATTACHED-pane re-test OR courier-poll-loop-fallback (courier ALREADY has a poll loop -> may be non-blocker by design, I'll confirm in patch). WS3 UNPIN GATE = must-fix lands + V5b resolved.
- **[S55 POST-PROBE -- must-fix MERGED+LINUX-VALIDATED (Task #7 CLOSED), harness fixes done, V5b scope-widened]** (1) MUST-FIX: V3 forced liveness=process-not-status. **Herald MERGED it into stationmaster-courier.py (L927-1068) -- VERIFIED correct. LINUX-VALIDATED: Hopper ran test_pid_liveness.py on rc host -> ALL 12 PASS exit 0 (live->True, dead-but-status:idle->False = drops the V3 lingering shape; procStart guard fires). Per Aen the unit-test SUFFICES, no full re-run/2nd OAuth. Commit gates both met (brief + green test); commit set ready.** [LESSON -- STRONG, S55] my proposed `_pid_alive` was LINUX-ONLY (os.kill+/proc); the LIVE courier runs on WINDOWS (Scheduled Task) -> would've BROKEN production liveness. MY ERROR: drafted against the THROWAWAY staged reference (no Windows path) instead of cross-reading the REAL stationmaster-courier.py -- a read-your-own-deployed-artifacts lapse. Herald cross-read the real file, caught it, wired it RIGHT: cross-platform `_pid_alive` (Windows tasklist conservative-on-can't-tell / POSIX os.kill) + procStart guard as a STRICTLY-NARROWING Linux-only OPTIONAL layer that degrades on Windows/`/proc`-fail (never flips conservative-alive->dead on transient error). I SYNCED the staged resolver to mirror it (so harness validates production-equiv logic). DISCIPLINE: when proposing a patch to a deployed file, READ THE REAL FILE first, not the throwaway reference -- the reference can lack a platform path the production file has. (2) 3 HARNESS drive-isolation fixes DONE+verified (order V1->V5->V4->V3->V2; V2 non-empty+fail-loud asserts; V4 is_new poller +60s window). (3) Cal V3-gotcha `sessions-pid-json-not-gc-status-idle-lingers` filed + decision read-back DONE. (4) V5b: courier poll-loop = NON-BLOCKER for COURIER correctness (#8 closed, re-test deferred to #9 RfC-scope). **P6 SCOPE FULLY RESOLVED (Herald 12:57):** Step-3 (restore-inboxes) = session's OWN startup script writing files while ACTIVE/mid-startup -> P4-class active-session delivery (V5a CONFIRMED on 2.1.181), NOT P6 idle-proactive-wake. So Step-3 rides P4, never depended on P6. VERDICT: P6 gates NOTHING in the unpin path (courier poll-loop non-blocking + WS2 Step-3 active-restore/P4 non-blocking). P6 only bites teamless-courier RfC's bare-idle-wake = caveated, DEFERRED #9 (RfC-scope, NOT unpin). **[SUPERSEDED 13:07 -- see reopen bullet below: a merged-file bug REOPENED #7; WS3 NOT clear yet]** (5) brief run-order correction added. COMMITS NEEDED: staging/resolve_team_dir.py (re-synced) + test_pid_liveness.py + test_pid_liveness_merged.py + harness.sh + lib/checks.sh (preflight de2f24e already in). [13:03 Herald wants belt-and-suspenders LITERAL-merged-file run (evidence-by-execution > equivalence for a production unpin): wrote staging/test_pid_liveness_merged.py -- importlib-by-path imports _pid_alive/_has_live_session from the ACTUAL merged stationmaster-courier.py (the module-load = the integration check the staged copy can't do) + same 12 assertions. Routed to Hopper (Tier R, no OAuth, throwaway tempdir). On "ALL PASS (merged file)" #7 = closed-BY-EXECUTION not inference. Herald FINAL Step-3 verdict: SEED-THEN-PROCEED branch (script writes inboxes while session ACTIVE, then reads as live = P4) -> P6 non-blocking, UNPIN=GO lifecycle-side. Herald merge scope = resolver+_pid_alive+auto-sentinel ONLY (InstanceLock PARKED per Aen cond-3).]
- **[S55 13:07 -- #7 REOPENED, merged-file run CAUGHT A REAL BUG. DO NOT UNPIN.]** Herald's belt-and-suspenders literal-merged-file run (Hopper) found a crash the equivalence-argument missed: merged stationmaster-courier.py `_pid_alive` (L943) opens `if pid <= 0:` with NO None/non-int guard -> `_pid_alive(None)` raises TypeError. 8/12 PASS then CRASH on assertion-9. IMPACT: a sessions/<pid>.json with null/malformed `pid` (partial write/corrupt) crashes the liveness filter -> resolver crash -> courier can't resolve team dir -- the exact messy-sessions/-dir case the V3 fix must survive. ROOT CAUSE: my STAGED resolve_team_dir.py HAS the guard (passed 12/12); the MERGE LOST it. MY MISS (sharpened lesson): my "byte-identical" diff checked the load-bearing FIELD-22 procStart logic but NOT the function HEAD -- "byte-identical on the part I diffed" != byte-identical. Evidence-by-EXECUTION (run the literal artifact) beats evidence-by-EQUIVALENCE-argument; Herald's insistence on the literal-file run is why this didn't ship. FIX: type-guard to TOP of merged _pid_alive before `<= 0`. **HERALD PORTED IT 13:12** (root cause: caller `d.get("pid",0)` returns 0 only for MISSING key; explicit JSON null -> None -> `None<=0` crash; guard-in-function defends every caller). Herald re-validated Windows 11/11 + 4/4 InstanceLock-no-regression + 17/17 integration. **I VERIFIED the patch on the LITERAL merged file (importlib-load, Windows): module clean + all 5 crash cases (None/abc/None+procstart/0/-1) -> False, no TypeError.** Routed Linux re-run to Hopper (test_pid_liveness_merged.py vs PATCHED courier; told him transit the CURRENT patched file NOT the buggy 56199B, grep guard first) -> on 12/12, #7 closed BY EXECUTION on the literal production file. **#7 CLOSED BY EXECUTION 13:11:** Hopper re-ran vs the PATCHED literal merged courier (56760B, guard-grep verified) on Linux -> ALL 12 PASS exit 0 (None + non-numeric now green; /proc procStart match/mismatch + PID-reuse pass; V3 pair live->True/dead-idle->False holds). CROSS-PLATFORM COMPLETE: Herald Win 11/11+4/4+17/17 + my literal-file guard-check + Hopper Linux 12/12, all vs the production file. **WS3 UNPIN PRE-GATE FULLY CLEAR** (#7 done, #8 non-blocking, P6/Step-3 non-blocking, harness done). REMAINING = Aen's config-flip 2.1.177->2.1.181 (#3) + commit (7 files: harness.sh + lib/checks.sh + brief + staging/{resolve_team_dir,test_pid_liveness,test_pid_liveness_merged}.py + Herald's stationmaster-courier.py diff incl entry-guard; InstanceLock parked). [LESSON: "byte-identical on the part I diffed" != byte-identical -- diffed field-22 procStart logic, MISSED the dropped entry-guard at the function HEAD. Evidence-by-EXECUTION beats equivalence; my own staged copy HAD the guard = run-don't-argue.] Probe->design->validate->catch-merge-bug->re-validate loop COMPLETE. Standing by for unpin exec or session close-out.
- **[S55 preflight guard -- ADDED, committed de2f24e per findings doc]** `phase_preflight` df -P / >= HOST_DISK_ABORT_PCT(90) abort, after the run-1 disk-full abort. (Run 1 aborted 11:55 host root 100% full; PO freed ~13.6G -> 76%; run 2 = the V1-V5 above.)
- **[S55 Herald coordination CLOSED]** OQ1 resolved false-conflict (one resolver, caller-relative: his lifecycle passes session_pid -> pid-first; my courier omits it -> glob+liveness; glob canonical). His WS2 doc = `docs/lifecycle-rework-implicit-teams-2026-06-18.md` (startup Step2 Create->Step2' Discover; shutdown S5 Release DELETED = process-exit IS release; stale-dir sweep = Brunel/Volta follow-up, NOT unpin-blocking). V2/V4 CONFIRMED his shapes. Added `--name` to staged resolver (bare slug for his T1/T2/T3 bash; default=full path for courier) -- verified, synced to brief Appendix A. His pgrep fidelity note FOLDED into lib/checks.sh V2a (pgrep|head is a probe stand-in for the caller's-own-pid; don't canonize it). His Section 3 unblocked; both docs ship.
- **[S55 POST-PROBE SEQUENCE -- carry]** When V1-V5 land green: I propose the real resolve_team_dir patch -> Herald merges into stationmaster-courier.py (his file), keying _has_live_session on V3's ACTUAL status finding (replaces the dead/exited/stopped GUESS). Cal stage-2: gotcha CONFIRMED, decision PARTIAL (blockquote enriched w/ glob-primary ranking + liveness-filter + WS1-doc pointer, folds at Hopper read-back).
- **S54 (2026-06-17) -- Task #4 (2.1.178+ implicit-teams probe) CLOSED. Built + drove all 6 probes + clean teardown. THE MIGRATION VERDICT:**
  - **P1 FAIL (load-bearing):** `team_name` is NOT honored on disk -- on-disk team name is `session-<id>`, the Agent-tool `team_name` arg is IGNORED. => FR's courier HARDCODED path `~/.claude/teams/framework-research/inboxes` BREAKS on 2.1.178+; courier needs **runtime name-discovery** (glob/read the actual session-<id> dir) before we can unpin.
  - **P2/P3/P4/P5 PASS:** SendMessage exists+delivers, members[]-injection still works, inbox-file-write still wakes a session, persistence holds. The injection+delivery PRIMITIVES survive 2.1.178+.
  - **P6 PASS (PO ideal):** external inbox-write proactively WAKES a bare session (no spawn, no member registered) -- a lone session is reachable with zero setup.
  - **NET:** only the hardcoded team-NAME breaks; every delivery/injection primitive our courier relies on survives. Migration is viable IF the courier switches to runtime team-name discovery. Findings doc + ops-log = local on Hopper's side for Aen's commit.
  - **Artifacts (designs/new/teams-migration-probe/, in main):** Dockerfile.probe + docker-compose.probe.yml + entrypoint-probe.sh + README. Auth ended = option (c) fresh `claude login` over tmux (no key, no creds copy; probe ~/.claude empty -> own throwaway token). Drive = host-net + WARP CA + NO sshd + `docker exec` tmux (Hopper's on-host throwaway divergence; NOT pushed). Canonical Dockerfile uid-1000 fix pushed @ ed1fe3d.
  - **My role:** authored+validated all artifacts, resolved every auth/net/CA flag, authorized final config; could NOT build (no rc-host docker access -> Hopper built+drove). Empirical wins this task: apex auth is OAuth-creds-file NOT API-key (compose env was a dead var); apex on 2.1.110 can't report 2.1.178.
  - **Gotchas filed -> Callimachus:** (1) WARP 2nd-container: bridge can't egress + host-net collides sibling ports (`ports:` remap is a no-op under host-net) -> host-net + docker-exec drive. (2) ubuntu:24.04 ships stock `ubuntu`@uid1000 -> `useradd -u 1000` fails -> `userdel -r ubuntu` first. (3) [earlier] scheduled-task courier restart vs pid-file stop-script.
  - [DEFERRED -> framework finding] courier runtime team-name-discovery design (replace hardcoded framework-research path with session-<id> glob) -- prereq for unpinning off 2.1.177. Owner TBD when FR schedules the migration.
- **S54 (2026-06-17) -- Task #2: FR `-courier` flip COMPLETED (round-trip GREEN end-to-end, #2 marked done).** Full loop proven: outbound deposit (accepted, no E_NOGRANT) + apex-collect (deposited_uncollected={}) + inbound ACK (apex@15:36, SEQ=FR-S54-COURIER-FLIP-1, tagged sender `apex-research-courier` NOT -ghost = Herald's rewrite_attribution flip live). Reply routed to team-lead = default_inbox fallback (apex addressed the team), expected. Live config + example config both on -courier; backwards-compat (-bridge still stripped) intact. Restart-tooling gotcha submitted to Callimachus. One daemon (pid 38044), task Ready.
  - [DONE] PREP edit: fr-courier.config.json ghost_outboxes -> ["apex-research-courier"] (was apex-research-bridge). No live impact until daemon restart.
  - [LEARNED-S54] Grants are TEAM-keyed, NOT channel-name-keyed. Flipping outbox -bridge->-courier does NOT touch consent (hub status: grants_out still has apex-research). And at prep time deposited_uncollected={} + waiting_for_me={} = clean flip window, no queued mail a rename could strand.
  - [GOTCHA-S54] Live outbox the courier watches = `apex-research-bridge` (config-MEMBER name, file = inboxes_dir/<name>.json). The visible `apex-bridge.json` is STALE/legacy (last real content 2026-06-12), NOT the live outbox -- don't confuse them.
  - FLIP PLAN: (1 done) config; (2) register apex-research-courier ghost member + empty inbox; (3) stop-fr-courier.ps1 (drains) then restart scheduled task; (4) verify one daemon + ping + log shows new outbox; (5) E2E SEQ-tagged round-trip to apex-research-courier; (6) ROLLBACK = restore -bridge config+member+restart. Mark #2 done ONLY after green round-trip.
  - [GATE 2026-06-17 13:20] Aen ACKED prep+plan (steps 1-6 APPROVED as written). Doc seq: example-config sync = MINE post-green-round-trip; inter-team-comms skill doc = Aen owns (no action from me).
  - [FLIP EXECUTED 2026-06-17 15:32 BOTH GATES CLEARED] Herald task#1 closed (--ping green, integ-test 17/17, strip -courier-then-bridge, 4 gates). Aen go. Steps 2-5 DONE: registered apex-research-courier member+inbox; daemon restarted (pid 38044, log "courier up ... outboxes=['apex-research-courier']", one daemon, task Ready); E2E SEQ=FR-S54-COURIER-FLIP-1 -> log "outbox apex-research-courier: consumed 1 entry" + "deposited 1/1 (accepted)"; deposited_uncollected={} (apex collected). OUTBOUND GREEN. example-config synced to -courier. AWAITING apex ACK (inbound leg) to close round-trip + mark #2 done.
  - [GOTCHA-S54 -> Callimachus] stop-fr-courier.ps1 / start-fr-courier.ps1 manage the PID-FILE lifecycle; the LIVE daemon runs under the SCHEDULED TASK (run-courier-hidden.vbs) which writes NO pid file. So stop-script finds nothing, its --drain-once is refused by the live daemon's lock. To restart the task-managed daemon: Stop-ScheduledTask (kills wscript launcher; detached python may outlive it) -> hard-kill the daemon pid directly -> Task Scheduler relaunches fresh on new config. Also: lock false-refuse hit me (lock pid aliased a LIVE process) but it was actually the relaunched daemon legitimately holding it -- distinguish "stale lock aliasing unrelated pid" from "live daemon correctly holding lock" by checking the pid's COMMANDLINE is fr-courier-daemon.
- **Current state (prior):** S53 (2026-06-16) CLOSED. CCR (Coordinated Container Rebuild) protocol SHIPPED+MERGED to main (0db208c); apex reference instance #1 reviewed + proven. My S53 role = deploy-surface specialist: built the typed contracts + manifest validator, reviewed apex PR #166 twice, did the read-only persistence gap analysis, shipped the durable-logs fix, reviewed Hopper's ccr-rebuild.sh. [S52 apex container hardening COMPLETE+PROVEN carried forward below in gotchas.]
- **WHAT I SHIPPED THIS SESSION:** (1) `types/t10-ccr-contracts.ts` (CcrManifest + RebuildReport typed contracts) + `teams/framework-research/poc/ccr/validate-manifest.ts`(+`.test.ts`, 5/5 TDD) -- commit 07f6fcc. (2) Durable per-service logs fix: `supervise()` redirect to `~/.claude/logs/<name>.log` + boot mkdir, on apex PR #165 branch `ce181ed6` AND FR design copy `ba58c72`. Used a WORKTREE (`git worktree add ../apex-ccr-logfix fr/apex-container-hardening-s52`) because the apex clone's `main` was DIRTY (344 behind, 5 uncommitted #165-leftover files) -- left it 100% untouched.
- **REVIEW VERDICTS (all read-only, reported to team-lead):** apex PR #166 first pass = DIFF CLEAN, 6/6 checklist, HIGH-RISK, blocked only on preconditions. #166 delta (11 paths now: +`.credentials.json` +`projects/`, quiescence simplified) = APPROVED, both new tags correct/volume-resident. Hopper's `ccr-rebuild.sh` = APPROVED (3 minor hardening notes; hard guards real: no down -v / no git clean / tag-before-build / ff-only / abort-on-dirty-TRACKED).
- **GAP ANALYSIS RESULT (read-only, #165 entrypoint vs #166 manifest):** persistence SOUND -- 7/8 then 9/9 stateful paths COVERED, either on the `apex-claude-home` persistent volume (`~/.claude/*`) or seed/restore in the entrypoint (`~/.ssh` key from build-secret seed Step-7b; known_hosts re-pinned Step-7c; sshd host-keys on volume Step-6c). settings.json+mcp.json already safe (volume + guarded `[ ! -f ]` create). Only real gap was durable output -- now fixed.
- **REMAINING CHAIN:** merge #165 -> #166 clears -> apex QUIESCENT + PO go -> Hopper rebuilds (via ccr-rebuild.sh). NOTE apex dirty main (the 5 #165-leftover files) flagged to PO; non-blocking, user sorts it.
- **FRAMEWORK FINDING (Cal-worthy, MINE to draft, team-credited [me=containerization+the split / team-lead=.claude.json instance / Hopper=st_dev reads]):** container hardening must INVENTORY EVERY ephemeral-home path the agent depends on (`~/.ssh` keys, `/etc/ssh` host keys, `.claude.json` config, courier lock/state). Fix-shape SPLITS by data type: generate-able (keys → generate-if-absent on persistent vol) vs stateful (config → restore/symlink, preserve content). 6 casualties this session = the teeth. Draft when Cal spawns. ALSO draft: set-e-supervisor, persistent-lock-container-instance-discriminator (boot_id is host-scoped, use PID-1 starttime), force-recreate gotchas.
- **[LESSON -- STRONG, S52] probe-beats-inference cuts BOTH ways:** my FIRST supervise() repro was UNFAITHFUL (omitted entrypoint `set -e` + terminal `exec`) → false "sound" verdict; Hopper's clean in-container kill overturned it; faithful repro + LIVE test nailed the real fix. Refused to pre-bake a fix (`setsid`) I couldn't verify on-target (no gosu/setsid locally); corrected team-lead's exec-orphaning hypothesis w/ 3 discriminating repros (avoided needless tini/s6); held a flattering "verified" until a teammate's contradicting word was explicitly reconciled. Faithful-repro + verify-before-completion (incl. proposed fixes) + live-substrate-authority + don't-close-over-an-unreconciled-contradiction.
- **[LESSON -- STRONG, S52] build-source has TWO layers:** FR dev-box clone I edit vs rc-HOST clone the image builds from. Verify edits are ON THE BUILD HOST (grep rc-host source) before declaring build-ready -- I checked LOCAL disk, missed the rc host lacked my edits; Hopper's pre-flight grep gate caught it. Also: live rc working tree carried uncommitted operational edits (GH_TOKEN) invisible upstream → pre-build branch-vs-rc diff catches the class.
- **[STANDING] role-split held all session:** git→team-lead (I never push); hub register/deposit + substrate writes→Hopper; courier.reference.py→Herald; I design+apply entrypoint/Dockerfile/compose in my MAY-WRITE + route everything outward-facing. Never executed against the deployed substrate myself.
- **[LESSON -- carry] read-your-own-deployed-artifacts before diagnosing** (designs/deployed/<team>/container/* = Layer-1 lineage; runtime truth needs live introspection). And: an artifact's prose must describe what it DOES, not what it should -- empirical-probe beats artifact-inference.
- **[METHOD -- S53] CCR persistence gap-analysis (reusable for any container team's rebuild review):** for EACH stateful path a manifest asserts, classify it COVERED-by-VOLUME / COVERED-by-SEED-or-RESTORE / GAP. Three inputs: (1) `docker-compose.yml` volumes = what survives rebuild for FREE (apex: `apex-claude-home`→`~/.claude`, `apex-research-repo`→`~/workspace`, `apex-source-data`→`~/source-data`; NOT volumes = `~/.ssh` + `$HOME` root); (2) the entrypoint's seed/restore/generate-if-absent steps = what's re-provisioned each boot; (3) the manifest's persistent_paths = the assertions. A path on no volume AND with no entrypoint restore = the real gap. Caught: the only S53 gap was durable per-service OUTPUT (supervised svc stdout → shared docker-logs, not a per-service file) -- fails CCR review item 5. Everything else was already covered.
- **[LESSON -- S53, STRONG] worktree-isolation for a dirty foreign clone:** when told to edit a branch in a clone whose working tree is DIRTY (uncommitted files you didn't create), do NOT stash/discard/checkout-over them. `git worktree add ../sibling <branch>` gives an isolated checkout of the branch, leaves the dirty main 100% untouched; edit+commit+push there, then `git worktree remove` + `git worktree prune`. The apex clone's `main` had 5 uncommitted #165-leftover files on a 344-behind main -- worktree let me ship `ce181ed6` to the #165 branch without touching them. Right default: surface the dirty state to the tasker, propose discard-vs-worktree-vs-operator-clean, let them pick (they chose worktree). Don't improvise destructive cleanup on files you didn't author.
- **[LESSON -- S53] match the FILE's local style, not a global rule:** the apex-repo `entrypoint-apex.sh` uses em-dashes in comments; the FR design copy (mitselek repo) uses ASCII ` -- `. I matched EACH file's existing convention for the same logical edit (em-dash in apex copy, ASCII in FR copy) -- "write code that reads like the surrounding code" beats a one-size repo rule when syncing the same change across two repos with different house styles.
- **[LESSON -- S53] commit only what you touched:** the mitselek repo had `team-lead.md` dirty (linter/team-lead edit) alongside my entrypoint edit. Staged ONLY the entrypoint file for my commit; left team-lead.md alone. Atomic commit = my change, not whatever else happened to be in the working tree.

---

## STANDING DECISIONS (carry forward)

[DECISION] Single-provider is correct default for agent runtime. Multi-provider = sidecar, not peer. Three integration seams: peer (Claude-only), sidecar/daemon, MCP server. Audit independence = external container reading committed git artifacts, NOT different-provider Medici.
[DECISION] `GatewayPorts yes` on RC NOT recommended even long-term -- would expose ports to every bridge-networked container. Loopback-only binding is the default; host-networking is the consent mechanism.
[PATTERN] `network_mode: host` simplification window: tunnels, sockets, local listeners free for the container. Tradeoff: breaks E-deployment portability (Swarm cannot host-network). Plan `b-host` vs `e-swarm` profiles in compose from day one.
[PATTERN] Author attribution: bold `(*FR:Brunel*)` per common-prompt. Never italic.
[DECISION -- S50, Aen-ratified Protocol-A-grade] Standby-agent hot-fix discipline. Fix-then-flag correct ONLY when ALL FOUR hold: (1) artifact in active deployment + known time-pressure; (2) fix in MAY-WRITE domain, bounded, no protocol/design surface; (3) flag immediately w/ reasoning + verification; (4) explicitly on standby for exactly this failure class. Remove any → default reverts to surface-before-fix. Filed Cal `process/standby-agent-fix-then-flag-discipline`.

## CARRY-FORWARD GOTCHAS (all containers)

[GOTCHA] PO edits live files in parallel during a Brunel pass. ALWAYS re-read before each Edit batch -- Edit tool's "File modified since read" catches it. If read >1 message ago, re-read.
[GOTCHA -- S52] Rebuilt image w/ UNCHANGED compose config is NOT adopted by plain `docker compose up -d` (compose sees no config delta → leaves old-image container running). MUST use `--force-recreate`. (Cal-candidate.)
[GOTCHA -- S53] An apex clone on disk can be on a stale `main` (e.g. 344 behind origin) with uncommitted leftover files from already-pushed work. A `git diff <branch> -- <file>` against such a stale local main is SWAMPED by the unrelated commits between the two bases (lit up the whole dashboard tree), so it can't cleanly tell you "is this dirt safe to discard." Don't infer safety from a confounded diff -- use a worktree off `origin/<branch>` and leave the dirty clone alone.
[GOTCHA -- S53] CCR review item-1 validator: run via an IN-REPO temp `.ts` file (`npx tsx ./_check.tmp.ts`), NOT `npx tsx -e '<inline>'`. The inline-eval form's relative import (`./teams/...`) silently fails to resolve on this Windows/tsx setup and produces NO output -- a false-green if you read silent-exit as PASS. (I caught this; Monte fixed the checklist, commit 84264f8.)
[GOTCHA -- S52] Stationmaster hub `sm@10.100.136.162:2222` (key ~/.ssh/sm_framework-research) is a RESTRICTED FORCED-COMMAND endpoint, NOT a shell -- speaks structured JSON requests (`{"v":1,...}`); shell-style input → `E_MALFORMED`. `sm-register`/deposit are structured protocol requests (Herald owns the shape), not literal commands. Never improvise a mutating request vs the shared hub.
[GOTCHA -- S52] Single-instance lockfile on a PERSISTENT volume + pid-only staleness check = false-refuse across container recreate. PID namespace resets per container, so a recorded pid from a prior container can alias a live unrelated process → `os.kill(pid,0)` true → lock judged not-stale → process refuses to start. Needs a CONTAINER-INSTANCE discriminator, not just pid liveness. NB: `boot_id` is WRONG (host-kernel-scoped → same across containers on a live host, unchanged by recreate); use PID-1 starttime (`/proc/1/stat` field 22, parse from last `)`) = the container's own init, changes per recreate. (apex courier S52; Herald owns the courier.reference.py fix.)
[GOTCHA -- S52, STRONG] A `( while true; do svc; ...; done ) &` restart-loop in a script with `set -e` MUST `set +e` inside the subshell. errexit is inherited; the supervisor's job is to handle the service's NON-ZERO exit, but under errexit the first non-zero exit kills the loop subshell before it relaunches → restart-on-exit SILENTLY fails (restart-on-container-boot still works, masking it). Verified empirically (apex courier S52). Reproduce the REAL env (set -e + the entrypoint's terminal `exec`), not a simplified shape -- an unfaithful repro gives a false "sound."
[GOTCHA -- S52] Testing a supervisor's relaunch: kill ONLY the leaf service pid (`kill -TERM <leaf>` / `pkill -f <svc>`), NOT a group kill (`kill -9 -<pgid>`) or the subshell pid. A group/subshell kill takes the loop too → looks like relaunch-failure but is a TEST ARTIFACT.
[GOTCHA -- S52] Keygen baked into a build flow MUST be generate-if-absent (`[ -f key ] || ssh-keygen ...`), else every rebuild regenerates → pubkey churn → forced re-register. Key persists in the gitignored build-context on the (ext4/durable) build host; reused on subsequent rebuilds. Durability needs NO `git clean -x` in the rebuild flow.
[GOTCHA -- S52, CORRECTED S53] Ephemeral-home paths the agent depends on DON'T survive rebuild: `~/.ssh` (courier private key, hub known_hosts), `/etc/ssh` (sshd host keys). Fix-shape splits: KEYS → generate-if-absent (host-keys) or build-secret-seed→copy-each-start (courier key) on a PERSISTENT path; STATEFUL CONFIG → keep on a persistent volume + guarded `[ ! -f ]` create (never overwrite). Private keys live ONLY on persistent vol, NEVER baked into a layer.
[CORRECTION -- S53, supersedes the S52 `.claude.json` framing] `~/.claude.json` (at `$HOME` ROOT, ephemeral overlay) is a CONFIRMED NON-ISSUE, not a stateful-must-preserve path. It holds onboarding/trust state that REGENERATES automatically; nothing in the container depends on it persisting. The S52 ".claude.json ordering trap / preserve-not-reset" wording CONFLATED it with `~/.claude/settings.json` + `~/.claude/mcp.json` (and S53-added `~/.claude/.credentials.json`) -- THOSE are the real stateful files, and they live UNDER `~/.claude/` (the persistent volume) with guarded-create, so they were always safe. So S52 queued-fix #3 ("persist .claude.json") is OBSOLETE/NOT-NEEDED. Lesson: distinguish `~/.claude.json` (root, ephemeral, throwaway) from `~/.claude/*.json` (volume, stateful) -- one char of path difference, opposite persistence semantics.
[GOTCHA] WARP TLS interception: `network_mode:host` + `NODE_EXTRA_CA_CERTS=/opt/warp-ca.pem` + system CA.
[GOTCHA] Named volumes created as root → `chown 1000:1000` in entrypoint.
[GOTCHA] SSH: useradd creates locked account. Fix: `usermod -p '*'` for pubkey auth.
[GOTCHA] Container rebuild regenerates SSH host keys → persist them on the state volume (generate-if-absent, restore to /etc/ssh before sshd). DONE for apex (Step-6c, S52); pattern from the stationmaster hub.
[GOTCHA -- S50, OpenSSH 9.x] `ssh-keygen -A -f <prefix>` does NOT create `<prefix>/etc/ssh` and FAILS if absent -- keys silently don't land. Fix: generate the single ed25519 key directly with explicit `-f <path>`. Defense: smoke-test host-key assertion.
[GOTCHA -- courier, S51] ghost_outbox name resolves to team by stripping `-bridge`; MUST be `<registered-team>-bridge` or deposit hits E_UNKNOWN_TEAM and the consignment retain-loops in the spool. Spool re-resolves dest at DEPOSIT time from current config → a config-fix+restart auto-heals stuck entries.
[GOTCHA -- Windows daemon stop, S51] PowerShell Stop-Process == hard kill; Python gets NO SIGTERM/SIGINT → in-process drain + atexit do NOT fire on stop. Drain must be external: stop→wait-exit→`--drain-once`. (POSIX delivers the signal; only the Windows kill path is affected.)
[GOTCHA] CRLF from Windows git autocrlf breaks entrypoints. Fix: `sed -i 's/\r$//'` then rebuild.
[GOTCHA] Inbox files created at agent registration time. Specialist → unregistered agent = message LOST. Spawn order: service-role agents BEFORE message senders.
[GOTCHA] Base64-encode-via-SSH strips shell-escape backslashes. Use heredoc with single-quote delimiter for scripts with `\"` or `\s`.
[GOTCHA] Consecutive `**Bold:**` lines collapse on GitHub. Use `- **Bold:**` bullet lists.
[GOTCHA] Container reference-memory path: `~/.claude/projects/-home-ai-teams/memory/` ($HOME-dir-encoded), NOT `~/.claude/memory/`. Verify before citing.
[GOTCHA -- S34] Edit tool with multi-hundred-line `old_string` is fragile. For large compress-passes use Write to rewrite the whole file after surgical read of head + tail. (Used again for this S52 shutdown prune.)

## DEFERRED (future surfaces)

- courier-side CLI version re-validation (hub insensitive, courier sensitive -- S3 retention flip; apex image baked 2.1.162 / runtime 2.1.173 = version skew watch)
- OAuth on hr-devs PROD-LLM container (PO manual step)
- Hub container as standalone Docker image
- raamatukoi-dev VPS container deployment
- MCP server pattern for visual QA service
- Provider outage behavior in containers (what does Claude process do on API failure?)
- External audit container architecture spec

## INFRA REFERENCE

- Stationmaster hub: `sm@10.100.136.162 -p 2222`; FR key `~/.ssh/sm_framework-research`; host-key fp SHA256:CNcFjOxr...K13U (full line in FR `~/.ssh/known_hosts` `[10.100.136.162]:2222 ssh-ed25519 AAAA...DYVix`). Hub is a FORCED-COMMAND endpoint (structured JSON, not a shell). FR grants both ways with apex-research.
- apex-research container: hostname apex-research; tailscale LOGGED OUT → OLD 100.96.54.170 CGNAT STALE; live exec route = ssh `dev@` the rc HOST then `docker exec apex-research` (root inside) -- Hopper-confirmed, tailnet-independent. Entrypoint `/entrypoint-apex.sh` (MY artifact). Volumes (st_dev): PERSISTENT LVM 65024 = ~/.claude, ~/workspace, ~/source-data (+ now ~/.claude/ssh-host-keys/, courier state); EPHEMERAL overlay 78 = /, /tmp, ~/.ssh. apex's OWN sshd host key now PERSISTENT + STABLE (Step-6c): fp SHA256:z1/fXAE8...vA root@apex-research.
- Build source = `apex-migration-research/` repo ROOT (Dockerfile.apex + entrypoint-apex.sh + docker-compose.yml); compose is `network_mode: host`, NO cloudflared sidecar (live-confirmed). The rc HOST has its own clone -- edits MUST transit there (git, team-lead) before build; verify w/ grep on the rc-host source.
- Cloudflared tunnel: `526a23d1-...` → `apex-research.dev.evr.ee` → `http://apex-research:5173`. QUIC blocked → `--protocol http2`.
- Designs repo: `mitselek-ai-teams/designs/deployed/<team>/container/`. Prism (active): `~/Documents/github/.mmp/prism` ↔ `mitselek/prism` (PRIVATE).
