# decisions/ -- Card Index

11 cards. Each card is a ~30-line extractive summary (TLDR + Key ideas) of the full entry one level up at `decisions/<name>.md`. (*FR:Callimachus*)

**Stage-2-confirms gate** (#70): each card carries a `stage-2` field. decisions/ -- **9 confirmed, 0 partial, 0 pending, 0 legacy-unaudited = 9, MEASURED 2026-08-28** *[was an unstamped "8 confirmed" that had been true before `two-islands-by-design` was filed 2026-08-27; an unstamped count is read as current, so it was simply false. Re-measured and stamped.]* (single-source/authoritative-pointer = confirmed; stationmaster confirmed via Aen read-back 2026-06-12; the two S51 stationmaster decisions -- fan-out CR-4 + §4 text-field errata -- confirmed via Herald read-back 2026-06-15). courier-must-runtime-discover-team-name (filed-on-behalf of Hopper/Brunel 2026-06-17): Brunel read back 2026-06-18 → partial; **Hopper read back 2026-06-18 (S56) → confirmed** (both co-authors in; Brunel's refinement folded blockquote→body). The 2026-06-18 WS2 batch (filed-on-behalf Herald, issue #86) -- lifecycle-release-evaporates-under-implicit-teams + startup-create-collapses-to-discover -- both **confirmed via Herald full-entry read-back 2026-06-18**. Audit: `grep -rlE '^stage-2: (pending|partial)'\|grep -rlE '^stage-2: (pending|partial)' decisions/cards/`.

**2026-08-27 15:33 (+1 card, 8 -> 9; gate: 9 confirmed).** **`two-islands-by-design-hub-topology-follows-network-boundary`** (team-lead relaying the PO's #108 A1 ruling, **confirmed**, author-is-filer, pointer to `issuecomment-5439161208`): the two stationmaster hubs are islands by design -- EVR declines tailnet, so topology follows the network boundary; no federation (S49 no-relay unopened); **git repo = the inter-island bridge** (FR spawns on two boxes; both islands see the same commits); a1.1 optional opportunistic sagres membership on the home box, a1.2 required EVR-island formation. Resolves the same-day two-hubs gotcha's open question (two networks, honestly named) and instantiates the precondition-needs-an-owner remedy (startup host-check with an owner and a moment).

**2026-08-27 (0 cards added; 1 card amended; gate unchanged: 8 confirmed).** `stationmaster-post-office-model` amended (Herald, Protocol A, #108 amendment A5): rejected alternative 4 "Mail over MCP" is **superseded for the outbound leg** -- the deployed `comms` MCP `send` has deposited mail via an MCP tool call since #100 (2026-07-16); the S49 rationale survives for the inbound leg only (retained invariant: inbound never over MCP; courier owns collect -> inject -> ack). Secondary finding folded: the card's sanctioned MCP control plane (grant/revoke/status/registry) was never built. Amend-not-erase (original bullet kept, marked); herald added to `source-agents`; verified at `comms-mcp.py:146/317/342` and `protocols.md:72`. Cross-linked to the same-day two-hubs gotcha (`gotchas/singular-convention-plural-instances-enumerate-from-the-registry`), which the card's YAGNI exclusion of relaying turns into a partition.

| Card | Full entry |
|---|---|
| Codename "Lelle" -- the Gen-3 Inter-Team Comms Method for the EVR Island | [card](lelle-gen-3-evr-island-comms.md) · [full](../lelle-gen-3-evr-island-comms.md) |
| How the Truth Loop's Shape Tightened, v1 to v4 -- Two Questions That Dissolved Two Nodes | [card](truth-loop-shape-tightened-v1-to-v4.md) · [full](../truth-loop-shape-tightened-v1-to-v4.md) |
| Audit Independence Requires a Separate Container, Not a Provider Swap | [card](audit-independence-architecture.md) · [full](../audit-independence-architecture.md) |
| Courier Must Runtime-Discover the Team Name (drop hardcoded framework-research) | [card](courier-must-runtime-discover-team-name.md) · [full](../courier-must-runtime-discover-team-name.md) |
| Shutdown S5 (Leadership-Release) Evaporates Under Implicit Teams | [card](lifecycle-release-evaporates-under-implicit-teams.md) · [full](../lifecycle-release-evaporates-under-implicit-teams.md) |
| Startup Step 2 (Create) Collapses to Step 2' (Discover) | [card](startup-create-collapses-to-discover.md) · [full](../startup-create-collapses-to-discover.md) |
| Fan-Out Routing (CR-4): Per-Destination Outboxes, Normative v1 | [card](fan-out-routing-per-destination-outboxes-cr4.md) · [full](../fan-out-routing-per-destination-outboxes-cr4.md) |
| Pin Renderable-Body Field to `text` at §4 -- Clarifying Errata, No Bump | [card](text-field-pin-clarifying-errata-no-bump.md) · [full](../text-field-pin-clarifying-errata-no-bump.md) |
| Stationmaster: The Post-Office Model for Inter-Team Mail | [card](stationmaster-post-office-model.md) · [full](../stationmaster-post-office-model.md) |
| Two Islands by Design -- Hub Topology Follows the Network Boundary; Git Is the Bridge | [card](two-islands-by-design-hub-topology-follows-network-boundary.md) · [full](../two-islands-by-design-hub-topology-follows-network-boundary.md) |
| Token/Cost Tracking Is Out of Scope for Teams | [card](cost-tracking-out-of-scope.md) · [full](../cost-tracking-out-of-scope.md) |


> **[+1 CARD 2026-09-04, S73]** `truth-loop-shape-tightened-v1-to-v4` (**schliemann, `source-team: apex-research`**) = **pending**, breaking this subdir's all-confirmed run. Header 9 -> 10, **counted against files on disk**; gate now **confirmed 9 / partial 0 / pending 1 / legacy-unaudited 0 = 10**. **Filed in `decisions/` because the REJECTED ALTERNATIVES are the payload** and dissolved nodes have no other home in this wiki -- this is another team's design decision held as knowledge, not a copy of any artifact of ours, so the pointers-not-copies boundary is not in play. **Every one of the three revisions was subtractive; no version added a station.** Two stakeholder questions each dissolved a node without proposing a replacement. **The entry is an explicit Stage-1 relay fold** per `patterns/relay-to-primary-artifact-fidelity-discipline`: the v2/v3/v4 change lists are **primary-verified** from the playbook header, while **v1's baseline and the two PO questions -- the reason this material was singled out -- are `[FLAG] relay-only`**, and the artifact section 6 that would settle them **supersedes rather than corroborates** when someone who can fetch it reads it. A stated non-claim: the header says *two* design challenges and lists *three* version bumps; **the mapping is not inferred.** (*FR:Callimachus*)

> **[SECTION 6 FOLDED, SAME SESSION 2026-09-04 09:32]** Team-lead read the presentation artifact's full 283-line HTML and quoted §6 verbatim, closing **both** `[FLAG] relay-only` rows on `truth-loop-shape-tightened-v1-to-v4`. **Provenance recorded exactly rather than collapsed: primary-QUOTED by a reader who saw it, not primary-verified by the filer** -- a verbatim quotation can still be a quotation of the wrong section, so the chain is named. **Gate unchanged at `pending`, deliberately: supplying a source is not a read-back of the argument built on it.** Four things the fold changed. **(1) The mapping is now established** -- and it **narrows this entry's own claim**: challenges 1 and 2 dissolved the two nodes, but **a SELF-RUN adversarial pass, not a stakeholder, found the six inefficiencies**, so *outside review did the work* was too strong as filed. **(2) A credit corrected** -- *the retro produced the material, the challenges produced the shape* was filed as the librarian's reading and §6 shows the source team said it first (*"Encapsulating the loop exposed design debt in the retro shape"*); recorded as a restatement. **(3) TWO discrepancies between apex-research's OWN artifacts, recorded and deliberately NOT resolved** -- §6 names **three** challenges where the playbook header says **two**, and the header credits **v3** with *stumble-translation added* while §6's **v1 and v2 boards already draw that path**. **Found by putting two of their artifacts side by side: not an independent method, but a different one.** **(4) There is no v3 board** -- §6 draws v1, v2 and v4 only, so **the version carrying the two most consequential positional changes is the one the trail never drew.** The v2 board also shows the gate sitting **upstream of the register crossing** with `translate + PUBLISH` collapsed into one node, which means **no human saw the reader-facing text before publication at all** -- v3's repositioning introduces a review point v2 lacked, rather than choosing between two documents. (*FR:Callimachus*)

> **[+1 CARD 2026-09-04 10:0x, S73]** `lelle-gen-3-evr-island-comms` (team-lead relaying a PO ruling) = **pending**. Header 10 -> 11, counted against files on disk; gate at filing **confirmed 9 / partial 0 / pending 2 / legacy-unaudited 0 = 11**. **The gate call went against the submission's own label.** It was sent as *author-is-filer*, but **the decision's author is the PO, the filer is the librarian, and team-lead is a witness relaying it** -- filed-on-behalf, fail-closed by the three-bucket rule. **[GATE CLOSED SAME SESSION 10:53] Team-lead reviewed that call, accepted it, and read the entry back -- *faithful, no corrections*.** Sole `source-agent`, so the gate closes; **decisions/ re-measured: confirmed 10 / partial 0 / pending 1 / legacy-unaudited 0 = 11.** **Referent recorded rather than left implicit: the confirmed text is the entry at commit `ab622c7`** (md5 `c58e6fe2...`, 11341 B; card `06f478e4...`, 5632 B), verified against the commit and not a working-tree hash -- **the axis-2 remedy, since `stage-2` carries no version.** **The gate does not close the entry's two open items:** the Koidula collision and the one-token scope point are both with the PO, the latter also with Herald as a spec question (10:50). **A PO answer that changes scope is an amendment against the referent, not a re-run of this read-back.** Codename **Lelle** for the gen-3 EVR-island comms method **and its hub instance**, over Tapa and Koidula, **for disambiguation** -- "the hub" already means three things and this wiki carries that defect twice. Lineage verified against our own entries: ghost-bridge (retired) -> stationmaster v1.0.0 / `prod-llm` -> Lelle; **sagres out of scope.** Ledger **#116** confirmed OPEN via `gh`. **Three librarian additions the submission did not carry. (1)** The #107/#111 unowned-proposal finding was **verified, not relayed** (API counts + repo grep) -- and **#107's count changed under the measurer**, 0 early in the session and 1 twenty minutes later, so the zero-comment claim is stated **with both ends of its window**. **(2)** Ruled a **near-instance** of `precondition-without-an-owner-is-no-precondition` and **cross-linked without counting it**, on disjoint remedies, with a promotion condition written while the reasoning was in hand. **(3)** Two things raised for the record and not disputed: **the ruling assigns one token to both a method and an instance** -- the same shape as the ambiguity it fixes, harmless while one island runs one instance and offered to Herald as a one-sentence spec question -- and **`Koidula`, a rejected alternative, is already an agent persona name in 11 repo files**, offered without any inference about what the PO knew. (*FR:Callimachus*)

> **[CONTENT CORRECTION 2026-09-04 11:0x, S73]** `lelle-gen-3-evr-island-comms`: the island-membership line **struck, not erased**, and replaced with Herald's live registry read (spec §10.1, commit `22772b3`, read at source by the librarian). **The registry holds five names and neither Pepys nor Paunvere is among them.** **Pepys is an agent name in apex's playbook** -- reachable only as `pepys@apex-research`, and the agent part never reaches the wire. **Paunvere is not a registrant and runs no courier at all** (zero occurrences of `courier` or `stationmaster` in its whole deployment package). **Only `framework-research` ↔ `apex-research` is a live, fully-granted, Lelle-capable pair**, and apex's courier currency is `[speculative]` in the spec. **Operationally: for Paunvere "adopt Lelle" is stationmaster onboarding from nothing, not a courier bump -- schedule separately.** **The decision is unchanged; what was wrong was a descriptive line about who is currently reachable.** **This is the first change to this entry's CONTENT rather than its metadata, and it lands OUTSIDE the `ab622c7` referent** -- the 10:53 confirmation does not extend to it, it changes what a reader concludes about rollout scope, **so team-lead's re-read is owed and the referent must be re-stated against the new commit.** Struck rather than deleted because **a silently corrected membership list hides that anyone believed the wider one.** Gate figure unchanged pending that re-read. (*FR:Callimachus*)

> **[SECOND READ-BACK, REFERENT ADVANCED 2026-09-04 11:15, S73]** Team-lead re-read the `lelle-gen-3-evr-island-comms` island-line correction: **faithful, no corrections**, confirming both the registry table and the *onboarding from nothing, not a courier bump* pull-forward. **`stage-2` stays `confirmed`; referent advanced `ab622c7` -> `f914efd`** (entry md5 `220d3943...`, 16014 B; card `2a3967d8...`, 8103 B), **hashed from the blobs inside the commit rather than the working tree.** **The prior referent is kept as history, not overwritten** -- `ab622c7` covered the 10:53 read-back, the island correction landed outside it, and the 11:15 read-back covers `f914efd`. **Two read-backs, two referents, and the chain says which text each one covered.** The reason to keep both is the same reason the field needed a referent at all: **a referent overwritten on every amendment records only the latest state and loses exactly the history it exists to provide.** decisions/ gate unchanged and re-measured: **confirmed 10 / partial 0 / pending 1 / legacy-unaudited 0 = 11.** (*FR:Callimachus*)

> **[OPEN ITEM CLOSED IN THE SPEC 2026-09-04, S73]** The one-token-two-things point raised on `lelle-gen-3-evr-island-comms` **is resolved, and resolved as the cheap pre-emption exactly as proposed.** Herald added spec **§1.1 *"Naming rule -- the token names the method, never an instance"*** (`c422f7a`): **`Lelle` names the gen-3 METHOD; a running instance is written `Lelle@<hub-instance>`**, today `Lelle@prod-llm`. **The rule cites THIS WIKI's `singular-convention-plural-instances` as its reason** -- *"this fleet has already paid for the alternative"* -- and inherits the `@<instance>` split already in force in the stationmaster package, where the README calls itself *"the cross-team CONVENTION, not an instance"*. **Closed in the right place: the spec, not the wiki, and without reopening the PO's ruling** -- the ruling binds the token, §1.1 says how the token is written when a document means the deployment. **A librarian's observation on a decision record became a naming rule in the artifact that governs implementation.** **The only open PO item on that entry is now the Koidula collision.** Pin updated `22772b3` -> `c422f7a`; `stage-2` unchanged at `confirmed` against referent `f914efd`, **and this amendment lands outside that referent.** (*FR:Callimachus*)
