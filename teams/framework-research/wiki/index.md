# Wiki Index

Knowledge base for the framework-research team. Curated by Callimachus (Librarian).

## Directories

| Directory | Contents |
|---|---|
| `patterns/` | Reusable patterns extracted from submissions |
| `gotchas/` | Cross-agent pitfalls |
| `decisions/` | Architecture decisions with rationale |
| `contracts/` | API shapes, type definitions |
| `references/` | Pointers to external artifacts/configs with TTL (operational, not generalizable) |
| `process/` | Emerging process patterns (research team) |
| `observations/` | Cross-cutting insights citing topic files (research team, never authoritative) |
| `findings/` | Pre-topic-file findings (research team) |
| `archive/` | Stale or superseded pages |

## Card Tier (three-tier context system, #68)

Every wiki entry has a ~30-line **card** (TLDR + Key ideas) at `<subdir>/cards/<name>.md` -- an extractive summary for fast querying. Each subdir's `cards/INDEX.md` lists its cards with links to both the card and the full entry. Cards are queryable summaries; full entries are evidentiary.

| Subdir | Entries | Card index |
|---|---|---|
| `patterns/` | 93 | [patterns/cards/INDEX.md](patterns/cards/INDEX.md) |
| `gotchas/` | 42 | [gotchas/cards/INDEX.md](gotchas/cards/INDEX.md) |
| `process/` | 11 | [process/cards/INDEX.md](process/cards/INDEX.md) |
| `references/` | 9 | [references/cards/INDEX.md](references/cards/INDEX.md) |
| `observations/` | 3 | [observations/cards/INDEX.md](observations/cards/INDEX.md) |
| `decisions/` | 8 | [decisions/cards/INDEX.md](decisions/cards/INDEX.md) |
| `contracts/` | 5 | [contracts/cards/INDEX.md](contracts/cards/INDEX.md) |
| `findings/` | 1 | [findings/cards/INDEX.md](findings/cards/INDEX.md) |
| `archive/` | 0 | [archive/cards/INDEX.md](archive/cards/INDEX.md) (no archived entries yet) |

**Total: 172 entries carded.** Card schema (validated S39): `title`, `directory`, `status`, `confidence`, `source-agents`, `discovered`, `last-verified`, `related`, `tags` + TLDR + Key ideas. `source-team` on cross-pollinated entries; `ttl` on TTL'd entries; **`migration-target` on `findings/` entries** (names the topic file the finding is destined for, so the 3-session `[MIGRATION-STALE]` rule has a concrete destination to fire against -- directory convention set with the first findings entry, 2026-08-03). No `id` field (filename is key).

## Entries

### patterns/

- [`citation-backed-beats-posture-backed-when-fact-is-subtle.md`](patterns/citation-backed-beats-posture-backed-when-fact-is-subtle.md) -- Refines topic-10's competency taxonomy: citation-backed is strongest not for auditability but because evidence-per-clause *forces claim decomposition* -- a subtle multi-part fact (Entu `_sharing`: create-time escalation-copy AND no post-creation propagation) can't be filed as a posture-prompt one-liner without leaving a visible gap. Posture prompts can be honest and still wrong (compress two clauses into one); the index's structure does the noticing. Source: celes/callimachus/finn. entu-api#42
- [`in-process-respawn.md`](patterns/in-process-respawn.md) -- Three-step fix for respawning agents after a crash without tmux (jq cleanup of dormant config.json entries)
- [`cathedral-trigger-quality-teams.md`](patterns/cathedral-trigger-quality-teams.md) -- Cathedral tier is deterministic when the team's mission IS eliminating structural debt
- [`multi-repo-xp-composition.md`](patterns/multi-repo-xp-composition.md) -- Shared ARCHITECT + separate PURPLEs + shared Librarian for multi-repo teams with different stacks
- [`xp-cycle-for-infrastructure.md`](patterns/xp-cycle-for-infrastructure.md) -- XP pipeline applies to infrastructure stories (test setup, CI), not just features
- [`model-tiering-by-consequence.md`](patterns/model-tiering-by-consequence.md) -- Opus for invisible/accumulating errors, sonnet for test-gated output; cost excluded per PO directive #49
- [`claude-infrastructure-dependencies.md`](patterns/claude-infrastructure-dependencies.md) -- Three layers of Claude-specific dependencies: infrastructure (hardest), protocol conventions (medium), model naming (easiest)
- [`multi-provider-integration-seams.md`](patterns/multi-provider-integration-seams.md) -- Peer (Claude-only), daemon/sidecar (Eilama), MCP server (proposed) -- seam choice by interface complexity
- [`protocol-interpretation-variance.md`](patterns/protocol-interpretation-variance.md) -- Four dimensions of multi-provider protocol risk: format compliance, authority boundaries, relay fidelity, structured ACKs
- [`platform-vs-provider-lock-in.md`](patterns/platform-vs-provider-lock-in.md) -- Claude Code (platform) and Anthropic (provider) are distinct lock-in risks; migration planning must separate them
- [`correlated-failure-single-provider.md`](patterns/correlated-failure-single-provider.md) -- All 5 enforcement layers (E0-E4) collapse simultaneously on provider outage; T04 gap identified, multi-provider mitigation has 7 governance prerequisites
- [`integration-seam-governance-impact.md`](patterns/integration-seam-governance-impact.md) -- Sidecar = minimal governance (below governance layer); peer = non-linear governance (inside governance layer); classify before analyzing
- [`model-inventory-baseline.md`](patterns/model-inventory-baseline.md) -- **`[TTL-EXPIRED]` 2026-07-10, flagged 2026-08-03, still un-actioned.** 68 slots across 9 teams: 63% opus, 35% sonnet, 1.5% local LLM. **Treat as a 2026-04-10 historical snapshot, not current state.** Deliberately NOT re-verified: model inventory is substrate truth in deployed rosters/configs, and a librarian re-read would only confirm the page still says what it said. **Owner: team-lead**, to route to a specialist with substrate access.
- [`five-layer-provider-lock-in.md`](patterns/five-layer-provider-lock-in.md) -- Infrastructure, protocol, knowledge, prompt, governance: five lock-in layers with different switching costs; execution layer is the exception
- [`framework-participating-vs-service-roles.md`](patterns/framework-participating-vs-service-roles.md) -- Roles using SendMessage/Librarian/authority = Claude-only; roles with test-gated I/O = provider-agnostic; tools (MCP) are not agents
- [`pass1-pass2-rename-separation.md`](patterns/pass1-pass2-rename-separation.md) -- Framework-wide identifier renames separate into Pass 1 (prose, ship now) and Pass 2 (machine identifiers, batch later); schema changes ship in Pass 1 because they enable new behavior
- [`within-document-rename-grep-discipline.md`](patterns/within-document-rename-grep-discipline.md) -- Grep the whole document before editing the declaration site; references live in the body, not the schema block. Sibling to pass1-pass2-rename-separation at file scope
- [`protocol-shapes-are-typed-contracts.md`](patterns/protocol-shapes-are-typed-contracts.md) -- Field-set divergence between independently-drafted producer/consumer specs breaks protocols silently; cross-read both ends, lift consumer's field set verbatim. Cross-team scope
- [`convention-as-retroactive-telemetry.md`](patterns/convention-as-retroactive-telemetry.md) -- Consistently-enforced conventions produce retroactive telemetry as a byproduct with zero instrumentation cost; the discipline IS the instrumentation
- [`rule-erosion-via-reasonable-exceptions.md`](patterns/rule-erosion-via-reasonable-exceptions.md) -- Exceptions to hard rules are corrosion vectors; "prudent pause beats permission grant" -- permission removes one blocker, not all of them
- [`named-concepts-beat-descriptive-phrases.md`](patterns/named-concepts-beat-descriptive-phrases.md) -- Named concepts (citable, reusable, harder to misremember) beat descriptive phrases for cross-artifact references; naming signal is repeated multi-word description across contexts
- [`why-this-section-exists-incident-docs.md`](patterns/why-this-section-exists-incident-docs.md) -- Prompt sections added after incidents must name the incident inline; prevents future readers from deleting the fix as "redundant"
- [`prompt-to-artifact-cross-verification.md`](patterns/prompt-to-artifact-cross-verification.md) -- Before deploying a prompt, verify every referenced artifact exists at the declared path with the declared structure; post-bootstrap gate (spec-vs-output variant) in the structural-discipline cluster. **Amended 2026-05-06 with runtime-variant extension:** the discipline applies at TWO lifecycle stages -- pre-merge (prompt-author) AND runtime/mid-task (prompt-recipient at task-start + every artifact-write event). Same gate-mechanism, different agent role. Two S27 instances: brief-frame-vs-artifact-mismatch (Herald caught Aen's spawn brief naming `types/t09-protocols.ts` on `mitselek/prism` where it doesn't exist); filename-vs-conceptual-numbering-collision (Herald caught `03-` filename slot already taken). n=2 cumulative on runtime-variant. Sources: Brunel, Herald
- [`first-use-recursive-validation.md`](patterns/first-use-recursive-validation.md) -- When a new rule's first application catches its own author violating it, that is recursive validation -- strongest evidence the rule is load-bearing. Observed: Structural Change Discipline commit `589fda9` → `48ac09e`
- [`world-state-on-wake.md`](patterns/world-state-on-wake.md) -- On compaction/rebuild/respawn recovery, agents read a world-state snapshot before acting on stale memory. Seed material for Volta's persist/restore extension. Source: Aalto (uikit-dev) -- first externally-sourced pattern
- [`tmux-pane-border-format-for-teams.md`](patterns/tmux-pane-border-format-for-teams.md) -- Three labeling styles for tmux `pane-border-format` (role ID, persona, combined), syntax for the conditional chain, runtime persistence caveat. Sibling pattern to the pane-labels gotcha. Source: Aalto + team-lead
- [`scope-block-drift-from-practice.md`](patterns/scope-block-drift-from-practice.md) -- Agent prompt `MAY WRITE` blocks drift from practice in two variants: letter lags practice (Finn, Aeneas) or internal contradiction (Brunel). Prevention: draft scope block last, after workflow. Structural-discipline cluster member (gates 1+2). n=3. Source: Celes, team-lead
- [`structural-match-beats-free-string-for-protocol-filters.md`](patterns/structural-match-beats-free-string-for-protocol-filters.md) -- Structural JSON field match beats free-string substring for protocol-field filters; free-string false-positives on prose that discusses the protocol. Empirical: 25 vs 26 matches, 1 false positive (montesquieu.json). Cross-team scope. Source: Volta, F1
- [`governance-staging-for-agent-writes.md`](patterns/governance-staging-for-agent-writes.md) -- Agent writes routed through a staging table with tier assignment (type/sensitivity/source/role) and per-tier promotion rules (auto / conflict-detect / AI-reviewer with confidence floor / human-only). Confidence-floor + fail-closed escalation discipline is the transplantable policy piece independent of the staging infrastructure. FR's current Cal-direct-accept model is an implicit decision-by-inaction. Source: Finn, xireactor-brilliant digest §4(a)
- [`bootstrap-preamble-as-in-band-signal-channel.md`](patterns/bootstrap-preamble-as-in-band-signal-channel.md) -- Signals an agent must see at session start ride the bootstrap preamble (the required startup read), not an out-of-band channel. Durable state → runtime context at session birth. Same shape as FR's restore-inboxes + Read Order discipline; xireactor adds governance as a new payload class. Sibling to world-state-on-wake. Source: Finn, xireactor-brilliant digest §4(b)
- [`integration-not-relay.md`](patterns/integration-not-relay.md) -- Team-lead's job between specialists is integration, not relay -- specialist positions are time-indexed state, not typed values. Four-check discipline (walk history forward, pending-confirmation vs accepted, self-consistency with existing contracts, what-would-change-the-landing) + specialist-side "pre-fold consistency check" complement. Includes the meta-trap: discipline ritual running in place of object-level epistemic work. n=6+ in one session (4 team-lead instances + Brunel + Herald specialist-side articulations). Source: team-lead, Brunel, Herald, Monte, Cal
- [`substrate-invariant-mismatch.md`](patterns/substrate-invariant-mismatch.md) -- The code is right, the substrate is wrong. Self-consistent artifact + implicit invariant + substrate change = silent failure. **n=6** across distinct layers (dual-team-dir-ambiguity path-root + protocol-shapes-are-typed-contracts field-set + xireactor link-index write/read-coupling + teamcreate-in-memory-leadership disk-vs-runtime-state + Brilliant entry_links write-path-sync external confirmation + harness-inbox-write-vs-runtime-iteration message-bus delivery). Amended 2026-05-06 to n=6 with live-operational session-27 instance (Monte/Cal/team-lead joint dual-witness; two sub-shapes -- read-cursor-skip and on-disk-absence -- observed). **Hypothesis sharpened (Monte's structural insight): direction-asymmetric (worktree-OUTBOUND specifically broken), `dual-team-dir-ambiguity`-shaped at a different layer of the same filesystem-substrate stack -- same root cause (path-as-substrate-invariant), Instance 1 at bare-path-resolution layer, Instance 6 at worktree-mount-decomposition layer.** Promotion to common-prompt strongly indicated by same-root-cause-different-layer relationship at n=6. **2026-05-12: Remediation Shape enriched with proactive write-site multi-substrate flag sub-shape** -- RFC #66's substrate disclaimer applies all three remediation defenses simultaneously at write site (positive instance; n=6 unchanged). **2026-05-14: watch note added for `members-array-edit-honored-mid-session.md`** -- n=2 cross-substrate invariant currently holds; if a future substrate stops honoring on-demand `members[]` edits, ghost-bridge / on-demand library provisioning become candidate Instance 7. Source: Cal (lead), team-lead, Volta, Brunel, Finn, Schliemann, Monte; sub-shape addition: Callimachus + RFC #66; watch note: Callimachus
- [`worktree-spawn-asymmetry-message-delivery.md`](patterns/worktree-spawn-asymmetry-message-delivery.md) -- **Non-parent-process → recipient message delivery across worktree boundary is unreliable** despite harness reporting success on dispatch. Originally framed "worktree-OUTBOUND specifically" (n=2); **relaxed 2026-05-06 to "non-parent-process specifically"** with Cal → Brunel BROKEN data point (n=4+). Possible mechanisms: timing/per-pair specifics, mount-staleness drift, non-deterministic substrate-iteration. Two sub-shapes: (A) read-cursor-skip on present-on-disk message (no self-correction via re-iteration; absence-of-recovery is informative negative evidence); (B) on-disk-absence after harness reports success. Operational hypothesis (not yet harness-source-confirmed): worktree-mount-decomposition. Same-root-cause-different-layer relationship to `dual-team-dir-ambiguity` (path-as-substrate-invariant broken at bare-path-resolution layer vs worktree-mount-decomposition layer). **Codified workaround:** team-lead-parent-process → recipient relay is the only empirically reliable cross-boundary path. **n=4+ promotion-grade.** Confidence: high on failure-mode + workaround; hypothesis-class on root cause until harness-source confirmation. **2026-05-12: cross-linked to `inbox-file-write-as-wake-mechanism.md`** as the canonical substrate property this entry describes violations of (RFC #66 named the substrate; this entry catalogs the cross-mount exception). Sources: Monte, Brunel, Cal, team-lead
- [`relay-to-primary-artifact-fidelity-discipline.md`](patterns/relay-to-primary-artifact-fidelity-discipline.md) -- When async ratification chains carry typed-contract material via relay (team-lead → specialist; specialist → specialist), and a primary artifact may exist or arrive later, the discipline is a **two-stage lifecycle**: **Stage 1** (relay-only window) -- fold ONLY what's verbatim in the relay; mark gaps as gaps with FLAG annotations; do NOT implement speculative inferences. **Stage 2** (primary-artifact arrival) -- fetch primary artifact and supersede relay-fold; record divergences in revisions log. **Two anti-patterns** (one per stage): Stage 1 = **flag-then-implement-as-confirmed** (premature implementation); Stage 2 = **stale-relay-fold-survives-after-artifact-arrives** (premature stop). Production rule (Herald): **provenance-by-artifact-class beats provenance-by-recency**. **n=5 cumulative across 5 distinct lifecycle-application contexts** spanning S27–S29: (1) Brunel v0.3→v0.5 reversion (Stage 1 failure on design-doc); (2) Brunel v0.5→v0.6 supersession (Stage 2 catch on `supersedes?: string` vs `supersedes: string | null`); (3) Herald `envelopeId`→`recordId` evolution (producer-side third-party); (4) Cal curator-ACK-vs-wiki-body Stage 2 self-correction (recursive validation -- discipline catches its own author); (5) Monte+Cal jointly-authored entry under substrate-loss -- Cal Stage 1 honest-FLAG filing of `recursive-citation-as-canonical-validation.md` + Monte Stage 2 author-scratchpad fold (verbatim Protocol A text unrecoverable; author-scratchpad as next-best primary artifact). Cross-class sibling to Instance 4: single-author vs joint-author lifecycles produce same Stage 1 + Stage 2 cadence -- discipline applies symmetrically across source-agent identity. **Already Protocol-C-promoted in S28** (common-prompt "Relay Fidelity Discipline" subsection). Originally filed as `fold-only-what-is-verbatim.md`; renamed + extended 2026-05-06; Instance 5 folded 2026-05-07. Sibling at relay-fidelity layer to `integration-not-relay.md` (parent at content layer). Sources: Brunel, Herald
- [`discriminator-field-name-consistency-over-uniqueness.md`](patterns/discriminator-field-name-consistency-over-uniqueness.md) -- When adding a new discriminated-union member to an existing federation type system, **inherit the discriminator field name from existing unions in the system** -- do NOT name from naive disambiguation pressure (e.g., `kind` → `recordKind`). Cross-union field-name *consistency* preferable to *uniqueness* because typed-language consumers disambiguate by union annotation at call-site, not by inspecting discriminator field names. Anti-pattern: **conservative-answer rename for "type-system disambiguation"** is future-proofing-by-rename (per `no-future-proofing.md`). First instance: Brunel's `kind` → `recordKind` over-engineering -- same incident as `relay-to-primary-artifact-fidelity-discipline.md` Instance 1 (the receiver-side discipline that should have prevented it). Adjacent to `canonical-taxonomy-check-before-naming.md` at the field-name layer. n=1 watch posture. Source: Herald
- [`windows-user-context-persistent-bridge.md`](patterns/windows-user-context-persistent-bridge.md) -- Persistent operator-side network bridge on Windows without admin rights, via user-context Task Scheduler with at-logon + power-resume triggers + supervisor-of-supervisor loop + hidden-window launcher (wscript+VBS). Six components, all load-bearing. Amended 2026-04-29 after 24h field testing surfaced two failure modes (autossh status-127 fatal exit + visible Win11 console window). Source: team-lead. n=1 throughout, watch for comms-hub Windows-operator bridge as potential n=2
- [`live-inject-plus-dockerfile-bake-dual-track.md`](patterns/live-inject-plus-dockerfile-bake-dual-track.md) -- Container dependency additions ship both halves: `docker exec -u root apt-get install` for the live container + same package list baked into the Dockerfile, in the same work session. Live-only = silent rebuild regression; Dockerfile-only = blocks live work. Source: Brunel. n=1, watch for next mid-cycle dep addition
- [`wiki-cross-link-convention.md`](patterns/wiki-cross-link-convention.md) -- Markdown links in prose, not bare text, when wiki entries reference identifiable repo artifacts. Frontmatter is the structured-link layer; markdown-in-prose is the click-through layer. Section-scoped first-occurrence linking; default-to-link on judgment calls. **First cross-pollination filing** -- sourced from apex-research (Schliemann/Eratosthenes) via Finn's comparative analysis. Introduces new `source-team` frontmatter field as single-entry experiment. n=2 across two teams' wikis, but co-discovered not independently rediscovered
- [`operational-team-archetype.md`](patterns/operational-team-archetype.md) -- A new team archetype distinct from research/development/XP-pipeline, recognized by four co-occurring characteristics: no TDD pipeline, succession-framing as first-class, low-volume cadence (1-3 sessions/week), persistent roster + episodic sessions. Mission-framing pattern: "load-shed primary stakeholder via PO as liaison, succession-readiness baked in." First instance: esl-suvekool (mitselek/Haapsalu-Suvekool, Tobi/Lyyd/Saar/Tamp, deployed 2026-05-01/02). n=1, watch posture; promotion trigger = second similar team requesting same shape. Source: team-lead
- [`claude-startup-md-as-cross-team-handoff.md`](patterns/claude-startup-md-as-cross-team-handoff.md) -- Place `.claude/startup.md` at a target team's repo root so PO opening Claude in that workdir auto-bootstraps the target team-lead persona, calls TeamCreate, runs standard startup. Resolves the mutual-exclusivity-of-team-leadership constraint (designing team-Y from team-X session works; spawning agents into team-Y from team-X session does not). Static-to-dynamic handoff: designing team places the artifact; target team's first-session dynamic work (`TeamCreate`, agent spawns) originates from inside the target's own session. First instance: esl-suvekool commit `0e461be`, PO confirmed auto-bootstrap 2026-05-02. n=1, watch posture. Source: team-lead
- [`oss-thin-integration-anti-extension-signal.md`](patterns/oss-thin-integration-anti-extension-signal.md) -- Five joint signals (single function call to external service + one call site + no event surface + locked schema + stateless single-shot) identify an OSS substrate where the data model usually survives but the orchestration must be replaced, not extended. Reframes "extend or fork?" as "replace or fork?" before sunk-cost commitments accumulate. First instance: Brilliant Tier 3 reviewer (xireactor-brilliant @eb1d1bf v0.5.1). n=1 watch posture. Source: Finn (#64)
- [`poll-only-substrate-sidecar-derivation.md`](patterns/poll-only-substrate-sidecar-derivation.md) -- When OSS substrate has no push events but rich poll-able append-only tables, sidecar polling with sequence-cursor → rule-based derivation → emit *derived* events only (heat/collision/gap). Cost bounds at derivation-rate, not raw-rate. Independence-preserving (no fork). First instance: Brilliant `request_log + entry_access_log + audit_log` triad. Throughput verified at FR-style speculative volume. n=1 watch posture. Source: Finn (#64)
- [`path-namespace-as-federation-primitive.md`](patterns/path-namespace-as-federation-primitive.md) -- Logical-path namespacing per team inside a shared central knowledge store (`Projects/<team>/*`, `Meetings/<team>/<date>`, etc.) is itself the federation contract -- sufficient in operational practice, no formal cross-team API or replication protocol layered above. Convention does the work. First instance: esl-suvekool on Brilliant (mitselek/Haapsalu-Suvekool @e7d2546f). Load-bearing for FR Phase A (issue #65). n=1 watch posture. Source: Finn (#64)
- [`two-consumer-pattern.md`](patterns/two-consumer-pattern.md) -- When a knowledge store has two consumer classes with different access capabilities (one queries directly via MCP/REST, one cannot -- e.g., claude.ai Projects), the right architecture is a manual synthesis-and-handshake bridge (`[SYNC BRIEF]` ↔ `[SYNC: YYYY-MM-DD]` echo + `.last-sync` anchor) -- not "force everything through one channel" or "build infra to give snapshot consumers direct access." Bridge, not sync. First instance: esl-suvekool's `roadwarrior-sync` skill. Sibling to path-namespace-as-federation-primitive. n=1 watch posture. Source: Finn (#64)
- [`no-future-proofing.md`](patterns/no-future-proofing.md) -- Don't add features, surfaces, abstractions, allocations, or fallbacks for scenarios that haven't happened. Design from *observed* need, not *anticipated* need. Three forms: YAGNI on speculative requirements, no pre-allocation, no fallback chains. Carve-outs for reversibility-asymmetric decisions (public APIs additive-only, security/durability primitives like audit logs). Closing-as-YAGNI is a valid outcome distinct from "watch candidate with promotion criteria." Promoted from user-memory discipline after citation-hygiene gap surfaced 2026-05-05. n=many across team-lead's standing posture + Finn (xireactor) + Brunel (xireactor) + Cal (today's batch + Protocol B response to Brunel). Sources: team-lead, Finn, Brunel, Callimachus
- [`dispatch-granularity-matches-recovery-handler.md`](patterns/dispatch-granularity-matches-recovery-handler.md) -- Semantic distinctions between sub-cases belong WHERE they affect handler behavior, not WHERE they originate. If two sub-cases share recovery family + permitted actions + escalation target + idempotency (4-of-4 twins criteria), they belong as sub-discriminator INSIDE the recovery shape, not peer classes at the top-level dispatch enum. Counter-shape uses a `kind` field -- dispatch-by-state-value, contract is in the type. First instance: Prism deliverable B + Surface 2 coordination loop, Herald Mod 2 retraction in favor of Monte's `CuratorUnavailableRecovery.kind` collapse. n=1 watch posture. Sources: Herald, Monte
- [`coordination-loop-self-correction.md`](patterns/coordination-loop-self-correction.md) -- Cross-specialist `[COORDINATION]` loops can produce within-specialist self-corrections at coordination-tempo (single exchange) rather than session-tempo (separate sessions). Catcher reads own v1 against peer's reshape using the same structural test they'd apply to a third specialist's work; if reshape is better, retract by structural argument not social. Anti-shapes: performative agreement, adversarial defense. Reshaper-side discipline: apply structural improvement to own work first, don't expect catcher to retract. **n=2 within a single 5-message exchange** (Monte v1.0→v1.1 + Herald Mod 2 retraction, 2026-05-05). **Named sub-shape variants** (folded 2026-05-06 per Aen 11:26): (A) prior-cross-specialist-argument-as-trigger -- Herald cites Monte's prior-session argument back at own framing; (B) pre-commit-to-extension irony -- catcher's own prior pre-commit as scaffolding that enables a *different* and better commitment, even when the specific extension is retracted. Trigger taxonomy under unified self-correction mechanism. Sources: Herald, Monte
- [`worktree-isolation-for-parallel-agents.md`](patterns/worktree-isolation-for-parallel-agents.md) -- For parallel-agent contexts on a shared clone, default to `git worktree add` to give each specialist a separate physical working directory. Failure mode without it: A's uncommitted changes block B's branch switch; stash risks abandonment; force-switch silently overwrites; sequential handoff serializes work. Three joint conditions: multiple parallel specialists + shared clone + branch overlap incidental-not-intentional. **Amended 2026-05-06 to n=7 cumulative** across 5 work types and 4 specialists (Brunel near-miss + Monte preemptive collision + Herald table-fix/PR#7 + Herald deliverable-C/PR#9 + Herald envelope-v1.1/PR#10 + Herald SemVer-bump/PR#11 + Herald 04-spec-drafting/PR#12 + Herald 02-§4-R9-amendment/PR#13). **Named sub-shape: dirty-main-worktree-bypass** (Herald PR #11 + PR #12, n=2 cumulative on this sub-shape -- using worktree to bypass team-lead's uncommitted main-worktree state). **Cross-link to substrate-invariant-mismatch Instance 6: orthogonal failure layers** -- worktree-isolation works for git workflows; harness inbox-write layer needs separate fix (codified in `worktree-spawn-asymmetry-message-delivery.md`). Promotion to common-prompt strongly indicated. Sources: Brunel, Monte, Herald
- [`timestamp-crossed-messages.md`](patterns/timestamp-crossed-messages.md) -- In multi-agent inboxes, two messages from different specialists can be **timestamp-crossed** -- composed in parallel but rendered in arrival order, which the receiver may misread as causal sequence. Failure mode: **silent merge** (treating message B as response to A when B was composed independent of A). Discipline: **detect (composition-time vs arrival-time skew, restated information, same-content branching) + surface (don't bridge -- making the cross visible to both senders)**. Composes with re-process-inbox-first. Substrate-level: inbox guarantees ordering, not causality. **Promotion-strong at n=8 cumulative** within session 26 Prism Phase A coordination -- the discipline scaled to high-volume coordination without escalation. Distinct mechanism from within-loop self-correction (parallel vs sequential substrate). Source: Herald
- [`semver-strict-typed-contract-discipline.md`](patterns/semver-strict-typed-contract-discipline.md) -- When versioning a typed contract, bump level is determined by the **consumer's type-check work**, not by whether substrate provides migration. Aen's framing: *"Migration mechanism makes the bump SAFE, not 'minor.'"* Two-gate discipline: type-check delta gates major-vs-minor-or-patch; runtime-semantics delta gates minor-vs-patch. Failure mode named: **migration-eased version-bump deflation** (conflating runtime compat with type-level compat). First instance: Prism envelope v1.1.0 → v2.0.0 PR #11 -- adding required `curatorAuthority` with substrate default; bump corrected to major because consumer's strict-TypeScript type-check would fail against v1.1.0 shape. Sibling to worktree-isolation in version-control discipline cluster. n=1 watch posture. Source: Herald
- [`substrate-shape-vs-authority-shape-orthogonality.md`](patterns/substrate-shape-vs-authority-shape-orthogonality.md) -- Substrate-shape (where data lives, who owns storage, how it scales) and authority-shape (who can write, who reviews, where decisions land) are **orthogonal axes** in federation/multi-tenant designs. Two halves compose: **diagnostic** (Monte) -- *axes are orthogonal; conflating imports the wrong failure mode* -- and **normative** (Herald) -- *asymmetries should live above the substrate, not in the substrate*. Failure mode named: **axis-conflation by reading-substrate-as-authority** (M2 reading Brunel's hub-and-spoke as authority claim) and its symmetric cousin reading-authority-as-substrate. Four-step application: surface axes + test orthogonality + locate asymmetry + defend placement. First instance: Prism Phase A -- Monte's Surface 1 §1.4 M2 rejection (diagnostic) + Herald's deliverable C (normative); composition holds across Brunel topology + M3 federation-curators-as-class + Surface 3 DACI. n=1 watch posture (filed by Aen 17:25 ratification). Sources: Herald, Monte
- [`canonical-taxonomy-check-before-naming.md`](patterns/canonical-taxonomy-check-before-naming.md) -- Before naming a new design element by reference to an existing protocol or codename, run a canonical-taxonomy lookup against the source-of-truth artifacts (`types/t09-protocols.ts`, common-prompt headers, codename registries). Three outcomes: match (use the name), collision (different role, pick another), discard (name was retired -- prior reason imports against reuse). Failure mode named: **letter-pattern naming** (treating taxonomy as alphabetical sequence rather than structural slots). **n=2 cumulative**: session #59 W3-vs-Protocol-D distinction + session 26 Prism codename collision-check. Two-axis check: protocol-name layer + codename/registry layer. Source: Monte
- [`lossless-independent-convergence.md`](patterns/lossless-independent-convergence.md) -- When two specialists working from orthogonal starting questions independently arrive at the same structural shape and the deliverables compose without rework, that's lossless convergence -- structurally stronger evidence the shape is emergent than worked-it-out convergence (the latter is compatible with shared blind spots; the former survives independent scrutiny). Three joint conditions: orthogonal starts + independent first-ships + no-rework integration. **n=2 cumulative, promotion-grade** per Aen's 2026-05-05 16:46 ratified phrasing -- session #59 (three-state model + Flow B/Protocol D, Herald §2 + Monte §3.6) cited via prose; today's session 26 Prism Phase A submission (cadence axis + asymmetry axis) is the n=2 making it promotion-grade. Schema-purity: source-agents stays [herald, monte]; #59 cumulative cited in prose only. Sources: Herald, Monte
- [`protocol-completeness-across-surfaces.md`](patterns/protocol-completeness-across-surfaces.md) -- When a protocol contract is decomposed across multiple surfaces (separate documents, separate authors), completeness is "every dispatch enum value has a recovery handler, every recovery has a terminal action, every authority field has a governance surface, every rejection has a route back to the producer." Aen's framing (17:13): *"every value has a destructor; every error has a recovery."* First instance: Prism Phase A composition (Herald deliverables A+B + Monte Surfaces 1+2+3) -- closed loop verified across 5-class WriteRejection enum × ProducerAction terminal actions × CuratorAuthority + §3.4 ratification protocol. **Promotion-grade at n=1 by Herald's joint-cross-specialist criterion** (Herald saw from dispatch side, Monte from recovery side, both arrived at same property at first surfacing). Sources: Herald, Monte
- [`field-level-overlap-one-truth-not-mirror.md`](patterns/field-level-overlap-one-truth-not-mirror.md) -- When two fields in a typed contract carry the same logical information at different levels (e.g., `envelope.sourceTeam` + `envelope.entry.sourceTeam`), the right shape is **one field, one truth at the level governance lives** -- not two fields with a documented mirror invariant. Failure mode: **mirror-invariant rot** (works on day 1, drifts when a second write path lands on one field but not the other). Three-rule discipline: detect overlap (same value across well-formed instances OR documented mirror invariant) + choose canonical location (where governance lives) + remove the mirror (don't sync, structurally eliminate). Pairs with audit-trail-for-rejection-rationale. **n=2 cumulative, promotion-grade** per Aen's S26 [LEARNED]-cluster re-classification -- Herald's PR #8 sourceTeam dedup (specific catch) + named-class generalization. Sibling to dispatch-granularity at the contract layer (where-distinction-lives vs where-value-lives). Sources: Herald, Monte
- [`audit-trail-for-rejection-rationale.md`](patterns/audit-trail-for-rejection-rationale.md) -- When a design decision **removes** a field, dedups a redundant element, or rejects a candidate variant, the would-be-alternative + reason for rejection must be **cited inline** at the surviving artifact so future readers do not re-introduce the rejected element from absence-of-justification. Failure mode named: **silent removal** (rationale lives only in integrator's head; future reader's reasoning suggests the rejected element is unopposed). Threshold-discipline (one paragraph at canonical site, not full re-litigation). Same-class pattern as `why-this-section-exists-incident-docs` at incident-doc layer -- both name silent re-introduction; both fix with inline citation that travels with the surviving artifact. First instance: Prism PR #8 Mod 1 sourceTeam dedup audit-trail. Pairs with field-level-overlap-one-truth-not-mirror as a unit on every dedup decision. n=1 watch posture. Source: Monte
- [`surfacing-cost-asymmetry-stale-context.md`](patterns/surfacing-cost-asymmetry-stale-context.md) -- Surfacing (per `timestamp-crossed-messages`) has cost; the cost is **asymmetric across stale-context conditions**. **Sender-side** (Herald #34): surfacer can produce false-positive surface from stale inbox; **receiver-side** (Monte #4b): recipients absorb cost regardless of whether the surface is real. Three-rule operational hygiene: process inbox to current before surfacing + state substrate freshness when surfacing + recognize cost-asymmetry as default. Composes with `timestamp-crossed-messages` (paired discipline). **Merged per Aen's "same observation from sender vs receiver" merge-test** -- same property at two ends of one transaction, single entry preserves transactional view that makes cost-asymmetry observable. **n=2 cumulative** (Herald n=1 sender-side + Monte n=2 receiver-side cumulative). Sources: Herald, Monte
- [`snapshot-state-mis-names-path-to-end-state.md`](patterns/snapshot-state-mis-names-path-to-end-state.md) -- When labeling an in-progress path artifact (file path, branch, status, milestone), the snapshot's mid-stream state can mis-name the path itself by treating present state as if it were the end-state. Failure mode: **labels capture state at snapshot time; paths continue past snapshot time; labels stay frozen**. Three-rule discipline: label the path/intent not the snapshot state + if snapshot-naming is unavoidable mark it explicitly with freshness signal + re-label when path moves. First instance: Aen's Phase A path-misnaming (self-reported); first-person flag is part of what makes the instance load-bearing. n=1 watch posture; sibling at the labeling-discipline family alongside pass1-pass2-rename-separation, world-state-on-wake. Sources: Herald, team-lead
- [`api-gateway-error-vs-actual-server-state.md`](patterns/api-gateway-error-vs-actual-server-state.md) -- When a client receives a *gateway* error (504, 502, connection-reset), the error reflects the gateway's view of the request lifecycle -- **not** necessarily the server's view. Failure mode: **client-server temporal divergence at the gateway boundary**. Discipline: treat gateway errors as ambiguous (not failed) + verify server state before retrying + use idempotency keys when possible + distinguish gateway 5xx from server 5xx. Herald's framing: *"API gateway errors are not necessarily request-failures; verify state before retrying."* First instance: Herald's `gh pr create` 504-then-success in Phase A (server had created PR despite the 504; retry without verify would have produced duplicate). n=1 watch posture; sibling to substrate-invariant-mismatch (gateway-vs-server divergence is a substrate-invariant instance). Source: Herald
- [`repo-as-durable-store-teamdelete-as-release-primitive.md`](patterns/repo-as-durable-store-teamdelete-as-release-primitive.md) -- Two-invariant rule for team lifecycle protocols on platforms with split disk + in-memory state: (1) all durable state in one canonical store (the team repo), (2) in-memory state requires its own explicit release primitive (`TeamDelete`) distinct from disk cleanup. Each invariant alone is insufficient. Phase 4a (repo-persist) + Step S5 (TeamDelete) instantiate the rule. Cross-team field instances n=3 (apex-research #62, FR session 21, esl-suvekool 22→23) but combined-invariant articulation n=1. Pattern angle (fix-shape, generalizable) distinct from `gotchas/teamcreate-in-memory-leadership-survives-clear` (incident). Sibling fix-shape under `substrate-invariant-mismatch`. Confidence medium-high. Source: Volta
- [`single-channel-saturation-via-mode-partition.md`](patterns/single-channel-saturation-via-mode-partition.md) -- Before projecting saturation on a "channel" carrying multiple kinds of traffic, run a three-question decomposition test: do the modes share recipient + cadence + decision surface? If any one differs, modes are separable and saturation forecasts must partition per-mode; aggregate-saturation across separable modes is a phantom bottleneck. **Converse of `substrate-shape-vs-authority-shape-orthogonality.md`** (same higher-order discipline at different surface -- orthogonality separates design-time axes; mode-partition separates load-forecast streams; both resist collapsing-multi-into-one). First instance: Monte's drift-detection v1.0 -- Aen's *"digest carries advisory-fatigue load; escalation carries breach load; they do not compete for the same channel"* resolved an n=20 advisory-fatigue forecast as phantom. Cross-cutting reach to T03 mesh-topology debates + T07 tier-0/tier-2 alert partition. n=1 watch posture. Source: Monte
- [`recursive-citation-as-canonical-validation.md`](patterns/recursive-citation-as-canonical-validation.md) -- When an author queries a wiki entry they themselves authored -- independent of the canon-creation moment, in the course of unrelated work -- that query is recursive validation **by query**, structurally distinct from `first-use-recursive-validation.md` (recursive validation **by application**). Three-row family-distinction table on two axes (mechanism + what-is-validated): citation validates canon indexability; application validates rule effectiveness. **Explicitly NOT a `two-consumer-pattern` instance** (different-access-bridge vs same-access-temporal-independence -- cross-reference prevents mis-filing). **First instance**: Monte's T04 §Recipient-and-authority-chain citation chain (Monte 10:50 Protocol B query → Monte 11:06 T04 citing his own session-26 wiki entries → Cal ~11:18 Protocol B response citing Monte's T04 prose back to Monte) -- two-layer recursion both validating canon indexability. Adjacent (NOT first-instance source): Instance 4 of `relay-to-primary-artifact-fidelity-discipline.md` (Cal's curator-ACK Stage 2 self-correction is a separate recursive moment from the same family). Joint source-agents [monte, callimachus] per Cal's 11:36 framing being load-bearing on structural categorization; promotion-grade-at-n=1 by joint-cross-specialist criterion. **FLAGs closed 2026-05-07 via Stage 2 author-scratchpad fold** (verbatim Protocol A text unrecoverable; structural framing verified-as-written by Monte's S29 AMENDMENT). Confidence upgraded medium → high. Sources: Monte, Callimachus
- [`ghost-member-as-universal-integration-surface.md`](patterns/ghost-member-as-universal-integration-surface.md) -- **Separating interface from mechanism for team integration**: a ghost-member is a regular `members[]` entry whose backend is not a Claude agent but a pluggable transport (local-fs, ssh-tunnel, mtls-tcp, wss-relay, gh-issues, e2e-encrypted-wss). From inside the team, sending to a ghost is the same SendMessage call agents already use; behind the ghost, a daemon carries messages between two inbox files via any chosen mechanism. Reframes inter-team comms from "pick one transport" to "pick one abstraction with pluggable transports." Generalizes to alerts/webhooks/cross-LLM/human-as-ghost/library-query/inter-team relay -- universal integration surface for the team. Cross-pollinated from comms-dev/RFC #66; substrate-enablers `inbox-file-write-as-wake-mechanism.md` (wake) + `members-array-edit-honored-mid-session.md` (registration) + `inbox-slot-vs-members-validation-asymmetry.md` (lifecycle-asymmetry). **2026-05-14: confidence medium → high.** Both original upgrade triggers satisfied: (1) ghost-bridge v1 daemon (commit `9c5bf83`) implements local-fs plugin variant end-to-end across FR↔apex; (2) n=2 cross-substrate verification of mid-session `members[]` edit semantics closes original OQ #2. **2026-05-19: cross-linked to two new Brunel S31 PoC entries** -- `patterns/agenttype-vs-backendtype-separation.md` (RFC #66 reality-vs-example shipped-shape) + `patterns/per-message-color-overrides-registered-default.md` (display-precedence rule for cross-team identity preservation). Remaining promotion trigger: second team independently arriving at the abstraction without RFC #66 cross-pollination. Source: callimachus (filer), RFC #66 (subject artifact)
- [`service-team-topology.md`](patterns/service-team-topology.md) -- **Members-are-ghosts-of-consumer-teams topology**: a team whose `members[]` are ghost representations of the teams it serves (library, manager/coordinator, audit/compliance, DevOps/SRE, cross-team Finn). Canonical view at the service team; per-consumer view at the consuming end; one-sided ACL; audit-trail-is-inbox; findings-ingest is just inbound messages. Directly answers Issue #47 OQ1/OQ2/OQ3/OQ5/OQ7/OQ8 (4 hold clearly, OQ1+OQ8 partial; OQ4+OQ6 unaddressed). Built on `ghost-member-as-universal-integration-surface.md`; composes with FR's existing cross-pollination flow (`wiki-cross-link-convention.md`, `companion-pair-submission-protocol.md`) -- same pattern at higher frequency + structured substrate. **For FR specifically**: under the third reading (library team as new peer-service alongside per-team Callimachi), FR's wiki/ continues unchanged; cross-team-applicable entries flow up via `findings` ghost using existing `source-team` frontmatter. Cross-pollinated from comms-dev/RFC #66 + Finn's #47. **n=1 watch posture, confidence: medium**. Source: callimachus (filer), RFC #66 + #47 (subject artifacts)
- [`agenttype-vs-backendtype-separation.md`](patterns/agenttype-vs-backendtype-separation.md) -- **Canonical-shape finding from RFC #66 PoC**: the harness's `members[]` entries ship **two orthogonal type fields** per member -- `agentType` (role-semantic: team-lead/general-purpose/librarian/ghost) and `backendType` (substrate-layer: ssh-bridge, implicit-local, plugin-name). RFC #66's example treats them as conflated; actual shipped shape in `roster.json` keeps them separate. Top-level `backendType` (config externalized to daemon) vs RFC's nested `transport.plugin.config` (inline) -- candidate RFC #66 amendment. Step 2c `restore-ghost-members.sh` (commit `7ec8912`) is the reference implementation. Architectural-fact at n=2 cross-substrate (FR Windows-Git-Bash + apex Linux/Docker). Source: Brunel
- [`per-message-color-overrides-registered-default.md`](patterns/per-message-color-overrides-registered-default.md) -- **Display-precedence rule**: presentational metadata at the message level overrides member-level. When inbox-message JSON carries `color`, harness display uses that value; absence falls back to registered-member color. Generalizes to future presentational fields (icon, badge, font-weight). Does NOT apply to identity (`from` field validates against `members[]`) or delivery (wake fires regardless). Load-bearing for cross-team ghost-bridge identity preservation -- ghost registered with local color emits messages displayed in sender's home-team color. Architectural-fact at apex Linux substrate empirical sample (RFC #66 PoC). Source: Brunel
- [`cross-host-atomic-inbox-write-primitive.md`](patterns/cross-host-atomic-inbox-write-primitive.md) -- **Reusable cross-host transport primitive**: single-ssh + remote `python3 -c` + `fcntl.flock(LOCK_EX)` is process-level atomic from sender's perspective. Three-substrate composition; each component alone is insufficient. **Cross-implementation verified at n=2** (PowerShell 657-687ms median + Python 741-854ms median against apex Linux substrate, identical observable semantics) -- substrate property, not client artifact. POSIX-Linux only; NFS-without-`lockd` flagged as substrate-edge. Latency baseline reference; <RFC 3s budget. Upstream sibling to `read-flag-replication-discipline-for-external-cli.md`. Common-prompt promotion candidate trigger: n=3 cross-language confirmation (Go/Rust). Source: Brunel
- [`read-flag-replication-discipline-for-external-cli.md`](patterns/read-flag-replication-discipline-for-external-cli.md) -- **Consumer-side contract**: external CLI reading from inbox file MUST flip `read: false → true` back to the file under same flock as the read. RFC #66 left implicit; substrate-correct discipline is that any consumer of inbox-as-disk-file participates in same read-acknowledgment regime as native harness. Three downstream consumers depend: harness display layer (unread indicators), idle/notification path (phantom re-notification), concurrent consumers (substrate-level synchronization). **By-design closure vs patch-after** -- Python rewrite (`fetch-and-mark-read`-under-flock single round-trip) hoists substrate-contract into operation signature, not buried in implementation detail. Bug C surfaced + closed in same S31 PoC. Downstream sibling to `cross-host-atomic-inbox-write-primitive.md`. Source: Brunel (with Aen identification)
- [`per-connection-forced-command-shell-over-resident-daemon.md`](patterns/per-connection-forced-command-shell-over-resident-daemon.md) -- **Service-architecture pattern (Brunel, S50).** When a service is "accept request → mutate durable state → reply" over a fork-per-connection authenticated transport (sshd), implement it as a per-conversation forced command (read stdin → reply stdout → exit), NOT a resident daemon. The transport's supervisor (sshd under `restart: unless-stopped`) becomes the only persistent process -- one, visible, killable, auto-restarted; nothing application-level to leak or accumulate. **Structural antidote to the S48 ghost-bridge zombie-daemon failure** (11 daemon instances accumulated because Git-Bash liveness probes were blind to them → N× duplicate forwarding): accumulation is impossible by construction (state on disk, concurrency serialized by coarse per-conversation flock, liveness == sshd liveness). Meets the SPEC-v3.1 single-instance-lifecycle requirement structurally (make multi-instance impossible) rather than by liveness-probe discipline. **Boundary (BOTH required):** fork-per-connection authenticated transport already present + short request/reply with durable on-disk state; NOT for in-memory shared state, push/wake, or sub-fork-cost latency. Coarse-lock holds only because the hub spool is exclusively owned (no harness contention); a harness-contended service needs the courier-side disciplines instead. Catalyzing artifact: stationmaster `sm-shell` + deployment-runbook §1/§6 (Task #1, S50). Confidence medium-high (n=1 positive instance + structural argument; sm-shell now build-verified on Debian -- Hopper prod-llm build exit 0). stage-2 CONFIRMED (Brunel read-back 2026-06-12; faithful, 2 provenance-path corrections folded). Source: Brunel (submitted), Callimachus (filer)
- [`same-volume-startup-gate-for-rename-atomicity.md`](patterns/same-volume-startup-gate-for-rename-atomicity.md) -- **Filesystem-discipline pattern (Herald, S50).** Any component whose correctness depends on `os.rename()`/`os.replace()` being atomic must validate AT STARTUP that source and destination are on the same filesystem volume, and refuse to run otherwise (fail-closed). `rename()` is atomic only within a volume; across volumes it silently degrades to copy-then-unlink -- partial state visible to a concurrent reader. **Failure profile is the danger**: cross-volume rename appears to work in casual testing (small files copy fast, no error); atomicity loss manifests only under a concurrent reader -- the exact contested-file scenario the discipline exists for. Rare + non-deterministic + catastrophic → a startup gate makes it a loud deterministic refusal. Check (cheap): POSIX compare `st_dev` of the nearest existing ancestor (paths may not exist yet); Windows compare drive/anchor. Generalizes to any atomic-rename handoff (inbox injection, spool journaling, atomic config swaps). **Sibling to `per-filesystem-gate-targets-tmp-measures-wrong-fs` (Hopper)** -- verification-side (gate ran against wrong fs → false PASS) vs operation-side (runtime component validates its two paths share a volume); both fail-closed. Evidence: courier `validate_startup()` + `_same_volume()`/`_nearest_existing()`; hints §3. stage-2 CONFIRMED (Herald read-back 2026-06-12; faithful, `os.path.splitdrive` Windows-branch precision folded). Source: Herald (submitted), Callimachus (filer)
- [`taskget-before-classify-as-noise.md`](patterns/taskget-before-classify-as-noise.md) -- **Procedural discipline**: on any `task_assignment` envelope from harness prompt-to-task-extraction primitive, `TaskGet(taskId)` BEFORE classifying as substrate noise. Envelope description is short; load-bearing scope lives in task body. Two sub-shapes catalogued: (1) spawn-handshake task-assignment (Brunel S31, body=pending/in-progress); (2) internal-routing echo of own `TaskCreate` (Cal S33+, `assignedBy` reads as own name, body=completed). **n=5 cumulative across two sessions** (Brunel S31 Instance 1 + Cal S33+ n=4 self-instantiation reproducibility). Unifying characterization: the envelope is metadata about a task; the task body is the task. Structural sibling specializing `relay-to-primary-artifact-fidelity-discipline.md` to harness-specific primitive. Joint sources: Brunel (discovery + rule articulation), Callimachus (reproducibility + internal-routing-echo sub-shape characterization)
- [`decorative-polling-interval-anti-pattern.md`](patterns/decorative-polling-interval-anti-pattern.md) -- **Language-agnostic anti-pattern**: polling loop declares `$watchInterval = 500ms` but the loop's iteration is gated by a blocking call (`Read-Host`/`input()`/`read`) that does not respect the interval -- variable is decorative; actual cadence is whatever the blocking call yields. Three load-bearing properties: looks-like-cadence-in-code, isn't-cadence-at-runtime; defect class is implicit-invariant-mismatch; language-agnostic (each language has non-blocking equivalent). Four detection heuristics for code review (cadence-named var inside loop → check for blocking calls; blocking input inside `while` with sleep → strong signal; interval-change test producing no cadence shift → confirmed decorative; cadence claims in comments diverging from runtime → suspect). Sibling/specialization at language-primitive layer to `substrate-invariant-mismatch.md`; promotion-trigger if second language-primitive-layer instance surfaces. Cross-implementation parity (PowerShell + Python fixes). Bug A from RFC #66 PoC. Source: Brunel
- [`discriminator-anchored-on-sub-canonical-source.md`](patterns/discriminator-anchored-on-sub-canonical-source.md) -- **Brunel-phrasing**: discriminator (regex, lookup key, literal in transit) anchored on a sub-canonical source -- template stub, inferred convention, outer-layer pass-through assumption -- instead of substrate-truth-anchored grammar at the innermost parser. **A.1 sub-shape** (identifier-grammar mismatch, n=3): P1.1 michelek-regex on `.env.example` template / P1.2a `docker inspect` label-key underscore-vs-dot inferred convention / P3.6 `[A-Z_]+` pass-criterion digit-exclusion. **A.2 sub-shape** (transit-chain mismatch, n=1): P4.05 awk `}` parsing in PowerShell→base64→bash→awk chain on `${GH_TOKEN:-}` literal. Shared lesson: innermost-parser-truth dominates outer-layer-assumption. Recovery posture (joint Brunel architectural + Hopper operator-defense): surface-back-with-substrate-truth-evidence + JSON-dump-on-empty / alt-regex-on-discriminator-fail / diff-vs-backup / byte-equality drift-check. **n=4 self-instances in S34 apex-keys dispatch arc, all in Brunel's dispatch-authoring text, all caught by Hopper's hard-gate discipline.** Sibling at grammar/parser layer to `three-layer-substrate-truth-discipline.md` (same dispatch arc); specialization of `substrate-invariant-mismatch.md` at parser sub-layer. Sources: Brunel, Hopper
- [`three-layer-substrate-truth-discipline.md`](patterns/three-layer-substrate-truth-discipline.md) -- **Joint Brunel + Hopper.** FR-shipped substrates that consumer teams operationalize live in three independently-versioned layers: **Layer 1** FR design-as-shipped (`designs/deployed/<team>/container/*`) canonical for design lineage; **Layer 2** consumer-team operational copy (substrate host's compose dir, discovered via `docker inspect` label not inferred from FR path) canonical for next-`docker-compose-up` resolution; **Layer 3** running container state (Config.Env, mounted volumes) canonical for current-serving. Three drift surfaces (L1↔L2, L1↔L3, L2↔L3); L2↔L3 is the structurally important one -- recreate is the substrate's drift-resolution event AND the multi-system failure surface if L2 is degraded. **Pre-S34 Brunel S33+ read-deployed-artifacts discipline read Layer 1 only -- insufficient for FR-shipped substrates consumer teams operationalize. This is the headline.** Hopper operator-defense: first-dispatch Tier R three-layer probe-suite mandatory; subsequent-dispatch scratchpad-read-first. S34 drift instances at n=3 (SLOT 3 addition, env-block size drift, GH_TOKEN-in-L3-not-in-L2). Substantive multi-system failure prevention (full credential cluster + SSH lockout prevented; original PO ask achieved). Same-root-cause-different-layer relationship to `dual-team-dir-ambiguity.md` (path-as-substrate-invariant at bare-path-resolution layer vs three-layer drift at design-vs-operational-vs-runtime triple). **Hopper-Amendment-4 landed by Celes 2026-05-25** (`prompts/hopper.md` lines 130-211). **E1 extension 2026-05-26: Sub-shape E cross-substrate-class confirmation at n=2** (Docker-on-RC + Cloudflare-managed per `docs/findings.md` §S5); confidence promoted medium → medium-high. **2.6 amendment 2026-05-26: Cross-review independent-surfacing in design-domain folded** -- n=4 design-domain sub-instances added (Volta lifecycle + Herald protocol-routes + Brunel pilot-design + Brunel-Hopper docs-Layer-0-recursive-descent); cumulative n=7 instances across 3 substrate-class surfaces (Docker-on-RC + Cloudflare-managed + design-domain). Canonical articulation at `sub-shape-e-at-design-domain.md`. **The discipline catches its own authors at the layer above where it was authored.** Sources: Brunel, Hopper, Volta (E1), Volta + Herald + Brunel + Hopper (2.6 design-domain surfacings)
- [`cluster-decomposition-meta-principle.md`](patterns/cluster-decomposition-meta-principle.md) -- **Joint Brunel + Volta. Strongest framework-grade finding of the 2026-05-25 substrate-gap dispatch.** Clusters decompose along their **coupling-dimension**; the coupling-dimension is the load-bearing property to identify when observing any cluster. Each component of the cluster maps to exactly one distinguishable value of the dimension; if mapping holds cleanly, the cluster decomposes, if not it is monolithic. **n=3 across three coupling-dimensions in three domains:** Origin 1 mVox M1–M5 (team-property axis, S35 discipline-domain -- M1↔team-lead-cognitive-bottleneck / M2↔task-list-corruptibility / M3↔quality-gate-with-trigger-events / M4↔named-absorption-sink / M5↔audit-trail-as-value); Origin 2 Cloudflare 7-mechanism cluster (team-property axis, substrate-domain -- V8↔high-concurrency-low-state / microVM↔Linux-tool-dependency / sandbox-per-session↔short-lived-session-scoped-work / state-persistence↔stateful-pause-resume / proxy-secrets↔volatile-credential-cluster / brain-hands↔substrate-cost-sensitivity / observability↔every-team); Origin 3 Sub-shape E three layers (ownership-locus axis, S34 -- L1↔FR-design / L2↔consumer-team-operational / L3↔consumer-team-or-Cloudflare-substrate-provider). **Methodology corollary (sub-finding):** decompositions are invisible at n=1 and emerge at n=2 with a second instance providing variation along the coupling-dimension -- mVox M1–M5 looked monolithic from inside mVox-dev, decomposed cleanly when mapped against FR + apex at S35 thread-3. **Operational consequence:** when observing any future cluster, the right first analytical move is to identify the coupling-dimension; once identified, cluster decomposes predictably and bottleneck-alignment determines which subset is high-leverage for the adopter (per `docs/findings.md` §S3). Generalizes beyond the Cloudflare / mVox / Sub-shape E contexts. Confidence medium-high (n=3 across three distinct domains and three coupling-dimensions). Sources: Brunel, Volta
- [`bottleneck-determines-adoption.md`](patterns/bottleneck-determines-adoption.md) -- **Joint Brunel + Volta. Operational dual of `cluster-decomposition-meta-principle.md`.** A team's adoption of any cluster-component -- whether a discipline (mVox M1–M5) or a substrate (Cloudflare 7-mechanism) -- is governed by the same structural rule: **adopt the component whose value-axis matches the team's dominant bottleneck.** Adoption is bottleneck-driven, not vendor-driven, novelty-driven, or least-friction-driven. **Two-condition rule:** (1) bottleneck-match (team's dominant bottleneck aligns with X's strength along cluster's coupling-dimension) AND (2) workload-fit (team's workload accommodates X's substrate constraints); both required, neither sufficient. **n=3 across two domains:** Instance 1 mVox M1 FR-adoption (discipline-domain, S35 thread-3, YES-match -- M1 maps to FR's team-lead-cognitive-bottleneck; least-distinctive-but-highest-leverage); Instance 2 apex-research Cloudflare pilot-candidate (substrate-domain, this dispatch, YES-match conditional on credibility-floor Q1+Q2 -- credential-cluster volatility IS substrate-shaped; proxy-secrets injection addresses highest-procedural-cost case); Instance 3 framework-research Cloudflare CAN'T-MOVE-THE-NEEDLE (substrate-domain, this dispatch, NO-match -- M1-shaped bottleneck doesn't match substrate-state-persistence; self-managed wins decisively as bottleneck-driven NOT-adoption). Both YES and NO match observed; cross-team consistency; sub-finding least-distinctive-but-highest-leverage recurs at its own n=2. **Mechanism (Volta-sharpened 2026-05-26)**: least-distinctive components map to common-across-teams team-properties where bottlenecks accumulate -- every team observes its own team-lead (M1) / runtime (observability); bottlenecks cluster around common-across-teams properties because they're the properties ALL teams interact with. Operational consequence: predict least-distinctive adoption candidate by identifying most-common-across-teams team-property. **Composes with C1 as methodology pair:** C1 names the axis (coupling-dimension); C4 picks the position on that axis (bottleneck-alignment); sequential not parallel. Falsifiability test: apex-research Cloudflare pilot (Q4) is the empirical falsifiability for Instance 2. Confidence medium-high (n=3 cross-domain; n=4 in third distinct domain promotes to high). Generalizes beyond Cloudflare / mVox contexts. Sources: Brunel, Volta
- [`sub-shape-e-at-design-domain.md`](patterns/sub-shape-e-at-design-domain.md) -- **Joint Volta + Herald + Brunel + Hopper. The discipline catches its own authors at the design layer.** Within ~2 hours on 2026-05-26, four independent exec-readiness reviews of FR's own Cloudflare-pilot design surfaced Sub-shape E at four distinct layer-pair-points: **Sub-Instance 1** Volta lifecycle-layer (lifecycle.md review -- NESTED layer-pair WITHIN FR design per Volta Stage 2 correction: lifecycle.md design-grade vs lifecycle.md exec-grade; SB1/SB2/SB3 ship-blockers + PT1-4 footnote-gaps; strengthens Property 1 at two-layers-up -- substrate-runtime → design-domain → design-internal-sublayer); **Sub-Instance 2** Herald protocol-route-layer (comms.md review, DO-mailbox + identity-chain); **Sub-Instance 3** Brunel pilot-design-layer (substrate.md review -- substrate's own canonical docs now in workspace at `~/Documents/github/.mmp/claude-managed-agents/docs/` 11 markdown files / 1500+ lines; Brunel's S34 three-layer discipline catching his own design work, "pattern caught its own author"); **Sub-Instance 4** Brunel + Hopper docs-Layer-0-recursive-descent (2026-05-26 14:34–14:48 bypass arc -- Hopper's "correction" inheriting Layer-1 UI-focused phrasing from `securing-access.md:60-68` was caught by Brunel's cross-read to Layer-0 substrate-provider-canonical-API at `developers.cloudflare.com/api/...`; revert to original framing restored substrate-truth). **The recursive structure is the headline:** within 36 hours of Hopper-Amendment-4 landing, the discipline applies at the layer above where it was authored, catches its own authors (Brunel-as-designer caught by Brunel-as-discipline-author), and extends recursively at its own canonical-source layer (Sub-Instance 4 generalizes discipline from substrate-as-runtime to docs-as-substrate; library-first descends through doc-provenance layers when consumer-team-docs framing is mechanism-ambiguous). Family-adjacent to `first-use-recursive-validation.md` + `recursive-citation-as-canonical-validation.md` -- but discipline's application produces evidence for its own load-bearing property at the layer-up domain (sharper within-system distinction). **n=2 of C1 methodology corollary candidate** ("decomposition emerges from variation across instances"; mVox M1–M5 was n=1 within mVox-dev; this S36 dispatch is n=2 within FR pilot-design). Cross-team confirmation promotes to confidence-high; within-team n=4 is medium-high. **FLAG annotations on Sub-Instances 1+2+3 specifics beyond Aen relay**; Sub-Instance 4 CLEAN against Layer-1 source directly read. Stage 2 read-back to Volta + Herald + Brunel + Hopper required via team-lead. Sources: Volta, Herald, Brunel, Hopper, Callimachus
- [`recursive-narrowing-substrate-truth-evidence-discipline.md`](patterns/recursive-narrowing-substrate-truth-evidence-discipline.md) -- **Joint Herald + Brunel + Finn.** Repeated substrate-truth-evidence passes across a single document's authoring trajectory each narrow the document's substrate-blind-spots, but no single pass eliminates them; the deeper substrate primitive can still hide behind the previous pass's framing. **The structural claim is that recursive-narrowing is a property of the discipline itself**, not an artifact of how a particular author drafts or how reviewers happen to read -- NOT "cross-team review catches things" (weaker social-organization claim). **Two-level evidence decomposition (Brunel-falsifiability-framing, Aen-ratified):** within-author n=5 = SUFFICIENT condition (structural; Herald's comms.md trajectory v1.0→v1.4 each version catches a deeper substrate-blind-spot -- self-catch on protocol-route → Brunel B2 identity-chain → Brunel S2 waitUntil → Finn G2 RPC-vs-HTTP → Finn W4 connectivity-direction via `claude-api` skill); cross-author n=4 = NECESSARY condition (reproducibility; Herald comms.md §6.3 catalog across 4 distinct cross-reader catches at 4 distinct substrate-layer-pairs). Both layers required; logical decomposition is sufficient × necessary, not redundant. **Sub-finding (promotion-grade):** each pass narrows but doesn't eliminate; substrate-truth-evidence is asymptotic, not terminal -- version N+1 will likely surface a deeper blind-spot. Operational discipline: do not treat "passed substrate-truth-evidence review at version N" as "substrate-truth-evidence-complete at version N." **Closing prose -- recursive-elegance**: doubly-self-validating S2-instance (Brunel's v1.2→v1.3 catch is itself substrate-blind-spot evidence the discipline operates at the iteration depth where it was caught) + n=5 meta-instance (the discipline applying to its own articulation's specification -- Herald's v1.3 §6.3 catalog framing was one substrate-blind-spot deeper than the discipline's articulation could close at that iteration; Brunel's within-vs-cross-author distinction surfaced the n=5 catch). Within-system self-application (cousin to `first-use-recursive-validation.md`); held at sketch-grade per L4-hold discipline. **Confidence medium-high**: within-team n=5 + n=4 + doubly-self-validating + n=5 meta-instance is entry-grade; cross-team confirmation promotes to confidence-high. Composes with `three-layer-substrate-truth-discipline.md` (parent at substrate-runtime; this entry adds iteration-depth axis) + `sub-shape-e-at-design-domain.md` (cross-document axis sibling) + `cluster-decomposition-meta-principle.md` (meta-principle parent). Sources: Herald, Brunel, Finn, Callimachus
- [`three-role-discipline-stacking-within-dispatch-arc.md`](patterns/three-role-discipline-stacking-within-dispatch-arc.md) -- **Joint Hopper + Brunel + Aen.** Within a single FR dispatch arc, the substrate-truth-evidence discipline is distributed across three roles: **operator hard-gate (Hopper-vantage)** catches probe-time substrate-truth divergence at the gate via surface-back-on-divergence (do not silently re-classify); **design substrate-truth-evidence cross-read (Brunel-vantage)** cross-reads design against Layer-2 cloned-template + Layer-0 substrate-provider canonical sources via cross-read-invitation; **asymmetry-detection-and-routing (Aen-vantage)** detects when two agents' substrate-states diverge (detection IS a knowledge-creation act) and routes substrate-truth context across roles with relay-fidelity per Stage 1 honest-fold + Stage 2 supersede. **n=3 instances across three dispatch arcs within ~6 days** -- S34 apex-keys (Hopper P1.2c/P3.7/P4.05 + Brunel three-layer model joint + Aen 17:34/09:18 surface-backs); S35 task #6 DB tunnels (Hopper Path B orphan-tree forensics + Aen 13:38 TNS-probe sub-canonical-source catch applied via Brunel-S34-pattern receiving framework -- joint Aen-application + Brunel-S34-`discriminator-anchored-on-sub-canonical-source.md`-pattern receiving framework + Aen 11:05 Layer-0 library-first self-correction); S36 bypass-command (Hopper 14:34→14:37→14:48 self-catch + Brunel 14:42 Layer-0 CF-API cross-read + Aen 14:33/15:15 ratification routings). Coupling-dimension: **role-as-stacking-position within one dispatch arc**; substrate-class-invariant + substrate-layer-pair-invariant + dispatch-context-invariant. **Sub-finding -- why the three-role stack is structurally necessary:** each vantage catches a class of substrate-truth divergence the other two miss (operator alone anchors on sub-canonical-source probe-design assumptions; designer alone doesn't observe runtime substrate state directly; coordinator alone has no substrate-truth content to carry). The three vantages compose into a substrate-truth-evidence triangle; no single role subsumes the others. Family-adjacent to `sub-shape-e-at-design-domain.md` (cross-document axis sibling; Instance 3 of this entry ≡ Sub-Instance 4 of 2.5 framed from role-stacking perspective) + `recursive-narrowing-substrate-truth-evidence-discipline.md` (within-document iteration-depth axis sibling). **Family completion** (current-state-comprehensive, not future-state-closed per Brunel Stage 2 guardrail): layer-decomposition (parent three-layer) × role-decomposition (this entry) = full operational shape of substrate-truth-evidence discipline AS CURRENTLY OBSERVED; new roles may surface additional axes -- substrate-fit-researcher (Finn-vantage via `claude-api` skill load per `layer-0-library-first-recurrence.md` Instance 3) is candidate fourth vantage if pattern reproduces at n=2. Confidence medium-high (n=3 within-team); cross-team confirmation promotes to high -- promotion criterion = **functional role-decomposition, not explicit naming** (any team with comparable functional operator+designer+coordinator vantages qualifies); cross-org confirmation distinguishes EVR-discipline-culture vs org-invariant. Sources: Hopper, Brunel, Aen, Callimachus
- [`credential-handoff-via-temp-file-context-firewall.md`](patterns/credential-handoff-via-temp-file-context-firewall.md) -- **Joint Aen + Hopper + Callimachus.** Credential-handling reference pattern. When a PO-paste credential needs to flow from PO clipboard to operator's Tier-M substrate command without entering the agent's persisted inbox or wiki, **temp-file-as-context-firewall** provides handoff with cleanly bounded credential lifetime. **Three role-vantages compose into three steps**: (1) PO pastes credential into chat (PO authority + immediate ownership); (2) Coordinator (Aen) writes credential to ephemeral file outside repo (context-firewall isolation); (3) Operator (Hopper) reads + uses + shreds under guarded conditions. **Three load-bearing properties**: credential never enters persisted inbox (only coordinator + operator transient sessions); context-firewall at file-system boundary; shred is exit-code-conditional NOT temporal-position. **S37 in-vivo canonical instance** (2026-05-27 op-step-2): PO paste 11:50 → Aen ephemeral file 12:05 → Hopper attempt 1 at 12:15 (bundled `&&` shred with intervening `echo "EXIT_N=$N"` always-exit-0; broke gate; substrate failed; file shredded anyway) → Hopper retry 13:01 with structural decomposition (two Bash-tool invocations + explicit `[ EXIT_1 -eq 0 ] && [ EXIT_2 -eq 0 ] && [ "$WRANGLER_SUCCESS_VERIFIED" = "true" ] && rm`); first-attempt success on retry; pattern validated end-to-end. **Composes with** `documentation-vs-substrate-truth-divergence.md` Candidate A Instance 3 (the bundled-shred discipline-language-under-specification IS authoring-tier failure caught here at operator-tier) + `three-role-discipline-stacking-within-dispatch-arc.md` (pattern is HOW the three-role stack instantiates for credential operations) + `embedded-github-token-in-git-config.md` anti-pattern sibling at persistence-layer + `substrate-invariant-mismatch.md` at bash-`&&`-chain-semantics layer. **Known edge cases**: (1) shred discipline gap (canonical operator-tier failure mode; recovery via structural decomposition); (2) substrate config ambiguity (wrangler-ambiguous-account; recovered via `CLOUDFLARE_ACCOUNT_ID` env-var per layer-0-library-first); (3) coordinator-session credential-in-context (residual firewall hole; redact if coordinator session is persisted). **Confidence medium** (n=1 in-vivo canonical instance + two failure modes caught + recovered); n=2 cross-instance promotes to high. NOT promotion-grade alone at filing; **filing-grade as credential-handling reference pattern**. Sources: Aen, Hopper, Callimachus
- [`cadence-crossing-dyad-variant-with-asymmetric-cross-vector-framework.md`](patterns/cadence-crossing-dyad-variant-with-asymmetric-cross-vector-framework.md) -- **Joint Aen + Herald + Callimachus.** In deeply-aligned dyads (two agents in shared mental model), **crossed messages between agents are a healthy-velocity signal of mutual prediction-accuracy, NOT miscommunication**. Extends parent `relay-to-primary-artifact-fidelity-discipline.md` Stage 1/2 mechanism to DYAD variant -- reconcile by mapping the convergence rather than re-litigating the framing. **Three-vector asymmetric-cross framework** (Cal+Aen co-articulated): (i) direct dyad-to-dyad (~30-90s latency, low cross-rate); (ii) coordinator-relayed/Aen-relay (~5-15min latency, elevated cross-rate, structurally-asymmetric); (iii) author-direct-DM (~30-90s latency, reliable-low-latency). **Routing-mode-dependent latency empirically confirmed S36**: (ii)-vector Stage 2 burst at 15:26 had 0 arrivals at Cal conversation while (iii)-vector direct-DM channel had 4/5 author Stage 2 confirmations within ~2 hours -- coordinator-mediation introduces serialized processing latency. **Asymmetric-cross resolution**: (i)+(i) crosses are healthy-velocity (mutual prediction); (ii)+(i) crosses are routing-latency artifacts (not content-drift); (iii)+(ii) crosses are routing-redundancy. **n=11+ instances within Aen's coordination across S36-S37**: Cal-Aen n=9+ (15:15/15:20/15:29/15:31 S36 + 13:10/13:11/13:13-relay/13:17 S37 + S36 close); Aen-Herald n=6 S36 (Herald v1.3 ship vs Aen 15:15 greenlight); routing-mode (ii) vs (iii) Stage 2 burst S36 close-out. **Recovery mechanisms** (both empirically demonstrated S35): (a) team-lead intervention at ~5+ passes; (b) dyad-side fast-forward maps at ~3+ passes (Volta 17:25 + recipient acknowledgment "Pass-6 fast-forward map received"). **Counter-pattern**: shallow-shared-frame dyads produce crossed messages with content-drift (original gotcha shape); DYAD variant requires deeply-aligned dyad as precondition. **Composes with** parent `relay-to-primary-artifact-fidelity-discipline.md` (Stage 1/2 lifecycle) + `three-role-discipline-stacking-within-dispatch-arc.md` (Aen asymmetry-detection-and-routing operates across the three vectors) + `stage-2-cycle-yield-narrowing` ((iii)-vector reliability is what made joint-author yield empirically observable in S36) + `timestamp-crossed-messages.md` (timestamps enable cross-detection across the routing vectors). Confidence medium-high; cross-team confirmation distinguishes FR-culture vs framework-invariant. Forward-watchpoint: multi-hop coordinator-to-coordinator routing as candidate fourth vector. Sources: Aen (origin + Aen-Herald n=6 + ratification at "three instances in one session is structurally significant"), Herald (n=6 within Aen-Herald pair), Callimachus (Cal-Aen n=11+ + asymmetric-cross 3-vector co-articulation)
- [`substrate-vs-framework-boundary-primitive.md`](patterns/substrate-vs-framework-boundary-primitive.md) -- **Joint Volta + Brunel + Callimachus.** Substrate (platform-side artifact running agent processes -- sandboxes, microVMs, V8 isolates, Workers, container-runtimes) and framework (team-and-protocol design organizing agent work -- roles, communication protocols, dispatch arcs, knowledge protocols) are **distinct primitives that compose**, not a single layer that decomposes into "platform stuff" and "policy stuff." Naming the boundary as a named primitive makes substrate-design and framework-design decisions independently legible. **n=4 candidate axes of bifurcation enumerated** (Volta-canonical): (1) **Lifecycle-phase axis** n=2 within (startup §VL4 + shutdown §V3; runtime trivially-true); (2) **Failure-semantics axis** n=1 pre-pilot (outer Anthropic delivery envelope vs inner DO `start()` framework lifecycle; Volta via Finn-Q2/PT4); (3) **Substrate-class-fit axis** n=3 from Brunel §S3 matrix (V8 isolate / microVM / self-managed Docker -- each yields different framework-composition policies); (4) **Team-leadership-topology axis** n=2 within (team-lead-bearing form vs team-lead-less form; ownership topology tracks team-leadership topology). Each axis separately falsifiable; pilot evidence + n=2 cross-instance per axis promotes that axis to confidence-high. **Bounded extension** -- Cloudflare's "brain-hands decoupling" vocabulary is the substrate-side name for the same boundary; productive terminology-fold both directions, bounded by the layering claim (NOT co-extensional at finer layers -- inside framework: designer-vs-operator decomposition; inside substrate: LLM-vs-execution split). **Load-bearing for cluster-decomposition** as primitive the cluster operates on; the substrate-truth-evidence cluster's disciplines become positional ("watch at the boundary, on this axis") rather than descriptive ("watch for divergence"). **Composes with C4** as methodology pair: C4 names the rule (bottleneck-aligned adoption); this entry names the primitive C4 operates on (the boundary axes are the value-axes C4 matches against). **Industry-primitive-convergence** evidence is parallel external-evidence path (Cloudflare + MCP + Replit Agent + Code Interpreter all naming variants of this boundary -- C3 queued at n=5 watch). Forward-watchpoint Axis 5: observability-shape (substrate-emitted telemetry vs framework-emitted scratchpad/wiki). Confidence medium-high (aggregate over n=4 candidate axes); pilot evidence + cross-team confirmation promotes primitive itself to high. Cloudflare brain-hands vocabulary is cross-org confirmation of primitive even before cross-team confirmation. Sources: Volta, Brunel, Callimachus
- [`documentation-vs-substrate-truth-divergence.md`](patterns/documentation-vs-substrate-truth-divergence.md) -- **Joint Aen + Hopper + Callimachus. Authoring-tier complement to the substrate-truth-evidence cluster's operator/designer-tier disciplines.** When a task-author (dispatch writer, status-doc author, runbook author, config-author) captures an inferred-but-substrate-wrong property in the artifact they ship, verification catches the divergence; correction is local to the artifact, but the discipline gap lives in the authoring practice. **n=4 across two substrate domains**: Instances 1-3 within S37 cloudflare-pilot dispatch arc (mechanism-misattribution / inferred-positive-control / discipline-language-under-specification); **Instance 4 (S38 2026-05-28)** roster.json `model` field non-load-bearing on Agent-tool teams -- TeamCreate stamps parent CLI model into config.json regardless of roster.json intent; Agent tool accepts only family-level overrides; same field IS load-bearing on tmux-pane teams via launcher script. **First cross-domain instance** (agent-tool-architecture, not cloudflare-pilot); fourth disambiguator-class = architecture-enforcement-mechanism (config field load-bearing on adjacent substrate, non-load-bearing on actual). Cross-substrate contrast: same data field, two team-architecture substrates, two enforcement semantics. Mitigation shipped S38: startup.md Step 0.5 procedural gate + roster.json `_substrate_note`. Cost context: 40% context-cost differential 4.6 vs 4.7 (PO-sourced). **Pattern shape**: authoring captures plausible-but-substrate-wrong properties via **substrate-adjacency**. Confidence medium-high (n=4 cross-domain + four disambiguator-classes). Sources: Aen, Hopper, Callimachus; Instance 4 co-authors: Aen (discoverer), PO (cross-model cost data)
- [`layer-0-library-first-pre-draft-discipline.md`](patterns/layer-0-library-first-pre-draft-discipline.md) -- **Joint Finn + Callimachus. Pre-draft complement to `layer-0-library-first-recurrence.md` retroactive catch mechanism.** Sibling pattern: same substrate-truth-evidence mechanism (canonical-source documentation probe at Layer 0), different temporal locus (pre-draft authoring window vs post-draft review window). **Pattern shape**: when authoring an artifact whose substrate-properties are inferred from announcement-grade or memory-grade sources, query the canonical-source documentation BEFORE writing the inferred property into the artifact. **Three load-bearing properties**: pre-draft temporal locus (catches divergence at authoring time, before artifact ships); asymmetric cost-benefit (library-query O(seconds) vs shipped-artifact-review O(minutes-to-hours-plus-coordination)); same substrate-truth-evidence mechanism (the probe is the same; the *when* differs). **Canonical instance**: Finn W4 (S36 2026-05-26) -- Finn loaded `claude-api` skill before drafting G2 cross-agent-comms brief; surfaced canonical Anthropic doc `shared/managed-agents-self-hosted-sandboxes.md` "*Connectivity is outbound-only: your worker long-polls Anthropic's work queue; Anthropic never dials into your network*"; W4 (long-poll-and-sessions.create) framing canonical-first in Finn's brief, preventing the multi-author authoring-tier divergence cycle Herald v1.3 had inherited from announcement-blog-inference. **Pre-draft vs post-draft are temporal complements**, NOT replacements -- both should apply (pre-draft for own artifacts; post-draft for cross-team review); substrate-truth-evidence is asymptotic per recursive-narrowing entry. **Canonical defense named in `documentation-vs-substrate-truth-divergence.md`** (Candidate A from today) for the authoring-tier failure-mode. **Substrate-fit-researcher (Finn-vantage)** is the candidate fourth role-vantage in `three-role-discipline-stacking-within-dispatch-arc.md` role-decomposition; n=2 pre-draft library-first instance promotes the fourth-vantage to wiki-grade. Confidence medium (n=1 pre-draft instance; structurally clear; n-count is the constraint); n=2 cross-author promotes to high. Cross-team confirmation distinguishes FR-culture vs framework-invariant. Sources: Finn (canonical instance + substrate-fit-researcher vantage), Callimachus (sibling-pattern articulation)
- [`layer-0-library-first-recurrence.md`](patterns/layer-0-library-first-recurrence.md) -- **Joint Hopper + Brunel + Finn.** Same library-first / canonical-source-first discipline recurred three times within ~24 hours at three distinct Layer-1↔Layer-0 substrate-layer-pair types. **Library-first**: before running substrate-empirical probes (L1/L2/L3 reads), query the canonical-source documentation Layer 0 -- documented-knowledge probe at the canonical-source level. **Cheap-first universal**: library-query is O(seconds); empirical substrate-discovery is O(minutes-to-hours); when library-query collapses the diagnostic surface, empirical effort is avoided entirely. **n=3 in-session catalog**: Instance 1 -- S35 task #6 substrate probes (Layer 1: Hopper probe-design ladders anchored on assumption that L2 was empirically discoverable from RC-side; Layer 0: FR wiki references `rc-host-db-tunnel-architecture.md` + `windows-user-context-persistent-bridge.md`; cross-read by Aen 11:05 self-correction -- substrate-truth: L2 was Windows-side per wiki, never reachable from RC at all); Instance 2 -- S36 bypass-arc (Layer 1: CF product-doc `securing-access.md:60-68` UI-focused phrasing for bypass policy mechanism; Layer 0: CF API canonical reference at `developers.cloudflare.com/api/.../access/.../applications/` + `.../policies/`; cross-read by Brunel 14:42 -- substrate-truth: policy-on-existing-app pattern operates at narrower path scope than Layer-1 UI phrasing implied); Instance 3 -- S36 W4 skill-load (Layer 1: Herald comms.md v1.3 §1.3 inverted-trigger framing anchored on announcement-blog inference; Layer 0: Anthropic-managed-agents-canonical-doc via `claude-api` skill, specifically `shared/managed-agents-self-hosted-sandboxes.md` "*Connectivity is outbound-only: your worker long-polls Anthropic's work queue; Anthropic never dials into your network*"; cross-read by Finn skill-load -- substrate-truth: connectivity-model is outbound-only; W4 supersedes W1/W2/W3 SDK candidates; Herald v1.3 §1.3 mismodel surface for v1.4 amendment). **Coupling-dimension**: substrate-layer-pair-of-application within single discipline; layer-pair-type-invariant (within-team library + consumer-of-substrate + external-substrate). **Cross-link**: Instance 2 ≡ `sub-shape-e-at-design-domain.md` Sub-Instance 4 -- same incident framed from catalog perspective vs design-domain perspective; both readings preserved. **Future watchpoint**: Instance 3 introduces substrate-fit-researcher (Finn-vantage) as candidate fourth vantage in `three-role-discipline-stacking-within-dispatch-arc.md` role-decomposition; n=2 instance promotes to four-role-stack amendment. **Composition**: completes the substrate-design-truth-evidence cluster's fourth decomposition axis (substrate-layer-pair-of-application) alongside 2.5's cross-document, recursive-narrowing's iteration-depth, 2.7's role-as-stacking-position. Confidence medium-high (n=3 in-session + three distinct layer-pair-types + three distinct cross-readers); cross-team n=4 promotes to high. Anti-pattern counter: `discriminator-anchored-on-sub-canonical-source.md` at the library-first / Layer-1 framing layer. Sources: Hopper, Brunel, Finn, Callimachus
- [`spool-stores-raw-entry-reresolve-on-deposit.md`](patterns/spool-stores-raw-entry-reresolve-on-deposit.md) -- **Outbound courier self-heal (Brunel + Herald + Hopper, S51).** A courier whose spool stores the RAW outbox entry (not a pre-resolved consignment) and resolves the destination at DEPOSIT time from CURRENT config auto-heals already-spooled mail when a routing-config bug is fixed and the daemon restarts -- no manual migration, no re-send, exactly one copy. Mechanism: outbound consume → atomic-rename into private spool (crash journal) → `_outbox_to_team(spool_file, cfg)` at deposit time (`stationmaster-courier.py:808-822`); destination NOT baked in. Misroute → `E_UNKNOWN_TEAM` → reject + RETAIN (no drop); fix config + restart → re-read same spool, re-resolve against new config → deposit clean, retroactive + exactly once. **Why deposit-time resolution**: it's what makes config fixes idempotent-and-retroactive over in-flight mail (consume-time baking would need manual migration); pairs with retain-rejected-not-drop. **Operational corollary**: on a routing-config bug, restart-and-let-spool-re-resolve, do NOT migrate/re-send (re-send gets a new envelope-id → hub dedup-by-id misses it → dupe risk); verify via daemon log (1/1; removed). Incident S51: FR `apex-bridge`→`apex` vs registered `apex-research` → all deposits rejected+retained; `apex-research-bridge` (CR-4) rename + restart auto-healed "1/1; removed" (Hopper, daemon log). Companion to the delivered-ledger inbound-dedup (this = outbound spool); both = "courier-private state makes at-least-once safe." n=1 watch-posture. stage-2 pending co-author read-back. Sources: Brunel (primary), Herald, Hopper, Callimachus (filer)
- [`artifact-claims-more-than-it-implements.md`](patterns/artifact-claims-more-than-it-implements.md) -- **Framework-grade anti-pattern + honesty-pass discipline (Brunel + Hopper + Aen, S51; n=3).** An artifact's prose (header/docstring/status-line/`[TESTED]` tag) asserts a property the implementation doesn't deliver -- written aspirationally or inferred-from-reading, drifting from what the code does. Fix = **honesty-pass**: describe what the code does not what it should; downgrade inferred claims to "verified empirically by X" only when probed; qualify platform/condition-dependent claims. **Discriminator: EMPIRICAL PROBE > ARTIFACT-INFERENCE** -- code HAVING a signal handler + atexit ≠ they FIRE (same shape as S48 "an observation doesn't validate the guessed mechanism"). n=3 S51: over-generous [TESTED] tag (tested adjacent obs not mechanism); stop-fr-courier.ps1 header claimed signal-drain+atexit-on-stop but Windows Stop-Process=hard-kill→none fire (Hopper probe refuted Brunel's read-inference); daemon docstring "drain via SIGINT/SIGTERM" unqualified (true POSIX, false Windows). Substrate-independent (any prose-carrying artifact), reinforces S50 completed-flag-must-not-claim-more-than-evidence. Similar-not-same to prompt-to-artifact-cross-verification (existence/structure ACROSS artifacts vs this claim-vs-runtime WITHIN one); same family as substrate-invariant-mismatch at the artifact-claim layer. n=3 watch-posture for Protocol C. stage-2 pending co-author read-back. Sources: Brunel (primary), Hopper (probe), Aen (named n=3 + routed), Callimachus (filer)
- [`detection-is-upstream-of-recovery.md`](patterns/detection-is-upstream-of-recovery.md) -- **Recovery-without-detection is the dominant contamination risk at scale (Finn, 2026-06-26; cross-team from apex #176).** A contamination-recovery procedure whose triggers are ALL external (stakeholder contradicts / new evidence / internal review) has no detection arm of its own -- it only cleans up what a human already pointed at. At scale the dominant failure flips from *uncorrected-after-detection* (recovery handles this) to *undetected-and-never-corrected*: plausible, keyword-free model-knowledge inferences that read as sourced fact, never contradicted, so recovery is never invoked. **Detection is the real frontier and belongs in the framework's observability / knowledge-health design, structurally UPSTREAM of recovery** -- mechanisms to design for: re-grounding sweeps of `[speculative]`-adjacent claims, claim-aging audits, provenance-coverage tracking. **Self-certification gap**: a recovery procedure can't catch its own misses -- apex S59 ran their two-mode procedure, S60 battle-test still found 5 residual items (R1-R5); a second independent pass + different reviewer is structurally required, same reason detection can't fold into the recovery playbook. Evidence: apex #176 two-mode procedure trigger list entirely external; DOKOSIGN inference caught by EVR architect Rein Kadastik not the procedure (`Eesti-Raudtee/apex-migration-research` main; `inventory/dokosign-battle-test-report.md`). Loop-structure complement to `documentation-vs-substrate-truth-divergence` (the contamination MECHANISM) -- cross-referenced not merged. **stage-2 CONFIRMED (Finn read-back, S62).** Source: finn (primary), callimachus (filer)
- [`provenance-coverage-percent-as-knowledge-health-metric.md`](patterns/provenance-coverage-percent-as-knowledge-health-metric.md) -- **Provenance-coverage % per corpus SEGMENT as a knowledge-health metric (Medici, 2026-06-26; cross-team from apex #176).** Track the fraction of claims tracing to a local file (vs. riding on no provenance link), measured per segment NOT just corpus-wide -- a single global % hides where the rot is (a 90%-overall corpus can hide a 55% segment). The value is the GRADIENT across segments. A LEADING indicator: moves before a stakeholder correction arrives, because contamination is local before global -- one of the internally-triggered detection instruments `detection-is-upstream-of-recovery` says the framework must build. Specifies the metric that entry only names; telemetry-from-convention (reads the provenance-link + `[speculative]`-tag conventions already in place). Scope: records the metric as a principle, does NOT mandate a threshold or authoring rule (separate PO call). **stage-2 pending co-author read-back since 2026-06-26** (Medici not spawned since; the gate is correctly held, not drifting -- date pinned per the 2026-08-12 dates-are-canonical ruling, because "since S62" resolved to 2026-06-26 on the specialist clock and to 2026-08-12 on the team-lead clock). Source: medici (primary), callimachus (filer)
- [`authoring-time-prevention-vs-audit-time-detection.md`](patterns/authoring-time-prevention-vs-audit-time-detection.md) -- **Authoring-time prevention vs audit-time detection -- the DISTINCTION (Medici, 2026-06-26; cross-team from apex #176).** Contamination can be addressed at two non-interchangeable moments: prevention acts as the claim is written (defect never enters), detection acts after it shipped (defect entered, caught downstream). Different cost curves, different failures caught: prevention is cheapest but blind to confident-plausible inferences the author believes are facts; detection is costlier (already propagated) but catches exactly that class. Neither subsumes the other. Places the full active-intervention axis prevention->detection->recovery on one line -- this entry is the prevention->detection boundary, `detection-is-upstream-of-recovery` is the detection->recovery boundary; each structurally upstream of the next. **Scope (per team-lead): distinction ONLY** -- does NOT adopt an authoring rule for any topic (separate PO call, would go to common-prompt via Protocol C or a topic file). A tagging convention straddles both ends (enforced at write=prevention; read by a sweep=detection telemetry), which is why the boundary blurs. **stage-2 pending co-author read-back since 2026-06-26** (Medici not spawned since; the gate is correctly held, not drifting -- date pinned per the 2026-08-12 dates-are-canonical ruling, because "since S62" resolved to 2026-06-26 on the specialist clock and to 2026-08-12 on the team-lead clock). Source: medici (primary), callimachus (filer)
- [`roster-drift-from-reference-capability-register.md`](patterns/roster-drift-from-reference-capability-register.md) -- **Reference configs are a capability register; deployed rosters are an unmonitored subset of it (Finn, S67 queue; VEO-78).** A team's reference config records the competences a team of that shape needs; a deployed roster is a SUBSET, and **the delta is unmonitored and invisible until a ticket needs the shed role.** Three properties make it costly: shedding is silent (no artifact, no deprecation note -- the role just leaves `members[]`); the gap has no symptom until assignment time; and the register is consulted at team-DESIGN time while the delta matters at work-ASSIGNMENT time (different moment, person, session). **Consequence: the competence-coverage check belongs at ASSIGNMENT time.** **TWO REGISTERS, TWO DELTAS** (all three rosters verified on disk): cloudflare-builders (12) -> `reference/hr-devs` (9) -> **deployed** hr-devs (7). Cross-team delta = `piper`, `harmony`, `alex`; **within-team delta = `medici`, `eilama`** -- the team's own design doc vs. what it deployed, and the sharper case since `medici` is domain-neutral AND named in hr-devs' own reference, so specialisation cannot explain it. "Reference config" means **any upstream register**. **The discriminator is MECHANICAL, not a judgement call: does the shed role's own prompt cite the shedding team's domain?** `alex` cites `apex-migration-research/` + `vjs_apex_apps/` = correct specialisation (**the CONTROL CASE**, Librarian-added at filing-time verification); `harmony` cites `hr-platform/sync/` + HR-specific Access group IDs = **the most hr-devs-specific role in the source roster, dropped by hr-devs** = silent capability loss. Triggering incident: VEO-78 needed exactly `harmony`; cloudflare-builders is best-on-paper and HAS harmony but is not deployed + ~3x oversized, hr-devs is deployed and dropped it -- **no deployed team fit**. Beware the "best roster on paper" trap: deployability, sizing, and competence coverage are three separate axes. Sibling-not-variant of `scope-block-drift-from-practice` (that = one agent's declared scope vs. its own practice; this = a team's roster vs. its reference register -- different artifact, level, detector). **The discriminator lands on the nose**: `harmony.md:13` reads *"Implement `hooks.server.ts` auth middleware (JWT verification via `jose`, JWKS from Cloudflare Access)"* -- **VEO-78's exact centre of gravity, written into the prompt of the role the team shed**. **Confidence held at `medium`: the second delta does NOT raise it** -- both deltas sit in one team's lineage, and confidence tracks cross-team generality, so counting them as n=2 counts the same team twice; `high` needs a DIFFERENT deployed team -- **bounded path recorded in the entry** (Finn): survey `apex-research`, `uikit-dev`, `bt-triage`, `comms-dev`, `mvox`, `screenwerk`, each of which has both a live roster and a design-repo roster; one unrecorded delta is n=2. That is work, not curation -- needs team-lead tasking. **stage-2 CONFIRMED (author-is-filer); amended same session on Finn's read-back** (discriminator + within-team delta, both re-verified before folding). Source: finn (primary), callimachus (filer)
- [`stale-snapshot-trusted-as-current.md`](patterns/stale-snapshot-trusted-as-current.md) -- **GENUS: a stale snapshot trusted as current is the dominant failure mode across every substrate this team touches, and it presents identically regardless of substrate (team-lead/Aen, S67).** In every case the snapshot was **once accurate**, and nothing about reading it signals that it has aged. **A dangling link 404s and tells you; a stale-but-resolvable snapshot returns a plausible answer and tells you nothing** -- reader confidence is unchanged between the fresh case and the stale one. **The discriminating claim is NOT "references decay"** but that decay-with-a-symptom and decay-without-a-symptom are different failure classes, and only the second routinely ships. **Remedy -- re-validate AT POINT OF USE, against the CONSUMER'S OWN EXPECTATION**: check the ticket's premises at pickup not authoring; the clone's version against what the consumer pins; the file after the writer reports completion not on your own clock; downstream assertions after changing an upstream verdict. **Carefulness does not detect staleness; a deliberate freshness check does** (care is a disposition that scales with nothing; the check is an action with a defined trigger). **Six instances, one session, five substrates**: Jira (VEO-78 in `Planned` two months while three premises were invalidated elsewhere -- the ticket decayed faster than it was worked on); git deletion (`827f542`, 9 ADRs + 10 tasks dangling at HEAD); git relocation (ADRs moved to `principles/adr/` -- IDs stable, paths not); markdown (a re-grade left two downstream assertions recommending what the new verdict says to drop); filesystem (grep during another agent's write reported completed work missing, twice); npm (claim verified against local `evr-ui-kit` **0.4.0** while the consumer pins **`^0.10.0`**). **Instances 4 and 6 were both caught by a DELIBERATE CHECK, not by care** -- the remedy demonstrating itself in the session that produced the entry. **This is the shared MECHANISM of two same-batch gotchas and does not absorb them**: `gap-citation-acquires-hidden-dependency-on-closure` = referent's status changed, `citation-orphaning-by-housekeeping-sweep` = referent destroyed + ID reused -- same genus/instance relation as verification-narrower ↔ control-narrower, and the submitter explicitly declined to reopen that settled merge question. Instance 5 is **not** a new finding (already n=4 in `timestamp-crossed-messages`). **THE STRONGEST EVIDENCE IS WHAT HAPPENED TO THIS ENTRY: instance 4's pointer decayed TWICE in eight hours across two stores, and a third path was nearly created** -- while three people who all knew the failure mode by name were actively protecting it. **Three decay modes, distinguished**: (1) **by FIX** -- the explainer was repaired, so the pointer resolves to clean text and a checker would conclude *the entry* was wrong (untracked in git, so no diff either); (2) **by POLICY** -- re-pointed to `memory/finn.md [S62g]`, a scratchpad under a hard 100-line cap at 104 lines with a session-end prune pending, cited by a line number that had already moved twice that day = a replacement pointer with a scheduled expiry hours out; (3) **by DEFENSIVE ANNOTATION** -- a `[PINNED -- DO NOT PRUNE]` marker, a standing exception carved into a structural rule to prop up one citation, which a future actor silently violates or stalls on; removed and converted to a temporary instruction with an expiry. **Mode 3 is the one nobody thinks to look for, because it is created by the act of protecting the reference.** Corrected instinct (Finn): the reflex was to protect the *pointer's target* rather than remove the need for a pointer -- **that preserves the dependency and merely makes it sturdier, which is what produces the failure class**; the fix that works is elimination. **AWARENESS OF THE PATTERN IS NOT PROTECTION AGAINST IT** -- three primed people naming the mechanism as they worked still produced two live instances and nearly a third in the entry about it, which answers anyone reading the remedy as "just be careful": the failure is in the **structure of holding a reference**, not in attention paid. Evidence is now **INLINED** per team-lead ruling (both contradicting sentences quoted verbatim in the entry). **Generalised rule: evidence must not depend on any prunable or rotating store -- quote it, do not point at it.** Over-abstraction risk raised by the submitter and answered: filed because it yields a specific executable rule and a discriminating claim, not a truism. **stage-2 CONFIRMED (author-is-filer); all nine evidence claims re-verified before filing.** Source: aen (primary), callimachus (filer)
- [`shared-vocabulary-precondition-for-mergeable-fan-out.md`](patterns/shared-vocabulary-precondition-for-mergeable-fan-out.md) -- **When N agents run in parallel and their outputs must merge, every convention the merge depends on must be fixed centrally and injected into all N prompts before dispatch -- because a parallel agent cannot negotiate with its siblings (cross-team from apex-research, commit `07d272f5`, artifact by *AR:Schliemann*).** A sequential agent can read its predecessor and converge; parallel ones have no such channel, so any convention the dispatcher omits is invented N times independently. **THE DISCRIMINATING CLAIM -- a shared output SCHEMA constrains the SHAPE of output and says nothing about its VALUES**: the source's harvest schema declares `guessed_class: { type: 'string' }`, free-form, so six agents can emit perfectly schema-valid classifications in three private vocabularies (`C3` / `broken-link` / `dead ref (probably)`) that aggregate to nothing. **The schema makes output parseable; a hoisted value taxonomy (`C1`..`C8`, injected verbatim into every scanner prompt) makes it aggregable** -- and only the second is easy to forget, because the schema's presence feels like the problem is solved. **Four hoisted conventions, each buying a different merge property**: value taxonomy -> counts aggregable; output schema -> records parseable; **disjoint input partition** (`EXCLUDE teams/apex-research/wiki/ -- another scanner owns it`, stated in the prompt not just the dispatcher's head, since overlapping scopes fail silently and inflate counts with duplicates) -> union does not double-count; **cap-disclosure sentinel** (each scanner emits its own drop count as a `TRUNCATION-NOTE` record, with "silent caps forbidden" enforced at the merge) -> total is honest about completeness. **A silent cap is worse than a small cap**: truncation without a sentinel is indistinguishable from absence -- the merge comes out complete-looking and short and says nothing about it. **Shared vocabulary is NOT early precision**: a two-phase `guessed_class` -> `verified_class` split (with `FALSE_POSITIVE` available, counted then dropped) shares the vocabulary from the start but not the confidence -- fixing verdicts early would push judgment onto the agent with the least context. **Output-side twin of `worktree-isolation-for-parallel-agents`** (input-side working-tree contention): same enabling role, opposite ends of the pipeline, unrelated remedies -- cross-linked, NOT merged into a "parallel agents need coordination" abstraction that would carry no executable rule. **`confidence: medium`, pinned to the weakest load-bearing claim, not an average**: the artifact's design is directly evidenced in committed source at a named commit, but **the counterfactual (omit the taxonomy -> N private vocabularies -> unmergeable) is reasoned, never field-observed**, and n=1. **Path to high requires a real second instance -- an observed merge that failed this way, or a second independent design hoisting a taxonomy for the same stated reason; a second reading of this same artifact is one observation read twice** (same independence-of-observations discipline as `roster-drift-from-reference-capability-register`). Load-bearing strings are quoted verbatim in the entry rather than cited by line, per this wiki's own `stale-snapshot-trusted-as-current` rule. **Incidental cross-link**: the `C3`/`C4` classes are an operational **detector** for the failure mode `gotchas/citation-orphaning-by-housekeeping-sweep` + `gotchas/gap-citation-acquires-hidden-dependency-on-closure` describe from the victim's side, and a shipped instance of the internally-triggered detection `detection-is-upstream-of-recovery` (Finn, S62, also from apex) named as the frontier -- that entry predicted the shape, a sibling team built the instrument. **Naming: `merge contract` REJECTED on a canonical-taxonomy check** -- `contracts/` is a claimed slot in this wiki's own directory taxonomy (API shapes / type definitions) and the name would invite mis-filing forever. **A sibling candidate from the same commit was REJECTED on the record**: *workflow-as-committed-artifact* (n=1, and the load-bearing mechanism is harness-provided product surface -- a team using a documented feature as designed is a usage datapoint; stripped of specifics it reads *commit your reusable automation*). **stage-2 pending** -- librarian-authored on a candidate relayed second-hand by team-lead, so neither author-is-filer nor a joint read-back; advances on his or Finn's read-back, fail-closed per the gate. Source: aen (relayed candidate), schliemann/apex-research (source artifact), callimachus (primary-source analysis + filer)

### gotchas/

- [`contract-enforcement-gap-non-claude.md`](gotchas/contract-enforcement-gap-non-claude.md) -- No mechanism to define, test, or enforce protocol compliance for non-Claude participants; prerequisite gap for multi-provider expansion
- [`external-synthesis-overreach.md`](gotchas/external-synthesis-overreach.md) -- External reviewers promote conditionals to recommendations, treat hypotheticals as confirmed, and flatten specialist nuance; verify after any outsider synthesis
- [`cloudflare-d1-migration-query.md`](gotchas/cloudflare-d1-migration-query.md) -- 10 D1 gotchas from hr-devs: CASCADE ignores PRAGMA, safe `_new` rename pattern, BLOB traps, silent row drops, MCP write access ($5K incident)
- [`dual-team-dir-ambiguity.md`](gotchas/dual-team-dir-ambiguity.md) -- Bare `teams/<team>/` resolves to two distinct dirs (Repo team config dir at `$REPO`, durable; Runtime team dir at `$HOME`, ephemeral). Wrong-root writes vanish silently on container rebuild. Fix: leading Path Convention section anchors all bare paths to `$REPO`. Production: Eratosthenes first boot, 2026-04-13
- [`no-teamdelete-stale-session-dirs-accumulate.md`](gotchas/no-teamdelete-stale-session-dirs-accumulate.md) -- On CLI 2.1.178+ `TeamDelete` is gone, so `~/.claude/teams/session-<id>/` dirs accumulate on every exit. NON-FATAL (the discovery resolver's PROCESS-liveness filter skips dead dirs); remediation = an out-of-band pid-guarded sweep that MUST exclude the live session's own dir. **`confidence: high`** -- OQ2 RESOLVED by WS3b V3 (worst case: sessions-json not GC'd, status:idle lingers -> liveness must be process-not-status). WS2 lifecycle rework, issue #86. Source: Herald, Hopper, Brunel
- [`sessions-pid-json-not-gc-status-idle-lingers.md`](gotchas/sessions-pid-json-not-gc-status-idle-lingers.md) -- **Substrate-property, version-stamped 2.1.181, probe-verified (Hopper V3).** `~/.claude/sessions/<pid>.json` is NOT GC'd on exit (neither `/exit` nor `kill -9`); the dead entry lingers `status:"idle"` -- identical to a live idle session, so `status` CANNOT distinguish dead from live. Liveness MUST be process-liveness (`os.kill(pid,0)`/`/proc/<pid>` on the `pid` field), PID-reuse-guarded via `procStart`, NOT the status string. Fixes the WS1 resolver's status-allowlist before the unpin; fills the substrate sheet's one open GC-on-exit gap (as a 2.1.181 datapoint). Source: Hopper (probe), Brunel (resolver fix), Herald (independent V3 route)
- [`cold-start-discovery-false-negative-config-before-sessions-json.md`](gotchas/cold-start-discovery-false-negative-config-before-sessions-json.md) -- **Version-stamped 2.1.181, S58.** On session start `config.json` is written EAGERLY but `sessions/<pid>.json` lands only ~10-25s later at interactive-ready (V4 window). A discovery/liveness/existence probe run inside that window sees partial state (eager dir, no pid entry) and can FALSELY conclude lazy-create/non-existence -> the **root cause of the S57 false halt** (overturned by S58's live PASS). Rule: within ~25s of cold start, "nothing here" MUST await/retry before reporting absence -- never conclude lazy-create from a single in-window read. Sibling of `sessions-pid-json-not-gc` (same artifact, opposite end of life: birth/absent/false-negative vs death/lingers/false-positive). Source: Aen (live-diagnosed), Hopper (V4 probe), Herald (generalization + Stage-2)
- [`orphan-courier-holds-lock-across-sessions-wrapper-cannot-reclaim.md`](gotchas/orphan-courier-holds-lock-across-sessions-wrapper-cannot-reclaim.md) -- **Version-stamped 2.1.181, S58 (Bug B).** A LIVE prior-session courier (Scheduled-Task-started, explicit config) can stay alive across a session boundary holding `courier.lock`; the new wrapper tracks only its own pidfile, so its stop step can't reclaim a lock it didn't create -> new couriers die `FileExistsError`. Fix (BOTH LANDED): identity-based stop sweep (CIM cmdline match, clear lock only if recorded pid dead) + Scheduled Task DISABLED (Hopper, 16:06; orphan-spawn class closed). The lock behaved correctly (it SHOULD refuse a live holder); the defect was the orphan being alive. **Live-contention sibling** of `courier-scheduled-task-restart-vs-stale-pidfile` (dead-pid stale PRESENCE) -- kept separate, not folded. Source: Aen (live-diagnosed), Brunel (fix + curation)
- [`explicit-courier-config-hardcoded-path-stale-on-2.1.181.md`](gotchas/explicit-courier-config-hardcoded-path-stale-on-2.1.181.md) -- **Version-stamped 2.1.181, S58 (Bug C).** The explicit `fr-courier.config.json` hardcodes `inboxes_dir = ...teams\framework-research\inboxes`, which doesn't exist on 2.1.181 (team dir is `session-<id>`) -> the "always-safe explicit-fallback courier" delivers nothing + the drain errors rc=1, breaking Direction #4's "explicit fallback stays up safe between sessions." Fix (LANDED): drain against the launched config (option-1); Direction #4 amendment RATIFIED + CLI-version-split (2.1.177 explicit = rollback baseline; 2.1.178+ per-session, between-session persistence retired). Rejected: a session-aware explicit config (would duplicate the auto-resolver). Same root cause as `courier-must-runtime-discover-team-name`, different site. Source: Aen (live-diagnosed), Brunel (fix)
- [`courier-restart-needs-inboxes-dir-step25-before-step3.md`](gotchas/courier-restart-needs-inboxes-dir-step25-before-step3.md) -- **Version-stamped 2.1.181, S58 (Bug A).** On a cold session the courier restart binds `inboxes_dir` to `session-<id>/inboxes`, but `inboxes/` is created lazily and the step that mkdir's it (Step 3, restore-inboxes.sh) ran AFTER the courier step (2.5) -- the dependency ran backwards. Fix (BOTH LANDED): reorder -- courier step moved to **Step 3.5** (after Step 3) + courier self-`mkdir`s its inboxes_dir in validate_startup. NOT the stale-pidfile gotcha (that's stale-guard; this is missing-precondition-by-order). Slug names the now-fixed defect, kept for link stability. Source: Aen (live-diagnosed), Volta (fix + Stage-2)
- [`lockfile-pid-staleness-false-refuse-across-container-recreate.md`](gotchas/lockfile-pid-staleness-false-refuse-across-container-recreate.md) -- A single-instance lockfile on a PERSISTENT volume + pid-only staleness check = FALSE-REFUSE across a container recreate: the PID namespace resets per container, so an old recorded pid aliases a live unrelated process -> `os.kill(pid,0)`=true -> lock judged not-stale -> daemon refuses to start. Fix needs a container-instance discriminator: **`boot_id` is WRONG** (host-kernel-scoped, same across containers); use **PID-1 starttime** (`/proc/1/stat` field 22, changes per recreate). **Field-22 sibling** of `sessions-pid-json-not-gc` (same "OS start-time beats a recorded flag/id" lesson, for lock-liveness vs session-liveness). Live home: `stationmaster-courier.py` InstanceLock. Source: Brunel (diagnosed S52), Herald (owns the fix)
- [`embedded-github-token-in-git-config.md`](gotchas/embedded-github-token-in-git-config.md) -- Fleet-standard `clone_or_pull()` persists GITHUB_TOKEN to `.git/config` via `sed`-injected auth URL. Fix: transient `http.extraheader` via `-c`, never persisted. Affects all teams descending from `evr-ai-base`
- [`tmux-pane-labels-decoupled-from-personas.md`](gotchas/tmux-pane-labels-decoupled-from-personas.md) -- uikit-dev tmux pane titles show role IDs (`@component-dev-1`) not persona names (Eames). Presentation layer reads layout config, not roster. Cognitive cost, functionally correct routing. Source: Aalto. Scope pending Brunel investigation
- [`persist-project-state-leaks-per-user-memory.md`](gotchas/persist-project-state-leaks-per-user-memory.md) -- `persist-project-state.sh` mirror semantics correct for container-scoped team repos, wrong for multi-workstation shared team repos; session 8 leak of 36 files / ~175KB of per-user auto-memory into `mitselek/ai-teams`. Volta owns fix. Sibling to `dual-team-dir-ambiguity`
- [`jq-file-vs-arg-escape-divergence.md`](gotchas/jq-file-vs-arg-escape-divergence.md) -- `\s` in bash single-quoted jq arg works; same `\s` in a `.jq` file is invalid escape. Extraction to `.jq` requires `\\s`. jq file parser vs command-line arg parser use different string-parsing paths. Source: Volta, F1
- [`warp-dns-vs-routing-asymmetry-rc-host.md`](gotchas/warp-dns-vs-routing-asymmetry-rc-host.md) -- RC-host WARP resolvers silently fail on `*.evr.ee` hostnames while IP routing works fine; use IPs directly in RC-origin configs. Cross-team. TTL: 2026-10-24. Source: Brunel
- [`cross-msys-argv-mangling.md`](gotchas/cross-msys-argv-mangling.md) -- Two MSYS-flavored Windows binaries on opposite sides of an exec mangle argv at the boundary; child prints usage banner and exits 255. Fix: pin child to Windows native OpenSSH. Lurks beneath the windows-user-context-persistent-bridge pattern. Source: team-lead
- [`ai-teams-user-no-sudo-use-docker-exec-root.md`](gotchas/ai-teams-user-no-sudo-use-docker-exec-root.md) -- Team containers intentionally deny `ai-teams` passwordless sudo; in-container `sudo` over SSH hangs unrecoverably. Workaround: SSH to host as `dev` with `id_ed25519`, then `docker exec -u root <container>` for elevated ops. Prerequisite for the live-inject + Dockerfile-bake dual-track pattern. Source: Brunel
- [`create-perm-as-404-disguise.md`](gotchas/create-perm-as-404-disguise.md) -- `createConfluencePage` against a space without create-permission returns 404, not 403 -- Atlassian disguises permission denial as not-found (security best-practice, prevents leaking space existence to unauthorized callers). Diagnostic chain typically wastes time on space-ID/path checks before reaching the permission cause. Workaround: publish in a permitted space + use two-stage adoption to escalate. Architectural fact (revision trigger = Atlassian API contract change, not n+1 sightings). TTL: 2027-05-04. Source: team-lead
- [`teamcreate-in-memory-leadership-survives-clear.md`](gotchas/teamcreate-in-memory-leadership-survives-clear.md) -- Claude Code parent CLI holds team-leadership state in-memory, separate from disk. `rm -rf $TEAM_DIR` cleans disk but does NOT release in-memory leadership; next `TeamCreate` returns "Already leading team." Recovery requires explicit `TeamDelete()`. Mitigation (commit `426194d`): every-startup `TeamDelete + TeamCreate + verify-on-disk` (best-effort delete handles both fresh-start and stale-state); graceful-shutdown S5 step `TeamDelete()` after final push. **n=2 cross-team confirmation** (Schliemann/apex #62 + Volta/FR session 21 -- promotion-grade per Volta's criterion); third empirical reproduction during esl-suvekool 22→23 boundary. Sibling to `dual-team-dir-ambiguity` (different layer, same root-cause shape -- both substrate-invariant-mismatch instances). Sources: Volta + Schliemann (cross-team)
- [`cross-document-prose-procedure-drift.md`](gotchas/cross-document-prose-procedure-drift.md) -- Cross-document, within-team gate-1 family member. When a structural change to a procedure is applied to one document but not the prose-explainer documents that describe it, the prose goes stale silently -- no tool reports the inconsistency. First instance: FR S5 collapse (`startup.md` 2026-04-30, commit `426194d`) left `topics/06-lifecycle.md` asserting "TeamDelete is pointless" for 7 days until the topic-06 rewrite (2026-05-06) repaired it. Discipline extension: `grep -r` across the repo before any structural procedure change, not just within-document grep. Architectural-fact entry -- n+1 sightings will not strengthen; revision triggers are tooling addition (cross-document drift detection in CI) or procedure consolidation. Missing-middle in the gate-1 family between `within-document-rename-grep-discipline` (one file) and `pass1-pass2-rename-separation` (cross-team). Source: Volta
- [`inbox-drained-on-spawn-clear-without-deliver.md`](gotchas/inbox-drained-on-spawn-clear-without-deliver.md) -- **`[TTL-EXPIRED]` 2026-08-07, flagged 2026-08-12. Version-coupled harness substrate last verified 2026-05-12 on a CLI several releases old; this wiki records one UNANNOUNCED inbox-semantics flip between adjacent versions, so treat as a superseded-CLI observation until a live pre-spawn-inbox spawn experiment re-runs it (owner: team-lead to route -- a librarian cannot). The spawn-prompt relay-fold workaround is safe either way.** Distinct sub-shape from `worktree-spawn-asymmetry-message-delivery.md`: when an agent is spawned with messages already on disk in their inbox file, the spawn handshake drains the file to `[]` **without delivering the queued messages into the spawned agent's conversation channel**. Drain ≠ deliver. First instance 2026-05-07 FR S29: Cal spawned with `inboxes/callimachus.json` at 21400 bytes (Monte's two Protocol A submissions verified on disk pre-spawn); file dropped to 2 bytes at file-mtime = spawn-window; conversation channel received no submissions. Both sender (Monte) + recipient (Cal) parent-process, no isolation flag -- distinct mechanism from worktree-OUTBOUND mount-staleness. Workaround: team-lead **spawn-prompt relay-fold** (Stage 1 discipline per `relay-to-primary-artifact-fidelity-discipline.md`). Detectable only by parent-process observer via file-stat byte-count correlation at spawn-mtime. Architectural-fact entry -- TTL 2026-08-07 for harness-fix re-verify; n+1 sightings of same mechanism do not strengthen. **2026-05-12: cross-linked to `inbox-file-write-as-wake-mechanism.md`** as canonical substrate property; this entry is the spawn-handshake drain-vs-deliver decoupling exception. Sources: team-lead, Callimachus
- [`edit-tool-read-state-expires-on-intervening-tool-call.md`](gotchas/edit-tool-read-state-expires-on-intervening-tool-call.md) -- **Joint Callimachus + Finn. Third harness-substrate gotcha** in the wiki (alongside `inbox-drained-on-spawn-clear-without-deliver.md` + `subagent-harness-blocks-curator-role-repo-write.md`). Claude Code Edit tool's substrate-side check is **not "file has ever been read"** but **"file's Read-state slot is still valid at Edit-call time"** -- any intervening tool call expires the Read-state deterministically; subsequent Edit calls fail with `"File has not been read yet. Read it first before writing to it."` Failure mode is silent expiration (no warning, no degradation signal -- only the next failed Edit announces the slot is gone); the error message points to the wrong remediation ("you forgot to Read" vs "Read earlier and state expired"). **Substrate-mechanism (Finn S36 correction)**: deterministic, not statistical -- any intervening tool call (Bash, Grep, Read of other file, SendMessage) invalidates the prior Read-state slot; not just file-mutating calls (broader-mechanism correction superseded Cal's earlier Write-only-invalidator framing). **n=11+ Cal cumulative across S33-S37 + n=3+ Finn across S35-S36** with fresh in-vivo S37 instance (~80 tool calls between Read of `memory/callimachus.md` and Edit; failure-then-recovery cycle observed deterministically). **Recovery primitive**: re-Read before Edit when previous Read is more than ~5 tool calls ago OR more than ~5 minutes real-time OR conversation has crossed SendMessage round-trip. Cheap precaution; loud failure if skipped. **Composes with** `substrate-invariant-mismatch.md` (Edit-tool authored expectation = "file has been read"; actual substrate = "Read-state slot is per-file-and-per-call-invalidated"); `documentation-vs-substrate-truth-divergence.md` at meta-level (Edit-tool authoring captures inferred-but-substrate-wrong "Read-state is durable" property -- harness-tool authoring layer complement to dispatch-author authoring layer). **Failure-mode siblings at harness-substrate layer** (three distinct sub-layers): spawn handshake / subagent Write pattern-match / Edit-tool Read-state slot. **Architectural-fact** (harness-implementation behavior; n+1 sightings do NOT raise confidence further); revision trigger = harness behavior change (Anthropic update). Confidence high. TTL 2026-11-27 for re-verification. Sources: Callimachus, Finn
- [`inverted-trigger-primitives-antipattern-on-poll-based-substrates.md`](gotchas/inverted-trigger-primitives-antipattern-on-poll-based-substrates.md) -- **Joint Finn + Callimachus.** When designing event-flow protocols, substrate's connectivity model is a load-bearing invariant: trigger-direction must match substrate's connectivity-direction. **Inverted-trigger primitives** (substrate-fires-event-toward-worker framing) are an antipattern on **poll-based substrates** (substrate refuses inbound connectivity to worker; worker initiates connection to substrate's work queue). Mismodel-via-announcement-grade-inference is the trap shape -- announcement blogs commonly use "agent receives events" vocabulary consistent with either connectivity model; only canonical-source API documentation distinguishes. **n=4 cross-substrate generalization**: (1) Anthropic Managed Agents (poll-based, canonical instance per Finn S36 `claude-api` skill load -- Herald comms.md v1.3 §1.3 caught as antipattern instance); (2) Postgres LISTEN/NOTIFY (event-based, **counter-instance** -- substrate supports inverted-trigger via persistent connection); (3) Cloudflare Durable Object `alarm()` (poll-pattern-with-substrate-fired-callback, substrate owns worker lifecycle -- antipattern doesn't apply); (4) Filesystem-watch / inotify (no network boundary -- antipattern doesn't apply). **Substrate-class-invariant rule**: when substrate's connectivity model is network-outbound-only, inverted-trigger primitives are antipattern; substrate-correct primitive is poll-based (worker long-polls substrate work queue). **Discriminator question**: does substrate dial into worker's network across network boundary? **Compositions**: `layer-0-library-first-pre-draft-discipline.md` prevention discipline + `documentation-vs-substrate-truth-divergence.md` Candidate A authoring-tier failure family + `discriminator-anchored-on-sub-canonical-source.md` family-adjacency at substrate-mechanism-naming layer + `substrate-invariant-mismatch.md` defect-class at connectivity-direction layer + `recursive-narrowing` Herald v1.3→v1.4 W4 catch as row-5 of within-author catalog. **Architectural-fact** (canonical-source quote names deliberate connectivity-model design choice; n+1 sightings do NOT strengthen). Confidence medium-high (n=1 antipattern instance + n=4 substrate-class generalization). TTL 2026-11-27 for canonical-source re-verification. Cross-team confirmation expected from any team designing against poll-based substrate. Sources: Finn (canonical instance + skill-load substrate-truth surface), Callimachus
- [`inbox-retention-flip-pending-only-queue.md`](gotchas/inbox-retention-flip-pending-only-queue.md) -- **Version-coupled harness-substrate flip (CLI 2.1.170).** A live inbox file is a pending-only queue: messages written `read: false` then REMOVED on delivery (drained to `[]` within <0.7s), not retained with `read: true`. This is a flip from the S30–S47 accumulating-log model, shipped UNANNOUNCED (no CHANGELOG/docs/release-note through 2.1.170; old-behavior version unknown, flip bracketed between S47 substrate and 2.1.170). Replicated on a second host (apex-research). **Casualties (TRUTHS.md I-1):** persist/restore/sanitize scripts, ghost-bridge v1/v2 read-flag dedup, any retention-asserting wiki entry -- all now capture only undelivered residue; ghost outboxes survive by accident (no consumer drains a session-less name, T3.a). **Unlike the version-stable wake/registration/lifecycle substrate-property family, this entry IS version-coupled** -- revision trigger = CLI version change (local CLI now 2.1.175; re-run probe-1b before trusting on any version ≠ 2.1.170; n+1 on 2.1.170 doesn't strengthen). Anchors the version-stamped property sheet (references). Source: team-lead (TRUTHS.md), Callimachus (filer)
- [`per-filesystem-gate-targets-tmp-measures-wrong-fs.md`](gotchas/per-filesystem-gate-targets-tmp-measures-wrong-fs.md) -- **Substrate-gate trap (Hopper, S50 Task #3).** A gate testing a per-filesystem property (atomic exclusive-create/rename, fsync durability, hardlink, case-sensitivity) measures whatever filesystem its test path lands on -- not necessarily the deployment's. If it uses a default temp dir and `/tmp` is **tmpfs** (in-memory, the common default on modern Linux), it measures tmpfs, PASSES, and says nothing about the disk-backed fs the deployment uses → **false-confident PASS.** Instance: T6.a exclusive-create race harness on prod-llm -- `tempfile.mkdtemp()` → `/tmp` = tmpfs (uninformative pass); deployment data lands on `/var/lib/docker/volumes/` = ext4-on-LVM, where the authoritative re-run also passed 50/50 (Python `open('x')` + bash `set -C`). **Rule:** resolve where deployment data lands FIRST (`df -T <path>`; `df -T /var/lib/docker` for a Docker named volume), run on THAT device; "ran on the host, passed" is insufficient if host `/tmp` is tmpfs. Harness mitigation: `t6a-race-harness.py` prints `df -T target` in its fingerprint header every run. **Observation-based** (`/tmp`-is-tmpfs is a per-host config default, varies -- re-check with `df -T`, never assume). **Closes the T6.a Linux re-verification owed before deploy (D10)** -- updates the property sheet's T6.a row. Source: Hopper (submitted), Callimachus (filer)
- [`ssh-keygen-A-ignores-f-path-prefix.md`](gotchas/ssh-keygen-A-ignores-f-path-prefix.md) -- **OpenSSH CLI gotcha (Hopper, S50 Task #7; verified OpenSSH 9.2p1 / Debian bookworm-slim).** `ssh-keygen -A -f <dir>` does NOT write host keys under `<dir>` -- `-A` (generate all default key types) writes to the compiled-in default `/etc/ssh/`; `-f` (the single-key keyfile flag) is silently ignored in `-A` mode, it is NOT a relocation prefix. Failure chain: a container entrypoint running `ssh-keygen -A -f "$STATE_DIR"` to persist host keys on a mounted volume gets ZERO keys there → sshd pointed at the empty path exits "no hostkeys available" → container crash-loops under `restart: unless-stopped`. **Invisible at build time** (`docker compose build` succeeds; entrypoint runs only at `up`) -- runtime-only-defect class. Fix: generate each type directly in single-key mode where `-f` IS honored (`ssh-keygen -t ed25519 -f "$HK_DIR/ssh_host_ed25519_key" -N ""`), guard with `[ ! -f ... ]` for idempotent persist; skip `-A` entirely if sshd_config references one type. Diagnostic signature: logs "Unable to load host key"→"no hostkeys available -- exiting"; `docker inspect` ExitCode=1 + climbing RestartCount; volume key-dir enumerates EMPTY. **Architectural-fact** (deliberate OpenSSH CLI design; revision trigger = OpenSSH contract change, NOT n+1). This is the substrate root-cause of the standby-discipline Clause-A incident (`f022fed`); sibling runtime-vs-build divergence to the tmpfs-gate gotcha. Source: Hopper (submitted), Callimachus (filer)
- [`nologin-shell-defeats-forced-command.md`](gotchas/nologin-shell-defeats-forced-command.md) -- **OpenSSH/sshd gotcha (Hopper, S50 Task #7; verified OpenSSH 9.2p1 / Debian bookworm-slim).** A `/usr/sbin/nologin` login shell SILENTLY defeats an `authorized_keys` forced command. sshd runs `command="..."`/`restrict,command="..."` by invoking the account's LOGIN SHELL as `<shell> -c "<cmd>"`; if the shell is `nologin` it refuses to exec -- prints "This account is currently not available.", exit 1 -- so the forced command (the whole interaction model for a forced-command-only account) NEVER runs. Footgun because `nologin` *looks* like correct service-account hardening but removes the function while leaving auth + transport intact. **Why it hides -- protocol layer not transport**: sshd starts, host key fine, port listens, pubkey auth succeeds, port-liveness healthcheck green, and a unit test invoking the program DIRECTLY passes; only an over-real-ssh acceptance test reveals it. Fix: `useradd --shell /bin/sh` -- the security gate is `restrict`+`command=`+pubkey-only (sshd enforces independent of shell); the shell only needs to exec the forced command, `nologin` adds no security here. **Architectural-fact** (sshd-invokes-login-shell is deliberate; revision trigger = sshd contract change, NOT n+1). **Companion to the ssh-keygen gotcha** (same #7 deploy): both runtime-only/protocol-layer defects the build + direct-unit-test miss, caught only by over-real-ssh acceptance -- shared lesson: forced-command-over-ssh services need an over-real-transport acceptance test. Defect Dockerfile:22 → fixed `909bbe9`. Source: Hopper (submitted), Callimachus (filer)
- [`courier-originates-routing-protocol-leaves-undefined.md`](gotchas/courier-originates-routing-protocol-leaves-undefined.md) -- **Protocol-edge routing gap (Herald, S50).** In the stationmaster post-office model a consignment is `{"to": <team>, "entry": {verbatim harness inbox entry}}` and the hub routes by `to` -- but a harness inbox entry has NO destination field and a ghost outbox is a generic slot, so the courier MUST supply `to` from outside the entry, and protocol v1.0.0 never says where. The §4 "forward `entry` verbatim" guarantee actively prevents routing data living in the payload, yet the hub requires `to` -- the tension is structural, and the courier is the one place that originates routing the protocol doesn't define. **v1 convention (Aen-ratified, courier scope, no protocol change):** single configured outbox → its one destination; `<team>-bridge` → strip `-bridge` → `<team>`. **Undefined multi-outbox-fan-out case → refuse-and-retain** (log, hold in spool, never drop per hints §7 no-TTL; never guess) -- queued PROTOCOL-AMENDMENT CANDIDATE pending PO ratification (write-up in `memory/herald.md`, 3 candidates, none decided). **Generalizable lesson:** when a wire protocol forwards an opaque payload verbatim, routing metadata not IN the payload must come from the edge component; any uncovered case refuses-and-retains, never silently drops or guesses. stage-2 CONFIRMED (Herald read-back 2026-06-12; faithful, no corrections; the `from_team`-hub-sourced-vs-`to`-no-hub-source asymmetry adopted as the cleanest statement of why the gap is asymmetric). Source: Herald (submitted), Callimachus (filer)
- [`deposit-ok-without-data-line-means-nothing-landed.md`](gotchas/deposit-ok-without-data-line-means-nothing-landed.md) -- **Stationmaster deposit silent-failure (Herald, S51).** A successful `deposit` returns a per-consignment DATA LINE (`{"id":...,"to":...,"status":"accepted"}`) AFTER the `ok:true` envelope. **`ok:true` with NO data line = nothing landed** -- typically an empty/malformed request body (consignment never reached stdin). The envelope reports session/transport health, NOT payload acceptance; the data line is the only acceptance receipt. **Rule: no data line = no deposit, re-send; do NOT delete the spool entry.** Re-send is safe -- `deposit` dedups by id (per-command idempotency). Data-line-level corollary of the transport-failure rule (no envelope = retry); inverse of api-gateway-error-vs-actual-server-state (wrapper status ≠ real outcome). Live S51: a `/tmp` path mismatch sent an empty body, deposit returned ok-no-data-line, caught + re-sent. Co-discovered with the CR-7 `text`-field contract. stage-2 pending (Herald read-back). Source: Herald (submitted), Callimachus (filer)
- [`cf-access-apex-sso-header-trust-without-jwt-verify.md`](gotchas/cf-access-apex-sso-header-trust-without-jwt-verify.md) -- **Security architectural-fact, cross-team-forwarded from apex (idx 18).** The CF Access → APEX SSO bridge (`rumba_sso_login`, f600/rumba) trusts the plain `Cf-Access-Authenticated-User-Email` header via origin-lock; **JWT signature verification (`Cf-Access-Jwt-Assertion` against CF Access team certs + aud/exp/iat checks) is an explicit code TODO.** Until implemented, the email header is trusted without cryptographic verification -- origin-lock is a network-position control, not an identity control; the JWT assertion is the cryptographic proof CF actually authenticated the named user. First live legacy↔target auth bridge (CF Zero Trust + legacy APEX via iframe + origin-lock). **NOT FR's to fix** (apex repo `vjs_db_vjs_guard`); recorded as a framework-relevant auth-bridge pattern + gap. Architectural-fact: revision trigger = the JWT-verify TODO landing, NOT n+1 sightings. Evidence: PR vjs_db_vjs_guard#9 (issuecomment-4602468374). stage-2 confirmed (maintainer-authoritative -- apex's own TODO analysis). Sibling to create-perm-as-404-disguise. Source: Herald (forwarded), Schliemann/apex (analysis), Callimachus (filer)
- [`subagent-harness-blocks-curator-role-repo-write.md`](gotchas/subagent-harness-blocks-curator-role-repo-write.md) -- **Sub-shape E instance at the team-design vs subagent-harness-implementation layer pair.** Subagent with curator-role wiki-writer authority dispatched to Write a repo-root `docs/<name>.md` (canonical work product per team design); harness pattern-matches and **rejects with literal error**: `Subagents should return findings as text, not write report files.` Three-layer drift: L1 team design (curator authority); L2 dispatch operationalization; L3 subagent harness pattern-match enforcement contradicts L1. First instance 2026-05-26 FR S35: Cal dispatched to Write `docs/findings.md` (joint Brunel + Volta substrate-gap analysis); Write rejected; recovery via surface-back-with-substrate-truth-evidence (literal error text) + recovery-options enumeration (a/b/c/d: team-lead-Writes / alt-filename / team-scope sibling / disable-sandbox not-recommended); team-lead sanctioned option-a; Stage-2 supersession produced same outcome as direct subagent Write would have. Loud-rather-than-silent sibling to `inbox-drained-on-spawn-clear-without-deliver.md` (different harness layer, same shape: design-authorization and runtime-implementation contradict). Pre-emptive workaround: team-lead flags the harness-restriction in dispatch prompt rather than attempt-then-surface. Pattern-match heuristic empirically uncertain at filing (path-prefix `docs/`? `findings` substring? `.md` extension? combination?); future-work probe enumerated. Architectural-fact at the subagent-harness layer; n+1 sightings do not strengthen; revision trigger = Anthropic harness change OR new sub-shape discovery OR empirical pattern-match heuristic confirmation. TTL 2026-08-26. Source: Callimachus
- [`v2-ghost-bridge-restart-redelivery-dupe-motivates-hub-ledger.md`](gotchas/v2-ghost-bridge-restart-redelivery-dupe-motivates-hub-ledger.md) -- **The defect the stationmaster hub was built against (Herald + Schliemann, S51; grounded from 06-10 dupe exchange).** The v2 ghost-bridge daemon forwarded a remote outbox→local inbox by flipping each entry's `read` flag true but **NOT deleting** it from the source; the daemon crashed/restarted repeatedly (no supervisor -- SPOF), and each **restart re-scanned + re-forwarded** the outbox, delivering the same message N times. **"old-name-4x vs fresh-name-1x"**: same message arrived 4× via the old persistently-registered outbox (re-forwarded per restart) but exactly 1× via a fresh never-registered name (no backlog to re-forward) -- the controlled comparison that isolated the cause; N = restart count, not a fixed multiplier (bursts 5×/8×/4×/3×). Driver = outbox-name-persistence + read-flag-flip-without-delete + restart re-scan (NOT a hub or delivery-substrate problem). **Why it motivates the hub** (the "why" the stationmaster decision omits): DELETE-on-ack not flag-flip → nothing left to re-forward; courier delivered-ledger keyed by hub envelope `id` → dedups any at-least-once redelivery = at-least-once-with-dedup. **DISTINCT from `patterns/read-flag-replication-discipline-for-external-cli.md`** -- that's the consumer-side flip-under-flock discipline; this is the restart failure-mode the discipline does NOT cover (v2 *had* the flip, still re-delivered; the pattern itself disclaims restart-redelivery + protocol-dedup). Evidence: apex-fr-backlog-copy.json idx 22–32 (Schliemann↔FR count-reports + apex root-cause ack) + README DECOMMISSIONED-2026-06-15 banner. stage-2 pending (Herald read-back). Source: Herald (submitted), Schliemann/apex (joint count-reports), Callimachus (filer)
- [`ubuntu2404-stock-ubuntu-user-uid-1000-collision.md`](gotchas/ubuntu2404-stock-ubuntu-user-uid-1000-collision.md) -- **Container-build gotcha (Brunel + Hopper, teams-migration-probe 2026-06-17).** `ubuntu:24.04` (Noble) ships a stock `ubuntu` user pre-created at uid/gid 1000, so a Dockerfile creating the agent runtime user with `--uid 1000` collides (`UID 1000 is not unique`). Fix: `userdel -r ubuntu` then create at 1000, OR renumber the stock user, OR pick a different uid (e.g. 1001) and keep volume-mount ownership + host-side `docker exec -u <uid>` consistent. **Architectural-fact** about the base image -- revision trigger = base-image change, NOT n+1 sightings. Sibling to `ai-teams-user-no-sudo` (privilege layer; this is the user-creation/uid layer). stage-2 confirmed (architectural-fact). Source: Brunel + Hopper (submitted), Callimachus (filer)
- [`warp-host-sshd-2222-collision-with-apex-live.md`](gotchas/warp-host-sshd-2222-collision-with-apex-live.md) -- **Shared-host port-collision gotcha (Brunel, 2026-06-17).** On the WARP rc host (`100.96.54.170`) a second container publishing/binding sshd on `:2222` collides with the apex live team container (which owns `:2222` for `ai-teams@container` SSH). Fix for a throwaway/probe container: run `network_mode: host` + skip entrypoint sshd (no published ports), drive via `docker exec`-into-tmux instead of SSH-in; pair with the WARP CA mount for egress. General rule: on a shared host, a throwaway container claims no published ports and is driven via `docker exec`. Exactly how teams-migration-probe was driven -- apex `:2222` never touched. Sibling to `warp-dns-vs-routing-asymmetry-rc-host`. stage-2 confirmed. Source: Brunel (submitted), Callimachus (filer)
- [`courier-scheduled-task-restart-vs-stale-pidfile.md`](gotchas/courier-scheduled-task-restart-vs-stale-pidfile.md) -- **Courier restart-singleton gotcha (Brunel, 2026-06-17).** A Windows Task-Scheduler-relaunched courier collides with a stale pidfile left by an ungraceful exit: the relaunch either no-ops (thinks a courier is alive) or two instances race over the same inbox/outbox files. `MultipleInstances IgnoreNew` guards trigger double-fire while live, NOT stale-pidfile-after-ungraceful-exit. Fix: on startup validate the pidfile against a LIVE process (pid running AND cmdline matches the courier, guard recycled pids); if not live, treat as stale, remove, start fresh; no-op only on confirmed-live. Bare "exists -> alive" is the bug. Why it matters: the courier performs the inbox-file writes that wake recipients; a no-op restart silently stops cross-team delivery. Courier-specific instance of `windows-user-context-persistent-bridge` component #5 (stale-process cleanup). stage-2 pending (filed-on-behalf, single-incident). Source: Brunel (submitted), Callimachus (filer)
- [`verification-narrower-than-it-appears.md`](gotchas/verification-narrower-than-it-appears.md) -- **GENUS: a health check that verifies the NEIGHBOURS of a thing is routinely mistaken for verifying the thing itself (Hopper + Aen, S66/S67 queue; GH #104).** Silent and self-consistent: the check passes, the substrate is declared healthy, and the disconfirming evidence sits unread in an artifact you already had the path to. **Aen, verbatim: "Absence of error is not evidence of function when you are reading the wrong stream."** Canonical technical instance: **a TCP-connect probe on a reverse-SSH `-R` forward proves only that sshd holds the remote listener** -- sshd's `accept()` is local and completes BEFORE it opens the channel to the originating client, so `nc -z` / `/dev/tcp` reads OPEN even when the far side cannot reach the target at all. Two windows, permanent one first: **(a) PERMANENT** -- session healthy, originating host can't reach the forward target; probe reads OPEN forever and waiting never corrects it; **(b) STALENESS** -- originating host asleep, listener held until keepalives fire (`ServerAliveInterval` x `ServerAliveCountMax`, ~90s; never if unset), then self-corrects. Fix = L7 probe exercising the whole path. Three further live instances: wrong ARTIFACT (task/process/hub-ping green while the courier's own log held 83 transport failures, mail spooled undelivered); wrong STREAM (`fr-courier.log` stdout 0 bytes read as healthy while failures went to `.err`); and **signal DESIGN** -- hub `deposited_uncollected:{}` reads identically before a deposit and after a collection, a gauge that cannot distinguish two states. **n=3 across 2 agents in one session** kills the "one operator's discipline problem" reading. **CONFIDENCE HELD at `medium` deliberately: high on the genus, medium on the `-R` mechanism -- do NOT promote without running the `-R`-to-unreachable-target experiment named in the entry; genus n+1 does not settle it.** Revision trigger on the mechanism sub-claim only (deliberate OpenSSH design). Sibling of `control-narrower-than-its-name`, cross-linked NOT merged (observe side vs. act side). stage-2 pending -- filed on behalf, Hopper not spawned. Source: hopper (primary), aen (instance 3), callimachus (filer)
- [`capability-guard-conflates-tool-absent-with-check-failed.md`](gotchas/capability-guard-conflates-tool-absent-with-check-failed.md) -- **`if command -v X && X <args>; then OK; else WARN; fi` collapses two different facts into one branch (Hopper, S66/S67 queue; GH #104).** "X isn't installed" and "the thing X tests is broken" both land in `else`, and `else` almost always blames the thing under test. Written to make the check SAFE when the tool is missing, it instead makes it **dishonest** -- on a host lacking X the check can never report OK, and the operator is told the substrate is down. Silent, permanent, self-consistent. Fix: three distinguishable states (`SKIP: X unavailable, check not performed` / OK / WARN); better, remove the dependency (`timeout 3 bash -c "echo -n > /dev/tcp/host/port"` instead of `nc`). Generalises to any `capability-present && capability-says-yes` conjunction -- a two-term conjunction cannot report three states. **Live instance: `entrypoint-apex.sh:368` -- `nc` is absent from the apex container, so the gate printed `WARN: DB tunnel down` on every boot regardless of true state and was NEVER capable of reporting OK**, while the tunnels were in fact UP. apex was about to mirror the idiom for 11443 = a second permanently-dead probe. Degenerate extreme of `verification-narrower-than-it-appears` (not a check narrower than it appears -- one that never ran); kept separate, different remedy, and both defects sit on the SAME line so fixing only the guard leaves a probe that now runs and is still weak. stage-2 pending -- filed on behalf, Hopper not spawned. Source: hopper (primary), callimachus (filer)
- [`reverse-forward-carries-originating-host-warp-identity.md`](gotchas/reverse-forward-carries-originating-host-warp-identity.md) -- **ARCHITECTURAL-FACT: a reverse forward through a CF-Access-fronted host carries the ORIGINATING machine's WARP device identity, not the consumer's (Hopper, S66/S67 queue; GH #104).** The consuming container inherits an identity it does not hold and cannot see. apex's stated premise for #104 was FALSE but the ask survived: Oracle works because `vjsdbtest.evr.ee` -> 10.100.34.42 (RFC1918, CF not in path), whereas `gitlab.evr.ee` -> CF anycast (172.66.43.48 / 172.66.40.208 / 2606:4700:3108::ac42:28d0) -- no internal IP to forward to, and a TCP forward cannot route around CF Access. What passes Access is the workstation's `CF_Authorization` JWT (`iss: eestiraudtee.cloudflareaccess.com`, `device_id: 95fd90cf-...`, **`warp_as_auth: true`**). **SCOPED: `high` but explicitly limited to this workstation, this WARP enrolment, this CF Access policy** -- per Aen, *"a successful production path is not an experiment"*; generalising needs a deliberate 2nd-workstation/2nd-enrolment experiment. **SNI/Host precondition, not a follow-up**: TLS terminates at the CF edge, so `https://127.0.0.1:11443` sends Host `127.0.0.1` and CF will not route it -- requires `127.0.0.1 gitlab.evr.ee` in the container's `/etc/hosts`. **Measured correction SUPERSEDING an earlier FR draft**: GitLab echoes the received `Host`, so Host-derived redirects PRESERVE the port; only `external_url`-generated URLs are portless (the earlier "all redirects portless" claim was a pre-tunnel workstation probe). n+1 sightings do NOT raise confidence; revision trigger = substrate change (CF Access policy, WARP enrolment model, gitlab moving off anycast, or GitLab `external_url` config). TTL 2026-11-03. stage-2 pending -- filed on behalf, Hopper not spawned. Source: hopper (primary), callimachus (filer)
- [`control-narrower-than-its-name.md`](gotchas/control-narrower-than-its-name.md) -- **GENUS: an operation whose name implies a total teardown is frequently scoped narrower than the name says (Hopper, S66/S67 queue; GH #104).** It succeeds, reports success, and leaves the part you actually needed changed still running. **Instance (a) -- editing a script does not change a running loop**: bash re-reads a script between TOP-LEVEL commands, and a `while true; do ... done` is ONE top-level compound command parsed into process memory that never completes, so an on-disk edit cannot reach it and killing the supervised CHILDREN is not enough -- the interpreter process holding the loop must itself restart (observed: killed all four autossh/ssh pids, respawn returned fresh pids carrying the OLD argv; wrapper bash pid 18832 alive throughout). **Instance (b) -- `Stop-ScheduledTask` orphans the descendant tree, it does not reap it**: on a wscript -> bash-supervisor -> autossh -> ssh shape, Stop killed only the top-level `wscript.exe` and left **seven orphans**, including the `ssh.exe` still holding the remote binds; a straight Stop-then-Start would hit `ExitOnForwardFailure` against its own predecessor's binds and churn-loop. **RULE: any stop-then-start of a Task-Scheduler-supervised tree needs an explicit straggler sweep in between.** **TRAP: sweep by EXPLICIT PID, never by command-line filter** -- Hopper's own probe shells matched the filter and a filter-based kill would have killed the shell he was standing in (deterministic `Get-CimInstance` + `Stop-Process` form in the entry; the rejected Git Bash `pkill -f` is the same genus one layer down -- matches nothing, exits 0, operator believes it applied). Genus complement to the two courier gotchas (those are incidents with landed fixes; this is the reusable rule) and the *why* behind `windows-user-context-persistent-bridge` component #5. Sibling of `verification-narrower-than-it-appears`, cross-linked NOT merged. stage-2 pending -- filed on behalf, Hopper not spawned. Source: hopper (primary), callimachus (filer)
- [`gap-citation-acquires-hidden-dependency-on-closure.md`](gotchas/gap-citation-acquires-hidden-dependency-on-closure.md) -- **A ticket citing an OPEN architectural gap acquires a hidden dependency on whatever decision later closes it (Finn, S67 queue; VEO-78).** Citing a gap is citing a **status**, not a fact -- statuses change, the citation does not. At authoring time it reads "here is the unaddressed problem this solves"; at pickup time the gap may be closed and the closing decision now governs the work, with nothing in the ticket signalling it. **The asymmetry: closing a gap is a visible event in the DECISION record and an invisible one in every artifact that cited the gap -- the citation is a one-way link and closure does not walk back up it.** The ticket did nothing wrong; the defect is structural. Live instance: VEO-78 cited `C2` (open resilience gap from a multi-lens review); **ADR-013 closed C2 eight days after the ticket was written and raised the DoD bar** -- anyone implementing from the ticket's own text builds to the pre-ADR bar and is measured against the post-ADR one. Remedy is READER-side at pickup: look up the gap's current status, read the closing decision, raise any bar/scope/premise change as a scope question before implementing. **Deliberately NOT merged with `citation-orphaning-by-housekeeping-sweep`** (submitter's call, Librarian-endorsed): status-changes vs. referent-destroyed, reader-re-resolves vs. convention+sweep-hygiene, different owner. n=1. **stage-2 CONFIRMED (author-is-filer).** Source: finn (primary), callimachus (filer)
- [`citation-orphaning-by-housekeeping-sweep.md`](gotchas/citation-orphaning-by-housekeeping-sweep.md) -- **A housekeeping sweep that deletes working artifacts and repairs its IN-REPO references leaves every OUT-OF-REPO citation dangling (Finn, S67 queue; VEO-78).** Out-of-repo citations have no visible symptom inside the repo to repair -- **in-repo gets a decision, out-of-repo gets neither care nor notice.** In-repo refs are "fixed" by demoting link -> plain text, which silences the link-checker without resolving the reference. **Compounded by document-local IDs cited across document boundaries**: `C2` names THREE unrelated things in one repo (the resilience gap in the 2026-05-31 review, a traceability regression in the 2026-06-10 review, a CI check ID in `check-principles.py`) -- **a plausible-but-wrong substitution is worse than a 404**, since a dead link announces itself and a live link to the wrong thing does not. Evidence (independently re-verified at HEAD before filing, `Eesti-Raudtee/Arhitecture`): commit **`827f542`** 2026-06-10, Valeri Kuzmin, *"T-62: clear drafts/ and reviews/ folders for a fresh start"*; **11 files deleted**; **9 ADRs (011-017, 019, 021) + 10 task files (T-44..T-50, T-58, T-59, T-62) still dangling**; de-link visible at `principles/adr/adr-013-resilient-integration.md:6`. Downstream casualty Jira VEO-78 **INHERITED** its broken reference from ADR-013 rather than authoring it -- **citation rot propagates by copying, and the copier looks like the culprit**. **TONE DISCIPLINE, written into the entry as a required section: this was a documented tradeoff, not a cover-up** (the commit body states artifacts are recoverable from git history and the de-linking deliberate) -- frame as a blind spot at the repo boundary, and **verify the commit MESSAGE before ascribing intent from the DIFF**. Fix = cite durable ID-stable artifacts (ADRs) never working artifacts; never cite a doc-local ID across a doc boundary; **cite ADRs by NUMBER not path** (VEO-78 also cites `principles/adr-003-...` after ADRs moved to `principles/adr/`); sweeps should survey out-of-repo citers or record that they didn't. Deliberately NOT merged with the cite-the-gap gotcha. **stage-2 CONFIRMED (author-is-filer).** Source: finn (primary), callimachus (filer)

### decisions/

- [`cost-tracking-out-of-scope.md`](decisions/cost-tracking-out-of-scope.md) -- Token/cost tracking is an organizational concern, not a team concern; no quantitative data exists by design
- [`audit-independence-architecture.md`](decisions/audit-independence-architecture.md) -- Audit independence requires a separate read-only container, not a Medici provider swap; Medici's tool deps make in-place swap infeasible
- [`stationmaster-post-office-model.md`](decisions/stationmaster-post-office-model.md) -- **S49 inter-team-comms architecture decision (pointer to `poc/ghost-bridge/stationmaster-protocol.md` v1.0.0 RATIFIED).** Mail moves through a central hub on a post-office model: customers dial OUT (outbound ssh only); hub holds NO credentials, only registered pubkeys; never initiates connections. Five named sub-decisions: (1) **post-office pivot** (pubkey+team in authorized_keys w/ forced command; two-phase deposit/collect+ack, at-least-once); (2) **channel-is-identity** (channel auth IS the signature -- a grant on an authenticated session is the team's signed word; `from_team` stamped from channel not content); (3) **consent = unilateral receive-grants** (one grant = one direction, route table compiled from grants); (4) **transport-failure rule** (response envelope authoritative; no envelope = retry whole conversation; safe via per-command idempotency); (5) **inject-before-ledger** (crash between = one duplicate, accepted cost; reverse = silent loss, worse). Records rejected alternatives the contract states but doesn't argue (hub-pull, true-mirror, co-sign ceremony, mail-over-MCP, relaying). Supersedes ghost-bridge v2 daemon. stage-2 CONFIRMED (Aen read-back 2026-06-12; one correction folded -- onboarding doc ACCEPTED not DRAFT, commit `87ef7d4`; reference courier landed S50). Source: team-lead (protocol author), Callimachus (filer)
- [`fan-out-routing-per-destination-outboxes-cr4.md`](decisions/fan-out-routing-per-destination-outboxes-cr4.md) -- **Fan-out routing CR-4, PO-ratified 2026-06-15 (Herald, S51).** Resolves the deferred amendment-candidate in `courier-originates-routing-protocol-leaves-undefined.md`. **Candidate A: per-destination outboxes** -- outbox `<team>-bridge` → `<team>` (strip `-bridge`), one outbox per destination team, NORMATIVE v1. Single-outbox multi-destination fan-out is OUT OF SCOPE (courier refuses-and-retains). Rejected: (B) routing sidecar / local entry-envelope (adds a local-format contract); (C) leave-undefined (status-quo-with-a-name). Why A: zero protocol change (courier-hints convention only); external n=2 corroboration (FR reference courier + apex's live courier independently used `<team>-bridge`→`<team>`, apex T10); sender-borne cost where the routing knowledge is. B/C kept as fallbacks only if a real consumer forces single-outbox fan-out (no-future-proofing). stage-2 pending (Herald read-back). Source: Herald (submitted), Callimachus (filer)
- [`courier-must-runtime-discover-team-name.md`](decisions/courier-must-runtime-discover-team-name.md) -- **Probe-forced migration decision (Hopper + Brunel, teams-migration-probe 2026-06-17).** On CLI **2.1.178+** the on-disk team dir name is `session-<id>` (random per session; the Agent-tool `team_name` is IGNORED on disk -- cosmetic chat label only). The courier's hardcoded `~/.claude/teams/framework-research/inboxes/` path CANNOT survive. **Decision:** the courier MUST runtime-discover the team name -- glob `~/.claude/teams/*/config.json` `.name`, OR derive from `sessions/<pid>.json` `sessionId` (first 8 hex = the `session-<id>` slug) -- never hardcode a literal team name. This is the SINGLE gating migration cost: members[] injection (P4) + inbox-file-write delivery (P5/P6) both SURVIVE 2.1.178+; fix the path and the comms layer migrates cleanly, no mechanism redesign. **Prerequisite for unpinning the local CLI off 2.1.177.** Rejected: keep hardcoding / trust `team_name` (resolves to a nonexistent dir; chat-claimed names are not authoritative). Operational scope (not common-prompt-grade as written). Revision trigger = a future CLI restoring controllable team names. stage-2 PARTIAL (Brunel read back 2026-06-18; Hopper read-back still owed -- + a pending glob-PRIMARY/pid-TIEBREAKER + liveness-filter refinement queued in a blockquote). Shares the `resolve_team_dir` resolver with the two WS2 lifecycle decisions below. Source: Hopper + Brunel (probe), Callimachus (filer)
- [`lifecycle-release-evaporates-under-implicit-teams.md`](decisions/lifecycle-release-evaporates-under-implicit-teams.md) -- **WS2 lifecycle rework, issue #86 (Herald, design-accepted by Aen 2026-06-18).** On CLI 2.1.178+ shutdown **Step S5 "Release team leadership" (`TeamDelete`) is DELETED, not replaced** -- leadership is the implicit property of a live session, released by PROCESS EXIT; no `TeamDelete` to call, nothing to release. Shutdown 5 phases → 4; the startup-P2/shutdown-P5 symmetry argument evaporates too. The dir-removal residue of S5's old job -> stale dirs (see `no-teamdelete-stale-session-dirs-accumulate`). **Supersedes the S5 mitigation in `teamcreate-in-memory-leadership-survives-clear` for 2.1.178+** (version-coupled; old entry retained, not archived). Rejected: new release primitive / `rm -rf` own dir (nothing to release; deleting the live dir is self-sabotage). stage-2 pending (Herald read-back). Source: Herald (submitted), Callimachus (filer)
- [`startup-create-collapses-to-discover.md`](decisions/startup-create-collapses-to-discover.md) -- **WS2 lifecycle rework, issue #86 (Herald, design-accepted by Aen 2026-06-18).** On CLI 2.1.178+ startup **Step 2 (`TeamDelete` + `TeamCreate` + verify) collapses to Step 2' Discover** -- the team auto-exists eagerly (P3), so the session discovers its `session-<id>` dir via the shared `resolve_team_dir` resolver (glob `.name` canonical; pid tiebreaker when the caller holds the pid; liveness filter; fail-fast never-guess) and verifies operational, rather than creating. The verify-config.json half survives, repurposed as the discovery+operational gate. No create-retry: genuine absence = STOP+report (substrate fault). ONE resolver, two callers -- shared with the courier (WS1, `courier-must-runtime-discover-team-name`); in-session lifecycle passes the pid, detached courier omits it. **Supersedes the startup `TeamDelete`+`TeamCreate` mitigation in `teamcreate-in-memory-leadership-survives-clear` for 2.1.178+** (version-coupled). Rejected: keep TeamCreate / hand-roll mkdir+config.json (tool gone; races the harness eager write). stage-2 pending (Herald read-back). Source: Herald (submitted), Callimachus (filer)
- [`text-field-pin-clarifying-errata-no-bump.md`](decisions/text-field-pin-clarifying-errata-no-bump.md) -- **§4 text-field pin, clarifying errata, NO version bump, PO-ratified 2026-06-15 (Herald, S51).** Pins the consignment renderable-body field to `text` at protocol §4 (contract level), binding the SENDER (couriers MUST NOT remap → preserves verbatim-forward). Contract-level ratification of the CR-7 finding (`contracts/consignment-body-in-text-field-not-content.md`). Rejected: onboarding-convention-only (the hazard is contract-level; §4 silence reproduces the bug for protocol-only readers); major version bump (nothing conformant breaks -- SendMessage-origin already complies). Chosen: §4 clarifying errata (same posture as the §5.5 errata precedent). No-bump rationale: documents an already-held invariant, doesn't change behavior; only a hand-crafting sender's knowledge changes (doc gap, not contract change). stage-2 pending (Herald read-back). Source: Herald (submitted), Callimachus (filer)

### contracts/

- [`courier-rotation-contract-has-teardown-half-reap-before-acquire.md`](contracts/courier-rotation-contract-has-teardown-half-reap-before-acquire.md) -- **S58 (Herald).** Courier rotation is a SYMMETRIC contract: `reap-prior (kill + drain + release lock) -> resolve live dir -> acquire`. The Step 2.5 "re-resolve on restart" rule is only the ACQUIRE half; a rotation that re-resolves but leaves the old courier running (holding its lock, maybe polling a dead `session-<id>`) is INCOMPLETE -> lock collision or two couriers race. The reap is the structural complement, NOT an optional `.ps1` detail. **Mechanism vs requirement:** the lock-staleness PID-1-starttime spec detects WHEN a lock is reapable; THIS contract requires the reap actually happen at every rotation -- both needed. Live instance = S58 Bug B (the silence the contract closes). Source: Herald (author-submitted)
- [`speculative-marker-for-cross-team-drafts.md`](contracts/speculative-marker-for-cross-team-drafts.md) -- Inline `[speculative]` marker in cross-team handoff drafts flags author inference vs verified claim. Greppable + scannable; supports reviewer-targeted action during Stage 1 of two-stage adoption. Survival count tracked at each stage transition (decreasing across stages = speculation resolving). Distinct from `confidence: speculative` frontmatter (per-entry, future readers) and prose hedges (no machine-actionable contract). First-instance counts: 16 in standard, 2 in intake, 2 in tracking issue. Source: team-lead. n=1 deliberate, structural argument independent of sample
- [`entu-competency-index-schema.md`](contracts/entu-competency-index-schema.md) -- Schema for the competency index backing Entu's product-native consultant agents (entu/api#42); the PO-chosen **spine** (prompts consume an auditable claim→evidence KB, not embed knowledge). Atomic unit = a **claim** (verifiable assertion) + `evidence[]` of `{type: docs\|openapi\|src\|probe, ref, excerpt?}` + `verification` (doc-cited/spec-derived/live-probe) + `confidence` (backed/partial/unverified) + `domain`. Re-points `WikiProvenance` rather than reinventing (evidence.ref↔sourceFiles, ttl/status direct reuse; architectural-fact-vs-observation split → doc/spec-cited vs probe-TTL'd). One-claim-per-file (diffable, PR-able, auditable-before-hiring); `search_claims`/`get_claim` query surface; `confidence`→deterministic runtime label + `[GAP]` loop (citation-backed row of topic-10's three-way taxonomy). §3 evidence-ref formats drafted-to-plausible, reconcile vs Finn grounding digest. Source: callimachus. TTL 2026-09-06
- [`registered-two-meanings-deposit-error-semantics.md`](contracts/registered-two-meanings-deposit-error-semantics.md) -- **`[S51-design-review]` Stationmaster error-semantics contract (Brunel, S50; Aen-graded).** "registered" means two things in the hub: REGISTERED-BY-OPERATOR (key in `authorized_keys` via `sm-register`) vs KNOWN-TO-REGISTRY (entry in `registry.json`, lazy-populated on first connection via `touch_last_seen`). The deposit path's `is_registered()` keys off the SECOND -- so a deposit to a registered-but-never-connected team returns `E_UNKNOWN_TEAM`, not `E_NOGRANT`. **3-state deposit-rejection ladder**: E_UNKNOWN_TEAM (never connected) → E_NOGRANT (known, ungranted) → accepted (known + granted). **Defensible v1, not a bug**: granting requires connecting (grant = authenticated session, channel-is-identity), so a never-connected team can't have granted anyone → deposits bounce regardless; E_UNKNOWN_TEAM is a *more-precise* rejection. **Real seam for v2**: if v2 wants deposits to a registered-but-dormant team to QUEUE, `registry.json` must populate at `sm-register` time not first-connect -- a latent behavior-change point that should be a deliberate v2 decision. Catalyzing incident: S50 smoke-test ran deposit-before-grant before the recipient connected, hub correctly returned E_UNKNOWN_TEAM, test failed (Hopper caught at #7 acceptance); fix = the 3-state ladder. stage-2 CONFIRMED (Brunel read-back 2026-06-12; faithful, no corrections; contracts-over-gotcha + confidence-HIGH calls endorsed). Source: Brunel (submitted), Callimachus (filer)
- [`consignment-body-in-text-field-not-content.md`](contracts/consignment-body-in-text-field-not-content.md) -- **CR-7 wire-shape contract (Herald, S51).** The stationmaster consignment `entry` body MUST be in the **`text`** field. The harness renders a message body from `text`; canonical entry shape is `{from, read, summary, text, timestamp, type}`. A body in `content` (or no `text`) is forwarded verbatim, accepted by the hub, but renders as **`undefined`** on the recipient (only `summary` shows as the preview chip). Silent at deposit, visible only on render -- the hub validates the consignment envelope, not the entry's renderability. **Fix at the SENDER**: §4 forwards `entry` verbatim, so the entry must arrive already shaped with `text`; couriers MUST NOT remap content→text (that would break verbatim-forward). Normal path is safe (SendMessage-origin writes `text`); the hazard is hand-crafted deposits. Pinned at §4 (clarifying errata -- see `decisions/text-field-pin-clarifying-errata-no-bump.md`), onboarding Step 5, courier-hints Outbound §3. Caught live S51, apex (Schliemann) root-caused (apex T11/T12). stage-2 pending (Herald read-back). Source: Herald (submitted), Callimachus (filer)

### observations/

- [`knowledge-coherence-as-provider-constraint.md`](observations/knowledge-coherence-as-provider-constraint.md) -- The binding multi-provider constraint is knowledge coherence (semantic compatibility of agent artifacts), not infrastructure lock-in
- [`compaction-stale-state-deployed-teams.md`](observations/compaction-stale-state-deployed-teams.md) -- Aalto (uikit-dev) reports 5 compaction incidents + ranked wishlist; 5-10% of teammate messages were stale re-announcements. First externally-sourced Protocol A submission
- [`librarian-growth-curves-by-team-type.md`](observations/librarian-growth-curves-by-team-type.md) -- Cross-Librarian comparison: framework-research 34 entries / apex-research 0 entries. Three non-exclusive hypotheses (team-type ceiling, Phase 2 accelerator, submitter floor). Speculative, revisit 2026-04-28

### references/

- [`rc-host-db-tunnel-architecture.md`](references/rc-host-db-tunnel-architecture.md) -- RC-host SSH keys (dev@:22 with `id_ed25519`, ai-teams@:2222 with `id_ed25519_apex`) + operator-triggered reverse SSH tunnel giving apex-research container `localhost:11521/11522` → VJSDBTEST/VJSDBTEST2. Script at `apex-migration-research/.claude/bin/open-db-tunnels.sh` (commit `c79b838`). TTL: 2026-10-24. Source: team-lead
- [`evr-sso-is-entraid-not-wso2.md`](references/evr-sso-is-entraid-not-wso2.md) -- Substrate fact: EVR's SSO/IdP is EntraID (Microsoft Azure AD), NOT WSO2. WSO2 at EVR is Micro Integrator for TAF/TAP message routing -- orthogonal to identity. Confusion-trap: WSO2 has a parallel Identity Server product, plausible-but-wrong inference. Authoritative cites: FSM Confluence `536248326`, INFOSEC Confluence `851607559`. Brunel verified session 22, correcting team-lead's hedge. TTL: 2027-05-04. Architectural-fact reference (revision trigger = actual identity-stack change, not n+1 sightings). Source: team-lead, Brunel
- [`inbox-file-write-as-wake-mechanism.md`](references/inbox-file-write-as-wake-mechanism.md) -- **Canonical substrate property (wake stage)**: the harness wakes a team member when their inbox file is written. Wake mechanism IS the file write. Two corollaries: member identity = `members[]` array membership (no live process required); external processes are first-class teammates (anything that reads/writes JSON inbox files). Empirical basis: RFC #66 Findings 1-3, sandbox team `sendmessage-experiment` on macOS Darwin 25.4.0. **2026-05-14: Linux substrate verified empirically** via parallel cross-substrate n=2 work with the new companion-pair sibling. **2026-05-19: third-leg sibling filed** (`inbox-slot-vs-members-validation-asymmetry.md`); companion-pair becomes three-way substrate-property family (wake + registration + lifecycle-asymmetry). Substrate this entry articulates is what failure-mode entries `worktree-spawn-asymmetry-message-delivery.md` and `inbox-drained-on-spawn-clear-without-deliver.md` describe violations of. Architectural-fact (revision trigger = harness substrate change, not n+1 sightings). TTL: 2026-11-12. Source: callimachus (filer), RFC #66
- [`inbox-substrate-properties-2.1.170.md`](references/inbox-substrate-properties-2.1.170.md) -- **Version-stamped empirical sheet -- curated pointer to `poc/ghost-bridge/TRUTHS.md` (23 atomic T-entries, Aen).** 14 inbox substrate properties verified against CLI `2.1.170` on Windows/Git-Bash/NTFS: drain (pending-only, ≲0.8s), wake-on-write (≲0.5s, wakes idle session, T4.a), arbitrary-`from`-passthrough (T4.b), enqueue-lag-VARIABLE 0.5–9s (T2.a), direct-write-faster-than-dispatch (T4.d), mtime-lies (T2.b), lazy-create (T2.c/T4.c), append-preserves-foreign (T5.b), consume-by-rename (T5.a), exclusive-create-atomic 50/50-race-clean (T6.a -- **Git-Bash/NTFS only, Linux re-run owed for deploy**), multi-entry-batch-in-order (T6.b), ghost-outbox-persists-≥10min (T3.a). OPEN: sub-second drain-rewrite clobber window untested (external writers must treat inbox as RMW-contended). The ledger is the evidentiary source; this sheet makes properties queryable. Local CLI now 2.1.175 -- NOT current-validity claims. TTL 2026-09-10. Source: team-lead (TRUTHS.md), Callimachus (filer)
- [`drain-on-delivery-datapoint-2.1.173.md`](references/drain-on-delivery-datapoint-2.1.173.md) -- **Version-tracking datapoint, CLI 2.1.173 (Herald + Schliemann, S51; apex customer #2).** Compact POINTER extending the 2.1.170 sheet's T1.b Drain row -- the courier's drain-on-delivery model HOLDS on 2.1.173: live member inbox → `[]` after delivery (inbound verify-empty→exclusive-create VALID); session-less ghost OUTBOX accumulates (outbound consume-by-rename VALID); ghost inbox with no live reader also no-drain. Discriminator: drain is gated on a LIVE consuming agent, not on the inbox file existing. **CAVEAT**: observational steady-state snapshot, NOT a timed inject/drain-latency test -- confirms the model, does not re-measure the ≲0.5s/≲0.8s figures; do not cite for timing. Architectural-fact-adjacent: revision trigger = CLI-version substrate change, not n+1 sightings (the Drain row already flipped unannounced between adjacent versions). Third sample; agrees with 2.1.170 baseline, tracked against 2.1.175 skew-flag (local now 2.1.177). Schliemann's apex T14, folded into onboarding Appendix B. Short TTL 2026-09-15 (active version skew). stage-2 pending. Source: Herald (submitted), Schliemann (original observation), Callimachus (filer)
- [`members-array-edit-honored-mid-session.md`](references/members-array-edit-honored-mid-session.md) -- **Canonical substrate property (registration stage). Three-way sibling with wake-mechanism + inbox-slot-asymmetry.** The harness honors plain JSON file edits to `~/.claude/teams/<team>/config.json` `members[]` on the next `SendMessage` validation; no restart, no `TeamCreate`/`TeamDelete` dance, no special API required. Mid-session ghost-member registration is **O(file-edit)**. **Verified n=2 cross-substrate 2026-05-14**: FR on Windows-Git-Bash (Aen, ~09:42) + apex-research on Linux/Docker (Schliemann, ~09:48), independently, parallel within a six-minute window. Closes OQ #2 of `ghost-member-as-universal-integration-surface.md` (member-list cache window). Carries amendment lineage table (v1 S31 → v2 ~09:42 → v3 ~09:46) -- first FR entry where the lineage axis is **[DISPUTE]** (each step finds the prior claim substantively wrong, not narrower-scope-wrong); descriptive at n=1, watch for second dispute-lineage entry to consider promotion of the representation. **2026-05-19: third-leg sibling filed** (`inbox-slot-vs-members-validation-asymmetry.md`, Brunel S31 lifecycle-asymmetry). Architectural-fact (revision trigger = Anthropic adds caching or removes file-edit honor, not n+1 sightings). TTL: 2026-11-14. Sources: Aeneas (FR team-lead), Callimachus (filer); joint observation with apex-research/Schliemann
- [`teams-substrate-2.1.179-implicit-teams.md`](references/teams-substrate-2.1.179-implicit-teams.md) -- **Version-stamped substrate sheet, CLI 2.1.178/2.1.179 "implicit teams" (Hopper + Brunel, teams-migration probe 2026-06-17; committed `b37b938`).** After `TeamCreate`/`TeamDelete` were stripped, teams are implicit: a lone authenticated session is already a 1-member self-led team (`config.json` written EAGERLY before any spawn; `inboxes/` created LAZILY). **On-disk team name = `session-<id>`** (random per session; Agent-tool `team_name` IGNORED on disk -- agent chat may CLAIM a name, filesystem is authoritative) but **discoverable** (glob `teams/*/config.json` `.name` or `sessions/<pid>.json` `sessionId`). `sessions/<pid>.json` = live session registry (pid→sessionId→status). **members[] injection (P4) + external inbox-file-write wake/deliver to a bare lone session (P5/P6) BOTH survive 2.1.178+** (external write proactively wakes ~15s once the inbox file exists; courier must create it first). Inbox entries gain a `"type"` field on 2.1.179. Migration verdict: only the hardcoded team-name path breaks -> `decisions/courier-must-runtime-discover-team-name.md`. Next version datapoint in the inbox-substrate-property family (after the 2.1.170 sheet + 2.1.173 datapoint). Version-coupled; revision trigger = CLI version change, n+1 on 2.1.179 doesn't strengthen. TTL 2026-09-17. Curated pointer to the probe-findings doc. stage-2 confirmed (empirical probe + same-session hook proofs). Source: Hopper + Brunel (probe), Callimachus (filer)
- [`claude-code-hook-wake-primitives.md`](references/claude-code-hook-wake-primitives.md) -- **Verified hook wake/inject primitives, CLI 2.1.178/2.1.179 (Hopper + Brunel, proven empirically 2026-06-17).** The in-harness complement to inbox-file-write wake. `UserPromptSubmit` and `Stop` hooks inject `additionalContext` into the live conversation; the `Stop` hook fires **content-agnostically on every turn-end** and can **block + inject** to wake the model and force another turn. Harness caps **consecutive** Stop-hook blocks at **9** (`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`); correct discipline is to check `stop_hook_active` and yield rather than count up to the cap. `asyncRewake: true` is an OFFICIAL setting -- a background hook wakes the model on exit-2 (the supported async re-engagement path, doesn't consume the block budget). Two wake substrates: external inbox-file-write (courier) + hooks (turn-end / prompt-submit / background exit-2); injected `additionalContext` = turn-end analogue of the bootstrap-preamble in-band signal channel, a good vector for a world-state-on-wake snapshot. Version-coupled; revision trigger = CLI change to hook lifecycle / cap / `stop_hook_active` / `asyncRewake`. TTL 2026-09-17. stage-2 confirmed (empirically proven this session). Source: Hopper + Brunel, Callimachus (filer)
- [`inbox-slot-vs-members-validation-asymmetry.md`](references/inbox-slot-vs-members-validation-asymmetry.md) -- **Canonical substrate property (lifecycle-asymmetry stage). Third-leg sibling to wake-mechanism + members-array-edit.** The read-path validation gating dispatch (sender-side `members[]` check) and the write-path that persists inbox files on disk are **decoupled**. An inbox file at `~/.claude/teams/<team>/inboxes/<name>.json` can exist and be written-to even when `<name>` is NOT in `members[]`. ACL is **one-sided at the write layer** -- harness validates dispatch authority but does not GC inbox files on member removal. Empirical basis: apex `hammurabi.json` residual-orphan inbox (5 active members + 1 removed-but-persistent inbox; Brunel S31 substrate probe Linux/Docker 2026-05-12). Three operational implications: orphan inbox files are normal substrate state; re-adding removed member surfaces stale messages; external-CLI consumers must not infer member-active state from inbox-file presence (substrate-invariant-mismatch defect). The three-way substrate-property family articulates: dispatch (members[]-gated) + wake (file-write-gated) + lifecycle (inbox-file persistence outlives members[] entry). Architectural-fact (revision trigger = harness adds GC sweep or couples membership-to-inbox-lifecycle, not n+1 sightings). TTL: 2026-11-19. Source: Brunel

### process/

- [`protocol-c-graduation-path.md`](process/protocol-c-graduation-path.md) -- First proven Protocol C cycle: cluster identification → gate-mapping synthesis → proposal → review → L1 law. Commit `589fda9`. Precedent for future promotions
- [`two-stage-adoption-for-org-standards.md`](process/two-stage-adoption-for-org-standards.md) -- Three-stage workflow for adopting org-wide standards when the publisher lacks create-permission in the canonical destination: Stage 0 (proposal in own space + banner), Stage 1 (escalation issue to canonical-space owner), Stage 2 (final move + banner removed). Decouples authoring / ownership-transfer / authority. First instance: EVR sisene konteinerite standard (session 22, Confluence page 1713864752 + TPS-583). Pairs with create-perm-as-404 gotcha and `[speculative]` marker convention. Confidence: medium (n=1 deliberate, substrate-relevant). Source: team-lead
- [`soft-verdict-discipline-on-substrate-mapping-briefs.md`](process/soft-verdict-discipline-on-substrate-mapping-briefs.md) -- When PO framing is in flight and team-lead asks for a substrate map (not an architecture pick), the deliverable is a table of N candidate options × {accommodates without modification / accommodates with additions only / requires replacement} with concrete code-cost notation per cell -- not a "recommended" verdict. Structurally separates substrate facts from architecture choice; the substrate's accommodation profile is the same regardless of which shape gets picked. Includes "convert soft-verdict to verdict" rule (file follow-up, do NOT retrofit map). First instance: Finn's mid-session reframe of Q7 during #64 thinktank, after PO walked back event-driven framing. n=1 watch posture. Source: Finn (#64)
- [`cross-repo-glance-confirm-citation-before-inheritance.md`](process/cross-repo-glance-confirm-citation-before-inheritance.md) -- When a stakeholder cites a pattern as battle-proof at repo X and the team is about to design downstream work assuming inheritance, run a 30-60 minute read-only glance (clone + grep + skim canonical docs) before continuing. Output: 3-bullet top-line + source paths checked + quotes-with-file:line + unattributed-claims list. Cheap verification with asymmetric payoff: confirms move forward or reframes downstream work before assumptions accumulate. First instance: Finn's polyphony-dev (no-match) + Haapsalu-Suvekool (confirm) double-glance during #64 thinktank -- ~90min Finn-time avoided week+ of misdirected phase-A. n=1 watch posture. Source: Finn (#64)
- [`stage-2-feedback-typology.md`](process/stage-2-feedback-typology.md) -- **Joint Callimachus + Volta.** Catalogs five empirical shapes Stage 2 read-back responses take when joint authors review a Protocol A Stage-1-honest-fold filing. **Discriminator question (Volta-canonical, single-question test)**: does the proposal preserve the existing claim-base or extend it? Preserve → fold-discipline; extend → defer-discipline per `cluster-decomposition-meta-principle.md` methodology corollary (do not lean harder at n=1). **Five shapes (n=7 instances within S36 alone)**: Shape-A **pure renaming-of-cleavage** (n=3 in S36: Volta C1 Surface 3, Aen 2.7 Surface 3, Hopper 2.5 Refinement 2 -- fold immediately); Shape-B **forward-claim-extension defer** (n=2 in S36: Brunel C1 lean-harder + Brunel C4 structural-detector -- defer to n=2 of meta-principle-instances); Shape-C **mechanism-sharpening-within-claim-base** (Volta-contribution n=1 in S36: Volta C4 Surface 4 -- preserves claim-base AND extends mechanism explanation; fold immediately with mechanism-labeling); Shape-D **factual-correction-on-inferred-content-where-FLAG-invited-correction** (n=1 in S36: Volta 2.5 Sub-Instance 1 -- distinct epistemic layer from A/B/C/E; Volta-refined discriminator "verify vs evaluate framing"; fold immediately + strengthens evidence base because corrected content becomes first-author-verified); Shape-E **axis-distinction discipline-check** (Herald-contribution n=1 in S36: Herald 2.5 Sub-Instance 2 -- cross-entry framing-coherence; fold immediately with cross-reference update). **Two distinct epistemic layers** (Volta-LEARNED): substantive-content verification (Shape-D; `[FLAG]`-invited; "are the facts right?") precedes framing-quality (A/B/C/E; "is the framing of verified facts right?"). Routing table at five-shape × claim-base × mechanism × locus × fold-discipline. **Joint-cycle structural-yield hypothesis** (Volta sketch-grade): joint Stage 2 cycles may have higher structural yield than solo because within-system self-applications more visible at n=2 authors. Falsifiable at n=2 comparison. Forward-watchpoints: Shape-F "proceeding-to-as-gate-slip" (S37 in-vivo); Shape-G "L4-hold-as-response" (sketch-grade from C1 cycle); cross-team confirmation distinguishes FR-discipline-culture vs framework-invariant. Confidence medium-high (n=5 shapes × n=7 instances within S36); cross-cycle n=2 promotes to high. Sources: Callimachus (typology emerging during C1 cycle), Volta (single-question discriminator + Shape-C contribution + two-epistemic-layer distinction)
- [`stage-2-cycle-yield-narrowing-to-read-back-phase.md`](process/stage-2-cycle-yield-narrowing-to-read-back-phase.md) -- **Joint Callimachus + Aen.** Empirical narrowing of Volta's S36 sketch-grade hypothesis (joint Stage 2 read-back cycles have higher structural yield than solo): the yield differential is **at the read-back phase, NOT the drafting phase**. Stage 1 honest-fold is solo-author work even on joint-source material; joint-author contribution shows up at Stage 2 read-back where multiple authors' vantages interact with the drafted artifact. **n=5 cumulative confirmation without counter-instance across S36-S37**: C4 (Brunel+Volta), 2.5 (Volta+Herald+Brunel+Hopper 4-author), 2.6 amendment (Brunel+Hopper+Volta), Recursive-Narrowing (Herald+Brunel+Finn), 2.7 (Hopper+Brunel+Aen) -- all five solo-author drafted clean from committed scratchpad/ops-log/Aen-relay sources. Discriminator question: **is the source-of-truth committed to scratchpad/ops-log/direct-DM artifact at drafting time?** YES → solo-author Stage 1; NO → surface-back or wait for commit. **Operational consequence**: drafting strategy is solo-author against committed sources; coordination overhead in drafting is wasted unless source-of-truth is not yet committed. Joint-author yield is collected AT Stage 2 (Shape-A/B/C/D/E per typology sibling entry), not DURING drafting. **Three load-bearing reasons** for solo drafting: Stage 1 is fundamentally a source-reading operation (no duplication-yield); FLAG annotations resolve mid-draft uncertainty (reserves slot for Stage 2); joint-author cohort interaction is structurally distinct from joint-author drafting (different epistemic modes). **Cross-cycle observation pattern**: within-system self-applications also surface as sub-observations within Stage 2 cycles, NOT Stage 1 -- consistent with the narrowing. Composes with `stage-2-feedback-typology.md` (sibling-process-discipline; typology operationalizes Stage 2 phase that S6 narrowing isolates) + `relay-to-primary-artifact-fidelity-discipline.md` (the lifecycle this entry operates within). Confidence medium-high (n=5 cumulative, no counter-instance); falsifiability via committed-source joint-entry requiring drafting coordination; cross-team confirmation distinguishes FR-culture vs framework-invariant. Sources: Callimachus (empirical narrowing across drafting cycles), Aen (ratification at S36 15:20+15:26 as "strong meta-observation")
- [`companion-pair-submission-protocol.md`](process/companion-pair-submission-protocol.md) -- **Cross-pollinated from apex-research.** Submission shape for discoveries with two distinct views that belong as cross-linked-but-separate wiki entries (not bundled, not "see also" footnoted). Two preconditions (BOTH required): audience-/format-split + bidirectional cross-link load-bearing. Five pair-shape axes from apex-research (notation/spec, prose/code-block, status/companion-artifact, mechanism/UI-trap, convention/violation). FR has **n=2 latent instances force-folded** before the protocol existed: (1) `gotchas/teamcreate-in-memory-leadership-survives-clear` ↔ `patterns/repo-as-durable-store-teamdelete-as-release-primitive` (status/companion-artifact axis); (2) `patterns/tmux-pane-border-format-for-teams` ↔ `gotchas/tmux-pane-labels-decoupled-from-personas` (mechanism/UI-trap axis). FR adopts apex-research's submitter-declared form, retaining curator-decompose discipline as the fallback. Naming-collision note: within FR, refer to as **Companion-Pair Submission**, not "Protocol C" (FR's letter-slot is taken by Knowledge Promotion). n=2 cross-team co-discovery; first FR submitter-declared instance pending. Sources: Schliemann + Eratosthenes (apex-research); FR latent instances curator-recognized 2026-05-07
- [`stage-2-confirms-filing-gate.md`](process/stage-2-confirms-filing-gate.md) -- The Stage-2-confirms filing gate (#70): "confirmed = ALL co-authors have read the entry back." Each wiki card carries a `stage-2` field (`confirmed`/`partial`/`pending`); the three-bucket assignment rule (single-source + architectural-fact = confirmed; multi-author or filed-on-behalf without documented read-back = pending; joint entries advance pending→partial→confirmed as read-backs land). Defines the gate that all card INDEX headers audit via `grep -rl 'stage-2: pending'`. Source: Callimachus (gate definition); Finn + Herald #70 co-conveners. *(Backfilled to this list 2026-06-12 -- entry was carded since S40 but missing from the main-index entries list; index-drift reconciliation.)*
- [`control-signal-semantics-at-authority-boundaries.md`](process/control-signal-semantics-at-authority-boundaries.md) -- **PO-formulated coordination discipline (Mihkel, S60; Aen-submitted). One mechanism, three loci.** An agent (or coordinator relaying a principal's words) executes control language **literally and eagerly** -- imprecise control language resolves toward *more* action, *sooner*, *more destructively* than intended. **Sub-lesson 1 -- musing is not commission (reading input)**: "we could / maybe / I want to think about" from the principal is thinking-out-loud → discuss or record, never spawn/task; only explicit direction starts a lane (instance S60: PO mused "we could use stationmaster with a dedicated station" → Aen tasked Herald a design lane off it). **Sub-lesson 2 -- stop is not revert (writing a stop's content)**: a stop means *cease*, never *destroy*; never bundle `git checkout`/cleanup/undo into a stop -- untouched uncommitted tree = decision *deferred* (cheap), reverted tree = work *lost* (instance S60: HOLD worded "checkpoint and stop... doc stays parked" → Herald reverted a finished `protocols.md` §7 edit + rewrote with banners = expensive multi-round retraction; plain "cease" would have preserved it). **Sub-lesson 3 -- control acts at boundaries, not mid-flight (timing a stop)**: "stop" = "don't start the next thing," never "reach into the running thing"; let in-flight work reach its end and reroute at the seam. Companions: racing a control message to a working agent is how work dies (prefer letting an agent finish over mid-flight redirection -- control-message case of timestamp-crossed-messages); a slow-down changes HOW you operate, not another thing to execute at max speed (instance S60: PO said "please don't rush" → Aen *immediately* fired a HOLD that raced Herald's already-completed work). Confidence high (PO directive, not hypothesis); n=1 empirical per sub-lesson (single S60 session, all three in sequence). stage-2: **confirmed** (Aen submitter read-back 2026-07-14; Herald cross-credited as sub-lesson-2 witness, authorship PO(Mihkel)->Aen). Promotion-bound to the po-team design (`designs/new/po-team/`, Herald+Celes carrying); Protocol C common-prompt promotion is the follow-on. Sources: Aen (submitted), Herald (failure-instance co-author); PO-originated (Mihkel)
- [`standby-agent-fix-then-flag-discipline.md`](process/standby-agent-fix-then-flag-discipline.md) -- **Operational discipline, two clauses on one axis (Brunel, S50; Aen-confirmed).** Axis: *what may an agent touch on behalf of work someone else owns, and under what conditions?* Governing principle for both -- **act on the coordination surface freely; touch the owned artifact only under tight, named conditions.** **Clause A (modify an accepted artifact): fix-then-flag.** Default is surface-before-fix; the bounded exception lets a standby agent fix-then-flag ONLY when ALL FOUR hold -- (1) live deployment + time-pressure window; (2) MAY-WRITE domain, bounded, no protocol/design surface; (3) immediate flag with reasoning + verification evidence (owner vetoes retroactively); (4) explicitly on standby for this failure class. Remove any -- especially (1)/(2) -- and it reverts. The four conditions ARE the guards against silent-broadening. Incident: S50 `ssh-keygen -A -f` entrypoint bug fix (commit `f022fed`), Aen ratified 17:03. **Clause B (create an artifact ahead of its owner): staging-on-behalf scope.** Stage only the coordination/dispatch layer ahead of a soon-to-spawn owner, NOT the owned artifact (risks duplicate-by-collision). Incident: Brunel staged a deploy dispatch package (coordination -- value, caught version-skew bugs) AND a T6.a harness (owned -- collision; Hopper independently built the better one, Brunel's queued for `git rm`); Aen confirmed generalization 17:08. Designer-side counterpart to Hopper's operator-side hard-gate; bounded exception to common-prompt surface-before-fix (`playbooks/verify-structural-change.md`), does NOT weaken it. **Protocol C candidate at n≥2** (each clause accrues its own count); n=1-per-clause filing-grade watch-posture, confidence medium. stage-2 CONFIRMED (Brunel read-back 2026-06-12; faithful, source-files narrowed to per-clause artifacts `entrypoint.sh`/`t6a-race-harness.py`). Source: Brunel (submitted), Callimachus (filer)

- [`within-entry-class-split-observed-genus-designed-mechanism.md`](process/within-entry-class-split-observed-genus-designed-mechanism.md) -- **Curation convention: when a wiki entry may carry BOTH entry classes at once (Callimachus + Aen, S67).** An entry normally carries one class -- observation-based (n+1 sightings raise confidence) or architectural-fact (only a substrate change revises it). A few legitimately carry both, applied to different claims **within the same entry**: an **observed genus** plus **the designed-substrate mechanism that explains one instance of it**. **Recognition trigger (both required)**: (1) a recurring failure shape established by sightings, whose intentionality isn't in question because nobody designed it; (2) a claim about how a deliberately-built system behaves (API contract, protocol ordering semantics, vendor posture). **The real test is whether the entry's claims have DIFFERENT revision triggers** -- if more sightings would legitimately strengthen one and legitimately do nothing for the other, the split is real; one trigger covering everything means file it one class. **Anti-trigger**: don't reach for this on any entry that merely mentions a designed system (most do) -- ask *"would n+1 sightings promote this entry as a whole, even though one claim cannot be promoted that way?"* **Why one class per entry fails here** (Aen): all-observation-based makes the designed sub-claim promotable by sightings, the exact error the architectural-fact convention prevents; all-architectural-fact strips the genus of dedup-as-confirmation, its legitimate promotion path. **Filing rules**: state the split explicitly; scope the Revision-trigger section to the designed sub-claim and say so in the heading; **pin `confidence` to the weakest load-bearing claim, not an average**; pin any hold in all three places (entry, card, subdir INDEX) because a hold recorded once erodes. Instance 1 = `gotchas/verification-narrower-than-it-appears` (genus at n=3/2 agents + the `-R` mechanism as deliberate OpenSSH design). **n=1, filed as a named convention rather than a one-off because a precedent whose second instance nobody can recognise is a one-off wearing a convention's clothes** (Aen). stage-2 CONFIRMED. Sources: callimachus (convention + instance), aen (endorsement + the recognition-trigger requirement)

### findings/

- [`non-persistent-overlay-host-disqualified-as-shared-substrate.md`](findings/non-persistent-overlay-host-disqualified-as-shared-substrate.md) -- **FIRST `findings/` ENTRY. A non-persistent host reached over an overlay network is disqualified as a shared deployment substrate (Hopper, S67 queue). Migration target: `topics/11-deployment-lifecycle.md`.** Not only because it can be powered off, but because it can **idle-suspend under active remote load via a power policy the obvious OS knobs do not govern**, presenting to every remote consumer as a repeating host outage. **The trap is that the fix looks done**: you disable the visible idle-suspend setting, the box keeps sleeping, and diagnosis misroutes toward power hardware or network. Worked example RC host `100.96.54.170`: three hypotheses fell in sequence, each on evidence -- power fault REJECTED (boot count unbroken, apex `RestartCount=0`, no reboot); network/WARP flap REJECTED as the mechanism (journal showed a multi-minute TOTAL FREEZE, not a live-but-unreachable box); **s2idle SUSPEND CONFIRMED** (`/sys/power/suspend_stats/success` incremented live 3 -> 4 -- the kernel's own counter -- with `mem_sleep=[s2idle]`, a `suspend.target` fingerprint at the drop timestamp, and RAM-preserved resume). **Root cause = headless + greeter scope**: monitors removed, so nobody logs into a graphical session and the box sits at the **GDM greeter on `seat0`** while all work arrives as **seatless SSH**; the greeter judges idle on local seat input, which headless never provides and SSH does not reset, so the idle timer runs to completion and suspends **under active remote load**. **The scope trap: `org.gnome.settings-daemon.plugins.power sleep-inactive-ac-type = nothing` is the USER SESSION, not the greeter** -- checking it looks like the fix and changes nothing. **Fix blocks the mechanism, not the trigger**: `systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target` works regardless of which idle scope triggered it, so you needn't name the trigger correctly to end it; **verify on the mechanism's own counter going quiet** (held flat at 4 for 35 min spanning 2-3 would-be cycles), not on the mask command succeeding. **Plausible-but-WRONG lead: the removed display cable / EDID** -- the suspend is idle-timer-driven, not EDID-driven. **Framework consequence: never pin persistent shared workloads to a host that (a) has a human power button and no auto-recovery, (b) sits behind a flapping overlay, or (c) runs a desktop session stack that idle-suspends -- especially headless.** Genus link on the availability axis: the *symptom* cannot distinguish suspend / power-off / network drop (`verification-narrower-than-it-appears`) and the *fix* appears total but misses the greeter scope (`control-narrower-than-its-name`); **both firing on one incident is why this is a finding rather than a third gotcha** -- the durable output is a substrate-selection criterion. stage-2 pending -- filed on behalf, Hopper not spawned; full text read from his preserved scratch file, not reconstructed. Source: hopper (primary), callimachus (filer)

(*FR:Callimachus*)
