# Sensitive research -- in-company public but treated as confidential per subject
teams/*/sensitive/

# Per-machine Claude-CLI permission allowlist (paths and grants are machine-local)
.claude/settings.local.json
.DS_Store
