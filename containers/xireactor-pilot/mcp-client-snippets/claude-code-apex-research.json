{
  "_comment": "apex-research tenant — MCP stdio config snippet. For apex-research agents running INSIDE the apex-research container on RC, XIREACTOR_API_URL can use docker DNS (http://xireactor-api:8010) if the apex-research container joins the xireactor-pilot_default network. Otherwise use the Tailscale host loopback URL below.",
  "mcpServers": {
    "xireactor-apex": {
      "command": "python",
      "args": [
        "-m",
        "xireactor_mcp.server"
      ],
      "env": {
        "XIREACTOR_API_URL": "http://100.96.54.170:8010",
        "XIREACTOR_BEARER_TOKEN": "<APEX_BEARER_TOKEN>",
        "XIREACTOR_ORG_ID": "apex-research"
      }
    }
  }
}
